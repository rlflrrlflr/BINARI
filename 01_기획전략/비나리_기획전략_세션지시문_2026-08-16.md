# 세션별 지시문 (붙여넣기용) — 2026-08-16

**쓰는 법**: 이미 열려 있는 전략·로직·디자인·데이터 세션에 해당 블록을 통째로 붙여넣는다.
**정본**: 판정 근거는 `방향점검_2026-08-15`, 배분은 `작업배분_2026-08-15 §6`. 이 파일은 그 둘을 세션이 바로 먹을 수 있는 모양으로 자른 것이다.
**첫 줄이 왜 재기반인가**: 이 리포는 여러 세션이 병렬로 커밋한다. 8/14 하루에 두 세션이 각각 110커밋·50커밋 뒤처진 코드를 최신으로 알고 결론을 냈다. 이어 쓰는 세션일수록 먼저 당겨야 한다.
**순서**: 로직 A(비용 누수) → 데이터 1 → 로직 B(곁 탭) → 전략/디자인. 돈이 새는 자리가 맨 앞이다.

---

## ① 로직 세션

```
먼저 git fetch origin main 하고 브랜치가 뒤처졌으면 코드 읽기 전에 재기반해.
이 리포는 여러 세션이 병렬로 커밋해서 지금 네가 아는 버전이 최신이 아닐 수 있다.

근거 문서: 01_기획전략/비나리_기획전략_방향점검_2026-08-15.md (§1-1, §1-8, §1-9)
배분 정본: 01_기획전략/비나리_기획전략_작업배분_2026-08-15.md §6-2, §6-4

【A. 지금 새고 있는 것 — 먼저】

A-0. tier 비용 누수 봉합 (api/judge.js)
  tier를 클라이언트가 보내는데 Origin 검사는 스크립트로 위조 가능하다.
  "결제 후 문제"로 분류돼 있었지만 지금도 유료 모델 경로가 열려 있다.
  결제 배선 전까지 서버에서 tierKey="free" 고정(한 줄), 또는 Vercel에
  BINARI_MODEL_PAID가 미설정인지 확인하고 근거를 작업로그에 남겨.

A-1. privacy.html 배너 문구
  내부 TODO("변호사 검토를 받고")가 라이브에서 유저에게 보인다.
  법률 자문 전까지 쓸 임시 한 줄로 교체.

A-2. 궁합 엔진 버그 셋
  축⑨ v가 상수다(same ? 1 : 1). 축⑥ 문구가 조건보다 더 말한다.
  셋째는 app/작업지시_역할과초대_2026-08-15.md §B-0-b 참조.

A-3. eval 실측에서 나온 진짜 실패 둘 (248건 전량 실행 결과, 방향점검 §1-9)
  · direction=undefined 1건(P1-Q19): 파싱 후 direction이 없으면 재요청하거나
    HOLD 폴백하는 가드. 지금은 방향 없는 판결이 그대로 화면에 나간다.
  · 용어·은유 누출 10건(4.0%): 앞면에 "괘야"·"대운이 터를"·"임오 대운",
    근거에 "물 고이는 시기"·"문이 열려" 같은 은유 서술어가 샜다.
    앞면 맨말 원칙을 프롬프트에서 더 못 박아.

A-4. 평가 하네스 갱신 둘 (app/eval/run-eval.mjs) — 안 고치면 다음 실행도 오탐 4건 재생산
  · :58 TODAY가 옛 시계 형식("N시")인데 앱은 v132.10~11부터 시진("축시(토)")을 보낸다.
  · :131 동률 기대가 go>=stop?"GO"인데, v132.5 규칙은 "표가 다수를 못 만들면
    모델이 낸 방향 유지"다. 지금 동률 3건이 불일치로 잘못 잡힌다.
  · --pids 페르소나 필터는 이미 있다(커밋 88d2724).

【B. 곁 탭 — "아무 기능 없음" 해소】

지금 곁 탭은 "보이는 것"만 있고 "할 수 있는 것"이 0이다.
곁 렌더는 ?gyeot=1~3 디버그로만 채워지고, MATCH_LAST_KEY가 한 칸 덮어쓰기라
명부 자체가 없다. 라이브 탭엔 "누가 서게 되면 이 자리에..." 문구뿐이다.
설계 정본: 03_비주얼프로토타입/비나리_비주얼프로토타입_곁탭IA_v01.md §6
창업자 결정 1 = 절충안(궁합 본 사람을 '답 대기'로 세운다).

B-1. 곁 명부 — binari.gyeot 배열(~50줄). 궁합 돌린 상대를 '답 대기'로 적재.
B-2. 곁 탭 2층 — 명부 렌더(흐린 자리) + 비었을 때 "궁합 보러 가기" CTA.
     빈 옆자리에 탈출구가 없는 게 지금 제일 큰 구멍이다.
B-3. 곁 렌더를 명부에 연결(gyeotRef ← 명부, ?gyeot 디버그는 유지).
B-4. 초대·회신(정식 자리 승격)은 손대지 마 — 배분표 순서대로 분모 100 이후다.

서버 불필요. 카피 톤("답 대기"라는 말 자체)은 디자인 세션 몫이니 자리만 잡아둬.

작업 후 작업로그·변경이력 갱신하고 지정 브랜치에 커밋·푸시. main은 지시 있을 때만.
```

---

## ② 데이터 세션

```
먼저 git fetch origin main 하고 뒤처졌으면 재기반해. 이 리포는 여러 세션이 병렬로 커밋한다.

근거 문서: 01_기획전략/비나리_기획전략_방향점검_2026-08-15.md §1-6
배분 정본: 01_기획전략/비나리_기획전략_작업배분_2026-08-15.md §6-1

【먼저 알아야 할 것 — 실측 규칙】
모든 수치는 이중 필터로 산출한다:
  is_internal ≠ true  AND  person_id NOT IN COHORT 436757 (계측 분리 이전 내부 4명)
is_internal만 걸면 코호트 안의 내부 헤비유저(666·614이벤트)가 외부로 집계돼서
공유·리텐션 수치가 통째로 틀린다. 실제로 8/15에 그 오류로 결론을 냈다가 8/16에 뒤집었다.
이중 필터 기준 현재 실측(45일): 판결 68회/12명 · 공유 발신 0 · 카드 저장 0.

1. 계측 오염 마감
   · 팀 5명 전원 ?i=1 처리 — 코호트·플래그 둘 다 밖인 내부인이 아직 있다.
     시금석: tab_switched 62회/2명(내부 테스트 의심).
   · 앞으로 손 실측은 전부 이중 필터로.

2. 리텐션 자산 3종(아침문안·판결록·스트릭) 계측 부착
   지금 track() 호출이 0건이라 D7 게이트가 "왜 다시 왔는가"를 못 읽는다.
   D7 창 정의도 못 박아(정확히 Day7인지 7일 내인지).

3. 판결 품질 사람 채점 (창업자와 함께)
   시트: "비나리 판결 채점 (248건 · 2026-08-16)" — 248문항, 담당 5명 배분됨.
   점수(1~5)·메모를 채우고, 코드 열(P1-Q01 형식)을 키로 app/eval/verdicts.csv에 병합.
   자동검사는 대놓고 틀린 것만 잡는다. 꽂히는지는 사람만 안다.

4. 루프 지표 재산출(이중 필터로) · q_anon 분포 분석(창업자 결정 3 이행) ·
   imprint/match 클릭률은 offer_shown이 생긴 v128 이후 창으로만 산출.

5. 게이트 대시보드 산식 갱신
   · e67ieZvV가 letter_clicked/verdict_shown인데 실제는 2단 fake-door
     (price_shown→intent_confirmed)다. 96% 전환은 오염이 아니라 확인 버튼이
     무료 콘텐츠 관문이라서 나온 수치다.
   · HANDOVER의 "이벤트 19종"을 실제 인벤토리(~58종)로 갱신.
```

---

## ③ 전략 세션

```
먼저 git fetch origin main 하고 뒤처졌으면 재기반해. 이 리포는 여러 세션이 병렬로 커밋한다.

근거 문서: 01_기획전략/비나리_기획전략_방향점검_2026-08-15.md
배분 정본: 01_기획전략/비나리_기획전략_작업배분_2026-08-15.md §6-3

【이 표 전체의 선행 조건 — 창업자 결정 회수】
 ① 광고 예산
 ② 예창패/사업자 (20일 대기 — G0-b 시계가 여기 멈춰 있다)
 ③ 커밋 이력 PII 재작성 여부 (46ca39d·6b84fe8 — 결정필요사항에 미등재라 도달 경로부터 없다)
 ④ 서신 fake-door 무료지급 코호트 처리

【문서 정정 — 방치하면 새 세션이 낡은 걸 근거로 역행한다】
· 바이럴루프판단 §1 실측 정정: 공유 5건·k≈0.14는 코호트 미적용 산출이라 전부
  내부 행동이었다. 이중 필터 실측(판결 68회/12명 · 공유 발신 0)으로 교체하고,
  결론 문장 "병목은 발신"을 "발신 0"으로 고쳐 — 루프는 오염된 게 아니라 시동이 안 걸렸다.
· CLAUDE.md 헌장 계측 조항이 v124.3 가명처리 결정과 모순.
· CLAUDE.md §새 후보의 "App.jsx:1977 구현" 기재가 v128.1 MBTI·가치여정 제거와 모순.
· 지표포트폴리오설계_v01(MBTI·가치 "유지" 기재), 루프배관 §1-5(완료 미반영) 갱신.
· 개발 로그 이원화 봉합(app/작업로그.md → 정본 합류), ROADMAP 15↔68행 모순.

【판단이 필요한 것】
· 판별 기준 분모 통일 — 생일 제출 40%(분모=유입) vs 50%(분모=opens),
  공유율 15% vs 8%가 정본 두 곳에 병존. 분자·분모를 이벤트명으로 못 박아.
· 카드발 유입 귀속 방식 결정(QR / 짧은 주소 / 간접 추정 수용). 지금은 전부 direct로 섞인다.
· 4,900 실마진 표(부가세·PG·API 원가) — 8/2 지적 후 여전히 0건.
· 판결문 45자 제한 재판단 — eval 248건에서 길이초과 14건이 전부 "구체성을 채우다
  넘친" 사례다. 제한을 지키게 할지 늘릴지, 판결문 형식 보존 헌장과 같이 판단해.

전략 판단이 오가면 전략로그.md에 append(요청·판단 근거·기각된 선택지·결론).
```

---

## ④ 디자인 세션

```
먼저 git fetch origin main 하고 뒤처졌으면 재기반해. 이 리포는 여러 세션이 병렬로 커밋한다.

1. 곁 탭 카피 — 빈 상태 문구와 '답 대기'라는 말 자체의 톤.
   정본 어휘: 03_비주얼프로토타입/비나리_비주얼프로토타입_곁탭IA_v01.md §3
   (자리 잡는 건 로직 세션이 한다. 말만 정하면 된다.)

2. 4,900원 리포트에 상품명이 없다 — 경쟁사는 '청월아씨 정통사주'처럼 이름이 붙는다.
   문구만 바꾸는 일이라 페르소나 헌장 위반이 아니다. 근거: 한국본진실행계획 §2-6

【금지선 — 헌장】
신규 캐릭터 제작·수호신 비주얼 교체·정령 시각요소 부활은 금지.
톤은 판돈 축(속결=정령 심드렁 / 의식=수호신 현행)으로만 가르고, 바꿀 건 문구뿐이다.
```

---

## 지금 사람이 해야 하는 것 (세션이 못 하는 일)

| 무엇 | 누가 | 왜 막혀 있나 |
|---|---|---|
| 광고 예산 결정 | 창업자 | G0-b(분모 100)가 여기서 20일째 멈춤 |
| 예창패/사업자 | 창업자 | 결제·정산 배선의 선행 조건 |
| 소재 12개 제작 | 팀(마케터 5인) | 획득은 팀의 홈그라운드인데 이 레인만 공급 0 |
| 248건 판결 채점 | 전원(50건씩) | 자동검사는 "틀린 것"만 잡는다. "꽂히나"는 사람만 안다 |

> **사슬**: 결정 1·2(사람) → 소재 12개(팀) → 집행 → 분모 100 → 그때 D7·가격·루프 전부.
> 이 사슬에서 **코드가 막고 있는 건 없다.** 첫 고리만 사람이다.
