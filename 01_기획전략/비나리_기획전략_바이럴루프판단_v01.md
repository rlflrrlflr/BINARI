# 바이럴 루프 판단 v01 — 전략·로직 기록

작성 2026-08-15 · 이 문서는 **전략·로직·정책 판단만** 담는다. 시각 설계는 `03_비주얼프로토타입/`으로 간다.
근거: PostHog 45일 **이중 필터**(`is_internal`≠true **+ 코호트 436757 제외** — 게이트 대시보드와 같은 정의) · `app/src/lib/imprint.js`(sky9) · `app/src/lib/sky.js` · 팀피드백트리아지 · 경쟁사분석

> **[2026-08-17 정정 — §1 전면 교체]** 초판 §1 수치(판결 101회/14명 · 공유 5건/2명 · k≈0.14)는 `is_internal` **단일** 필터 산출이라 **코호트 436757(계측 분리 이전 내부)이 안 빠져 있었다.** 공유 5건은 전원 그 코호트의 헤비 person 2인(666·614 이벤트) 것이다.
> **틀린 것은 계측이 아니라 이 문서의 손 실측 절차다** — 게이트 대시보드 4종은 처음부터 이 코호트를 제외해 왔다. 이후 손 실측은 이중 필터를 강제한다(`app/HANDOVER-데이터계측-2026-07-25.md` §2-D 규칙 1).
> 정정에 따라 §4(판별 기준)·§5-4·§6 도 같이 손봤다.

---

## 1. 실측 — 루프는 오염된 게 아니라 **시동이 안 걸렸다** (외부 발신 0)

45일 · **이중 필터**(`is_internal`≠true **+ 코호트 436757 제외**) = 게이트 대시보드와 같은 정의.
아래는 `app/HANDOVER-데이터계측-2026-07-25.md` §2-D 규칙 1 의 기준선(2026-08-17)과 **같은 값**이다.

| 단계 | 외부 실측 (8/17) | 초판(코호트 미적용) |
|---|---|---|
| 판결 노출 `verdict_shown` | **69회 / 13명** | ~~101회 / 14명~~ |
| 생일 제출 `birth_submitted` | 13 / 13명 | — |
| **공유 발신** `verdict_shared` | **0건** | ~~5건 / 2명~~ |
| 카드 저장 `card_saved` (v130) | **0건** | — |
| 공유 유입 `shared_verdict_view` | 5회 / 4명 | 동일 |
| 유입 후 CTA `shared_cta` | 4회 / 3명 | 동일 |
| first-touch=`share` | `app_open` **7회 / 5명** → 온보딩 3회/2명 → **생일 제출 2회/2명** | ~~opens 7 → 3 → 2~~ (단위 미표기였다) |

**해석 — 병목이 아니라 부재다.** 초판의 공유 5건은 전원 코호트 436757의 헤비 person 2인 것이었다.
**외부 유저의 공유 발신은 45일간 0건**이고 카드 저장도 0건이다.
따라서 「병목은 발신」은 틀린 서술이다 — **발신은 막힌 게 아니라 일어난 적이 없다.**
공유 버튼이 판결 카드 최상단 금색 CTA라는 사실은 그대로이므로 **위치 가설은 여전히 기각**이다.

**k는 계산하지 않는다.** 분자가 0이면 나오는 건 낮은 계수가 아니라 **비율의 부재**다.

**수신 레그는 살아 있다.** 유입 5회/4명·CTA 4회/3명은 **내부가 보낸 링크를 외부가 연 것**이라 외부 신호로 유효하다.

> ⚠ **단위를 반드시 적는다 — 이 절이 회(evt)와 명(uniq)을 섞어 쓴 것이 게이트 분쟁의 원인이었다.**
> 초판은 표에 `5회 / 4명` 이라 옳게 적어 놓고 본문에서 "유입 5명"으로 읽었고, `app/작업지시_역할과초대_2026-08-15.md` 가 그 5를 「5명」으로 옮겨 갔다.
> `app/HANDOVER-데이터계측-2026-07-25.md` §2-C 가 이미 경고한 오류다 — **`app_open` 은 방문 수다. 사람 수가 아니다.**

> ⚠ **표본 한계 — 이 절의 수치는 전부 13명 위에서 잰 것이다.**
> 0건은 「유저가 안 보낸다」의 증거가 **아니다.** 게이트 8%를 적용해도 이 표본에서 기대되는 발신은 1건 안팎이라 **표본만으로도 0이 정상 범위**다.
> **이 절은 루프를 기각하는 근거로 쓸 수 없다.** 가르는 것은 **분모 100뿐**이다.

---

## 2. 정책 판단 — 결과 공개 범위 (창업자 결정 2026-08-15)

> "결과를 오픈해도 상관없을 것 같다. 귀인지도도 다 오픈되는데 그냥 하더라. 아니면 오픈 가능한 정보를 선별하든가."

**채택**: 결과 공개를 원칙적으로 허용한다. 단 **선별한다.**
선별 기준은 취향이 아니라 **역산 가능성**이다 — 아래가 이 문서의 가장 중요한 발견이다.

### ⚠️ 아홉 하늘 값은 몇 개만 모여도 생년월일이 복원된다

각 값은 서로 다른 주기의 나머지(remainder)다. 중국인의 나머지 정리 그대로, **주기가 서로 소일수록 조합의 유일성이 급격히 올라간다.**

| 값 | 주기 |
|---|---|
| 마야 촐킨(톤×날) | 260일 |
| 자바 웨톤(요일×파사란) | 35일 |
| 나크샤트라 | 27분할(달 위치) |
| 달 위상 | 약 29.5일 |
| 아칸 요일이름 | 7일 |
| 일본 혼메이세이 | 9년 |
| 납음(년 갑자) | 60년 |

- **촐킨 × 웨톤**만으로 LCM 1,820일 ≈ **약 5년 창에서 유일**하다. 나이대만 알면 생일이 좁혀진다.
- 여기에 **납음·혼메이세이**(연 주기)가 더해지면 **연도까지 특정**된다.
- 즉 "아홉 이름을 한 장에 다 싣는다"는 **생년월일을 공개하는 것과 실질적으로 같다.**

**따라서 정책**: 공개용 이미지 한 장에 실을 수 있는 파생 이름은 **최대 2개, 주기가 겹치는 조합은 금지**(촐킨+웨톤 동시 노출 불가). 가장 안전한 형태는 **한 장에 하나**다.
→ 시각 설계에서 "아홉 개를 한 장에 나열"하는 안은 이 이유로 기각한다.

*(이 판단은 계산 근거가 명확하지만, 실제 위험 크기는 표본·맥락에 따라 다르다. 법무 검토 대상이 아니라 **설계 기본값**으로 둔다 — 기본을 안전하게 두고, 열고 싶으면 그때 판단한다.)*

---

## 3. 궁합 종속성 해제

앞선 권고(궁합 초대 루프)는 **철회하지 않되 1순위에서 내린다.** 창업자 지적이 맞다 — 우리는 사주 한 축이 아니다.

| 루프 후보 | 완결에 필요한 사람 | 마찰 | 우리만 가능? |
|---|---|---|---|
| 궁합 초대 | **2명** | 상대 생일 요청 + 관계 노출 | 아니오(8개 중 5개사 보유) |
| 판결 자랑(부적·공유) | 1명 | 사적 고민 노출 · 맥락 필요 | 부분(판결 포맷) |
| **파생 이름 한 장** | **1명** | 거의 없음(내 것만 나감) | **예 — 9엔진 보유는 우리뿐** |
| 수호신 초상(소지품) | 1명 | 없음(자랑이 아니라 소지) | 예 |

**로직상 결론**: 루프의 1차 단위는 **1인 완결형**이어야 한다. 2인 완결형(궁합)은 1인 루프가 돌기 시작한 뒤 **2차 증폭**으로 붙인다.

---

## 4. 판별 기준 (미리 정한다)

| 관측 | 뜻 |
|---|---|
> **⚠ 산식 정본은 여기가 아니다.** 분자·분모·단위는 `app/HANDOVER-데이터계측-2026-07-25.md` **§2-D 규칙 4** 가 이벤트 이름으로 못 박는다. 값이 갈리면 §2-D 를 따른다.
> **⚠ 이 표는 G0-b(분모 100) 통과 후에만 판정한다.** 현행 외부 실측은 분자 0 / 분모 69회·13명이라 세 줄 전부 판정 불능이다.

| 관측 | 산식(정본 §2-D 규칙 4) | 뜻 |
|---|---|---|
| **총공유발신율**이 **15% 미만** (새 공유 단위 배포 후에만 판정) | (`verdict_shared` + `card_saved`) evt / `verdict_shown` evt | 발신 마찰이 물건 문제가 아니었다 → 루프 대신 광고 소재로 |
| **공유 1건당 유입(k)**이 **1.0 미만** | `shared_verdict_view` evt / `verdict_shared` evt | 받는 쪽 훅이 약하다 → 랜딩·OG 문제 |
| **공유유입 온보딩완주율**이 **40% 아래로 하락** | `birth_submitted` uniq / `app_open` uniq (둘 다 `ft_source='share'`) | 온보딩 길이가 병목 |

⚠ 초판이 비교 기준으로 삼던 「현행 5%」는 내부 2인의 행동이었다 — **비교 기준선은 5%가 아니라 0%**다.
⚠ 셋째 줄을 원래 「유입→생일 제출」이라 불렀는데, `birth_submitted` 는 온보딩 **마지막** 화면에서 발사된다 — 생일 입력이 아니라 **온보딩 완주**다.
⚠ **게이트 수치가 정본 두 곳에서 다르다.** 첫 줄 **15%** 와 `app/작업지시_역할과초대_2026-08-15.md` §F 의 **≥8%** 는 **분자가 달라 별개 지표**다(카드 포함 여부 — 사다리 두 칸이지 모순이 아니다). 셋째 줄 **40%** 와 같은 표의 **≥50%** 는 **같은 지표**이고 §2-D 규칙 4 가 분모를 통일했다. **어느 수치도 내리지 않는다.**

**계측 현황(v130 · 코드 확인)**: ~~`card_saved`~~ · ~~`card_kind`~~ **부착 완료** — `App.jsx` 의 `saveOrShareCard()` 안 `track("card_saved", { card_kind, way, sky_n })` 한 곳을 부적·각인·궁합이 전부 지난다. **외부 실측은 0건.**
**남은 미부착**: 카드발 유입 경로 구분(`ft_source` 는 `?v=`·`?ref=` 만 알아보는데 카드에는 둘 다 없다 — §5-4·작업배분 §6-2) · 공유 발신 **완료/취소** 구분(`verdict_shared` 는 `navigator.share` **앞에서** 발사돼 지금은 "발신 시도율"이다) · OG 노출 경로 구분.

---

## 5. 미결 (다음 세션 또는 창업자)

1. ~~공개 이미지에 실을 파생값 **화이트리스트 확정**~~ → **v130 에서 확정·구현**(아래 §5-1)
2. ~~궁합 결과 공개 범위~~ → **v130 에서 확정·구현** — §2 기준을 끝까지 밀어 **상대 값 0개**로 갔다(아래)
3. 광고 소재로서의 루프 자산과 유기적 공유 자산을 **같은 규격으로 만들지, 따로 만들지**
4. **(신규) 카드발 유입 귀속** — 카드에 주소는 그렸지만 `?ref=` 는 못 붙인다(사람이 쿼리까지 안 친다).
   당분간 `card_saved` 대비 direct 증가로 **간접 추정**만 된다 — **단 현행 `card_saved` 외부 실측이 0건이라 추정할 분자조차 없다**(§1). 실질적으로 이 항목은 **분모 100 이후**로 넘어간다.
   **QR·짧은 주소는 2026-08-17 판정됐다 — 짧은 주소(`/c` → `?ref=card` 리다이렉트) 채택, QR 보류.** 사유·편집 지시는 `01_기획전략/비나리_기획전략_작업배분_2026-08-15.md` §6-6.
   ⚠ **그리고 전제가 하나 틀렸다 — 부적 카드에는 주소가 아예 없다.** 주소를 그리는 `cardFoot()` 호출부는 각인·궁합 둘뿐이고 `buildBujeokPoster()` 는 자체 푸터를 그린다. 부적은 direct 로 섞이는 게 아니라 **애초에 안 들어온다.**

### 5-1. 확정된 수록 범위 (v130 구현)

| 카드 | 싣는 것 | 파생 이름 | 근거 |
|---|---|---|---|
| **각인** | 겉·속 두 줄 · 태어난 해의 이름(납음) · 수호신 · 주소 | **1개** | 겉·속은 일간 파생 — **10일 주기**라 날짜를 못 좁힌다. 촐킨·웨톤은 제외(둘만으로 LCM 1,820일) |
| **궁합** | 관계 한 줄 · 조심할 것 한 줄 · 수호신 · 주소 | **0개** | 축 이름조차 제외 — "충"이면 한쪽을 아는 사람에게 **상대의 자리 글자**가 좁혀진다. 상대는 미동의 제3자 |

**둘 다 총점을 안 싣는다.** 화면에서 "총점을 앞세우지 않는다"고 해 놓고 카드 한복판에 숫자를 박으면
그 원칙이 앱 밖에서 무너진다.

**링크 안은 기각.** 각인 본문은 4,000자라 URL 에 안 들어가고, 넣을 수 있는 건 결국 **입력**
(생년월일·시·도시)인데 그건 설계 헌장이 금지하는 세 값이다. → 보낼 물건은 문서가 아니라 **그림**이다.

**배관**: 부적·각인·궁합이 전부 `saveOrShareCard` 하나를 지난다 — `shareRisk` 판정도 `card_saved` 계측도
빠질 수 없다. 위험 판정은 **그리기 전에** 한다.

---

## 6. 한 줄

> 지금 문제는 "공유를 어떻게 유도하나"가 아니라 **"보낼 물건이 없다"** 이다.
> 판결은 사적이고, 궁합은 둘이 필요하다. **혼자서 완결되고 자랑이 아닌 물건**이 아직 없다.

**(2026-08-17 보강 — 결론은 그대로, 근거가 한 칸 더 앞으로 갔다)**

> 초판은 「공유가 5%밖에 안 된다」를 근거로 이 결론을 냈다. 이중 필터 실측은 그보다 앞이다 — **외부 발신 0건.**
> 루프는 막힌 게 아니라 **아직 시작한 적이 없다.**
>
> **그리고 그 0은 13명 위의 0이다.** 「물건이 없어서 0」인지 **「사람이 없어서 0」인지 이 표본으로는 못 가른다.**
> 두 가설을 가르는 장치는 새 공유 단위가 아니라 **분모 100**이다.
> → 순서는 리포가 이미 적어 둔 대로다: **배관 → 획득(분모 100) → 그때 루프**(`app/작업지시_루프배관과획득_2026-08-15.md` §0, G0-b 가 G3 보다 먼저).
