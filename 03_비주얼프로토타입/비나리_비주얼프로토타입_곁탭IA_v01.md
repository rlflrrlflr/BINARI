# 곁 탭 IA v01 — 판결 / 곁 두 탭으로 가른다

작성 2026-08-15 · 창업자 지시(2026-08-15): *"탭을 두 개로 나누고 아예 다른 컨셉을 만들까? 친구 탭을 만들어서. 지금 궁합도 거기로 보내던가. 우리 컨셉이랑 맞는 용어를 추천해줘도 좋아"* → **진행**
근거: `app/src/App.jsx`(현행 로비 실측) · `app/public/gyeot-mock.html`(곁 시안) · `03_비주얼프로토타입/비나리_비주얼프로토타입_관계표현인계서_v01.md` · `01_기획전략/비나리_기획전략_바이럴루프판단_v01.md` · 설계 헌장(CLAUDE.md)

이 문서는 **화면 구조와 어휘**를 정한다. 가격·게이팅·계측 판단은 `전략로그.md` 2026-08-15 항목에 따로 적었다.

---

## 0. 한 줄

> 지금 앱은 **고민이 있어야만 열 이유가 있다.** 곁 탭은 고민 없이 열 이유를 만드는 유일한 자리다.

---

## 1. 지금 상태 — 코드 실측 (앞선 추정 정정)

먼저 정정한다. 코드 주석(§1327)은 궁합을 *"각인의 애드온 — 내 명식이 이미 있어야 열린다"* 라고 적고 있지만,
**실제 화면에서는 각인과 궁합이 로비의 나란한 두 버튼이다.**

| 항목 | 실측 |
|---|---|
| 탭 | **없다.** 판결 로비 하나 + 모달들 |
| 각인 진입 | 로비 버튼 `setImprintOpen(true)` (App.jsx §4821) |
| 궁합 진입 | **곁 탭 빈 화면 CTA** `setMatchOpen(true)` — ⚠ **곁이 하나라도 서면 사라진다**(창업자 게이트: 첫 한 명만 직접 입력, 그 뒤는 초대 `inviteNew()`). ~~로비 버튼~~ 은 v165 가 제거했다 (2026-08-30 갱신) |
| 노출 조건 | 둘 다 `!ritual && !res && saju` — 판결 전 로비에서만 |
| 상태 | `imprintOpen` / `matchOpen` 두 boolean, 형제 관계 |

**이 정정이 계획을 바꾼다.** 궁합을 탭으로 옮기는 건 결제 구조 수술이 아니라 **버튼 하나를 다른 화면으로 옮기는 일**이다.
`MatchDoc` 은 이미 자기 완결 컴포넌트(`saju`, `birth`, `onClose`)라 부모만 갈아 끼우면 된다.
비용이 낮으므로 §6의 순서에서 궁합 이동을 뒤로 미룰 이유도 줄었다 — 다만 **곁이 먼저 있어야 한다**는 이유는 그대로다(§6).

**없는 것**: 탭 자체 · 곁이라는 개념 · 관계 화면의 수호신(인계서 §1) · 재방문 컨텍스트를 담을 자리.

---

## 2. 왜 두 탭인가 — 두 국면의 성격이 정반대다

| | **판결** 탭 | **곁** 탭 |
|---|---|---|
| 성격 | 일회성 · 사적 · 무거움 | 누적 · 보여줄 만함 · 가벼움 |
| 설계 헌장 | **모를 권리** — 시스템이 먼저 펼치지 않는다 | **알 권리** 쪽 — 있는 건 다 보여준다 |
| 여는 이유 | 고민이 생겼을 때 | **아무 일 없어도** |
| 자산 | 판결록(과거형) | 곁(현재형, 계속 변함) |

한 화면에 둘을 같이 두면 **모를 권리와 알 권리가 같은 자리에서 충돌한다.** 지금 로비가 정확히 그 상태다 —
"두드려봐, 답은 거기 있어"(먼저 안 펼침) 옆에 각인·궁합 상품 버튼 두 개가 상시 붙어 있다.

그리고 D7 게이트 관점에서 이게 구조적 약점이다. 판결은 **수요가 있을 때만** 열리는 물건이고,
습관은 수요가 없을 때도 열리는 물건에서만 생긴다. 곁은 후자를 만들 수 있는 유일한 축이다
(수호신·판결 포맷·되물음 셋 중 **되물음=재방문 컨텍스트**에 해당).

---

## 3. 어휘 — 「곁」

**탭 이름은 「곁」.**

| 근거 | 내용 |
|---|---|
| 그림과 글자가 같은 말을 한다 | 시안의 시각 문법이 *궤도를 도는 빛* 이다. 곁 = 옆에 있는 자리. 이름과 형상이 어긋나지 않는다 |
| 연애로 안 좁혀진다 | 궁합 화면이 이미 *"연인만이 아니야 — 같이 일하는 사람, 가족, 동업자에게도"* 라고 말한다. 탭 이름이 그걸 배신하면 안 된다 |
| 소유가 아니라 **위치**를 말한다 | 수호신은 내 것, 곁은 옆에 선 것. 두 형상 합성 금지(인계서 §3 원칙 1)와 어긋나지 않는다 |
| 순우리말·두 글자 | 하단 탭 라벨로 들어간다 |

### 기각한 후보

| 후보 | 기각 사유 |
|---|---|
| **인연** | ①사주앱 클리셰라 차별점이 안 된다 ②전제 (D) *"대규모 풀 → 글로벌 인연 매칭"* 을 **현재 형태로는 불가능**으로 판정하고 로드맵에서 제거했다. 그 단어를 화면에 박으면 **기각한 방향이 이름으로 되살아난다** |
| **귀인** | 경쟁사 「귀인지도」와 정면 충돌 |
| **연(緣)** | 한 글자 한자 단독은 읽기가 애매 |
| **사이** | 탭 라벨로는 조사가 안 붙는다("사이 탭"). **다만 관계 자체를 부르는 말로는 채택** — 인계서가 이미 *"우리는 사이를 그린다"* 로 쓰고 있다 |

### 확정 어휘 한 벌

| 자리 | 말 | 쓰지 말 것 |
|---|---|---|
| 탭 이름 | **곁** | 친구 · 인연 · 관계 |
| 한 사람 | **곁 하나** / 곁에 선 사람 | 슬롯 · 카드 · 멤버 |
| 부르는 행위 | **곁에 부른다** | 초대하기 · 친구 추가 |
| 둘 사이 | **사이** | 궁합도(度) · 점수 |
| 유료 문서 | **궁합** (그대로 둔다) | — |

궁합만 기존 이름을 유지한다. 검색성과 즉시 이해도가 자산이고, 이건 **문서 이름**이지 화면 이름이 아니다.

### 어휘 확장 — 곁의 두 층 (2026-08-16, 창업자 결정 E-2 #1 반영)

창업자 결정: *회신 온 사람은 정식 자리, 내가 궁합 본 사람은 '답 대기' 자리로 흐리게.*
**층 구분은 채택하되 「답 대기」라는 말은 안 쓴다.**

| 기각 사유 | 내용 |
|---|---|
| 행정 용어다 | 대기번호·승인대기·처리중 계열이다. 세계관이 관공서가 된다 |
| **사람을 규정한다** | "답 안 준 사람". §3은 사람을 등급이 아니라 **자리**로만 부르기로 했다 |
| **셈을 부른다** | 대기열엔 순번이 있다. "대기 2명"이 자연스럽게 따라오고 그건 §5 개수 표기 금지 위반이다 |

**채택: 「부른 곁」.** 정본 어휘 「곁에 부른다」에서 그대로 파생한다.

| 층 | 화면 말 | 코드 값 | 시각 |
|---|---|---|---|
| 대답이 온 쪽 | **곁** | `standing` | 궤도 앞줄, 제 밝기 |
| 내가 부르기만 한 쪽 | **부른 곁** | `called` | 뒤로 물러나 흐리게 |

- 상태가 아니라 **내 행위**를 말한다 — 기다림이 아니라 부름이다. 상대를 평가하지 않는다.
- 새 낱말이 아니라 파생어라 학습 비용 0.
- **라벨은 필요할 때만 붙인다.** 밝기·거리만으로 갈리면 그게 제일 좋다(§5 개수 표기 금지와 가장 잘 맞는다).
- 차선이던 「기척」은 예쁘지만 새 낱말이고 뜻이 모호해 기각.

### 빈 상태 카피 (v134.3 적용)

> 곁
> {오행 성격} 네 곁에, 오늘도 이렇게 서 있어.
> **곁은 네가 불러야 서. 부르면 나와 같이 돌아.**

고친 셋 — ①"옆자리" → 정본은 「곁」(같은 뜻의 다른 말을 두면 어휘가 갈린다)
②"누가 서게 되면"(주체 없는 수동태) → 「부른다」(수동태는 화면을 **대기실**로 만든다)
③"이 자리에 같이 보일 거야" → 자리=슬롯을 암시한다.
⚠ **결핍을 말하지 않는다** — "아직 없어"는 §5 개수 표기 금지의 정신을 문장으로 어기는 것이다.

---

## 4. 화면 구조

```
┌──────────────────────────────┐
│                              │
│      (현행 화면 그대로)        │   ← 판결 탭: 아무것도 안 바꾼다
│                              │
├──────────────────────────────┤
│   판결          곁            │   ← 하단 탭 2개
└──────────────────────────────┘
```

### 곁 탭 — 층 세 개

**1층 · 내 수호신 (항상 있다)**
곁이 0이어도 이 층이 화면을 완결시킨다. 빈 슬롯·진행바·"0명" 표기 **금지**.
> ⚠️ **경계 (2026-08-30 창업자 지시) — 이 금지는 「1층」 조항이다.**
> *"저렇게 자리를 비워놓고 누르면 초대되게 해야 채우고 싶은 충동이 들 거 같아."*
> **2층(펼친 명부) 목록 끝에는 빈 칸 한 개를 둔다** — 그게 사람을 부르는 문이다. 누르면 초대 링크가 만들어진다.
> 뒤집힌 게 아니라 **자리가 갈렸다**: 아무도 없는 사람에게 못 채운 자리를 보이는 건 그대로 금지(1층),
> 이미 곁이 선 사람에게 다음 한 칸을 보이는 건 의도(2층).
> **조건 둘은 그대로 산다** — ①칸은 **언제나 하나**다(여럿이면 "몇 자리 남았다"는 진행바가 되어 §5 개수 금지를 정면으로 어긴다) ②칸 안에 숫자를 안 쓴다.
> 검사: `e2e/gyeot-check.mjs` ⑧ 이 칸 개수와 숫자 유무를 문다.
곁이 생기면 §곁 시안 그대로 궤도에 빛이 선다.

> ⚠️ **크기 규칙 — v01 정정(v132.1)**
> 이 문서를 넘길 때 구두로 *"곁 탭은 뒤로 물러난다"* 고 말했고 그대로 구현됐다. **그 지시는 폐기한다.**
> 곁 탭만 작은 값(`vh*0.5`/캡 600, 확대율 없음)을 쓴 결과 실측 **466px — 판결(719px)의 65%** 가 되어
> 창업자 지적을 받았다("판결도 곁도 수호신이 너무 작게 보여").
> 다시 재 보니 **궤도 반경은 본체의 1.05배**뿐이다(곁 시안 실측). 물러날 이유가 거의 없었다.
>
> - **판결·곁은 같은 size 식을 쓴다** — `min(vw*1.1, vh*0.57, 640)`
> - 여유는 **CSS 확대율로만** 준다 — 판결 `1.85` / 곁 `1.72`(`.halo.wide.lobbyscale` / `.gyeotscale`)
> - ⚠️ **`size`(backing 해상도)를 키워서 해결하지 마라.** 확대율은 컴포지터가 처리해 렌더 비용이 0이지만,
>   `size` 를 키우면 프래그먼트가 제곱으로 늘어난다 — 입자 27,000개 × 가산 블렌딩 4패스라 v127.1 인장 렉이 재발한다.
>
> 수호신이 탭마다 다른 크기로 보이면 **같은 존재로 안 읽힌다.** 소유물이라는 값어치가 거기서 깨진다.

**2층 · 곁에 선 사람들 (무료)**
곁 하나를 누르면 그 사람과 나의 **방향** 한 줄만 나온다 — 생/극/동, 색, 언제 마지막으로 주고받았나.
숫자·점수·게이지 없음(인계서 §3 원칙 2).

**3층 · 사이 (유료 4,900원 = 지금의 궁합)**
2층에서 한 사람을 **펼치면** 열린다. `MatchDoc` 을 그대로 쓴다.

이 층 구분이 하는 일: 궁합이 *"메뉴에서 고르는 상품"* 에서
***"이미 곁에 있는 사람을 더 알고 싶어질 때 펼치는 것"*** 으로 바뀐다. 재구매 축(사람 수만큼)은 그대로다.

### 곁이 많아질 때 (시안 ⑤ vs ⑥에서 판정)

| 규칙 | 내용 |
|---|---|
| **입자 예산 고정** | 몇 명이 오든 곁이 쓰는 총량은 본체 입자의 16% 고정. 사람이 늘면 각자가 옅어진다. 성능도 인원과 무관 |
| **앞줄은 셋까지** | 최근 주고받은 셋만 궤도에 서고, 나머지는 바깥으로 물러나 배경 성운이 된다 |

두 번째가 더 중요하다. 열 명을 다 그리는 순간 그건 **친구 수 카운터**고,
숫자를 안 쓰기로 한 원칙이 그림으로 되살아난다. 셋으로 자르면 화면이 말하는 게
"몇 명 모았나"가 아니라 **"요즘 누가 곁에 있나"** 가 된다.

*상한 3은 잠정값이다.* 시안(`/gyeot-mock.html`) ⑥번 판에서 4~5도 견딜 만해 보인다 — 창업자 확정 대기.

---

## 5. 하지 말 것

- **판결 탭을 건드리는 것** — 이번 작업에서 판결 화면은 문구 하나도 안 바꾼다. 탭 껍데기만 씌운다
- **곁 탭 첫 화면을 결제벽으로 만드는 것** — 무료 유저가 열었을 때 살 것부터 보이면 빈 슬롯 금지 원칙과 같은 이유로 실패한다
- **개수 표기** — "곁 3", 배지 숫자, 진행바 전부 금지
  → ⚠️ **2026-08-17 창업자 지시로 한 자리만 열렸다**(아래 「이름과 써머리」 참조). **사람은 여전히 안 센다.**
    세는 대상이 *사람*이면 친구 수 카운터고, *자리(역할)*면 분포다 — "판을 같이 키우는 자리 2명"은 후자다
- **하트·커플·핑크** — 인계서 §6. 곁은 연애 기능이 아니다
- **신규 캐릭터·마스코트** — 설계 헌장 명시 금지(2026-07-25)
- ~~**상대 이름 받기**~~ → **2026-08-17 창업자 결정으로 뒤집혔다.** 아래 절 참조.
  (기존 근거 「엔진이 의도적으로 안 받는다」는 **지금도 맞다** — 이름은 계산에 안 들어간다.
   바뀐 건 "계산에 안 쓰니 받지 말자"에서 "계산엔 안 쓰지만 **알아보려면 필요하다**"로다)

---

## 5-2. 이름과 써머리 (2026-08-17 창업자 결정)

> 창업자: *"곁 탭 최초 진입한 사람은 궁합 볼 사람의 이름을 쓰게 하고, 한 번 쓴 사람은 두 번을 클릭해야
> 이름 목록을 볼 수 있게 해줘. 마치 판결 탭처럼. 두 번 탭 하면 써머리로 그래프와 표를 보여주고
> 그 밑에 리스트형으로 이름들이 보이게. 그리고 이 정보가 판결 탭에도 흘러가게."*

**뒤집힌 근거는 이론이 아니라 관찰이다.** 경쟁 앱(도령)에서 사람들은 "심부름"인 줄 알면서도 이름을 넣는다 —
*나와 저 사람이 어떤 관계인지가 궁금해서*다. 설계 세션의 반대 논거("엔진이 안 쓰는 값은 받지 말자")는
**엔진 기준**이었고, 유저 기준으로는 이름이 곧 "누가 누구인지"였다.

### 지킨 것 (결정이 바뀌어도 안 바뀌는 선)

| 무엇 | 어떻게 |
|---|---|
| 이름은 **계산에 안 쓴다** | 엔진은 여전히 생년월일만 본다. 비워도 궁합·역할이 그대로 나온다 |
| 이름은 **기기 밖으로 안 나간다** | 서버 0 · 계측 0 · **AI 0**. AI엔 `곁1`·`곁2` 자리표와 역할 이름만 보내고, 돌아온 문장에서 앱이 기기 안에서 이름으로 바꾼다 |
| 밖으로 나가는 면엔 **가림말** | 공유 문안·부적 이미지에서는 자리표를 「곁에 선 사람」으로 바꾼다 — 곁은 미동의 제3자다 |
| 연락처는 **여전히 안 받는다** | 이 선까지 같이 무너뜨리지 않는다 |
| 개수는 **자리를 센다** | 사람 수 카운터를 안 만든다. §5 개수 금지가 막으려던 것(수집 경쟁)은 그대로 막힌다 |

### 두 겹으로 두는 이유 (두 번 두드리기)

판결 탭의 `awake` 와 **같은 문법**이다. 새로 배울 게 없다.
① 1층은 *수호신과 곁이 도는 그림*이고, 목록이 상시로 깔리면 그 그림이 안 보인다
② **이름이 적힌 목록은 남이 옆에서 볼 수 있는 화면**이라 한 번의 의도적인 동작 뒤에 두는 게 맞다

### 순위 방어는 그대로 (v134.2)

- 써머리 표는 **개수 내림차순**이다. 이건 사람 줄 세우기가 아니라 **범주의 분포**다.
- **이름 목록은 최근순 그대로** — 표의 순서를 절대 안 따라간다. 따라가면 "1등 자리의 사람"이 맨 위에 서고
  그 순간 목록이 순위가 된다.

---

## 6. 순서 (이 순서를 지켜라)

| 단계 | 무엇 | 왜 이 순서인가 |
|---|---|---|
| **1** | 하단 탭 껍데기 2개. 곁 탭엔 내 수호신(1층)만 | 가장 되돌리기 쉬운 변경. 다른 세션과의 충돌면도 여기가 제일 작다 |
| **2** | 곁 렌더 이식 — 시안 셰이더를 본체 입자 재배정으로 옮긴다 | 곁이 없는 곁 탭은 지금보다 나쁘다 |
| **3** | 곁 목록(2층) + 곁에 부르기 | 여기서 처음으로 **혼자가 아닌 화면**이 된다 |
| **4** | 궁합을 로비 버튼에서 3층으로 이동 | **마지막이다.** 궁합부터 옮기면 곁이 아직 없어서 탭에 상품 하나만 덩그러니 놓인다 |

**⚠️ 다른 세션과의 충돌**: 탭 도입은 이 리포에서 **가장 충돌면이 넓은 변경**이다(`App.jsx` 렌더 루트).
단계 1을 착수하기 전에 `origin/main` 을 받아 다른 세션 작업과 맞추고, 단계마다 쪼개서 올린다.

---

## 7. 이어서 볼 문서

- `app/public/gyeot-mock.html` — 곁 시안 실물(`node app/tools/build-gyeot-mock.mjs` 로 재생성)
- `03_비주얼프로토타입/비나리_비주얼프로토타입_관계표현인계서_v01.md` — 관계를 어떻게 그리는가(원칙 5개)
- `01_기획전략/비나리_기획전략_바이럴루프판단_v01.md` — 1인 완결형이 먼저, 2인은 2차 증폭
- `전략로그.md` 2026-08-15 — 상품 구조·게이팅·계측 판단
