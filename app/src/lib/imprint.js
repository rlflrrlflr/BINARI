/* ── 각인 판정 엔진 ────────────────────────────────────────────────────────
   생년월일시 → **읽을 수 있는 문서**로 바꾼다. sky.js 는 계산만 하고, 무슨 말로 옮길지는 여기가 정한다.

   창업자 지시(2026-08-12) 넷을 규칙으로 못 박은 파일이다.
   ① **뜻을 먼저, 그림은 나중.** "너는 산이다"는 아무 말도 안 한 것이다.
      → "쉽게 흔들리지 않는 사람이다. 한번 정하면 잘 안 바꾼다"가 먼저 나온다.
   ② **기법 이름은 화면에 안 나간다.** 십성·대운·용신 같은 말은 여기서 한 글자도 만들지 않는다.
   ③ **뎁스.** 짝이면 키·생김새·집안·벌이·만나는 시기까지 내려간다. "좋은 사람 만난다"로 끝내지 않는다.
   ④ **각주.** 모든 문장은 note 를 함께 낸다. 화면에서 숨기든 말든, 근거 없는 문장은 만들지 않는다.

   ⚠ 지어내지 않는다. 아래 표에 없는 조합은 문장을 내지 않고 자리를 비운다.
   ⚠ 성별이 없으면 짝·자식 자리를 비운다. 시(時)가 없으면 그렇다고 적는다.

   ⑤ **아는 값은 맞히지 않는다 — 받아서 해석한다.** (창업자 실사용 제보 2026-08-14로 추가)
      키·배우자 생김새·언제 만났나는 전부 **유저가 정답을 이미 아는 값**이다.
      맞혀도 "그래서?"이고 틀리면 나머지 서른 칸까지 같이 죽는다. 비대칭이 너무 크다.
      → 아는 값은 **입력으로 받아 해석**하고, 모르는 값(앞일·왜 그런지)만 단언한다.
      → 기혼자에게 배우자 외모·집안·벌이를 예언하지 않는다. 그건 예언이 아니라 오답 노출이다. */

import {
  jdFromKST, sunLongitude, moonLongitude, jdn, sunSign, moonSign, nakshatra, tzolkin,
  ascendant, signOf, wholeSignHouse, profection, isDayBirth, vimshottari,
  seun, wolun, favor, sipseong as ssOf, GAN as GANK, JI as JIK, GANH, JIH, JI_BONGI as JB,
  nayin, honmeisei, weton, akan, lifePath, moonPhase, partOfFortune, HOUSE_KO,
} from "./sky.js";

/* 간지가 한글로도 한자로도 들어온다 — 앱의 대운은 한글("임오"), 흐름 엔진은 한자("壬午").
   한쪽만 찾으면 −1 이 나오고, 그러면 여든 해가 전부 같은 값이 된다(실측으로 잡았다). 둘 다 본다. */
const gIdx = (c) => { const i = GANK.indexOf(c); return i >= 0 ? i : GANH.indexOf(c); };
const jIdx = (c) => { const i = JIK.indexOf(c); return i >= 0 ? i : JIH.indexOf(c); };

/* ── 말 표 ───────────────────────────────────────────────────────────── */
const EL_KO = { 목: "나무", 화: "불", 토: "흙", 금: "쇠", 수: "물" };
const ZO_EL = { 양자리: "불", 사자자리: "불", 사수자리: "불", 황소자리: "흙", 처녀자리: "흙", 염소자리: "흙",
  쌍둥이자리: "공기", 천칭자리: "공기", 물병자리: "공기", 게자리: "물", 전갈자리: "물", 물고기자리: "물" };

/* ⚠ **표는 전부 '-야/-어'체로 쓴다.**(2026-08-14)
   v116 까지 표는 사전처럼 "-다"체였고 감싸는 문장은 "-야"체였다. 그래서 한 문단 안에서
   말투가 두 번 바뀌었다 — "너는 감정이 먼저 보이는 사람이야. 기분이 얼굴에 그대로 나온다."
   창업자 지적("신도 아닌 거 같아")의 정체가 이거다. 신은 사전을 읽지 않는다.
   새 문구를 표에 넣을 때 **반드시 '-야/-어'로 끝낸다.** 검사(imprint-check ⑫)가 이걸 지킨다. */

/* 겉 — 남들 눈에 보이는 모습. 일간 오행에서 나온다. 은유가 아니라 행동으로 쓴다 */
const SURFACE = {
  목: { w: "계속 자라려는 사람", d: "가만히 있으면 시들해져. 늘 다음 걸 봐. 멈춰 세우면 못 견뎌" },
  화: { w: "감정이 먼저 보이는 사람", d: "기분이 얼굴에 그대로 나와. 숨기려 해도 티가 나. 대신 뒤끝이 없어" },
  토: { w: "쉽게 흔들리지 않는 사람", d: "한번 마음을 정하면 잘 안 바꿔. 재촉해도 서두르지 않아. 사람들이 기대는 쪽이야" },
  금: { w: "한번 정하면 안 바꾸는 사람", d: "정하기까지가 오래 걸려. 대신 정하고 나면 끝까지 가. 끊고 맺는 게 분명해" },
  수: { w: "깊이 생각하는 사람", d: "결정 앞에 오래 서 있어. 얕은 답에 만족을 못 해. 그래서 늘 조금 늦어" },
};
/* 속 — 서양 해·달자리의 원소에서 나온다. 겉과 다를수록 이 사람의 급소가 된다 */
const INNER = {
  공기: { w: "생각이 빠른 사람", d: "하고 싶은 말이 늘 가득해. 머리는 벌써 몇 걸음 앞에 가 있어" },
  불: { w: "마음이 먼저 뜨거워지는 사람", d: "참는 게 제일 어려워. 하고 싶으면 지금 해야 해" },
  물: { w: "감정이 깊고 오래 가는 사람", d: "겉으로는 잘 안 보여. 혼자 오래 담아 둬" },
  흙: { w: "실속부터 따지는 사람", d: "뜬 얘기를 못 견뎌. 손에 잡히는 것만 믿어" },
};
/* 가장 약한 자리 — 이 사람 인생의 축이 된다. 없는 것이 있는 것보다 많은 걸 정한다.
   ⚠ **이름을 은유로 짓지 않는다.**(2026-08-14) v117 까지 t 가 "같이 갈 사람"·"말이 나갈 문"·"쥐는 손"
   같은 명사구였고, 본문에서 "네게는 같이 갈 사람이 얇아"로 조립됐다.
   창업자 판정: "뭔 말이야? 은유 같지도 않고 비유 같지도 않다."
   맞다 — 사람이 얇을 수 없고 문이 얇을 수 없다. 뜻은 안 전해지고 폼만 잡은 문장이다.
   → t 는 **제목용 한 낱말**, plain 은 **뜻이 바로 서는 완결 문장**으로 나눈다. 본문은 plain 을 쓴다. */
const BLOCK = {
  식상: { t: "말", plain: "하고 싶은 말을 제때 못 꺼내",
    s: "할 말이 안에서만 돌아", w: "참다가 한 번에 터져. 그리고 그게 제일 가까운 사람한테 가",
    trig: "하고 싶은 말을 오래 참았을 때",
    fix: "말로 못 하는 걸 <b>손으로 내놓는 일을 하나 만들어.</b> 글·그림·영상·만들기 — 결과물이 남는 거면 뭐든. 그게 네 입을 대신해" },
  재성: { t: "돈 쥐기", plain: "돈이 손에 안 남아",
    s: "지나가는 건 보이는데 남는 게 없어", w: "크게 벌어도 남는 게 적어. 버는 재주보다 지키는 장치가 먼저야",
    trig: "돈이 계속 새 나갈 때",
    fix: "<b>월급이나 고정 계약으로 받아.</b> 투자·사업처럼 네가 직접 굴리는 돈은 새. 굴릴 거면 남한테 맡겨" },
  관성: { t: "규칙", plain: "네 생활을 잡아 줄 틀이 없어",
    s: "정해진 게 없으면 흐트러져", w: "남이 정한 틀을 못 견디는데, 틀이 없으면 또 무너져. 스스로 만든 규칙만 지켜",
    trig: "생활이 흐트러졌을 때",
    fix: "<b>네 규칙을 네가 적어서 눈에 보이는 데 붙여 둬.</b> 남이 정해 준 규칙은 안 지켜지고, 규칙이 없으면 무너져" },
  인성: { t: "도움받기", plain: "남한테 부탁을 못 해",
    s: "혼자 다 지고 가", w: "도와줄 사람이 없어서가 아니라 안 불러서야. 그래서 늘 네가 제일 지쳐",
    trig: "혼자 다 지려고 할 때",
    fix: "<b>\"도와줘\"라고 먼저 말하는 것.</b> 딱 이 한 마디야. 네가 평생 배워야 할 기술은 이거 하나뿐이야" },
  비겁: { t: "사람", plain: "무리에 잘 못 섞여",
    s: "혼자 하는 게 빨라", w: "여럿이 하면 속이 터져서 결국 네가 다 해. 그래서 짐도 혼자 져",
    trig: "곁에 아무도 없을 때",
    fix: "<b>혼자 한다고 치고 일정을 짜.</b> 사람이 붙으면 다행이고, 안 붙어도 계획이 안 무너져. 나쁜 게 아니라 미리 계산에 넣으라는 말이야" },
};
/* 두꺼운 자리 — 이 사람이 어디서 밥을 버나 */
const THICK = {
  식상: { job: "네가 만든 걸 네 이름으로 내놓는 일", ex: "기획·창작·교육·요식", grew: "시키는 대로 하는 게 답답했고, 네 방식으로 바꿔야 손이 움직였어" },
  재성: { job: "사고팔고 연결해서 차액을 남기는 일", ex: "영업·유통·중개·자영업", grew: "값과 이익이 먼저 보였고, 숫자로 말할 때 설득력이 붙었어" },
  관성: { job: "조직에서 직함을 받고 오래 앉아 있는 일", ex: "공공·법률·행정·교육·인사", grew: "규칙이 분명한 데서 오히려 편했고, 맡기면 끝까지 했어" },
  인성: { job: "아는 만큼 값이 붙는 일", ex: "연구·상담·자격증이 필요한 전문직", grew: "설명해 주면 빨리 알아들었고, 다 알고 나서야 움직였어" },
  비겁: { job: "네 실력이 곧 간판인 일", ex: "1인 사업·프리랜서·기술직", grew: "누구 밑보다 혼자가 빨랐고, 그래서 부딪히기도 했어" },
};
const ORGAN = {
  목: { part: "간과 눈, 그리고 힘줄", sym: "눈이 빨리 피로해지고, 화를 삼키면 옆구리와 어깨가 결려", over: "성질이 먼저 솟고 참으면 두통이 와" },
  화: { part: "심장과 혈관", sym: "손발이 차고 가슴이 자주 두근거려. 겨울마다 기운이 확 꺼져", over: "열이 잘 오르고 잠이 얕아. 입안이 자주 헐어" },
  토: { part: "위장과 소화", sym: "잘 체하고, 찬 걸 먹으면 바로 탈이 나", over: "몸이 잘 붓고 무거워져. 생각이 많아 잠을 뒤척여" },
  금: { part: "코와 기관지, 그리고 피부", sym: "환절기마다 기침이 오래가. 코가 자주 막히고 살갗이 메말라", over: "숨이 얕고 잔기침이 길어. 피부가 예민해 잘 뒤집혀" },
  수: { part: "콩팥과 방광, 그리고 뼈", sym: "허리가 쉽게 시고 저녁이면 힘이 뚝 떨어져", over: "아랫배가 차고 잘 부어. 밤에 자주 깨" },
};
const FACE = {
  목: "갸름하고 이목구비가 또렷해. 키에 비해 마른 인상이야",
  화: "이마가 넓고 눈이 살아 있어. 표정이 자주 바뀌어",
  토: "둥글넓적해. 코가 크고 두툼해. 웃으면 인상이 확 좋아져",
  금: "윤곽이 분명해. 코가 오뚝하고 턱선이 살아 있어. 차가워 보인다는 말을 들어",
  수: "부드럽고 둥근 편이야. 피부가 희고 눈에 물기가 있어",
};
const BUILD = { 목: "마르고 세로로 긴 쪽", 화: "날렵하고 잘 안 찌는 쪽", 토: "뼈대가 굵고 어깨가 넓은 쪽", 금: "단단하고 균형 잡힌 쪽", 수: "살이 잘 붙는 쪽" };
const VOICE = { 목: "맑고 또렷해", 화: "빠르고 높아", 토: "낮고 울림이 있어", 금: "또박또박 끊어져", 수: "낮고 부드러워" };
/* 키 — ⚠ **다루지 않는다. 예측도 안 하고 입력도 안 받는다.**
   v115: 일간·상승궁을 cm 로 환산하는 표(H_EL/H_ASC)로 예측했다 → 어느 유파에도 없는 지어낸 표였다.
   v116: 그럼 받아서 해석하자고 바꿨다 → 더 나빴다. 179를 받아 172를 빼고 "7cm 큰 쪽이라 눈에 띈다"고 썼다.
         창업자 판정: "너무 전문성 없어 보이고 바보 같아. 신도 아닌 거 같아."
   **맞다. 그건 해석이 아니라 뺄셈이다.** 유저가 준 값을 되읊는 순간, 문서는 아는 척하는 계산기가 된다.
   받은 값이 값을 하려면 **명식과 교차해서 유저가 모르던 걸 내놔야** 하는데, 키에는 교차할 축이 없다.
   → 키 항목을 없앤다. 몸은 골격·얼굴·목소리로만 말한다(이건 명식에서 나온다). */
/* 짝의 자리에 앉은 성질.
   w·d 는 **미혼**에게 쓰는 예측 문구, g·b 는 **기혼**에게 쓰는 관계 해석 문구다(§⑤).
   d 를 마침표로 잘라 재활용하면 "안정적이다." 세 글자가 한 항목이 된다 — 실제로 그랬다. 따로 쓴다. */
const SPOUSE = {
  비견: { w: "대등한 사람", d: "친구처럼 지내. 대신 서로 안 굽혀서 부딪혀",
    g: "서로를 동료로 봐. 존댓말이 사라져도 예의는 남아 있는 사이야.", b: "둘 다 안 굽혀. 사과가 늦어서 작은 일이 사흘을 가." },
  겁재: { w: "자기 일과 벌이가 확실한 사람", d: "기대오는 사람과는 오래 못 가. 돈은 처음부터 갈라 두는 게 나아",
    g: "각자 제 몫을 해. 서로에게 기대지 않아서 부담이 없어.", b: "돈이 얽히면 급격히 나빠져. 보증·공동명의·빌려주기 셋이 특히 그래." },
  식신: { w: "편안한 사람", d: "먹고사는 걱정이 덜해. 대신 긴장이 없어 늘어지기도 해",
    g: "같이 있으면 긴장이 풀려. 먹고사는 걱정을 덜 하는 집이야.", b: "긴장이 없어서 늘어져. 서로에게 무심해지는 게 가장 큰 위험이야." },
  상관: { w: "재주 있고 말 잘하는 사람", d: "자극이 되는 만큼 말로 상처도 주고받아",
    g: "대화가 재밌어. 서로를 자극해서 각자 한 단계씩 자라.", b: "말로 이기려 들어. 이긴 쪽도 남는 게 없는 싸움이 반복돼." },
  정재: { w: "알뜰하고 성실한 사람", d: "안정적이야. 대신 답답하게 느껴지는 순간이 와",
    g: "생활이 안 흔들려. 돈과 살림에서 이미 신뢰가 쌓여 있어.", b: "다 예측돼서 답답해져. '설렘이 없다'는 말이 어느 시점에 나와." },
  편재: { w: "활달하고 씀씀이 큰 사람", d: "함께 벌이는 재미가 있어. 씀씀이는 맞춰야 해",
    g: "같이 벌이고 같이 쓰는 재미가 있어. 사람도 잘 불러.", b: "씀씀이가 안 맞으면 그게 모든 싸움의 진짜 원인이 돼. 미루면 커져." },
  정관: { w: "반듯하고 책임감 있는 사람", d: "기댈 만해. 대신 원칙에서 서로 안 물러서",
    g: "약속을 지켜. 밖에서 부끄러울 일이 없는 사람이야.", b: "원칙에서 둘 다 안 물러서. 옳고 그름을 따지다 마음이 상해." },
  편관: { w: "강단 있는 사람", d: "위기에 든든해. 평소엔 팽팽해",
    g: "위기에 진짜 든든해. 큰일일수록 이 사람이라는 걸 겪어서 알아.", b: "평소가 팽팽해. 별일 아닌데 목소리가 먼저 올라가." },
  정인: { w: "품어 주는 사람", d: "보살핌을 받아. 대신 어리광이 늘 수 있어",
    g: "받아 줘. 밖에서 지쳐 들어와도 집에서 회복이 돼.", b: "받는 게 익숙해져. 어느 순간 네가 어린 쪽이 되어 있어." },
  편인: { w: "생각이 깊은 사람", d: "통하면 크게 통해. 혼자 있는 시간을 많이 필요로 해",
    g: "통하면 크게 통해. 말 안 해도 아는 순간들이 있어.", b: "혼자 있는 시간을 많이 필요로 해. 그게 거리로 느껴지는 때가 와." },
};
/* 7하우스 사인 → 배우자의 겉모습. 서양 전통에서 짝의 외형을 직접 다루는 자리다 */
const H7 = {
  양자리: { h: 2, look: "다부지고 움직임이 빨라. 이마가 넓고 눈매가 강해", air: "먼저 말을 걸고 먼저 움직여" },
  황소자리: { h: -1, look: "목이 짧고 몸이 탄탄해. 피부가 좋고 목소리가 좋아", air: "느리지만 한번 정하면 안 바꿔" },
  쌍둥이자리: { h: 2, look: "마른 편이고 팔다리가 길어. 실제 나이보다 어려 보여", air: "말이 빠르고 손짓이 많아. 사람 많은 자리에서 빛나" },
  게자리: { h: -1, look: "둥글고 부드러워. 얼굴이 희고 눈이 커", air: "먼저 챙겨. 집을 중요하게 여겨" },
  사자자리: { h: 1, look: "머리숱이 많고 자세가 곧아. 어디서든 눈에 띄어", air: "무리의 중심에 서. 인정받는 걸 중요하게 여겨" },
  처녀자리: { h: 0, look: "단정하고 군더더기가 없어. 마른 편이야", air: "꼼꼼해. 정리된 걸 좋아해" },
  천칭자리: { h: 1, look: "이목구비가 고르고 인상이 좋아. 옷을 잘 입어", air: "부딪히는 걸 싫어해. 중간에 서는 사람이야" },
  전갈자리: { h: 0, look: "눈매가 깊어. 말수가 적은데 존재감이 있어", air: "속을 잘 안 보여. 한번 믿으면 끝까지 가" },
  사수자리: { h: 2, look: "키가 크고 다리가 길어. 잘 웃어", air: "솔직해. 갇히는 걸 못 견뎌" },
  염소자리: { h: 0, look: "뼈대가 분명하고 나이보다 어른스러워 보여", air: "계획대로 가. 책임을 먼저 져" },
  물병자리: { h: 1, look: "체형이 길쭉하고 인상이 독특해", air: "남과 다른 걸 좋아해. 거리를 둬" },
  물고기자리: { h: -1, look: "눈이 크고 인상이 순해. 몸이 부드러워", air: "잘 맞춰 줘. 마음이 약해" },
};
const SS_EVENT = {
  정재: "월급이 오르거나, 계약이 하나 길게 붙거나, 집·차처럼 이름이 올라가는 물건이 생겨",
  편재: "부업·투자·중개처럼 목돈이 오가는 판이 열려. 크게 들어오고 크게 나가",
  식신: "손에 익은 걸로 벌어먹는 자리가 생겨 — 먹는 일·가르치는 일·만드는 일",
  상관: "말·글·영상·기획처럼 티 나는 걸 내놓게 돼. 대신 윗사람과 한 번은 부딪혀",
  정관: "직함이 생기거나 승진하거나, 자격증·시험처럼 이름이 서는 일이 와",
  편관: "책임이 갑자기 얹혀. 이직·발령·수술·소송처럼 몰아치는 일이야",
  정인: "배움이 붙어. 학교·자격·문서, 그리고 도와주는 어른이 나타나",
  편인: "혼자 파는 일이 열려 — 자격·연구·기술·상담 쪽이야",
  비견: "동업·팀·같은 처지의 사람이 붙어. 독립을 생각하게 돼",
  겁재: "경쟁자가 생기고 돈이 새. 보증·동업·빌려주기 셋이 특히 그래",
};
/* 유년 구간(18세 미만)에는 어른의 사건을 쓰면 안 된다.
   실사용 판결문에 **"5~14세: 월급이 오르거나 집·차처럼 이름이 올라가는 물건이 생긴다"**가 그대로 나왔고,
   같은 뿌리로 돈 자리가 "돈이 도는 구간은 5~14세로 이미 지나갔어"라는 결론까지 냈다(유저는 스물다섯이다).
   짝의 시기에는 18세 필터를 걸어 뒀으면서 여든 해 지도와 돈·일에는 안 걸었다. 같은 자리에 같은 필터를 건다. */
const SS_CHILD = {
  정재: "부족함 없이 자라. 용돈이든 물건이든 아쉬웠던 기억이 적어",
  편재: "집안 형편이 오르내려. 그걸 어린 눈으로 다 봐",
  식신: "먹고 노는 데 걱정이 없어. 손재주가 일찍 보여",
  상관: "말이 빨리 트여. 어른 앞에서도 할 말은 다 하는 아이야",
  정관: "규칙 있는 데서 잘 커. 어른들이 예뻐하는 쪽이야",
  편관: "눌리는 일이 있어. 크게 혼나거나 한 번 앓거나",
  정인: "배움과 어른 복이 붙어. 누군가 너를 챙겨 줘",
  편인: "혼자 노는 시간이 길어. 남들이 안 파는 걸 파",
  비견: "또래가 늘 곁에 있어. 형제나 친구가 세상의 전부야",
  겁재: "뺏기거나 겨루는 일이 잦아. 그게 승부욕으로 남아",
};
const SS_BAND = {
  정재: "월급과 살림이 자리 잡는 열 해", 편재: "큰돈이 오가는 열 해", 식신: "잘하는 걸로 먹고사는 열 해",
  상관: "네 이름 걸고 내놓는 열 해", 정관: "직함과 책임이 붙는 열 해", 편관: "눌리고 버티는 열 해",
  정인: "배우고 도움받는 열 해", 편인: "혼자 파고드는 열 해", 비견: "사람이 몰리는 열 해", 겁재: "경쟁자가 생기고 돈이 새는 열 해",
};
const GRP_OF = { 비견: "비겁", 겁재: "비겁", 식신: "식상", 상관: "식상", 정재: "재성", 편재: "재성", 정관: "관성", 편관: "관성", 정인: "인성", 편인: "인성" };
/* 인도 시기 이름도 나이를 탄다 — "어머니와 집이 세상의 전부인 시기"가 **서른다섯 살 칸**에 붙어 있었다.
   유년 전제로 쓴 문구를 성인 구간에 그대로 붙이면 그 한 줄이 그 칸 전체를 우습게 만든다. */
const DASHA_KO = {
  달: "어머니와 집이 세상의 전부인 시기", 화성: "몸이 마음보다 앞서는 시기", 라후: "바깥이 궁금해지는 시기",
  목성: "넓어지고 풀리는 시기", 토성: "책임이 무거워지는 시기", 수성: "말과 셈이 밝아지는 시기",
  케투: "안으로 접히는 시기", 금성: "사람과 즐거움이 느는 시기", 해: "이름이 서는 시기",
};
const DASHA_ADULT = { 달: "집과 안쪽 사람이 중심이 되는 시기", 라후: "낯선 것에 끌리고 한 번 흔들리는 시기" };
const dashaWord = (lord, from) => (from >= 18 && DASHA_ADULT[lord]) || DASHA_KO[lord];

/* ── 아홉 하늘 ────────────────────────────────────────────────────────────
   v118 은 아홉 문명에게 **같은 질문**을 던지고 다섯 낱말 중 하나에 투표시켰다. 두 가지가 망가졌다.
   ① 다섯 낱말을 **오행으로** 잡았더니 아홉을 전부 사주 어휘로 번역한 셈이 됐다.
      다른 하늘을 사주의 렌즈로 읽으면 당연히 사주로 읽힌다.
   ② 아홉 표를 다섯 칸에 뿌리면 3:3:3 같은 무승부가 흔하다. 실제로 그렇게 나왔고,
      "이게 네 급소야"라고 써 놓고 정작 아무것도 못 가리켰다.
   ③ 결정적으로 — **표를 세고 나서 그 결과로 아무것도 안 했다.** 나머지 본문은 100% 사주였다.
      창업자 판정: "그냥 사주인데 굳이 쟤네 왜 붙였지 싶어." 맞다. 부록이었다.

   → 투표를 없애고 **분업**으로 바꾼다. 각 하늘이 **사주가 못 하는 일 하나씩**을 맡는다.
     사주에 없는 축(삶의 영역 배치 · 방위 · 날의 무게 · 달의 방향 · 120년 주기)을 각자 채운다.
     그리고 **사주와 어긋나는 곳을 따로 모아 보여준다** — 그게 이 문서가 사주 앱과 갈리는 지점이다. */

/* 마야 열세 톤 — 그 날의 역할이 얼마나 센가 */
const TZ_TONE = { 1: "처음 여는", 2: "둘로 가르는", 3: "움직여 잇는", 4: "틀을 세우는", 5: "가운데서 버티는",
  6: "흐름을 고르는", 7: "비추어 되돌아보는", 8: "고르게 나누는", 9: "끝까지 밀어붙이는",
  10: "실제로 드러내는", 11: "풀어 놓아주는", 12: "모아 이해하는", 13: "넘어서는" };
/* 구성기학 아홉 별의 정위(定位) 방위 — 팔괘 배당 그대로다. 사주에는 방위 축이 거의 없다 */
const GU_DIR = { 일백수성: ["북", "물의 자리"], 이흑토성: ["남서", "땅의 자리"], 삼벽목성: ["동", "우레의 자리"],
  사록목성: ["남동", "바람의 자리"], 오황토성: ["가운데", "중심의 자리"], 육백금성: ["북서", "하늘의 자리"],
  칠적금성: ["서", "못의 자리"], 팔백토성: ["북동", "산의 자리"], 구자화성: ["남", "불의 자리"] };
/* 수비학 — 이 수로 태어난 사람이 평생 배우는 것 */
const LP_TASK = { 1: "혼자 서는 법", 2: "둘 사이를 잇는 법", 3: "밖으로 꺼내는 법", 4: "쌓아 올리는 법",
  5: "한 자리에 묶이지 않는 법", 6: "떠맡되 짓눌리지 않는 법", 7: "혼자 깊이 파는 법",
  8: "돈과 힘을 다루는 법", 9: "놓아주는 법", 11: "남을 비추는 법", 22: "크게 짓는 법", 33: "가르치는 법" };
const EL_OF_NAYIN = (t) => ["목", "화", "토", "금", "수"].find((e) => t.includes(e)) || null;
const EL_KO2 = { 목: "나무", 화: "불", 토: "흙", 금: "쇠", 수: "물" };

const SEOUL = { lat: 37.5665, lon: 126.978 };const HANGUL_AGE = (y, now) => now.getFullYear() - y + 1;
/* 조사 — "힘줄야"가 화면에 나왔다. 받침을 보고 고른다 */
const jong = (s) => { const c = (s || "").trim().slice(-1).charCodeAt(0); return c >= 0xac00 && c <= 0xd7a3 && (c - 0xac00) % 28 > 0; };
const IYA = (s) => s + (jong(s) ? "이야" : "야");
const EUN = (s) => s + (jong(s) ? "은" : "는");
const IGA = (s) => s + (jong(s) ? "이" : "가");
const NUM_KO = ["", "하나", "둘", "셋", "넷", "다섯", "여섯", "일곱", "여덟", "아홉"];
const nk = (n) => NUM_KO[n] || String(n);
/* 열 해 이름은 관형형("혼자 파고드는 열 해")이라 "열 해"만 떼면 "혼자 파고드는에"가 된다(실제로 나왔다) */
const bandName = (t) => String(t).replace(" 열 해", " 시기");
/* 문장 끝에 마침표가 없으면 붙인다 — 표의 조각을 이어 붙일 때 "…온다 사람이"가 된다 */
const dot = (s) => { const t = String(s).trim(); return /[.!?…]$/.test(t) ? t : t + "."; };

/** 각인 한 벌을 만든다. saju/idx/counts/ladder 는 App.jsx 가 계산해 넘긴다. */
export function readImprint({ saju, ladder, birth, sex, now = new Date(), lat = SEOUL.lat, lon = SEOUL.lon,
  /* ── 선택 입력 (각인을 열 때만 묻는다. 무료 온보딩은 그대로 둔다) ──
     ⚠ 이 셋이 없어도 문서는 나온다. 있으면 **틀린 말을 안 하게 된다.**
     특히 married/kids 가 없으면 마흔 살 기혼자에게 "서른에 짝을 만난다"고 쓰게 된다 — 그 한 줄이 문서 전체를 죽인다.
     metAge 는 §⑤ 규칙 — **맞히지 않고 받아서 해석**하는 값이다.
     ⚠ 받은 값을 그냥 되읊는 건 해석이 아니다(키 179 → "7cm 크다"). 명식과 교차해 **유저가 모르던 걸** 내놓거나, 안 쓴다. */
  married = null, kids = null, timeAcc = null, metAge = null } = {}) {
  if (!saju || !saju.idx || !birth || !birth.y) return null;
  const idx = saju.idx, counts = saju.counts;
  const notes = [];
  const fn = (t) => { notes.push(t); return notes.length; };   // 각주 등록 → 번호

  /* ── 하늘 값 ── */
  const noH = idx.hG == null;
  const jd = jdFromKST(+birth.y, +birth.m, +birth.d, noH ? 12 : +birth.h, noH ? 0 : (+birth.min || 0));
  const asc = ascendant(jd, lat, lon), ascSign = signOf(asc);
  const sunH = wholeSignHouse(sunLongitude(jd), asc);
  const sun = sunSign(jd), moon = moonSign(jd);
  const day = isDayBirth(jd, lat, lon);
  const age = HANGUL_AGE(+birth.y, now), ageFull = age - 1;
  const dasha = vimshottari(jd, +birth.y, 7);

  /* ── 십성 무리 세기 ── */
  const ss = {};
  const put = (g) => { const t = ssOf(idx.dG, g); ss[t] = (ss[t] || 0) + 1; };
  [idx.yG, idx.mG].forEach(put); if (idx.hG != null) put(idx.hG);
  [idx.yJ, idx.mJ, idx.dJ].forEach((j) => put(JB[j])); if (idx.hJ != null) put(JB[idx.hJ]);
  const G = { 비겁: 0, 식상: 0, 재성: 0, 관성: 0, 인성: 0 };
  for (const k in ss) G[GRP_OF[k]] += ss[k];

  const me = ["목", "목", "화", "화", "토", "토", "금", "금", "수", "수"][idx.dG];
  const innerEl = ZO_EL[sun] === ZO_EL[moon] ? ZO_EL[sun] : ZO_EL[sun];   // 해·달이 갈리면 해를 따른다(태생 기질)
  const split = ZO_EL[sun] !== ZO_EL[moon];

  /* ── 축: 가장 빈 자리 ── */
  const order = ["식상", "재성", "관성", "인성", "비겁"];
  const minV = Math.min(...order.map((k) => G[k]));
  const blockKey = order.find((k) => G[k] === minV);
  const maxV = Math.max(...order.map((k) => G[k]));
  const thickKey = order.find((k) => G[k] === maxV);
  const lackEl = Object.keys(counts).filter((k) => counts[k] === 0);
  const maxEl = Object.entries(counts).sort((a, b) => b[1] - a[1])[0];

  /* ── 흐름: 대운 · 다샤 · 프로펙션 ── */
  const bands = (ladder || []).filter((du) => gIdx(du.ganji[0]) >= 0 && jIdx(du.ganji[1]) >= 0).map((du) => {
    const s = ssOf(idx.dG, gIdx(du.ganji[0]));
    const d = dasha.periods.find((p) => du.startAge - 1 >= p.from && du.startAge - 1 < p.to);
    /* 유년 구간(끝나는 나이가 열여덟 미만)에는 어른의 사건을 쓰지 않는다 */
    const young = du.endAge < 18;
    return { from: du.startAge, to: du.endAge, ss: s, el: du.el, young,
      title: young ? "자라는 열 해" : SS_BAND[s], event: young ? SS_CHILD[s] : SS_EVENT[s],
      dasha: d ? d.lord : null, dashaKo: d ? dashaWord(d.lord, du.startAge) : null, ganji: du.ganji };
  });
  const cur = bands.find((b) => age >= b.from && age <= b.to) || null;

  /* ── 짝: 세 셈이 만나는 나이 ── */
  const spouseSS = ssOf(idx.dG, JB[idx.dJ]);
  const h7Sign = signOf((asc + 180) % 360);
  const h7 = H7[h7Sign] || H7.천칭자리;
  const profAges = [...Array(70)].map((_, i) => i).filter((a) => profection(asc, a).house === 7);
  const jupiter = dasha.periods.find((p) => p.lord === "목성");
  const relief = bands.find((b) => b.ss === "정재" || b.ss === "편재");
  /* 짝의 시기 — 혼인 가능 구간(22~45세) 안에 걸치는 후보만 센다.
     처음엔 그냥 "목성 시기 시작"을 후보로 썼는데, 사람에 따라 그게 예순여섯에 오기도 한다.
     그걸 평균에 넣으니 후보 폭이 36년·53년까지 벌어졌다(검사가 잡았다). 범위 밖은 후보가 아니다.
     그리고 겹칠 때만 "같은 때를 가리킨다"고 말한다 — 흩어진 걸 평균 내면 합의가 아니라 눈속임이다.

     ⚠ **서양 축을 뺐다(2026-08-14).** 프로펙션 7하우스는 나이 % 12 == 6, 즉 **누구나 6·18·30·42세**다.
        상승궁이 무엇이든 값이 같다 — 개인차가 0인 격자를 "세 번째 증언"이라고 세고 있었다.
        게다가 최종 답을 그 격자에 스냅시켜서, 200명을 돌리면 **148명이 '30세'**로 나왔다(실측).
        상수를 표로 세면 합의가 아니라 정족수 조작이다. 개인차가 있는 두 축만 센다. */
  const LO = 22, HI = 45;
  const overlaps = (f, t) => t > LO && f < HI;
  const mateWhen = (() => {
    const c = [];
    /* 인도 — 금성(짝)과 목성(확장)이 이 구간에 걸치면 그 시작을 후보로 */
    for (const lord of ["금성", "목성"]) {
      const d = dasha.periods.find((x) => x.lord === lord && overlaps(x.from, x.to));
      if (d) { c.push({ k: "남쪽", a: Math.round(Math.max(d.from, LO)), why: `${lord} 시기 ${d.from}~${d.to}세` }); break; }
    }
    /* 동아시아 — 남자는 재물의 자리, 여자는 자리·책임의 자리가 짝을 맡는다 */
    const want = sex === "F" ? ["정관", "편관"] : ["정재", "편재"];
    for (const band of bands.filter((b) => want.includes(b.ss) && overlaps(b.from, b.to))) {
      c.push({ k: "동쪽", a: Math.max(band.from, LO), why: `${band.from}~${band.to}세 구간` });
    }
    if (!c.length) return { age: null, agree: false, spread: null, cands: c };
    const lo = Math.min(...c.map((x) => x.a)), hi = Math.max(...c.map((x) => x.a));
    /* 후보 중 하나를 그대로 쓴다 — 평균을 내면 아무 셈도 가리키지 않은 나이가 답이 된다 */
    const age = c.length === 1 ? c[0].a : Math.round(c.reduce((t, x) => t + x.a, 0) / c.length);
    return { age, agree: c.length >= 2 && hi - lo <= 8, spread: hi - lo, lo, hi, cands: c, only: c.length === 1 };
  })();
  const mateAge = metAge != null ? +metAge : mateWhen.age;
  /* 배우자 자리를 정면으로 치는 구간 = 관계가 흔들리는 때.
     **어린 나이는 뺀다** — "세 살에 만나는 사람은 오래 못 간다"가 화면에 실제로 나왔다.
     그리고 짝을 만나기 전인지 뒤인지로 갈라야 뜻이 달라진다(전=지나갈 인연, 후=흔들리는 결혼). */
  const chung = (idx.dJ + 6) % 12, hyeong = [(idx.dJ + 3) % 12, (idx.dJ + 9) % 12];
  const shakeAll = bands.filter((b) => { const j = jIdx(b.ganji[1]); return (j === chung || hyeong.includes(j)) && b.to >= 18; });
  const shakeBefore = shakeAll.filter((b) => mateAge == null || b.from < mateAge);
  const shakeAfter = shakeAll.filter((b) => mateAge != null && b.from >= mateAge);

  /* ── 몸: 어디가 약한가 ─────────────────────────────────────────────────
     v115 는 **빈 오행 하나**만 보고 "평생 약한 곳"을 못 박았다. 그게 틀렸다.
     실사용 제보(창업자, 2026-08-14): 목이 0개라 "간·눈·힘줄"이라고 적었는데 실제 병력은
     폐렴·충수염·당뇨 — 전부 금·토 계열이었다. **넘치는 쪽을 안 봐서 생긴 오답이다.**
     명리 건강론에서 병은 태과·불급 **양쪽**에서 오고, 극을 받아 상하는 자리도 따로 본다.
     세 축을 다 계산하고 표가 겹치는 곳을 먼저 적되, **갈리면 갈린다고 쓴다**(짝의 시기와 같은 원칙). */
  const beaten = { 목: "토", 화: "금", 토: "수", 금: "목", 수: "화" };
  const minEnt = Object.entries(counts).sort((a, b) => a[1] - b[1])[0];
  const axes = [];
  axes.push({ el: lackEl.length ? lackEl[0] : minEnt[0], k: "비어서",
    why: lackEl.length ? `${lackEl[0]} 0개 — 여덟 글자에 한 자도 없다(불급)` : `${minEnt[0]} ${minEnt[1]}개로 가장 얇다(불급)` });
  if (maxEl[1] >= 3) axes.push({ el: maxEl[0], k: "넘쳐서", why: `${maxEl[0]} ${maxEl[1]}개(태과) — 넘치는 것도 병이 된다` });
  if (maxEl[1] >= 3 && (counts[beaten[maxEl[0]]] || 0) <= 1)
    axes.push({ el: beaten[maxEl[0]], k: "눌려서", why: `가장 센 ${maxEl[0]}(${maxEl[1]}개)이 ${beaten[maxEl[0]]}을 친다(피극)` });
  /* ⚠ **독립이 아닌 축은 표를 더하지 않는다.** 피극("금이 목을 친다")과 불급("목이 0개")은
     같은 사실을 두 번 말한 것이지 증거 둘이 아니다. 그냥 세면 목이 2표가 되어 "합의"로 굳고,
     정작 넘치는 금(폐·기관지)은 본문에서 사라진다 — 실사용 오답이 정확히 이 경로로 났다.
     짝의 시기에서 상수였던 프로펙션을 증언으로 셌던 것과 같은 종류의 실수다. */
  const tally = {};
  for (const a of axes) { if (a.k === "눌려서" && tally[a.el]) continue; tally[a.el] = (tally[a.el] || 0) + 1; }
  const ranked = Object.entries(tally).sort((a, b) => b[1] - a[1]);
  const weakEl = [ranked[0][0]];
  const healthAgree = ranked[0][1] >= 2;
  const uniq = [...new Set(axes.map((a) => a.el))];
  /* 같은 자리를 두 축이 가리키면 문장에서도 한 번만 부른다 — "간과 눈은 비어서, 간과 눈은 눌려서"가 실제로 나왔다 */
  const axGrp = uniq.map((e) => ({ el: e, short: ORGAN[e].part.split(",")[0], ks: axes.filter((x) => x.el === e).map((x) => x.k) }));
  const axSent = axGrp.map((g) => `<b>${EUN(g.short)}</b> ${g.ks.join("·")}`).join(", ") + " 약해";

  /* ⚠ 이 자리는 **겉모습만** 다룬다. 건강은 아래 「몸 — 어디가 약하게 왔나」 4단이 맡는다.
     v117 은 둘 다에서 '평생 약한 곳'을 썼고, 창업자가 "생김새와 몸은 왜 두 번 써줘"라고 지적했다.
     같은 내용을 두 번 쓰면 두 번째가 첫 번째를 무효로 만든다 — 자리를 하나로 정한다. */
  const body = [];
  body.push(
    ["몸", `<b>${BUILD[me]}</b>${jong(BUILD[me]) ? "이야" : "야"}.${(maxEl[1] >= 3 && (maxEl[0] === "토" || maxEl[0] === "수")) ? " 살이 잘 붙어. 평생 관리해야 할 몸이야." : ""}`,
      fn(`일간 오행 ${me} 의 체상. 가장 많은 기운은 ${maxEl[0]} ${maxEl[1]}개 — ${maxEl[0] === "토" || maxEl[0] === "수" ? "부기·살집으로 본다" : "체형에 큰 영향은 없다"}.`)],
    ["얼굴", FACE[me], fn(`일간 오행 ${me} 의 상(相).`)],
    ["목소리", `${VOICE[me]}.${G.식상 === 0 ? " 말수가 적어서 더 그렇게 들려." : ""}`,
      fn(`일간 오행 ${me} + 표현을 맡은 자리 ${G.식상}개.`)],
  );


  /* ── 짝 ─────────────────────────────────────────────────────────────
     **기혼과 미혼에 같은 문서를 주면 안 된다.** 미혼에게 배우자 외모·집안·벌이는 예언이지만,
     기혼에게는 이미 답을 아는 문제다 — 맞혀도 소득 없고 틀리면 문서 전체가 죽는다(§⑤).
     그래서 기혼이면 **예측을 걷어내고 관계 해석으로 바꾼다**: 무엇을 보고 끌렸나, 어디서 부딪히나,
     언제 흔들리나. 앞의 둘은 이미 일어난 일의 설명이라 반증되지 않고, 셋째는 아직 안 온 일이다. */
  let mate = null, mateMode = null;
  if (sex && married === true) {
    const sp = SPOUSE[spouseSS];
    mateMode = "wed";
    const metBand = metAge != null ? bands.find((b) => +metAge >= b.from && +metAge <= b.to) : null;
    mate = [
      ["네가 끌린 자리", `너는 <b>${sp.w}</b>에게 끌리게 되어 있어. 취향이 아니라 <b>구조야</b> — 몇 번을 다시 골라도 결국 그 결로 가.`,
        fn(`일지(배우자 자리)에 앉은 것이 ${spouseSS}. 기혼 입력이 있어 외모·집안·벌이 예측은 걷어냈다 — 유저가 이미 아는 값이다.`)],
      ["잘 되는 지점", `<b>${sp.g}</b> 두 사람이 처음에 서로를 알아본 지점이고, 지금도 그 자리는 남아 있어. 흔들릴 때 돌아갈 곳도 여기야.`,
        fn(`${spouseSS}의 순기능.`)],
      ["부딪히는 지점", `<b>${sp.b}</b> 반복되는 싸움이 있다면 대개 이 한 가지가 옷을 갈아입고 나오는 거야. <b>매번 다른 일로 싸우는 것 같아도 원인은 하나야.</b>`,
        fn(`${spouseSS}의 역기능. 부부 갈등은 새 원인이 아니라 같은 축의 재발로 본다.`)],
      ["네가 조심할 것", G.식상 === 0
        ? "<b>밖에서 참은 걸 집에서 풀어.</b> 네게 말이 나갈 문이 얇아서, 눌린 압력이 가장 가까운 사람한테 가. 미리 알면 반은 막혀."
        : `<b>${["정재", "정관", "정인"].includes(spouseSS) ? "붙어 있으려다 숨 쉴 틈을 안 남긴다" : "각자 몫을 나누다 어느 순간 남처럼 산다"}.</b> 네 구조가 기울면 그쪽으로 기울어.`,
        fn(`${spouseSS} + 표현을 맡은 자리 ${G.식상}개. 표현이 막히면 압력이 가장 가까운 자리로 향한다.`)],
      ...(metBand ? [["만난 때", `<b>${metAge}세 — ${metBand.title}</b>였어. ${dot(metBand.event)} 사람이 먼저 온 게 아니라 <b>그 시기가 사람을 데려온 거야.</b>`,
        fn(`유저 입력 ${metAge}세 → ${metBand.from}~${metBand.to}세 구간(${metBand.ss}). 시기를 맞히지 않고 <b>받아서 해석했다</b>.`)]] : []),
      /* 흔들리는 때는 **아직 안 온 것**만 쓴다. 지난 구간을 앞일처럼 적으면 그게 §⑤ 위반이고,
         일흔여덟에 흔들린다는 말은 맞든 틀리든 오늘의 유저에게 아무 쓸모가 없다. */
      ...(() => { const s = shakeAfter.find((x) => x.to >= age && x.from <= 70);
        return [["흔들리는 때", s ? `<b>${Math.max(s.from, age)}~${s.to}세에 한 번 크게 흔들려.</b> 그때 원인은 사람이 아니라 <b>일과 돈</b>이야. 원인을 사람에게서 찾으면 못 풀어.`
          : "<b>낮아.</b> 앞으로 그 자리를 정면으로 치는 구간이 없어. 흔들린다면 흐름 때문이 아니라 둘 사이의 일이라는 뜻이야.",
          fn(s ? `${s.from}~${s.to}세 구간이 배우자 자리를 충 또는 형한다. 이미 지난 구간은 뺐다 — 앞일만 적는다.` : `혼인 이후·현재(${age}세) 이후 구간에 배우자 자리를 치는 글자가 없다.`)]]; })(),
    ];
  } else if (sex) {
    const sp = SPOUSE[spouseSS];
    mateMode = "pre";
    mate = [
      ["인상", `<b>${h7.h >= 2 ? "키가 큰 편" : h7.h <= -1 ? "아담한 편" : "평균 근처"}.</b> ${h7.look}`,
        fn(`짝의 자리가 ${h7Sign} — 상승궁 ${ascSign}의 맞은편이다. 서양 전통에서 이 자리가 배우자의 겉모습을 다룬다. <b>다만 키·체형은 추정이다</b> — 사인별 체상은 유파 편차가 크고 cm 로 환산할 표준은 없다. 기혼자에게는 이 줄을 아예 내지 않는다(이미 아는 값이라 맞혀도 소득이 없고 틀리면 문서가 죽는다).`)],
      ["분위기", h7.air, fn(`${h7Sign}의 성질.${sunH === 7 ? " 게다가 태양이 그 자리에 든다 — 배우자가 사회적으로 드러나는 사람이 된다." : ""}`)],
      ["성격", `<b>${sp.w}</b>${jong(sp.w) ? "이야" : "야"}. ${dot(sp.d)}`, fn(`일지(배우자 자리)에 앉은 것이 ${spouseSS}.`)],
      ["집안", spouseSS === "정재" || spouseSS === "정관" ? "<b>반듯해.</b> 크게 부자는 아닌데 부족하지도 않아. 결혼하고 나서 그 집에서 실질적인 도움을 받아"
        : spouseSS === "편재" || spouseSS === "편관" ? "<b>기복이 있어.</b> 좋을 땐 크게 좋고 아닐 땐 아니야. 처음부터 셈을 분명히 해 두는 게 나아"
        : "<b>평범해.</b> 집안 덕을 크게 보는 쪽은 아니야",
        fn(`${spouseSS}의 성질로 본 처가·시가. 재물의 자리가 내 자리에 앉으면 덕이 있다고 본다.`)],
      ["벌이", spouseSS === "정재" ? "<b>자기 벌이가 확실해.</b> 월급쟁이거나 전문직이야. 크게 버는 쪽이 아니라 끊기지 않는 쪽이야"
        : spouseSS === "편재" ? "<b>버는 폭이 커.</b> 사업이나 성과급 쪽이야. 좋을 때와 아닐 때 차이가 커"
        : "<b>보통이야.</b> 맞벌이가 기본값이 돼",
        fn(`${spouseSS} — 고정 수입인지 변동 수입인지를 가르는 자리다.`)],
      ["어떻게 만나나", sunH === 7 ? "<b>사람들 있는 자리에서 만나.</b> 소개나 일 관계야. 우연히 길에서 마주치는 그림이 아니야"
        : "<b>가까운 데서 만나.</b> 오래 알던 사이거나, 같은 공간에 있던 사람이야",
        fn(`태양이 ${sunH}하우스. 7하우스면 공개된 자리, 아니면 생활 반경 안에서 본다.`)],
      /* 문구가 셈의 개수를 그대로 말한다 — 하나뿐인데 "여럿이 가리킨다"고 쓰면 그게 거짓말이다 */
      ["언제 만나나",
        !mateAge ? "혼인 구간에 걸리는 값이 없어서 <b>못 짚어.</b> 없는 걸 지어내느니 비워 두는 게 나아"
          : mateWhen.only ? `<b>${mateAge}세 전후야.</b> 다만 이걸 가리키는 셈이 <b>하나뿐</b>이라 넉넉히 봐야 해`
            : mateWhen.agree ? `<b>${mateAge}세 전후야.</b> 두 셈이 ${mateWhen.spread === 0 ? "같은 해를" : `${mateWhen.spread}년 안에서`} 겹쳐`
              : `<b>${mateWhen.lo}세에서 ${mateWhen.hi}세 사이야.</b> 두 셈이 갈려서 한 해로 못 좁혀 — 그중 <b>${mateAge}세</b>가 가장 유력해`,
        fn(!mateAge ? `22~45세에 걸치는 후보가 0개.`
          : `${mateWhen.cands.map((x) => `${x.k} ${x.a}세(${x.why})`).join(" · ")} → 폭 ${mateWhen.spread}년. ` +
            `${mateWhen.only ? "후보가 <b>하나</b>라 합의라고 쓰지 않았다" : mateWhen.agree ? "8년 이내라 <b>합의</b>로 봤다" : "8년을 넘어 <b>갈린다고 표시</b>했다"}. ` +
            `<b>서양 축은 뺐다</b> — 프로펙션 7하우스는 누구나 6·18·30·42세라 개인차가 0인 격자다(v115 까지 이걸 세 번째 증언으로 세는 바람에 200명 중 148명이 '30세'로 나왔다).`)],
      ["그전에 오는 인연", shakeBefore.length ? `<b>${shakeBefore[0].from}~${shakeBefore[0].to}세에 만나는 사람은 오래 못 가.</b> 그 사람이 나빠서가 아니라 그때 그 자리가 흔들리게 되어 있어` : "짝을 만나기 전에 크게 흔들리는 구간은 없어",
        fn(shakeBefore.length ? `${shakeBefore[0].from}~${shakeBefore[0].to}세 구간의 글자가 배우자 자리를 충 또는 형한다. 열여덟 미만 구간은 인연으로 세지 않는다.` : "짝을 만나기 전(18세~) 구간에는 배우자 자리를 치는 글자가 없다.")],
      ["결혼 후", `<b>${["정재", "정관", "정인"].includes(spouseSS) ? "붙어 사는 부부가 돼" : "각자 몫이 분명한 부부가 돼"}.</b> ` +
        (G.식상 === 0 ? "위험은 하나야 — <b>밖에서 참은 걸 집에서 풀어.</b> 미리 알면 반은 막혀" : "크게 부딪히는 구조는 아니야"),
        fn(`${spouseSS} + 표현을 맡은 자리 ${G.식상}개. 표현이 막히면 압력이 가장 가까운 자리로 향한다.`)],
      ["갈라설 위험", shakeAfter.length ? `<b>${shakeAfter[0].from}~${shakeAfter[0].to}세에 한 번 크게 흔들려.</b> 그때 원인은 사람이 아니라 일과 돈이야` : "<b>낮아.</b> 결혼한 뒤로 그 자리를 정면으로 치는 구간이 없어",
        fn(shakeAfter.length ? `${shakeAfter[0].from}~${shakeAfter[0].to}세 구간이 배우자 자리를 다시 친다.` : "짝을 만난 뒤로 배우자 자리를 치는 구간이 여든까지 없다.")],
    ];
  }

  /* ── 뒤집히는 조건 ── */
  const dark = bands.filter((b) => ["편관", "정관", "겁재"].includes(b.ss));
  const trig = [
    { t: BLOCK[blockKey].trig, w: `${dot(BLOCK[blockKey].w)} 신호는 이래 — 말수가 줄고, 대답이 짧아지고, 혼자 있으려 해. 그 다음이 폭발이야.`,
      n: fn(`가장 빈 자리가 ${blockKey} ${G[blockKey]}개. 없는 자리가 압력이 쌓이는 자리가 된다.`) },
    { t: "여럿이 네 자리를 정할 때", w: "누가 시키는 건 참아. 여럿이 네 몫을 대신 정하는 건 못 참아. 가만있다가 갑자기 판을 엎고, 그러고 나서 자기를 더 미워해.",
      n: fn(`나를 누르는 자리(관성) ${G.관성}개${dark.length ? ` + ${dark[0].from}~${dark[0].to}세 구간이 그 성질` : ""}.`) },
    { t: "몸이 처질 때", w: "마음보다 몸이 먼저 무너져. 몸이 무너지면 성격이 바뀌어. 느긋하던 게 없어지고 짜증이 앞서.",
      n: fn(`${weakEl[0]} 자리가 얇아 계절이 바뀔 때 먼저 신호가 온다.`) },
  ];

  /* ── 해와 달 ── */
  const yong = G.비겁 + G.인성 >= 4 ? [{ 목: "화", 화: "토", 토: "금", 금: "수", 수: "목" }[me], { 목: "토", 화: "금", 토: "수", 금: "목", 수: "화" }[me]]
    : [{ 화: "목", 토: "화", 금: "토", 수: "금", 목: "수" }[me], me];
  const gi = ["목", "화", "토", "금", "수"].filter((e) => !yong.includes(e)).slice(0, 2);
  const yrs = seun(idx.dG, now.getFullYear(), 6).map((x) => ({ ...x, f: favor(x.el, yong, gi) }));
  const mos = wolun(idx.dG, idx.yG, now.getFullYear()).map((x) => ({ ...x, f: favor(x.el, yong, gi), m: +x.start.slice(5, 7) }));
  const hardMonths = mos.filter((x) => x.f < 0).map((x) => x.m);
  const softMonths = mos.filter((x) => x.f > 0).map((x) => x.m);

  /* ── 아홉 자리 × 4단 ────────────────────────────────────────────────────
     "태어날 때 이랬다 → 자라며 이렇게 나타났다 → 지금 어디다 → 앞으로 이렇게 된다".
     지난 일을 앞일처럼 쓰지 않는다 — married/kids 가 있으면 그 자리를 과거형으로 돌린다. */
  const bandAt = (a) => bands.find((b) => a >= b.from && a <= b.to) || null;
  const nextOfGrp = (g) => bands.find((b) => b.from > age && GRP_OF[b.ss] === g) || null;
  /* ⚠ off 는 **자리마다 완전히 다른 문장**이어야 한다.
     v116 판결문에서 "지금 열 해(25~34세)는 다른 쪽에 쏠려 있어."가 **다섯 자리에 그대로 반복**됐다.
     같은 문장이 다섯 번 나오면 읽는 사람은 이게 조립품이라는 걸 즉시 안다. 공통 앞머리를 없앤다. */
  const nowLine = (g, on, off) =>
    !bands.length ? "성별이 없어서 지금이 어느 열 해인지 못 짚어"
      : !cur ? `아직 첫 열 해가 시작되기 전이야. ${bands[0].from}세부터 큰 흐름이 돌기 시작해`
        : GRP_OF[cur.ss] === g ? `<b>${cur.from}~${cur.to}세 — 지금이 바로 그 열 해야.</b> ${on}`
          : off;
  const nextLine = (g, none) => {
    if (!bands.length) return "흐름의 방향이 안 서서 못 펼쳤어";
    const d = nextOfGrp(g);
    return d ? `<b>${d.from}~${d.to}세</b> — ${d.title}가 와. ${d.event}` : none;
  };
  const young = age < 14;
  const D = [];
  const putD = (k, t, a, b2, c, d, n) => D.push({ k, t, steps: [["새겨질 때", a], ["자라면서", b2], ["지금", c], ["앞으로", d]], n });

  /* 몸 — **한 자리로 못 박지 않는다.** 축이 셋(비어서·넘쳐서·눌려서)이고, 겹칠 때만 한 곳이라고 쓴다.
     보충의 방향도 축마다 반대다: 비어서 약한 자리는 채워 주는 열 해가 도움이지만,
     넘쳐서 약한 자리는 같은 기운이 또 들어오면 **더 나빠진다.** v115 는 둘을 구분 없이 "채워 줘"라고 썼다. */
  const mainAxis = axes.find((a) => a.el === weakEl[0]) || axes[0];
  const emptySide = mainAxis.k !== "넘쳐서";
  putD("몸", "몸 — 어디가 약하게 왔나",
    healthAgree || uniq.length === 1
      ? `<b>${ORGAN[weakEl[0]].part}</b>${jong(ORGAN[weakEl[0]].part) ? "이야" : "야"}. ${mainAxis.k === "비어서" ? (lackEl.length ? "있어야 할 것이 아예 안 들어 있어." : "다른 자리보다 눈에 띄게 얇아.") : mainAxis.k === "넘쳐서" ? "모자라서가 아니라 <b>넘쳐서</b> 약해 — 과열되는 쪽이야." : "가장 센 기운에게 <b>눌려서</b> 상하는 자리야."}`
      : `<b>한 곳이 아니야.</b> ${axSent} — 원인이 반대라 <b>대처도 반대야.</b> 비어서 약한 자리는 채워야 하고, 넘쳐서 약한 자리는 덜어내야 해. 같은 방법을 둘 다에 쓰면 한쪽이 더 나빠져.`,
    /* 축이 갈리면 증상도 갈린다 — 한쪽 증상만 적으면 나머지 자리는 이름만 부르고 마는 셈이 된다 */
    `어릴 때부터 ${dot(ORGAN[weakEl[0]].sym)}` +
      (uniq.length > 1 ? ` 그리고 ${dot(axGrp.find((g) => g.el !== weakEl[0]).ks.includes("넘쳐서") ? ORGAN[axGrp.find((g) => g.el !== weakEl[0]).el].over : ORGAN[axGrp.find((g) => g.el !== weakEl[0]).el].sym)}` : "") +
      " 크게 아프진 않은데 잔병이 안 끊기는 쪽이야.",
    (() => { const c2 = cur; if (!c2) return "아직 첫 열 해가 시작되기 전이야";
      if (c2.el === weakEl[0]) return emptySide
        ? `<b>${c2.from}~${c2.to}세 — 지금 열 해가 그 자리를 채워 줘.</b> 몸으로는 가장 수월한 구간이야`
        : `<b>${c2.from}~${c2.to}세 — 지금 열 해가 그 자리에 기운을 더 부어.</b> 이미 넘치는 쪽이라 <b>이 구간이 제일 조심할 때야</b>`;
      return `지금 열 해(${c2.from}~${c2.to}세)는 이 자리와 직접 관계는 없어. 관리는 계속 필요해`; })(),
    (() => { const f = bands.find((b) => b.from > age && b.el === weakEl[0]);
      return f ? (emptySide ? `<b>${f.from}~${f.to}세</b>부터 그 기운이 들어와 — 그 열 해에 몸이 한 단계 편해져`
        : `<b>${f.from}~${f.to}세</b>에 그 기운이 또 들어와 — 넘치는 쪽이라 <b>그때 한 번 크게 앓을 수 있어</b>. 미리 알면 대비가 돼`)
        : "여든까지 그 기운이 오가는 열 해는 없어. <b>흐름을 기다릴 자리가 아니라 평생 관리할 자리</b>라는 뜻이야"; })(),
    fn(`축 ${axes.length}개 — ${axes.map((a) => a.why).join(" / ")}. ${healthAgree ? `${weakEl[0]}이 겹쳐 한 곳으로 봤다` : "겹치는 자리가 없어 전부 적었다"}.`));

  putD("마음", "마음 — 어떤 사람으로 자라나",
    `${SURFACE[me].w}으로 왔어. ${SURFACE[me].d}.`,
    `${INNER[innerEl].d}. ${split ? "<b>겉과 속이 다른 사람</b>이라, 남들이 네 속을 자주 오해해." : "겉과 속이 같은 방향이라 오해는 덜 받아."}`,
    cur ? `<b>${cur.from}~${cur.to}세 — ${cur.title}</b>야. ${cur.dashaKo ? cur.dashaKo + "이기도 해." : ""}` : "아직 첫 열 해가 시작되기 전이야",
    (() => { const d = bands.find((b) => b.from > age); return d ? `<b>${d.from}세부터 사는 방식이 바뀌어</b> — ${d.from}~${d.to}세는 ${bandName(d.title)}야. ${dot(d.event)}` : "여든까지의 흐름은 아래 지도에 다 펼쳐 뒀어"; })(),
    fn(`일간 ${GANK[idx.dG]}(${me}) + 서양 해자리 ${sun}·달자리 ${moon}${split ? " — 원소가 갈린다" : ""}.`));

  /* ⚠ **"자리가 N개"라고 쓰지 않는다.**(창업자 2026-08-14: "직관적인 말도 빙빙 둘러서
     무슨 그릇이 어떻니 저떻니 그래… 신의 컨셉은 말을 모호하게 하란 게 아니고 말투나 비주얼을 말한 거야")
     유저는 3개가 많은지 적은지 모르고, 안다 해도 뭘 해야 할지 모른다.
     은유를 서술어로 쓰지 않는다 — 그릇 · 문 · 결 · 자리 · 팔 힘.
     대신 **① 실제로 벌어지는 상황 ② 그때 네가 하는 행동 ③ 그래서 생기는 결과** 순으로 쓴다.
     상담실과 철학관은 둘 다 단정적으로 말한다. 모호한 건 신비가 아니라 회피다. */
  putD("말", `${BLOCK[blockKey].t} — 네게 가장 약한 자리`,
    `<b>${dot(BLOCK[blockKey].plain)}</b> ${dot(BLOCK[blockKey].s)} 태어날 때부터 그랬어.`,
    `${dot(BLOCK[blockKey].w)}`,
    nowLine(blockKey, "<b>지금이 이걸 고칠 수 있는 때야.</b> 이 열 해가 지나면 다시 어려워져",
      `<b>지금은 이걸 고칠 때가 아니야.</b> ${cur ? bandName(cur.title) : "다른 쪽"}에 힘이 다 쏠려 있어서, 무리해서 고치려 들면 다른 데가 어긋나. 지금은 그냥 알고만 있어`),
    nextLine(blockKey, `여든까지 <b>이게 저절로 나아지는 때는 안 와.</b> 시간이 해결해 주지 않는다는 뜻이야. 네가 직접 해야 해 — ${BLOCK[blockKey].fix}`),
    fn(`가장 빈 자리 ${blockKey} ${G[blockKey]}개. 없는 것이 있는 것보다 많은 걸 정한다.`));

  putD("공부", "공부 — 어떻게 배워야 남나",
    G.인성 >= 3 ? "<b>책상에 앉아서 배우는 게 맞는 사람이야.</b> 자격증·학위·수료증처럼 종이로 남는 게 실제로 네 값을 올려 줘. 부딪히면서 배우는 것보다 <b>먼저 배우고 시작하는 쪽</b>이 결과가 좋아"
      : G.인성 === 0 ? "<b>앉아서 외우는 건 안 맞아.</b> 일단 해보고 깨지면서 배우는 사람이야. 강의 결제해 놓고 안 듣는 일이 반복됐다면 게을러서가 아니라 <b>방식이 안 맞아서</b>야"
        : "<b>둘 다 되는 편이야.</b> 배워서도 익히고 해보면서도 익혀. 한쪽으로 안 치우쳐서 어떤 일에 들어가도 적응은 해",
    G.인성 >= 3 ? "설명해 주면 빨리 알아들었을 거야. 대신 <b>다 알기 전엔 시작을 못 해서</b> 남보다 출발이 늦었어"
      : "가만히 앉혀 두면 오래 못 갔을 거야. <b>만들면서 배우는 게 네 방식</b>이야",
    nowLine("인성", "<b>지금 배우면 남아.</b> 미뤄 둔 시험·자격증·학위가 있다면 지금이 제일 잘 붙는 때야",
      "<b>지금은 배우는 것보다 하는 게 남아.</b> 긴 과정에 등록하면 중간에 놓기 쉬워. 필요한 것만 짧게 끊어서 사"),
    nextLine("인성", "여든까지 <b>배움이 저절로 붙는 때는 안 와.</b> 필요하면 그때그때 돈 주고 사야 해. 때를 기다리지 마"),
    fn(`배움을 맡은 자리 ${G.인성}개.`));

  putD("일", "일 — 어떤 일이 맞나",
    `<b>${THICK[thickKey].job}</b>이 맞아. 구체적으로는 <b>${THICK[thickKey].ex}</b> 쪽이야.`,
    `${dot(THICK[thickKey].grew)}`,
    nowLine(thickKey, "<b>지금은 네 방식이 그대로 먹히는 때야.</b> 옮기거나 벌일 거면 지금이야",
      "<b>지금은 네 방식이 잘 안 먹혀.</b> 밀어붙이면 부딪히기만 해. 이 시기에 남는 건 성과가 아니라 <b>배운 것</b>이야"),
    nextLine(thickKey, "여든까지 <b>네 방식이 판을 주도하는 때는 다시 안 와.</b> 자리를 옮겨 다니지 말고 <b>지금 있는 데서 깊이 파는 게</b> 남아"),
    fn(`가장 두꺼운 자리 ${thickKey} ${G[thickKey]}개.`));

  {
    const jd2 = G.재성, weakRich = jd2 >= 3 && (G.비겁 + G.인성) <= 2;
    const money = bands.filter((b) => GRP_OF[b.ss] === "재성" && !b.young);   // 유년 구간은 돈의 구간이 아니다
    putD("돈", "돈 — 어떻게 벌고 어디서 새나",
      weakRich ? "<b>너는 돈 벌 기회를 남보다 많이 만나. 그런데 그걸 감당할 체력과 사람이 부족해.</b> 큰 건이 눈앞에 와도 혼자 다 하려다 몸이 상하거나, 같이 하던 사람과 틀어지면서 놓쳐"
        : jd2 === 0 ? "<b>월급처럼 정해진 돈이 맞고, 굴리는 돈은 새.</b> 못 번다는 게 아니야 — 투자·사업처럼 변동이 큰 돈은 손에 안 남는다는 뜻이야"
          : jd2 >= 3 ? "<b>크게 들어오고 크게 나가는 사람이야.</b> 월급만 받고 살기엔 답답하고, 실제로 판이 열리면 남보다 크게 받아"
            : "<b>크게 터지지도, 끊기지도 않아.</b> 남들이 볼 때 '늘 적당히 있는 사람'이야",
      weakRich ? "큰돈이 눈앞을 지나가는 걸 여러 번 봤을 거야. 잡으려다 <b>몸이 먼저 무너지거나 같이 하던 사람과 틀어진 적</b>도 있고. <b>능력이 모자라서가 아니라 혼자 다 하려고 해서야</b>"
        : jd2 === 0 ? "통장에 남는 게 일한 것보다 늘 적었을 거야. <b>큰 판보다 꼬박꼬박이 네 방식</b>이야"
          : "쓸 만큼은 들어왔고, 급할 때 어디선가 생기는 편이었을 거야",
      nowLine("재성", "<b>지금 돈이 실제로 움직여.</b> 계약·거래·목돈이 이 구간에 몰려 있어",
        "<b>지금은 버는 것보다 지키는 때야.</b> 무리해서 굴리면 새"),
      money.length ? (() => { const f = money.filter((b) => b.to > age);
        return f.length ? `<b>${f[0].from}~${f[f.length - 1].to}세가 네 돈의 시간</b>이야. ${f.map((b) => `${b.from}~${b.to}세는 ${bandName(b.title)}`).join(", ")}. 이때 벌어 둔 걸로 나머지를 살아`
          : `돈이 크게 도는 시기는 <b>${money[0].from}~${money[money.length - 1].to}세로 이미 지나갔어.</b> 앞으로는 새로 버는 것보다 <b>있는 걸 안 잃는 쪽</b>이 맞아`; })()
        : "여든까지 <b>큰돈이 도는 시기가 없어.</b> 한 방을 기다리지 말고 <b>고정 수입을 두껍게 하는 게</b> 네 정답이야",
      fn(`재물 자리 ${jd2}개 · 나를 받치는 힘 ${G.비겁 + G.인성}개.${weakRich ? " 재다신약 배치." : ""}`));
  }

  putD("사람", "사람 — 곁에 누가 남나",
    G.비겁 === 0 ? "<b>여럿이 하는 걸 못 견디는 사람이야.</b> 회의가 길어지면 속으로 '내가 하고 말지' 하고, 실제로 그렇게 해. 그래서 일은 되는데 <b>사람이 안 남아</b>"
      : G.비겁 >= 3 ? "<b>곁에 사람이 끊이지 않아.</b> 대신 나눌 때 네 몫이 제일 작고, <b>빌려준 돈이 안 돌아온 적</b>이 한 번은 있어"
        : "<b>친구가 많진 않은데 오래 가.</b> 무리의 중심에 서진 않지만, 정작 일이 생기면 다들 너한테 물어봐",
    "먼저 다가가는 일은 드물었을 거야. 그래서 손해 보는 데가 딱 여기야 — <b>부탁을 못 해서 결국 네가 다 져.</b>",
    nowLine("비겁", "<b>지금 사람이 몰려.</b> 동업·팀·독립 이야기가 이 구간에 나와",
      "<b>지금은 사람보다 네 일이 먼저야.</b> 사람은 그 다음에 따라와"),
    nextLine("비겁", "여든까지 <b>사람이 몰리는 때는 안 와.</b> 혼자 가는 게 기본값인 삶이야 — 나쁜 게 아니라 <b>미리 계산에 넣으라는 말</b>이야"),
    fn(`같이 가는 자리 ${G.비겁}개.`));

  if (sex) {
    const g2 = sex === "F" ? "관성" : "재성";
    putD("연애", "연애 — 인연이 어떻게 오나",
      G[g2] === 0 ? "<b>가만히 있으면 아무 일도 안 생겨.</b> 소개받거나 모임에 나가거나 — <b>네가 자리를 만든 데서만</b> 인연이 나와"
        : G[g2] >= 3 ? "<b>사람이 끊긴 적은 없을 거야.</b> 문제는 늘 고르는 쪽이었어. 정하는 데 오래 걸리고, 정하고 나서도 '저쪽이 나았나' 싶은 마음이 남아"
          : "<b>때 되면 만나고 때 되면 정리되는 편이야.</b> 크게 애태우거나 크게 데인 적은 없어",
      young ? "아직 오지 않았어." : (G[g2] === 0 ? "돌아보면 늘 <b>네가 먼저 움직인 자리</b>에서 생겼을 거야" : "크게 애쓰지 않아도 곁에 사람이 있었을 거야"),
      married === true
        ? (cur && GRP_OF[cur.ss] === g2
            ? `<b>${cur.from}~${cur.to}세 — 지금 이 관계의 온도가 크게 바뀌는 때야.</b> 좋은 쪽으로든 아닌 쪽으로든 움직이니까, 미루던 얘기가 있으면 지금 해`
            : "<b>지금은 관계가 조용한 때야.</b> 밋밋하게 느껴진다면 사이가 식은 게 아니라 <b>둘 다 다른 데 힘을 쓰고 있는 거야</b>")
        : nowLine(g2, "<b>지금 인연이 실제로 움직여.</b>", "<b>지금은 저절로 굴러오는 때가 아니야.</b> 오면 네가 만든 자리에서 와"),
      married === true ? "<b>이미 만났으니 이 얘긴 지나갔어.</b> 앞으로 여기서 갈리는 건 새 인연이 아니라 <b>그 사람과 지내는 온도</b>야"
        : nextLine(g2, "여든까지 <b>저절로 오는 때는 안 와.</b> 기다리지 말고 네가 자리를 만드는 쪽이 맞아"),
      fn(`${sex === "F" ? "여자에게 관성" : "남자에게 재성"}이 인연의 자리. ${G[g2]}개.${married === true ? " 기혼 입력이 있어 '앞으로'를 과거형으로 돌렸다." : ""}`));

    const g3 = sex === "F" ? "식상" : "관성";
    putD("자식", "자식 — 어떻게 오고 어떻게 키우나",
      (G[g3] === 0 ? "<b>늦게 오거나, 애쓴 만큼 와.</b> 안 생긴다는 뜻이 아니라 저절로는 잘 안 온다는 뜻이야"
        : G[g3] >= 3 ? "<b>아이한테 시간과 돈이 많이 들어가는 사람이야.</b> 아이 문제가 네 인생 결정에 계속 끼어들어"
          : "<b>무리 없이 와.</b> 아이 문제로 크게 애먹는 구조는 아니야") + (noH ? " 다만 <b>태어난 시를 몰라</b> 이 자리의 절반은 못 읽었어." : ""),
      young ? "아직 오지 않았어." : kids === false ? "아직 오지 않았어. 조급해할 자리는 아니야."
        : kids === true ? "아이가 오기까지 순탄했든 아니든, <b>그 곡절이 전부 여기 적혀 있어.</b>"
        : "아이 문제로 크게 흔들린 적이 있다면 그게 여기야.",
      kids === true ? "<b>이미 왔어.</b> 지금 물음은 '올까'가 아니라 <b>'어떻게 키우나'</b>야" : nowLine(g3, "<b>지금 아이 일이 실제로 움직여.</b>", "<b>지금은 그쪽보다 다른 게 먼저 움직이는 때야.</b>"),
      kids === true ? `아이와 부딪히는 지점도 똑같아 — <b>${dot(BLOCK[blockKey].plain)}</b> 네게 약한 그게 아이한테도 그대로 가. 미리 알면 반은 막혀`
        : nextLine(g3, "여든까지 <b>그 일이 저절로 움직이는 때는 안 와.</b> 흐름을 기다릴 자리가 아니라는 뜻이야"),
      fn(`${sex === "F" ? "여자에게 식상" : "남자에게 관성"}이 자식의 자리. ${G[g3]}개.${kids != null ? ` 자녀 유무 입력(${kids ? "있음" : "없음"})을 반영했다.` : ""}`));
  }

  /* ── 아홉 하늘의 분업 ────────────────────────────────────────────────
     각 하늘이 **사주가 못 하는 일 하나씩**을 맡는다. 같은 질문에 투표시키지 않는다 —
     투표를 시키면 오행 어휘로 환원돼 결국 사주로 읽힌다(v118 에서 실제로 그랬다). */
  const sky9 = [];
  const putS = (from, ask, val, say, why) => sky9.push({ from, ask, val, say, n: fn(`${from} — ${why}`) });

  /* ① 서양 — 삶을 열두 자리로 쪼갠다. **사주에 없는 축이다.** */
  {
    const moonH = wholeSignHouse(moonLongitude(jd), asc);
    const pofH = wholeSignHouse(partOfFortune(jd, asc, day), asc);
    putS("서양 점성술", "인생의 무게가 어디에 실렸나", `${sunH}번째 자리`,
      `네 삶의 무게는 <b>${HOUSE_KO[sunH]}</b>에 실려 있어. 마음이 쉬는 곳은 <b>${HOUSE_KO[moonH]}</b>이고, 복이 도는 곳은 <b>${HOUSE_KO[pofH]}</b>${jong(HOUSE_KO[pofH]) ? "이야" : "야"}. ` +
      `${sunH === moonH ? "무게와 쉼이 같은 자리라 삶이 단순해. 대신 그 자리가 무너지면 통째로 무너져." : "무게가 실린 곳과 마음이 쉬는 곳이 달라. 그래서 잘 되고 있는데도 헛헛한 때가 있어."}`,
      `상승궁 ${ascSign} 기준 홀사인. 태양 ${sunH}·달 ${moonH}·파트오브포춘 ${pofH}하우스. 삶을 열두 영역으로 쪼개는 축은 사주에 없다. <b>계산 그대로다</b> — 옮긴 말이 없다.`);
  }
  /* ② 인도 — 120년 주기. **사주의 열 해와 마디가 다르다.** 어긋나는 지점이 이 절의 핵심이다 */
  const dashaTurn = dasha.periods.map((p) => Math.round(p.from)).filter((x) => x >= 12 && x <= 75);
  const daeunTurn = bands.map((b) => b.from).filter((x) => x >= 12 && x <= 75);
  const bothTurn = daeunTurn.filter((a2) => dashaTurn.some((d) => Math.abs(d - a2) <= 3));
  const onlyDasha = dashaTurn.filter((d) => !daeunTurn.some((a2) => Math.abs(d - a2) <= 3));
  {
    const nextBoth = bothTurn.find((x) => x > age), nextOnly = onlyDasha.find((x) => x > age);
    putS("인도 점성술", "시간의 마디가 사주와 어디서 어긋나나", `${dashaTurn.length}번 바뀜`,
      (bothTurn.length ? `<b>${bothTurn.join("·")}세</b>에는 두 셈이 같이 바뀌어 — 여기가 <b>진짜 전환기</b>야. 이때 온 변화는 되돌아가지 않아. ` : "두 셈이 같은 해에 바뀌는 지점이 없어. 네 인생은 큰 마디 없이 흘러가는 쪽이야. ") +
      (onlyDasha.length ? `반대로 <b>${onlyDasha.join("·")}세</b>는 사주로는 조용한데 <b>인도 셈만 바뀐다고 해.</b> 겉으로는 아무 일 없는데 속이 뒤집히는 때가 이때야.` : "사주가 조용한데 인도 셈만 바뀌는 해는 없어.") +
      (nextBoth || nextOnly ? ` 다음은 <b>${Math.min(...[nextBoth, nextOnly].filter(Boolean))}세</b>야.` : ""),
      `다샤 전환 ${dashaTurn.join("·")}세 vs 대운 전환 ${daeunTurn.join("·")}세. 인도는 120년을 아홉으로 불균등하게 쪼개고 사주는 열 해로 균등하게 쪼갠다 — <b>주기가 달라서 어긋나는 지점이 생긴다.</b> 이건 사주만으로는 나오지 않는 값이다. <b>계산 그대로다</b> — 두 전환표를 맞대었을 뿐이다.`);
  }
  /* ③ 마야 — 260일력에서 그 날이 맡은 역할. 사주의 일주와 층이 다르다 */
  {
    const tz = tzolkin(jdn(+birth.y, +birth.m, +birth.d));
    const nm = tz.sign.replace(/\(|\)/g, " ").trim().split(" ");
    putS("마야", "태어난 날이 맡은 역할", `${tz.tone}·${tz.sign}`,
      `<b>${TZ_TONE[tz.tone]} ${nm[1] || nm[0]}</b>의 날에 왔어. 마야는 사람을 성격이 아니라 <b>맡은 일</b>로 읽어 — 네가 어떤 사람이냐가 아니라 <b>무엇을 하러 왔느냐</b>야.`,
      `촐킨 ${tz.tone}톤 ${tz.sign}. 260일 신성력의 날 이름과 톤. 톤의 뜻을 우리말로 옮기는 데 <b>해석이 섞인다</b>.`);
  }
  /* ④ 일본 구성기학 — 방위. 사주에는 방위 축이 거의 없다 */
  {
    const hm = honmeisei(+birth.y, +birth.m <= 2).name, g = GU_DIR[hm];
    if (g) putS("일본 구성기학", "어느 쪽으로 움직여야 풀리나", hm,
      `네 별의 자리는 <b>${g[0]}쪽</b>, ${g[1]}야. 막혔다 싶을 때 <b>${g[0]}쪽으로 움직여</b> — 책상 방향, 이사, 출장, 오래 머물 자리. 이건 성격이 아니라 <b>쓰는 법</b>이야.`,
      `본명성 ${hm} — 팔괘 정위 배당 그대로라 <b>변환이 없다</b>. 방위를 실용 지침으로 쓰는 축은 사주에 거의 없다.`);
  }
  /* ⑤ 자바 — 태어난 날의 '무게'를 숫자로 센다 */
  {
    const w = weton(+birth.y, +birth.m, +birth.d);
    const heavy = w.neptu >= 14 ? "무거운" : w.neptu <= 10 ? "가벼운" : "중간";
    putS("자바", "태어난 날이 얼마나 무거운가", `${w.day}·${w.pasaran} ${w.neptu}`,
      `자바는 태어난 날의 무게를 숫자로 세. 네 수는 <b>${w.neptu}</b>, 7에서 18 사이에서 <b>${heavy} 쪽</b>이야. ` +
      `${w.neptu >= 14 ? "무거운 날은 짐이 잘 얹혀. 남들이 너한테 자꾸 기대고, 너도 거절을 못 해." : w.neptu <= 10 ? "가벼운 날은 잘 움직여. 대신 뿌리내리는 데 남보다 오래 걸려." : "치우치지 않아. 어디에 놓아도 무리 없이 굴러가."}`,
      `웨톤 ${w.day}요일 + ${w.pasaran} = 신붕 ${w.neptu}. 자바 전통이 택일·궁합에 실제로 쓰는 수치라 <b>변환이 없다</b>.`);
  }
  /* ⑥ 달 — 차오르는 때 났나 기우는 때 났나. 사주에 없는 축 */
  {
    const mp = moonPhase(+birth.y, +birth.m, +birth.d);
    const rising = mp.age < 14.8;
    putS("달의 위상", "시작에 강한가 마무리에 강한가", mp.name,
      `<b>${mp.name}</b>에 태어났어. ${rising ? "<b>차오르는 달</b>이야 — 벌이는 데 강하고 접는 데 약해. 시작한 게 쌓여만 가고 정리가 안 되면 그건 네 게으름이 아니라 결이야."
        : "<b>기우는 달</b>이야 — 거두고 정리하는 데 강하고, 맨바닥에서 새로 벌이는 건 남보다 힘들어. 이미 있는 걸 고치는 자리에 두면 제일 잘해."}`,
      `삭망 나이 ${mp.age}일 → ${mp.name}. 차오름/기움은 사주에 없는 축이고, 그것을 성향으로 옮기는 데 <b>해석이 섞인다</b>.`);
  }
  /* ⑦ 서아프리카 아칸 — 요일이 곧 이름이자 역할 */
  {
    const ak = akan(+birth.y, +birth.m, +birth.d, sex !== "F");
    putS("서아프리카 아칸", "요일이 준 이름과 역할", ak.name,
      `아칸에서는 태어난 요일이 곧 이름이야. 너는 <b>${ak.name}</b> — "${ak.trait}". 이름이 역할을 정하고, 평생 그 이름으로 불려.`,
      `${ak.day}요일 → ${ak.name}. 아칸 전통이 붙여 둔 이름과 뜻을 그대로 써서 <b>변환이 없다</b>.`);
  }
  /* ⑧ 수비학 — 평생의 과제 한 문장 */
  {
    const lp = lifePath(+birth.y, +birth.m, +birth.d), t = LP_TASK[lp];
    if (t) putS("수비학", "평생 배우는 게 무엇인가", `${lp}번`,
      `네가 평생 배우는 건 <b>${t}</b>${jong(t) ? "이야" : "야"}. 잘해서가 아니라 <b>못해서 계속 배우게 되는 것</b>이야. 같은 종류의 일이 형태만 바꿔 반복되는 이유가 이거야.`,
      `라이프패스 ${lp} — 이 수의 통설 과제를 우리말로 옮기는 데 <b>해석이 섞인다</b>.`);
  }
  /* ⑨ 납음 — 사주 안의 다른 층. 여덟 글자와 갈릴 수 있다 */
  const nayinEl = EL_OF_NAYIN(nayin(+birth.y));
  {
    const ny = nayin(+birth.y);
    putS("동아시아 소리", "여덟 글자와 소리가 같은 말을 하나", ny,
      `태어난 해의 소리는 <b>${ny}</b>, ${EL_KO2[nayinEl]}의 기운이야. ` +
      `${nayinEl === me ? `여덟 글자와 <b>같은 ${EL_KO2[me]}</b>이라 결이 한 방향으로 모여. 네 성질이 뚜렷한 이유야.`
        : `여덟 글자는 <b>${EL_KO2[me]}</b>인데 소리는 <b>${EL_KO2[nayinEl]}</b>야. <b>같은 사주 안에서도 두 층이 갈려</b> — 사람들이 널 한 마디로 못 부르는 이유가 여기 있어.`}`,
      `납음 ${ny}(${nayinEl}) vs 일간 ${GANK[idx.dG]}(${me}). 이름에 오행이 들어 있어 <b>변환이 없다</b>.`);
  }

  /* ── 사주와 어긋나는 곳 ────────────────────────────────────────────────
     창업자 요청: "기존 사주와 다르게 해석되는 부분도 알려줘."
     이게 이 문서가 사주 앱과 갈리는 지점이고, 아홉 하늘을 붙인 이유 그 자체다.
     **어긋남이 없으면 없다고 쓴다** — 억지로 만들면 그게 더 나쁘다. */
  /* 아홉 하늘이 **본문을 실제로 바꾸게** 한다. v118 은 세어만 놓고 아무것도 안 해서
     "굳이 쟤네 왜 붙였지"가 됐다. 인도 셈과 겹치는 전환 나이를 여든 해 지도에 표식으로 얹는다. */
  for (const b of bands) {
    b.doubleTurn = bothTurn.includes(b.from);
    b.dashaOnly = onlyDasha.some((d) => d >= b.from && d <= b.to);
  }
  const clash = [];
  if (split) clash.push({ t: "겉과 속", w: `여덟 글자는 너를 <b>${SURFACE[me].w}</b>이라고 해. 그런데 서양 해자리(${sun})는 <b>${INNER[innerEl].w}</b>이라고 해. <b>둘이 안 맞아.</b> 사주만 보면 네 겉모습만 읽고 끝나. 남들이 아는 너와 네가 아는 네가 다른 이유가 여기 있어.`,
    n: fn(`일간 ${me} vs 서양 ${sun}(${ZO_EL[sun]}) — 오행과 사원소가 다른 방향을 가리킨다.`) });
  if (nayinEl && nayinEl !== me) clash.push({ t: "글자와 소리", w: `같은 사주 안에서도 갈려. 여덟 글자는 <b>${EL_KO2[me]}</b>인데 태어난 해의 소리는 <b>${EL_KO2[nayinEl]}</b>야. 요즘 사주 풀이는 대개 소리를 안 봐 — 그래서 <b>이 층이 통째로 빠진 채</b> 읽히고 있어.`,
    n: fn(`납음 ${nayin(+birth.y)}(${nayinEl}) vs 일간 ${me}.`) });
  if (onlyDasha.length) clash.push({ t: "시간의 마디", w: `사주는 열 해마다 균등하게 끊어. 인도 셈은 <b>120년을 아홉으로 불균등하게</b> 끊어. 그래서 <b>${onlyDasha.join("·")}세</b>는 사주로 보면 아무 일 없는 해인데 인도 셈으로는 판이 바뀌는 해야. <b>사주만 보면 이 해를 놓쳐.</b>`,
    n: fn(`다샤 전환 ${dashaTurn.join("·")}세 중 대운 전환과 3년 이상 떨어진 것: ${onlyDasha.join("·")}세.`) });
  if (bothTurn.length) clash.push({ t: "겹치는 마디", w: `반대로 <b>${bothTurn.join("·")}세</b>는 두 셈이 <b>같은 해</b>를 가리켜. 서로 아는 사이도 아닌 두 체계가 같은 해를 짚으면 그 해는 무겁게 봐야 해.`,
    n: fn(`대운 전환과 다샤 전환이 3년 이내로 겹치는 나이.`) });
  clash.push({ t: "사주에 아예 없는 것", w: `<b>삶을 열두 자리로 쪼개는 축</b>(서양) · <b>어느 쪽으로 움직여야 풀리는가</b>(구성기학) · <b>태어난 날의 무게</b>(자바) · <b>시작에 강한가 마무리에 강한가</b>(달) — 이 넷은 사주에 없어. 사주만 보면 <b>이 질문 자체를 못 해.</b>`,
    n: fn("사주 체계에 대응 개념이 없는 축들. 아홉 하늘 절의 ①④⑤⑥이 이 자리를 채운다.") });

  /* ── 지금 확인해 보아라 — 근거를 안 대는 대신 확인할 방법을 준다 ── */
  const checks = [
    [`${BUILD[me].replace(" 쪽", "")} 체형인가`, "타고난 골격으로 봤어"],
    /* 약한 자리는 축이 여럿이라 **어느 쪽이 맞는지 되묻는다.** 못 박고 틀리느니 묻고 맞히는 게 낫다.
       ⚠ 이 칸은 순수 텍스트로 그려진다(App.jsx `<p>{w}</p>`) — 태그를 넣으면 <b>가 화면에 그대로 찍힌다. */
    ...axGrp.map((g) => [`${IGA(g.short)} 자주 말썽인가`,
      `${g.ks.join("·")} 약한 자리로 봤어. ${axGrp.length > 1 ? "둘 중 안 맞는 게 있으면 그건 접어 둬" : "아니면 접어 둬"}`]),
    [`${dot(FACE[me].split(".")[0])} 그런 말을 듣는가`, "얼굴의 결로 적었어"],
    [`${dot(BLOCK[blockKey].s)} 그런 편인가`, "가장 빈 자리에서 나오는 증상이야"],
    [split ? "겉으로 보이는 모습과 속이 다르다는 말을 듣는가" : "겉과 속이 같다는 말을 듣는가", split ? "겉과 속의 결이 갈려" : "겉과 속의 결이 같아"],
    [`${INNER[innerEl].w}인가`, "속의 성질로 적었어"],
    [hardMonths.length ? `${hardMonths.slice(0, 3).join("·")}월쯤 유독 컨디션이 떨어지는가` : "계절을 타지 않는 편인가", "무거운 달로 계산했어"],
    [G.비겁 === 0 ? "부탁을 잘 못 하는가" : "곁에 사람이 끊이지 않는가", "같이 가는 자리로 적었어"],
    [G.인성 >= 3 ? "혼자 두면 시작을 안 하는가" : "몸으로 해봐야 이해가 되는가", "배움의 자리로 적었어"],
    [`${THICK[thickKey].job}이 편한가`, "가장 두꺼운 자리로 적었어"],
    /* cur.title 에서 "열 해"를 떼면 "요즘이 닫혀 있던 말이 나오는 같은가"가 된다(실제로 나왔다). 문장째로 쓴다 */
    [cur ? `요즘이 「${bandName(cur.title)}」 같은가` : "요즘 흐름이 바뀌는 느낌이 있는가", "지금 열 해로 적었어"],
  ];

  return {
    age, ageFull, noHour: !!noH, sex: sex || null,
    domains: D, checks, mateMode, sky9, clash,
    health: { axes, agree: healthAgree, els: uniq },
    given: { married, kids, timeAcc, metAge: metAge ?? null, city: birth.city || null },
    core: {
      surface: SURFACE[me], inner: INNER[innerEl], split,
      block: BLOCK[blockKey], blockKey, blockN: G[blockKey],
      n1: fn(`일간 ${GANK[idx.dG]} — ${me}. 남들 눈에 보이는 결이 여기서 나온다.`),
      n2: fn(`서양 해자리 ${sun}(${ZO_EL[sun]})·달자리 ${moon}(${ZO_EL[moon]})${split ? " — 둘이 갈린다. 겉과 속이 다른 구조다" : " — 둘이 같다"}. 인도 달자리 ${nakshatra(jd, +birth.y)}. 마야 ${tzolkin(jdn(+birth.y, +birth.m, +birth.d)).tone} ${tzolkin(jdn(+birth.y, +birth.m, +birth.d)).sign}.`),
      n3: fn(`${blockKey} ${G[blockKey]}개 — 다섯 자리 중 가장 비었다. 없는 것이 있는 것보다 많은 걸 정한다.`),
    },
    body, mate, trig,
    when: { hardMonths, softMonths, years: yrs.map((y) => ({ y: y.year, f: y.f })),
      n: fn(`올해 열두 달을 절기로 갈라 채울 기운(${yong.join("·")})·부담되는 기운(${gi.join("·")}) 대비 −3~+3으로 매겼다.`) },
    job: { ...THICK[thickKey], thickKey, n: fn(`가장 두꺼운 자리가 ${thickKey} ${G[thickKey]}개.`) },
    bands, cur,
    dasha: dasha.periods.map((p) => ({ ...p, ko: DASHA_KO[p.lord] })),
    notes,
    _raw: { me, G, ss, counts, ascSign, ascDeg: asc, sun, moon, sunH, h7Sign, spouseSS, yong, gi, day },
  };
}
