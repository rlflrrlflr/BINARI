## [2026-08-26] 초대와 회신 — L-1 처리방침 · L-2 /api/invite (v140)

**지시서**: `app/작업지시_초대와회신_2026-08-26.md` · 근거 `방향점검_2026-08-26` §축5·§종합
**순서 지킴**: L-1 을 끝내고 L-2 를 시작했다. L-3(B 랜딩)은 **디자인 시안 대기**라 안 건드렸다.

### L-1. 처리방침을 코드보다 먼저

화면에 두 문장이 떠 있었다 — §5-1 *"저희 서버에 저장하지 않습니다"* · §보안 *"서버에 저장하지 않고 기기에만"*.
저장을 붙이는 순간 둘 다 거짓이 된다. v127.7 은 **약속을 먼저 쓰고 코드가 안 따라간** 사고였고,
이번은 방향만 반대고 결과는 같다.

- **§5-1 은 안 고쳤다.** 판결 공유(`?v=`)와 초대 링크는 다른 물건이고, 그 문장은 **지금도 참**이다.
  사실이던 것까지 흐리지 않으려고 **새 절 §5-2** 를 만들었다.
- §보안 「저장 최소화」 줄에는 **단서만** 붙였다.
- 먼저 `privacy-check` 를 돌려 확인했다 — 그 두 문장을 **검사가 물고 있지 않았다.**
  그래서 개정하면서 **새 문장을 검사로 못 박았다**(113 → **124**). 문장만 고치면 다음 세션이 되돌린다.

**정직한 고지 1을 반드시 넣은 이유**: 방향점검이 새로 잡은 것 — 우리 설계 열쇠였던
*"사이 값은 역산이 안 된다(미지수 둘, 방정식 하나)"* 는 **제3자에게만 참**이다.
초대를 받은 사람은 자기 값을 알므로 미지수가 하나가 되어 **보낸 이의 값을 풀 수 있는 유일한 사람**이 된다.
개수 제한으로 못 막으니 **보이게 적었다** — *"초대는 생년월일을 알려도 괜찮은 사람에게만."*

### L-2. `/api/invite` — 의존성 0개 추가

- Vercel 선택적 catch-all(`api/invite/[[...seg]].js`) 하나로 네 엔드포인트. `vercel.json` 에 rewrite 0줄.
- 저장소는 Upstash REST 를 `fetch` 로 직접. **deps 3개 그대로**(검사가 센다).
- **환경변수가 없으면 메모리 폴백.** `judge.js` 의 `_hits` Map 과 같은 패턴이고 **한계도 같다** —
  인스턴스당·휘발. 주석에 *"폴백은 개발·검사용이지 운영용이 아니다"* 로 명기했다.
- **생년월일 원값을 서버가 막는다.** `axes` 에 `y`·`birth` 류 키가 섞이면 400.
  클라이언트만 믿으면 어느 판에서 누가 넣어 보내고, 그 순간 §5-2 가 거짓이 된다.
- **B의 파생값(bAxes)은 저장하지 않는다.** 지시서 §3 요청 본문에는 있지만 §2-2 「저장하는 것」에도
  §10 「B의 값을 A에게 — 안 한다」에도 없다. 받아서 버린다(인터페이스만 맞춤). 검사가 주석 밖 사용 0을 확인한다.

### ⚠️ 지시서 두 줄이 부딪혀서 판단한 자리

| 어디 | 무엇 |
|---|---|
| §3 L77 | *"notify=false 면 answered 를 기록하지 않는다"* |
| §3 L78·L177 | *"응답 1회 + 만료가 재공유 방어다"* |

전자를 **글자대로** 따르면 **동의를 끈 사람의 링크만 계속 열려 있게** 되어 후자가 깨진다 —
하필 방어가 제일 필요한 쪽에서. 그래서 **소비는 언제나 하고, A에게 보이는 것만 동의로 갈랐다.**
지켜야 할 뜻(*"동의가 진짜 선택지"* = A가 아무것도 못 얻는다)은 그대로고, §5-2 의
*"보낸 이에게는 아무것도 전달되지 않습니다"* 도 GET 이 지킨다.
**음성 테스트로 증명했다** — 글자대로 짰을 때 재공유 방어가 뚫리는 것을 검사가 잡는다(아래 ⑥).

### L-4. 검사

신규 `e2e/invite-check.mjs` **33/33** — 브라우저·네트워크 없이 핸들러를 직접 부른다(프레임워크 0).
**음성 테스트 6종을 실제로 돌려** 검사가 잡는지 확인했다:

| 깨뜨린 것 | 잡혔나 |
|---|---|
| ① 응답 1회 방어 제거 | ✅ 2건 |
| ② 동의 무시하고 A에게 알림 | ✅ |
| ③ 생년월일 원값 차단 제거 | ✅ 2건 |
| ④ TTL 을 방침과 다르게 | ✅ |
| ⑤ 취소가 실제로 안 지움 | ✅ 2건 |
| ⑥ **지시서 글자대로** notify=false 미기록 | ✅ 재공유 방어 뚫림 |

기존 전량: 검진 심각 0 · smoke 35/35 · verdict 35/35 · **retention 26/26** · **privacy 124/124** ·
**track 24/24** · **doc 21/21** · 곁탭 37/37 · 궁합 55/55 · 각인 130/130 · 리포트 122/122 ·
역할 26/26 · 곁명부 42/42 · 공유서명 16/16 · 공개표면 33/33

### 자기 오탐이 네 번째였다

소스를 grep 하는 검사가 **자기 주석에 걸린 게 네 번째**다
(`same ? 1 : 1` · `\bidx\b` · 5-g 의 (O) 예시 · 이번 "match.js").
원인은 언제나 같다 — **주석이 금칙 문자열을 인용한다.** 인용을 막는 건 답이 아니다(설명하려면 필요하다).
**검사 쪽에서 주석을 걷는 게 답**이고, 이번엔 그걸 헬퍼로 못 박았다.

### 남긴 것

- **L-3(B 랜딩 + 곁 승격)은 안 했다** — 지시서가 *"디자인 세션 시안이 오면 붙인다. 아직이면 L-1·L-2 를
  먼저 끝내라"* 고 했고 시안이 아직이다. `GY_STANDING` 은 렌더·정렬·CSS 가 그대로 있고 **채울 경로만** 없다.
- **운영에서 아직 안 돈다.** KV 프로비저닝이 창업자 몫(§8)이고, 그전엔 메모리 폴백이라
  인스턴스가 갈리면 A의 초대를 B가 못 찾는다. **이건 한계지 버그가 아니다** — 주석·이 로그에 적어 둔다.
- 계측 5종(§6)은 L-3 에서 화면이 붙을 때 같이 단다. 지금 이벤트만 먼저 심으면 **아무도 안 밟는 이름**이 남는다.

---

## [2026-08-17] 앞면 용어 누출 — 범인은 프롬프트 자신의 (O) 예시였다 (v138 · 0-f)

**배경**: 어제 A-3 에서 "샌 말이 전부 이미 금지 목록에 있으니, 목록 대신 **실측 누출 문장의 (X)→(O) 짝**을
박겠다"고 했다. 다른 세션이 **같은 60칸을 재실행**해 확인했는데:

| | 결과 |
|---|---|
| `direction=undefined` 가드 | 재발 **0** ✅ |
| 동률 오탐(A-4 하네스) | **소멸** ✅ |
| 은유 서술어 | 7 → **2** ✅ |
| **용어 누출** | 3 → **4** ❌ P7-Q17 은 같은 칸·같은 단어(「임오 대운」) 재발 |

**내 베팅이 절반 틀렸다.** 은유엔 먹혔고 용어엔 안 먹혔다.

### 왜 안 먹혔나 — 파 보니 규칙 문제가 아니었다

`SYS` 안에 이런 (O) 예시가 있었다:

> (O)"**무오 대운** — 앞으로 십 년, 불기운이 세지는 때야. 밀어붙이면 되는 판이지."

이건 **뒷면(reasons) 용 예시**고 뒷면은 용어를 써도 된다. 그런데 **`SYS` 는 콜1(앞면)·콜2(뒷면) 공용**이다.
모델은 이 (O) 를 앞면에도 적용한다 — 바로 위 줄에서 "verdict 에 대운을 한 글자도 쓰지 마라"고 해도 소용없다.
**모델은 규칙보다 예시를 따른다.** 이 리포가 이미 아는 원칙이고, 검사 5-g 가 그 원칙으로 만들어진 검사다.

**그런데 5-g 가 (O) 예시를 「은유」로만 재고 있었다.** 앞면 금칙어는 안 봤다.
원칙을 아는 것과 그 원칙을 **검사로 세우는 것**은 다른 일이었다.

### 세 겹으로 고쳤다

**① 예시에 층을 못 박고 앞면 대응쌍을 붙였다**
`(O)` → `(O·뒷면)`. 그리고 짝을 붙였다 — 짝 제시가 은유를 7→2 로 줄인 **바로 그 방식**이다:
> (X·앞면)"무오 대운이라 밀어붙일 때야" → (O·앞면)"앞으로 십 년은 밀어붙이면 되는 판이야."

콜2 지시문의 예시 목록에도 "이 용어들은 reasons 전용"을 붙였다.

**② 검사 5-g 확장 (= 0-f)**
층 표시 없는 (O) 가 앞면 금칙어를 담으면 **심각**. 규칙을 **원인이 있는 자리**에 놓았다 —
배분표가 요구한 "앞면 금칙어 정규식"을 출력이 아니라 **프롬프트 예시**에 건 이유다.
덤으로 **하네스(`eval/run-eval.mjs`)의 `JARGON` 과 목록이 문자열까지 같은지 대조**한다.
주석으로 "같다"고만 적어 두면 갈린다 — 갈리면 한쪽이 통과시킨 걸 다른 쪽이 잡고, **그 오탐이 진짜 실패를 가린다**(A-4 에서 겪었다).

**③ 런타임 계측 — `verdict_shown.jargon` / `detail_shown.sub_jargon`**
새 이벤트를 안 만들고 **분석이 이미 보는 이벤트에** 속성으로 붙였다.
이게 없으면 재발 여부를 알려면 **매번 API 키를 들고 평가를 돌려야 한다** — 이 세션엔 키가 없다.
⚠ **문장을 코드로 고치지는 않는다.** 앞면은 45자 한 문장이라 낱말을 들어내면 뜻이 깨진다.
고치는 건 프롬프트 몫이고, 코드 몫은 **샜다는 사실을 남기는 것**이다.

### 알고 남긴 구멍

**괘 이름(중수감·수뢰둔…)은 목록에 없어 안 잡힌다.** 실측으로 확인했다(subline 에 "중수감"을 넣어도 null).
넓히지 않은 이유 둘:
- 하네스가 **지금 이 목록으로** "용어노출 3→4"를 세고 있다. 중간에 바꾸면 **판이 서로 비교가 안 된다.**
- 지금은 `COIN_RITUAL=false` 라 주역 축이 프롬프트에 안 들어가서 **괘 이름이 나올 경로가 없다.**

동전을 되살리는 날 **양쪽 목록을 같이** 넓힐 것 — 코드 주석에 남겼다.

### 검사

- 검진 5-g 확장 — **오염 주입 두 갈래로 발화 확인**: ①(O) 에 금칙어 되살리기 ②하네스 목록 갈라놓기
- 런타임 계측은 **실제로 돌려서** 확인(앞면 "무오 대운" → `jargon:"무오"`, 뒷면 근거는 안 잡음)
- 전량: 검진 심각 0 · smoke 35/35 · verdict 35/35 · doc-check 21/21 · 곁탭 37/37 · 궁합 55/55 ·
  각인 130/130 · 리포트 122/122 · 개인정보 113/113 · 역할 26/26 · 곁명부 42/42 · 공개표면 33/33

### 남긴 것

- **실효는 다음 eval 실행에서만 확인된다**(키 없음). 다만 이번엔 ③ 덕에 **프로덕션에서도 보인다** —
  `verdict_shown` 에서 `jargon` 이 null 이 아닌 비율을 세면 된다.
- 이번 수정도 **프롬프트 쪽이 절반**이다. 그게 또 안 먹으면 다음 수는 출력 후처리인데,
  45자 한 문장을 코드로 다시 쓰는 건 뜻을 깨뜨릴 위험이 커서 **아직 권하지 않는다.**

---

## [2026-08-17] 곁에 이름이 붙었다 — 그리고 그 이름을 기기 안에 가뒀다 (v136)

**지시(창업자)**: *"곁 탭 최초 진입한 사람은 궁합 볼 사람의 이름을 쓰게 하고, 한 번 쓴 사람은 두 번을
클릭해야 이름 목록을 볼 수 있게. 마치 판결 탭처럼. 두 번 탭 하면 써머리로 그래프와 표, 그 밑에 리스트.
그리고 이 정보가 판결 탭에도 흘러가게 — 내 사업에 도움이 될 사람이 있을까? 했을 때."*

### 내가 반대했고, 근거가 틀렸다

직전 턴에 **"이름은 독"**이라고 답했다. 근거 셋 — ①엔진이 안 쓴다 ②목록이 사람 명부가 된다
③역산에 얼굴이 붙는다. 창업자가 든 반례는 **관찰**이었다: 도령에서 사람들은 심부름인 줄 알면서도
이름을 넣는다. *나와 저 사람이 어떤 관계인지가 궁금해서*다.

내 ①은 **엔진 기준**이었다. 유저 기준으로 이름은 계산에 쓰는 값이 아니라 **"누가 누구인지"**다.
곁이 넷만 되어도 오행 점 네 개는 서로 구별이 안 된다 — 이름이 없으면 목록이 아니라 얼룩이다.
②③은 여전히 유효한 위험이라 **없애는 대신 조건으로 바꿨다**(아래).

### 지킨 선 — 이름은 기기 밖으로 한 걸음도 안 나간다

| 어디 | 어떻게 |
|---|---|
| AI | **이름 대신 `곁1`·`곁2` 자리표 + 역할 이름**만 보낸다. 돌아온 문장에서 앱이 기기 안에서 바꾼다 |
| 계측 | 이름 0. 「적었는지 여부」(`named:true`)만 센다 |
| 서버 | 명부를 보내는 배선이 애초에 없다 |
| 공유·부적 | 자리표를 「곁에 선 사람」 가림말로 — **곁은 미동의 제3자다** |
| 계산 | 이름은 안 쓴다. 비워도 궁합·역할이 그대로 나온다 |

⚠️ **처음엔 `r1.verdict` 를 직접 이름으로 바꿨다가 되돌렸다.** r1 은 밖으로 나가는 길을 **전부** 탄다 —
`convo`(다음 턴 프롬프트) · `v_anon`(PostHog) · `explainMsg`(콜2) · 공유 페이로드 · 서신(콜3).
한 줄 바꾸면 다섯 곳으로 동시에 샌다. 그리고 화면 바로 아래에 "서버로도 통계로도 안 나가"라고
적어 놨으므로, 그건 **v127.7 과 같은 종류의 사고**가 된다 — 약속을 먼저 쓰고 코드가 안 따라간 것.
치환은 **화면과 기기 저장에서만** 한다.

### 만든 것

- **명부 스키마**: `name`(기기 전용, 12자) + `dg`(상대 일간 0~9 — 역할 10종을 세는 데 쓴다).
  `alias` → `name` 승계. 같은 칸에 다른 뜻을 담아 두면 다음 사람이 개인정보 등급을 잘못 읽는다.
- **이름 칸**: 궁합 폼에 추가. **선택이다** — 필수로 만들면 이름을 모르는 사람은 궁합을 못 본다.
  `MATCH_LAST_KEY` 에서 이름은 **뺐다**. 남기면 다음 사람 폼에 앞사람 이름이 미리 채워진다.
- **두 겹 진입**: `tryGyeotOpen` — 판결 탭 `tryWake` 와 **같은 350ms 규칙**. 문법이 같아야 배울 게 하나다.
- **써머리**: 역할별 막대그래프 + 표(쓸모 한 줄) + 아래 이름 리스트.
  ⚠️ 곁탭IA §5 개수 금지와 부딪히는 자리 — **사람이 아니라 자리를 센다.** "친구 3명"은 카운터고
  "판을 같이 키우는 자리 3명"은 분포다. §5 가 막으려던 것(수집 경쟁)은 그대로 막힌다.
  ⚠️ 표는 개수 내림차순, **이름 목록은 최근순 그대로**. 같은 순서로 맞추면 "1등 자리의 사람"이 맨 위에
  서고 그 순간 목록이 순위가 된다(v134.2 방어 ①).
- **역할표 단일화**: `match.js` 의 `ROLE` 을 모듈로 올려 `roleOf()` 로 내보냈다. 베끼면 궁합 문서와
  곁 써머리가 **같은 사람을 두고 다른 말**을 한다.
- **판결 연결**: `gyeotPromptLine` — 자리표와 역할만. "누가/사람/도움/함께" 질문에만 쓰라고 못 박았고,
  없으면 억지로 고르지 말라고도 적었다.

### 화면이 두 번 깨졌다 — 둘 다 눈으로만 잡혔다

1. **패널이 화면 위로 잘렸다** (`top:-90px`). 아래 고정이라 위로 자라는데 그 자리엔 페이지 스크롤이
   안 붙어 잘린 부분에 영영 못 닿는다. **4명에서 이미** 그랬고 상한은 24명이다.
   → 열렸을 때만 패널이 스크롤 컨테이너. 안쪽 목록 상한은 **없앴다**(중첩 스크롤은 모바일에서 안쪽이 먼저 먹는다).
2. **그래프가 통째로 사라졌다** (실측 `340x0`). max-height 를 가진 flex 컬럼에서 자식이 기본
   `flex-shrink:1` 로 눌리는데 `<svg>` 는 대체요소라 **0까지** 눌린다 — 막대는 제 크기인데 상자만 0이라
   화면엔 아무것도 안 남았다. → `.open > * { flex:none }`.
3. 라벨을 오른쪽 정렬 132px 에 넣었더니 제일 긴 자리 이름이 잘렸다("가 쥐고 있어야…").
   자리 이름은 **줄일 수 없는 값**이라(줄이면 표와 그래프가 같은 것을 다르게 부른다) 막대를 좁혔다.

셋 다 검사가 아니라 **스크린샷과 박스 실측**으로 잡았다. 레이아웃은 통과/실패가 아니라 보이냐 안 보이냐다.

### 검사

- `gyeot-check` 30→**37** — 이름 격리 7건을 **실제로 돌려서** 본다: 프롬프트를 열어 이름이 없는지,
  계측을 열어 이름이 없는지, **그런데 화면엔 보이는지**. 소스 grep 이 아니라 런타임이라야 약속의 증거가 된다.
- `gyeot-roster-check` 32→**36** · `report-check` 121→**122** · `privacy-check` 111→**113**
- 뒤집힌 규칙을 쓰던 검사 넷을 같이 고쳤다(검진 궁합·저장키, privacy-check 둘) — "안 받는다"에서
  **"연락처는 안 받고, 받은 이름은 안 나간다"**로. ⚠️ 규칙이 바뀌면 검사도 같이 바꿔야 하고,
  안 바꾸면 **검사가 옛 결정을 대신 우긴다.**
- 전량: 검진 심각 0 · smoke 35/35 · verdict 35/35 · doc-check 21/21 · 곁탭 37/37 · 궁합 45/45 ·
  각인 130/130 · 리포트 122/122 · 개인정보 113/113 · 역할 26/26 · 곁명부 36/36 · 곁예산 13/13

### 남긴 것

- **v134 이전에 든 곁은 `dg` 가 없어 역할을 못 읽는다.** 써머리에서 "n명은 자리를 아직 못 읽었어 —
  궁합을 다시 보면 읽혀"로 **세지 않고 알린다.** 명부가 갓 생긴 기능이라 실사용 영향은 거의 없다.
- 판결이 곁을 실제로 얼마나 잘 고르는지는 **실모델로 안 재 봤다**(이 세션에 API 키 없음).
  프롬프트 규칙("누가/사람/도움 질문에만")이 지켜지는지는 다음 eval 에서 볼 항목이다.
- 곁탭IA §5 의 두 금지(이름·개수)를 **지운 게 아니라 조건부로 열었다**고 문서에 적었다(§5-2 신설).

---

# app/작업로그.md — **폐지됨 (2026-08-17)**

> **개발 로그의 정본은 리포 루트 `작업로그.md` 하나다.** (`CLAUDE.md` §기록 규칙)
>
> 이 파일에만 있던 10건(2026-08-06 v109 ~ 2026-08-17 데이터 레인)은 **전문 그대로 루트 `작업로그.md` 끝의 「[합류] app/작업로그.md 이관」 절로 옮겼다.** 유실 없다.
>
> **여기에 새로 적지 마라.** 두 갈래가 생긴 원인이 "코드 세션은 `app/` 안에서 일하니 `app/작업로그.md` 가 자연스러워 보인다"였고, 그래서 8/12 각인 4단계·8/15 가명처리 전환 같은 무게 있는 기록이 정본 밖에 쌓였다(방향점검 §1-7).
>
> 이 껍데기를 지우지 않고 남긴 이유도 같다 — 파일이 없으면 다음 세션이 **다시 만든다.**
>
> ⚠ **봉합 당일에 벌써 한 번 갈렸다.** 다른 세션이 같은 날 여기에 「[2026-08-16] 새고 있던 넷 봉합」을 새로 적었고, 병합에서 충돌로 잡아 루트로 합류시켰다. **이 파일이 자연스러워 보인다는 게 바로 그 증거다.**
