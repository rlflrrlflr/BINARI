# CLAUDE.md — 비나리 상시 컨텍스트

모든 세션이 자동으로 읽는 파일. **짧게 유지한다** — 상세는 정본 문서로 링크만 건다.

## 제품

비나리(BINARI) — 생년월일·성격 데이터로 '수호신'을 조립하고, 고민에 AI가 **판결**(GO/STOP/HOLD)을 내려주는 의사결정 지원 웹앱. React 웹, Vercel 라이브(`binari-sepia.vercel.app`). 창업자 강석우.

**팀 5명** — 강석우 · 연지 대리 · 정민 대리 · 주영 매니저 · 재민 매니저. 전원 한국, **같은 광고회사 PM팀 소속 전략·크리에이티브 마케터**. 회사 동료들이 앱을 보고 "같이 개발하고 싶다"며 합류한 사이드 프로젝트 팀(본업 병행). 이미 QA 루프로 기능 중(v34 실사용 제보 2건이 연지 대리발 — `변경이력.md` 2026-07-21).
→ **가용 시간은 주 40시간이 아니다.** 마일스톤은 본업 병행 기준으로 잡는다.
→ **개발은 병렬화되지 않는다.** 리포 이력상 코드 작업은 전부 강석우+AI 라인, 팀에 개발자 없음.
→ **단 늘어나는 건 실행력만이 아니다 (2026-08-02 정정).** 팀원이 만든 8개사 경쟁 분석 덱(`04_리서치/비나리_리서치_경쟁분석8개사_v01.pdf`)이 **AI가 이 리포에서 만든 어떤 경쟁 분석보다 낫다.** 이전 기술("늘어나는 건 획득·검증·QA 실행력")은 팀을 손으로만 본 것이다. 팀은 **축을 세울 수 있다.**
→ **그래서 분업이 이렇다.** 시장·경쟁·유저 조사는 **팀이 넓게 그린다** / AI는 그 위에서 **틀린 곳을 찾는다**(적대적 검증) / 법·세무·데이터·코드는 AI. 반대로 하면 둘 다 얕아진다. **AI가 먼저 조사하면 팀의 홈그라운드를 뺏는다.**
→ **팀 산출물은 카톡에 두지 않는다.** 경쟁 분석 덱이 `TalkFile_` 이름으로 왔다 — 카톡에만 있으면 6주 뒤 사라진다. 팀이 만든 문서는 `04_리서치/`에 편입하고 **작성자를 명기**한다(저작권이 개인에게 있으므로 기여 기록이 곧 권리 기록이다).
→ **팀 역량 = 제품 최대 병목과 일치.** 경쟁 분석이 지적한 비나리 최대 약점은 "유저를 데려올 방법이 공백"이고, 팀 본업이 전략·크리에이티브다. 획득 실험은 팀의 홈그라운드.
→ **계측 주의**: 팀·지인 유입이 지표에 섞이면 D7 게이트가 무의미해진다. **손 실측은 반드시 이중 필터로 한다** — `is_internal != true` **AND** `person_id NOT IN COHORT 436757`. 플래그 하나만 걸면 플래그 배포(7/25) 이전 유입 내부인이 외부로 잡힌다. 실제로 8/15 전략 문서가 그 오류로 공유율 5%를 유저 신호로 실었고 8/16에 뒤집혔다(이중 필터로는 외부 공유 발신 **0건**). 정본은 `app/HANDOVER-데이터계측-2026-07-25.md` §2-D.

## 문서 지도 — 무엇이 어디의 정본인가

| 물음 | 정본 |
|---|---|
| 왜 존재하는가 (창업자 원형 비전) | `01_기획전략/비나리_기획전략_비전원형_v01.md` |
| 그 비전이 실현 가능한가 (검증·판정·이동 전략) | `01_기획전략/비나리_기획전략_목표검증이동전략_v01.md` |
| **지금 무엇을 할 것인가 (한국 본진 실행계획·팀 배치·G0 설계)** | `01_기획전략/비나리_기획전략_한국본진실행계획_v01.md` |
| **사장님이 결정할 것 (9개 — 7·8·9는 2026-08-17 신설)** | `01_기획전략/비나리_기획전략_결정필요사항_v01.md` |
| 세금·결제·팀원 지급 (세무사 질문 목록 포함) | `01_기획전략/비나리_기획전략_세무결제판정_v01.md` |
| 팀에게 설명할 자료 ①현황·검증결과·6주계획·역할 | `01_기획전략/비나리_기획전략_팀브리핑_v01.html` |
| 팀에게 설명할 자료 ②권리·보상·겸업 (지분/저작권/취업규칙) | `01_기획전략/비나리_기획전략_팀브리핑_권리와보상_v01.html` |
| **작명 기능을 언제·어떻게 붙일 것인가 (구상·보류 중)** | `01_기획전략/비나리_기획전략_작명기능구상_v01.md` |
| **관계를 어떻게 그릴 것인가 (궁합 국면 시각 문법)** | `03_비주얼프로토타입/비나리_비주얼프로토타입_관계표현인계서_v01.md` |
| **화면을 판결/곁 두 탭으로 어떻게 가를 것인가 (어휘·층·순서)** | `03_비주얼프로토타입/비나리_비주얼프로토타입_곁탭IA_v01.md` |
| **곁의 사람에게 어떤 역할 이름을 줄 것인가 (아홉 축 역할표·고르는 규칙·바닥 대응)** | `01_기획전략/비나리_기획전략_아홉축역할설계_v01.md` |
| 다른 세션에 넘길 작업 지시서 | `app/작업지시_법정고지와결제준비_2026-07-26.md`, `02_프롬프트엔진/비나리_프롬프트엔진_톤개선지시서_v01.md` |
| **이 리포의 폴더·파일명·로그 규칙은 왜 이런가 (다른 프로젝트 이식용)** | `01_기획전략/비나리_기획전략_저장소규칙인계서_v01.md` |
| 제품·코드 현황과 다음 과제 | `01_기획전략/비나리_기획전략_개발인계서_v15.md`, `app/ROADMAP.md` |
| **계측은 무엇을 어떻게 재는가 (이벤트 인벤토리·이중 필터·D7 정의)** | `app/HANDOVER-데이터계측-2026-07-25.md` §2·§2-D |
| 무엇을 바꿨나 (한 줄 대장) | `변경이력.md` |
| **전략**에서 왜 그렇게 판단했나 | `전략로그.md` |
| **개발**에서 왜 그렇게 판단했나 | `작업로그.md` |
| 실사용자가 뭐라고 했나 | `04_리서치/비나리_리서치_유저피드백_v01.md` |
| **팀 회의록·유저 평가를 무엇으로 처리하기로 했나 (반영/보류/기각 + 세션 배분)** | `01_기획전략/비나리_기획전략_팀피드백트리아지_v01.md` |
| **경쟁 지형 (팀원 작성, 8개사)** | `04_리서치/비나리_리서치_경쟁8사가격포지셔닝_요약_v01.md` (원본 PDF·텍스트추출 동봉) |
| **경쟁 단일 해부 (사주도령 — 귀인 지도 루프)** | `04_리서치/비나리_리서치_사주도령해부_v01.md` |
| **개인정보·법정고지 미결 (코드 세션용 작업지시)** | `app/작업지시_개인정보와법정고지_2026-08-14.md` |
| **바이럴 루프를 지금 고르지 않는 이유 + 배관 5건·획득 (코드/팀용 작업지시)** | `app/작업지시_루프배관과획득_2026-08-15.md` |
| **역할 화법(긍정 서술) 교정 + 초대 루프 (코드 세션용 작업지시)** | `app/작업지시_역할과초대_2026-08-15.md` |
| **초대→회신 루프: 링크 받은 사람이 생일을 넣으면 둘 사이를 본다 (저장소 도입·처리방침 선행·세션별 붙여넣기 포함)** | `app/작업지시_초대와회신_2026-08-26.md` |
| **어느 세션이 무엇을 가져가나 (작업 배분 라우팅 표)** | `01_기획전략/비나리_기획전략_작업배분_2026-08-15.md` |
| **다른 세션에 그대로 붙여넣을 지시문 4블록 (로직·데이터·전략·디자인) + 진행 현황** | `01_기획전략/비나리_기획전략_세션지시문_2026-08-16.md` |
| **바이럴 루프를 왜 아직 안 고르나 + 공개 범위 정책 (역산 기준)** | `01_기획전략/비나리_기획전략_바이럴루프판단_v01.md` |
| **유인 동기의 심리학 근거 + 운세방법론 조합 루프 제안 (설계안, 분모 100 이후)** | `01_기획전략/비나리_기획전략_유인동기와루프설계_v01.md` |
| **지금 방향이 맞는가 (여섯 축 감사 · 세션 업무분장 근거)** | `01_기획전략/비나리_기획전략_방향점검_2026-08-15.md` |
| **제품이 목표를 칠 단계인가 (제품 관점 여섯 축: 일상동기·혼합·말투·경외·바이럴 + 자료원 지도)** | `01_기획전략/비나리_기획전략_방향점검_2026-08-26.md` |
| **문장이 어렵거나·장황하거나·알맹이 없는 곳은 어디인가 (전 표면 감사 · 안 건드릴 것 목록 포함)** | `app/작업지시_문장감사_2026-08-28.md` |
| **공유 카드를 무엇으로 그릴 것인가 (부적 폐기 · 아홉 문명 문양 · 역산 제약)** | `03_비주얼프로토타입/비나리_비주얼프로토타입_카드시각문법_v01.md` |
| **홀로 스킨이 왜 안 읽히나 (대비 실측 · 다크 섬 규칙 · 토큰화 안)** | `03_비주얼프로토타입/비나리_비주얼프로토타입_홀로가독성감사_v01.md` |

## 기록 규칙 (세션마다 지킬 것)

- **전략 판단**(목표·시장·과금·우선순위·피벗)이 오간 세션은 `전략로그.md`에 append — *요청 · 판단 근거 · 기각된 선택지 · 결론* 형식. 중요한 판단이 나올 때마다 즉시 기록하고, 세션 끝에 몰아서 정리하지 않는다.
- **개발 작업**은 리포 루트 `작업로그.md` **하나**, 모든 변경은 `변경이력.md` 한 줄.
  → ⚠ **`app/작업로그.md` 에 적지 마라 (2026-08-17).** 코드 세션이 `app/` 안에서 일하다 보니 그쪽이 자연스러워 보여서 실제로 로그가 두 갈래로 갈렸고, 8/12 각인 4단계·8/15 가명처리 전환 같은 무게 있는 기록이 **정본 밖에 쌓였다.** 9건 전문을 루트로 합류시키고 원본은 포인터 껍데기만 남겼다(지우면 다음 세션이 다시 만든다).
- 전략 결론이 바뀌면 **아래 §전제 표와 해당 정본 문서를 함께** 고친다. 로그만 쌓고 표를 방치하지 않는다.
- 새 세션은 이 파일 + 위 표의 정본 2개(비전원형·목표검증)만 읽으면 전략 맥락이 선다. 전략로그 전문은 필요할 때만.

## 현재 전략 상태 (2026-07-25 검증 기준 · 상세는 목표검증이동전략_v01)

| 전제 | 상태 |
|---|---|
| (A) 결정 외주화 습관화 | **조건부가능** — 독촉 없는 D7 자발 재방문 20% 게이트가 판별. 단독 앱 습관화 선례는 시장에 없음 |
| (B) 광고 / 100~200원 초소액 | **사실상 기각(2026-08-02)** — ①바닥은 880~990원에 이미 2개사(팔자냥·사주아이), "가격이 유일한 훅"은 해자 없음 ②그 아래 칸은 **0원**이다. 관측된 최소 결제 단위는 1,000원(운세위키 타로 1장)·990원(아스텔로 대화 1회)이고 그 밑은 유료 상품이 아니라 무료다. **PG 건당 고정비는 「미확인 — 보류」다(2026-08-17)** — 정률 가정이면 100원 상품 PG는 2.8~10원이라 음수가 안 되는데, **그 정률 가정 자체가 리포 근거 0건**이다. 세무사·PG사 직접 확인 항목. **단 (B)의 결론과 주근거는 안 바뀐다** — 주근거는 처음부터 원가가 아니라 시장 구조(옆칸이 0원)였다. 실마진 실측은 `세무결제판정_v01` §9. → **실험군을 '가격 실험'에서 '무료 대비 지불이유 실험'으로 재정의.** 그리고 이 전제의 근거였던 "점신 연 99억"은 **포스텔러 수치 오귀속**이었다. (기존: ~~조건부가능 — 690~1,900원·구독을 실험군에 포함~~) **단 실물은 이미 4,900원(서신)·9,900원(각인) 2층이고 둘 다 실결제 배선 0**(v119~ 확인) — 이 칸의 '초소액' 논의는 실물보다 두 층 아래를 다투고 있다 |
| (C) **한국=본진 / 글로벌=조건부 옵션** | **재설정(2026-07-25)** — "당장 글로벌 진출 생각 없다"(창업자). 글로벌 작업은 §트리거 전까지 **0**. **시장 크기는 트리거가 아님** — "일본이 크다"는 정의를 맞추면 입증 실패, 비나리가 선 앱 세그먼트는 일본에서 규모 비공개 + 6개 중 유일 횡보 |
| (D) 대규모 풀 → 글로벌 인연 매칭 | **현재형태로는 불가능** — 로드맵에서 제거. 관계 판결 또는 커뮤니티로만 재진입 |
| "사주는 믿게 만드는 장치일 뿐" | **의심 중** — 사내 실측(헤비유저만 몰입, ritual 15:quick 1)에 더해 **외부 데이터도 반증 방향**(이용 동기 1위 불안해소 54.2% vs 방향성 33.8%, Pew 중대결정 의존 "많이" 1%). 수요 본체가 '판결'이 아니라 '정서 진정'일 가능성 — G1·G2 실측으로만 판별 |
| 시장 궤적 (2026-07-25 신규 검증) | **한국은 일본을 뒤따르지 않는다** — 모든 수요 지표에서 한국이 동률·상위(결정시 점술의존 韓20%>日19%, 무종교 52%>42%, 점 신뢰 40%>31.8%). 일본은 예언자가 아니라 **중력계**로만 참조: "유료화가 진행되면 코어는 여성·중장년·연애·사람개입·소액종량으로 끌린다" — 인지하되 따라가지 않는다 |
| "무종교화 → 토속신앙 대체" | **인과 기각, 결론만 부분 인정** — 유료 점술 경험률 32년 정체(38→43→40→40%), 20대가 가장 덜 믿음(27% < 60대+ 46%), 무종교인 점경험 42% ≈ 전체 41%. 커지는 건 수요 총량이 아니라 **"믿지 않으면서 소비하는 저비용 AI 채널"**(20대 AI운세 46% vs 신뢰 27%) |

**관문(G0~G3)**: **G0-a 내부 계측 분리(선결)** → G0-b 지인 외 100~200명 → D7 20% → 비신자 재방문(신자의 절반+) → 과금 전환 2%+ / 공유발 유입 10%+. 게이트 실패 시 정답은 이동이 아니라 피벗. **게이트 수치는 사정에 맞춰 내리지 않는다** — 내리면 반증 장치가 자기 위안이 된다.

**최대 경쟁자는 경쟁 앱이 아니라 ChatGPT다.** *(2026-08-02 보강: ①이건 우리 통찰이 아니라 **업계 공통 카피**다 — 포스텔러 "Chat-GPT도 흉내낼 수 없는 만세력". 포지셔닝이 아니라 입장료이므로 **대외 메시지에 쓰지 말 것.** ②**판결 포맷은 방어자산에서 경합자산으로 강등** — 단언 화법은 프롬프트 한 줄이고 용용사주가 "바람필 확률 76%"로 이미 점유했다. 대ChatGPT 방어력은 유지, 대경쟁앱 방어력은 0. ③아래 '테크랩스 영업익 -30%'는 근거에서 뺀다 — 전사 지표인데 점신은 약 11%다.)* 한국 앱 세그먼트를 꺾은 것으로 지목된 것도 범용 AI(점신 MAU -22.6%). 방어 가능한 건 사주 해석 품질이 아니라 범용 AI가 복제 못 하는 것들인데, **셋 중 둘이 이미 깎였다 (2026-08-14).**
- **수호신(시각적 소유물)** — 유지. 단 사주도령이 캐릭터 '청명'에 상품별 전용 일러스트 9종 + 서사까지 투자했다(성과 데이터는 0이므로 보류 표시).
- ~~**판결 포맷(의례성)**~~ — **경합자산으로 강등**(2026-08-02). 단언 화법은 프롬프트 한 줄이고 용용사주가 이미 점유.
- ~~**되물음(재방문 컨텍스트)**~~ — **목록에서 제외(2026-08-14).** 근거 셋이 같은 방향이다: ①창업자 실사용 판단 "재방문 유도 안 하고 어노잉하기만" ②실측 `askback_shown` 1명·`followup_answered` 1명/3건(n이 작아 기능 사망 선고는 못 하나 **자원을 몰아줄 근거도 없다**) ③사주도령이 같은 걸 **무료로 하루 20번** 준다. **그리고 원인이 진단된다 — 되물음은 재방문 시 수호신이 먼저 묻는 push라 §모를 권리를 우리가 스스로 어긴 기능이다.** 기능 제거가 아니라 **자원 배분 대상에서 제외.**
- **새 후보(미검증)**: **판결 하나가 11개 체계를 동시에 싣는다** — 사주(네 기둥·오행 분포·일간)·납음·대운·삼재·토정비결 / 별자리·달 위상·달 별자리 / 베다 나크샤트라 / 마야 촐킨 / 수비학 라이프패스. 조립 지점은 `App.jsx` 의 **`makeSystem()`** 한 곳이고, 의식 경로에선 주역이 유저 턴에 더해져 12가 된다. **단 표를 세는 축은 그중 8개까지다**(`VOTE_AX` — 달 위상·달 별자리·나크샤트라는 '달' 한 축, 납음·대운은 '사주' 축으로 녹인다). ⚠ **그리고 실제로 표를 던지는 건 지금 다섯이다** — v129.2(8/15) 이후 실측에서 주역·토정비결·삼재가 전부 0표이고 평균 `total` 이 7.3 → **5.0** 으로 내려앉았다(주역은 `COIN_RITUAL = false` 라 설계대로, 삼재는 해당 연도만이라 정상, **토정비결만 설명이 안 맞는다** — §설계 헌장 참조). n=9라 축의 사망 선고는 아니다. 각인·궁합은 한 번 더 갈라진다 — **「아홉 하늘」은 투표가 아니라 분업이다**(`app/src/lib/imprint.js`: 서양 점성술·인도 다샤·마야 촐킨·일본 구성기학·자바 웨톤·달 위상·서아프리카 아칸·수비학·동아시아 소리 — 각자 사주가 못 하는 질문 하나씩). 계산은 되는데 **일부러 안 붙인 것도 둘** 있다(마야 하압·태국 요일 — 사유가 `sky.js` 에 못박혀 있다). 경쟁 8개사는 **전원 만세력 단일 엔진**이라 상품을 나눠도 같은 데이터를 다르게 자를 뿐이다. **우리만 데이터가 11개인데 판결 한 줄 뒤에 숨겨놨다.** 상품화 검증 진행 중.
  → **목록·좌표 정정(2026-08-17). 숫자 11은 살아남았고 틀린 건 목록과 줄번호였다.** ①**MBTI·가치여정은 제거됐다** — v128.1(`6215305`)에서 개념·계측 속성·프롬프트 축·온보딩 화면이 통째로 빠졌고, 얼굴 시드로 남겨뒀던 MBTI 저장값도 v129.1(`d590de4`)에서 폐기됐다. 지금 수호신 질감은 저장값이 아니라 명식에서 매번 파생한다(`texture()`). 코드에 남은 「MBTI」는 **전량 주석·이력**이다. ②**바이오리듬은 v34에 판결에서 완전히 빠졌다**(SYS 층위·reasons·콜1/콜2 스키마·프로필 주입 전부 삭제). 지금 유일한 쓰임은 재방문 아침 문안 카드 하나뿐 — **방어 자산 후보가 아니다.** ③좌표 `App.jsx:1977` 은 **도입 커밋(`3d3d85f`) 시점에도 체계 목록이 아니었다**(그때 그 줄은 `track("letter_clicked", …)`). 파일이 자라서 밀린 게 아니라 **처음부터 틀렸다.** → **줄번호만 적으면 검증이 안 된다. 코드를 가리킬 땐 함수명으로 적는다.**

## 응답 규칙 (강석우 지시 2026-07-25)

- **주장만 하지 말고 근거도.** 판단·권고·설계 선택에는 근거를 붙인다 — 리포 내 사실(파일·커밋·로그), 실측 수치, 출처 있는 외부 자료 중 하나. 근거가 없으면 "근거 없음/추정"이라고 표시한다.
- 추론과 사실을 섞지 않는다. 데이터로 확인한 것과 개연성으로 판단한 것을 구분해 쓴다.
- **새 자료를 근거로 기존 결론을 뒤집기 전에 그 자료부터 검증한다 (2026-08-02 추가).** 8/2에 팀원 경쟁 덱을 받고 검증 없이 결론 6건을 뒤집었다가, 검증 후 대부분을 되돌렸다. **출처가 팀이든 창업자든 외부 AI든 검증 면제는 없다.** 뒤집을 근거가 미확인이면 결론을 뒤집는 게 아니라 **보류에 둔다.**
- **과잉 자기정정 금지.** "무엇을 틀렸냐"는 물음에 틀리지 않은 것까지 인정하는 건 정직이 아니라 부정확이다. 정정은 근거가 확인된 것만.
- **문서·절 번호만 대지 않는다 (강석우 지시 2026-08-26 추가).** 창업자는 **모바일에서 리포를 못 연다.** 「§5-1 참조」·「결정 7」·「§2-D 규칙 4」처럼 번호만 던지면 그 문장은 아무 정보도 전달하지 않는다. **번호를 쓸 때는 내용을 한 줄로 같이 쓴다** — 「결정 7(저장소를 비공개로 돌리는 건)」·「이중 필터(내부인을 두 번 거르는 규칙)」. 같은 이유로 리포 안에서만 통하는 말(분모 100·G0-b·시험 발행·모를 권리·부른 곁/선 곁)도 **처음 꺼낼 때 한 번은 풀어 쓴다.** 대화 상대가 리포를 열 수 있다고 전제하지 않는다.

## 설계 헌장 (불변 제약 — 어길 거면 먼저 묻는다)

- **"수호신은 이미 너를 안다"** — 자동 파생 가능한 값은 손으로 되묻지 않는다.
- **모를 권리 — 판결 국면 한정** — 판결 흐름에서는 시스템이 먼저 펼치는 push 금지. 깊이는 유저가 당긴다(L1→L2→L3, 속결→의식, 노크→수령). 예외는 안전 가드레일뿐.
- **알 권리 — 리포트 국면(2026-08-06 판정)** — **리포트는 정반대다.** 값을 치른 문서를 여는 유저는 이미 깊이를 당긴 상태다. 여기서 정보를 클릭 뒤에 숨기는 건 권리 존중이 아니라 **값어치 은닉**이다. **줄 수 있는 건 다 주고, 완결시키고, 초보도 알아듣게 해설을 붙인다.** 근거: 실유저 지적 2건이 정확히 이 증상 — "4,900원 답변이 중복됨"(줄 게 더 있는데 안 줌)·"카드 뒷면 근거를 안 알려줘서 몰랐다"(있는데 숨김). 적용 대상은 서신·상세 리포트·(향후) 작명 리포트.
- **판결문 형식 보존** — 지표를 심화해도 축·카드 수를 늘리지 않는다.
- **정보가 나가는 경로 (2026-08-14 문언 확장 — 기존 '계측'만으로는 공유 경로가 빠져나갔다)**
  - **계측(PostHog)** — 실명·생년월일 원값 전송 금지.
    **질문·답변 본문은 2026-08-15 창업자 결정으로 전송한다 — 단 `anon()` 가명처리를 거친 것만**
    (`q_anon`·`v_anon`·`sub_anon`·`fun_anon`·`reasons`·`letter`). 원문 그대로는 여전히 금지.
    ⚠ 이 줄이 예전엔 "질문 원문 전송 금지(현행 유지)"였다 — 그대로 읽고 `q_anon` 을 위반으로 오판해
    되돌리지 말 것. 경계는 **"본문 금지"가 아니라 "가명처리를 안 거친 본문 금지"** 이고,
    `e2e/privacy-check.mjs` A-3 이 그 경계를 검사한다.
  - **판결 생성(Anthropic)** — 판결에 필요한 범위만(질문 원문 + 사주 프로필). **「이름은 호칭 용도로만 전송된다」는 틀렸다 — 2026-08-17 정정.** 프로필에 `호칭: {이름} (한자 …) / 이름 소리오행 …` 형태로 실린다(`App.jsx` 의 `soundElements()` 와 그 아래 프로필 조립부). v105.5 성명학 수용의 결과다. **단 뒷부분은 참이다 — 앱의 사주 계산·판결 가중치 축에는 안 들어간다**(모델에 주는 재료일 뿐, 축 신설 없음). 처리방침 §5에 이름·한자·소리오행 이전을 명시할 것 — 현행 §5는 "질문 원문 및 사주 프로필"까지만 적혀 있다. **⚠ 이 줄은 코드가 나중에 어긴 게 아니라 문언이 처음부터 틀렸다**(줄은 8/14, 소리오행은 8/2 도입).
  - **⚠ 계측에 태어난 도시(`city`)가 원값으로 나간다** — `demoProps()` 가 싣는다. 처리방침 §1에는 고지돼 있으나(「…관계상태·태어난 도시」) 이 헌장에는 없었다. **계속 보낼지 축소할지는 창업자 결정 사항.**
  - **초대 서버(`/api/invite`) — 2026-08-28 신설, 이 서비스에서 유일하게 서버에 값을 저장하는 곳.**
    올라가는 건 **관계 계산용 좌표 열넷**(`matchAxes()` — 일간·일지·납음·해자리·달자리·나크샤트라·라시·웨톤·촐킨·수)과
    응답 여부·시각뿐이다. **생년월일 원값은 안 간다**(서버가 `y/m/d/h/min` 키를 400 으로 막는다).
    ⚠ **2026-08-28 창업자 결정으로 좌표가 양방향이 됐다** — 받은 사람이 **동의하면** 그 사람의
    좌표와 이름도 보낸 사람에게 간다. 그래야 보낸 사람도 둘 사이를 본다.
    그 전엔 「답이 왔어」 한 비트만 갔는데(지시서 §10), **그러면 초대를 보내고도 사이를 영영 못 본다** —
    게이트를 세워 놓고 대가가 없었다. 동의를 끄면 **답했다는 사실조차** 안 간다.
    ⚠ **그래서 동의의 뜻이 바뀌었고 화면 문구도 같이 바꿨다**(「답했다는 걸 알려도 될까」 →
    「○○도 둘 사이를 보게 할까」). **문구를 안 고치고 값만 보내면 그건 동의가 아니라 기만이다** —
    `e2e/privacy-check.mjs` 가 방침·화면·코드 셋을 맞대어 문다.
    ⚠ **좌표는 되짚을 수 있고 이제 양방향이다** — 상대는 자기 값을 알아서 미지수가 하나가 된다.
    숨기지 않고 처리방침 §5-2 와 B 화면이 그대로 말한다. 30일 TTL·응답 1회.
    **부를 사람의 이름(A가 적어 둔 것)은 여전히 서버로 안 간다.**
  - **공유 링크·이미지** — **실명은 절대 싣지 않는다.** 질문 원문은 공유 문구에 유저가 보고 보내는 것이므로 링크에 포함 가능, 단 처리방침 §5-1에 고지한다. **부적 이미지에는 질문 원문 미포함**(현행 유지).
  - 판단 기준 한 줄: **유저가 무엇이 나가는지 보고 있는가.** 안 보이는 채로 나가면 그건 유출이다.
- **곁 게이트 — 「첫 곁은 내가 직접 넣어 공짜, 그 다음부터는 그 사람이 직접 넣는다」(창업자).** 곁에 사람을 넣는 문은 둘인데(내가 생일을 손으로 넣는 궁합 폼 / 초대 링크를 받은 사람이 자기 생일을 넣는 것) **손으로 넣는 문은 평생 한 번만 열린다.** 곁이 하나라도 서면 그 문은 사라지고 남는 건 초대뿐이다.
  → ⚠ **이 줄이 여기 없어서 사고가 났다 (2026-08-30 신설).** 규칙은 코드 주석 두 곳·`작업로그.md`·`변경이력.md` 세 줄에 있었는데 **전략 레인(`전략로그.md`·`01_기획전략/`·이 파일)에는 0건**이었다. 그래서 전략 세션이 카드 지시서에 **정반대 권고**를 썼고 창업자가 잡아냈다. 더 나쁜 건 침묵이 아니라 **어긋난 기록**이었다 — 전략로그의 최신 기록이 「궁합은 곁이 생기는 **유일한 입구**」라 적고 같은 판에서 궁합을 무료로 풀어, 읽으면 **무제한처럼 보였다.**
  → ⚠ **그리고 규칙이 적혀만 있고 집행이 안 되고 있었다.** 게이트가 **문 앞에서 한 번만** 검사돼, 문서 안 「다른 사람과도 봐볼게」가 폼을 되돌려 **초대 0건에 다섯 명**까지 직접 입력됐다(실측). 2026-08-30 수정 + `e2e/gyeot-check.mjs` ⑧이 이제 **성질**을 문다(「직접 입력 폼에 두 번 도달할 수 있는가」).
  → **남은 재개방 경로 셋은 창업자 결정 사항이다** — 곁을 전부 지우기 / 로비의 「처음부터 다시」 / 자기 초대를 자기가 답하기. 상세는 `변경이력.md` 2026-08-30.
- **토속 비중 동결** — 삼재·토정은 조건부/옵트인 유지. **⚠ (2026-08-26 정정) 세 층 중 두 층이 문언과 다르다**: 화면은 ~~옵트인 접기(지켜짐)~~ **옵트인 화면 자체가 제거돼 있다**(smoke.mjs가 부재를 검사 — 유저가 끌 수단이 없다) · 실투표는 최근 9건 전부 0(지켜짐) · **프로필 주입에는 게이트가 없다** — `tojung()` 결과가 무조건 실린다. 지금은 모델이 안 써서 결과가 우연히 헌장과 같은 방향인데, **「제공된 축을 빠짐없이 넣어라」 쪽으로 프롬프트를 한 번만 조이면 토속 비중이 아무도 결정하지 않은 채 올라간다**(v129가 이미 마야 축에 그 계열 수정을 했다). 정본·미결은 `app/HANDOVER-데이터계측-2026-07-25.md` §2-D 규칙 5.
- **페르소나 톤(2026-07-25 판정)** — 신규 캐릭터(고양이 등) 제작·수호신 비주얼 교체·정령 시각요소 부활 **금지**. 톤은 **판돈 축(속결=정령 심드렁 / 의식=수호신 현행)**으로만 가르고, 변경 범위는 프롬프트 문구뿐. 근거(2026-08-02 재검증 — **금지는 유지, 근거 3개 중 2개 교체**): ①심드렁 캐릭터의 앱 페르소나 전환 성공 사례 0건 — **유지** ②~~헬로우봇 MAU 9만 vs 점신 95만~~ — **인용 금지.** 출시시기·카테고리·수익모델이 섞인 비교이고, 점신 MAU가 리포 안에서 87.5만/95만/150만 세 값으로 흩어져 있다 ③Co-Star는 **반례가 아니라 이 헌장의 예시다** — 정확히는 "마스코트 없이, 독점적 시각 언어(흑백 미니멀·초현실 콜라주) + 심드렁한 톤의 결합으로". 비나리의 파라메트릭 수호신·부적 포스터가 그 자리에 있다 ④추가 근거: 8개사를 캐릭터 유무로 갈라도 **가격과 무상관**(최저가 팔자냥·최고가 청월당 둘 다 캐릭터). 살아남는 실행 하나 — **우리 4,900원 리포트엔 상품명이 없다**(청월당은 '청월아씨 정통사주'). 문구만 바꾸는 일이라 헌장 위반 아님. 상세는 한국본진실행계획 §2-6

## 운영 규칙

- `main` 푸시는 사용자의 **명시 지시가 있을 때만**. main 푸시 = Vercel 자동배포(1~2분). 환경변수 변경은 수동 Redeploy.
- 작업은 지정된 피처 브랜치에서 커밋·푸시. PR은 요청받았을 때만.
- **세션 시작 즉시 `git fetch origin main` 하고 브랜치가 뒤처졌으면 코드를 읽기 전에 재기반한다 (2026-08-14 신설).** 2026-08-14 하루에 **두 세션이 같은 실패를 했다** — 전략 세션이 110커밋 뒤처진 v94 를 최신으로 알고 결론 15건을 냈고, 계측 세션도 "저장소가 50커밋 뒤처져 있어"(커밋 `8bb1f80`)라고 스스로 적었다. 개인의 부주의가 아니라 **여러 세션이 병렬로 커밋하는 이 리포의 구조**가 원인이다.
  → 세션이 여럿 붙는 날에는 **결론을 내기 직전에 한 번 더 확인한다.** 이날 main 은 두 시간에 일곱 판(v119→v125) 움직였다.
  → 다른 세션이 만든 작업지시·인계 문서는 **CLAUDE.md 문서 지도에 올려야 읽힌다.** 브랜치에만 두면 그 세션은 존재를 모른다.
- 파일명 규칙: `비나리_{영역}_{산출물}_vNN.ext` (버전은 항상 맨 뒤). 구버전은 `09_구버전/`으로.
- **개인정보는 커밋하지 않는다** (강석우 지시 2026-08-12). 실명·생년월일·생시·거주지는 리포에 남기지 않는다 — 특히 **생일+생시+지역 세 값이 함께 적히면 그것만으로 한 사람이 특정된다.** 검증·예시가 필요하면 **가상 명식**을 쓴다(`05_실험/판독기_v01/verify-sky.mjs`의 `FIX_A`/`FIX_B` 참고). 검사에 필요한 건 "고정된 입력"이지 "진짜 입력"이 아니다. 실사용 산출물(가족 리포트 등)은 스크래치패드에만 두고 커밋 대상에 넣지 않는다.
  → 값을 못 박은 검사(`=== "바라니"`)는 입력을 가상으로 바꾸면 깨진다. **두 모듈을 맞대는 검사로 바꾸면** 입력과 무관하게 살아 있고 원래 잡으려던 것도 그대로 잡는다.
