# 작업로그

세션에서 오간 **요청 · 판단 근거 · 작업 결과**를 시간순으로 누적한다. `변경이력.md`가 "무엇을 바꿨나"의 한 줄 대장이라면, 이 문서는 "왜 그렇게 판단했나"의 서술 기록이다. 최신 항목이 아래로 쌓인다.

---

## 2026-07-10 · 세션 1 — 백업 저장소 구축 ~ v16 개발 ~ 이펙트 방향

### [1] 저장소 초기화 (v15)
- **요청**: BINARI를 GitHub `rlflrrlflr/BINARI`에 올려라. 폴더 5개(01~04, 09), 파일명 `비나리_{영역}_{산출물}_vNN`(버전 맨 뒤), 루트에 README·변경이력, 커밋 "init: BINARI 백업 v15".
- **판단**: 레포가 빈 상태(`app` 1바이트)였고 Google Drive 전문검색에도 '비나리/BINARI' 없음 → 임의 파일을 지어내지 않고 **규칙에 맞는 구조(스캐폴드)부터** 세우는 게 유일하게 안전한 진행.
- **결과**: 폴더 5개 + 각 README + 루트 README/변경이력 생성, 빈 `app` 삭제, `init: BINARI 백업 v15` 커밋·push.

### [2] 실제 산출물 편입
- **요청**: MVP 코드(`v15_binarimvp.jsx`) + 개발 인계서 제공.
- **판단**: 업로드 파일명이 버전을 앞에 뒀음(규칙 위반) → `비나리_비주얼프로토타입_MVP_v15.jsx`로 리네임해 03에 배치. 인계서는 기획 문서이므로 `비나리_기획전략_개발인계서_v15.md`로 01에.
- **결과**: 두 산출물 배치, README에 제품 개요·산출물 현황표 보강, `init: BINARI 백업 v15` 커밋에 amend(단일 init 유지).

### [3] PM 전략분석
- **요청**: 냉철한 시장분석가·베테랑 PM 관점. 뻔한 AI 조언 배제. ①페르소나 하루/최종목표 ②현실 마찰/기능점검 ③실무 기능 로드맵 ④단계별 디벨롭.
- **판단**: 추측이 아니라 **코드 사실**에 정박해야 함 → ultracode 워크플로로 8에이전트 교차검증: 코드 898줄 라인단위 실사 1 + 한국 운세시장/행동심리 웹리서치 2 + 4렌즈 분석 4 + 적대적 비평 1. 비평 단계에서 발명된 KPI 수치·푸시 없는 알림 서사·아티팩트 localStorage 오판 등 9건을 기각.
- **결과**: 4단계 분석 완성 → `01_기획전략/비나리_기획전략_PM전략분석_v15.md`로 문서화(이 세션에서 소급 저장). 핵심 결론: **"4개의 배관 공사(저장·에러복구·내보내기·API프록시) 전에는 리텐션·바이럴·수익화 전부 공중누각."**

### [4] 개발 루프 (v16, 반복 1~8)
- **요청**: 계획대로 개발, loop 엔지니어링.
- **판단**: 코드 사실이 정한 순서 — 0단계(아티팩트 탈출: 배포가능화+데드엔드 수리)를 리텐션(1단계 B1~B7)보다 먼저. 매 반복은 **transform 스크립트로 "정확히 1회 매칭" 검증 후 치환** → 잘못된 위치 수정 원천 차단. 매 반복 `vite build` 통과를 게이트로.
  - 왜 계정 대신 localStorage(B1): 가입 마찰만 얹고 지금 단계 추가가치 0. 멀티기기·결제 필요해지는 2단계 이슈.
  - 왜 Supabase 0단계 배제: 저장할 서버 데이터가 아직 없음. 비개발자에게 두 번째 학습곡선.
  - 왜 속결 모드 역전(B5): 3초짜리 결정에 6단계 동전 의식은 데일리 사용을 죽임. 프롬프트의 "유저가 청한 경우만 주역"과 UI를 일치.
- **결과**: 반복 1(0단계)→2(B1 기억)→3(B2 데일리)→4(B4 부적내보내기)→5(B3 되물음)→6(B5 속결)→7(B6판결록+B7스트릭)→8(런타임검증). 반복 8에서 Playwright 모바일 뷰포트 스모크 **18/18 통과**(온보딩·속결/의식 실패복구·재회·데일리). v16 스냅샷 03 승격, v15는 09로.

### [5] 최종 전달
- **요청**: 완료되면 최종 파일 제공.
- **결과**: `비나리_웹앱_v16.zip`(app 전체+문서, node_modules 제외) + 단일파일 v16 스냅샷 + 검증 스크린샷 2장 전달.

### [6] 이펙트 방향 결정 (→ v17 A단계)
- **요청**: 레퍼런스 영상 3개 첨부. 지금 버전 불만족 이유/개선법/중독 안 되는 이유/이펙트가 적용 안 되는 이유/보는 사람 심리.
- **판단**: 영상 프레임 실사 → 3개 모두 **GPU 파티클(수십만~수백만) + 유속장(flow field) + 잔상 누적 + 블룸**. 현재 GuardianCanvas는 Canvas2D 1,150~1,600 입자가 **제자리 사인파 진동** → "살아있음"의 핵심인 흐름·인과가 없음. 노력 문제가 아니라 Canvas2D+단일파일 선택의 물리적 천장.
- **결과**: `04_리서치/비나리_리서치_이펙트레퍼런스분석_v16.md`로 문서화. **A단계**(Canvas2D에서 잔상+유속장+가짜블룸+증량, 저리스크) vs **B단계**(WebGL 전환) 제시 → 사용자 **A단계 선택**. (작업 착수 직전 본 로그 정리 요청으로 일시 중단)

---

### [7] v17 A단계 — 수호신 유속장 리라이트
- **요청**: A단계로 진행(사용자 선택). + 진행 중 "작업 로그(판단근거·요청·결과) 쌓고 있냐" 지적 → 본 로그와 PM전략분석·이펙트분석 문서를 소급 저장(위 [3][6] 반영).
- **판단**: 이펙트 분석의 격차 3개(제자리 진동/전삭제/저밀도)를 Canvas2D 한계 안에서 직격. ①잔상은 배경색 fillRect(=stage 그라디언트 가림) 대신 **destination-out으로 알파만 감소**(투명 캔버스 유지 표준기법) ②각 입자에 vx/vy 속도 부여 후 유사 컬노이즈로 밀되 오행 형태(place)로 **스프링 복원** → 흐름과 형상 양립 ③입자 증량(3,200~4,200)+arc→fillRect 배칭으로 성능 상쇄. 어셈블 강도(1-k)로 초기엔 흐름 강·안정 후 형태로 수렴.
- **결과**: 렌더 확인 — 소용돌이 유속장+중앙 코어 발광+잔상 궤적(레퍼런스 영상1 근접). 헤드리스 52fps(소프트웨어 렌더, 실기기 GPU선 더 여유), 스모크 18/18 회귀 없음. **성능 선제최적화는 보류**(실기기 데이터 없이 입자 감량은 로드맵의 '데이터 없는 최적화 금지' 원칙 위반) → 저사양 FPS 확인을 로드맵에 남김.

### [8] 실사용자 피드백 2건 + v18 방향 확정
- **요청/입력**: ①사주 헤비유저 테스트 — "엄청 빠지더라"(코어 첫 실증) ②비신자 — "안 물어본 정보까지 말해주는 게 꺼려진다, 물어본 수준 답변은 좋다". + 사용자 스크린샷으로 v17 아티팩트 실행 확인(판결·저장이 아티팩트에서 죽는 문제 노출), 아키텍처·프로세스 불만 분석 요청.
- **판단**: (a) v16의 "아티팩트 탈출"이 사용자의 유일한 실행 환경을 부숨 — 듀얼 모드(아티팩트 폴백)로 복구가 최우선 (b) 헤비유저층은 만세력을 검산하는 층 — 절기 근사 ±1일이 신뢰 붕괴 지뢰 (c) 비신자 피드백은 '모를 권리' 원칙으로 일반화 — push 요소(토정비결 자동 노출·아침 문안 자동 펼침·프롬프트 범위 무제한)를 pull로 전환 (d) 이펙트 A단계의 백색 포화·폭발 문법·텍스트 겹침은 응급수술 대상.
- **결과**: `04_리서치/비나리_리서치_유저피드백_v01.md` 신설. **v18 스코프 확정**: ①듀얼 모드 API+저장 폴백 ②이펙트 수술(코어 포화·containment·텍스트 분리) ③모를 권리 반영(프롬프트 규칙·토정비결 접기·아침 문안 노크형) ④만세력 정밀화. 병행: 사용자 트랙 — Vercel 배포(~30분)·테스터 5~10명 모집.

### [9] v18 루프 (반복 10~12) — 테스트 루프 복구·이펙트 수술·만세력 정밀화
- **요청**: "A로 우선 진행" 후 유저 스크린샷 5장(클로드 앱에서 v17 판결 실패) 공유하며 "진행".
- **판단**: (반복 10) 아티팩트 호환은 런타임 폴백이 정답 — 도메인 감지는 취약하므로 /api/judge 콘텐츠타입/404로 실패 감지 후 직접 호출 전환, 첫 성공 경로 캐시. (반복 11) 백색 포화의 본질은 가산량/페이드율 평형>1 — 밝기가 아니라 평형점을 조정. 금 폭발은 "바깥 컨베이어+잔상=폭발 문법"이 원인 — 팔 따라 상시 관통+회전으로 교체, 팔 적을수록 폭 확대. 과교정(패널 불투명 → 수호신 가림) 1회 발생·스크린샷 검수로 잡음 — **렌더 검수 없는 시각 변경은 금지**를 교훈으로. (반복 12) 절기 근사표는 헤비유저 검산에서 무조건 발각 — Meeus 저정밀 태양황경(±수분)+NOAA 균시차로 교체, 도시 경도 매핑. 검증은 널리 알려진 기준값(병인일·무오일·입춘 당일 시각 분기·균시차 극값·춘분 0°)으로.
- **결과**: 반복 10(듀얼모드+저장 셈+모를 권리, 스모크 21/21) → 반복 11(이펙트 수술, 21/21) → 반복 12(만세력 10/10, 빌드+스모크 21/21). v18 스냅샷 03 승격. 플랫폼 진단: 클로드 앱 "Invalid response format"은 아티팩트 프록시 한계(v15 인계서 기록과 일치) — **폰 테스트는 Vercel 배포가 유일한 답**.

### [10] 모바일 실측 — v18 아티팩트 3중 문제
- **입력**: 유저가 v18을 모바일 클로드 앱 아티팩트에서 구동한 스크린샷 2장. ①질문칸 안 보임 ②좌우 여백 ③판결 "Invalid response format".
- **판단/진단**: ①②는 내 CSS 문제(qbox 밑줄+검정 placeholder가 파티클에 묻힘 / scene max-width 400 여백) → v19에서 박스화·풀폭 수정. ③의 "Invalid response format"은 **우리 코드 문자열이 아님**(우리 에러는 전부 한글) = 클로드 앱 아티팩트 런타임이 우리 API 호출(정식 Messages 형식 fetch)을 거부하고 던진 것. v15 인계서의 "모바일 API 실패, PC 크롬 확인"과 일치 — **모바일 아티팩트는 이 앱의 API를 못 돌린다(플랫폼 차이)**. 앞서 "v18이면 모바일 아티팩트에서 될 것"이라 한 내 추론은 오판(정정). window.claude.complete 방식 폴백은 2콜·캐싱 설계를 훼손하는 오버엔지니어링이라 배제. **판결의 모바일 해결은 배포가 유일·확실**(배포 웹앱은 아티팩트 런타임을 안 거치고 /api/judge를 씀).
- **결과**: v19 CSS 수정(스모크 21/21). 판결 문제는 코드로 못 고침 — 배포 필요를 명확화.

### [11] 아티팩트 판결 복구 (v19) — 내가 버렸던 내장 API 되살림
- **요청**: "이전엔 아티팩트에서 API 물려서 됐다. 다시 그렇게 해줘. 배포 전제하지 말고."
- **판단**: 사용자가 옳다. 아티팩트에는 **window.claude.complete(문자열 in/out)** 내장 API가 있고, 이게 "물려있던" 정체. v16에서 내가 이걸 버리고 /api/judge로 바꾼 게 근본 원인. 앞서 "window.claude.complete는 2콜·캐싱 훼손이라 안 함"이라던 내 판단을 뒤집음 — 초기 단계의 최우선 가치는 '지금 폰에서 되는 것'이고, 캐싱은 그 다음. 판결 경로를 3-way로: complete(있으면 최우선)→server→direct. system+messages를 한 프롬프트로 병합, 콜1/콜2는 프롬프트 텍스트로 구분. **검증을 사용자에게 떠넘기지 않기 위해** window.claude.complete를 mock 주입해 헤드리스에서 콜1·콜2 판결 렌더까지 확인(4/4). (첫 시도에서 캡처 타이밍이 카드 애니메이션보다 일러 오탐 → 대기 로직으로 수정.)
- **결과**: `app/e2e/verdict.mjs` 신설(판결 회귀 4/4), 스모크 21/21 유지. v19 스냅샷 승격. 배포 환경에선 complete 부재→자동으로 /api/judge. **아티팩트·배포 양쪽 다 지원.** 단 아티팩트 런타임에 window.claude.complete가 실제 존재하는지는 사용자 실측으로 최종 확인 필요(mock은 우리 코드가 올바르게 호출·파싱함만 증명).

### [12] v20 QC — 폭포수 + 진단 도구 (유저: "도대체 왜 되던 게 안 되냐, QC 완벽하게")
- **타임라인 규명**: ~v14 window.claude.complete(모바일 OK) → v15 캐싱 위해 직접 fetch로 교체(이때 모바일 첫 파손 — v15 인계서에 "Invalid response format" 기록 있음) → v16~17 /api/judge(전부 파손, 내 회귀) → v18~19 복구 시도. v19에서 complete를 되살렸는데도 모바일이 깨진다 = 앱의 completion 브리지 자체가 현재 오류를 내거나 우리 호출 형태를 거부.
- **판단**: 클로드 앱 내부는 헤드리스로 재현 불가능한 블랙박스 — 추측 반복 대신 ①폭포수(한 경로의 어떤 실패도 다음 경로로 이월, 성공 경로만 기억) ②격리 진단 도구(폰에서 complete/fetch/storage를 개별 검사해 화면에 원문 표시) ③모바일 고장 재현 테스트(complete가 "Invalid response format"을 던지는 환경을 mock으로 재현 → 폭포수로 판결 성사 + 화면 무에러 확인).
- **결과**: QC 배터리 37/37. 남은 유일한 미지수는 폰 런타임의 실제 지원 범위 — 진단 아티팩트 실측(사용자)으로 확정하는 구조로 전환. 교훈: "환경이 다른 버그는 코드 수정 반복이 아니라 격리 진단부터".

### [13] 진단 v01 실측 결과 — 플랫폼 능력표 확정 (iPhone·클로드 앱)
- **실측**(사용자 스크린샷 4장): ①window.claude 존재(complete·sendConversationMessage·openExternal·downloadFile), UA=WKWebView(Safari 토큰 없음) ②complete("안녕") → 앱 전체 흰 화면 "Error in Claude completion"+아티팩트 리셋(로그 소실로 확인) — **짧은 프롬프트로도 죽음 = 크기 문제 아님, 브리지 고장** ③직접 fetch → "Invalid response format" 거부(단, 아티팩트는 생존) ④localStorage ✅.
- **확정 결론**: 클로드 iOS 앱 웹뷰 안에는 AI 호출 길이 없음(코드로 불가). 저장은 됨. v20의 complete 우선 호출은 앱에서 아티팩트를 죽이는 지뢰였음.
- **조치(v21)**: UA로 앱 웹뷰 감지(iOS && Safari 토큰 부재 / Android && wv) → complete 봉인, 전 경로 차단 시 정직한 안내("사파리에서 claude.ai를 열거나 PC에서"). 판결 테스트에 S3(앱 웹뷰 재현: complete 미호출·안내 표시·재시도 생존) 추가 — 9/9.
- **다음 실측**: 같은 진단을 **폰 사파리(claude.ai)**에서 — ② 성공 시 폰 테스트는 사파리로 해결, 실패 시 폰은 배포가 유일(이젠 데이터 근거).

### [14] 진단 2차(모바일 크롬) — 근본 원인 최종 확정: Anthropic 서버측 고장
- **실측**(iPhone·크롬 CriOS/150, iOS 26): ①window.claude ✅ ②complete 호출 왕복 성공하나 **빈 문자열 반환** + 토스트 "**Internal server error while processing action**" ③fetch 동일 서버 오류 ④localStorage ✅.
- **최종 결론**: 클로드 앱의 "Invalid response format"과 모바일 크롬의 "Internal server error"는 **같은 뿌리** — Anthropic의 모바일 아티팩트 completion 백엔드가 500을 반환 중. 앱은 그 500을 파싱 못해 아티팩트를 죽이고, 크롬은 토스트+빈 문자열로 표면화. **유저 폰 무죄, 코드 무죄, "업데이트 안 했는데 왜 깨졌나"의 답 = 서버가 바뀌었다/고장났다.**
- **함의**: 폰에서 아티팩트 판결은 Anthropic이 복구할 때까지 불가(모든 경로 실측 완료). v21 폭포수는 빈 응답을 실패로 처리하고 우아하게 안내하므로 **서버가 복구되는 순간 코드 변경 없이 폰에서 자동으로 살아남**. 즉시 해결책은 배포뿐(이제 가정이 아니라 실측 근거).

### [15] v22 착수 — UX 비평 반영 + 지표 포트폴리오 설계
- **요청**: "컴터 쓸 수 있기 전까지 그거라도 하자"(UX 4종 승인) + "로직도 — 동서양 기운의 종류가 충분한가, 토속 말고 신뢰도 있되 덜 알려진 것, 기존 데이터만으로, 가치여정 단계·숫자도 판단해봐".
- **판단**: ①지표 감사 — 실제 구성이 한국+그리스뿐이라 "온 세상" 스토리라인 미충족. 추가 4종 확정: 달 별자리·나크샤트라(달 황경 공유, 인도 축)·납음오행(사주 심화, 시적 이름)·마야 촐킨(제3문명). 축은 '마야' 1개만 신설(판결문 보존 — "지금 판결문 좋아" 피드백). 상승궁·자미두수는 Phase 2. ②가치여정 — 방법론(가치 카드 소트)은 ACT 실사용 도구라 신뢰 앵커로 유지, 숫자만 다이어트: 24→16, 8→4→1→6→3→1(13→10탭). 상세는 `비나리_기획전략_지표포트폴리오설계_v01.md`.
- **결과(반복1)**: 장면 분리(gpanel을 캔버스 오버레이→하단 흐름), 발화 1개 원칙(형상 설명 삭제), 동전 0.75s 공중 연출, 금 강철빛. 30/30 테스트+렌더 검수. 다음: 반복2 지표 4종 → 반복3 가치여정 개편.

### [16] v22 반복2·3 — 지표 4종 구현 + 가치여정 개편 (44/44)
- **반복2**: 달 황경(Meeus 10항) 구현 — 검증을 월식·신월 정렬로(식은 태양-달 정렬의 절대 기준, 오차 0.04°/0.02°). 달 별자리=회귀황도, 나크샤트라=항성황도(라히리 아야남샤 근사) — 아는 사람이 검산해도 관례와 일치하도록 좌표계를 갈라 적용. 납음 30종 테이블(수호신 소개 문구를 납음의 시적 이름으로 교체 — '대지을' 조사 오류도 함께 수정), 촐킨 GMT 584283(2000-01-01=11 이크 앵커 검증). 프로필은 기존 축 심화+마야 1축 신설 — 판결문 형식 보존.
- **반복3**: VALUES24→16(중복 8개 흡수), 6→3→1. e2e 3종+샷 스크립트 갱신 — sed 접두사 겹침으로 샷 스크립트가 5개 선택으로 깨진 것을 grep 검수로 잡음(교훈: 리스트 치환은 경계 포함 패턴으로).
- **잔여 주의**: 구버전 localStorage(8/4개 선택 저장분)는 새 표기(16→6→3→1)와 숫자 불일치하나 판독엔 무해 — 리셋 없이 호환.

### [17] v23 — 실기기 스크린샷 비평 3반복 (온보딩~의식 전면 개편, 44/44 + 육안 검수)
- **요청**: 온보딩 스크린샷 5장+동전 스크린샷과 함께 "루프 하는 동안 너가 보는 거 맞아? 내가 마음에 안 드는 이유를 말해봐"(첫 시작~질문 결과 전, 동전 던지기까지) → 비평 제시 후 "진행".
- **비평(5죄)**: ①조각을 모으는 여정인데 **쌓이는 게 안 보인다**(오브가 처음부터 끝까지 같은 크기·같은 색) ②리빌이 작은 원 안 12px — **절정을 읽을 시간이 없다** ③MBTI 16그리드 = **세계관 속 설문지**(수호신이 "이미 아는" 존재인데 유형 코드를 고르게 함) ④의식 화면에 입력창·빈 점선 스캐폴드가 남아 **무대가 없다** ⑤플레이트 위치·줄바꿈·카피 등 마감 잔손.
- **판단·구현**:
  - 반복1 — DustOrb를 성장형으로(`stage` 0~3: 생일→성격→가치 단계마다 입자 150+140씩 증량, `tint` 오행색): "모은다"의 시각 증거. 수호신 팔 수 상한 `3+((lp-1)%5)`(3~7)로 과밀 해소 + 금 폼 리드암 비대칭(정오각 대칭의 인공감 제거). 카피·플레이트 마감.
  - 반복2 — 리빌을 화면 중앙 무대로(`.rvstage`/`.rvbig` 22px 금빛, 항목당 1.15s, `rvAbsorb`로 오브에 흡수): 절정을 읽게 한다. MBTI는 16그리드 폐지 → "요즘의 너는 어떤 모습이야? — 내 기억이 맞는지 골라줘" 2택×4(같은 정보, 설문 문법 제거·픽션 문법 유지). 4행 완성 시 자동 조립.
  - 반복3 — 의식 장면화: ritual 중 textarea 숨기고 질문을 인용문(`.qquote`)으로 무대에 올림, 빈 점선 6줄 폐지 → 던질 때마다 효가 낙하(`hexDrop`)하며 아래부터 쌓임(column-reverse, 전통 초효=바닥), 동전 포물선(`coinFly` 0.75s), 낙착마다 수호신 요동(agitate 0.6s·6효째 1.4s 클라이맥스), 중도 취소 '물음을 고칠래'.
- **검수 체계 신설**: "너가 보는 거 맞아?" 지적 수용 — `e2e/journey.mjs`(전 여정 풀페이지 14컷, BIRTH 파라미터) 신설, 매 반복 빌드+배터리(스모크 21·판결 9·만세력 14)+**렌더 육안 검수**를 통과 조건으로. 반복3 검수에서 동전 공중+질문 인용문 한 프레임, 괘 완성("화산려 · 변효 4,5,6효 → 수산건") 확인.
- **결과**: 3반복 모두 44/44 PASS. e2e 셀렉터는 MBTI 픽션형·"동전 셋" 부분일치로 갱신. v23 스냅샷 승격(03), zip·단일파일 전달. 교훈 재확인: **렌더 검수 없는 시각 변경 금지** — 테스트 통과와 "보기 좋음"은 다른 문제.

### [18] v24 — 판결 전 단계 강화 (유저 레퍼런스·지적 3건 반영, 45/45)
- **요청**: "데스크탑 전엔 모바일 아티팩트에서 판결문을 못 보니, 그 이전 단계를 탄탄히" + 질문 3건: ①MBTI 2지선다 왜/인식 안 됨 ②혈액형 뜬금없고 개인정보, 판결에 영향 없으면 제거 ③레퍼런스 2건(3D 입자 구체·폭발 블룸) 제공, 판결 전 이펙트가 수호신과 동떨어짐 + 스텝별 성장 안 보임.
- **진단**: 성장 미체감은 이중 원인 — 유저 실행본이 v22 아티팩트(성장 미탑재)였고, v23 성장도 입자 수 증가뿐이라 '자라는 순간'이 없었음. 오브(평면 먼지)와 수호신(유속장 생물)은 시각 DNA가 단절돼 있었음. 혈액형은 코드 확인 결과 SYS에 "판결 미반영" 명시 — funLine·오라 색에만 쓰여 제거 무해, 지표 포트폴리오 원칙("신뢰도 있되 덜 알려진 것")과도 정반대라 제거가 정합.
- **구현**:
  - 반복1 — DustOrb를 3D 회전 입자 구체로 리라이트(구면 좌표+Y축 회전+기울임 투영, 깊이별 명암·크기). 성장 = 응집(jitter 0.62→0.08)·입자 230→620·자전 가속·오행 착색, 단계 상승 순간 펄스+성장 링. 가치의 방 섹션 key 제거로 오브 연속성 확보(펄스가 씹히지 않게), 텍스트만 key 페이드.
  - 반복2 — BirthCanvas 신설: 수축(1.15s, 자전 가속)→섬광(0.23s)→방사 스트릭 블룸(1.8s, EXPOSOMES 레퍼런스) 후 수호신 페이드인. 재회 유저는 기존대로 전환 생략.
  - 반복3 — MBTI 4행 그리드 폐지 → 순차 문항("기억 N/4" + 수호신의 질문 + 2택 + '아까 걸로 돌아갈래'), 완료 시 "기억났어" + '다시 떠올릴래'. 혈액형 전면 제거: 상태·저장·프로필·SYS 3곳·콜2·UI·CSS·시드. 정령은 달 별자리로 재배선(funLine 유지), 수호신 곁 위성은 달빛 은색 상시로. 구버전 localStorage는 blood 필드 무시로 호환.
- **주의(기록)**: 수호신 시드에서 혈액형이 빠져 기존 유저의 수호신 형상이 미세하게 달라짐 — 출시 전이라 수용.
- **결과**: 빌드 통과, 스모크 22(혈액형 부재 검증 1건 추가)/판결 9/만세력 14 = 45/45. journey 15컷(기억2/4 컷 추가) 육안 검수 — 먼지 구름(오프닝)→구체 성장+펄스 링(가치)→방사 블룸(탄생)→수호신 사슬 확인. v24 스냅샷 승격.

### [19] v25 — 신을 완성하는 정보(음력·이름·성별→대운)
- **요청**: "성별·이름은 왜 안 넣었어? 이런 게 잘 모여야 신의 조각이 잘 모일 것 같은데. 신을 완성하는 데 꼭 필요한 정보가 뭘까" + "다 추가" + 이전에 말한 '음력 안 돼서 간절기 애들 오판' 건 확인.
- **판단 프레임(제시)**: "조각이 모여야 신이 모인다"는 감정적으론 맞지만 폼 비대의 본능 — 혈액형을 지운 잣대와 충돌. 원칙: "이미 너를 안다=생일에서 파생되는 건 안 묻는다"(사주·별자리·달·수비학은 파생이라 안 물음). 이름·성별은 파생 불가 → 물어도 세계관 안 깨짐, 단 '기억을 되찾는' 프레임일 때만. 두 관문: ①판결을 바꾸는가/친밀도를 높이는가 ②마찰·거부감 대비 값. → 이름(친밀도, 성명학은 한자 필요라 옵션 보류), 성별(대운을 지어야만 값을 가짐; 단독은 혈액형 2.0), 음력(정확성 버그). 직업·연애상태 등은 배제(모를 권리·신비감).
- **간절기 진단**: 절기(節氣) 경계 자체는 v18에서 태양황경으로 이미 처리됨(입춘·월주). 다만 음력 미지원과 겹치는 인구가 동일 — 음력 설날이 매년 입춘 근처라, 음력 1~2월생이 음력을 양력칸에 넣으면 날짜+사주년이 동시에 뒤집힘. 즉 음/양력 토글이 그 '간절기 오판'의 실제 해결책.
- **구현**:
  - 반복1(음력) — `lunar2solar`(기존 LUNAR표·solar2lunar의 역) 신설, 음/양력 토글+윤달 체크, doReveal에서 음력이면 양력 정규화 후 모든 계산(사주·달·별자리·수비학·토정·삼재)에 양력 전달. birth에 정규화된 양력 저장(원본 음력은 lunarNote). 구버전 저장분은 cal 부재→양력 기본으로 호환.
  - 반복2(이름) — birth.name 선택 입력(맨 위, "너를 뭐라고 부를까?"), 프로필 상단 '호칭' 주입 + SYS "결정적 순간에 이름 부른다(B ○○아·A ○○님)" 규칙, 리빌 멘션 "그래 — 서연, 너는 원래…". 판결 미반영(친밀도 전용) — 혈액형과 다른 점은 값이 '이름 불러주는 존재'라는 정서.
  - 반복3(성별→대운) — `daeun` 신설: 양남음녀 순행/음남양녀 역행, 대운수=순행 다음 節·역행 이전 節까지 일수/3(태양황경으로 節 크로싱 탐색), 현재 나이로 대운 간지(월주±step) 산출. 성별 선택 시에만 계산, 프로필 타이밍 층에 "대운(현재 인생 시기): 무인(토) 33~42세 역행"으로 주입. SYS는 대운을 별도 축 신설 없이 사주 근거에 녹이도록 지시 → 판결문 형식 보존.
- **검증**: 만세력 24/24 — 음↔양 왕복 6/6, 설날 2건(2000.2.5·1993.1.23), 윤달 구분, 설날생 사주년(음력정월→양력1.23→입춘전→임신년), 대운 방향(양년 남 순행/여 역행)·대운수 범위·pre·첫대운=월주±1. 전체 55/55. 프로필 실측(headless로 window.claude.complete 가로채 프롬프트 원문 확인): 호칭/성별/대운 3항목 정상 주입. e2e 셀렉터는 이름 입력(input.in.wide) 추가로 밀려서 `input.in:not(.wide)`로 수정.
- **주의(기록)**: 대운 간지는 알고리즘(방향·대운수·간지 스텝)을 내부 검증했으나 외부 만세력과의 대조는 사용자 실측 권장. 대운수는 세는나이 근사(10년 버킷엔 무해). 이름은 수호신 시드에 미포함(형상 불변).

### [20] v26 — 전방위 자가 검토 → 동화 도입부·조각 보태기·조기 뒤집기
- **요청**: "놓친 게 뭐냐, 내가 마음에 안 든다면 왜일 것 같냐, 전방위 검토" → 검토 제출 후 지시: ①이름(상표)은 걱정 말 것 ②도입부는 동화 영화처럼 ③추가 입력 경로 OK ④판결문은 유지(콜2 백그라운드 구조 유지, 일찍 뒤집으면 '근거 모으는 중' 안내만) ⑤나머지 최적안 ⑥배포 재촉 금지.
- **자가 검토에서 인정한 결함**: v25 입력 화면의 서식 회귀(픽션 원칙을 스스로 어김), 음력 입력의 조용한 덮어쓰기(변환을 보여주지 않음), 기존 사용자가 리셋 없이 새 조각을 못 넣음, 잔글씨 4개 조항의 약관화, 이름 활용처 부족. 콜2는 확인 결과 이미 백그라운드 프리로드(v15 설계)였고 부족한 건 조기 뒤집기 반응뿐이었음.
- **구현**: step1을 bstep 0~3 장면화(장면당 수호신 한 문장+최소 입력+금 버튼 하나, '아까 장면으로' 후진, 장면별 검증), 오프닝 버튼·브랜드 4.4s 지연 등장(타이틀 시퀀스), 음력 변환은 리빌 사주 카드에 "음력 X.X — 하늘의 달력으로 바꿔 읽었어"로 노출, 재회 인사 "다시 왔네, ○○". 조각 보태기: 이름·성별 결손 시 노크("아직 못 찾은 조각이 있대")→인라인 패널→저장(saveMemory 즉시 호출, 구버전 저장 name/sex undefined 호환 심). 카드: 콜2 진행 중에도 뒤집기 허용, 뒷면에 점멸 "조각들이 근거를 모으고 있어…", 힌트 문구도 수집 중 노출.
- **검증**: 55/55(스모크 22·판결 9·만세력 24) + journey 17컷(이름·하늘·흐름 장면 추가) 육안 검수 + 보태기 E2E 실측(노크→패널→저장→노크 소멸, localStorage에 이름·성별 반영 확인). e2e 온보딩 3종을 장면 흐름으로 갱신.
- **보류(다음 후보)**: 실기기 FPS 측정, 대운 경계 나이 정밀화(세는나이 근사), 야자시, 이름의 아침 문안·의식 확장. 배포는 사용자 판단 대기(재촉 안 함).

### [21] v27 — 수호신 다양성·리빌 다이어트·정령 제거 (실기기 4장 피드백)
- **요청/질문**: ①이름 필수·한자 여부 ②텍스트 뚱뚱, 스크롤해야 정보 보임 — 같은 위치서 수호신이 바뀌는 모션으로 '모이는 게' 인지되게 ③외곽 도는 것=혈액형 정령 잔재, 빼도 됨 ④"저 모양 경우의 수가 어떻게 돼? 크게 안 다르던데 다른 사람 거 보니까".
- **진단(경우의 수)**: 코드 분석 결과 실루엣을 결정하는 축이 사실상 오행 주기운 5종뿐. 팔 수(라이프패스 3~7)는 목·금 두 형태에서만 사용(화·수·토는 계산만 하고 미사용), 색은 오행 팔레트 안에서 별자리로 ±33° 미세 회전, MBTI·달은 밀도·속도·밝기(움직임)만 → 정지 화면에선 티 안 남. 그래서 같은 오행끼리(예: 금-금)는 은청색 6갈래로 거의 동일. 이론 조합은 수만이나 "한눈에 다른" 실루엣은 5개. 유저 진단 정확.
- **구현**: ①팔 수를 5형태 전부에 — 화(arms개 불꽃 혀)·수(arms개 물결 층)·토(arms갈래 봉우리 lobe). E/I→R 배율(외향 1.06/내향 0.9), T/F→입자 크기(사고 0.84/감정 1.24). → 5형태×팔 3~7 + 크기/질감 = 뚜렷 실루엣 대폭 증가. ②정령 위성 블록 삭제. ③멘션 3→2줄(중간 흔들림 줄 제거)·"요즘의 너는?" 축약·.mention 17→14.5px, DustOrb stage를 `1 + 답한 문항수*0.5`로(제자리 성장+펄스+성장 링). 이름은 선택·한글 유지 답변(성명학=한자 필요라 미채택, 오직 호칭).
- **검증**: 렌더 4프로필 대조 — 토(3갈래 라벤더)·금(금빛 6엽 별)·목(다엽 나무)·수(초록 물기둥) 완전 상이 확인. 리빌 mid-MBTI 캡처: 세로 오버플로 0px(스크롤 불필요), q1→q3 오브 성장·링 육안 확인. 전 배터리 55/55.
- **미해결 고지**: 모바일 아티팩트 판결 간헐 성공/실패 = Anthropic 서버 간헐 고장(코드 무관, 폭포수가 처리). 대운 외부 대조·실기기 FPS·야자시 여전히 보류.

### [22] v28 — 심화 지표를 수호신에 배선 + 판결 반응 연기 (지문화)
- **요청**: ①"사주뿐 아니라 심화버전(납음·촐킨·나크샤트라·대운)도 넣었는데 그건 디자인에 영향 안 줘? 사주 베이스는 결국 5형태 변주라 비슷해 보여" ②절기 넣었냐(→ 사용자: 안 넣어도 됨) ③"판결할 때 괘 결과에 따라 수호신이 다른 모습/행동 보이면?" + 지난 합의(상단 문장 1회성·이름 실명 카피). "다 진행".
- **진단**: 수호신 비주얼이 읽는 건 오행(형태)·별자리(색)·라이프패스(팔)·MBTI(크기·질감)·달(밝기)뿐. 납음(30)·촐킨(260)·나크샤트라(27)·대운은 전부 판결문 텍스트로만 가고 캔버스엔 미반영 → 260×30×27 지문을 계산하고 5형태만 그림. 규율: 각 지표를 서로 다른 지각 채널에 배선(안 그러면 만다라 노이즈).
- **구현**:
  - ① GuardianCanvas에 birth 전달, 내부에서 촐킨(20날개→코어 마디 수 3~8·13톤→층 1~3/강도)·납음(30→흐름 2차 진동 주파수·진폭)·나크샤트라(27→강조색 hue 갈래)·대운(현재 간지 오행→중앙 아우라색) 파생. 중앙에 회전하는 코어 문양(사람마다 다른 '심장') 신설.
  - ③ reactRef 신설, 판결 낙착 시 direction 세팅 + 카드 지연 700→1400ms(몸짓 먼저 보이게). draw()에서 GO=목표위치 +0.5 확장·밝기 +0.5 / STOP=−0.45 수축·−0.55 감광 / HOLD=잔잔한 ±맥동. 0.18s 상승→1.7s 소멸 엔벨로프.
  - 상단 자기소개(guardianIntro)는 introSeen 게이트로 첫 만남 1회만(첫 질문 시 off). 이름 카피 "이름이어도 별명이어도"→"네 이름을 다시 들려줄래 — 어릴 적 내가 부르던 그 이름"(실명 유도, 스킵 유지). 절기 미채택.
- **검증**: 렌더 실측 — 같은 납음-금 2명(칼끝의 금/백랍의 금)이 촐킨 코어·형태·색으로 완전 상이. 판결 GO(팔 펼침+발광) vs STOP(수축+감광) 대비 뚜렷, 코어 노드 고리 가시. 전 배터리 55/55(카드 지연 변경에도 판결 9/9 유지).
- **보류**: 실기기 FPS(코어 문양·반응으로 draw 부하 소폭↑), 대운 외부 대조, 야자시. 배포는 사용자 판단 대기.

### 아직 안 끝난 것 / 다음
- **[선택] v17 B단계** — WebGL(three.js/regl) 전환. A단계 반응 본 뒤 결정.
- **[관찰필요] 저사양 안드로이드 FPS** — 4,200 입자 실기기 확인.
- **[미해결] 텍스트/수호신 겹침** — 수호신이 강해지며 gtext 가독성 저하. 레이아웃 조정 후보(A단계 범위 밖).
- **[사용자 액션] Vercel 배포** + `ANTHROPIC_API_KEY` + 지출한도, 실기기 실API 테스트.
- **[미착수] 키프리스 '비나리' 상표 검색.**

---

## 2026-07-20 · 세션 (계속) — 아티팩트 구동 진단 + v29 정독 다이어트

### [A] "아티팩트로 구동" — 두 런타임의 차이 규명
- **요청**: v28을 아티팩트로 띄워라. 이어서 "첨부 JSX 그대로 React 아티팩트로, 코드 수정 말고".
- **판단**: 브라우저는 JSX 원문을 못 돌리므로 JSX→JS 변환만(로직 무수정)으로 단일 HTML 자립화 — App.jsx의 `import react`/`export default`/테스트 export 3개 배관만 조정, esbuild로 JSX 트랜스파일, React18 UMD 인라인. Artifact 툴로 발행(같은 파일경로=같은 URL 유지).
- **결과 & 진단**: 발행 URL에서 온보딩·수호신·의식·괘까지 전부 정상이나 **판결만 "Failed to fetch"**. 원인 = 런타임 이원화: claude.ai **대화창** 아티팩트는 `window.claude.complete` 주입(→판결 O), Claude **Code 웹** 아티팩트는 미주입(→`/api/judge` 없음→직접호출 CSP 차단). artifact-capabilities 스킬로 확인: 발행 아티팩트가 선언 가능한 능력은 `downloads`·`mcp`뿐, completion 능력 자체가 없음. 즉 코드 무죄, 경로 문제. → 사용자에게 대화창 붙여넣기 경로 안내, v28 JSX 전달. **판결 완주 확인됨**(대화창에서 가치/바이오리듬/마야/토정 L3까지).

### [B] v29 — 실사용 4지적 반영 (정독·인지)
- **요청**(대화창 판결 완주 후): ①온보딩 회상 텍스트는 한 번만, 선택 땐 숨김 ②수호신 자기소개(칼끝의 금…)는 생성 때만 ③판결이 백에서 이펙트 돌아 느림·스크롤도 느림 ④(재확인) 판결 뱉을 때부터 전체 이유를 백에서 돌려라, 결론부터.
- **판단**: ④는 이미 구현(콜1 결론 setRes 직후 fetchDetail 선로드, 카드 뒤집기 전 완성) — 코드로 확인. 체감 지연의 진짜 원인은 ③ 캔버스(3.2~4.2k 입자 60fps)가 메인스레드를 점유해 콜2 렌더·카드 뒤집기·스크롤과 경합. → 판결 대기·정독 구간에만 프레임 솎기.
- **구현**(transform34, 9치환):
  - ① reveal 화면의 회상 나레이션(mention)+근거박스(refbox)를 `dims 0개일 때만` 렌더 → 첫 MBTI 픽부터 숨김(짧은 "요즘의 너는?" 헤딩·문항만 유지).
  - ② guardianIntro 게이트를 introSeen→**justBorn**으로: 탄생(3.2s) 순간 on, 10.5s에 off. 질문 화면 상주 제거. 재회 인사는 introSeen 유지.
  - ③ **restRef**(busy||res 동안 true) + GuardianCanvas draw 스로틀: rest && !요동 && !반응(1.8s) && 46ms 미만이면 프레임 스킵(~21fps). 반응 연출·대기 요동은 풀프레임 보존.
- **검증**: 빌드 OK, 스모크22/판결9/만세24 = 55/55 유지. 신규 `e2e/v29-check.mjs` 7/7 — ①선택 전 노출→첫 픽 후 숨김·문항 계속 ②탄생 직후 자기소개 노출("나는 '길가의 흙'의… 존재야") ③스로틀 중 판결 L1 렌더·캔버스 생존·무오류. 렌더 2컷 육안(MBTI 정리·탄생 인사). 아티팩트 HTML v29 재빌드→같은 URL 재발행.
- **보류/다음**: 실기기 스크롤 체감(헤드리스 통과, 실측은 사용자), 자기소개 소멸 페이드(현재 즉시 제거), Vercel 배포(공유 URL 판결). 판결문 형식·콜1/콜2 구조 불변.

### [C] 배포 테스트 관문 — Anthropic API 크레딧 결제
- **상황**: 사용자가 Vercel 배포(공유 URL 판결)를 위해 Anthropic 콘솔에서 API 크레딧 US$5 구매를 시도 → **구매 버튼 비활성**. 화면상 총 결제 금액·예상 세금이 `$--`(미산정)로, 청구지 주소/결제수단 미완이 유력.
- **판단(중요)**: **자기 테스트는 결제 불필요** — claude.ai 대화창 아티팩트(`window.claude.complete`)로 이미 판결 완주 확인됨(구독으로 커버, API 과금 없음). API 크레딧은 **Vercel 공유 URL(다수 테스터·상시 가동)** 경로에서만 필요. 결제는 급하지 않음.
- **다음**: (선택) 배포로 갈 경우에만 크레딧+환경변수+지출한도. 그 전까지 v29 대화창 아티팩트로 ①②③ 체감 검수.

---

## 2026-07-21 · 세션 (계속) — Vercel 실배포 + v30 실사용 피드백

### [D] Vercel 배포 완료 (0단계 달성)
- **경과**: 사용자가 API 크레딧 결제 후 배포 결정. Vercel 임포트 화면에서 두 함정 발견 — ①`main`이 34커밋 낡음(app이 1바이트 파일이던 초기 커밋) ②Root Directory `./`. 사용자 명시 허락받아 피처 브랜치를 **main에 fast-forward 머지**(GitHub MCP로 실제 반영 확인) → Vercel이 최신 main 재스캔하니 `app/`이 폴더로 노출 → Root=app 선택 가능. `ANTHROPIC_API_KEY` 설정, Deploy.
- **결과**: `binari-sepia.vercel.app` 라이브. 서버 프록시(`/api/judge`) 경로로 **판결 완주 확인**(window.claude.complete 없이). 아티팩트 URL에서 막히던 판결이 배포 URL에선 뚫림 = **0단계(아티팩트 탈출) 사실상 달성.**
- **운영 규칙(사용자 지시)**: 이후 main은 "푸시해" 지시가 있을 때만 올림. 코드는 main 자동배포(1~2분), 환경변수는 수동 Redeploy.

### [E] v30 — 배포 실사용 피드백 6종 (한 배치)
- **요청(실사용 중 연발)**: ①판결 카드 뜨면 뒤 수호신 안 돌아도 됨(RAM·스크롤 막힘) ②부적 버튼 안 눌림 ③재설정('다른 사람이야?') 무반응 ④동전 던질수록 CTA가 밀려 내려감 ⑤CTA를 버튼인지 모름(예시문인 줄) + 캔버스가 배경과 네모로 구분됨 + 모델 추천.
- **진단·구현**:
  - ①②는 **동일 원인**: 카드 뜬 뒤에도 4,200입자가 메인스레드 포화 → 클릭·스크롤 드랍. restRef를 3단계 수치(0/46/300ms)로: 대기 46ms, **카드 정독 300ms(~3fps 사실상 정지)**. 반응 1.8s는 풀프레임 유지. → 스크롤·부적클릭 회복.
  - ③ `window.confirm`이 웹서 씹힘(b 케이스) → 앱내 확인 UI(응/아니).
  - ④ `.hexlines min-height:8px`→`88px`로 6효 공간 예약 → 버튼 불변.
  - ⑤ 캔버스 가장자리 radial 마스크(58%→88%)로 네모 경계 제거. `.btn.ghost` 색이 `#8a7f95`(본문 회색)라 버튼 인식 실패 → 금색 미세 채움+테두리+그림자+hover/press. `.dimopt`도 소폭 강화.
  - **회상 나레이션 순차**(별도 지시): mention+근거를 문항과 한 화면에 두지 말고 → 먼저 노출→'응,기억나' 탭→문항(recallSeen). v29의 dims-게이트 대체.
  - **모델 Sonnet4.6→Sonnet5**: 인트로가($2/$10, 8월까지)로 품질↑·비용≈. 단 Sonnet5 기본 thinking-on이 콜1(320토큰)을 먹으므로 `thinking:{type:"disabled"}` 필수 추가(judge.js+callDirect).
- **검증**: 빌드 OK(컨테이너 재활용으로 node_modules dev deps 유실 → `npm ci` 복구), 63/63(smoke22·판결9·만세24·v30체크8), 데스크톱 렌더로 네모경계 소멸·CTA 버튼화 확인. v30 스냅샷 승격. main 푸시(자동배포).

### 백로그 (배포 전/중 결정 대기)
- **[결정됨] 데이터 계측 = PostHog(무료)** — 익명ID+핵심 퍼널+D1/D7. 송신 추상화 한 겹 두어 나중 Supabase/자체수집 전환 가능(사용자 확인). 질문원문·실명 미전송 원칙. 아직 미착수.
- **[탐색] 수호신 무(無)구심점 시각** — 사용자 레퍼런스 3종(불=꼬여오르는 리본/바람=꼬리 소용돌이 유속/연기=난류 블룸) 전부 중앙 코어 없이 흐름으로 존재. 현재 가디언은 코어 글로우+촐킨 코어 문양+금 방사살로 구심점을 강제. 자유화하려면 파라메트릭 유일성(오행·촐킨·납음·나크샤트라)·시드 재현·모바일 60fps를 어떻게 보존할지가 관건. Canvas2D로 리본/유속 근사 가능, 난류 블룸은 WebGL(로드맵 B단계) 후보. → **[F]에서 B단계로 실행됨.**

---

## 2026-07-21 · 야간 자율 루프 — v31 WebGL 수호신 (로드맵 B단계)

### [F] WebGL 리라이트 — "이쁜 거부터" (사용자 지시: 밤새 작업+검증, 기존 버전 폴백 유지)
- **요청**: PostHog보다 WebGL 먼저. 기존(Canvas2D) 유지·폴백. 아침까지 /loop 자율 반복으로 검증까지 완료. 중간에 "일단 푸시 해놔" → v31 초판을 main에 푸시(라이브 반영), 이후 튜닝 계속.
- **설계 판단**:
  - **라이브러리 0** — three.js(+150KB) 대신 raw WebGL. 입자 위치를 **정점 셰이더가 시간 함수로 계산**(상태 없음) → 버퍼 피드백 불필요, 코드 단순, 번들 +8KB, 메인스레드 완전 해방(v30의 "카드 뜨면 정지" 땜빵이 GL 경로에선 구조적으로 불필요하나 배터리 위해 rest 스로틀 유지).
  - **무구심점**: 중앙 글로우·촐킨 코어 문양 폐지. 유일성은 흐름의 성질로 재배선 — 촐킨 20날개=가닥 수(3~8)·13톤=꼬임 밀도, 납음=결(주파수·진폭), 나크샤트라=강조색, 대운=색조 틴트. 시드(seedRnd) 동일 → 재현성 보존.
  - **형태 5**(레퍼런스 매핑): 화=꼬여 오르는 리본 기둥(ref 불) · 수=흐르는 물결 층 · 목=뻗는 가지 흐름 · 금=궤도 빛줄기 · 토=중심 없는 난류 융기(ref 연기).
  - **폴백 3중**: glDetect 실패·셰이더 컴파일/링크 예외·webglcontextlost → 전부 기존 GuardianCanvas(Canvas2D, 무수정)로 즉시 전환. `?r=2d`로 강제 폴백 디버그.
- **검증 루프 기록**:
  - R0(초판): webgl-check 7/7 신설 통과(SwiftShader, 렌더러 표식 `data-renderer` 부여) → 사용자 지시로 main 푸시(`eaf414a`).
  - R1 캡처: 전반 과암·화 코일 기계적·목이 **아래로 자람**(클립공간 y부호 실수)·토 도넛(중심 공백). → 튜닝 R1: 알파·포인트 상향+유사 블룸 패스(u_psMul), 화 가닥 해시(반경·위상·속도 가닥별 상이+기둥 사행), 목 상향+뿌리 분산, 토 pow0.75+2옥타브.
  - R2 캡처(오행 5종 생일 스캔: esbuild로 App.jsx export를 노드에서 로드해 calcSaju 스캔): 금 백화 도넛 발견 → 오행별 알파 계수 F_AL(금 0.3, 밀집형 하향)+반경 분포 확대+포인트 계수 F_PS. 목 뿌리 핫스팟 smoothstep 컷. 표적 재캡처로 5형태 전부 육안 통과 — 특히 화 리본(적)·화형 금색 조합이 레퍼런스에 근접.
  - 함정 기록: 캡처 프로필의 시각(시주)이 바뀌면 main 오행이 뒤집혀 태그가 어긋남 — 스캔은 12:00 기준으로 통일해야 함.
- **결과**: 테스트 70/70(smoke22·판결9·만세24·v30체크8·webgl7), 렌더 5형태 육안 OK. v31 스냅샷 승격, 문서 4종 갱신. 튜닝(R1+R2)분도 main 푸시 — 초판(목 역방향·과암·금 백화)을 라이브에 남겨두는 게 더 나쁘다고 판단, "일단 푸시" 지시의 연장으로 해석.
- **다음(사용자 결정 대기)**: PostHog 계측 착수. B단계 후속(실기기 FPS·프레임버퍼 트레일)은 선택.

---

## 2026-07-26 · 세션 — G0-a 계측 분리 완결 (개발 큐 검수 + PostHog 격리)

### [A] 상황: 개발 큐 4건(D1~D4)이 v93에 이미 구현돼 있었다
- 이 세션의 클론은 v45였고 main은 v93. `git fetch` + 머지(무충돌) 후 확인하니 `한국본진실행계획_v01` §5-1의 개발 큐 4건이 **전부 main에 구현·주석까지 D1~D4로 표기**된 상태였다(별도 세션에서 실행된 것으로 보임 — 이 세션은 구현이 아니라 **검수**를 수행).
- 구현 확인 내역: D1 `binari.internal.v1`(+`?i=1` 영구화, super property `is_internal`) · D2 `binari.firsttouch.v1`(utm 5종+클릭ID 종류만 저장, 최초 1회 고정) · D3 `binari.belief.v1`(첫 판결 후 1탭, `belief_answered`, 민감정보 우려로 PROFILE_KEYS에 넣어 동의자 한정 — 전체 집계 필요 시 빼면 됨) · D4 `letter_clicked`(노출=verdict_shown이라 CTR 계산 명확, 결제 미구현).
- **검수 결과**: `vite build` OK · 스모크 **26/26** · 런타임 검증(Playwright) — ①utm 유입 후 파라미터 없는 재방문에도 `ft_content=A_h2_fire` 유지(D2 핵심: direct 덮임 방지) ②`?i=1` 후 재방문에도 `is_internal:true` 유지. 검증 스크립트는 일회성(커밋 안 함).

### [B] PostHog측 분리 — G0-a의 나머지 절반
- **정적 코호트 436757** "내부 테스터(계측분리 이전)" — 현재 전체 유저 4명 스냅샷(전략로그 [3]에서 외부 유입 0건 확인된 그 4명). 플래그 도입 이전 데이터를 영구 격리.
- **게이트 대시보드 1907151** "외부 유저 전용(G0~G3)" — 인사이트 4종, 전부 `is_internal ≠ true` + 코호트 436757 제외 필터:
  1. `xscpleu9` G1 D7 리텐션(target=verdict_shown, return=app_open, 게이트 20%)
  2. `1oXlGP4C` 완주 퍼널(app_open→birth→awaken→question→verdict, 완주CAC 분모)
  3. `e67ieZvV` fake-door CTR(letter_clicked/verdict_shown, 죽음신호 1%·0% 명기)
  4. `l8iwivtm` 소재별 완주 유저(ft_content 분해 — 소재 판정 축)
- **판단**: 리텐션 target을 app_open이 아닌 `verdict_shown`으로 — G0-b 카운트 단위("판결 1회 이상 본 비내부 유저")와 모수를 일치시키기 위함. 실행계획 §5-5와 동일 논리.

### [C] G0-a 통과 판정
실행계획 §5-1의 통과 기준 3항 중 ①외부 필터 대시보드 존재 ✅ ②신규 유입의 utm_content가 재방문까지 유지 ✅(런타임 실측) ③기존 4명 내부 코호트 편입 ✅. **G0-a 통과. 다음은 G0-b — 개발 라인 작업 없음, 소재 12종 제작·메타 집행(마케터 라인)이 크리티컬 패스.**
- 주의 1: 팀 5인은 **각자 폰·PC에서 `binari-sepia.vercel.app/?i=1`로 1회 접속**해야 이후 트래픽이 내부로 태깅된다(새 기기·시크릿창은 다시 필요). 공유 링크에 `?i=1`이 섞여 나가지 않게 주의.
- 주의 2: v93 코드는 main에만 있고 이 브랜치는 main을 머지해 반영. **main 재배포는 불필요**(이미 라이브).

## [2026-08-02] v106 · 셈 — 실기기 스샷 3장에서 역산한 카드 손질

유저 실기기 스샷 3장("저녁 밥"·"점심"·"런칭할까")의 위화감을 진단 → 모델 탓이 아니라 배선·프롬프트임을 확인(Vercel 런타임 로그: free/paid 전부 claude-sonnet-5, 하이쿠 미사용).

- **①이 핵심**: `tallyVotes`의 against(반대 표)를 표시부가 그대로 받아 "찬성"이라 적음. GO가 강할수록 약해 보이는 역전 — 스샷 세 장 전부 "N개 중 1개 찬성"이었던 이유. 라벨 의미는 질문의 행동 기준(찬성=GO표)으로 정리하고 표시 수는 total-against.
- ②④⑤는 위화감의 나머지: 톤 내부값 노출("· 격려"), 풀네임 3연호("강석우"×3), 세 층이 같은 말 반복.
- 검진에 ①② 재발 검사 추가. HEAD(수정 전)로 되살려 둘 다 검출되는 것 확인.
- 회귀: 검진 0심각 / smoke 25/25 / verdict 29/29 / scope 54/54 / webgl 7/7 / v29 8/8 / 만세력 28/28 / 계측 24/24 / 출처 20/20.
- 남긴 것: 속결(오늘의 물음)에도 運命合意判決 도장·神 인장이 붙는 문제 — 시각 요소 변경은 헌장(판결문 형식 보존·톤 변경 범위는 프롬프트 문구뿐)에 걸려 보류. 바꾸려면 먼저 물을 것.

## [2026-08-02] v107 · 그릇 — 상세 리포트를 명리 판단 순서로

실사용 명식 하나를 실제 로직으로 뽑는 과정에서 리포트의 구조 문제 발견 → 딥리서치(동률 해석·용신·철학관 구성)로 확인 → 반영.

- 십성 동률: slice(0,3)이 삽입 순서를 대표로 만들던 것 → 3위 동값 전부 표시. 프로브로 7개 표기 확인.
- 대운 사다리: daeun()을 10년 간격 호출해 여든까지. 성별 없으면 안내 줄("성별을 더하면 펼쳐줄게"). 비어 있던 오행이 들어오는 구간에 라벨 — 서신 예시의 "빈 금이 채워지는 구간" 장치를 무료 리포트에도 일반화.
- 신강·신약: 통설 개수 판정(비겁+인성 4↑/2↓)을 '간이'로 명시하고 계측(strength·lack_el)에 추가.
- 조후용신·격국은 보류 — 궁통보감 조견표를 검증 없이 넣는 건 지어내기와 같다(전략로그 2026-07-28 기준 유지).
- e2e/report-check.mjs 신설: 카드 뒷면(리포트)은 어떤 e2e도 열지 않던 자리. 성별 버튼 문구('남')와 뒤집기 조건(why+콜2 도착)을 실제 경로대로 태움. 9/9.
- 전체 회귀 그린: smoke 25 / verdict 29 / scope 54 / webgl 7 / v29 8 / 만세력 28 / 계측 24 / 출처 20 / report 9. 검진 0심각.

## [2026-08-02] v107.1 · 용신 — 보류를 '지어내지 않는 범위'로 좁혀 반영

직전 v107에서 조후용신·격국을 보류했다(원문 조견표 미확보). 다시 보니 보류할 것과 채택할 것이 섞여 있었다.

- **억부용신은 보류할 이유가 없었다.** 신강·신약에서 기계적으로 도출된다 — 신약이면 인성·비겁, 신강이면 식상·재성·관성. 조견표가 필요 없고 지어낼 여지도 없다. 딥리서치 확인 결과 실무 적용률 약 70%.
- **조후는 표를 흉내 내는 대신 계산 가능한 사실로만.** 월지 계절 + 오행 편중(여름 화≥3 → 수·금 / 겨울 수≥3 → 화·목). 궁통보감 120칸을 웹 인용으로 재구성하는 건 토정비결 원문 지어내기와 같은 위반이라 하지 않았다.
- **갈릴 때 우리가 정하지 않는다.** 일치=강조, 불일치=둘 다 + "유파에 따라 달라지는 자리". 이게 유파 문제를 회피가 아니라 정직으로 푸는 방법이다.
- 검산: 둘째(병오 을미 임자 을사) 억부 금·수 / 조후 수·금 → 일치. 딥리서치가 찾은 궁통보감 미월 임수 처방(신금 정인으로 수원)과 독립적으로 같은 답.
- 대운 사다리에서 용신 구간을 ★로 띄우니 "언제가 내 계절인가"가 한눈에 잡힌다 — 유료 서신의 핵심 장치를 무료 리포트에 일반화한 셈.
- report-check 9→12. 전체 회귀 그린, 검진 0심각.

---

## 2026-08-11 · v110 — 정직성 4규칙 (리포트·서신)

**요청** — 외부 사주 프롬프트("30년 경력 역술가" 계열)를 놓고 차용 여부 판단 → 앱 반영.

**1차 판정과 그 오류.** 나는 차용 4 / 기각 4로 갈랐는데 **기각 쪽이 틀렸다.** 분량 지침과
연애·결혼·직업·건강 같은 삶의 영역 대부분을 잘라냈다. 창업자 교정:

> *"리포트는 운명을 알고 싶은 사람을 위한 서비스야. 알 권리는 최대한 보장해줘야 해.
> 네가 버린 것 중에 무조건 피해야 하는 '언제 죽는지', '자살' 같은 치명적 내용은 없는 것 같아.
> 수호신이긴 하지만 인간 역술가를 사칭하라기보다는 역술가의 화법이 왜 그렇게 이뤄졌는지 생각하고,
> 인간이 궁금해할 만한 부분은 말해줘야지."*

**내가 놓친 것** — §3-5에서 '판결=모를 권리 / 리포트=알 권리'로 이미 갈라놨는데도, 리포트의 범위를
다시 판결 기준으로 재단했다. 유저는 **운명을 알고 싶어서** 값을 치른다. 안 쓰는 건 배려가 아니라 은닉이다.
→ 금지선을 셋으로 확정: **수명·죽음의 시기 / 자살 암시 / 의학적 판정(병명·수술·투약)**. 그 밖은 전부 다룬다.
역술가 페르소나는 **사칭만** 금지하고 화법의 기능(전 영역·구체성·궁금한 것 먼저)은 가져온다. 상세는 구상 §3-8.

**구현 — 상세 리포트**
- **확신도 3단 꼬리표**(`CF`/`Cf`). 계산값(만세력에서 그대로) / 통설 해석(유파 갈림) / 곁가지(신살).
  범례를 본문 맨 앞에 둔다. 명식·읽지못한것·타고난것·흐름=계산값, 힘의 저울·채울 기운·짝·일=통설, 신살·띠·날=곁가지.
  — 셋을 같은 목소리로 말하면 **전부가 헐거워진다.** 반증을 파는 회사에서 가장 값싼 정직성이 이것이다.
- **'읽지 못한 것' 절 신설.** 시 미상·성별 미입력·도시 미입력·미래 생일이 각각 **무엇을 못 잡게 하는지**를
  한자리에 모았다. 전에는 이 사실들이 다른 문장 꼬리에 흩어져 있었다. 빠진 게 없으면 **"전부 읽혔어"라고 말한다** —
  '침묵'과 '완전'은 유저에게 전혀 다른 정보다.
- **십성 해설 3단화.** `SS_TIP`을 `{e 쉬운 말, r 실제로 나타나는 모습, s 그늘}` 로 폈다.
  앞 두 단만 쓰면 사전을 베낀 것이고, 세 번째가 있어야 이 사람 얘기가 된다. 그늘은 겁주기가 아니라 **같은 성질의 뒷면**이다.
- **배우자궁 공개**(`SPOUSE`). 일지 십성은 계산해 두고 화면에 안 쓰던 값이다. 위에서 '일=나·배우자'라고
  써놓고 정작 배우자 자리를 안 읽어주는 건 알 권리에 어긋난다. 사람들이 사주에서 가장 먼저 묻는 자리이기도 하다.

**구현 — 서신(콜3) 프롬프트**
`[정직성]` 블록 신설. ①근거의 급을 말투로 구분(계산값은 단정 / 유파 갈림은 "통설로는" / 신살·띠는 "참고로",
그 위에 결론 금지) ②없는 정보는 없다고 쓴다(시 미상이면 시 관련 서술 금지 — 추정으로 메우면 나머지도 의심받는다)
③용어 3단 ④좋은 면 옆에 그늘. 범위는 좁히지 않았다 — 서신은 여전히 '이 질문 하나'에 답하는 상품이다.

**검증** — 빌드 통과. report-check **20→26/26**(확신도 3종·범례·읽지못한것·3단 해설·배우자궁 신규 5건).
smoke 25/25 · verdict 34/34 · scope-check 54/54. 화면 오류 0.

**부수** — 작명 엔진 v02 뜻 필터에서 이름자 두 개가 조용히 죽던 버그 교정(`瑥` 사람 이름 온 = 훈이 '사람 이름'뿐이라
화이트리스트에 걸릴 단어가 없음 / `利` = 표준 훈 '이로울'이 수집 사전에 누락). 작명용으로 만들어진 글자가
작명 필터에 탈락하는 역설이었다.

---

## 2026-08-11 · 리포트 구조 전환 + 판독기 v01

**요청** — *"리포트는 사주 베이스가 아니고 각 항목별 설명이 들어가는 형태. 태어날 때 정해진 운명이 이랬어,
그래서 크다가 이런 느낌이 들었을 거야, 넌 지금 어느 단계야, 앞으로 이렇게 될 거야."*
그리고 *"선을 좀 넘어. 어떤 일이 생길지 딱 명확히 말하든가, 어떤 류의 일이 생기는데 예를 들면 머머머다 이렇게 하든가."*
*"너무 어렵게 설명해서 알 권리를 해친다."*

**한 것 — 순차 4단계, 각 단계마다 검증**

1. **전략로그 기록**(`2477602`) — 쌓인 판단 5건(과금 층·차별점·각인 세계관·매개 화자·항목별 4단)과 위험 3건.
2. **항목별 데이터 계산** — 두 명식의 십성 그룹·재고·대운별 십성·세운을 뽑았다. 여기서 서사 둘이 새로 나왔다:
   동생은 **재물의 창고(戌)가 21~30세 대운에 열리는데 하필 그 구간이 여든 해 중 유일한 골**이고,
   형은 **식상이 0인데 58세에 처음으로 표현의 대운**이 온다.
3. **리포트 재작성** — 열 항목(성격·배움·재능·일·돈·연애·결혼·자녀·몸·사람) × 4단.
   항목마다 여든 해 위에 지금 위치를 찍는 미니 타임라인. 「지금」 칸은 금색 밴드.
4. **판독기 v01** — 아래 별항.

**구체성을 올린 실제 예** — *"재물이 움직인다"* → *"스물하나에서 서른 사이에 한 번 크게 잃는다.
형태는 셋 중 하나다 — ①빌려주고 못 받는다 ②동업했다 갈라선다 ③될 것 같은 데 한 번에 넣었다 날린다."*
*"직업 변동 가능성"* → *"첫 직장을 삼 년 채우지 못할 가능성이 높다."*
금지선 셋(수명·죽는 때 / 자살 암시 / 의학적 판정)은 그대로 지켰다.

**검수에서 잡은 것 셋**
- **클래스명 충돌** — 새로 만든 선언문 `.say`가 최상위 「수호신이 말하는 자리」 `.say`와 겹쳐
  엉뚱한 회색 박스가 깔렸다. `.decl`로 분리(9곳). 문서 높이 1,118px 감소로 확인.
- **타임라인 라벨 겹침** — 0세 아이라 「지금」 마커가 0 눈금과 붙어 "0지금"으로 찍혔다. 26px 이내면 눈금을 숨긴다.
- **갈림 표시 누락** — 「여러 하늘의 증언」에서 `.sky.no`에 스타일이 없어 **게이지에만 붉게 찍히고 표에서는 안 보였다.**

**판독기 v01** (`05_실험/판독기_v01/`) — 11종 + 자체 검증 27건.
**검증이 실제로 오류를 잡았다**: 자바 웨톤 기준점을 자바력 원년(1633-07-08 율리우스 = '금요일 레기')에 걸었는데
**그날은 금요일이 아니라 월요일**이었다. 그레고리 환산과 율리우스력 직접 계산이 같은 답을 냈다.
1945-08-17 인도네시아 독립일로 교체했고, **절대 명칭은 여전히 외부 확인이 필요하므로 `derived`로 표시**했다 —
검증 못 한 것을 검증했다고 쓰지 않는다.

**판독기가 낸 실제 결과**
- 동생: 여덟 글자(큰 물) · 납음(천하수) · 구성기학(일백**수**성) — **셋이 독립적으로 "물"**. 합의.
- 형: 여덟 글자가 *"표현 자리 0"*인데 서양 태양·달이 **둘 다 공기**(말하고 옮기는 기운). 갈림.
  → 모순이 아니라 **"안에는 공기가 가득한데 나올 문이 안 났다"**로 합쳐졌다.
  **갈리는 자리가 그 사람의 급소를 가리킨다** — 이게 이 절의 상품성이다.
- 형제의 라이프패스가 **둘 다 6**. 여덟 글자는 거의 모든 것이 반대인데 이 수만 겹친다.

**검증** — 리포트 구조 검사(10항목 × 4단 = 40단, 「지금」 10, 타임라인 10, 판독기 13, 해부축 8) 통과.
넘침 0, 화면 오류 0. 판독기 27/27 PASS.

---

## 2026-08-14 · v116 — 각인 정직성: **유저가 아는 값을 맞히려 들지 않는다**

### 요청
창업자 실사용 제보. "팩트랑 다른 게 너무 많다. 나 키 178이고, 와이프는 170이야. 우리는 20대에 만났고.
이런 것들은 근거가 뭐였어? 평생 약한 곳은 간과 눈, 힘줄이라고 나오는데,
나는 사실 폐렴, 충수염, 당뇨 등 대사기관이 약한 편이야."

### 근거를 되짚은 결과 — 셋 중 둘은 버그, 하나는 근거 없음

**① 키 — 근거 없음. 내가 지어낸 표였다.**
`H_EL`(일간 오행 → ±cm) · `H_ASC`(상승궁 → ±cm)로 남 172 / 여 160 기준선에 보정을 얹고 있었다.
**어느 유파에도 오행을 cm로 환산하는 표준이 없다.** 각주에 "추정"이라고 적어 두긴 했지만
그 각주는 라이브에서 안 보인다 — 유저가 보는 건 「178~184cm」라는 굵은 글씨뿐이다.

**② 짝을 만나는 시기 — 상수를 증언으로 세고 있었다.**
"세 가지 셈이 전부 이 나이를 가리킨다"의 세 번째(서양)가 프로펙션 7하우스였는데,
프로펙션 7하우스는 `나이 % 12 == 6`, 즉 **상승궁이 무엇이든 누구나 6·18·30·42세**다.
24~40 필터를 통과하는 건 **언제나 30 하나뿐**. 게다가 최종 답을 그 격자에 스냅시켜서,
**200명을 돌리면 148명(84%)이 "30세"**로 나왔다(실측). 개인차 0인 값을 정족수에 넣은 것 —
정직성 판정(≤8년 합의) 자체가 무의미했다. 독립이 아닌 것을 표로 세면 합의가 아니라 조작이다.

**③ 약한 장기 — 축 하나만 봤다.**
`weakEl = 비어 있는 오행` 하나로 못 박았다. 그런데 명리 건강론에서 병은 **태과·불급 양쪽**에서 온다.
목 0개 → "간·눈"이라고 적었지만 실제 병력은 폐렴(금)·충수염·당뇨(토) — **넘치는 쪽**이었다.
코드에 `over`(태과) 필드를 이미 갖고 있으면서 `maxEl >= 4`일 때만 보조로 붙였다. 8글자에 4개는 드물다.

### 고친 것

**규칙 하나를 새로 못 박았다(파일 헤더 §⑤).**
> **유저가 정답을 아는 값은 맞히지 않는다. 받아서 해석한다.**
> 맞혀도 "그래서?"이고 틀리면 나머지 서른 칸까지 같이 죽는다. 비대칭이 너무 크다.

- **키**: `H_EL`/`H_ASC` 삭제. 선택 입력으로 받고, **안 적으면 키 얘기를 아예 안 한다.**
  받으면 통계 평균(질병관리청 국민건강영양조사 남 172·여 159) 대비로 해석한다.
- **짝의 시기**: 서양 축 제거 → 두 축(대운·다샤)만. 격자 스냅도 제거.
  후보가 하나뿐이면 "셈이 하나뿐"이라고 적는다. 분포가 22~45로 퍼졌다(최다 22세 33%).
- **약한 장기**: 세 축(비어서·넘쳐서·눌려서) 전부 계산. 겹치면 한 곳, 갈리면 둘 다 적고
  **"원인이 반대라 대처도 반대"**까지 쓴다. ⚠ **피극은 불급과 같은 자리를 가리키면 표를 안 더한다** —
  "금이 목을 친다"와 "목이 0개"는 같은 사실의 두 표현이지 증거 둘이 아니다.
  이걸 그냥 세면 목이 2표로 굳어 금(폐)이 본문에서 사라진다. ②와 정확히 같은 종류의 오류다.
- **기혼자 분기**: 배우자 외모·집안·벌이 **예언을 걷어냈다.** 기혼자에게 그건 예언이 아니라 오답 노출이다.
  대신 관계 해석으로 바꿨다 — 무엇에 끌렸나 / 잘 되는 지점 / 부딪히는 지점 / 조심할 것 / 흔들리는 때.
  `SPOUSE` 표에 `g`·`b` 필드를 새로 채웠다(`d`를 마침표로 잘라 쓰니 "안정적이다." 세 글자가 한 항목이 됐다).
- **선택 입력 2개 추가**: 만난 시기(기혼일 때만 뜸) · 키. 여전히 전부 선택이고 각인 열 때만 묻는다.
- 잔버그: "힘줄야"(조사), "…온다 사람이"(마침표 누락), "간과 눈는"(은/는),
  기혼자에게 **78~87세에 흔들린다**(지난 구간까지 세고 있었음 → 현재 이후·70세 이하만).

### 검증
각인 42/42(신규 16) · 리포트 65/65 · 스모크 25/25 · 판결 34/34 · 스코프 54/54 ·
v29 8/8 · WebGL 7/7 · 여러 하늘 12/12 · 검진 clean(신규 검사 1 추가).

새 검사 중 셋이 **이번 사고를 직접 겨눈다**:
- 「짝의 시기가 한 나이로 쏠리지 않는다」 — 120명 돌려 최다 나이가 50% 미만인지
- 「키를 안 주면 키 얘기를 안 한다」 — 본문에 `cm`이 한 글자도 없는지
- 「기혼에게 배우자 외모·집안·벌이를 안 쓴다」
검진에도 「지어낸 사실 — 키는 받아서 해석」을 추가해 `H_EL`/`H_ASC` 부활 시 즉시 울게 했다.

### 남은 것
`FACE`·`VOICE`·`BUILD`도 검증 가능한 주장이지만 **본인이 거울로 확인하는 값**이라
키만큼 치명적이지 않다고 보고 유지했다. 실사용에서 또 어긋나면 같은 규칙(§⑤)을 적용한다.

---

## 2026-08-14 · v117 — 각인 말투: **신은 사전을 읽지 않는다**

### 요청
창업자가 자기 판결문 전문을 붙여 놓고 물었다.
"판결문 보면 무슨 잘못이 있는 거 같아? 키 입력했더니 그게 답변에 저렇게 영향이 가면 믿음이 가겠어?
키가 179이기 때문에 주변의 이목을 끈다? 너무 전문성 없어 보이고 바보 같아. **신도 아닌 거 같아.**"

### 지적된 것 — 그리고 그게 맞은 이유

v116 에서 "키는 맞히지 말고 **받아서 해석**하자"로 바꿨는데, 나온 문장이 이랬다.
> "네가 적은 179cm는 또래 평균보다 7cm 큰 쪽이야. 서 있기만 해도 먼저 눈에 띄어…"

**이건 해석이 아니라 뺄셈이다.** 179 − 172 = 7. 유저가 준 값을 되돌려주고, 거기서 나온 말은
키만 아는 사람이면 누구나 할 수 있는 말이다. **받은 값이 값을 하려면 명식과 교차해서
유저가 모르던 걸 내놔야 하는데, 키에는 교차할 축이 없다.** → **키를 아예 없앴다.**
v115(지어내기) → v116(되읊기) → v117(안 다루기). 두 번 틀리고 나서야 자리를 찾았다.

### 말 안 하신 것 셋 — 이게 더 컸다

**① 말투가 한 문단 안에서 두 번 바뀐다.** ← "신도 아닌 거 같아"의 정체
> "너는 감정이 먼저 보이는 사람**이야**. 기분이 얼굴에 그대로 나온**다**."

감싸는 문장은 신의 반말("-야")인데 표에서 꺼낸 문장이 전부 사전체("-다")였다.
한 사람이 말하다 갑자기 사전이 끼어드는 소리로 들린다. **표 열두 개를 통째로 "-야/-어"체로 다시 썼다**
(SURFACE·INNER·BLOCK·THICK·ORGAN·FACE·VOICE·SPOUSE·H7·SS_EVENT·trig·checks — 54개 문자열).

**② 유년 구간에 어른의 사건을 쓰고 있었다.**
여든 해 지도에 **"5~14세 — 월급이 오르거나 계약이 길게 붙거나 집·차가 생긴다"**가 그대로 나갔다.
같은 뿌리로 돈 자리가 스물다섯 살 유저에게 **"돈이 도는 구간은 5~14세로 이미 지나갔어"**라고 결론지었다.
짝의 시기에는 18세 필터를 걸어 뒀으면서 여든 해 지도와 돈에는 안 걸었다.
→ `SS_CHILD` 표 신설(유년 구간 전용 문구), 돈 구간 계산에서 유년 제외,
   인도 시기 이름도 나이를 타게 했다(서른다섯 칸에 "어머니와 집이 세상의 전부인 시기"가 붙어 있었다).

**③ 같은 문장이 다섯 자리에 반복.**
"지금 열 해(25~34세)는 다른 쪽에 쏠려 있어."가 공부·일·돈·사람·연애 다섯 곳에 똑같이 나왔다.
같은 문장이 다섯 번 나오면 읽는 사람은 이게 조립품이라는 걸 즉시 안다.
→ 공통 앞머리를 없애고 자리마다 다른 문장으로 썼다.

### 그 밖의 잔버그(전부 화면에 실제로 나갔다)
- 그림 안 글자가 `slice(0,9)`로 잘려 **"감정이 먼저 보이"**에서 끝남 → 어절 단위 두 줄 배치
- 확인 문항에 **`<b>`가 날것으로 찍힘** — 그 칸은 순수 텍스트로 그려진다(`<p>{w}</p>`)
- **"셋 중"인데 실제로는 두 개** 표시 · **"간과 눈, 그리고 힘줄가"** · **"…거래가 곧 실력인 일야"**
- **"요즘이 닫혀 있던 말이 나오는 같은가"** — `title.replace(" 열 해","")`가 문장을 부숨
- 기혼자의 연애 「지금」이 **"저절로 오지 않아"** — 이미 만난 사람에게 할 말이 아니다
- 기혼자의 자식 「자라면서」가 "흔들린 적이 있다면" — 아이가 이미 있는데 조건문

### 검증
각인 46/46(신규 4묶음) · 리포트 65/65 · 스모크 25/25 · 판결 34/34 · 스코프 54/54 ·
v29 8/8 · WebGL 7/7 · 여러 하늘 12/12 · 검진 clean

**말투 검사(⑫)가 이번 사고의 핵심 장치다.** 화면에 나가는 모든 문장을 모아
사전체 종결(-ㄴ다/-이다/-한다/…)로 끝나는 게 하나라도 있으면 운다.
표에 새 문구를 넣다가 무심코 "-다"로 끝내면 즉시 잡힌다 — 이게 없으면 반드시 되돌아온다.
검진에도 두 검사를 넣었다(키 부활 감지 · 말투 규칙과 유년 표 생존).

### 남은 것
- 「지금」 칸에서 「마음」과 「말」이 같은 열 해 이름을 두 번 부른다. 문장은 다르지만 겹쳐 읽힌다.
- `FACE`·`VOICE`·`BUILD`는 여전히 검증 가능한 주장이다. 거울로 확인하는 값이라 키만큼
  치명적이지 않다고 보고 뒀다 — 실사용에서 어긋나면 키와 같은 길을 간다.

---

## 2026-08-14 · v118 — 아홉 하늘: **사주는 아홉 중 하나다**

### 요청 (창업자, 다섯 갈래)
① "'네게는 같이 갈 사람이 얇아' < 뭔 말이야? 이런 비문은 쓰지 마. 은유 같지도 않고 비유 같지도 않고.
   내가 구체적이고 직관적으로 적어달라 했잖아" ② "생김새와 몸은 왜 두 번 써줘"
③ "태어난 장소는 판결에 영향 없나?" ④ "그냥 사주 내용이랑 꼭 같은데,
   저번에 글로벌로 찾은 생년월일로 운명을 점치는 방법들은 **적용이 된 거야 만 거야?**"

### ④ 먼저 — 감사 결과: **거의 안 됐다**

sky.js 에 판독기가 **열한 개** 있는데 각인 본문에 영향을 준 건 **셋**뿐이었다.

| 판독기 | v117 까지의 실상 |
|---|---|
| 서양 해·달자리 | 「속」 한 줄 (원소만) |
| 상승궁 | 미혼일 때 짝의 인상 |
| 인도 다샤 | 여든 해 지도의 곁말 |
| 마야 촐킨 · 나크샤트라 | **각주 한 줄 장식** |
| 자바 웨톤 · 아칸 · 태국 요일 · 구성기학 · 납음오행 · 라이프패스 · 달 위상 | **한 번도 호출 안 됨** |

계산해 두고 안 쓰면 없는 것과 같다. 사주가 아홉 중 하나가 아니라 **전부**였으니
읽으면 사주로 읽힐 수밖에 없었다. 창업자 체감이 정확했다.

### 새 절 — 「아홉 하늘이 뭐라고 하는가」

동아시아 여덟 글자 · 납음오행 · 구성기학 · 자바 웨톤 · 서양 해자리 · 마야 촐킨 ·
나크샤트라 · 아칸 요일이름 · 수비학 — **아홉이 각각 자기 값을 내고, 다섯 낱말 중 하나로 증언한다.**
겹치면 확정, 갈리면 그게 급소. **사주는 아홉 중 하나로만 세어진다.**

지어내지 않기 위한 장치 셋:
1. **다섯 낱말은 오행 그대로다** — 목·화·토·금·수를 행동으로 옮긴 것(먼저 움직이는 / 드러내고 말하는 /
   버티고 기다리는 / 자르고 정리하는 / 맞추고 스며드는). 새 어휘를 만들지 않았다.
2. **변환이 없는 것과 해석이 섞인 것을 구분해 각주에 적는다.** 납음·구성기학은 이름에 오행이
   들어 있어 변환이 아예 없고, 자바 파사란은 전통이 이미 다섯 방위에 배당해 두었다.
   마야·나크샤트라·아칸·수비학은 각 전통의 성격 서술을 옮긴 것이라 **"해석이 섞인다"**고 명시한다.
3. **합의 조건을 못 박았다** — 넷 이상 + 2위와 두 표 이상 차이일 때만 "겹친다"고 쓴다.
   3:3:3 을 1위라고 부르면 그건 합의가 아니라 반올림이다(짝의 시기에서 배운 것과 같다).

그리고 **여덟 글자만 다수와 갈리면 그 사실을 따로 적는다** —
"사주만 보고 판단한 너와 진짜 너가 다를 수 있다는 뜻이야." 이게 이 절의 가장 값진 한 줄이다.

### ① 비문
`BLOCK.t` 가 "같이 갈 사람"·"말이 나갈 문"·"쥐는 손" 같은 명사구였고, 본문에서
"네게는 **같이 갈 사람이 얇아**"로 조립됐다. 사람이 얇을 수 없고 문이 얇을 수 없다.
뜻은 안 전해지고 폼만 잡은 문장이다. → `t`(제목용 한 낱말)와 `plain`(뜻이 바로 서는 완결 문장)으로 나눴다.
"사람 — 네게 가장 약한 자리 / **무리에 잘 못 섞여. 혼자 하는 게 빨라.**"

### ② 중복
「생김새와 몸」의 '평생 약한 곳'과 「몸 — 어디가 약하게 왔나」 4단이 같은 말을 두 번 했다.
같은 내용을 두 번 쓰면 두 번째가 첫 번째를 무효로 만든다.
→ 「생김새」는 겉모습만(몸·얼굴·목소리), 건강은 4단이 전담.

### ③ 태어난 장소 — **절반만 쓰고 있었다**
`readImprint` 에 **경도만 넘기고 위도는 서울 고정**이었다. 상승궁은 위도로 갈리므로
제주(33.5°)에서 태어난 사람이 서울(37.6°) 값을 받고 있었다. 물어 놓고 절반만 쓴 셈이다.
→ `CITY_LAT` 25개 도시 추가, `lat: cityLat(birth.city)` 연결. 실측 상승궁 이동 2.5도.
⚠ 검사는 **자리 이름이 아니라 각도**로 해야 한다 — 위도 차 4도는 상승궁을 2~3도 움직이는데
   자리 폭이 30도라 대개 같은 자리에 머문다. 이름으로 검사하면 통과해 버린다(처음에 그랬다).

### 검증
각인 62/62(+16) · 리포트 71/71(+6) · 스모크 25/25 · 판결 34/34 · 스코프 54/54 ·
v29 8/8 · WebGL 7/7 · 여러 하늘 12/12 · 검진 clean(+1)

새 검사가 지키는 것: 아홉이 전부 증언하는가 · 사주가 가중치 없이 하나로만 세어지는가 ·
해석이 섞인 문명이 그렇다고 밝히는가 · 60명 중 40종 이상 다른 조합이 나오는가 ·
합의율이 0%도 100%도 아닌가 · 비문이 없는가 · 위도가 실제로 각도를 움직이는가.
검진에도 「판독기 8종이 본문에 물려 있음」을 넣어, 다시 장식으로 돌아가면 울게 했다.

### 남은 것
- 달 위상·태국 요일·하압·아쉬타쿠타는 아직 안 물렸다. 달 위상은 다섯 낱말 축에 안 맞고
  (차오름/기움은 다른 축이다), 아쉬타쿠타는 상대가 있어야 한다 — 궁합 기능이 생길 때 붙인다.
- 아홉 중 넷(마야·인도·아칸·수비학)은 해석이 섞인다. 각주에 밝혔지만,
  실사용에서 "이 문명 해석이 틀렸다"는 지적이 나오면 그 표부터 고친다.

---

## 2026-08-14 · v119 — 하늘의 분업: **투표를 시키면 사주가 된다**

### 요청
"여전히 사주 베이스처럼 느껴지고 여러 운세 방식은 유효하지 않은 거 같아.
아 뭔가 **새롭다는 느낌보다 그냥 사주인데 굳이 쟤네 왜 붙였지** 싶어.
**기존 사주와 다르게 해석되는 부분도 알려줘.** 리포트 문자 수가 제한되어 있어?"

### v118 이 실패한 이유 — 셋

**① 다섯 낱말을 오행으로 잡았다.** 아홉 문명을 목·화·토·금·수로 번역했으니
**다른 하늘을 사주의 렌즈로 읽은 것**이다. 당연히 사주로 읽힌다. "지어내지 않으려고"
공용어를 오행으로 골랐는데, 그 선택 자체가 문제였다.

**② 아홉 표를 다섯 칸에 뿌리면 무승부가 흔하다.** 창업자 판결문에 **3:3:3** 이 나왔다.
"이게 네 급소야"라고 써 놓고 아무것도 못 가리켰다. 판별력이 없는 설계였다.

**③ 결정적 — 세어 놓고 그 결과로 아무것도 안 했다.** 나머지 본문은 100% 사주였다.
투표 결과는 독립된 상자였고 한 문장도 바꾸지 않았다. **그래서 부록으로 읽혔다.**

### 고친 방향: 투표 → **분업**

각 하늘이 **사주가 못 하는 질문 하나씩**을 맡는다. 같은 질문에 투표시키지 않는다.

| 하늘 | 맡은 질문 | 사주에 있나 |
|---|---|---|
| 서양 | 인생의 무게가 어디에 실렸나(열두 자리) | **없음** |
| 인도 | 시간의 마디가 사주와 어디서 어긋나나 | 주기가 다름(120년 불균등 vs 10년 균등) |
| 마야 | 태어난 날이 맡은 역할 | 층이 다름 |
| 일본 구성기학 | 어느 쪽으로 움직여야 풀리나(방위) | **없음** |
| 자바 | 태어난 날이 얼마나 무거운가(신붕 수) | **없음** |
| 달의 위상 | 시작에 강한가 마무리에 강한가 | **없음** |
| 서아프리카 아칸 | 요일이 준 이름과 역할 | 없음 |
| 수비학 | 평생 배우는 게 무엇인가 | 없음 |
| 동아시아 소리(납음) | 여덟 글자와 소리가 같은 말을 하나 | 사주 안의 다른 층 |

**「사주와 다르게 읽히는 곳」 절을 새로 만들었다**(창업자 직접 요청). 다섯 갈래:
겉과 속 · 글자와 소리 · 시간의 마디 · 겹치는 마디 · 사주에 아예 없는 것.
⚠ 어긋남이 없으면 **없다고 쓴다** — 억지로 만들면 그게 더 나쁘다.
겉과 속이 같은 사람에게는 그 항목을 아예 안 낸다(검사로 못 박음).

**가장 값진 산출물은 시간축이다.** 사주 대운은 열 해 균등, 인도 다샤는 120년을 아홉으로 불균등.
그래서 **두 전환표를 맞대면 사주만으로는 안 나오는 값**이 나온다 —
겹치는 나이(= 진짜 전환기)와 다샤만 바뀌는 나이(= 겉은 조용한데 속이 뒤집히는 해).
이건 계산 그대로라 해석이 안 섞인다.

### 근거의 급을 아홉 전부에 붙였다
**계산 그대로**(서양 하우스·인도 전환표) / **변환이 없다**(구성기학 팔괘 정위·자바 신붕·아칸 이름·납음) /
**해석이 섞인다**(마야 톤 뜻·달 위상 성향·수비학 과제). 급을 안 밝히면 지어낸 것과
옮긴 것이 같은 목소리로 나간다. 검사가 아홉 전부에 정확히 하나씩 붙는지 본다.

### 결과가 본문을 바꾸게 했다
여든 해 지도의 각 구간에 `doubleTurn`(두 셈이 같이 바뀌는 해 ◆)과
`dashaOnly`(사주는 조용한데 인도 셈만 바뀌는 구간) 표식을 얹었다.
이제 아홉 하늘이 사주 절을 **실제로 흔든다.** 세어만 놓지 않는다.

### 문자 수
**상한은 없다.** 회귀 검사에 4,000자 **하한**만 있다(값어치 두께 보장용).
v119 는 그보다 훨씬 길다 — 자를 이유가 없어서 안 잘랐다.

### 검증
각인 65/65 · 리포트 72/72 · 스모크 25/25 · 판결 34/34 · 스코프 54/54 ·
v29 8/8 · WebGL 7/7 · 여러 하늘 12/12 · 검진 clean

새 검사가 지키는 것: **아홉이 서로 다른 질문을 맡는가**(투표로 되돌아가면 운다) ·
사주에 없는 축을 실제로 채우는가 · 아홉 전부가 근거의 급을 밝히는가 ·
어긋남이 없을 때 안 쓰는가 · 60명 중 50종 이상 다른 값이 나오는가 ·
**결과가 여든 해 지도를 바꾸는가**.

### 남은 것
- 태국 요일·하압·아쉬타쿠타는 아직 안 물렸다. 아쉬타쿠타는 상대가 있어야 하므로 궁합 기능 때.
- 「지금·앞으로」 4단은 여전히 사주 대운이 단독으로 굴린다. 인도 다샤를 여기까지 물리면
  자리마다 "두 셈이 뭐라고 하나"가 붙는데, 문장이 두 배로 늘어 읽기가 무거워질 위험이 있다.
  다음 실사용 피드백을 보고 판단한다.

---

## 2026-08-14 · v120 — 빙빙 안 돌린다: **모호한 건 신비가 아니라 회피다**

### 요청
"심리상담처럼, 철학관처럼 명확하게 좀 말해줘 리포트에서.
**도대체 어떤 프롬프트가 들어갔길래 직관적인 말도 빙빙 둘러서 무슨 그릇이 어떻니 저떻니 그래?**
내가 말했지만 **신의 컨셉은 말을 모호하게 하란 게 아니고 말투나 비주얼을 말한 거야.**"
(그리고 "전략 세션에서 쓴 브랜치 읽혀?" — `claude/goal-validation-feasibility-u2b9ob`, 읽힌다)

### 먼저 사실관계 — **프롬프트가 아니다**
`imprint.js` 의 API 호출 **0회**. 각인은 LLM 을 쓰지 않는다.
서신(4,900원)은 프롬프트지만 각인은 **전부 이 파일의 하드코딩 표**다.
"그릇이 어떻니"는 프롬프트가 만든 게 아니라 **내가 표에 그렇게 써 넣은 것**이다.
→ 검사 ㉑ 로 못 박았다: 각인 엔진에 `fetch(`·`/api/` 가 없어야 한다.
   문장이 이상하면 프롬프트가 아니라 **표를 고쳐야 한다**는 사실을 코드가 기억하게 했다.

### 진단 — 세 가지 회피 습관

**① "자리가 N개"** — "재물 자리가 3개로 두꺼운데 너를 받치는 힘은 1개야."
유저는 3개가 많은지 적은지 모르고, 안다 해도 뭘 해야 할지 모른다. **내부 셈을 화면에 흘린 것**이다.

**② 은유를 서술어로 썼다** — 그릇 · 팔 힘 · 결 · 문 · 기운.
"돈은 보이는데 쥘 팔 힘이 모자라는 그릇이야" 는 아무 말도 안 한 문장이다.

**③ 관형형을 잘라 쓴 자리** — `title.replace(" 열 해","")` 가
"혼자 파고드**는**에 힘이 쏠려 있어서" 를 만들었다. → `bandName()` 으로 "시기"를 붙인다.

### 고친 규칙 (파일에 못 박음)
> **① 실제로 벌어지는 상황 → ② 그때 네가 하는 행동 → ③ 그래서 생기는 결과** 순으로 쓴다.
> "자리가 N개"를 쓰지 않는다. 은유를 서술어로 쓰지 않는다.
> **상담실도 철학관도 단정적으로 말한다. 모호한 건 신비가 아니라 회피다.**

전후:

| 전 | 후 |
|---|---|
| 재물 자리가 3개로 두꺼운데 너를 받치는 힘은 1개야. 돈은 보이는데 쥘 팔 힘이 모자라는 그릇이야 | **너는 돈 벌 기회를 남보다 많이 만나. 그런데 그걸 감당할 체력과 사람이 부족해.** 큰 건이 눈앞에 와도 혼자 다 하려다 몸이 상하거나, 같이 하던 사람과 틀어지면서 놓쳐 |
| 같이 갈 자리가 비었어 | **여럿이 하는 걸 못 견디는 사람이야.** 회의가 길어지면 속으로 '내가 하고 말지' 하고, 실제로 그렇게 해. 그래서 일은 되는데 사람이 안 남아 |
| 인연을 맡은 자리가 3개야 | **사람이 끊긴 적은 없을 거야.** 문제는 늘 고르는 쪽이었어. 정하는 데 오래 걸리고, 정하고 나서도 '저쪽이 나았나' 싶은 마음이 남아 |
| 굴려서 남기는 쪽으로 새겨졌어 | **사고팔고 연결해서 차액을 남기는 일이 맞아.** 구체적으로는 영업·유통·중개·자영업 쪽이야 |
| 앉아서 외우는 쪽은 아니야 | **앉아서 외우는 건 안 맞아.** 강의 결제해 놓고 안 듣는 일이 반복됐다면 게을러서가 아니라 **방식이 안 맞아서**야 |
| 도와달라고 말하는 법 | **"도와줘"라고 먼저 말하는 것.** 딱 이 한 마디야 |

열 해 이름도 전부 구체화했다 — "손에 익은 걸로 사는 열 해" → "잘하는 걸로 먹고사는 열 해",
"닫혀 있던 말이 나오는 열 해" → "네 이름 걸고 내놓는 열 해".

### 검증
각인 69/69(+4) · 리포트 72/72 · 스모크 25/25 · 판결 34/34 · 스코프 54/54 ·
v29 8/8 · WebGL 7/7 · 여러 하늘 12/12 · 검진 clean

**검사 ⑳ 이 이번 사고의 재발 장치다.** 화면에 나가는 모든 문장을 모아
금칙 은유 6종(그릇 · 팔 힘 · 자리가 N개 · 결 · 문 · 기운이 흐른다)을 정규식으로 잡는다.
표에 새 문구를 넣다가 은유로 도망가면 즉시 운다. 아홉 자리의 「새겨질 때」가
전부 25자 이상 단정문인지도 함께 본다("~일 수도"·"아마"·"것 같아" 금지).

### 남은 것
- 「자라면서」 칸 일부가 여전히 "~했을 거야" 추정형이다. 지난 일은 확인이 안 되니
  단정하면 틀렸을 때 문서가 죽는다 — 의도한 것이지만, 네 칸 중 한 칸이 계속 추정형이면
  전체 톤이 약해 보일 수 있다. 실사용 반응 보고 판단한다.

---

## 2026-08-14 · v121 — 판결도 안 돌린다: **모델은 규칙보다 예시를 따른다**

### 요청
"이거 판결문도 적용되어 있나? **판결도 되게 애매모호할 때 많던데.**"
(그리고 "「자라면서」 추정형은 일단 남겨" — 유지)

### 감사 결과 — 절반만 적용돼 있었다

**앞면(verdict, 45자)에는 규칙이 이미 강하게 있었다**: 일반론 금지 · 은유 없이 직답 ·
행동 명확성("가을에 다시 던져" 금지) · 지표 정박 · 출력 직전 자가점검 · 회피 금지.

**그런데 뒷면(subline·reasons)은 은유를 허용하고 있었다.** "뜻→은유 순서"만 요구했다.
**앞면만 조이면 모호함은 뒷면으로 숨는다.** 유저가 실제로 오래 읽는 건 뒷면이다.

### 진짜 원인 — **프롬프트가 자기 예시로 자기 규칙을 어기고 있었다**

프롬프트에 이 한 줄이 **모범 예시(O)** 로 박혀 있었다:
> (O)"편재 둘에 암록까지 — 크게 들어오는 재물의 **그릇**이야"

한 줄에 위반이 둘이다.
① **편재·암록**은 v112 에서 만든 [용어 금지] 목록의 말이다.
② **"그릇"** 은 읽고 나서 아무것도 모르는 은유다 — 각인에서 방금 걷어낸 바로 그 단어다.

**모델은 금지 조항보다 (O) 예시를 강하게 따른다.** 금지를 열 줄 써 놔도 예시 한 줄이 이긴다.
"그릇이 어떻니"의 진원지가 여기였다. 예시를 고쳤다:
> (O)"돈이 크게 들어오고 크게 나가는 쪽이야. 벌 땐 몰아서 벌어."
> (X)"편재 둘에 암록까지 — 크게 들어오는 재물의 그릇이야"(용어 노출 + 뜻이 안 서는 은유)

「근거 구체성」의 예시도 같은 병이었다 — (O)"**다섯째 자리**가 지금은 물러서랬어" 를
(O)"지금 손볼 건 **사람 문제**야 — 힘은 있는데 순서가 틀렸어" 로 바꿨다.

### 뒷면에 v120 규칙을 이식
㉠ 뜻이 안 서는 은유를 서술어로 쓰기 ㉡ 개수만 던지기("재성이 셋이야") ㉢ 추상명사로 도망가기
㉣ 판정 유예 어미("~일 수도"·"두고 봐야") — 넷 다 금지.
쓰는 순서: **실제 상황 → 그때 하는 행동 → 그래서 생기는 결과.** 은유는 그 뒤 한 조각까지만.

### 검사 둘을 새로 걸었다

**① 정적 — 프롬프트가 자기 예시로 자기 금칙을 어기는가**(검진 5-g)
SYS 프롬프트에서 `(O)"..."` 형태 예시를 **전부 뽑아**(현재 23개) 금칙 은유가 들어 있는지 본다.
규칙이 아니라 **예시를 검사한다** — 이 사고의 정확한 형태가 그거였으니까.
API 키 없이 배포 전에 돌아간다.

**② 실측 — 평가 하네스의 모호함 축**(eval/run-eval.mjs)
지금까지 자동검사는 **앞면만** 봤다. `--full` 로 받은 subline·reasons 도 같은 잣대로 잰다:
은유서술어 · 판정유예 · 개수만. 호출 순서를 바꿔 뒷면을 받은 뒤 한 번에 채점하게 했다.

⚠ **e2e/verdict.mjs(34건)는 모형 응답으로 배선만 본다** — 모델이 실제로 뭐라고 쓰는지는
평가 하네스만 안다. 그래서 프롬프트를 손대면 `node eval/run-eval.mjs --full --via=...` 를
한 번 돌려야 실제 문장이 확인된다(이번엔 안 돌렸다 — 키가 Vercel 에만 있고 프록시가 막혀 있다).

### 검증
각인 69/69 · 리포트 72/72 · 스모크 25/25 · 판결 34/34 · 스코프 54/54 ·
v29 8/8 · WebGL 7/7 · 여러 하늘 12/12 · 검진 clean(+1)

### 남은 것 (창업자 확인 필요)
프롬프트 수정은 **실모델로 확인하지 않았다.** 평가 하네스를 한 번 돌려야 한다 —
`ANTHROPIC_API_KEY=... node eval/run-eval.mjs --full` 또는 `--via=https://binari-sepia.vercel.app`.
27문항×5인 기준 20분 안팎, 약 $1 미만. 돌리면 모호함 플래그가 CSV 에 그대로 뜬다.

---

## 2026-08-14 · v122 — 그림과 뎁스: **어긋남을 한 절에 가둬 두면 부록이 된다**

### 요청 (다섯)
"리포트 **너무 글의 나열**이야. **그림**도 있으면 좋겠고, **모션**도 있으면 좋겠어.
처음에 사주에서 못 읽는 부분을 말해주는 건 신기한데, **그게 밑에까지는 영향이 안 가는** 거 같네.
**생김새는 어디 근거가 있는 거야?**
사람들이 궁금해할 만한 내용에 대한 뎁스를 말해줬는데 적용이 안 되어 있어 —
**돈을 벌면 언제 얼마나 뭘 해서, 그걸 벌기까지의 여정 그래프**도 있으면 좋고,
**연인을 만나면 언제 어느 방향 뭐하는 사람 생김새 성격 취향 연봉 집안 궁합 자녀 결혼**.
그리고 이 리포트는 나중에 **다른 사람의 생년월일을 넣으면 궁합** 혹은 그 이상."

### ③ 생김새의 근거 — 답
**오행 체상론(五行體相)** 이다. 사람을 목·화·토·금·수 다섯 형(形)으로 나눠 보는 관상 계열 전통
(『신상전편』 등)으로, 실재하는 유파다. **그런데 우리 구현은 축소판이다** —
정통 체상론은 일간·월령·격국을 함께 보는데 우리는 **일간과 최다 오행 둘만** 쓴다.
그리고 **맞았는지 검증한 적이 없다.**

각주가 "일간 오행 목의 상(相)" 한 줄뿐이라 계산값과 같은 급으로 읽혔다. 세 가지를 다 적게 고쳤다:
유파 이름 · 축소판이라는 사실 · 미검증이라는 사실. 그리고 **최다 오행 보정을 실제로 얹었다**
(v121 까지는 각주에만 언급하고 문장에는 안 썼다).
키(cm)를 뺀 것과 같은 이유로 여기도 숫자는 안 쓴다.

### ② 어긋남이 밑까지 안 간 이유 — 맞는 지적
어긋남을 **「사주와 다르게 읽히는 곳」 한 절에 모아 두기만** 했다. 아홉 자리는 여전히 사주 단독이었다.
→ 자리마다 **그 영역을 서양이 어떻게 보는지** 한 줄을 붙였다. 대응이 깔끔하게 떨어진다:
몸1 · 말3 · 마음4 · 자식5 · 연애7 · 공부9 · 일10 · 사람11 · 돈2 하우스.
- 태양이 그 하우스 → "**서양 셈도 여기를 인생의 중심으로 봐. 두 셈이 겹쳐**"
- 달 → "마음이 쉬는 곳", 파트오브포춘 → "복이 도는 자리"
- 아무것도 없음 → "**서양 셈으로는 조용해. 사주가 더 무겁게 보는 영역**"

이제 어긋남이 문서 전체에 퍼진다. 검사가 **겹치는 자리가 사람마다 옮겨 다니는지**까지 본다.

### ④ 돈의 여정 — 그래프 + 구간별 데이터
여든 해를 재물 관점 −3~+4 로 매겨 **곡선**으로 그렸다(면적 채움 + 절정·바닥·지금 표식 + 그려지는 모션).
구간마다 **"뭘 해서 버나"** 를 붙였다 — "월급·고정 계약처럼 끊기지 않는 돈",
"사업·투자·중개처럼 크게 오가는 돈", "벌어도 새는 돈 — 보증·동업·빌려주기".

⚠ **금액은 안 쓴다.** 명리 어디에도 원 단위 표준이 없다 — 키(cm)를 뺀 것과 같은 이유다.
"얼마나"는 남과의 비교가 아니라 **네 여든 해 안에서 언제가 제일 두꺼운가**로 답한다.
그건 계산에서 실제로 나온다. 검사가 `\d+(원|만원|억)` 를 잡는다.

### ④ 짝의 뎁스 — 열 항목 → **열넷**
새로 넷: **어느 쪽에서 오나**(십이지 방위 배당 — 변환 없음) · **뭐하는 사람**(일지 십성의 직업군) ·
**취향**(짝의 자리 별자리 원소) · **둘이 섞이는 결**(일간 vs 일지 오행 생극 — 변환 없음).
연봉은 "고정급인지 성과급인지, 너보다 많이 버는 쪽인지"까지만 — **숫자는 안 찍는다.**
「둘이 섞이는 결」은 기혼자에게도 준다(관계 해석이라 안전하다).

### ① 그림과 모션
그래프가 셋 → **다섯**. 새로: **돈의 여정 곡선**, **방위 나침반**(막힐 때 움직일 쪽 · 짝이 오는 쪽 — 사주에 거의 없는 축이라 그림 값어치가 크다).
모션: 절·그래프가 올라오며 나타나고(`impRise`), 돈 곡선은 **그려진다**(`stroke-dasharray` draw-on).
`prefers-reduced-motion` 을 존중한다 — 검사가 그것까지 본다.

### ⑤ 궁합(상대 생년월일) — 이번엔 안 했다
창업자가 "**나중에**"라고 명시했다. 다만 재료는 이미 다 있다 — `sky.js` 의 `ashtakuta`(36점 8항목)가
계산까지 되어 있고 지금 한 번도 안 쓰인다. 붙일 때 필요한 건 계산이 아니라 **화면과 입력**이다.
「둘이 섞이는 결」을 이번에 넣은 건 그 자리를 미리 열어 둔 것이기도 하다.

### 검증
각인 **97/97**(+28) · 리포트 **79/79**(+7) · 스모크 25/25 · 판결 34/34 · 스코프 54/54 ·
v29 8/8 · WebGL 7/7 · 여러 하늘 12/12 · 검진 clean

새 검사: 아홉 자리 전부에 서양 시선이 붙는가 · 자리마다 같은 말만 하지 않는가 ·
겹치는 자리가 사람마다 옮겨 다니는가 · 돈 곡선이 사람마다 다른가(40명 중 6종+) ·
금액을 지어내지 않는가 · 짝의 아홉 항목이 실재하는가 · 생김새 각주가 유파·축소판·미검증을 밝히는가 ·
그림이 다섯 이상인가 · 모션이 접근성 설정을 존중하는가.

---

## 2026-08-14 · v123 — 너의 이야기: **신화는 모호하지 않다**

### 요청
"혹시 **동화나 영화처럼** 읽히게 할 수 있을까? **신화**나."

### 먼저 — 지난 여섯 판과 충돌하는가? 아니다

지난 여섯 판이 **모호함을 걷어내는 작업**이었다("그릇이 어떻니"·"빙빙 둘러서"·
"신의 컨셉은 말을 모호하게 하란 게 아니고 말투나 비주얼"). 그런데 이번엔 신화다.
되돌리는 것처럼 보이지만 아니다 —

> **신화는 모호하지 않다.** "오디세우스는 십 년을 떠돌았다"는 정확한 문장이다.
> 신화가 주는 건 모호함이 아니라 **배열**이다: 결핍에서 열고, 시련을 지나, 관문에서 갈리고, 돌아온다.

그래서 **바꾼 건 문장이 아니라 순서**다. 검사도 둘을 같이 본다 —
서사 구조가 서는가, **그리고 문장이 여전히 구체적인가**(⑳의 금칙 은유를 서사에도 그대로 적용).

### 바꿀 데이터가 없었다 — 이미 이야기 모양이었다

| 신화의 자리 | 우리가 이미 갖고 있던 값 |
|---|---|
| 결핍 | 가장 빈 자리(BLOCK) |
| 장(章) | 대운 여덟 구간 |
| 관문 | **두 셈이 겹치는 해**(대운 전환 ≈ 다샤 전환) |
| 숨은 마디 | 다샤만 바뀌는 구간 — 겉은 조용한데 속이 뒤집히는 때 |
| 시련 | 배우자 자리를 치는 구간 |
| 보물 | 재물 곡선의 절정 |
| 조력자 | 인성 구간 |
| 이름 | **납음 60갑자** — "검봉금 = 칼끝에 서린 쇠". 이름 자체가 이미 이미지다 |

새 계산은 **0건**이다. 각주에 "다시 배열했을 뿐"이라고 적고, 검사가 장의 사건이
여든 해 지도와 **같은 값인지** 대조한다.

### 화자도 이미 있었다
"-야"체 반말을 유지하면 그대로 **수호신이 들려주는 이야기**가 된다.
문어체("…한 아이가 왔다")로 갔으면 톤이 깨졌을 텐데, 그럴 필요가 없었다.

> **1995년 가을, 네가 왔어.** 그해에 붙은 이름은 **산두화** — **산꼭대기의 불**이라는 뜻이야.
> 다만 셈이 어느 쪽으로 갈리든 **한 가지가 처음부터 비어 있었어 — 돈이 손에 안 남아.**
> **이건 그 빈 하나를 두고 벌어지는 여든 해의 이야기야.**
>
> 제3장 · 25~34세 — **혼자 굴을 판 장**  ◂ 여기
>   [조력자] 누군가 너를 끌어 주는 장이야. 혼자 힘으로 넘은 게 아닌 구간이야
>
> …너는 지금 **제3장**에 서 있어. 남은 장이 **5개**야.
> 처음부터 비어 있던 그 하나는 끝까지 안 채워져. **영웅이 되는 건 결핍을 없애서가 아니라 그걸 안고도 걸어서야.**

### 영화처럼 — 여정 지도
굽이치는 길에 여덟 장을 노드로 찍었다. **지나온 길은 이어진 선, 앞으로는 점선**,
지금 서 있는 자리가 **맥동**한다. 표식은 글자가 아니라 기호로 — ◆관문 ✦보물 ▲시련 ◇조력자 ◉숨은 마디.
선이 순차로 나타난다(단계별 지연). `prefers-reduced-motion` 존중.

### 배치 — 이야기가 앞, 근거가 뒤
겉·속 다음에 바로 이야기를 두고, 아홉 하늘·아홉 자리·짝은 그대로 뒤에 남겼다.
**이야기가 훅이고 나머지가 근거**다. 둘 중 하나를 고르게 하지 않았다 —
읽고 싶은 만큼만 내려가면 되니까.

### 잡은 버그
서사가 **관문 표식을 하나도 못 받고 있었다.** 두 셈의 시간 마디를 아홉 하늘 절 안에서
계산하는데 서사가 그보다 **위에** 있었다. 계산을 `bands` 직후로 끌어올렸다.
→ 교훈: **쓰는 곳보다 먼저 계산한다.** 검사가 "관문 표식은 두 셈이 겹치는 장에만 붙는다"로 잡았다.

### 검증
각인 **112/112**(+15) · 리포트 **84/84**(+5) · 스모크 25/25 · 판결 34/34 ·
스코프 54/54 · v29 8/8 · WebGL 7/7 · 여러 하늘 12/12 · 검진 clean

새 검사 중 둘이 중요하다:
- **「이야기도 모호함으로 도망가지 않는다」** — 서사라고 봐주지 않는다. ⑳의 금칙을 그대로 건다
- **「장의 사건이 여든 해 지도와 같은 값이다」** — 서사가 새 값을 지어내면 즉시 운다

### 남은 것
장 제목(CH_TITLE)은 십성 열 개에 하나씩이라 **같은 십성이면 같은 제목**이 나온다.
30명 중 8종 이상이면 통과하게 해 뒀지만, 사람이 늘면 겹쳐 보일 수 있다.
나이대·표식을 섞어 변주하는 건 다음 판에.

---

## 2026-08-14 · v124 — 직장생활: **직업 선택과 조직 적응은 다른 축이다**

### 요청
"직장생활도 봐주면 어때. **일 스타일, 잘하는 것, 잘 맞는 사람, 어느 위치,
얼마나 오래 다니고, 언제 이직하고** 등등."

### 왜 「일」 한 칸으로는 부족했나
기존 「일 — 어떤 일이 맞나」는 **직업 선택**의 축이다(무슨 일을 하느냐).
직장은 다른 축이다 — **조직 안에서 어떻게 굴러가느냐.**
**맞는 직업을 골라도 조직에서 못 버티는 사람이 있다.** 그래서 절을 따로 뒀고,
검사가 두 절의 문장이 겹치는지 실제로 대조한다(겹치면 둘 중 하나가 죽는다).

### 아홉 항목
| 항목 | 무엇으로 보나 |
|---|---|
| 일 스타일 | 일간 오행의 업무 성정 |
| 잘하는 것 | 가장 두꺼운 십성 무리 |
| **안 되는 것** | 가장 빈 자리 — **못하는 걸 아는 게 더 쓸모 있다.** 피할 수 있으니까 |
| 어느 위치까지 | 관성 + 비겁 → 실무 / 중간 / 리더 / 독립 |
| 잘 맞는 상사 | 나를 누르는 자리(정관·편관·없음) |
| 같이 일하면 좋은 사람 | 오행 상생 — 나를 낳는 쪽이 밀어 주고, 내가 낳는 쪽에 쏟는다(변환 없음) |
| 한 곳에 얼마나 | 관성·비겁·식상 배치 → 안정(5년+) / 보통(3~5년) / 이동(2~3년) |
| 서양이 보는 네 자리 | **중천(MC) 사인** — 사주가 "어떻게 일하나"라면 이건 "어디서 이름이 나나". 축이 달라 안 겹친다 |
| 번아웃 신호 | 가장 빈 자리가 **직장에서 드러나는 형태** → 가장 약한 장기 |

번아웃은 별도 문장을 새로 썼다. 처음엔 `BLOCK.s`를 갖다 썼는데
재성이 빈 사람에게 "지나가는 건 보이는데 남는 게 없어"가 번아웃 신호로 나왔다 — 그건 돈 얘기지 번아웃이 아니다.
직장 맥락 전용으로 다섯 개를 새로 썼다: "점심을 혼자 먹기 시작하면 그게 신호야" 식.
**압력은 빈 자리에서 시작해 얇은 자리(몸)로 간다** — 순서를 알면 앞에서 끊을 수 있다.

### 이직 시기 — 두 층으로
- **십 년 판**(대운): 다음 판이 바뀌는 나이 + 편관·겁재 구간("밀려서 움직이게 되는 때")
- **해마다**(세운): **올해부터 여섯 해**를 순역으로 계산해 막대로 그렸다.
  "옮기기 좋은 해 — 자리가 열려" / "지금 자리에서 값이 오르는 해" /
  "밀려서 움직이게 되는 해 — 미리 준비해 둬" / "움직이면 손해 보는 해 — 버티는 게 남아"

⚠ **좋은 해가 없으면 없다고 쓴다.** 여섯 해 안에 자리가 열리는 해가 없으면
"지금 자리에서 값을 올리는 게 남아"로 닫는다 — 억지로 하나 고르지 않는다(검사가 지킨다).

### 검증
각인 **130/130**(+18) · 리포트 **88/88**(+4) · 스모크 25/25 · 판결 34/34 ·
스코프 54/54 · v29 8/8 · WebGL 7/7 · 여러 하늘 12/12 · 검진 clean

새 검사 중 셋이 이 절의 존재 이유를 지킨다:
- **「일」 자리와 문장이 안 겹친다** — 문장 단위로 실제 대조한다
- **번아웃이 직장 맥락 문장이다** — 돈·말 증상을 갖다 쓰면 운다
- **사람마다 도달 위치·재직 주기가 갈린다**(40명 중 3종·2종 이상)

### 남은 것
- 「같이 일하면 좋은 사람」은 오행으로만 말한다. 상대 생년월일을 받으면
  **실제 그 사람과의 결**을 말할 수 있다 — 궁합 기능이 붙는 자리와 같다.
- 연봉·직급 같은 숫자는 여전히 안 쓴다. 명리에 그 표준이 없다(키·금액과 같은 규칙).

---

## 2026-08-14 · v125 — 궁합: **"87점, 잘 맞아요"는 어디서든 살 수 있다**

### 요청
"궁합 기능도 붙여주고 가격을 어떻게 할지 제안해봐."

### 설계 — 각인과 같은 원칙
궁합은 이 제품에서 **가장 뭉개기 쉬운 자리**다. 시중 대부분이 "87점, 잘 맞아요"로 끝낸다.
그 한 줄은 어디서든 살 수 있고, 그래서 우리가 팔 이유가 없어진다. 네 규칙을 파일에 못 박았다:

1. **분업** — 아홉 축에게 같은 질문을 던져 평균 내지 않는다. 각자 **다른 것**을 본다:
   일간=누가 주고 받나 / 일지=같이 사는 게 맞나(육합·삼합·충·형) / 인도=여덟 항목 /
   해자리=성향 / 달자리=감정 / 자바=두 날의 무게 / 마야=맡은 일 / 납음=소리 / 수비학=배우는 게 같나
2. **갈림을 감추지 않는다** — 이게 알맹이다.
   *"여덟 글자·여덟 항목·해의 자리는 맞는다고 하고, 자리의 글자·달의 자리는 아니라고 해.
   **잘 통하는데 자꾸 부딪히거나, 부딪히는데 못 헤어지는 이유가 여기 있어.**"*
3. **총점을 맨 앞에 두지 않는다** — 숫자를 먼저 보면 나머지를 안 읽는다. 맨 뒤에 두고
   "이 숫자를 먼저 보지 마"라고 적는다.
4. **헤어지라고 말하지 않는다.** 판정은 "무엇을 조심하라"까지다. 관계를 끊는 결정은 우리 몫이 아니다.

인도 여덟 항목은 **합치지 않고 그대로 편다.** 항목마다 무엇을 보는지 적었다 —
"몸과 잠자리의 결", "생각이 통하나", "같이 살 때의 흐름". 나디(8점)는 인도가 가장 무겁게 보는데,
같은 체질이면 0점이다 — **좋고 나쁨이 아니라 "서로의 약점을 못 메운다"**는 뜻이라고 적었다.

### 상대의 이름·연락처는 안 받는다
생년월일만 받고 이 기기에만 남긴다. **남의 개인정보를 우리가 들고 있을 이유가 없다.**
CLAUDE.md §운영 규칙(개인정보 비커밋)의 연장이다. 검진이 이걸 지킨다.
그리고 **연인 전용이 아니라고 안내한다** — 같이 일하는 사람·가족·동업자에게 그대로 쓴다.

### 진입점
로비에 각인 옆. 각인 대비 클릭률로 수요를 잰다(`match_clicked` / `match_opened` / `match_run`).
**각인이 있어야 열린다** — 내 명식이 먼저 있어야 하니까. 그래서 궁합은 구조적으로 **각인의 2차 구매**다.

### 검증
궁합 **29/29**(신규) · 각인 130/130 · 리포트 **99/99**(+11) · 스모크 25/25 · 판결 34/34 ·
스코프 54/54 · v29 8/8 · WebGL 7/7 · 여러 하늘 12/12 · 검진 clean(+1)

검사가 지키는 것: 아홉 축이 서로 다른 질문을 맡는가 · 근거의 급(변환 없음/해석 섞임)을 밝히는가 ·
자·오를 충으로, 자·축을 육합으로 읽는가(전통 배당대로) · 갈릴 때 갈린다고 말하는가 ·
총점 각주가 "앞에 두지 않는다"를 밝히는가 · **헤어지라고 시키지 않는가** ·
60쌍 중 25종 이상 다른 판정이 나오는가.

⚠ 헤어짐 검사는 **명령형만** 잡는다. "부딪히는데 못 헤어지는 이유"는 관계를 설명한 것이지
권고가 아니다 — 서술까지 금지하면 관계를 관계로 말할 수 없어진다. 처음엔 그걸로 헛울음이 났다.

### 만들면서 잡은 것
회귀 검사를 각인 뒤에 뒀더니 "궁합 진입점이 없다"고 계속 울었다. 원인은 로비 버튼이
`!ritual && !res` 조건이라 **판결이 한 번 돌고 나면 사라지는 것**이었다.
검사를 판결 전으로 옮겼고, 왜 그 자리여야 하는지 주석에 남겼다.

### 남은 것
- 「같이 일하면 좋은 사람」(직장생활 절)은 아직 오행으로만 말한다. 궁합 엔진이 생겼으니
  **동료의 생년월일을 넣으면 그 사람과의 결**을 말할 수 있다 — 다음 판에 연결한다.
- 두 사람의 **시기 겹침**(대운·세운)은 아직 안 봤다. "올해 둘 사이가 어떤가"는 재구매 훅이 될 자리다.

---

## 2026-08-14 · v126 — 개인정보·법정고지 A항 다섯

### 요청
전략 세션 작업지시(`claude/goal-validation-feasibility-u2b9ob`의
`app/작업지시_개인정보와법정고지_2026-08-14.md`) **A항부터 처리.**

### 이건 개별 결함이 아니라 추세였다
작업지시가 표로 못 박아 둔 것: 각인 문서는 v113→v124 **여섯 판 연속 넓어졌는데
`ImprintDoc` 안의 고지 줄 수는 0에서 한 번도 안 올라갔다.**
그래서 이번엔 고치는 것만으로 끝내지 않고 **검사 파일을 하나 새로 만들었다**(`e2e/privacy-check.mjs` 38항목).
사람의 기억으로는 여섯 판을 못 버틴다.

### A-1. 처리방침이 안내한 거부 방법이 존재하지 않았다
문서가 두 곳에서 "온보딩의 분석 동의를 해제하는 것만으로 거부할 수 있다"고 적는데,
v122 가 그 체크박스를 화면에서 뺐고 `PROFILE_KEYS` 가 빈 집합이라 **손으로 키를 바꿔도 아무것도 안 막혔다.**

작업지시가 준 두 갈래 중 **①(수단을 실제로 만든다)** 를 택했다 — ②(문서를 동작에 맞춤)는 거부권 자체를 없애는 방향이다.
- `OPTOUT_KEY` 신설 + `track()` **조기 반환**. 속성 제거가 아니라 **전송 자체를 멈춘다**.
- 끄는 순간 대기 큐도 버린다 — 끄기 전에 쌓인 걸 나중에 보내면 거부가 무의미해진다.
- 로비에 「사용 통계 수집을 끌래」. 언제든 되켤 수 있다.
- **처리방침 두 문장을 실제 자리로 고쳤다** — 문서가 없는 절차를 가리키면 안 되지만,
  있는 절차를 **엉뚱한 곳**으로 가리켜도 마찬가지다.

### A-2. 공유 링크에 실명이 실렸다 — 두 경로
`?v=` 는 base64 인코딩만 거친 **평문**이다. 그리고 payload 의 `n` 만 지우면 부족하다 —
SYS 프롬프트가 판결문 안에서 이름을 부르게 하고 있어 **본문 문자열에 실린다.**
- `n` 제거 · 구링크의 `n` 폐기 · 수신 화면 실명 폴백 제거
- `stripName()` 신설 — 호격(지원아/지원님/지원 씨)과 문장 첫머리 호명을 지운다.
- **경계를 실제로 시험했다**(작업지시가 "지원 아끼지"를 예로 들었다). 여덟 경우 누출 0.
- ⚠ **트레이드오프를 알고 골랐다**: 안전한 규칙으로 안 지워지면 남은 이름을 **"너"로 갈아 끼운다.**
  이름이 흔한 명사와 겹치면("하늘아 오늘은 하늘이 흐려") 문장이 어색해진다 — 어색한 문장과 새는 실명 중
  어색한 쪽을 고른 것이고, 코드 주석에 그렇게 적었다.

### A-3. 계측에 판결 원문이 나갔다 — 앞면·뒷면 둘 다
처리방침 §9("분석 도구에는 질문 원문·이름·생년월일 원값을 전송하지 않고")와 정면 충돌이었다.
**뒷면이 더 나빴다** — 콜2 에 이름 금지 지시가 붙는 조건이 "앞면에서 이미 이름을 부른 경우"뿐이라,
**앞면이 이름을 안 부른 경우에만 뒷면이 이름을 부르는** 구조였다.
- `verdict` → `vlen` · `subline`/`funline` → 길이 · `reasons` → 축별 길이
- **처리방침도 같이 고쳤다.** 코드가 안 보내게 됐는데 문서가 "판결 결과 전체를 수집한다"고 적고 있으면
  그것도 코드·문서 충돌이다. 과대 기재도 충돌이다.

⚠ 검사를 처음엔 파일 전체에 걸었다가 `setRecords`(로컬 판결록)를 잡아 헛울음이 났다.
**나가는 것과 남는 것은 다르다** — `track()` 호출 안만 보도록 좁혔다.

### A-4. 각인에 건강 예측이 있는데 고지가 0건이었다
각인은 LLM 을 안 타므로 판결의 S3 가드레일("병세·완치·수명을 점치는 문장 절대 금지")을
**구조적으로 통과하지 않는다.** 그런데 여섯 판 동안 고지가 0이었다.
- 「그때 한 번 크게 앓을 수 있어」 → 「그 구간엔 특히 관리가 필요해. 정기 검진을 그때로 잡아 둬」
- 건강 각주를 **생김새와 같은 수준으로 정직화** — 오행-장부 대응은 전통 배당 그대로라 변환이 없지만,
  그걸 개인의 건강 예측으로 옮기는 데는 해석이 섞이고 **검증한 적이 없다**. 그리고 **의료 판단이 아니다**.
- 각인·궁합에 **문서 하단 고정 블록** 하나씩. 작업지시가 짚은 대로 **절마다 붙이지 않았다** —
  절마다 붙이면 다음 판에서 또 빠진다.

⚠ 클래스를 `ainote imp` 로 붙였다가 컨테이너 `.imp` 셀렉터가 둘을 잡아 회귀가 빨개졌다.
v111 의 `.decl`→`.dcl` 과 **같은 사고**다. `ainote docnote` 로 바꿨고 검사로 못 박았다.

### A-5. 공유 수신 화면·부적 이미지에 AI 표시가 없었다
- 수신 화면은 `position:fixed` 로 뷰포트를 덮어 아래 온보딩 고지가 **물리적으로 안 보인다.**
  링크로 처음 들어온 사람에게는 **거기가 비나리의 첫 화면**이다.
- 부적 이미지는 앱 밖으로 나가 혼자 돌아다닌다 — 그림 안에 안 그리면 표시가 사라진다.
둘 다 넣었다.

### 검증
**개인정보·법정고지 38/38(신규)** · 궁합 29/29 · 각인 130/130 · 리포트 103/103 ·
스모크 25/25 · 판결 34/34 · 스코프 54/54 · v29 8/8 · WebGL 7/7 · 여러 하늘 12/12 ·
검진 clean(A항 통합 검사 1건 추가)

### 남은 것 (작업지시 B·C·D — 이번 범위 밖)
- **B-1** 각인 선택 입력이 리셋에 안 지워져 **다음 사람에게 넘어간다**(키가 `binari_` 라 스윕에 안 걸림)
- **B-2** `clearMemory` 가 키 하나만 지우고 `posthog.reset()` 이 없다
- **B-3** 처리방침에 공유 링크 고지 — **A-2 를 고쳤으니 이제 쓸 수 있다**(§5-1 신설).
  이번에 §9 안전성 조치에 한 줄은 넣었지만 별도 절은 아직이다.
- **C-1~C-4** 결제 붙이기 전 필수(서신 fake-door 표시 · 번호 재발행 약정 · 무제한 유료 호출 · payment=())
- **D-1** 처리방침의 MBTI 4곳 — 이번에 수집 항목표 1곳만 지웠다. 나머지 3곳 남음
- **D-2** CLAUDE.md 의 `App.jsx:1977` 인용 오류

---

## 2026-08-14 · v127 — 저장 키 규칙: **암묵 규칙은 여섯 판을 못 버틴다**

### 요청
작업지시 **A-6**(신규 최우선) — 궁합(v125)이 붙으면서 **제3자의 생년월일**이 앱에 들어왔는데
저장 키가 밑줄이라 리셋에도 안 지워지고 **다음 사람에게 넘어간다.**

### 무엇이 문제였나 — 개별 결함이 아니라 규칙의 구멍
저장 키에 **점(`.`) 접두 = 관리 대상**이라는 **암묵 규칙**이 있었다. `clearMemory` 도,
내보내기/불러오기 스윕도 `binari.` 만 본다. 그런데 새 기능은 계속 **밑줄**로 키를 만들었다:
- `binari_imprint_extra` (v115 각인 선택 입력 — 혼인·자녀·만난 시기)
- `binari_match_last` (v125 궁합 **상대방 생년월일**)

둘 다 스윕 밖이라 **"다른 사람이야? — 처음부터 다시"를 눌러도 남았다.**
궁합 쪽이 특히 무겁다 — **그 사람은 이 앱을 쓴 적도, 동의한 적도 없다.**

작업지시가 정확히 짚었다: *"암묵 규칙이 있는데 새 기능은 계속 밑줄로 만든다 — 다음 기능에서 또 반복된다."*
그래서 개별 수정이 아니라 **규칙을 명시로 올리고 검사를 붙였다.**

### 고친 것
1. **두 키를 `binari.` 접두로 개명** + 구키 1회 마이그레이션(이미 쓰던 사람의 값을 안 잃으면서 규칙 밖 키를 없앤다)
2. **`clearMemory` 를 `binari.` 전량 스윕으로.** 단 **팀 플래그(`INTERNAL_KEY`)는 남긴다** —
   그게 날아가면 계측이 팀 유입으로 오염되고 D7 게이트가 무의미해진다(CLAUDE.md §계측 주의)
3. **`_ph.reset()`** 추가 — 앞사람의 `distinct_id` 를 물려주지 않는다(B-2 앞부분도 같이 해결)
4. **처리방침 §1 에 제3자 항목 신설** — 본인 정보만 적힌 표에 남의 정보가 조용히 추가돼 있었다.
   생년월일·시각·성별만 받고 **이름·연락처는 안 받으며**, 기기에만 저장되고 서버·분석 도구로 안 가며,
   리셋으로 삭제되고, 상대 본인이 열람·삭제를 요청할 수 있다는 것까지 적었다.

### 검사 — 재발을 잡는 게 이 항목의 핵심이다
`health-check` 5-j + `privacy-check` 11항목 + **smoke 브라우저 검사 6항목.**

⚠ 작업지시가 미리 경고한 함정을 그대로 피했다: 규칙을 "`binari_` 로 시작하면 실패"로 넣으면
`binari_bujeok`(다운로드 **파일명**)과 `__binari_t`(localStorage **가용성 프로브**)에서 매번 오탐한다.
**`localStorage.*Item("binari_` 저장 호출 패턴으로 좁혔다.**

그리고 **정적 검사만으로는 부족하다고 봤다.** 제3자 정보라 "코드가 그렇게 생겼다"로는 모자라서
smoke 에 실제 시나리오를 넣었다 — 상대 생년월일을 심어 두고 리셋 버튼을 눌러
**실제로 지워지는지 / 팀 플래그는 남는지**를 브라우저에서 확인한다.

### 잡은 것 하나 더
지난 판에 smoke 에 넣으려던 A-1 검사가 **아예 안 들어가 있었다.** smoke 는 `check()`/`browser` 규약을 쓰는데
내가 `ck()`/`b.close()` 앵커로 붙여서 치환이 조용히 실패했다 — 그런데 통과 개수가 그대로라 눈치를 못 챘다.
**"검사를 추가했다"고 로그에 쓰면서 실제로는 안 늘어난 것**이라 이번에 바로잡았다(25 → 31).
→ 교훈: 검사를 추가하면 **통과 개수가 늘었는지 확인한다.**

### 검증
개인정보·법정고지 **49/49**(+11) · 스모크 **31/31**(+6) · 궁합 29/29 · 각인 130/130 ·
리포트 103/103 · 판결 34/34 · 스코프 54/54 · v29 8/8 · WebGL 7/7 · 여러 하늘 12/12 ·
검진 clean(저장 키 규칙 검사 1건 추가)

### A항 전부 종료 — 남은 것
**B-1 은 A-6 과 같은 뿌리라 함께 해결됐다.** B-2 도 절반(clearMemory 스윕 + posthog reset)이 됐다.
남은 것: **B-2 잔여**(내보내기 스윕 대상 재점검) · **B-3**(처리방침 §5-1 공유 링크 고지 —
A-2 를 고쳤으니 이제 쓸 수 있다) · **C-1~C-4**(결제 전 필수) · **D-1**(처리방침 MBTI 잔여 3곳) ·
**D-2**(CLAUDE.md 의 `App.jsx:1977` 인용 오류).

---

## 2026-08-15 · v128 — 진입 모션과 결제 전 정직화

### 요청
> "그래프, 표가 나타는 모션이 주기가 너무 짧아서 발작일으키는것 처럼 느껴져. 그리고 그래프가 그려지는 모션이
> 있듯이 표도 채워지는 모션이 있어야하겠어 / 나타는 모션은 최초에 한 번만 보여주고, 스크롤해서 벗어났다가
> 다시 오면 다시 그려지게끔하면 좋겠어 / 다 진행해 안했던거"

### 1. 원인은 속도가 아니라 동시성이었다
v127 의 각인 CSS 는 이렇게 생겼었다.

```css
.imp .impdom,.imp .impsky,.imp .impclash,.imp .impband,.imp .impck{animation:impRise .5s … both}
.imp .impsvg{animation:impRise .55s … both}
.drawin .pulse{animation:impPulse 2.6s ease-in-out infinite}
```

선택자에 조건이 없다. **문서가 마운트되는 순간 해당 클래스 전부가 애니메이션을 동시에 시작한다.**
각인은 자리 9 + 하늘 9 + 확인 문항 12 + 여든 해 구간 + 그림 7 = **60여 블록**이다.
0.5초를 60개가 같이 하면 그건 "짧은 모션"이 아니라 **한 번의 경련**이다. 거기에 `.pulse` 가 무한 반복이었다.

그래서 고친 건 duration 이 아니라 **누가 언제 움직이는가**다.

### 2. `useReveal` — 화면에 들어온 것만 움직인다
```
REVEAL_SEL = .impsvg,.impdom,.impsky,.impclash,.impband,.impck,.imptrig,.impch,.improws,.impmrows,.impyrs,.impcore
```
- IntersectionObserver `threshold:[0, 0.12]` — **15%쯤 보이면 켜고, 완전히 사라지면 끈다.**
  경계에 히스테리시스를 둔 건 의도적이다. 켜는 선과 끄는 선이 같으면 스크롤 미세 떨림에 깜빡이는데,
  그게 정확히 창업자가 지적한 증상이다.
- 같은 콜백 배치에 여러 개가 들어오면 **문서 순서로 정렬해 80ms 씩 계단**(최대 6칸)을 `--d` 로 넣는다.
  하나만 들어오면 지연 0 — 스크롤 속도에 따라 자연스럽게 계단이 생겼다 없어졌다 한다.
- 나중에 생기는 블록(근거 펼치기 등)은 MutationObserver 가 주워서 같은 규칙을 준다.

**실측: 한 화면에 동시에 켜지는 블록 최대 11개**(전체 64개 중). v127 은 60여 개가 한꺼번이었다.

### 3. ⚠ 초기 숨김은 CSS 가 아니라 JS 가 붙인다
`.rv{opacity:0}` 를 CSS 에 두고 JS 가 `.rvin` 을 붙이는 게 흔한 구현인데, **그렇게 하면 스크립트가 죽는 순간
9,900원짜리 문서가 백지가 된다.** 그래서 `.rv` 자체를 훅이 붙인다 — 훅이 안 돌면 문서는 그냥 다 보인다.
설계 헌장의 「알 권리 — 리포트 국면」이 여기까지 미친다: **값을 치른 문서에서 "안 보임"은 최악의 실패다.**

### 4. 표도 채워진다
그래프에만 있던 draw-on 을 표에 옮겼다.
- 줄(`.impr`·`.impmrow`·`.impyr`)은 `clip-path:inset(0 100% 0 0)` → `inset(0)` 으로 **왼쪽부터 채워진다**(70ms 계단).
  컨테이너는 안 움직인다 — 칸이 흔들리면서 글자가 차면 읽기가 더 힘들어진다.
- 막대(여든 해·열두 달·여섯 해)는 `transform-box:fill-box` + `scaleY` 로 **축에서 자라 오른다.**
  위 막대는 아래에서, 아래 막대는 위에서 — 원점을 JSX 가 부호 보고 넣는다.
- 궁합 아쉬타쿠타 여덟 칸은 `scaleX` 로 좌→우.
- `.pulse` 의 `infinite` 삭제 → 3.2초 2회 후 멎음. **눈이 머무는 문서에 계속 뛰는 게 있으면 안 된다.**

`prefers-reduced-motion` 은 **모션만** 끈다. `opacity:1!important` 를 전 요소에 밀면 막대의 강약(SVG opacity 속성)까지
뭉개져서, 우리가 0으로 만든 것만 되돌린다.

### 5. "다 진행해 안했던거" — 작업지시 잔여 처리
| 항목 | 처리 |
|---|---|
| **B-2 잔여** | 저장 키 전수 확인 — 10개 전부 `binari.` 접두(+`binari.once.*`). 내보내기 스윕 대상 누락 **0**. 종료 |
| **B-3** | 처리방침 **§5-1 신설**. ⚠ 문구를 **코드를 읽고** 썼다 — payload 실물(`{q, d, v, s, a, t, c, hx}`)을 보고 "질문 원문이 실린다 · base64 라 암호화가 아니다 · 링크가 남는 곳은 다 읽는다 · 회수 못 한다 · 이름은 안 실린다"를 적었다. 검사도 문서와 payload 를 **맞대어** 본다 |
| **C-1** | "열람 후에는 환불되지 않아요" 삭제 → 「지금은 시험 발행이라 값을 받지 않아」. 서신 버튼에도 각인·궁합과 같은 **시험 발행 배지**. 전상법 §17⑥ 고지는 **결제 붙는 날 되살린다**(검진이 양방향 감시) |
| **C-2** | "번호를 대고 다시 받으면 돼" 2곳 삭제. `letterNo` 는 기록에서 계산한 비가역 해시고 계정·주문 DB 가 없어 **우리도 못 되살린다.** 사실대로 적고 실제로 듣는 대책(파일 저장)을 준다 |
| **C-3·C-4** | 결제 미연동이라 지금은 무해. **검진 4-3 을 확장**해 결제 라이브러리가 들어오는 순간 셋(tier 서버검증·재발행 `rec.paid`·`payment=()`)을 한꺼번에 깨우게 했다 |
| **D-1** | 처리방침 MBTI 2곳 삭제 + **계측 속성에서도 제거**. 안 묻는 값을 계속 실어 보내고 있었다(새 유저는 영구 null, 옛 유저 것만 나감). 남은 쓰임은 수호신 얼굴 시드 하나뿐이고 기기 밖으로 안 나간다 |
| **D-2** | 현행 CLAUDE.md 에 `App.jsx:1977` 인용도 "11개 체계" 목록도 **없다** — 전략 세션이 CLAUDE.md 를 다시 쓰면서 사라졌다. 고칠 대상 없음 |
| **D-3** | 세 상품의 클릭을 **같은 자로 맞췄다.** 각인·궁합 버튼에 값 표시(9,900·4,900) + `imprint_offer_shown`·`match_offer_shown` 노출 이벤트(방문당 1회). ROADMAP 의 비교 기준을 고치고 **"v128 이후 데이터로만 판정"**을 못 박았다 |

⚠ **v127까지 쌓인 각인·궁합 클릭은 가격 비교에 쓸 수 없다.** 가격이 안 보이는 버튼을 누른 것이라
서신 클릭(4,900원을 보고 누름)과 같은 종류가 아니다. 데이터를 버리는 게 아니라 **"호기심 클릭"으로 따로 읽는다.**

### 6. 검사
- **검진 5-k 신설** — 진입 애니메이션이 `.rvin` 을 안 거치면 실패, 각인에 `infinite` 가 있으면 실패,
  초기 숨김을 JS 가 안 붙이면 실패. 회귀를 시뮬레이션해 실제로 잡히는지 확인했다(`.imp .impdom{animation:…}` → 검출).
- **리포트 +8** — 브라우저에서 **계산된 스타일**로 본다. CSS 문자열만 보면 셀렉터가 어긋나도 통과한다(v126의 그 사고).
  동시 진입 상한 · infinite 0 · wipe 15줄 · 막대 7개 · **벗어났다 돌아오면 다시 그려지는지**를 실제로 스크롤해서 확인.
- **개인정보 +24** — B-3(문서↔payload 대조 10) · C-1·C-2(6) · D-1·D-3(8).
- **판결 +1** — "환불되지 않아요"의 존재를 지키던 검사를 뒤집었다. 검사가 규칙을 붙들고 있으면 규칙만 고쳐도 안 바뀐다.

### 검증
(병합 후 최종) 리포트 **111/111**(+8) · 개인정보 **108/108**(+24) · 판결 **35/35**(+1) ·
문서계측 16/16 · 계측 24/24 · 스모크 31/31 · 각인 130/130 · 궁합 29/29 · 스코프 54/54 ·
v29 8/8 · WebGL 7/7 · 여러 하늘 12/12 · 검진 clean(모션 검사 1건 추가)

### 7. 병합 — 같은 날 main 에 들어온 두 판과 합쳤다
`v127.1~127.5`(부적 인장·오행 색·광고 진입면)와 **문서 읽기 계측**(`useDocRead`), 그리고
창업자 지시 "질문과 답변은 다 남기자. 다만 개인 식별 불가능하게 해"에 따른 **가명처리(`anon`) 계측**이
먼저 들어와 있었다. 조정 둘:

- **ref 충돌** — 각인·궁합 루트 `<div className="imp fade">` 에 이미 `useDocRead` 의 `readRef` 가 붙어 있었다.
  **한 요소에 ref 는 하나뿐이다.** 두 훅이 각자 ref 를 들면 나중에 붙는 쪽이 앞의 것을 조용히 덮고,
  읽기 계측이나 모션 중 하나가 **소리 없이** 죽는다. `useReveal(ref)` 로 시그니처를 바꿔 같은 ref 를 공유한다.
  검증: `doc-check` 16/16(`imprint_read` read_pct=100·`match_read` read_pct=100) + 모션 검사 8건 동시 통과.
- **§5-1 문구** — "질문 원문은 전송하지 않습니다" 계열 문장이 처리방침에 남으면 안 된다(가명처리본은 실제로 나간다).
  공유 링크 절의 해당 줄을 **링크를 여는 쪽 이벤트**(`shared_verdict_view` = `{dir}` 하나)로 범위를 좁혀 다시 썼다.
  링크를 만든 사람 쪽 기록은 1항 가명처리 설명으로 넘긴다.

### 남은 것
- **C-3·C-4** 본작업 — 결제 배선과 **같은 커밋**에서. 지금 고치면 검증할 대상이 없다
- 서신 예시 5장 재작성(가상 명식으로) · 평가 하네스 실모델 실행 · 「같이 일하면 좋은 사람」에 궁합 엔진 연결
- 커밋 이력의 PII(`46ca39d`/`6b84fe8`) — history rewrite 미결정

---

## 2026-08-15 · v129 — 남은 것 넷

### 1. 시키는 축 ≠ 세는 축 — 열네 판 동안 어긋나 있었다
v114 에 MBTI 를 판결 축에서 뺐다. 그런데 **뺀 건 세는 쪽뿐이었다.**

```
App.jsx:3317  const VOTE_AX = new Set(["사주","달","별자리","수비학", …])   ← MBTI 없음
App.jsx:4120  "votes엔 이번 판결에 참여한 지표를 전부 넣는다(사주·달·별자리·MBTI·수비학·마야 …)"  ← MBTI 있음
```

모델은 시킨 대로 MBTI 표를 실어 보냈고, `tallyVotes` 가 그걸 **조용히 버렸다.**
즉 **모델은 여섯 축으로 판단하고 앱은 다섯 축으로 셌다.** `against/total` 이 모델이 잰 것과 다른 분모였다는 뜻이다.
평가 하네스의 콜2 축 열거에도 MBTI 가 남아 있어, 하네스는 **앱에 없는 프롬프트**로 앱을 채점하고 있었다.

**검진을 양방향으로 고쳤다.** 기존 검사는 "앱에 있는데 하네스에 없는 것"만 봤다 — 반대쪽을 못 잡았다.
- `프롬프트가 시키는 축` vs `VOTE_AX` 대조 (오늘 잡은 그 버그)
- `앱의 축 열거` vs `하네스의 축 열거` 대조
회귀 시뮬레이션으로 실제로 잡히는지 확인했다(MBTI 를 다시 끼우면 → "시키는데 안 세는 축 — MBTI").

**동의 화면의 거짓 두 줄도 같이 고쳤다.**
- "MBTI" — v114 이후 안 묻는데 열네 판 남아 있었다.
- **"네가 적은 질문은 보내지 않아"** — 8/15 가명처리 도입으로 **사실이 아니게 됐다.**
  처리방침은 고쳤는데 동의 화면은 안 고쳤다. **고지의 무게는 문서보다 이 줄이 무겁다** — 여기가 동의를 받는 자리다.
- 거부 수단 위치도 코드를 읽고 적었다. 그 버튼은 `!ritual && !res`, 즉 **로비**에 있다.
  "아래"라고 쓰면 또 없는 자리를 가리키게 된다 — A-1 이 정확히 그 사고였다.

### 2. 평가 픽스처에 실인물이 있었다
`eval/personas.json` 의 P1 이 **창업자 이름 + 사주 네 기둥**이었다.
사주 네 기둥은 60년 안에서 생년월일시와 **일대일**이라, 이름과 나란히 적히면 그 자체로 사람을 특정한다.
CLAUDE.md §운영 규칙이 금지하는 형태 그대로다. (`9de8adc` 가 "실인물 픽스처 없음"이라 적었는데, 이 파일은 안 봤다.)

**손으로 적은 명식 표를 통째로 폐기했다.** 픽스처엔 이제 **가상 생년월일만** 있고,
명식은 `eval/build-personas.mjs` 가 **앱이 export 하는 엔진**으로 그때그때 뽑는다. 이유 둘:
- PII 를 적을 자리 자체가 없어진다.
- 손으로 적은 값은 엔진을 안 따라온다 — v101 대운, v117 위도가 바뀌었을 때 하네스는 **앱과 다른 명식으로 앱을 평가**하고 있었을 것이다. 이제 진실이 한 곳에만 산다.

날짜는 커버리지를 보고 골랐다: **주기운 오행 다섯이 다 나오고**(금2 목2 화1 토2 수1), 남녀·대운 순역·
**시(時) 미상**·**입춘 경계(사주년이 전년으로 넘어감)**·도시 8종이 섞이게.
`eval/personas-check.mjs` 17항목 — 그중 하나가 **기둥 표기가 픽스처에 다시 들어오면 실패**다.
⚠ 그 검사를 처음엔 간지 두 글자로 짰다가 "한여름 **정오**"에 울었다(정+오가 그대로 간지다). 뒤따르는 년/월/일/시까지 봐야 기둥이다.

**그리고 `.mansae-test.tmp.mjs` 가 커밋돼 있었다** — App.jsx 통짜 esbuild 번들 6,229줄.
소스가 두 벌이 되고 비밀키·PII 검사도 두 벌을 봐야 한다. 제거하고 `*.tmp.mjs` 를 무시 목록에.

### 3. 「같이 일하면 좋은 사람」을 궁합으로 이었다
각인의 그 줄은 오행 한 줄이었다 — "금의 기운을 가진 사람이 너를 밀어 줘".
맞는 말이지만 **유저가 아는 건 오행이 아니라 사람**이다. 그래서 궁합에 「같이 일하면 어떤가」 절을 붙였다.

| 행 | 무엇으로 | 근거의 급 |
|---|---|---|
| 둘의 역할 | 일간끼리의 십성 10종 | 배당 그대로 · 직장 말로 옮기는 데 해석 |
| 누가 미나 | 오행 상생 — **각인과 같은 규칙**을 이 사람에게 적용 | 변환 없음 |
| 판은 누가 끄나 | 아쉬타쿠타 바르나(일에서의 결) | 채점 그대로 |
| 말이 통하나 | 아쉬타쿠타 그라하 | 채점 그대로 |
| 얼마나 붙어 있어도 되나 | 일지 육합/삼합/충/형 | 배당 그대로 |

⚠ **축을 안 늘렸다(아홉은 아홉).** ⚠ **총점에도 안 넣었다** — 연애 궁합과 일 궁합을 한 숫자로 뭉개면
"87점"이 무엇에 대한 87점인지 아무도 모르게 된다. ⚠ **"동업하지 마"라고 말하지 않는다** — 관계 판정과 같은 선이다.

⚠ 사고 하나: `Row` 를 ImprintDoc 안쪽에 두고 궁합에서 불러 **궁합 결과 화면이 통째로 죽었다.**
두 문서가 같이 쓰는 조각은 문서 밖에 둔다(`DocRow`). 검사가 잡았다 — `.imp` 가 사라져 타임아웃.

### 4. 태국 요일·하압은 **안 붙이기로 판정**
둘 다 계산은 맞고 검증도 됐는데 붙일 자리가 없다.
- **태국 요일** — 입력이 **요일 하나**인데 자바 웨톤과 아칸 데이네임이 이미 그 요일을 쓴다.
  붙이면 아홉 하늘 중 셋이 같은 입력을 세 번 읽는다. 제1규칙이 "분업이지 투표가 아니다"이고,
  창업자 판정("그냥 사주인데 굳이 쟤네 왜 붙였지")의 답이 정확히 그거였다.
- **하압** — 새로 답할 게 52년 주기(캘린더 라운드)뿐인데 그건 **모두에게 52세**로 상수다.
  v117 에 서양 프로펙션 축을 뺀 것과 같은 이유다(200명 중 148명이 "30세").

**판독기가 있다고 다 붙이지 않는다.** 사유를 `sky.js` 주석에 못 박았다 — 다음 세션이 "안 쓰네?" 하고 붙이지 않게.

### 5. 평가 하네스 실모델 실행 — **이 환경에서는 못 돈다**
로컬에 `ANTHROPIC_API_KEY` 가 없고, 경유 모드는 이 컨테이너의 네트워크 정책이 막는다:
`HTTP 403 — Host not in allowlist: binari-sepia.vercel.app`.
**하네스 자체는 네트워크 직전까지 정상**임을 확인했다(SYS 12,915자 추출 · 새 페르소나 조립 · CSV 출력).
네트워크가 되는 곳에서 이 한 줄이면 된다:
```
cd app && node eval/run-eval.mjs --full --via=https://binari-sepia.vercel.app
```

### 6. 병합 — main 이 같은 자리를 다르게 고쳐 놨다
`v127.5~v128.1`(공유 링크 통계 차단 · 문서 인장 철회 · **MBTI·가치여정 완전 제거**)이 먼저 들어와 있었다.
겹친 자리 넷:
- **VOTE_AX** — main 이 더 나아갔다(가치 축까지 제거). main 을 취하고, 이번 판의 교훈(양방향 대조)을 주석에 얹었다.
- **콜1 지시문** — main 이 이미 MBTI·가치를 뺐다. main 을 취했다.
- **동의 화면** — main 은 "MBTI"만 "관계"로 바꾸고 **"네가 적은 질문은 보내지 않아"는 그대로 뒀다.**
  코드를 확인했다 — `q_anon: anon(q, birth.name)` 이 여전히 나간다. **여전히 거짓이다.** 이번 판정대로 고쳤다.
- 버전 — v128.1 → v129.

**그리고 새 대조 검사가 병합 직후 바로 하나 잡았다.** main 이 앱에서 가치 축을 뺐는데
평가 하네스의 콜2 축 열거엔 `가치`가 남아 있었다 — 앱 8축 vs 하네스 9축. 맞췄다.
같은 종류로 하나 더 나왔다: 하네스 프로필이 **`가치여정(워드소팅 16→6→3→1)`을 계속 싣고 있었다.**
앱은 v128 에 가치여정을 통째로 없앴으니, 하네스는 **앱이 안 보내는 값을 실어 놓고 앱을 채점**하던 것이다.
그 줄을 앱이 실제로 보내는 **「요즘 삶의 국면(직업·관계)」**으로 갈고, 페르소나에도 job·rel 을 넣었다.
→ 검사 2건 추가(페르소나 19). **검사를 붙이자마자 두 건을 잡았다는 게 이 검사가 필요했다는 증거다.**

### 검증
궁합 **39/39**(+10) · 리포트 **114/114**(+3) · 평가 페르소나 **19/19**(신설) · 개인정보 108/108 ·
각인 130/130 · 판결 35/35 · 스모크 35/35 · 문서계측 16/16 · 계측 24/24 · 스코프 54/54 ·
v29 7/7 · WebGL 7/7 · 여러 하늘 12/12 · 만세력 28/28 · 검진 clean(축 대조 2건 추가)

### 남은 것
- **C-3·C-4** — 결제 배선과 같은 커밋에서(검진 4-3 이 그날 셋을 한꺼번에 깨운다)
- **평가 하네스 실모델 실행** — 네트워크 되는 곳에서
- **커밋 이력의 PII**(`46ca39d`/`6b84fe8`) — history rewrite 는 강제푸시라 **사장님 결정 사항**

---

## 2026-08-15 · v130 — 각인·궁합 공유 카드

### 요청
> "각인 궁합도 공유 가능하게 하면좋지 않을까"

### 0. 먼저 읽은 것 — 정책은 이미 서 있었다
디자인 세션이 같은 날 `01_기획전략/비나리_기획전략_바이럴루프판단_v01.md` 를 올려 뒀고,
v129.3 이 그 §2·§4 를 코드로 이행해 뒀다(`shareRisk` · `card_saved`). 그래서 **정책을 다시 세우지 않고 그 위에 얹었다.**
- 창업자 결정: 결과 공개는 허용하되 **선별**한다. 기준은 취향이 아니라 **역산 가능성**.
- §3: 루프의 1차 단위는 **1인 완결형**. 궁합(2인)은 2차 증폭.
- 미결 #1·#2 가 정확히 이번 작업이었다 — 공개 이미지 화이트리스트 확정 · 궁합 공개 범위.

### 1. 링크 안은 기각했다
가장 먼저 떠오르는 건 판결처럼 `?v=` 에 싣는 것이다. **못 한다.**
- 각인 본문은 4,000자가 넘어 URL 에 안 들어간다.
- 그러면 실을 수 있는 건 결국 **입력**(생년월일·시·도시)인데, 그게 CLAUDE.md 가 금지하는 바로 그 세 값이다 —
  "생일+생시+지역 세 값이 함께 적히면 그것만으로 한 사람이 특정된다".
→ **보낼 물건은 문서가 아니라 그림이어야 했다.**

### 2. 무엇을 실을지 — 정책 §2 를 그대로 적용
| 카드 | 싣는 것 | 파생 이름 | 왜 |
|---|---|---|---|
| **각인** | 겉·속 두 줄 + 태어난 해의 이름(납음) + 수호신 + 주소 | **1개**(납음, 60년) | 겉·속은 일간 파생 — 10일 주기라 날짜를 못 좁힌다. 촐킨·웨톤은 **안 싣는다**(둘만으로 LCM 1,820일 = 5년 창에서 유일) |
| **궁합** | 관계 한 줄 + 조심할 것 한 줄 + 수호신 + 주소 | **0개** | 축 이름조차 안 싣는다 — "우리는 충이야"면 한쪽을 아는 사람에게 **상대의 자리 글자**가 좁혀진다 |

**총점도 안 싣는다.** 화면에서 "총점을 앞세우지 않는다"고 해 놓고 카드 한복판에 숫자를 박으면
그 원칙이 앱 밖에서 무너진다. 카드 발치에 그 이유를 적어 뒀다.

### 3. 카드가 나가는 문을 하나로
`saveOrShareCard({build, cardKind, skyKinds, fileBase, title})` 로 배관을 뽑았다.
부적·각인·궁합이 **전부** 이 문을 지나므로 `shareRisk` 판정도 `card_saved` 계측도 빠질 수 없다.
⚠ 위험 판정은 **그리기 전에** 한다 — 그려 놓고 버리면 "위험"인데도 캔버스엔 이미 다 그려진 상태가 된다.

그리고 **카드에 주소를 그려 넣었다.** 지금까지 부적엔 주소가 없었다 —
그림이 퍼져도 받은 사람이 찾아올 길이 없었으니, 루프의 절반이 끊겨 있었던 셈이다.

### 4. ⚠ 검사가 거짓 통과하고 있었다
새 검사를 붙였는데 이상하게 전부 한 번에 통과했다. 파 보니 `sharerisk-check` 의 `grab()` 이
**함수 이름 뒤 첫 `{` 부터** 짝을 맞추고 있었다. 인자가 구조분해면(`function f({ a, b })`)
그 `{` 는 **인자 패턴**이라, 잘라 온 게 본문이 아니라 인자 목록이었다.
→ "본문에 X 가 없다" 류 검사가 **전부 거짓 통과**. 고치자마자 실제 위반 하나가 드러났다.

같은 판에 **문자열 앵커 검사도 성질 검사로 바꿨다.** 기존 검사는
`const risk = shareRisk(args.skyKinds || [])` 라는 문장을 통째로 찾고 있어서,
배선이 멀쩡한데 리팩터링만으로 빨개졌다. 이제 보는 건 성질이다 —
문이 하나인가 · 그 문이 판정을 거치는가 · **판정이 그리기 전인가** · `toDataURL` 이 한 곳뿐인가.

### 5. 고지
처리방침 §5-1 을 「공유 링크와 **이미지 카드**」로 넓히고 카드 둘의 수록 항목을 적었다.
그리고 **왜 개수를 제한하는가**를 적었다 — 파생값은 주기가 다른 나머지값이라 여러 개가 한 장에 모이면
역으로 생년월일이 좁혀진다는 것, 그래서 위험 조합이면 **그림 자체를 만들지 않는다**는 것.
A-4-c 가 잡은 추세(문서는 넓어지고 고지는 0)를 반복하지 않으려는 것이다.

### 검증
공개표면 **33/33**(+10) · 리포트 **121/121**(+7) · 개인정보 **111/111**(+3) · 판결 35/35 ·
궁합 39/39 · 각인 130/130 · 페르소나 19/19 · 스모크 35/35 · 문서계측 16/16 · 계측 24/24 ·
스코프 54/54 · v29 7/7 · WebGL 7/7 · 하늘 12/12 · 만세력 28/28 · 검진 clean

### 남은 것 (바이럴루프판단 v01 기준)
- **판별**: `card_saved{card_kind}` 가 쌓이면 §4 기준으로 읽는다 —
  공유율 15% 미만이면 "물건 문제가 아니었다" → 루프 대신 광고 소재로.
- **유입 귀속**: 카드에 주소는 그렸지만 `?ref=` 는 못 붙인다(사람이 쿼리까지 안 친다).
  카드발 유입은 당분간 `card_saved` 대비 direct 증가로 **간접 추정**만 된다. QR 이나 짧은 주소는 별도 판단.
- C-3·C-4(결제일) · 평가 하네스 실모델 실행(네트워크 되는 곳) · 커밋 이력 PII(사장님 결정)

---

## 2026-08-15 · v131 — 카드를 답에서 미끼로

### 지적
> "궁금함이 느껴지거나 재밌는 포인트가 없어. AI 특유의 설명적인 부분 '총점은 안 실어' 같은,
> 안 알려줘도 되는 어노잉하게 느껴지는 부분으로 정보를 채웠어.
> 호기심이 느껴지게끔해. 내가 궁합을 상대방에게 보내면 왜 보냈을까? 그 사람은 왜 올까?"

정확한 지적이다. v130 카드는 **미니 리포트**였다 — 답을 다 해 버려서 받은 사람이 물어볼 게 안 남았다.

### 1. 진단 — 카드와 문서는 목적이 다르다
| | 문서 | 카드 |
|---|---|---|
| 왜 있나 | **읽히려고** | **열리려고** |
| 그래서 | 설명이 있어야 한다 | 설명이 있으면 안 열린다 |

v130 은 문서 문구를 잘라 카드에 붙였다. 그래서 카드가 문서의 축소판이 됐고,
빈자리에 우리가 **우리 설계 원칙을 해설**해 놓았다 — "총점은 안 실어 — 궁합은 어디가 갈리는가로 읽는 거야".
그건 우리 규칙이지 받는 사람의 궁금증이 아니다. **그 자리가 훅이 들어갔어야 할 자리다.**

### 2. "왜 보냈을까 / 왜 올까"에 답 만들기
- **보내는 이유** — 카드에 **옮겨 적고 싶은 한 줄**이 있어야 한다. 분류명이 아니라 장면이어야 한다.
- **오는 이유** — 카드가 **자기 것으로는 완결되지 않아야** 한다.

그래서 **되묻는 줄**을 넣었다. 궁합 쪽 문구:
> 이건 내 쪽에서 본 거야. 네 쪽에서 보면 다르게 나와.

⚠ **이건 지어낸 훅이 아니라 사실이다.** 일간 십성은 방향이 있어서(내가 상대를 보는 관계 ≠ 상대가 나를 보는 관계)
같은 두 사람이라도 누구 쪽에서 읽느냐로 답이 갈린다. 거짓 훅을 붙이면 열어 본 사람이 배신감을 느낀다 —
**우리가 실제로 갖고 있는 성질을 그대로 말하는 게 제일 센 훅이었다.**

각인 쪽은 "너는 겉과 속이 같은 사람일까?" — 카드가 보낸 사람 얘기로 끝나지 않고 **보는 사람에게 넘어간다.**

### 3. 헤드라인을 분류명에서 장면으로
v130 헤드라인은 `clash.t` 였다 — "하늘끼리 갈린다". **우리 내부 분류명**이라 유저에게 아무 뜻이 없다.
십이지 관계를 일상 장면으로 옮긴 7종을 새로 썼다:

| 관계 | 카드 헤드라인 |
|---|---|
| 육합 | 밥때가 저절로 맞는 둘 |
| 삼합 | 같은 데를 보고 있는 둘 |
| 같음 | 설명 안 해도 통하는 둘 |
| 충 | 조용한 사이가 되긴 어려운 둘 |
| 형 | 크게 싸운 적은 없는데 자꾸 쌓이는 둘 |
| 자형 | 같은 실수를 같이 하는 둘 |
| 무관 | 저절로는 안 굴러가는 둘 |

⚠ **축 이름(충·육합·형)도 십이지 글자도 여전히 안 싣는다** — 한쪽 생일을 아는 사람에게 상대가 좁혀진다.
값이 아니라 **장면**으로 옮기면 재미는 살고 역산은 안 된다. 144쌍 전수로 7종이 다 나오는 것 확인.

### 4. 카드 문구를 엔진에 따로 뒀다
`readMatch().card = { head, line, ask }`. 문서의 `care[0]` 을 잘라 쓰지 않는다 —
문서 쪽은 왜 그런지까지 적어야 해서 길다("같이 사는 동안의 흐름이 낮게 나와. 큰 결정(이사·창업·대출)은…").
카드용은 짧은 명령형 7종을 따로 썼다: "이사·창업·대출은 한 박자 늦출 것. 둘이 따로 재 보고 정할 것."

**폴백도 다시 썼다.** 사장님이 받은 카드에 뜬 게 하필 폴백이었고 제일 밋밋했다 —
"특별히 못 박아 둘 위험은 안 잡혀." 아무 말도 안 하는 것과 같다.
→ "싸워서 무너지는 사이가 아니다. **당연해질 때 무너진다.**" 위험이 없으면 **무너지는 방식**을 말한다.

### 5. 조판 세 건 — 눈으로 보고 잡았다
카드는 그림이라 검사로는 "재미없다"도 "못생겼다"도 못 잡는다. `e2e/card-shot.mjs` 를 만들어 실물 PNG 를 떴다.
- **고아 어절** — 큰 글씨에서 "한번 정하면 안 바꾸는 / **사람**", "저절로는 안 굴러가는 / **둘**" 이 나왔다.
  그리디 랩만 하면 생긴다. ①줄이 넘치면 글자를 줄이고 ②마지막 줄이 한 어절이면 앞 줄에서 하나 내린다.
- **아우라가 그림과 따로 놀았다** — `cardBase` 가 y=620 고정으로 빛을 깔고 그림은 y=450 에 그렸다.
  **빛이 그림 밑에 안 깔려서** 수호신이 납작하고 흐리게 나왔다. 아우라를 `drawGuardianOn` 안으로 옮겨 같이 움직이게.
- **수호신이 손톱만 했다** — 화면 캔버스는 둘레 여백이 넓은데 가운데 정사각으로만 잘라 넣고 있었다.
  **알파 경계 상자**를 찾아 자른다(160×160 으로 축소해 훑으므로 비용은 무시할 수준). 못 읽으면 옛 방식으로 되돌아간다.

### 검증
공개표면 33/33 · 리포트 121/121 · 개인정보 111/111 · 판결 35/35 · 궁합 39/39 · 각인 130/130 ·
스모크 35/35 · 문서계측 16/16 · 계측 24/24 · 스코프 54/54 · v29 7/7 · WebGL 7/7 · 검진 clean
그리고 **실물 카드 두 장을 떠서 눈으로 확인**했다.

### 남은 것
- 카드발 유입 귀속(QR·짧은 주소) — 바이럴루프판단 v01 §5 미결 4
- C-3·C-4(결제일) · 평가 하네스 실모델 실행 · 커밋 이력 PII(사장님 결정)
## 2026-08-14 · v94 — 공유 경로 개인정보 정리 + 부적 계측 + 관계 축 진입

**계기**: 궁합 검증 워크플로가 우리 코드에서 문제를 짚었고, 직접 확인해보니 사실이었다. 창업자 승인 후 5건 일괄 처리.

### 1. 공유 링크에서 실명 제거 (`App.jsx` shareVerdict)
- `payload`에서 `n: (birth.name||"").trim()` **제거**.
- 판단 근거: **질문 원문(`q`)은 공유 문구에 이미 보이므로 유저가 알고 보낸다. 이름은 공유 문구에 안 보이고 URL에만 담겨 나가서 공유자가 모른다.** 보이는 것과 안 보이는 것의 차이로 갈랐다.
- 수신 화면은 손댈 필요 없었다 — `sharedIn.n ? ... : "어떤 이의 수호신이 이렇게 판결했어"` 폴백이 이미 있었다.
- base64는 암호화가 아니다. 링크를 받은 누구나 디코딩한다.

### 2. 부적 저장·공유 계측 (`saveOrShareBujeok`)
- 함수 전체에 `track()` 호출이 **0건**이었다. 잡히던 건 '펼치기' 버튼(`bujeok_opened`)뿐이라 **실제로 밖에 나갔는지를 몰랐다.**
- 3경로 전부 부착: `bujeok_saved{via:"share"|"download"|"newtab", dir}` + 취소는 `bujeok_cancelled`.
- 질문 원문·이름은 싣지 않는다(방향만).

### 3. 부적 포스터에 주소 (`buildBujeokPoster`)
- 기존 하단 텍스트가 `"비 나 리 · B I N A R I"`(브랜드명)뿐 — **받은 사람이 어디로 갈지 몰랐다.**
- `binari-sepia.vercel.app`을 y=1868에 추가. 우리 유일한 밖으로 나가는 자산에 목적지를 붙였다.

### 4. 관계 축 진입 — 문구·프롬프트만 (헌장 무위반)
- `QHINTS`에 3개 추가: "그 사람에게 마음을 더 줘도 될까?" / "이 사람과 계속 같이 일해도 될까?" / "먼저 연락해서 사과할까?"
- **결정 형태(GO/STOP/HOLD)를 유지**하면서 상대를 특정한다. 직장 관계까지 넓혔다(사주도령이 "팀장님은 어떤지"로 확장한 것과 같은 각도).
- `SYS`에 가드 1줄: **상대의 사주·별자리를 지어내지 않는다. 우리가 가진 건 유저 지표뿐이므로 "지금 너의 기운이 그 관계를 어떻게 통과하는가"로만 답한다. 상대의 마음·의도를 단정하지 않는다.**
- 상대 생년월일을 받지 않으므로 개인정보 이슈 0.

### 5. 처리방침 (`app/public/privacy.html`)
- §5 Anthropic 행: "질문 원문 및 사주 프로필" → **"질문 원문, 산출된 프로필, 그리고 입력하신 경우 이름(호칭)"**으로 명시. 이름이 가는데 안 적혀 있었다.
- **§5-1 '판결 공유 링크에 담기는 정보' 신설** — 질문 원문 포함 / 이름 미포함 / 회수 불가 / 서버 미보관 / 공유는 전적으로 선택. 타인 특정 가능 내용 주의 안내 추가.

### 헌장 문언 확장 (`CLAUDE.md`)
- 기존 "**계측** — 질문 원문·실명은 전송하지 않는다"는 **'계측'에 한정돼 공유 경로가 문언 밖으로 빠져나갔다.** 정작 노출은 계측보다 공유가 크다.
- 3경로(계측/판결생성/공유)로 나눠 각각 규칙을 박고, 판단 기준을 한 줄로 남겼다 — **"유저가 무엇이 나가는지 보고 있는가. 안 보이는 채로 나가면 그건 유출이다."**

### 함께 정리한 것
- **방어 3자산에서 '되물음' 제외.** 창업자 실사용("어노잉하기만") + 실측(n=1) + 경쟁사 무료화. **원인 진단: 되물음은 재방문 시 수호신이 먼저 묻는 push라 §모를 권리를 우리가 스스로 어긴 기능이다.** 기능 제거가 아니라 자원 배분 대상에서 뺀 것.
- **새 후보 등재: 11개 체계 동시 계산.** `App.jsx:1977`이 이미 사주·대운·삼재·토정·주역·납음·별자리·달위상·달별자리·수비학·나크샤트라·마야촐킨·MBTI·바이오리듬·가치여정을 LLM에 먹인다. 경쟁 8개사는 전원 만세력 단일 엔진. **우리만 데이터가 11개인데 판결 한 줄 뒤에 숨겨놨다.**

**빌드 통과** (`npm run build`, 291.90 kB / gzip 104.90 kB).
**아직 main에 안 올렸다** — main 푸시 = Vercel 자동배포이고 명시 지시가 필요하다. **①번 수정은 배포 전까지 라이브에 반영되지 않는다.**

### v94 보강 — 앞선 수정이 겉만 고쳤다 (적대적 검증 결과 반영)

변경분을 3축으로 검증시켰더니 **`n` 필드 삭제가 사실상 무효**였다. 확인한 것과 고친 것:

| 발견 | 확인 | 조치 |
|---|---|---|
| **판결문 자체가 이름을 부른다** | `App.jsx:1589` SYS — "호칭이 제공되면 결정적 순간에 이름을 부른다(B:○○아·A:○○님)". payload의 `v: res.verdict`에 그 문장이 그대로 실린다 | 공유 경로에서만 **호칭 형태를 걷어내는 `scrub()`** 추가. `이름+(아|야|님)` **뒤에 공백·문장부호가 올 때만** 지운다 — "지원아끼지"의 '지원아'를 잘못 지우지 않기 위해 |
| **구링크에는 이름이 들어 있다** | 이미 배포돼 나간 `?v=` 링크는 회수 불가인데 수신 화면이 `sharedIn.n`을 렌더 중이었다 | **폴백 분기 자체 제거** + `decodeShare`에서 `delete o.n` — URL→화면 경로 전체가 이름을 안 다룬다 |
| **계측에 판결 원문이 나간다** | `App.jsx:2010` `verdict: r1.verdict` — 그 문장에 이름이 들어갈 수 있으니 **헌장 §계측 실명 금지를 이미 어기고 있었다** | `verdict` → `vlen`(길이)로 교체. **그리고 내가 헌장 확장 때 계측을 "(현행 유지)"로 통과시킨 게 틀렸다** — 확인 안 하고 도장을 찍었다 |
| **처리방침 §5-1 세 문장 중 둘이 거짓** | ①"이름은 링크에 포함되지 않습니다" — 판결문 안에 있다 ②"공유하지 않으면 어떤 정보도 외부로 나가지 않습니다" — **같은 문서 §5가 Anthropic 전송을 적어놨다.** 문서가 자기와 충돌 | 둘 다 재작성. 이름은 "**남을 수 있다**+자동 제거는 완전하지 않다"로, 외부 전송은 "공유 링크를 만들지 않으면 **제3자에게** 전달되지 않는다(Anthropic·분석은 별개, 5·2항 참조)"로 |
| 시행일 미갱신 | `2026년 7월 24일` 그대로인데 §5·§5-1을 바꿨다 | 8월 14일 + 변경 요지 1줄 |
| 버튼 이름 불일치 | 방침은 '공유하기', 실물은 '카톡·라인으로 판결 보내기' | 실물로 맞춤 |
| **`clearMemory`가 6개 키 중 1개만 지웠다** | `store.removeItem(STORE_KEY)` 단독. `belief`(민감정보라고 코드 주석이 자인)·consent·firsttouch·daily가 남아 **"처음부터 다시"가 거짓**이었고, 리로드 시 앞사람 belief가 super property로 재부착됐다 | 5개 키 삭제 + `_ph.reset()`. **`INTERNAL_KEY`만 남긴다** — 지우면 팀 유입이 유저 지표에 섞여 D7 게이트가 무의미해진다 |
| 부적 새 탭 계측이 실패까지 'saved'로 집계 | `if/else` **밖**에서 track — `location.href = dataUrl` 폴백(브라우저가 차단)도 저장됨으로 찍혔다. iOS가 부적 저장의 주 플랫폼이라 도입 목적 자체가 무너진다 | `bujeok_viewer_opened` / `bujeok_failed`로 분리 |
| 공유 버튼에 인라인 고지 없음 | 부적엔 `fine` 안내가 있는데 공유엔 없다. **처리방침은 아무도 안 읽는다** | 버튼 아래 1줄 — "네 물음과 판결이 함께 담겨 나가. 보낸 링크는 되돌릴 수 없어." |

**교훈 하나만 남긴다.** 처리방침 §5-1을 **코드를 확인하지 않고 썼다.** 세 문장 중 둘이 틀렸고 **둘 다 우리에게 유리한 방향**이었다. 이번 세션 내내 남의 문서에 대고 하던 지적을 내 손으로 재현했다.

빌드 통과. **여전히 main 미배포.**

---

# [합류] `app/작업로그.md` 이관 (2026-08-17)

**개발 로그가 두 갈래였다.** 루트 `작업로그.md`(정본)와 별개로 `app/작업로그.md` 가 자라서, 8/12 각인 4단계·8/15 가명처리 전환 같은 무게 있는 기록이 **정본에 없는 쪽**에 쌓였다. 방향점검 §1-7이 지적한 그대로다.

아래 9건이 `app/작업로그.md` 에만 있던 전량이다(전문 이관, 유실 0). **원 문서는 포인터만 남긴 껍데기로 바꿨다.**
원 문서는 최신순이었고 이 문서는 오래된 순이라 **순서를 뒤집어** 넣었다 — 각 항목의 날짜 표기는 그대로다.

⚠ **날짜가 이 문서의 앞 항목들과 겹친다.** 8/6~8/17 구간은 위쪽에도 항목이 있으니, 그 구간을 조사할 때는 **두 곳을 다 봐야 한다.** 시간순으로 재정렬하지 않은 이유는 재정렬이 곧 원문 수정이고, 이관에서 가장 피해야 할 것이 유실이기 때문이다.

**앞으로 개발 로그는 루트 `작업로그.md` 하나다**(CLAUDE.md §기록 규칙).

## [2026-08-06] v109 · 알 권리 — 상세 리포트 완결화

**요청**: "오늘 만들어준 그런 식의 사주 리포트가 지금 상세 리포트 뽑으면 보이나? 지금 반영하면 좋겠는데. 그리고 알권리도 반영."

**확인한 구멍 — 셋 다 '있는데 숨긴' 것이었다**
1. `saju.pillars`(사주 여덟 글자)와 `saju.counts`(오행 개수)는 **온보딩 연출(step 2 · reveal===1, refbox)에만** 렌더됐다. v16 '수호신의 기억'으로 재방문 시 온보딩을 통째로 건너뛰므로 **유저는 자기 사주를 두 번 다시 볼 수 없었다.** 모든 판단의 뿌리인데 리포트에 없었다.
2. `MyeongsikReport` 가 `useState(false)` — 버튼을 눌러야 열렸다.
3. `.msrbody{max-height:170px}` — 열어도 한 번에 여섯 줄만 보였다.

**고친 것**
- `calcSaju`: 진태양시 보정값을 `tstAdj`(분)로 노출. 계산만 되고 화면 어디에도 없던 숫자였다.
- `MyeongsikReport` → 겉껍질(널 가드) + `MyeongsikReportBody`(훅). `open` 기본 `true`.
- 계측: `report_opened`(클릭) → `report_shown`(노출, `useEffect` 1회). 기본 펼침이 되면 클릭 계측은 영영 안 찍힌다.
- **명식 절 신설**: 네 기둥 카드(년·월·일·시, 일기둥 `.me` 강조) + 기둥 뜻 한 줄 + 일간(나) + 오행 막대 5종 + 계산 근거 고지(태양황경·율리우스일·만세력 28건 자동검증) + 진태양시 보정 분.
- 십성 전량 공개: `sipseongDist`가 0개인 십성을 아예 빼고 주므로 `SS10` 기준으로 채워 넣음.
- 온보딩 `<details className="refbox">` → `open`.

**레이아웃 회귀와 그 수정 (이번 작업에서 가장 위험했던 부분)**
`.vcard`는 `display:grid`에 앞뒤 면이 같은 `grid-area:1/1`이라 **카드 높이 = 두 면 중 큰 쪽**이다. 리포트를 키우자 뒷면이 카드를 늘렸고 **앞면 판결문 아래가 텅 비었다**(실측 430 → 593 → 1681px). 앞면은 코어 화면이라 이건 명백한 회귀다.
- 1차 시도(`.vscroll{flex:1;min-height:0}`)는 실패 — grid 행에 상한이 없어 `flex:1`이 안 먹는다.
- 2차 시도(`max-height:min(72vh,600px)`)는 600px로 묶이지만 여전히 앞면(430)보다 커서 공백이 남았다.
- **채택**: `.vface.back{position:absolute;inset:0}` — 뒷면을 카드 높이 산정에서 제외한다. 카드 크기는 앞면이 정하고, 길어진 리포트는 `.vscroll` 안에서 스크롤한다. 430px 복귀 확인.
- 부수: 한 문서로 흐르면서 본문이 고정 헤더 아래로 비쳐 `.vface.back .vtop`을 불투명 처리.
- 명식 네 기둥이 252px 폭에서 넘쳐 '시' 기둥이 잘렸다 → 기둥 뜻을 아래 한 줄로 내림.

**검증**: report-check 12→**20/20** · smoke 25/25 · verdict 34/34 · scope-check 54/54 · webgl 7/7 · v29 8/8 · mansae 28/28 · track 24/24 · origin 20/20 · 검진 심각 0(주의 3건은 기존: 형상 수식 이중화·dev 전용 audit·프록시로 라이브 확인 불가). 앞뒤 면 스크린샷 육안 확인.

**health-check에 추가한 4규칙(사고 #7 재발 방지)**: ①리포트 `useState(true)` ②`.msrbody`에 max-height 재등장 검출 ③`.vface.back` absolute + `.vcard` grid-template-rows 고정 ④리포트에 명식 원판 존재.

## [2026-08-12] v111 · 항목별 4단 — 리포트의 본체를 '삶의 자리'로

**요청(창업자 2026-08-11)**: "리포트는 지금처럼 사주베이스가 아니고 **각 항목별 설명이 들어가는 형태**.
태어날 때 정해진 운명이 이랬어, 그래서 크다가 이런 느낌이 들었을 거야, 넌 지금 어느 단계야, 앞으로 이렇게 될 거야.
항목도 여러가지겠지 — 학업 건강 연애 결혼 자녀 직장 궁합. 그리고 **선을 좀 넘어**. '어떤 류다' 이런 식으로 말하지 말고
어떤 일이 생길 거다 딱 명확히 말해. 아니면 어떤 류의 일이 생기는데 예를 들면 머머머머다, 이렇게 하던가."

**무엇이 문제였나**
리포트가 *사주 항목별*로 짜여 있었다 — 명식 → 타고난 것 → 흐름 → 사람 → 날 → 일.
전부 맞는 정보인데, 유저가 사려는 건 "내 십성 분포가 얼마인가"가 아니라 **"내 건강은 어떤가, 내 돈은 어떤가"**다.
같은 데이터를 **삶의 자리로 재배열**하는 것이 이번 작업이다.

**넣은 것 — `lifeDomains()` (순수 함수)**
- **삶의 자리 아홉**: 몸 · 마음 · 배움 · 일 · 돈 · 연애 · 결혼 · 자녀 · 사람 (연애·자녀는 성별 필요 → 없으면 일곱)
- **자리마다 4단**: 새겨질 때 → 자라면서 → 지금 → 앞으로
- 「지금」·「앞으로」는 **아홉 자리가 같은 규칙**으로 낸다 — 이 자리를 맡은 십성 무리가 지금 대운에 있나,
  없으면 언제 오나(`nowBy`/`nextBy`). 대운이 없으면(성별 미입력) 지어내지 않고 **못 짚는다고 말한다.**
- 「앞으로」에 `SS_EVENT` 사건 예시를 붙였다 — "정재 대운" 대신 **"월급이 오르거나, 계약이 하나 길게 붙거나,
  집·차처럼 네 이름이 올라가는 물건이 생겨"**. 나중에 맞았는지 틀렸는지 셀 수 있는 문장으로 쓴다는 뜻이다.
- `EL_ORGAN` — 오행별 장부와 증상. **병명이 아니라 '어디가 약한가'까지만**(창업자 지시 2026-08-10).
- 「몸」만 십성이 아니라 **오행 규칙**으로 움직인다. 가장 얇은 자리를 가장 센 기운이 치는 배치(火多金熔 꼴)를 따로 짚는다.
- 기존 절들은 「**셈의 근거** — 위 아홉 자리가 어디서 나왔나」 아래로 내려 보존했다. 알 권리는 그대로다.

**실측으로 잡은 결함 셋** (전부 화면을 실제로 찍어 보고 발견)
1. **글이 세로로 찢어졌다.** `.dstep` 이 flex 인데 강조 표시기 `Em` 이 만든 조각(`<span>`/`<b>`)이
   **하나하나 열이 됐다.** 본문을 `.dt` 한 겹으로 싸서 고쳤고, **회귀 검사로 못 박았다**
   (`p.children.length === 2 && children[1].classList.contains("dt")`).
2. **조사 오류** — "식신**로** 결이 바뀌어", "귀**이야**". 표에서 문장을 조립하니 받침 처리가 없으면 바로 티가 난다.
   `JONG()`(종성) → `RO()`/`IYA()` 헬퍼 신설. ㄹ 받침은 '로'를 쓴다.
3. **동률을 순서로 몰래 골랐다** — 십성이 전부 1개인 명식에서 "가장 두꺼운 게 겁재 1개"가 나왔고,
   일 자리에서도 재성 2·비겁 2 동률인데 배열 순서로 재성이 뽑혔다. **실사고 2026-08-02와 같은 병**이다.
   동률이면 **동률이라고 말하고 둘 다 낸다.**

**검사**: report-check **26 → 37항목**(4단 구조·세로 찢김·조사·사건 예시·아홉 제목).
smoke 25/25 · verdict 34/34 · scope-check 54/54 · 검진 79건 이상 없음(주의 4건은 기존).



## [2026-08-12] v112 · 용어 은닉 — 무슨 기법을 쓰는지 화면에 안 내보낸다

**요청(창업자)**: "어떤 분석 기법이 들어갔는지 안 나왔으면 좋겠어. 우리 기밀 유출 같아."

**먼저 정확히 짚은 것**
용어 자체(십성·대운·용신)는 **공개 지식**이라 그것만으로 기밀이 아니다. 실제로 새는 건
**어떤 기법을 어떤 표에 어떻게 매핑했는가**인데, 용어를 그대로 쓰면 그 조합이 한 화면에서 통째로 읽힌다.
이름을 갈면 정보량은 그대로면서 베끼는 비용만 오른다.
그리고 이건 창업자가 전에 낸 지시("너무 어렵게 설명해서 알권리가 헤치는 거 같아", 2026-08-11)와 **같은 방향**이다 —
알 권리는 **이해 도달**이지 용어 노출이 아니다.

**경계선을 먼저 정했다**
> **그 사람의 값은 보여주고(태어난 여덟 글자·간지·개수·나이 구간), 기법의 이름은 안 보여준다.**

값까지 지우면 v109에서 고친 알 권리 위반("카드 뒷면 근거를 안 알려줘서 몰랐다")으로 되돌아간다.

**바꾼 것**
- `EL_KO`(목→나무 …) · `SS_KO`(겁재→겨루는 힘 …) · `GRP_KO` · `SIN_KO`(문창귀인→글의 복 …) · `STR_KO`(신강→제 힘으로 미는 쪽) 신설.
  **코드 안의 키는 그대로 뒀다** — 키까지 갈면 명리 규칙과 대조가 안 돼 유지보수가 불가능해진다. 화면에 나가는 이름만 간다.
- 4단 본문에서 **대운 간지를 뺐다** — "33~42세 임오 대운" → "33~42세". 간지는 근거 부록에만 남는다.
- 연애·자녀 자리는 **어느 자리가 그 인연을 맡는지 말하지 않는다**("남자에게 인연은 재성" → "네 인연을 맡은 자리가"). 매핑 자체가 알맹이다.
- 확신도 꼬리표: 계산값/통설 해석/곁가지 → **확실한 것 / 갈리는 것 / 곁들이는 것**.
- 계산 근거 고지: "태양황경·율리우스일·만세력 대조" → "태양의 실제 위치를 직접 계산 · 대조 검증 28건". **신뢰 장치는 남기고 기법 이름만 뺐다.**
- **서신 프롬프트에 `[용어 금지]` 절 신설** — 프로필에는 용어를 그대로 실어 보낸다(모델이 추론해야 하니까).
  대신 **출력 금지 + 바꿔 쓰는 말 대응표**를 지시서에 박았다. 이게 없으면 9,900원짜리 문서가 용어를 그대로 받아쓴다.
  정직성 ③도 3단(용어→쉬운 말→모습)에서 **2단(쉬운 말→모습)**으로 고쳤다.

**되돌아오는 걸 막는 장치 둘** (이게 이번 작업의 본체다)
- `report-check`: **금칙어 43종을 실제 화면 텍스트에서 검사**한다. 하나라도 있으면 FAIL.
  동시에 "값은 그대로 남는가"(여덟 글자·흐름 구간)도 함께 본다 — 감추다가 근거까지 지우는 걸 막는다.
  `상관`은 "직접 상관없다"로도 쓰여 오탐이 나므로 따로 본다.
- `health-check` 검사 5-c: **서신 지시서에 [용어 금지] 절·대응표·프로필 주의문이 살아 있는지** + 리포트 렌더 구간에 용어 상수가 되돌아왔는지.
  ⚠ 첫 판에 헛울음이 났다 — 렌더 구간 슬라이스에 **코드 주석**이 섞여 "명식에서"를 잡았다. 주석을 걷어내고 보도록 고쳤다.

**검사**: report-check **37 → 43항목** · smoke 25/25 · verdict 34/34 · scope-check 54/54 · 검진 이상 없음(주의 4건은 기존).


## [2026-08-12] v113 · 각인 — 판결 밖의 두 번째 상품 (4단계 완주)

**요청(창업자)**: "1~4번까지 순차적으로 실행. 그리고 결제 안 붙어도 테스트는 해야 하니까 발행도 되게."

**자리를 먼저 정했다 — 서신 옆이 아니다**
| | 수호신의 서신 | 각인 |
|---|---|---|
| 무엇에 딸리나 | **질문 하나** | **사람 자체** |
| 몇 번 사나 | 판결마다 | **평생 한 번** |
| 진입점 | 판결 카드 뒷면 | **수호신 화면(로비)** |

재구매 구조가 반대라 같은 자리에 두면 안 된다. 판결 뒤에 붙이면 판결할 때마다 "또 사라는 건가"가 되고,
안 사는 사람에겐 매번 거절해야 하는 버튼이 된다. 그리고 "나는 너를 오래 지켜봤다"는 판결 끝보다
수호신 앞에서 나와야 세계관이 산다.

**2단계 — 엔진 이식** (`app/src/lib/sky.js`)
05_실험의 판독기 세 모듈을 한 파일로 합쳐 옮겼다. 각인의 「지금·앞으로」를 떠받치는 건 시간축 셋뿐이다 —
사주의 열 해 흐름 · 인도 다샤 · 서양 프로펙션. 나머지 열 종은 전부 정지 사진이다.
천문 함수가 App.jsx 에도 있는데 **합치지 않았다** — 사주 쪽은 만세력 28건 대조를 통과했고 거기를 건드리면
그 검증이 흔들린다. 대신 `e2e/sky-check.mjs` 로 두 벌이 240일치 같은 값을 내는지 못 박았다(차이 0).

**3단계 — 판정 규칙** (`app/src/lib/imprint.js`)
손으로 쓴 카피를 표로 바꿨다. 겉(SURFACE)·속(INNER)·**비어 있는 자리**(BLOCK)를 축으로 삼고,
몸 다섯 항목·짝 열 항목·뒤집히는 조건 셋·여든 해·열두 달을 낸다. 모든 문장이 각주 번호를 함께 낸다.

**검사가 잡은 것 셋** (전부 실물을 열어 보고 나온 것)
1. **여든 해가 통째로 같은 값**이었다. 앱의 대운은 한글 간지("임오"), 흐름 엔진은 한자("壬午") —
   한쪽만 찾으니 `indexOf` 가 −1 이었다. 둘 다 보게 고치고, **못 읽는 구간은 버린다**(조용히 틀린 값을 안 낸다).
2. **"세 셈이 전부 이 나이를 가리킨다"가 갈릴 때도 나왔다.** 목성 시기를 그대로 후보로 쓰니 사람에 따라
   예순여섯이 후보로 들어와 폭이 36년·53년까지 벌어졌다. **혼인 가능 구간(22~45세)에 걸치는 것만** 세고,
   폭 8년을 넘으면 "갈린다"고 적는다. 흩어진 걸 평균 내서 한 숫자로 내놓으면 합의가 아니라 눈속임이다.
3. **"3~12세에 만나는 사람은 오래 못 간다"** — 세 살짜리 인연. 인연 구간은 열여덟 이상만 세고,
   짝을 만나기 전/후로 갈라 뜻을 달리 준다(전=지나갈 인연, 후=흔들리는 결혼). 조사 오류("…일야")도 같이.

**4단계 — 화면** (`ImprintDoc`)
서신과 같은 전체 화면 자리에 걸되 상품은 별개다. **각주는 기본 접힘** — 검증용이라 늘 떠 있으면
기법이 그대로 노출된다. 「근거 보기」를 눌러야 열린다.

**1단계 — 진입점·발행** 결제 전이라 **무료로 열린다**(창업자 지시: 실물이 나와야 값을 매길 수 있다).
버튼에 「시험 발행」 배지를 달고 `imprint_clicked`·`imprint_opened`·`imprint_notes_toggled` 를 계측한다.
서신 4,900원 클릭률과 나란히 비교되므로 **"사람들이 질문 풀이를 원하나, 자기 자신을 원하나"가 숫자로 갈린다.**

**검사**: report-check 43→**57항목** · 여러 하늘 12/12 · 각인 엔진 26/26 ·
smoke 25/25 · verdict 34/34 · scope 54/54 · 검진 81건 이상 없음.


## [2026-08-12] v114 · MBTI 4문항 제거

**요청(창업자)**: "MBTI 묻는 건 이제 그냥 삭제해줘. 판결에 영향도 크지 않을 뿐더러 무슨 말인지 모르겠다는 사용자들이 많아서."

**그냥 지우면 안 되는 자리가 하나 있었다**
MBTI 는 온보딩 문항이면서 동시에 **수호신 비주얼의 축 하나**(밀도·속도·질서·반짝임)를 맡고 있었다.
문항만 지우면 `mbti` 가 null 이 되고, 렌더러의 `mbti?.[0] === "E"` 가 전부 false 로 떨어진다.
→ **새 유저의 수호신이 전부 같은 질감이 된다.** v27~v28 이 공들여 만든 다양성이 통째로 죽는다.

**그래서 묻지 않고 뽑는다** (`texture()`)
설계 헌장 그대로다 — *"수호신은 이미 너를 안다. 자동 파생 가능한 값은 손으로 되묻지 않는다."*
- E/I ← 사주의 뻗는 기운(목·화) vs 거두는 기운(금·수)
- N/S ← 별자리 원소(공기·불이면 N)
- T/F ← 일간 오행(금·토면 T)
- P/J ← 라이프패스 홀짝

**이미 저장된 mbti 는 버리지 않는다.** 시드 문자열에 들어가 있어서, 지우면 **쓰던 유저의 수호신 얼굴이 바뀐다.**
있으면 그대로 쓰고, 없을 때만 파생한다. 묻는 것만 없앤 것이지 받은 걸 버린 게 아니다.

**걷어낸 것** — DIMQ 표·`pickDim`·`dims` 상태·문항 화면·프롬프트의 MBTI 축(`VOTE_AX` 포함)·`reasons` 요구 문구.
회상 확인("응, 기억나") 뒤 곧장 마음의 방으로 간다. 온보딩이 한 화면 짧아졌다.

**검사가 잡은 것 — 이게 제일 컸다**
메모리 저장 조건이 `step === 3 && saju && mbti && core` 였다. mbti 가 null 이 되면서
**localStorage 가 통째로 저장이 안 됐다.** 재회·아침문안·판결록이 전부 죽는 조건이다.
스모크 5건이 한꺼번에 빨개져서 잡혔다. 조건에서 mbti 를 뺐다.
→ **필수 검증 목록에 '없어도 되는 값'이 섞여 있으면, 그 값을 없애는 날 앱이 조용히 죽는다.**

e2e 6개 파일이 MBTI 문항을 클릭하고 있었다. 경로를 고치고, v29-check 의 '문항 순차' 검사는
**'문항이 없다'는 검사로 뒤집었다** — 되돌아오면 우는 쪽으로.

**검사**: smoke 25/25 · report-check 57/57 · verdict 34/34 · scope 54/54 · v29 8/8 · webgl 7/7 ·
여러 하늘 12/12 · 각인 26/26 · 검진 81건 이상 없음.


## [2026-08-12] v115 · 각인 심화 — 9,900원짜리 두께로

**요청(창업자)**: "리온이 리한이 리포트 쓴 거보다 너무 빈약한데? 이거는 9,900원짜리 리포트라 생각하고
내용의 밀도와 깊이, 완성도를 높여봐. 그래프도 필요할 거고. + 추가로 받아야 하는 정보는 없어?"

**빠져 있던 건 4단 구조였다**
v113 각인은 겉·속·몸·짝·여든 해만 있었다. 스크래치패드 리포트의 **뼈대인 4단**
(태어날 때 → 자라면서 → 지금 → 앞으로)이 통째로 없었다. 그게 없으니 "지금 나에게 무슨 뜻인가"가 안 붙는다.

**넣은 것**
- **아홉 자리 × 4단 = 36칸** — 몸·마음·(가장 얇은 자리)·공부·일·돈·사람·연애·자식
- **그래프 셋** — ①겉/속과 막힌 문 도해 ②열두 달 막대 ③**여든 해 굴곡**(지금 구간을 금색으로)
- **확인 문항 열둘** — 근거를 안 대는 대신 확인할 방법을 준다. "일곱 이상 맞으면 참고할 만하다"
- 본문이 **4,700 → 5,957자**로. 회귀에 **4,000자 하한**을 못 박아 다시 빈약해지면 검사가 운다

**추가 입력 — 무엇을 받을지가 진짜 질문이었다**
도시는 이미 받고 있었다(선택). 실제로 값어치를 올리는 건 **지금 상태** 둘이다.

> 이걸 모르면 **마흔 살 기혼자에게 "서른에 짝을 만난다"고 쓰게 된다.** 그 한 줄이 문서 전체를 죽인다.

- **결혼했나 · 아이가 있나** — 두 번 탭. `married: true` 면 연애의 「앞으로」가
  *"이미 만났으니 이 자리는 지나갔어. 앞으로 말하는 건 인연이 아니라 그 사람과의 온도야"* 로 바뀐다.
  `kids: true` 면 자식의 「지금」이 *"이미 왔어. '올까'가 아니라 '어떻게 키우나'의 자리야"* 로 바뀐다.
- **묻는 자리는 각인을 열 때다.** 무료 온보딩을 무겁게 하지 않으면서 유료 문서만 정밀해진다.
  스무 살 미만에겐 안 묻는다. 안 알려줘도 문서는 나온다 — 그 자리가 헐거워질 뿐이다.
- 돈이 없으니 **선택**으로 두되, **왜 필요한지를 그 자리에서 설명**한다("이걸 모르면 지난 일을 앞일처럼 적게 돼").

**만들면서 걸린 것 둘**
1. `put` 이름 충돌 — 십성 세는 헬퍼와 4단 등록 헬퍼가 같은 이름이었다. `putD` 로 분리.
2. CSS 를 JS 주석 자리에 넣어 빌드가 깨졌다. 앵커가 스타일시트가 아니라 컴포넌트 주석이었다.

**검사**: report-check 57→**65항목**(4단 36칸·그래프 3·확인 12·본문 4,000자 하한·선택 입력이 실제로 문장을 바꾸는지) ·
각인 엔진 26/26 · 여러 하늘 12/12 · smoke 25/25 · verdict 34/34 · 검진 이상 없음.


## [2026-08-15] 유료 문서 계측 — '열었나'에서 '읽었나'로

**요청(창업자)**: "지금 메인, 브랜치 다 훑어봐 기능들이 상당히 업데이트되었는데 데이터 트래킹
안되고 있거나 더 새로운 개선 방안 있을지 / 내가 몰라서 말 못한거 까지 잡아내 그리고 바로 실행해"

**셈해 보니 비대칭이 심했다**
서신(4,900원)은 이벤트가 14개인데, **각인(9,900원)은 3개**, 궁합(4,900원)은 3개였다.
더 비싼 물건이 더 안 보였다. 구체적으로 안 보이던 것 셋:

1. **사고** — `readImprint`/`readMatch` 가 null 을 돌려주면 화면엔 오류가 뜨는데 우리한텐 아무것도 안 온다.
   궁합은 더 나쁘다: 입력 검증(`ok`)을 통과하고 `match_run` 까지 찍힌 다음 계산이 죽으면
   **말없이 입력 폼으로 되돌아간다.** 유저 눈엔 버튼이 안 먹는 것이고, 우리 쪽엔 "돌렸는데 결과가 없다"가 안 남는다.
2. **이탈** — 열람 수만 세면 두 줄 보고 닫은 사람과 끝까지 내린 사람이 같은 1건이다.
   9,900원짜리 문서가 값을 하는지에 데이터로 답할 수가 없었다.
3. **재구매** — 궁합의 존재 이유가 "각인은 평생 한 번, 궁합은 사람 수만큼"인데,
   그 논리가 작동하는지 볼 수 있는 유일한 신호(`다른 사람과도 봐볼게` 클릭)를 안 세고 있었다.

**넣은 것**
- `useDocRead(ev, props)` — 문서를 떠날 때 `read_pct`(스크롤 최대 도달률)·`sec`(머문 시간)을
  **열람당 1건만** 보낸다. 스크롤 컨테이너는 문서 자신이 아니라 바깥 `.readwrap` 이라 `closest` 로 올라가 붙인다.
  모바일은 탭을 안 닫고 떠나므로 `visibilitychange`·`pagehide` 에도 같은 flush 를 건다(중복은 `sent` 플래그로 막음).
- `imprint_failed` / `match_failed` — 실패 화면에도 `readRef` 를 붙여서
  "열자마자 깨져서 3초 만에 나갔다"가 남는다.
- `match_again` · `imprint_extra_answered` · `match_notes_toggled`.
- 데일리 리포트: 각인·궁합 줄 + 평균 읽음률, `말_문서실패`·`말_안읽힘` 코멘트.
  안 읽힘은 5건 이상 열렸는데 평균 40% 미만일 때만 뜬다 — 표본이 적을 때 우는 건 소용이 없다.

**별점은 안 붙였다**
서신엔 `letter_rated`(값어치 있었나)가 있는데 각인·궁합엔 없다. 그런데 그건 계측이 아니라
**화면을 새로 세우는 일**이라 여기서 조용히 하지 않았다. 읽기 깊이는 화면을 안 건드리고 얻히는
같은 방향의 신호라 그것부터 넣었다. 별점 UI 는 사장님 판단 대기.

**⚠ 답한 '값'은 보내지 않는다**
각인 문 앞의 선택 문항(혼인 여부·자녀 유무·만난 시기)은 처리방침 1조에 **"기기에만 저장"** 으로
고지돼 있다. 그래서 `imprint_extra_answered` 는 `answered`(답했는가)와 `n`(몇 개 답했는가)만 싣는다.
응답률을 보는 데는 그것으로 충분하고, 값을 실으면 고지 위반이다. 이 경계를 검사로도 못 박았다
(`doc-check`: "답한 '값'은 전송하지 않는다 — 키 자체가 없다").

**sky.js·imprint.js 에는 `track()` 을 넣지 않았다**
계산 라이브러리다. 계측은 화면 경계(App.jsx)에 있는 게 맞고, 순수 함수에 부작용을 넣으면
`sky-check`·`match-check` 같은 노드 단독 검사가 브라우저 전역을 요구하게 된다.

**검사**: doc-check **16/16 신설** · track 24/24 · report-check 103/103 · privacy 38/38 ·
궁합 29/29 · origin 20/20 · 검진 심각 0(주의 5건은 기존).
`e2e/onboard.mjs` 로 온보딩 헬퍼를 분리했고, `report-check` 에 `PW_CHROMIUM` 경로 우회를 붙였다
(번들 playwright 와 설치된 크로미움 버전이 어긋나는 환경에서 안 돌던 것).

---


## [2026-08-15] 질문·답변 본문 복원 — 단, 가명처리를 거쳐서

**요청(창업자)**: "질문과 답변은 다 남기자. 다만 개인 식별 불가능하게 해"

**왜 되돌리는가**
08-14 에 다른 세션이 판결문·근거 본문 전송을 끊고 글자 수만 남겼다. 개인정보 측면에선 안전한 선택이었지만,
그 결과 **어떤 질문에 어떤 답이 나갔고 유저가 그걸 '딱이야'로 봤는지 '빗나감'으로 봤는지를 맞춰 볼 수가 없어졌다.**
판결 품질을 고치려면 그 대조가 있어야 한다. 지금은 "만족도 33%"까지만 알고 왜인지는 영영 모르는 상태였다.

**문을 하나만 둔다 — `anon()`**
자유 서술이 계측으로 나가는 경로를 전부 이 함수 하나로 모았다. 여러 군데서 각자 지우면
한 군데를 빠뜨리는 날이 온다(08-14 의 뒷면 사고가 정확히 그거였다).

지우는 것: 본인 이름(stripName) · 이메일 · 링크 · 전화(`02)555-1234` 같은 괄호 표기 포함) ·
주민번호 · 카드/계좌 · SNS 계정 · 연월일 · 호칭 붙은 제3자 이름.
**지우고 비우지 않고 자리표를 남긴다** — `[메일]`·`[전화]`. 통째로 비우면 나중에
"이 판결이 왜 이상한가"를 읽을 수 없다.

**제일 어려웠던 건 오작동을 막는 쪽이었다**
"○○님"을 지우는 규칙은 만들기 쉬운데, 그러면 **"선생님"이 "○○님"이 되고 "부모님"이 "○○님"이 된다.**
데이터가 통째로 쓰레기가 된다. 그래서 `NOT_NAME` 목록(60여 개)으로 일반명사를 빼고,
검사에 "망가지면 안 되는 말" 여덟 개를 못 박았다(선생님·사장님·부모님·고객님·우리 팀장·어머님·형님·작가님).
그리고 한국어는 이름 뒤에 거의 항상 조사가 붙으므로("민준씨가") 조사까지 허용하도록 고쳤다 —
조사 없이 딱 끊길 때만 잡으면 실제 문장의 대부분을 놓친다.

**정직하게 적은 한계**
규칙으로는 **완전 익명화가 되지 않는다.** "3층 팀장이 어제 회식에서…" 같은 서술은 어떤 정규식으로도 못 잡고,
사람이 읽으면 특정될 수 있다. 그래서 이 산출물은 익명정보가 아니라 **가명정보**(개인정보보호법 §28-2)이고,
처리방침에 **무엇이 안 지워지는지까지 적었다.** 통계 목적 한정·미결합·보관기간이 함께 서야 성립하는 구조다.

**화면 문구도 같이 고쳤다**
로비 하단에 "질문 원문은 통계에 기록하지 않아요"가 있었다. 이 변경으로 **그 문장은 그 순간 거짓이 된다.**
검진이 이걸 심각으로 잡아 줬다(track 에 q 가 실리는지 대조하는 규칙이 있었다).
"질문과 답변은 이름·연락처를 지운 뒤 품질 확인용으로만 기록해요 — 끄고 싶으면 아래에서 끌 수 있어요"로 교체.
**앱이 화면에서 하는 약속과 코드가 하는 일이 어긋나는 건, 아무것도 안 적는 것보다 나쁘다.**

**검사 규칙을 뒤집었다**
privacy-check A-3 은 "본문이 나가면 FAIL"이었다. 지시가 바뀌었으니 검사도 바꿔야 하는데,
**그냥 지우면 안 된다** — 지우면 다음에 누가 원문을 그대로 실어도 아무도 안 운다.
그래서 **금지에서 경유 강제로** 바꿨다: 본문이 나가는 건 되고, `anon()` 을 안 거치는 건 안 된다.
health-check 의 신뢰 라인 규칙도 같은 방향으로 교체(화면이 '지운 뒤 기록'이라 적었는데 지우는 장치가
사라지면 심각으로 운다).

**검사**: privacy-check **76 → 84항목**(anon 누출 11 · 오작동 8 · 자리표 · 상한 · 빈값 · 고지 대조) ·
track 24/24 · doc 16/16 · report-check 103/103 · 궁합 29/29 · origin 20/20 · 검진 심각 0.
실제 질문 12개를 눈으로 대조 — 전남친/팀장님/언니/오빠/강남·판교 같은 평범한 문장은 한 글자도 안 변한다.

---


## [2026-08-16] 새고 있던 넷 봉합 + 곁 탭에 문 하나 (세션지시문 ① A·B)

**배분**: 작업배분 §6-2·§6-4 · 근거: 방향점검 §1-1·§1-9 · 곁탭IA §6

### A-0. tier 비용 누수 — "결제 후 문제"가 아니라 지금 새던 구멍

`api/judge.js` 는 `tier` 를 **요청 본문**에서 읽고, 방어는 Origin 헤더뿐이었다. Origin 은 스크립트가
마음대로 붙인다 — 즉 **결제가 없어도 아무나 `{tier:"paid"}` 로 유료 모델을 쓸 수 있었다.**
예전 주석은 이걸 "결제가 아직 없어서 무해하다"고 적어 뒀는데 그 전제가 틀렸다. 허용목록(TIERS)이
임의 모델 지정은 막지만 **우리가 등록해 둔 비싼 모델은 못 막는다.**

- `PAID_ENABLED = false` — 결제 배선 전까지 서버가 tier 를 안 믿는다. 결제 붙는 날 한 줄로 되돌리고 영수증 검증을 그 옆에 세운다.
- ⚠️ **봉합의 부작용을 하나 같이 막았다.** 로그의 콜 구분이 `tierKey === "paid" ? 3 : …` 이었는데
  tierKey 가 항상 free 가 되면 **서신이 전부 콜2 로 찍힌다** — v105.4 에서 고쳤던 오분류가 되살아난다.
  "무엇을 청했나(tierAsked)"와 "무엇으로 응답했나(tierKey)"를 갈라 `tier_served` 를 같이 남긴다.
- Vercel 환경변수는 이 세션에서 확인 불가(권한 없음). **코드 쪽에서 닫았으므로 env 설정과 무관하게 안전하다.**

### A-1. privacy.html 배너 — 내부 TODO 가 라이브에 떠 있었다

"(2) 변호사 또는 개인정보보호 전문가의 검토를 받고…" 가 **유저 화면 배너**였다. 유저 눈엔
*우리가 아직 안 한 일* 만 보이고, 정작 알아야 할 것(지금 무엇을 받고 무엇을 안 받는가)은 안 보였다.
→ 시험 운영 중이라는 사실 + 지금 방침은 그대로 지킨다 + 문의처 한 줄로 교체.
체크리스트 넷은 **HTML 주석으로 옮겼다** — 없애면 안 되는 목록이지만 배너 자리는 아니다.

### A-2. 궁합 엔진 버그 셋 (역할과초대 §B-0-b)

| | 무엇이 틀렸나 | 어떻게 고쳤나 |
|---|---|---|
| ① | 축⑨ `v` 가 `same ? 1 : 1` — 삼항인데 양쪽이 같아 **늘 긍정표** | `same ? 1 : 0`(마야 축과 같은 눈금). **A/B 실측: 「전부 어렵다」 분기 도달이 40,000쌍 중 0건 → 154건** |
| ② | 납음을 **달력 연도**로 셌다 — 각인은 입춘 보정 사주년 | `a.saju.nayin` 재사용. 두 번 계산하는 대신 한 곳에서 나온 값을 나눠 쓴다 |
| ③ | 축⑥ 이 **합만 보면서** "한쪽이 무겁고 한쪽이 가벼워"라고 단언 | `gap = |neptu차|` 를 실제로 재서, 갈렸을 때만 갈렸다고 말한다 |

①의 연쇄가 제일 컸다 — `plus` 가 항상 ≥1 이라 `split` 이 사실상 `minus` 만 보게 되고,
그래서 「아홉 축이 전부 어렵다」 분기가 **도달 불가능한 죽은 코드**였다. §B 설계의 시금석
("아홉 축이 전부 안 좋은 사람에게 뭐라 할 것인가")이 **코드가 막아서 존재할 수 없었다.**

### A-3. eval 실측이 잡은 진짜 실패 둘

- **direction=undefined 1건(P1-Q19)** — 방향 없는 판결이 그대로 화면에 나갔다. 가드를 넣되
  **HOLD 로 조용히 메우지 않는다**: 앞면 문장은 대개 이미 GO 처럼 쓰여 있어서 표식만 HOLD 로 붙이면
  고쳐진 게 아니라 **앞뒤가 어긋난 카드**가 된다(창업자: "모르겠다 식 hold 면 짜증난다").
  순서는 ①표가 있으면 표가 정한다 ②없으면 한 번 다시 청한다 ③그래도 없으면 실패로 떨군다(다시 청하기 있음).
- **용어·은유 누출 10건(4.0%)** — 샌 말이 **전부 이미 금지 목록에 있던 말**이었다("괘야"·"임오 대운").
  목록을 늘려도 안 고쳐진다는 뜻이다. 그래서 **실제로 새어나간 문장의 (X)→(O) 짝**을 프롬프트에 박았다
  (이 리포의 반복 발견: 모델은 금지 조항보다 예시를 따른다 — 검진 5-g 와 같은 이유).
  요령 한 줄도 같이: **용어를 지우지 말고 그 용어가 뜻하는 일로 바꿔 쓴다.** 지우면 문장이 빈다.
  은유 서술어 금칙을 서신에만 있던 것에서 **verdict·subline·reasons 전부**로 넓혔다.

### A-4. 평가 하네스 — 앱과 다른 프롬프트를 채점하고 있었다

- `:58` TODAY 가 옛 시계 형식(`14시`). 앱은 v132.10~11부터 **시진**(`미시(토)`)을 보낸다.
  시계를 뺀 건 그게 일반 상식("밤이니 자라")을 부르는 재료라서였는데, **하네스는 그 재료를 계속 보내고 있었다.**
- `:131` 동률 기대가 `go >= stop ? "GO"`(경험 편향). **v132.5 에 없어진 규칙**이다 — 지금 앱은 표가
  다수를 못 만들면 모델 방향을 그대로 둔다. 하네스만 옛 규칙에 남아 **동률 3건이 오탐**이었다.
  **오탐은 진짜 실패를 가린다** — 그래서 A-3 보다 이걸 먼저 고쳤다.

### B. 곁 탭 — 막다른 길에 문을 낸다

B-1(명부)·B-3(렌더 연결)은 **v134.2 에서 이미 했다**(`binari.gyeot.v1` + 순위 방어 셋 + `gyeotSeat`).
지시서가 그 머지 전에 쓰여 항목이 남아 있었다. 이번에 남은 건 B-2 하나:

- 곁이 0일 때 **나갈 문이 없었다.** "누가 서게 되면 이 자리에…"만 있고 *어떻게* 서게 하는지가 없다 —
  보이는 건 있고 할 수 있는 건 0인 화면이다. 그게 이 탭의 제일 큰 구멍이었다.
- ⚠️ 곁탭IA §5 「첫 화면을 결제벽으로 만들지 마라」와 부딪히지 않게 **값을 안 쓰고 강조 버튼도 안 쓴다** —
  상품 진열이 아니라 **자리를 채우는 유일한 경로의 안내**다. 검사로도 못 박았다(값·원 표기 0).
- B-4(초대·회신 승격)는 **안 건드렸다** — 분모 100 이후다.

### 검사

- 신설/확장: `match-check` 39→**45**(버그 셋 각각) · `gyeot-check` 21→**28**(B-2 문·계측·'답 대기') · 검진 **5-o** 신설
- 검진 5-o 는 넷 전부에 **오염을 주입해 실제로 붉게 뜨는지 확인**했다. 한 번은 자기 오탐이 났다 —
  깨진 꼴(`same ? 1 : 1`)을 찾게 했더니 **바로 위 주석이 그 문자열을 인용해서** 걸렸다.
  → 규칙은 "없어야 할 것"보다 **"있어야 할 것"** 으로 쓴다(주석에 안 걸린다).
- `gyeot-check` 의 로비 인사말 단언이 **시각 의존**이라 밤에 돌리면 실패했다(선재 결함). 둘 중 하나면 통과로 고침.
- 전량: 검진 심각 0 · smoke 35/35 · verdict 35/35 · doc-check 21/21 · 곁탭 28/28 · 궁합 45/45 ·
  각인 130/130 · 리포트 121/121 · 개인정보 111/111 · 역할 26/26 · 곁명부 30/30 · 곁예산 13/13 ·
  공유서명 16/16 · 공개표면 33/33 · 질감 9/9

### 남긴 것

- **BINARI_MODEL_PAID 의 Vercel 실제 설정값은 확인 못 했다**(세션 권한 없음). 코드에서 닫았으므로
  설정과 무관하게 안전하지만, 창업자 확인이 필요하면 그건 사람 쪽 항목이다.
- A-3 의 두 수정은 **프롬프트 변경**이라 실효는 다음 eval 실행에서만 확인된다. 이 세션엔 API 키가 없다
  (§6-1 데이터 레인 3번과 같은 제약). A-4 를 먼저 고쳐 뒀으므로 **다음 실행은 오탐 4건 없이 시작한다.**

---

## [2026-08-17] 데이터 레인 5건 — 계측 오염 마감·리텐션 자산·대시보드 산식

**요청(창업자)**: 방향점검 §1-6 / 작업배분 §6-1 데이터 레인 1~5번.

### 1. 손 실측 이중 필터 강제 (완료)

`daily-report.py` 가 `is_internal != true` **하나만** 걸고 있었다 — 8/15 전략 문서를 뒤집은
바로 그 오류다. `EXT` 상수 하나에 코호트 436757 제외를 더하고, 내부 쪽 줄은 그 여집합(`INT`)으로
맞췄다. 플래그만 보면 코호트 내부인이 어느 쪽에도 안 세어져 **합이 안 맞는다.**

`person_id NOT IN COHORT` 가 `countIf()` **안에서도** 컴파일되는지 실제로 확인하고 넣었다
(집계 조건에서 코호트가 안 먹으면 리포트가 조용히 빈 값을 낸다).

이중 필터 45일 실측: **판결 69회/13명 · 공유 발신 0 · 카드 저장 0 · 가격노출 26→확인 25(96%)→발행 24.**
문서의 68/12 와 하루치만 다르다 — 정의가 같다는 뜻이다.

### 2. 리텐션 자산 3종 계측 부착 + D7 창 정의 (완료)

`days_since_first` 를 **모든 이벤트의 공통 속성**으로 올렸다. 재방문을 세도 그게 D1인지 D7인지
D30인지 구분할 방법이 없던 게 근본 결손이었다. 이벤트를 새로 만드는 것보다 싸고,
어떤 지표든 D별로 쪼갤 수 있게 된다.

붙인 이벤트 7종: `streak_day`·`streak_broken` / `daily_offered`·`daily_opened`·`daily_received` /
`log_opened`·`log_row_opened`.

설계에서 신경 쓴 셋:
- **`daily_offered` 가 분모다.** 없으면 "안 열었다"가 *안 궁금했다*인지 *안 떴다*인지 영영 못 가른다.
  되물음이 문안 자리를 뺏는 구조라 `blocked_by_askback` 도 같이 싣는다 — 안 밝히면 노출 수가 서로를 오염시킨다.
- **끊김을 따로 남긴다**(`streak_broken`). 이어짐만 세면 "몇 명이 습관이 됐나"는 알아도
  **"며칠에서 끊기나"** 를 못 읽는다. 붙잡을 지점을 정하려면 끊긴 자리가 필요하다.
- **StrictMode 이중 발사 함정** — 스트릭은 `setState` 업데이터 안에서 계산되는데 React 는 개발 빌드에서
  업데이터를 두 번 부른다. 그대로 track 하면 이벤트가 두 배가 된다. 날짜를 열쇠로 한 ref 로 하루 1회를 잠갔다.

**D7 정의는 새로 정하지 않고 실물에서 확인해 적었다** — 게이트 인사이트 `xscpleu9` 가 이미
`RetentionQuery · period=Day · targetEntity=verdict_shown · retention_first_time · Day 7 ≥ 20%` 다.
즉 **"7일 안에 한 번"이 아니라 "7일째 되는 날에 활동"**. 둘은 크게 다르고, 롤링 쪽을 D7 이라 부르면
**게이트를 몰래 낮추는 것**이 된다(CLAUDE.md §관문). 보조로 볼 수는 있으나 반드시 다른 이름으로 부른다.
정본은 `app/HANDOVER-데이터계측-2026-07-25.md` §2-D.

### 4. 루프 지표 재산출 · q_anon 분포 (완료)

`offer_shown` 이라는 이벤트는 **없다** — 실제 이름은 `imprint_offer_shown`/`match_offer_shown` 이고
첫 발생은 2026-08-15T01:06:23Z 다. 그 창으로 좁힌 이중 필터 외부 실측:

| | 노출 | 클릭 | 비율 |
|---|---|---|---|
| 각인 | 7 | 2 | 28.6% |
| 궁합 | 7 | 6 | 85.7% (→ 실행 4) |

⚠ **노출 대상이 3명이다.** 비율을 신호로 읽으면 안 된다. 둘의 분모가 같은 건 두 제안이
같은 자리에 함께 뜨기 때문이고, 그래서 **상대 비교만** 의미가 있다(궁합 훅이 각인 훅보다 세다 —
디자인 레인의 "각인 카드 훅이 구조적으로 약하다" 판정과 방향이 같다).

`q_anon` 은 질문 11건 **전량에 부착**되어 있고, 잔존 식별자 검사(메일·전화·연도·URL·자리표) **전부 0건**.
가명처리가 운영에서 실제로 서 있다. 다만 평균 26자·최대 37자라 **분포를 분석할 표본이 아직 아니다** —
분모 100 이후로 미룬다.

### 5. 게이트 대시보드 산식 갱신 (완료)

`e67ieZvV` 를 `letter_clicked/verdict_shown` → `letter_intent_confirmed/letter_price_shown` 으로 교체.

**다만 지시를 그대로만 따르면 계기판이 거짓말을 한다.** 새 산식은 96%가 나오는데,
같은 방향점검 §1-3 이 "확인 버튼이 지불 관문이 아니라 **무료 콘텐츠 관문**"이라고 이미 판정했다.
그래서 이름을 "지불 의사 신호"에서 **"2단 fake-door = 확인/가격노출"** 로 바꾸고,
설명에 "결제가 붙기 전까지 죽음신호 판정에 쓰지 말 것"을 박았다.

HANDOVER §2 "이벤트 19종" → **실측 54종**(60일 실수신) 표로 교체하고,
"정본은 문서가 아니라 PostHog"이라고 적으며 재산출 질의를 함께 남겼다.
동의 2단계 서술도 사실에 맞췄다(PROFILE_KEYS 는 2026-07-28 이후 빈 집합, MBTI 는 제품에서 제거됨).

### 못 닫은 것 — 창업자 손이 필요하다

**팀 5명 ?i=1 은 여전히 0명이다.** 외부로 잡히는 13명 중 `?i=` 를 거친 사람이 한 명도 없다.
그리고 **`?i=1` 은 소급되지 않는다** — 지금 눌러도 과거 45일은 안 고쳐진다.
과거를 고치는 유일한 길은 코호트 436757 편입이고, 그건 사람을 지목하는 일이라 내가 임의로 하지 않았다.

시금석(`tab_switched`)은 **64회/3명, 그중 1명이 59회**로 커졌다(문서 기록은 62회/2명).
곁 탭이 만들어진 8/15~16 이틀에 집중됐다. 다만 **URL 로는 증명 실패** — 전원 운영 도메인이고
미리보기·`?trackdebug`·`?gyeot` 접속이 0이다. 정황뿐이라 임의 편입은 하지 않았다.

**검사**: retention-check **25항목 신설** · track 24/24 · doc 21/21 · privacy 111/111 ·
report 121/121 · verdict 35/35 · 궁합 39/39 · origin 20/20 · 검진 심각 0.

---

---

## [2026-08-17] 결정 9건 최적안 시행 — 무료 발행 표식·처리방침 문구·이력정리 준비

**요청(창업자)**: "최적안으로 우선 다 진행해" — 결정필요사항 9건 전부 추천안으로 확정하고 실행.

### 1. 결정 8 — 무료 발행 코호트 표식 (완료)

**아홉 중 유일하게 소급이 안 되는 항목이라 먼저 쳤다.** 지금 서신·각인·궁합은 값을 표시하고도
실물을 무료로 준다. 결제가 붙는 날 초기 표본 전원이 "공짜로 받아 본 사람"이 되고,
그 코호트로는 **처음 사는 사람의 지불 의사를 못 잰다.** 지금 표식을 안 남기면 나중에 못 가른다.

- `PRICING_MODE = "free_trial"` 을 `_superProps` 에 깔았다 — **모든 이벤트에 붙는다.**
  이벤트를 새로 만드는 것보다 싸고 어떤 지표든 체제별로 쪼갤 수 있다(§D3 belief 와 같은 패턴).
  결제 붙는 날 `"paid"` 로 한 글자만 바꾼다.
- `markFreeIssue(kind)` — 한 번이라도 무료로 받은 사람에게 `free_issued` 를 **person 속성**으로 박는다.
  `register()` 는 기기 단위라 기기를 바꾸면 끊긴다 → `$set` 으로 person 에.
  첫 수령 시점·종류는 `$set_once` 라 나중 수령이 덮지 않는다.
- **서신만 걸면 안 된다** — 각인·궁합도 값을 표시하고 무료로 준다. 셋 다 걸었다.
- ⚠ TDZ 주의 — `PRICING_MODE` 사용처(`_superProps` 조립)가 정의부보다 위라, 상수를 파일 앞
  `let _superProps` 옆으로 올렸다. 함수 안이라 실제로는 안 터지지만 순서에 기대는 코드를 남기지 않는다.

**검사 `app/e2e/pricing-check.mjs` 12/12 신설** — 소스 성질 8 + 실주행 4.
값이 아니라 **성질**을 본다(상수 이름이 바뀌어도 살아 있게): 고정 속성인가 · person 속성인가 ·
`$set_once` 인가 · 세 상품 전부 걸렸나 · 문이 하나인가 · 사람당 1회인가.
실주행에서는 **아직 아무것도 안 받았으면 표식이 안 나가는 것**까지 본다(오탐 방지).

### 2. 결정 9 — 처리방침 「(초안)」 제거 (완료)

`<h1>개인정보처리방침 (초안)</h1>` → `(초안)` 삭제. **이건 고지 문서지 초고가 아니고,
스스로 초안이라 부르면 고지로서의 지위가 약해진다.** 시험 운영 단계라는 사실은 바로 아래
안내문이 이미 밝히고 있어 정보량 손실이 없다.
*(내부 체크리스트 노출은 8/16 A-1에서 이미 HTML 주석으로 내려가 있었다 — 다시 배너로 올리지 말 것.)*
변호사 검토 자체는 결정 9(b)대로 **판매 개시 직전.**

### 3. 결정 7 — 이력 정리 준비 (실행 대기)

`tools/history-redact.py` 신설. 방식은 **값 치환이 아니라 파일 통째 교체** —
두 검증 스크립트의 모든 과거 판본을 지금의 가상 명식 판본으로 바꾼다.
값을 열거해 치환하면 ①그 스크립트 안에 PII 를 옮겨 적게 되고 ②같은 숫자를 쓰는 다른 파일까지 건드린다.

**AI 가 못 하는 것 둘이 남았다**: ①저장소 비공개 전환(GitHub 설정 화면, 도구 없음 — **이게 먼저다**)
②강제 푸시(다른 세션이 붙어 있는 동안 하면 그 커밋을 날린다 — 사람이 시점을 골라야 한다).
그리고 푸시 뒤에도 **GitHub 지원팀 캐시 삭제 요청**이 남는다 — 고아 커밋은 즉시 안 지워진다.

### 검사

빌드 OK · 개인정보 111/111 · 판결 35/35 · smoke 35/35 · track 24/24 · retention 25/25 ·
**pricing 12/12(신설)** · 검진 심각 0.

## [2026-08-18] 레인별 미완 마감 — 앞면 용어 가드는 코드로 옮겼다 (0-f · 데이터 4·5)

**요청**(창업자): "세션별로 미완된 것들 다 완료시켜."

### 0-f. 프롬프트로 두 번 실패한 자리

v134.4 가 A-3 으로 앞면 용어 누출을 **프롬프트 보강**으로 막으려 했다. 키 보유 세션이 60칸을 재실행해 대조한 결과 **3→4 건으로 늘었고**, P7-Q17 은 **같은 칸에서 같은 단어(「임오 대운」)가 재발**했다. 원인 가설은 뒷면(콜2) 지시문이 근거를 「용어 — 쉬운 풀이」로 쓰라며 예시로 "무오 대운"을 드는 것 — 앞면 금지와 옆자리 권장이 부딪힌다.

→ **세 번째로 조이지 않고 코드로 옮겼다.**

- `FRONT_JARGON` — 하네스 `JARGON` 과 **같은 목록**. 갈리면 한쪽만 잡는다.
- `verdict_jargon` 가드 — 앞면에 용어가 잡히면 **문장만** 다시 받는다.
  ⚠ **판정은 다시 하지 않는다.** 콜1 을 새로 부르면 `direction` 이 바뀔 수 있다 —
  용어 하나 고치자고 판결을 갈아치우는 셈이 된다. 그래서 rewrite 응답에서 `verdict` 만 취하고,
  비었거나 여전히 용어가 있거나 45자를 넘으면 **원문을 그대로 둔다**(용어 섞인 판결은 아쉽지만,
  뜻이 어긋난 판결은 제품 실패다). 실패 사유는 `why` 로 계측에 남긴다.
- 검진 **A-5** — ①가드 생존 ②앱·하네스 목록 일치.
  목록 문자열을 못 박지 않고 **두 파일에서 정규식을 뽑아 맞댄다.** 목록을 늘려도 검사는 살아 있고,
  원래 잡으려던 "둘이 갈렸다"는 그대로 잡는다(CLAUDE.md 가상명식 규칙과 같은 이유).
  ⚠ 같은 파일 882행에 다른 블록의 A-5 가 이미 있다 — 이 블록은 A-0~A-4 를 잇는 연번이다.
- **음성 테스트로 검증**: 목록을 갈라놓으면 "앱·하네스 목록이 갈림", 가드를 지우면 "가드가 없음" —
  둘 다 실제로 걸렸다. 통과만 보고 넘어가면 거짓 통과를 못 거른다(v130 `grab()` 사고와 같은 이유).

**실효는 다음 eval 실행에서만 보인다** — 이번 60칸은 가드 도입 전 산출이다.

### 데이터 레인 4·5

- v128 이후 창 실측(이중 필터): 궁합 노출 14/6명 → 클릭 13**/3명**(회수는 높은데 사람은 절반 — 소수 반복),
  각인 14/6 → 8/4, 서신 가격노출 3 → 확인 2, **card_saved 외부 첫 1건**, 공유 발신 **여전히 0**.
- **q_anon 분포(결정 3 이행)** — 외부 `question_asked` 116건 중 `q_anon` 부착은 13건뿐인데,
  나머지 103건은 **가명처리 도입(8/15) 이전**이라 원래 없다(버그 아님). 붙은 13건은 결측 0.
  분포는 「오늘 뭐먹지」 4 · 일·진로 2 · 연애·결혼 2 · … **n=13 이라 판독 불가로 판정**했다.
  최다가 「오늘 뭐먹지」인 건 CLAUDE.md 가 열어둔 물음과 같은 방향이지만 **13건으로 답하지 않는다.**
  분모 100 후 다시 돌릴 질의를 HANDOVER 에 박아뒀다.
- 대시보드 `e67ieZvV` 는 데이터 세션이 8/17 에 이미 2단 정의로 교체 — 확인만 했다.
  HANDOVER 요약표의 「19종」은 인벤토리 절만 고쳐지고 남아 있어 **54종**으로 정정.

### 안 고친 것

**건강인데HOLD(P7-Q23) — 분산으로 판정.** 같은 칸이 앞 실행에선 GO, 이번엔 HOLD 였다.
n=1 이고 실행 간에 흔들린 것이라 규칙 문제로 볼 근거가 없다. 다음 전량 실행에서 재발하면 그때 본다.
