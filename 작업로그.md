# 작업로그

세션에서 오간 **요청 · 판단 근거 · 작업 결과**를 시간순으로 누적한다. `변경이력.md`가 "무엇을 바꿨나"의 한 줄 대장이라면, 이 문서는 "왜 그렇게 판단했나"의 서술 기록이다. 최신 항목이 아래로 쌓인다.

---

## 2026-07-10 · 세션 1 — 백업 저장소 구축 ~ v16 개발 ~ 이펙트 방향

### [1] 저장소 초기화 (v15)
- **요청**: BINARI를 GitHub `rlflrrlflr/BINARI`에 올려라. 폴더 5개(01~04, 09), 파일명 `비나리_{영역}_{산출물}_vNN`(버전 맨 뒤), 루트에 README·변경이력, 커밋 "init: BINARI 백업 v15".
- **판단**: 레포가 빈 상태(`app` 1바이트)였고 Google Drive 전문검색에도 '비나리/BINARI' 없음 → 임의 파일을 지어내지 않고 **규칙에 맞는 구조(스캐폴드)부터** 세우는 게 유일하게 안전한 진행.
- **결과**: 폴더 5개 + 각 README + 루트 README/변경이력 생성, 빈 `app` 삭제, `init: BINARI 백업 v15` 커밋·push.

### [2] 실제 산출물 편입
- **요청**: MVP 코드(`v15_binarimvp.jsx`) + 개발 인계서 제공.
- **판단**: 업로드 파일명이 버전을 앞에 뒀음(규칙 위반) → `비나리_비주얼프로토타입_MVP_v15.jsx`로 리네임해 03에 배치. 인계서는 기획 문서이므로 `비나리_기획전략_개발인계서_v15.md`로 01에.
- **결과**: 두 산출물 배치, README에 제품 개요·산출물 현황표 보강, `init: BINARI 백업 v15` 커밋에 amend(단일 init 유지).

### [3] PM 전략분석
- **요청**: 냉철한 시장분석가·베테랑 PM 관점. 뻔한 AI 조언 배제. ①페르소나 하루/최종목표 ②현실 마찰/기능점검 ③실무 기능 로드맵 ④단계별 디벨롭.
- **판단**: 추측이 아니라 **코드 사실**에 정박해야 함 → ultracode 워크플로로 8에이전트 교차검증: 코드 898줄 라인단위 실사 1 + 한국 운세시장/행동심리 웹리서치 2 + 4렌즈 분석 4 + 적대적 비평 1. 비평 단계에서 발명된 KPI 수치·푸시 없는 알림 서사·아티팩트 localStorage 오판 등 9건을 기각.
- **결과**: 4단계 분석 완성 → `01_기획전략/비나리_기획전략_PM전략분석_v15.md`로 문서화(이 세션에서 소급 저장). 핵심 결론: **"4개의 배관 공사(저장·에러복구·내보내기·API프록시) 전에는 리텐션·바이럴·수익화 전부 공중누각."**

### [4] 개발 루프 (v16, 반복 1~8)
- **요청**: 계획대로 개발, loop 엔지니어링.
- **판단**: 코드 사실이 정한 순서 — 0단계(아티팩트 탈출: 배포가능화+데드엔드 수리)를 리텐션(1단계 B1~B7)보다 먼저. 매 반복은 **transform 스크립트로 "정확히 1회 매칭" 검증 후 치환** → 잘못된 위치 수정 원천 차단. 매 반복 `vite build` 통과를 게이트로.
  - 왜 계정 대신 localStorage(B1): 가입 마찰만 얹고 지금 단계 추가가치 0. 멀티기기·결제 필요해지는 2단계 이슈.
  - 왜 Supabase 0단계 배제: 저장할 서버 데이터가 아직 없음. 비개발자에게 두 번째 학습곡선.
  - 왜 속결 모드 역전(B5): 3초짜리 결정에 6단계 동전 의식은 데일리 사용을 죽임. 프롬프트의 "유저가 청한 경우만 주역"과 UI를 일치.
- **결과**: 반복 1(0단계)→2(B1 기억)→3(B2 데일리)→4(B4 부적내보내기)→5(B3 되물음)→6(B5 속결)→7(B6판결록+B7스트릭)→8(런타임검증). 반복 8에서 Playwright 모바일 뷰포트 스모크 **18/18 통과**(온보딩·속결/의식 실패복구·재회·데일리). v16 스냅샷 03 승격, v15는 09로.

### [5] 최종 전달
- **요청**: 완료되면 최종 파일 제공.
- **결과**: `비나리_웹앱_v16.zip`(app 전체+문서, node_modules 제외) + 단일파일 v16 스냅샷 + 검증 스크린샷 2장 전달.

### [6] 이펙트 방향 결정 (→ v17 A단계)
- **요청**: 레퍼런스 영상 3개 첨부. 지금 버전 불만족 이유/개선법/중독 안 되는 이유/이펙트가 적용 안 되는 이유/보는 사람 심리.
- **판단**: 영상 프레임 실사 → 3개 모두 **GPU 파티클(수십만~수백만) + 유속장(flow field) + 잔상 누적 + 블룸**. 현재 GuardianCanvas는 Canvas2D 1,150~1,600 입자가 **제자리 사인파 진동** → "살아있음"의 핵심인 흐름·인과가 없음. 노력 문제가 아니라 Canvas2D+단일파일 선택의 물리적 천장.
- **결과**: `04_리서치/비나리_리서치_이펙트레퍼런스분석_v16.md`로 문서화. **A단계**(Canvas2D에서 잔상+유속장+가짜블룸+증량, 저리스크) vs **B단계**(WebGL 전환) 제시 → 사용자 **A단계 선택**. (작업 착수 직전 본 로그 정리 요청으로 일시 중단)

---

### [7] v17 A단계 — 수호신 유속장 리라이트
- **요청**: A단계로 진행(사용자 선택). + 진행 중 "작업 로그(판단근거·요청·결과) 쌓고 있냐" 지적 → 본 로그와 PM전략분석·이펙트분석 문서를 소급 저장(위 [3][6] 반영).
- **판단**: 이펙트 분석의 격차 3개(제자리 진동/전삭제/저밀도)를 Canvas2D 한계 안에서 직격. ①잔상은 배경색 fillRect(=stage 그라디언트 가림) 대신 **destination-out으로 알파만 감소**(투명 캔버스 유지 표준기법) ②각 입자에 vx/vy 속도 부여 후 유사 컬노이즈로 밀되 오행 형태(place)로 **스프링 복원** → 흐름과 형상 양립 ③입자 증량(3,200~4,200)+arc→fillRect 배칭으로 성능 상쇄. 어셈블 강도(1-k)로 초기엔 흐름 강·안정 후 형태로 수렴.
- **결과**: 렌더 확인 — 소용돌이 유속장+중앙 코어 발광+잔상 궤적(레퍼런스 영상1 근접). 헤드리스 52fps(소프트웨어 렌더, 실기기 GPU선 더 여유), 스모크 18/18 회귀 없음. **성능 선제최적화는 보류**(실기기 데이터 없이 입자 감량은 로드맵의 '데이터 없는 최적화 금지' 원칙 위반) → 저사양 FPS 확인을 로드맵에 남김.

### [8] 실사용자 피드백 2건 + v18 방향 확정
- **요청/입력**: ①사주 헤비유저 테스트 — "엄청 빠지더라"(코어 첫 실증) ②비신자 — "안 물어본 정보까지 말해주는 게 꺼려진다, 물어본 수준 답변은 좋다". + 사용자 스크린샷으로 v17 아티팩트 실행 확인(판결·저장이 아티팩트에서 죽는 문제 노출), 아키텍처·프로세스 불만 분석 요청.
- **판단**: (a) v16의 "아티팩트 탈출"이 사용자의 유일한 실행 환경을 부숨 — 듀얼 모드(아티팩트 폴백)로 복구가 최우선 (b) 헤비유저층은 만세력을 검산하는 층 — 절기 근사 ±1일이 신뢰 붕괴 지뢰 (c) 비신자 피드백은 '모를 권리' 원칙으로 일반화 — push 요소(토정비결 자동 노출·아침 문안 자동 펼침·프롬프트 범위 무제한)를 pull로 전환 (d) 이펙트 A단계의 백색 포화·폭발 문법·텍스트 겹침은 응급수술 대상.
- **결과**: `04_리서치/비나리_리서치_유저피드백_v01.md` 신설. **v18 스코프 확정**: ①듀얼 모드 API+저장 폴백 ②이펙트 수술(코어 포화·containment·텍스트 분리) ③모를 권리 반영(프롬프트 규칙·토정비결 접기·아침 문안 노크형) ④만세력 정밀화. 병행: 사용자 트랙 — Vercel 배포(~30분)·테스터 5~10명 모집.

### [9] v18 루프 (반복 10~12) — 테스트 루프 복구·이펙트 수술·만세력 정밀화
- **요청**: "A로 우선 진행" 후 유저 스크린샷 5장(클로드 앱에서 v17 판결 실패) 공유하며 "진행".
- **판단**: (반복 10) 아티팩트 호환은 런타임 폴백이 정답 — 도메인 감지는 취약하므로 /api/judge 콘텐츠타입/404로 실패 감지 후 직접 호출 전환, 첫 성공 경로 캐시. (반복 11) 백색 포화의 본질은 가산량/페이드율 평형>1 — 밝기가 아니라 평형점을 조정. 금 폭발은 "바깥 컨베이어+잔상=폭발 문법"이 원인 — 팔 따라 상시 관통+회전으로 교체, 팔 적을수록 폭 확대. 과교정(패널 불투명 → 수호신 가림) 1회 발생·스크린샷 검수로 잡음 — **렌더 검수 없는 시각 변경은 금지**를 교훈으로. (반복 12) 절기 근사표는 헤비유저 검산에서 무조건 발각 — Meeus 저정밀 태양황경(±수분)+NOAA 균시차로 교체, 도시 경도 매핑. 검증은 널리 알려진 기준값(병인일·무오일·입춘 당일 시각 분기·균시차 극값·춘분 0°)으로.
- **결과**: 반복 10(듀얼모드+저장 셈+모를 권리, 스모크 21/21) → 반복 11(이펙트 수술, 21/21) → 반복 12(만세력 10/10, 빌드+스모크 21/21). v18 스냅샷 03 승격. 플랫폼 진단: 클로드 앱 "Invalid response format"은 아티팩트 프록시 한계(v15 인계서 기록과 일치) — **폰 테스트는 Vercel 배포가 유일한 답**.

### [10] 모바일 실측 — v18 아티팩트 3중 문제
- **입력**: 유저가 v18을 모바일 클로드 앱 아티팩트에서 구동한 스크린샷 2장. ①질문칸 안 보임 ②좌우 여백 ③판결 "Invalid response format".
- **판단/진단**: ①②는 내 CSS 문제(qbox 밑줄+검정 placeholder가 파티클에 묻힘 / scene max-width 400 여백) → v19에서 박스화·풀폭 수정. ③의 "Invalid response format"은 **우리 코드 문자열이 아님**(우리 에러는 전부 한글) = 클로드 앱 아티팩트 런타임이 우리 API 호출(정식 Messages 형식 fetch)을 거부하고 던진 것. v15 인계서의 "모바일 API 실패, PC 크롬 확인"과 일치 — **모바일 아티팩트는 이 앱의 API를 못 돌린다(플랫폼 차이)**. 앞서 "v18이면 모바일 아티팩트에서 될 것"이라 한 내 추론은 오판(정정). window.claude.complete 방식 폴백은 2콜·캐싱 설계를 훼손하는 오버엔지니어링이라 배제. **판결의 모바일 해결은 배포가 유일·확실**(배포 웹앱은 아티팩트 런타임을 안 거치고 /api/judge를 씀).
- **결과**: v19 CSS 수정(스모크 21/21). 판결 문제는 코드로 못 고침 — 배포 필요를 명확화.

### [11] 아티팩트 판결 복구 (v19) — 내가 버렸던 내장 API 되살림
- **요청**: "이전엔 아티팩트에서 API 물려서 됐다. 다시 그렇게 해줘. 배포 전제하지 말고."
- **판단**: 사용자가 옳다. 아티팩트에는 **window.claude.complete(문자열 in/out)** 내장 API가 있고, 이게 "물려있던" 정체. v16에서 내가 이걸 버리고 /api/judge로 바꾼 게 근본 원인. 앞서 "window.claude.complete는 2콜·캐싱 훼손이라 안 함"이라던 내 판단을 뒤집음 — 초기 단계의 최우선 가치는 '지금 폰에서 되는 것'이고, 캐싱은 그 다음. 판결 경로를 3-way로: complete(있으면 최우선)→server→direct. system+messages를 한 프롬프트로 병합, 콜1/콜2는 프롬프트 텍스트로 구분. **검증을 사용자에게 떠넘기지 않기 위해** window.claude.complete를 mock 주입해 헤드리스에서 콜1·콜2 판결 렌더까지 확인(4/4). (첫 시도에서 캡처 타이밍이 카드 애니메이션보다 일러 오탐 → 대기 로직으로 수정.)
- **결과**: `app/e2e/verdict.mjs` 신설(판결 회귀 4/4), 스모크 21/21 유지. v19 스냅샷 승격. 배포 환경에선 complete 부재→자동으로 /api/judge. **아티팩트·배포 양쪽 다 지원.** 단 아티팩트 런타임에 window.claude.complete가 실제 존재하는지는 사용자 실측으로 최종 확인 필요(mock은 우리 코드가 올바르게 호출·파싱함만 증명).

### [12] v20 QC — 폭포수 + 진단 도구 (유저: "도대체 왜 되던 게 안 되냐, QC 완벽하게")
- **타임라인 규명**: ~v14 window.claude.complete(모바일 OK) → v15 캐싱 위해 직접 fetch로 교체(이때 모바일 첫 파손 — v15 인계서에 "Invalid response format" 기록 있음) → v16~17 /api/judge(전부 파손, 내 회귀) → v18~19 복구 시도. v19에서 complete를 되살렸는데도 모바일이 깨진다 = 앱의 completion 브리지 자체가 현재 오류를 내거나 우리 호출 형태를 거부.
- **판단**: 클로드 앱 내부는 헤드리스로 재현 불가능한 블랙박스 — 추측 반복 대신 ①폭포수(한 경로의 어떤 실패도 다음 경로로 이월, 성공 경로만 기억) ②격리 진단 도구(폰에서 complete/fetch/storage를 개별 검사해 화면에 원문 표시) ③모바일 고장 재현 테스트(complete가 "Invalid response format"을 던지는 환경을 mock으로 재현 → 폭포수로 판결 성사 + 화면 무에러 확인).
- **결과**: QC 배터리 37/37. 남은 유일한 미지수는 폰 런타임의 실제 지원 범위 — 진단 아티팩트 실측(사용자)으로 확정하는 구조로 전환. 교훈: "환경이 다른 버그는 코드 수정 반복이 아니라 격리 진단부터".

### [13] 진단 v01 실측 결과 — 플랫폼 능력표 확정 (iPhone·클로드 앱)
- **실측**(사용자 스크린샷 4장): ①window.claude 존재(complete·sendConversationMessage·openExternal·downloadFile), UA=WKWebView(Safari 토큰 없음) ②complete("안녕") → 앱 전체 흰 화면 "Error in Claude completion"+아티팩트 리셋(로그 소실로 확인) — **짧은 프롬프트로도 죽음 = 크기 문제 아님, 브리지 고장** ③직접 fetch → "Invalid response format" 거부(단, 아티팩트는 생존) ④localStorage ✅.
- **확정 결론**: 클로드 iOS 앱 웹뷰 안에는 AI 호출 길이 없음(코드로 불가). 저장은 됨. v20의 complete 우선 호출은 앱에서 아티팩트를 죽이는 지뢰였음.
- **조치(v21)**: UA로 앱 웹뷰 감지(iOS && Safari 토큰 부재 / Android && wv) → complete 봉인, 전 경로 차단 시 정직한 안내("사파리에서 claude.ai를 열거나 PC에서"). 판결 테스트에 S3(앱 웹뷰 재현: complete 미호출·안내 표시·재시도 생존) 추가 — 9/9.
- **다음 실측**: 같은 진단을 **폰 사파리(claude.ai)**에서 — ② 성공 시 폰 테스트는 사파리로 해결, 실패 시 폰은 배포가 유일(이젠 데이터 근거).

### [14] 진단 2차(모바일 크롬) — 근본 원인 최종 확정: Anthropic 서버측 고장
- **실측**(iPhone·크롬 CriOS/150, iOS 26): ①window.claude ✅ ②complete 호출 왕복 성공하나 **빈 문자열 반환** + 토스트 "**Internal server error while processing action**" ③fetch 동일 서버 오류 ④localStorage ✅.
- **최종 결론**: 클로드 앱의 "Invalid response format"과 모바일 크롬의 "Internal server error"는 **같은 뿌리** — Anthropic의 모바일 아티팩트 completion 백엔드가 500을 반환 중. 앱은 그 500을 파싱 못해 아티팩트를 죽이고, 크롬은 토스트+빈 문자열로 표면화. **유저 폰 무죄, 코드 무죄, "업데이트 안 했는데 왜 깨졌나"의 답 = 서버가 바뀌었다/고장났다.**
- **함의**: 폰에서 아티팩트 판결은 Anthropic이 복구할 때까지 불가(모든 경로 실측 완료). v21 폭포수는 빈 응답을 실패로 처리하고 우아하게 안내하므로 **서버가 복구되는 순간 코드 변경 없이 폰에서 자동으로 살아남**. 즉시 해결책은 배포뿐(이제 가정이 아니라 실측 근거).

### [15] v22 착수 — UX 비평 반영 + 지표 포트폴리오 설계
- **요청**: "컴터 쓸 수 있기 전까지 그거라도 하자"(UX 4종 승인) + "로직도 — 동서양 기운의 종류가 충분한가, 토속 말고 신뢰도 있되 덜 알려진 것, 기존 데이터만으로, 가치여정 단계·숫자도 판단해봐".
- **판단**: ①지표 감사 — 실제 구성이 한국+그리스뿐이라 "온 세상" 스토리라인 미충족. 추가 4종 확정: 달 별자리·나크샤트라(달 황경 공유, 인도 축)·납음오행(사주 심화, 시적 이름)·마야 촐킨(제3문명). 축은 '마야' 1개만 신설(판결문 보존 — "지금 판결문 좋아" 피드백). 상승궁·자미두수는 Phase 2. ②가치여정 — 방법론(가치 카드 소트)은 ACT 실사용 도구라 신뢰 앵커로 유지, 숫자만 다이어트: 24→16, 8→4→1→6→3→1(13→10탭). 상세는 `비나리_기획전략_지표포트폴리오설계_v01.md`.
- **결과(반복1)**: 장면 분리(gpanel을 캔버스 오버레이→하단 흐름), 발화 1개 원칙(형상 설명 삭제), 동전 0.75s 공중 연출, 금 강철빛. 30/30 테스트+렌더 검수. 다음: 반복2 지표 4종 → 반복3 가치여정 개편.

### [16] v22 반복2·3 — 지표 4종 구현 + 가치여정 개편 (44/44)
- **반복2**: 달 황경(Meeus 10항) 구현 — 검증을 월식·신월 정렬로(식은 태양-달 정렬의 절대 기준, 오차 0.04°/0.02°). 달 별자리=회귀황도, 나크샤트라=항성황도(라히리 아야남샤 근사) — 아는 사람이 검산해도 관례와 일치하도록 좌표계를 갈라 적용. 납음 30종 테이블(수호신 소개 문구를 납음의 시적 이름으로 교체 — '대지을' 조사 오류도 함께 수정), 촐킨 GMT 584283(2000-01-01=11 이크 앵커 검증). 프로필은 기존 축 심화+마야 1축 신설 — 판결문 형식 보존.
- **반복3**: VALUES24→16(중복 8개 흡수), 6→3→1. e2e 3종+샷 스크립트 갱신 — sed 접두사 겹침으로 샷 스크립트가 5개 선택으로 깨진 것을 grep 검수로 잡음(교훈: 리스트 치환은 경계 포함 패턴으로).
- **잔여 주의**: 구버전 localStorage(8/4개 선택 저장분)는 새 표기(16→6→3→1)와 숫자 불일치하나 판독엔 무해 — 리셋 없이 호환.

### [17] v23 — 실기기 스크린샷 비평 3반복 (온보딩~의식 전면 개편, 44/44 + 육안 검수)
- **요청**: 온보딩 스크린샷 5장+동전 스크린샷과 함께 "루프 하는 동안 너가 보는 거 맞아? 내가 마음에 안 드는 이유를 말해봐"(첫 시작~질문 결과 전, 동전 던지기까지) → 비평 제시 후 "진행".
- **비평(5죄)**: ①조각을 모으는 여정인데 **쌓이는 게 안 보인다**(오브가 처음부터 끝까지 같은 크기·같은 색) ②리빌이 작은 원 안 12px — **절정을 읽을 시간이 없다** ③MBTI 16그리드 = **세계관 속 설문지**(수호신이 "이미 아는" 존재인데 유형 코드를 고르게 함) ④의식 화면에 입력창·빈 점선 스캐폴드가 남아 **무대가 없다** ⑤플레이트 위치·줄바꿈·카피 등 마감 잔손.
- **판단·구현**:
  - 반복1 — DustOrb를 성장형으로(`stage` 0~3: 생일→성격→가치 단계마다 입자 150+140씩 증량, `tint` 오행색): "모은다"의 시각 증거. 수호신 팔 수 상한 `3+((lp-1)%5)`(3~7)로 과밀 해소 + 금 폼 리드암 비대칭(정오각 대칭의 인공감 제거). 카피·플레이트 마감.
  - 반복2 — 리빌을 화면 중앙 무대로(`.rvstage`/`.rvbig` 22px 금빛, 항목당 1.15s, `rvAbsorb`로 오브에 흡수): 절정을 읽게 한다. MBTI는 16그리드 폐지 → "요즘의 너는 어떤 모습이야? — 내 기억이 맞는지 골라줘" 2택×4(같은 정보, 설문 문법 제거·픽션 문법 유지). 4행 완성 시 자동 조립.
  - 반복3 — 의식 장면화: ritual 중 textarea 숨기고 질문을 인용문(`.qquote`)으로 무대에 올림, 빈 점선 6줄 폐지 → 던질 때마다 효가 낙하(`hexDrop`)하며 아래부터 쌓임(column-reverse, 전통 초효=바닥), 동전 포물선(`coinFly` 0.75s), 낙착마다 수호신 요동(agitate 0.6s·6효째 1.4s 클라이맥스), 중도 취소 '물음을 고칠래'.
- **검수 체계 신설**: "너가 보는 거 맞아?" 지적 수용 — `e2e/journey.mjs`(전 여정 풀페이지 14컷, BIRTH 파라미터) 신설, 매 반복 빌드+배터리(스모크 21·판결 9·만세력 14)+**렌더 육안 검수**를 통과 조건으로. 반복3 검수에서 동전 공중+질문 인용문 한 프레임, 괘 완성("화산려 · 변효 4,5,6효 → 수산건") 확인.
- **결과**: 3반복 모두 44/44 PASS. e2e 셀렉터는 MBTI 픽션형·"동전 셋" 부분일치로 갱신. v23 스냅샷 승격(03), zip·단일파일 전달. 교훈 재확인: **렌더 검수 없는 시각 변경 금지** — 테스트 통과와 "보기 좋음"은 다른 문제.

### [18] v24 — 판결 전 단계 강화 (유저 레퍼런스·지적 3건 반영, 45/45)
- **요청**: "데스크탑 전엔 모바일 아티팩트에서 판결문을 못 보니, 그 이전 단계를 탄탄히" + 질문 3건: ①MBTI 2지선다 왜/인식 안 됨 ②혈액형 뜬금없고 개인정보, 판결에 영향 없으면 제거 ③레퍼런스 2건(3D 입자 구체·폭발 블룸) 제공, 판결 전 이펙트가 수호신과 동떨어짐 + 스텝별 성장 안 보임.
- **진단**: 성장 미체감은 이중 원인 — 유저 실행본이 v22 아티팩트(성장 미탑재)였고, v23 성장도 입자 수 증가뿐이라 '자라는 순간'이 없었음. 오브(평면 먼지)와 수호신(유속장 생물)은 시각 DNA가 단절돼 있었음. 혈액형은 코드 확인 결과 SYS에 "판결 미반영" 명시 — funLine·오라 색에만 쓰여 제거 무해, 지표 포트폴리오 원칙("신뢰도 있되 덜 알려진 것")과도 정반대라 제거가 정합.
- **구현**:
  - 반복1 — DustOrb를 3D 회전 입자 구체로 리라이트(구면 좌표+Y축 회전+기울임 투영, 깊이별 명암·크기). 성장 = 응집(jitter 0.62→0.08)·입자 230→620·자전 가속·오행 착색, 단계 상승 순간 펄스+성장 링. 가치의 방 섹션 key 제거로 오브 연속성 확보(펄스가 씹히지 않게), 텍스트만 key 페이드.
  - 반복2 — BirthCanvas 신설: 수축(1.15s, 자전 가속)→섬광(0.23s)→방사 스트릭 블룸(1.8s, EXPOSOMES 레퍼런스) 후 수호신 페이드인. 재회 유저는 기존대로 전환 생략.
  - 반복3 — MBTI 4행 그리드 폐지 → 순차 문항("기억 N/4" + 수호신의 질문 + 2택 + '아까 걸로 돌아갈래'), 완료 시 "기억났어" + '다시 떠올릴래'. 혈액형 전면 제거: 상태·저장·프로필·SYS 3곳·콜2·UI·CSS·시드. 정령은 달 별자리로 재배선(funLine 유지), 수호신 곁 위성은 달빛 은색 상시로. 구버전 localStorage는 blood 필드 무시로 호환.
- **주의(기록)**: 수호신 시드에서 혈액형이 빠져 기존 유저의 수호신 형상이 미세하게 달라짐 — 출시 전이라 수용.
- **결과**: 빌드 통과, 스모크 22(혈액형 부재 검증 1건 추가)/판결 9/만세력 14 = 45/45. journey 15컷(기억2/4 컷 추가) 육안 검수 — 먼지 구름(오프닝)→구체 성장+펄스 링(가치)→방사 블룸(탄생)→수호신 사슬 확인. v24 스냅샷 승격.

### [19] v25 — 신을 완성하는 정보(음력·이름·성별→대운)
- **요청**: "성별·이름은 왜 안 넣었어? 이런 게 잘 모여야 신의 조각이 잘 모일 것 같은데. 신을 완성하는 데 꼭 필요한 정보가 뭘까" + "다 추가" + 이전에 말한 '음력 안 돼서 간절기 애들 오판' 건 확인.
- **판단 프레임(제시)**: "조각이 모여야 신이 모인다"는 감정적으론 맞지만 폼 비대의 본능 — 혈액형을 지운 잣대와 충돌. 원칙: "이미 너를 안다=생일에서 파생되는 건 안 묻는다"(사주·별자리·달·수비학은 파생이라 안 물음). 이름·성별은 파생 불가 → 물어도 세계관 안 깨짐, 단 '기억을 되찾는' 프레임일 때만. 두 관문: ①판결을 바꾸는가/친밀도를 높이는가 ②마찰·거부감 대비 값. → 이름(친밀도, 성명학은 한자 필요라 옵션 보류), 성별(대운을 지어야만 값을 가짐; 단독은 혈액형 2.0), 음력(정확성 버그). 직업·연애상태 등은 배제(모를 권리·신비감).
- **간절기 진단**: 절기(節氣) 경계 자체는 v18에서 태양황경으로 이미 처리됨(입춘·월주). 다만 음력 미지원과 겹치는 인구가 동일 — 음력 설날이 매년 입춘 근처라, 음력 1~2월생이 음력을 양력칸에 넣으면 날짜+사주년이 동시에 뒤집힘. 즉 음/양력 토글이 그 '간절기 오판'의 실제 해결책.
- **구현**:
  - 반복1(음력) — `lunar2solar`(기존 LUNAR표·solar2lunar의 역) 신설, 음/양력 토글+윤달 체크, doReveal에서 음력이면 양력 정규화 후 모든 계산(사주·달·별자리·수비학·토정·삼재)에 양력 전달. birth에 정규화된 양력 저장(원본 음력은 lunarNote). 구버전 저장분은 cal 부재→양력 기본으로 호환.
  - 반복2(이름) — birth.name 선택 입력(맨 위, "너를 뭐라고 부를까?"), 프로필 상단 '호칭' 주입 + SYS "결정적 순간에 이름 부른다(B ○○아·A ○○님)" 규칙, 리빌 멘션 "그래 — 서연, 너는 원래…". 판결 미반영(친밀도 전용) — 혈액형과 다른 점은 값이 '이름 불러주는 존재'라는 정서.
  - 반복3(성별→대운) — `daeun` 신설: 양남음녀 순행/음남양녀 역행, 대운수=순행 다음 節·역행 이전 節까지 일수/3(태양황경으로 節 크로싱 탐색), 현재 나이로 대운 간지(월주±step) 산출. 성별 선택 시에만 계산, 프로필 타이밍 층에 "대운(현재 인생 시기): 무인(토) 33~42세 역행"으로 주입. SYS는 대운을 별도 축 신설 없이 사주 근거에 녹이도록 지시 → 판결문 형식 보존.
- **검증**: 만세력 24/24 — 음↔양 왕복 6/6, 설날 2건(2000.2.5·1993.1.23), 윤달 구분, 설날생 사주년(음력정월→양력1.23→입춘전→임신년), 대운 방향(양년 남 순행/여 역행)·대운수 범위·pre·첫대운=월주±1. 전체 55/55. 프로필 실측(headless로 window.claude.complete 가로채 프롬프트 원문 확인): 호칭/성별/대운 3항목 정상 주입. e2e 셀렉터는 이름 입력(input.in.wide) 추가로 밀려서 `input.in:not(.wide)`로 수정.
- **주의(기록)**: 대운 간지는 알고리즘(방향·대운수·간지 스텝)을 내부 검증했으나 외부 만세력과의 대조는 사용자 실측 권장. 대운수는 세는나이 근사(10년 버킷엔 무해). 이름은 수호신 시드에 미포함(형상 불변).

### [20] v26 — 전방위 자가 검토 → 동화 도입부·조각 보태기·조기 뒤집기
- **요청**: "놓친 게 뭐냐, 내가 마음에 안 든다면 왜일 것 같냐, 전방위 검토" → 검토 제출 후 지시: ①이름(상표)은 걱정 말 것 ②도입부는 동화 영화처럼 ③추가 입력 경로 OK ④판결문은 유지(콜2 백그라운드 구조 유지, 일찍 뒤집으면 '근거 모으는 중' 안내만) ⑤나머지 최적안 ⑥배포 재촉 금지.
- **자가 검토에서 인정한 결함**: v25 입력 화면의 서식 회귀(픽션 원칙을 스스로 어김), 음력 입력의 조용한 덮어쓰기(변환을 보여주지 않음), 기존 사용자가 리셋 없이 새 조각을 못 넣음, 잔글씨 4개 조항의 약관화, 이름 활용처 부족. 콜2는 확인 결과 이미 백그라운드 프리로드(v15 설계)였고 부족한 건 조기 뒤집기 반응뿐이었음.
- **구현**: step1을 bstep 0~3 장면화(장면당 수호신 한 문장+최소 입력+금 버튼 하나, '아까 장면으로' 후진, 장면별 검증), 오프닝 버튼·브랜드 4.4s 지연 등장(타이틀 시퀀스), 음력 변환은 리빌 사주 카드에 "음력 X.X — 하늘의 달력으로 바꿔 읽었어"로 노출, 재회 인사 "다시 왔네, ○○". 조각 보태기: 이름·성별 결손 시 노크("아직 못 찾은 조각이 있대")→인라인 패널→저장(saveMemory 즉시 호출, 구버전 저장 name/sex undefined 호환 심). 카드: 콜2 진행 중에도 뒤집기 허용, 뒷면에 점멸 "조각들이 근거를 모으고 있어…", 힌트 문구도 수집 중 노출.
- **검증**: 55/55(스모크 22·판결 9·만세력 24) + journey 17컷(이름·하늘·흐름 장면 추가) 육안 검수 + 보태기 E2E 실측(노크→패널→저장→노크 소멸, localStorage에 이름·성별 반영 확인). e2e 온보딩 3종을 장면 흐름으로 갱신.
- **보류(다음 후보)**: 실기기 FPS 측정, 대운 경계 나이 정밀화(세는나이 근사), 야자시, 이름의 아침 문안·의식 확장. 배포는 사용자 판단 대기(재촉 안 함).

### [21] v27 — 수호신 다양성·리빌 다이어트·정령 제거 (실기기 4장 피드백)
- **요청/질문**: ①이름 필수·한자 여부 ②텍스트 뚱뚱, 스크롤해야 정보 보임 — 같은 위치서 수호신이 바뀌는 모션으로 '모이는 게' 인지되게 ③외곽 도는 것=혈액형 정령 잔재, 빼도 됨 ④"저 모양 경우의 수가 어떻게 돼? 크게 안 다르던데 다른 사람 거 보니까".
- **진단(경우의 수)**: 코드 분석 결과 실루엣을 결정하는 축이 사실상 오행 주기운 5종뿐. 팔 수(라이프패스 3~7)는 목·금 두 형태에서만 사용(화·수·토는 계산만 하고 미사용), 색은 오행 팔레트 안에서 별자리로 ±33° 미세 회전, MBTI·달은 밀도·속도·밝기(움직임)만 → 정지 화면에선 티 안 남. 그래서 같은 오행끼리(예: 금-금)는 은청색 6갈래로 거의 동일. 이론 조합은 수만이나 "한눈에 다른" 실루엣은 5개. 유저 진단 정확.
- **구현**: ①팔 수를 5형태 전부에 — 화(arms개 불꽃 혀)·수(arms개 물결 층)·토(arms갈래 봉우리 lobe). E/I→R 배율(외향 1.06/내향 0.9), T/F→입자 크기(사고 0.84/감정 1.24). → 5형태×팔 3~7 + 크기/질감 = 뚜렷 실루엣 대폭 증가. ②정령 위성 블록 삭제. ③멘션 3→2줄(중간 흔들림 줄 제거)·"요즘의 너는?" 축약·.mention 17→14.5px, DustOrb stage를 `1 + 답한 문항수*0.5`로(제자리 성장+펄스+성장 링). 이름은 선택·한글 유지 답변(성명학=한자 필요라 미채택, 오직 호칭).
- **검증**: 렌더 4프로필 대조 — 토(3갈래 라벤더)·금(금빛 6엽 별)·목(다엽 나무)·수(초록 물기둥) 완전 상이 확인. 리빌 mid-MBTI 캡처: 세로 오버플로 0px(스크롤 불필요), q1→q3 오브 성장·링 육안 확인. 전 배터리 55/55.
- **미해결 고지**: 모바일 아티팩트 판결 간헐 성공/실패 = Anthropic 서버 간헐 고장(코드 무관, 폭포수가 처리). 대운 외부 대조·실기기 FPS·야자시 여전히 보류.

### [22] v28 — 심화 지표를 수호신에 배선 + 판결 반응 연기 (지문화)
- **요청**: ①"사주뿐 아니라 심화버전(납음·촐킨·나크샤트라·대운)도 넣었는데 그건 디자인에 영향 안 줘? 사주 베이스는 결국 5형태 변주라 비슷해 보여" ②절기 넣었냐(→ 사용자: 안 넣어도 됨) ③"판결할 때 괘 결과에 따라 수호신이 다른 모습/행동 보이면?" + 지난 합의(상단 문장 1회성·이름 실명 카피). "다 진행".
- **진단**: 수호신 비주얼이 읽는 건 오행(형태)·별자리(색)·라이프패스(팔)·MBTI(크기·질감)·달(밝기)뿐. 납음(30)·촐킨(260)·나크샤트라(27)·대운은 전부 판결문 텍스트로만 가고 캔버스엔 미반영 → 260×30×27 지문을 계산하고 5형태만 그림. 규율: 각 지표를 서로 다른 지각 채널에 배선(안 그러면 만다라 노이즈).
- **구현**:
  - ① GuardianCanvas에 birth 전달, 내부에서 촐킨(20날개→코어 마디 수 3~8·13톤→층 1~3/강도)·납음(30→흐름 2차 진동 주파수·진폭)·나크샤트라(27→강조색 hue 갈래)·대운(현재 간지 오행→중앙 아우라색) 파생. 중앙에 회전하는 코어 문양(사람마다 다른 '심장') 신설.
  - ③ reactRef 신설, 판결 낙착 시 direction 세팅 + 카드 지연 700→1400ms(몸짓 먼저 보이게). draw()에서 GO=목표위치 +0.5 확장·밝기 +0.5 / STOP=−0.45 수축·−0.55 감광 / HOLD=잔잔한 ±맥동. 0.18s 상승→1.7s 소멸 엔벨로프.
  - 상단 자기소개(guardianIntro)는 introSeen 게이트로 첫 만남 1회만(첫 질문 시 off). 이름 카피 "이름이어도 별명이어도"→"네 이름을 다시 들려줄래 — 어릴 적 내가 부르던 그 이름"(실명 유도, 스킵 유지). 절기 미채택.
- **검증**: 렌더 실측 — 같은 납음-금 2명(칼끝의 금/백랍의 금)이 촐킨 코어·형태·색으로 완전 상이. 판결 GO(팔 펼침+발광) vs STOP(수축+감광) 대비 뚜렷, 코어 노드 고리 가시. 전 배터리 55/55(카드 지연 변경에도 판결 9/9 유지).
- **보류**: 실기기 FPS(코어 문양·반응으로 draw 부하 소폭↑), 대운 외부 대조, 야자시. 배포는 사용자 판단 대기.

### 아직 안 끝난 것 / 다음
- **[선택] v17 B단계** — WebGL(three.js/regl) 전환. A단계 반응 본 뒤 결정.
- **[관찰필요] 저사양 안드로이드 FPS** — 4,200 입자 실기기 확인.
- **[미해결] 텍스트/수호신 겹침** — 수호신이 강해지며 gtext 가독성 저하. 레이아웃 조정 후보(A단계 범위 밖).
- **[사용자 액션] Vercel 배포** + `ANTHROPIC_API_KEY` + 지출한도, 실기기 실API 테스트.
- **[미착수] 키프리스 '비나리' 상표 검색.**

---

## 2026-07-20 · 세션 (계속) — 아티팩트 구동 진단 + v29 정독 다이어트

### [A] "아티팩트로 구동" — 두 런타임의 차이 규명
- **요청**: v28을 아티팩트로 띄워라. 이어서 "첨부 JSX 그대로 React 아티팩트로, 코드 수정 말고".
- **판단**: 브라우저는 JSX 원문을 못 돌리므로 JSX→JS 변환만(로직 무수정)으로 단일 HTML 자립화 — App.jsx의 `import react`/`export default`/테스트 export 3개 배관만 조정, esbuild로 JSX 트랜스파일, React18 UMD 인라인. Artifact 툴로 발행(같은 파일경로=같은 URL 유지).
- **결과 & 진단**: 발행 URL에서 온보딩·수호신·의식·괘까지 전부 정상이나 **판결만 "Failed to fetch"**. 원인 = 런타임 이원화: claude.ai **대화창** 아티팩트는 `window.claude.complete` 주입(→판결 O), Claude **Code 웹** 아티팩트는 미주입(→`/api/judge` 없음→직접호출 CSP 차단). artifact-capabilities 스킬로 확인: 발행 아티팩트가 선언 가능한 능력은 `downloads`·`mcp`뿐, completion 능력 자체가 없음. 즉 코드 무죄, 경로 문제. → 사용자에게 대화창 붙여넣기 경로 안내, v28 JSX 전달. **판결 완주 확인됨**(대화창에서 가치/바이오리듬/마야/토정 L3까지).

### [B] v29 — 실사용 4지적 반영 (정독·인지)
- **요청**(대화창 판결 완주 후): ①온보딩 회상 텍스트는 한 번만, 선택 땐 숨김 ②수호신 자기소개(칼끝의 금…)는 생성 때만 ③판결이 백에서 이펙트 돌아 느림·스크롤도 느림 ④(재확인) 판결 뱉을 때부터 전체 이유를 백에서 돌려라, 결론부터.
- **판단**: ④는 이미 구현(콜1 결론 setRes 직후 fetchDetail 선로드, 카드 뒤집기 전 완성) — 코드로 확인. 체감 지연의 진짜 원인은 ③ 캔버스(3.2~4.2k 입자 60fps)가 메인스레드를 점유해 콜2 렌더·카드 뒤집기·스크롤과 경합. → 판결 대기·정독 구간에만 프레임 솎기.
- **구현**(transform34, 9치환):
  - ① reveal 화면의 회상 나레이션(mention)+근거박스(refbox)를 `dims 0개일 때만` 렌더 → 첫 MBTI 픽부터 숨김(짧은 "요즘의 너는?" 헤딩·문항만 유지).
  - ② guardianIntro 게이트를 introSeen→**justBorn**으로: 탄생(3.2s) 순간 on, 10.5s에 off. 질문 화면 상주 제거. 재회 인사는 introSeen 유지.
  - ③ **restRef**(busy||res 동안 true) + GuardianCanvas draw 스로틀: rest && !요동 && !반응(1.8s) && 46ms 미만이면 프레임 스킵(~21fps). 반응 연출·대기 요동은 풀프레임 보존.
- **검증**: 빌드 OK, 스모크22/판결9/만세24 = 55/55 유지. 신규 `e2e/v29-check.mjs` 7/7 — ①선택 전 노출→첫 픽 후 숨김·문항 계속 ②탄생 직후 자기소개 노출("나는 '길가의 흙'의… 존재야") ③스로틀 중 판결 L1 렌더·캔버스 생존·무오류. 렌더 2컷 육안(MBTI 정리·탄생 인사). 아티팩트 HTML v29 재빌드→같은 URL 재발행.
- **보류/다음**: 실기기 스크롤 체감(헤드리스 통과, 실측은 사용자), 자기소개 소멸 페이드(현재 즉시 제거), Vercel 배포(공유 URL 판결). 판결문 형식·콜1/콜2 구조 불변.

### [C] 배포 테스트 관문 — Anthropic API 크레딧 결제
- **상황**: 사용자가 Vercel 배포(공유 URL 판결)를 위해 Anthropic 콘솔에서 API 크레딧 US$5 구매를 시도 → **구매 버튼 비활성**. 화면상 총 결제 금액·예상 세금이 `$--`(미산정)로, 청구지 주소/결제수단 미완이 유력.
- **판단(중요)**: **자기 테스트는 결제 불필요** — claude.ai 대화창 아티팩트(`window.claude.complete`)로 이미 판결 완주 확인됨(구독으로 커버, API 과금 없음). API 크레딧은 **Vercel 공유 URL(다수 테스터·상시 가동)** 경로에서만 필요. 결제는 급하지 않음.
- **다음**: (선택) 배포로 갈 경우에만 크레딧+환경변수+지출한도. 그 전까지 v29 대화창 아티팩트로 ①②③ 체감 검수.

---

## 2026-07-21 · 세션 (계속) — Vercel 실배포 + v30 실사용 피드백

### [D] Vercel 배포 완료 (0단계 달성)
- **경과**: 사용자가 API 크레딧 결제 후 배포 결정. Vercel 임포트 화면에서 두 함정 발견 — ①`main`이 34커밋 낡음(app이 1바이트 파일이던 초기 커밋) ②Root Directory `./`. 사용자 명시 허락받아 피처 브랜치를 **main에 fast-forward 머지**(GitHub MCP로 실제 반영 확인) → Vercel이 최신 main 재스캔하니 `app/`이 폴더로 노출 → Root=app 선택 가능. `ANTHROPIC_API_KEY` 설정, Deploy.
- **결과**: `binari-sepia.vercel.app` 라이브. 서버 프록시(`/api/judge`) 경로로 **판결 완주 확인**(window.claude.complete 없이). 아티팩트 URL에서 막히던 판결이 배포 URL에선 뚫림 = **0단계(아티팩트 탈출) 사실상 달성.**
- **운영 규칙(사용자 지시)**: 이후 main은 "푸시해" 지시가 있을 때만 올림. 코드는 main 자동배포(1~2분), 환경변수는 수동 Redeploy.

### [E] v30 — 배포 실사용 피드백 6종 (한 배치)
- **요청(실사용 중 연발)**: ①판결 카드 뜨면 뒤 수호신 안 돌아도 됨(RAM·스크롤 막힘) ②부적 버튼 안 눌림 ③재설정('다른 사람이야?') 무반응 ④동전 던질수록 CTA가 밀려 내려감 ⑤CTA를 버튼인지 모름(예시문인 줄) + 캔버스가 배경과 네모로 구분됨 + 모델 추천.
- **진단·구현**:
  - ①②는 **동일 원인**: 카드 뜬 뒤에도 4,200입자가 메인스레드 포화 → 클릭·스크롤 드랍. restRef를 3단계 수치(0/46/300ms)로: 대기 46ms, **카드 정독 300ms(~3fps 사실상 정지)**. 반응 1.8s는 풀프레임 유지. → 스크롤·부적클릭 회복.
  - ③ `window.confirm`이 웹서 씹힘(b 케이스) → 앱내 확인 UI(응/아니).
  - ④ `.hexlines min-height:8px`→`88px`로 6효 공간 예약 → 버튼 불변.
  - ⑤ 캔버스 가장자리 radial 마스크(58%→88%)로 네모 경계 제거. `.btn.ghost` 색이 `#8a7f95`(본문 회색)라 버튼 인식 실패 → 금색 미세 채움+테두리+그림자+hover/press. `.dimopt`도 소폭 강화.
  - **회상 나레이션 순차**(별도 지시): mention+근거를 문항과 한 화면에 두지 말고 → 먼저 노출→'응,기억나' 탭→문항(recallSeen). v29의 dims-게이트 대체.
  - **모델 Sonnet4.6→Sonnet5**: 인트로가($2/$10, 8월까지)로 품질↑·비용≈. 단 Sonnet5 기본 thinking-on이 콜1(320토큰)을 먹으므로 `thinking:{type:"disabled"}` 필수 추가(judge.js+callDirect).
- **검증**: 빌드 OK(컨테이너 재활용으로 node_modules dev deps 유실 → `npm ci` 복구), 63/63(smoke22·판결9·만세24·v30체크8), 데스크톱 렌더로 네모경계 소멸·CTA 버튼화 확인. v30 스냅샷 승격. main 푸시(자동배포).

### 백로그 (배포 전/중 결정 대기)
- **[결정됨] 데이터 계측 = PostHog(무료)** — 익명ID+핵심 퍼널+D1/D7. 송신 추상화 한 겹 두어 나중 Supabase/자체수집 전환 가능(사용자 확인). 질문원문·실명 미전송 원칙. 아직 미착수.
- **[탐색] 수호신 무(無)구심점 시각** — 사용자 레퍼런스 3종(불=꼬여오르는 리본/바람=꼬리 소용돌이 유속/연기=난류 블룸) 전부 중앙 코어 없이 흐름으로 존재. 현재 가디언은 코어 글로우+촐킨 코어 문양+금 방사살로 구심점을 강제. 자유화하려면 파라메트릭 유일성(오행·촐킨·납음·나크샤트라)·시드 재현·모바일 60fps를 어떻게 보존할지가 관건. Canvas2D로 리본/유속 근사 가능, 난류 블룸은 WebGL(로드맵 B단계) 후보. → **[F]에서 B단계로 실행됨.**

---

## 2026-07-21 · 야간 자율 루프 — v31 WebGL 수호신 (로드맵 B단계)

### [F] WebGL 리라이트 — "이쁜 거부터" (사용자 지시: 밤새 작업+검증, 기존 버전 폴백 유지)
- **요청**: PostHog보다 WebGL 먼저. 기존(Canvas2D) 유지·폴백. 아침까지 /loop 자율 반복으로 검증까지 완료. 중간에 "일단 푸시 해놔" → v31 초판을 main에 푸시(라이브 반영), 이후 튜닝 계속.
- **설계 판단**:
  - **라이브러리 0** — three.js(+150KB) 대신 raw WebGL. 입자 위치를 **정점 셰이더가 시간 함수로 계산**(상태 없음) → 버퍼 피드백 불필요, 코드 단순, 번들 +8KB, 메인스레드 완전 해방(v30의 "카드 뜨면 정지" 땜빵이 GL 경로에선 구조적으로 불필요하나 배터리 위해 rest 스로틀 유지).
  - **무구심점**: 중앙 글로우·촐킨 코어 문양 폐지. 유일성은 흐름의 성질로 재배선 — 촐킨 20날개=가닥 수(3~8)·13톤=꼬임 밀도, 납음=결(주파수·진폭), 나크샤트라=강조색, 대운=색조 틴트. 시드(seedRnd) 동일 → 재현성 보존.
  - **형태 5**(레퍼런스 매핑): 화=꼬여 오르는 리본 기둥(ref 불) · 수=흐르는 물결 층 · 목=뻗는 가지 흐름 · 금=궤도 빛줄기 · 토=중심 없는 난류 융기(ref 연기).
  - **폴백 3중**: glDetect 실패·셰이더 컴파일/링크 예외·webglcontextlost → 전부 기존 GuardianCanvas(Canvas2D, 무수정)로 즉시 전환. `?r=2d`로 강제 폴백 디버그.
- **검증 루프 기록**:
  - R0(초판): webgl-check 7/7 신설 통과(SwiftShader, 렌더러 표식 `data-renderer` 부여) → 사용자 지시로 main 푸시(`eaf414a`).
  - R1 캡처: 전반 과암·화 코일 기계적·목이 **아래로 자람**(클립공간 y부호 실수)·토 도넛(중심 공백). → 튜닝 R1: 알파·포인트 상향+유사 블룸 패스(u_psMul), 화 가닥 해시(반경·위상·속도 가닥별 상이+기둥 사행), 목 상향+뿌리 분산, 토 pow0.75+2옥타브.
  - R2 캡처(오행 5종 생일 스캔: esbuild로 App.jsx export를 노드에서 로드해 calcSaju 스캔): 금 백화 도넛 발견 → 오행별 알파 계수 F_AL(금 0.3, 밀집형 하향)+반경 분포 확대+포인트 계수 F_PS. 목 뿌리 핫스팟 smoothstep 컷. 표적 재캡처로 5형태 전부 육안 통과 — 특히 화 리본(적)·화형 금색 조합이 레퍼런스에 근접.
  - 함정 기록: 캡처 프로필의 시각(시주)이 바뀌면 main 오행이 뒤집혀 태그가 어긋남 — 스캔은 12:00 기준으로 통일해야 함.
- **결과**: 테스트 70/70(smoke22·판결9·만세24·v30체크8·webgl7), 렌더 5형태 육안 OK. v31 스냅샷 승격, 문서 4종 갱신. 튜닝(R1+R2)분도 main 푸시 — 초판(목 역방향·과암·금 백화)을 라이브에 남겨두는 게 더 나쁘다고 판단, "일단 푸시" 지시의 연장으로 해석.
- **다음(사용자 결정 대기)**: PostHog 계측 착수. B단계 후속(실기기 FPS·프레임버퍼 트레일)은 선택.

---

## 2026-07-26 · 세션 — G0-a 계측 분리 완결 (개발 큐 검수 + PostHog 격리)

### [A] 상황: 개발 큐 4건(D1~D4)이 v93에 이미 구현돼 있었다
- 이 세션의 클론은 v45였고 main은 v93. `git fetch` + 머지(무충돌) 후 확인하니 `한국본진실행계획_v01` §5-1의 개발 큐 4건이 **전부 main에 구현·주석까지 D1~D4로 표기**된 상태였다(별도 세션에서 실행된 것으로 보임 — 이 세션은 구현이 아니라 **검수**를 수행).
- 구현 확인 내역: D1 `binari.internal.v1`(+`?i=1` 영구화, super property `is_internal`) · D2 `binari.firsttouch.v1`(utm 5종+클릭ID 종류만 저장, 최초 1회 고정) · D3 `binari.belief.v1`(첫 판결 후 1탭, `belief_answered`, 민감정보 우려로 PROFILE_KEYS에 넣어 동의자 한정 — 전체 집계 필요 시 빼면 됨) · D4 `letter_clicked`(노출=verdict_shown이라 CTR 계산 명확, 결제 미구현).
- **검수 결과**: `vite build` OK · 스모크 **26/26** · 런타임 검증(Playwright) — ①utm 유입 후 파라미터 없는 재방문에도 `ft_content=A_h2_fire` 유지(D2 핵심: direct 덮임 방지) ②`?i=1` 후 재방문에도 `is_internal:true` 유지. 검증 스크립트는 일회성(커밋 안 함).

### [B] PostHog측 분리 — G0-a의 나머지 절반
- **정적 코호트 436757** "내부 테스터(계측분리 이전)" — 현재 전체 유저 4명 스냅샷(전략로그 [3]에서 외부 유입 0건 확인된 그 4명). 플래그 도입 이전 데이터를 영구 격리.
- **게이트 대시보드 1907151** "외부 유저 전용(G0~G3)" — 인사이트 4종, 전부 `is_internal ≠ true` + 코호트 436757 제외 필터:
  1. `xscpleu9` G1 D7 리텐션(target=verdict_shown, return=app_open, 게이트 20%)
  2. `1oXlGP4C` 완주 퍼널(app_open→birth→awaken→question→verdict, 완주CAC 분모)
  3. `e67ieZvV` fake-door CTR(letter_clicked/verdict_shown, 죽음신호 1%·0% 명기)
  4. `l8iwivtm` 소재별 완주 유저(ft_content 분해 — 소재 판정 축)
- **판단**: 리텐션 target을 app_open이 아닌 `verdict_shown`으로 — G0-b 카운트 단위("판결 1회 이상 본 비내부 유저")와 모수를 일치시키기 위함. 실행계획 §5-5와 동일 논리.

### [C] G0-a 통과 판정
실행계획 §5-1의 통과 기준 3항 중 ①외부 필터 대시보드 존재 ✅ ②신규 유입의 utm_content가 재방문까지 유지 ✅(런타임 실측) ③기존 4명 내부 코호트 편입 ✅. **G0-a 통과. 다음은 G0-b — 개발 라인 작업 없음, 소재 12종 제작·메타 집행(마케터 라인)이 크리티컬 패스.**
- 주의 1: 팀 5인은 **각자 폰·PC에서 `binari-sepia.vercel.app/?i=1`로 1회 접속**해야 이후 트래픽이 내부로 태깅된다(새 기기·시크릿창은 다시 필요). 공유 링크에 `?i=1`이 섞여 나가지 않게 주의.
- 주의 2: v93 코드는 main에만 있고 이 브랜치는 main을 머지해 반영. **main 재배포는 불필요**(이미 라이브).

## [2026-08-02] v106 · 셈 — 실기기 스샷 3장에서 역산한 카드 손질

유저 실기기 스샷 3장("저녁 밥"·"점심"·"런칭할까")의 위화감을 진단 → 모델 탓이 아니라 배선·프롬프트임을 확인(Vercel 런타임 로그: free/paid 전부 claude-sonnet-5, 하이쿠 미사용).

- **①이 핵심**: `tallyVotes`의 against(반대 표)를 표시부가 그대로 받아 "찬성"이라 적음. GO가 강할수록 약해 보이는 역전 — 스샷 세 장 전부 "N개 중 1개 찬성"이었던 이유. 라벨 의미는 질문의 행동 기준(찬성=GO표)으로 정리하고 표시 수는 total-against.
- ②④⑤는 위화감의 나머지: 톤 내부값 노출("· 격려"), 풀네임 3연호("강석우"×3), 세 층이 같은 말 반복.
- 검진에 ①② 재발 검사 추가. HEAD(수정 전)로 되살려 둘 다 검출되는 것 확인.
- 회귀: 검진 0심각 / smoke 25/25 / verdict 29/29 / scope 54/54 / webgl 7/7 / v29 8/8 / 만세력 28/28 / 계측 24/24 / 출처 20/20.
- 남긴 것: 속결(오늘의 물음)에도 運命合意判決 도장·神 인장이 붙는 문제 — 시각 요소 변경은 헌장(판결문 형식 보존·톤 변경 범위는 프롬프트 문구뿐)에 걸려 보류. 바꾸려면 먼저 물을 것.

## [2026-08-02] v107 · 그릇 — 상세 리포트를 명리 판단 순서로

실사용 명식 하나를 실제 로직으로 뽑는 과정에서 리포트의 구조 문제 발견 → 딥리서치(동률 해석·용신·철학관 구성)로 확인 → 반영.

- 십성 동률: slice(0,3)이 삽입 순서를 대표로 만들던 것 → 3위 동값 전부 표시. 프로브로 7개 표기 확인.
- 대운 사다리: daeun()을 10년 간격 호출해 여든까지. 성별 없으면 안내 줄("성별을 더하면 펼쳐줄게"). 비어 있던 오행이 들어오는 구간에 라벨 — 서신 예시의 "빈 금이 채워지는 구간" 장치를 무료 리포트에도 일반화.
- 신강·신약: 통설 개수 판정(비겁+인성 4↑/2↓)을 '간이'로 명시하고 계측(strength·lack_el)에 추가.
- 조후용신·격국은 보류 — 궁통보감 조견표를 검증 없이 넣는 건 지어내기와 같다(전략로그 2026-07-28 기준 유지).
- e2e/report-check.mjs 신설: 카드 뒷면(리포트)은 어떤 e2e도 열지 않던 자리. 성별 버튼 문구('남')와 뒤집기 조건(why+콜2 도착)을 실제 경로대로 태움. 9/9.
- 전체 회귀 그린: smoke 25 / verdict 29 / scope 54 / webgl 7 / v29 8 / 만세력 28 / 계측 24 / 출처 20 / report 9. 검진 0심각.

## [2026-08-02] v107.1 · 용신 — 보류를 '지어내지 않는 범위'로 좁혀 반영

직전 v107에서 조후용신·격국을 보류했다(원문 조견표 미확보). 다시 보니 보류할 것과 채택할 것이 섞여 있었다.

- **억부용신은 보류할 이유가 없었다.** 신강·신약에서 기계적으로 도출된다 — 신약이면 인성·비겁, 신강이면 식상·재성·관성. 조견표가 필요 없고 지어낼 여지도 없다. 딥리서치 확인 결과 실무 적용률 약 70%.
- **조후는 표를 흉내 내는 대신 계산 가능한 사실로만.** 월지 계절 + 오행 편중(여름 화≥3 → 수·금 / 겨울 수≥3 → 화·목). 궁통보감 120칸을 웹 인용으로 재구성하는 건 토정비결 원문 지어내기와 같은 위반이라 하지 않았다.
- **갈릴 때 우리가 정하지 않는다.** 일치=강조, 불일치=둘 다 + "유파에 따라 달라지는 자리". 이게 유파 문제를 회피가 아니라 정직으로 푸는 방법이다.
- 검산: 둘째(병오 을미 임자 을사) 억부 금·수 / 조후 수·금 → 일치. 딥리서치가 찾은 궁통보감 미월 임수 처방(신금 정인으로 수원)과 독립적으로 같은 답.
- 대운 사다리에서 용신 구간을 ★로 띄우니 "언제가 내 계절인가"가 한눈에 잡힌다 — 유료 서신의 핵심 장치를 무료 리포트에 일반화한 셈.
- report-check 9→12. 전체 회귀 그린, 검진 0심각.

---

## 2026-08-11 · v110 — 정직성 4규칙 (리포트·서신)

**요청** — 외부 사주 프롬프트("30년 경력 역술가" 계열)를 놓고 차용 여부 판단 → 앱 반영.

**1차 판정과 그 오류.** 나는 차용 4 / 기각 4로 갈랐는데 **기각 쪽이 틀렸다.** 분량 지침과
연애·결혼·직업·건강 같은 삶의 영역 대부분을 잘라냈다. 창업자 교정:

> *"리포트는 운명을 알고 싶은 사람을 위한 서비스야. 알 권리는 최대한 보장해줘야 해.
> 네가 버린 것 중에 무조건 피해야 하는 '언제 죽는지', '자살' 같은 치명적 내용은 없는 것 같아.
> 수호신이긴 하지만 인간 역술가를 사칭하라기보다는 역술가의 화법이 왜 그렇게 이뤄졌는지 생각하고,
> 인간이 궁금해할 만한 부분은 말해줘야지."*

**내가 놓친 것** — §3-5에서 '판결=모를 권리 / 리포트=알 권리'로 이미 갈라놨는데도, 리포트의 범위를
다시 판결 기준으로 재단했다. 유저는 **운명을 알고 싶어서** 값을 치른다. 안 쓰는 건 배려가 아니라 은닉이다.
→ 금지선을 셋으로 확정: **수명·죽음의 시기 / 자살 암시 / 의학적 판정(병명·수술·투약)**. 그 밖은 전부 다룬다.
역술가 페르소나는 **사칭만** 금지하고 화법의 기능(전 영역·구체성·궁금한 것 먼저)은 가져온다. 상세는 구상 §3-8.

**구현 — 상세 리포트**
- **확신도 3단 꼬리표**(`CF`/`Cf`). 계산값(만세력에서 그대로) / 통설 해석(유파 갈림) / 곁가지(신살).
  범례를 본문 맨 앞에 둔다. 명식·읽지못한것·타고난것·흐름=계산값, 힘의 저울·채울 기운·짝·일=통설, 신살·띠·날=곁가지.
  — 셋을 같은 목소리로 말하면 **전부가 헐거워진다.** 반증을 파는 회사에서 가장 값싼 정직성이 이것이다.
- **'읽지 못한 것' 절 신설.** 시 미상·성별 미입력·도시 미입력·미래 생일이 각각 **무엇을 못 잡게 하는지**를
  한자리에 모았다. 전에는 이 사실들이 다른 문장 꼬리에 흩어져 있었다. 빠진 게 없으면 **"전부 읽혔어"라고 말한다** —
  '침묵'과 '완전'은 유저에게 전혀 다른 정보다.
- **십성 해설 3단화.** `SS_TIP`을 `{e 쉬운 말, r 실제로 나타나는 모습, s 그늘}` 로 폈다.
  앞 두 단만 쓰면 사전을 베낀 것이고, 세 번째가 있어야 이 사람 얘기가 된다. 그늘은 겁주기가 아니라 **같은 성질의 뒷면**이다.
- **배우자궁 공개**(`SPOUSE`). 일지 십성은 계산해 두고 화면에 안 쓰던 값이다. 위에서 '일=나·배우자'라고
  써놓고 정작 배우자 자리를 안 읽어주는 건 알 권리에 어긋난다. 사람들이 사주에서 가장 먼저 묻는 자리이기도 하다.

**구현 — 서신(콜3) 프롬프트**
`[정직성]` 블록 신설. ①근거의 급을 말투로 구분(계산값은 단정 / 유파 갈림은 "통설로는" / 신살·띠는 "참고로",
그 위에 결론 금지) ②없는 정보는 없다고 쓴다(시 미상이면 시 관련 서술 금지 — 추정으로 메우면 나머지도 의심받는다)
③용어 3단 ④좋은 면 옆에 그늘. 범위는 좁히지 않았다 — 서신은 여전히 '이 질문 하나'에 답하는 상품이다.

**검증** — 빌드 통과. report-check **20→26/26**(확신도 3종·범례·읽지못한것·3단 해설·배우자궁 신규 5건).
smoke 25/25 · verdict 34/34 · scope-check 54/54. 화면 오류 0.

**부수** — 작명 엔진 v02 뜻 필터에서 이름자 두 개가 조용히 죽던 버그 교정(`瑥` 사람 이름 온 = 훈이 '사람 이름'뿐이라
화이트리스트에 걸릴 단어가 없음 / `利` = 표준 훈 '이로울'이 수집 사전에 누락). 작명용으로 만들어진 글자가
작명 필터에 탈락하는 역설이었다.

---

## 2026-08-11 · 리포트 구조 전환 + 판독기 v01

**요청** — *"리포트는 사주 베이스가 아니고 각 항목별 설명이 들어가는 형태. 태어날 때 정해진 운명이 이랬어,
그래서 크다가 이런 느낌이 들었을 거야, 넌 지금 어느 단계야, 앞으로 이렇게 될 거야."*
그리고 *"선을 좀 넘어. 어떤 일이 생길지 딱 명확히 말하든가, 어떤 류의 일이 생기는데 예를 들면 머머머다 이렇게 하든가."*
*"너무 어렵게 설명해서 알 권리를 해친다."*

**한 것 — 순차 4단계, 각 단계마다 검증**

1. **전략로그 기록**(`2477602`) — 쌓인 판단 5건(과금 층·차별점·각인 세계관·매개 화자·항목별 4단)과 위험 3건.
2. **항목별 데이터 계산** — 두 명식의 십성 그룹·재고·대운별 십성·세운을 뽑았다. 여기서 서사 둘이 새로 나왔다:
   동생은 **재물의 창고(戌)가 21~30세 대운에 열리는데 하필 그 구간이 여든 해 중 유일한 골**이고,
   형은 **식상이 0인데 58세에 처음으로 표현의 대운**이 온다.
3. **리포트 재작성** — 열 항목(성격·배움·재능·일·돈·연애·결혼·자녀·몸·사람) × 4단.
   항목마다 여든 해 위에 지금 위치를 찍는 미니 타임라인. 「지금」 칸은 금색 밴드.
4. **판독기 v01** — 아래 별항.

**구체성을 올린 실제 예** — *"재물이 움직인다"* → *"스물하나에서 서른 사이에 한 번 크게 잃는다.
형태는 셋 중 하나다 — ①빌려주고 못 받는다 ②동업했다 갈라선다 ③될 것 같은 데 한 번에 넣었다 날린다."*
*"직업 변동 가능성"* → *"첫 직장을 삼 년 채우지 못할 가능성이 높다."*
금지선 셋(수명·죽는 때 / 자살 암시 / 의학적 판정)은 그대로 지켰다.

**검수에서 잡은 것 셋**
- **클래스명 충돌** — 새로 만든 선언문 `.say`가 최상위 「수호신이 말하는 자리」 `.say`와 겹쳐
  엉뚱한 회색 박스가 깔렸다. `.decl`로 분리(9곳). 문서 높이 1,118px 감소로 확인.
- **타임라인 라벨 겹침** — 0세 아이라 「지금」 마커가 0 눈금과 붙어 "0지금"으로 찍혔다. 26px 이내면 눈금을 숨긴다.
- **갈림 표시 누락** — 「여러 하늘의 증언」에서 `.sky.no`에 스타일이 없어 **게이지에만 붉게 찍히고 표에서는 안 보였다.**

**판독기 v01** (`05_실험/판독기_v01/`) — 11종 + 자체 검증 27건.
**검증이 실제로 오류를 잡았다**: 자바 웨톤 기준점을 자바력 원년(1633-07-08 율리우스 = '금요일 레기')에 걸었는데
**그날은 금요일이 아니라 월요일**이었다. 그레고리 환산과 율리우스력 직접 계산이 같은 답을 냈다.
1945-08-17 인도네시아 독립일로 교체했고, **절대 명칭은 여전히 외부 확인이 필요하므로 `derived`로 표시**했다 —
검증 못 한 것을 검증했다고 쓰지 않는다.

**판독기가 낸 실제 결과**
- 동생: 여덟 글자(큰 물) · 납음(천하수) · 구성기학(일백**수**성) — **셋이 독립적으로 "물"**. 합의.
- 형: 여덟 글자가 *"표현 자리 0"*인데 서양 태양·달이 **둘 다 공기**(말하고 옮기는 기운). 갈림.
  → 모순이 아니라 **"안에는 공기가 가득한데 나올 문이 안 났다"**로 합쳐졌다.
  **갈리는 자리가 그 사람의 급소를 가리킨다** — 이게 이 절의 상품성이다.
- 형제의 라이프패스가 **둘 다 6**. 여덟 글자는 거의 모든 것이 반대인데 이 수만 겹친다.

**검증** — 리포트 구조 검사(10항목 × 4단 = 40단, 「지금」 10, 타임라인 10, 판독기 13, 해부축 8) 통과.
넘침 0, 화면 오류 0. 판독기 27/27 PASS.

---

## 2026-08-14 · v116 — 각인 정직성: **유저가 아는 값을 맞히려 들지 않는다**

### 요청
창업자 실사용 제보. "팩트랑 다른 게 너무 많다. 나 키 178이고, 와이프는 170이야. 우리는 20대에 만났고.
이런 것들은 근거가 뭐였어? 평생 약한 곳은 간과 눈, 힘줄이라고 나오는데,
나는 사실 폐렴, 충수염, 당뇨 등 대사기관이 약한 편이야."

### 근거를 되짚은 결과 — 셋 중 둘은 버그, 하나는 근거 없음

**① 키 — 근거 없음. 내가 지어낸 표였다.**
`H_EL`(일간 오행 → ±cm) · `H_ASC`(상승궁 → ±cm)로 남 172 / 여 160 기준선에 보정을 얹고 있었다.
**어느 유파에도 오행을 cm로 환산하는 표준이 없다.** 각주에 "추정"이라고 적어 두긴 했지만
그 각주는 라이브에서 안 보인다 — 유저가 보는 건 「178~184cm」라는 굵은 글씨뿐이다.

**② 짝을 만나는 시기 — 상수를 증언으로 세고 있었다.**
"세 가지 셈이 전부 이 나이를 가리킨다"의 세 번째(서양)가 프로펙션 7하우스였는데,
프로펙션 7하우스는 `나이 % 12 == 6`, 즉 **상승궁이 무엇이든 누구나 6·18·30·42세**다.
24~40 필터를 통과하는 건 **언제나 30 하나뿐**. 게다가 최종 답을 그 격자에 스냅시켜서,
**200명을 돌리면 148명(84%)이 "30세"**로 나왔다(실측). 개인차 0인 값을 정족수에 넣은 것 —
정직성 판정(≤8년 합의) 자체가 무의미했다. 독립이 아닌 것을 표로 세면 합의가 아니라 조작이다.

**③ 약한 장기 — 축 하나만 봤다.**
`weakEl = 비어 있는 오행` 하나로 못 박았다. 그런데 명리 건강론에서 병은 **태과·불급 양쪽**에서 온다.
목 0개 → "간·눈"이라고 적었지만 실제 병력은 폐렴(금)·충수염·당뇨(토) — **넘치는 쪽**이었다.
코드에 `over`(태과) 필드를 이미 갖고 있으면서 `maxEl >= 4`일 때만 보조로 붙였다. 8글자에 4개는 드물다.

### 고친 것

**규칙 하나를 새로 못 박았다(파일 헤더 §⑤).**
> **유저가 정답을 아는 값은 맞히지 않는다. 받아서 해석한다.**
> 맞혀도 "그래서?"이고 틀리면 나머지 서른 칸까지 같이 죽는다. 비대칭이 너무 크다.

- **키**: `H_EL`/`H_ASC` 삭제. 선택 입력으로 받고, **안 적으면 키 얘기를 아예 안 한다.**
  받으면 통계 평균(질병관리청 국민건강영양조사 남 172·여 159) 대비로 해석한다.
- **짝의 시기**: 서양 축 제거 → 두 축(대운·다샤)만. 격자 스냅도 제거.
  후보가 하나뿐이면 "셈이 하나뿐"이라고 적는다. 분포가 22~45로 퍼졌다(최다 22세 33%).
- **약한 장기**: 세 축(비어서·넘쳐서·눌려서) 전부 계산. 겹치면 한 곳, 갈리면 둘 다 적고
  **"원인이 반대라 대처도 반대"**까지 쓴다. ⚠ **피극은 불급과 같은 자리를 가리키면 표를 안 더한다** —
  "금이 목을 친다"와 "목이 0개"는 같은 사실의 두 표현이지 증거 둘이 아니다.
  이걸 그냥 세면 목이 2표로 굳어 금(폐)이 본문에서 사라진다. ②와 정확히 같은 종류의 오류다.
- **기혼자 분기**: 배우자 외모·집안·벌이 **예언을 걷어냈다.** 기혼자에게 그건 예언이 아니라 오답 노출이다.
  대신 관계 해석으로 바꿨다 — 무엇에 끌렸나 / 잘 되는 지점 / 부딪히는 지점 / 조심할 것 / 흔들리는 때.
  `SPOUSE` 표에 `g`·`b` 필드를 새로 채웠다(`d`를 마침표로 잘라 쓰니 "안정적이다." 세 글자가 한 항목이 됐다).
- **선택 입력 2개 추가**: 만난 시기(기혼일 때만 뜸) · 키. 여전히 전부 선택이고 각인 열 때만 묻는다.
- 잔버그: "힘줄야"(조사), "…온다 사람이"(마침표 누락), "간과 눈는"(은/는),
  기혼자에게 **78~87세에 흔들린다**(지난 구간까지 세고 있었음 → 현재 이후·70세 이하만).

### 검증
각인 42/42(신규 16) · 리포트 65/65 · 스모크 25/25 · 판결 34/34 · 스코프 54/54 ·
v29 8/8 · WebGL 7/7 · 여러 하늘 12/12 · 검진 clean(신규 검사 1 추가).

새 검사 중 셋이 **이번 사고를 직접 겨눈다**:
- 「짝의 시기가 한 나이로 쏠리지 않는다」 — 120명 돌려 최다 나이가 50% 미만인지
- 「키를 안 주면 키 얘기를 안 한다」 — 본문에 `cm`이 한 글자도 없는지
- 「기혼에게 배우자 외모·집안·벌이를 안 쓴다」
검진에도 「지어낸 사실 — 키는 받아서 해석」을 추가해 `H_EL`/`H_ASC` 부활 시 즉시 울게 했다.

### 남은 것
`FACE`·`VOICE`·`BUILD`도 검증 가능한 주장이지만 **본인이 거울로 확인하는 값**이라
키만큼 치명적이지 않다고 보고 유지했다. 실사용에서 또 어긋나면 같은 규칙(§⑤)을 적용한다.

---

## 2026-08-14 · v117 — 각인 말투: **신은 사전을 읽지 않는다**

### 요청
창업자가 자기 판결문 전문을 붙여 놓고 물었다.
"판결문 보면 무슨 잘못이 있는 거 같아? 키 입력했더니 그게 답변에 저렇게 영향이 가면 믿음이 가겠어?
키가 179이기 때문에 주변의 이목을 끈다? 너무 전문성 없어 보이고 바보 같아. **신도 아닌 거 같아.**"

### 지적된 것 — 그리고 그게 맞은 이유

v116 에서 "키는 맞히지 말고 **받아서 해석**하자"로 바꿨는데, 나온 문장이 이랬다.
> "네가 적은 179cm는 또래 평균보다 7cm 큰 쪽이야. 서 있기만 해도 먼저 눈에 띄어…"

**이건 해석이 아니라 뺄셈이다.** 179 − 172 = 7. 유저가 준 값을 되돌려주고, 거기서 나온 말은
키만 아는 사람이면 누구나 할 수 있는 말이다. **받은 값이 값을 하려면 명식과 교차해서
유저가 모르던 걸 내놔야 하는데, 키에는 교차할 축이 없다.** → **키를 아예 없앴다.**
v115(지어내기) → v116(되읊기) → v117(안 다루기). 두 번 틀리고 나서야 자리를 찾았다.

### 말 안 하신 것 셋 — 이게 더 컸다

**① 말투가 한 문단 안에서 두 번 바뀐다.** ← "신도 아닌 거 같아"의 정체
> "너는 감정이 먼저 보이는 사람**이야**. 기분이 얼굴에 그대로 나온**다**."

감싸는 문장은 신의 반말("-야")인데 표에서 꺼낸 문장이 전부 사전체("-다")였다.
한 사람이 말하다 갑자기 사전이 끼어드는 소리로 들린다. **표 열두 개를 통째로 "-야/-어"체로 다시 썼다**
(SURFACE·INNER·BLOCK·THICK·ORGAN·FACE·VOICE·SPOUSE·H7·SS_EVENT·trig·checks — 54개 문자열).

**② 유년 구간에 어른의 사건을 쓰고 있었다.**
여든 해 지도에 **"5~14세 — 월급이 오르거나 계약이 길게 붙거나 집·차가 생긴다"**가 그대로 나갔다.
같은 뿌리로 돈 자리가 스물다섯 살 유저에게 **"돈이 도는 구간은 5~14세로 이미 지나갔어"**라고 결론지었다.
짝의 시기에는 18세 필터를 걸어 뒀으면서 여든 해 지도와 돈에는 안 걸었다.
→ `SS_CHILD` 표 신설(유년 구간 전용 문구), 돈 구간 계산에서 유년 제외,
   인도 시기 이름도 나이를 타게 했다(서른다섯 칸에 "어머니와 집이 세상의 전부인 시기"가 붙어 있었다).

**③ 같은 문장이 다섯 자리에 반복.**
"지금 열 해(25~34세)는 다른 쪽에 쏠려 있어."가 공부·일·돈·사람·연애 다섯 곳에 똑같이 나왔다.
같은 문장이 다섯 번 나오면 읽는 사람은 이게 조립품이라는 걸 즉시 안다.
→ 공통 앞머리를 없애고 자리마다 다른 문장으로 썼다.

### 그 밖의 잔버그(전부 화면에 실제로 나갔다)
- 그림 안 글자가 `slice(0,9)`로 잘려 **"감정이 먼저 보이"**에서 끝남 → 어절 단위 두 줄 배치
- 확인 문항에 **`<b>`가 날것으로 찍힘** — 그 칸은 순수 텍스트로 그려진다(`<p>{w}</p>`)
- **"셋 중"인데 실제로는 두 개** 표시 · **"간과 눈, 그리고 힘줄가"** · **"…거래가 곧 실력인 일야"**
- **"요즘이 닫혀 있던 말이 나오는 같은가"** — `title.replace(" 열 해","")`가 문장을 부숨
- 기혼자의 연애 「지금」이 **"저절로 오지 않아"** — 이미 만난 사람에게 할 말이 아니다
- 기혼자의 자식 「자라면서」가 "흔들린 적이 있다면" — 아이가 이미 있는데 조건문

### 검증
각인 46/46(신규 4묶음) · 리포트 65/65 · 스모크 25/25 · 판결 34/34 · 스코프 54/54 ·
v29 8/8 · WebGL 7/7 · 여러 하늘 12/12 · 검진 clean

**말투 검사(⑫)가 이번 사고의 핵심 장치다.** 화면에 나가는 모든 문장을 모아
사전체 종결(-ㄴ다/-이다/-한다/…)로 끝나는 게 하나라도 있으면 운다.
표에 새 문구를 넣다가 무심코 "-다"로 끝내면 즉시 잡힌다 — 이게 없으면 반드시 되돌아온다.
검진에도 두 검사를 넣었다(키 부활 감지 · 말투 규칙과 유년 표 생존).

### 남은 것
- 「지금」 칸에서 「마음」과 「말」이 같은 열 해 이름을 두 번 부른다. 문장은 다르지만 겹쳐 읽힌다.
- `FACE`·`VOICE`·`BUILD`는 여전히 검증 가능한 주장이다. 거울로 확인하는 값이라 키만큼
  치명적이지 않다고 보고 뒀다 — 실사용에서 어긋나면 키와 같은 길을 간다.

---

## 2026-08-14 · v118 — 아홉 하늘: **사주는 아홉 중 하나다**

### 요청 (창업자, 다섯 갈래)
① "'네게는 같이 갈 사람이 얇아' < 뭔 말이야? 이런 비문은 쓰지 마. 은유 같지도 않고 비유 같지도 않고.
   내가 구체적이고 직관적으로 적어달라 했잖아" ② "생김새와 몸은 왜 두 번 써줘"
③ "태어난 장소는 판결에 영향 없나?" ④ "그냥 사주 내용이랑 꼭 같은데,
   저번에 글로벌로 찾은 생년월일로 운명을 점치는 방법들은 **적용이 된 거야 만 거야?**"

### ④ 먼저 — 감사 결과: **거의 안 됐다**

sky.js 에 판독기가 **열한 개** 있는데 각인 본문에 영향을 준 건 **셋**뿐이었다.

| 판독기 | v117 까지의 실상 |
|---|---|
| 서양 해·달자리 | 「속」 한 줄 (원소만) |
| 상승궁 | 미혼일 때 짝의 인상 |
| 인도 다샤 | 여든 해 지도의 곁말 |
| 마야 촐킨 · 나크샤트라 | **각주 한 줄 장식** |
| 자바 웨톤 · 아칸 · 태국 요일 · 구성기학 · 납음오행 · 라이프패스 · 달 위상 | **한 번도 호출 안 됨** |

계산해 두고 안 쓰면 없는 것과 같다. 사주가 아홉 중 하나가 아니라 **전부**였으니
읽으면 사주로 읽힐 수밖에 없었다. 창업자 체감이 정확했다.

### 새 절 — 「아홉 하늘이 뭐라고 하는가」

동아시아 여덟 글자 · 납음오행 · 구성기학 · 자바 웨톤 · 서양 해자리 · 마야 촐킨 ·
나크샤트라 · 아칸 요일이름 · 수비학 — **아홉이 각각 자기 값을 내고, 다섯 낱말 중 하나로 증언한다.**
겹치면 확정, 갈리면 그게 급소. **사주는 아홉 중 하나로만 세어진다.**

지어내지 않기 위한 장치 셋:
1. **다섯 낱말은 오행 그대로다** — 목·화·토·금·수를 행동으로 옮긴 것(먼저 움직이는 / 드러내고 말하는 /
   버티고 기다리는 / 자르고 정리하는 / 맞추고 스며드는). 새 어휘를 만들지 않았다.
2. **변환이 없는 것과 해석이 섞인 것을 구분해 각주에 적는다.** 납음·구성기학은 이름에 오행이
   들어 있어 변환이 아예 없고, 자바 파사란은 전통이 이미 다섯 방위에 배당해 두었다.
   마야·나크샤트라·아칸·수비학은 각 전통의 성격 서술을 옮긴 것이라 **"해석이 섞인다"**고 명시한다.
3. **합의 조건을 못 박았다** — 넷 이상 + 2위와 두 표 이상 차이일 때만 "겹친다"고 쓴다.
   3:3:3 을 1위라고 부르면 그건 합의가 아니라 반올림이다(짝의 시기에서 배운 것과 같다).

그리고 **여덟 글자만 다수와 갈리면 그 사실을 따로 적는다** —
"사주만 보고 판단한 너와 진짜 너가 다를 수 있다는 뜻이야." 이게 이 절의 가장 값진 한 줄이다.

### ① 비문
`BLOCK.t` 가 "같이 갈 사람"·"말이 나갈 문"·"쥐는 손" 같은 명사구였고, 본문에서
"네게는 **같이 갈 사람이 얇아**"로 조립됐다. 사람이 얇을 수 없고 문이 얇을 수 없다.
뜻은 안 전해지고 폼만 잡은 문장이다. → `t`(제목용 한 낱말)와 `plain`(뜻이 바로 서는 완결 문장)으로 나눴다.
"사람 — 네게 가장 약한 자리 / **무리에 잘 못 섞여. 혼자 하는 게 빨라.**"

### ② 중복
「생김새와 몸」의 '평생 약한 곳'과 「몸 — 어디가 약하게 왔나」 4단이 같은 말을 두 번 했다.
같은 내용을 두 번 쓰면 두 번째가 첫 번째를 무효로 만든다.
→ 「생김새」는 겉모습만(몸·얼굴·목소리), 건강은 4단이 전담.

### ③ 태어난 장소 — **절반만 쓰고 있었다**
`readImprint` 에 **경도만 넘기고 위도는 서울 고정**이었다. 상승궁은 위도로 갈리므로
제주(33.5°)에서 태어난 사람이 서울(37.6°) 값을 받고 있었다. 물어 놓고 절반만 쓴 셈이다.
→ `CITY_LAT` 25개 도시 추가, `lat: cityLat(birth.city)` 연결. 실측 상승궁 이동 2.5도.
⚠ 검사는 **자리 이름이 아니라 각도**로 해야 한다 — 위도 차 4도는 상승궁을 2~3도 움직이는데
   자리 폭이 30도라 대개 같은 자리에 머문다. 이름으로 검사하면 통과해 버린다(처음에 그랬다).

### 검증
각인 62/62(+16) · 리포트 71/71(+6) · 스모크 25/25 · 판결 34/34 · 스코프 54/54 ·
v29 8/8 · WebGL 7/7 · 여러 하늘 12/12 · 검진 clean(+1)

새 검사가 지키는 것: 아홉이 전부 증언하는가 · 사주가 가중치 없이 하나로만 세어지는가 ·
해석이 섞인 문명이 그렇다고 밝히는가 · 60명 중 40종 이상 다른 조합이 나오는가 ·
합의율이 0%도 100%도 아닌가 · 비문이 없는가 · 위도가 실제로 각도를 움직이는가.
검진에도 「판독기 8종이 본문에 물려 있음」을 넣어, 다시 장식으로 돌아가면 울게 했다.

### 남은 것
- 달 위상·태국 요일·하압·아쉬타쿠타는 아직 안 물렸다. 달 위상은 다섯 낱말 축에 안 맞고
  (차오름/기움은 다른 축이다), 아쉬타쿠타는 상대가 있어야 한다 — 궁합 기능이 생길 때 붙인다.
- 아홉 중 넷(마야·인도·아칸·수비학)은 해석이 섞인다. 각주에 밝혔지만,
  실사용에서 "이 문명 해석이 틀렸다"는 지적이 나오면 그 표부터 고친다.

---

## 2026-08-14 · v119 — 하늘의 분업: **투표를 시키면 사주가 된다**

### 요청
"여전히 사주 베이스처럼 느껴지고 여러 운세 방식은 유효하지 않은 거 같아.
아 뭔가 **새롭다는 느낌보다 그냥 사주인데 굳이 쟤네 왜 붙였지** 싶어.
**기존 사주와 다르게 해석되는 부분도 알려줘.** 리포트 문자 수가 제한되어 있어?"

### v118 이 실패한 이유 — 셋

**① 다섯 낱말을 오행으로 잡았다.** 아홉 문명을 목·화·토·금·수로 번역했으니
**다른 하늘을 사주의 렌즈로 읽은 것**이다. 당연히 사주로 읽힌다. "지어내지 않으려고"
공용어를 오행으로 골랐는데, 그 선택 자체가 문제였다.

**② 아홉 표를 다섯 칸에 뿌리면 무승부가 흔하다.** 창업자 판결문에 **3:3:3** 이 나왔다.
"이게 네 급소야"라고 써 놓고 아무것도 못 가리켰다. 판별력이 없는 설계였다.

**③ 결정적 — 세어 놓고 그 결과로 아무것도 안 했다.** 나머지 본문은 100% 사주였다.
투표 결과는 독립된 상자였고 한 문장도 바꾸지 않았다. **그래서 부록으로 읽혔다.**

### 고친 방향: 투표 → **분업**

각 하늘이 **사주가 못 하는 질문 하나씩**을 맡는다. 같은 질문에 투표시키지 않는다.

| 하늘 | 맡은 질문 | 사주에 있나 |
|---|---|---|
| 서양 | 인생의 무게가 어디에 실렸나(열두 자리) | **없음** |
| 인도 | 시간의 마디가 사주와 어디서 어긋나나 | 주기가 다름(120년 불균등 vs 10년 균등) |
| 마야 | 태어난 날이 맡은 역할 | 층이 다름 |
| 일본 구성기학 | 어느 쪽으로 움직여야 풀리나(방위) | **없음** |
| 자바 | 태어난 날이 얼마나 무거운가(신붕 수) | **없음** |
| 달의 위상 | 시작에 강한가 마무리에 강한가 | **없음** |
| 서아프리카 아칸 | 요일이 준 이름과 역할 | 없음 |
| 수비학 | 평생 배우는 게 무엇인가 | 없음 |
| 동아시아 소리(납음) | 여덟 글자와 소리가 같은 말을 하나 | 사주 안의 다른 층 |

**「사주와 다르게 읽히는 곳」 절을 새로 만들었다**(창업자 직접 요청). 다섯 갈래:
겉과 속 · 글자와 소리 · 시간의 마디 · 겹치는 마디 · 사주에 아예 없는 것.
⚠ 어긋남이 없으면 **없다고 쓴다** — 억지로 만들면 그게 더 나쁘다.
겉과 속이 같은 사람에게는 그 항목을 아예 안 낸다(검사로 못 박음).

**가장 값진 산출물은 시간축이다.** 사주 대운은 열 해 균등, 인도 다샤는 120년을 아홉으로 불균등.
그래서 **두 전환표를 맞대면 사주만으로는 안 나오는 값**이 나온다 —
겹치는 나이(= 진짜 전환기)와 다샤만 바뀌는 나이(= 겉은 조용한데 속이 뒤집히는 해).
이건 계산 그대로라 해석이 안 섞인다.

### 근거의 급을 아홉 전부에 붙였다
**계산 그대로**(서양 하우스·인도 전환표) / **변환이 없다**(구성기학 팔괘 정위·자바 신붕·아칸 이름·납음) /
**해석이 섞인다**(마야 톤 뜻·달 위상 성향·수비학 과제). 급을 안 밝히면 지어낸 것과
옮긴 것이 같은 목소리로 나간다. 검사가 아홉 전부에 정확히 하나씩 붙는지 본다.

### 결과가 본문을 바꾸게 했다
여든 해 지도의 각 구간에 `doubleTurn`(두 셈이 같이 바뀌는 해 ◆)과
`dashaOnly`(사주는 조용한데 인도 셈만 바뀌는 구간) 표식을 얹었다.
이제 아홉 하늘이 사주 절을 **실제로 흔든다.** 세어만 놓지 않는다.

### 문자 수
**상한은 없다.** 회귀 검사에 4,000자 **하한**만 있다(값어치 두께 보장용).
v119 는 그보다 훨씬 길다 — 자를 이유가 없어서 안 잘랐다.

### 검증
각인 65/65 · 리포트 72/72 · 스모크 25/25 · 판결 34/34 · 스코프 54/54 ·
v29 8/8 · WebGL 7/7 · 여러 하늘 12/12 · 검진 clean

새 검사가 지키는 것: **아홉이 서로 다른 질문을 맡는가**(투표로 되돌아가면 운다) ·
사주에 없는 축을 실제로 채우는가 · 아홉 전부가 근거의 급을 밝히는가 ·
어긋남이 없을 때 안 쓰는가 · 60명 중 50종 이상 다른 값이 나오는가 ·
**결과가 여든 해 지도를 바꾸는가**.

### 남은 것
- 태국 요일·하압·아쉬타쿠타는 아직 안 물렸다. 아쉬타쿠타는 상대가 있어야 하므로 궁합 기능 때.
- 「지금·앞으로」 4단은 여전히 사주 대운이 단독으로 굴린다. 인도 다샤를 여기까지 물리면
  자리마다 "두 셈이 뭐라고 하나"가 붙는데, 문장이 두 배로 늘어 읽기가 무거워질 위험이 있다.
  다음 실사용 피드백을 보고 판단한다.

---

## 2026-08-14 · v120 — 빙빙 안 돌린다: **모호한 건 신비가 아니라 회피다**

### 요청
"심리상담처럼, 철학관처럼 명확하게 좀 말해줘 리포트에서.
**도대체 어떤 프롬프트가 들어갔길래 직관적인 말도 빙빙 둘러서 무슨 그릇이 어떻니 저떻니 그래?**
내가 말했지만 **신의 컨셉은 말을 모호하게 하란 게 아니고 말투나 비주얼을 말한 거야.**"
(그리고 "전략 세션에서 쓴 브랜치 읽혀?" — `claude/goal-validation-feasibility-u2b9ob`, 읽힌다)

### 먼저 사실관계 — **프롬프트가 아니다**
`imprint.js` 의 API 호출 **0회**. 각인은 LLM 을 쓰지 않는다.
서신(4,900원)은 프롬프트지만 각인은 **전부 이 파일의 하드코딩 표**다.
"그릇이 어떻니"는 프롬프트가 만든 게 아니라 **내가 표에 그렇게 써 넣은 것**이다.
→ 검사 ㉑ 로 못 박았다: 각인 엔진에 `fetch(`·`/api/` 가 없어야 한다.
   문장이 이상하면 프롬프트가 아니라 **표를 고쳐야 한다**는 사실을 코드가 기억하게 했다.

### 진단 — 세 가지 회피 습관

**① "자리가 N개"** — "재물 자리가 3개로 두꺼운데 너를 받치는 힘은 1개야."
유저는 3개가 많은지 적은지 모르고, 안다 해도 뭘 해야 할지 모른다. **내부 셈을 화면에 흘린 것**이다.

**② 은유를 서술어로 썼다** — 그릇 · 팔 힘 · 결 · 문 · 기운.
"돈은 보이는데 쥘 팔 힘이 모자라는 그릇이야" 는 아무 말도 안 한 문장이다.

**③ 관형형을 잘라 쓴 자리** — `title.replace(" 열 해","")` 가
"혼자 파고드**는**에 힘이 쏠려 있어서" 를 만들었다. → `bandName()` 으로 "시기"를 붙인다.

### 고친 규칙 (파일에 못 박음)
> **① 실제로 벌어지는 상황 → ② 그때 네가 하는 행동 → ③ 그래서 생기는 결과** 순으로 쓴다.
> "자리가 N개"를 쓰지 않는다. 은유를 서술어로 쓰지 않는다.
> **상담실도 철학관도 단정적으로 말한다. 모호한 건 신비가 아니라 회피다.**

전후:

| 전 | 후 |
|---|---|
| 재물 자리가 3개로 두꺼운데 너를 받치는 힘은 1개야. 돈은 보이는데 쥘 팔 힘이 모자라는 그릇이야 | **너는 돈 벌 기회를 남보다 많이 만나. 그런데 그걸 감당할 체력과 사람이 부족해.** 큰 건이 눈앞에 와도 혼자 다 하려다 몸이 상하거나, 같이 하던 사람과 틀어지면서 놓쳐 |
| 같이 갈 자리가 비었어 | **여럿이 하는 걸 못 견디는 사람이야.** 회의가 길어지면 속으로 '내가 하고 말지' 하고, 실제로 그렇게 해. 그래서 일은 되는데 사람이 안 남아 |
| 인연을 맡은 자리가 3개야 | **사람이 끊긴 적은 없을 거야.** 문제는 늘 고르는 쪽이었어. 정하는 데 오래 걸리고, 정하고 나서도 '저쪽이 나았나' 싶은 마음이 남아 |
| 굴려서 남기는 쪽으로 새겨졌어 | **사고팔고 연결해서 차액을 남기는 일이 맞아.** 구체적으로는 영업·유통·중개·자영업 쪽이야 |
| 앉아서 외우는 쪽은 아니야 | **앉아서 외우는 건 안 맞아.** 강의 결제해 놓고 안 듣는 일이 반복됐다면 게을러서가 아니라 **방식이 안 맞아서**야 |
| 도와달라고 말하는 법 | **"도와줘"라고 먼저 말하는 것.** 딱 이 한 마디야 |

열 해 이름도 전부 구체화했다 — "손에 익은 걸로 사는 열 해" → "잘하는 걸로 먹고사는 열 해",
"닫혀 있던 말이 나오는 열 해" → "네 이름 걸고 내놓는 열 해".

### 검증
각인 69/69(+4) · 리포트 72/72 · 스모크 25/25 · 판결 34/34 · 스코프 54/54 ·
v29 8/8 · WebGL 7/7 · 여러 하늘 12/12 · 검진 clean

**검사 ⑳ 이 이번 사고의 재발 장치다.** 화면에 나가는 모든 문장을 모아
금칙 은유 6종(그릇 · 팔 힘 · 자리가 N개 · 결 · 문 · 기운이 흐른다)을 정규식으로 잡는다.
표에 새 문구를 넣다가 은유로 도망가면 즉시 운다. 아홉 자리의 「새겨질 때」가
전부 25자 이상 단정문인지도 함께 본다("~일 수도"·"아마"·"것 같아" 금지).

### 남은 것
- 「자라면서」 칸 일부가 여전히 "~했을 거야" 추정형이다. 지난 일은 확인이 안 되니
  단정하면 틀렸을 때 문서가 죽는다 — 의도한 것이지만, 네 칸 중 한 칸이 계속 추정형이면
  전체 톤이 약해 보일 수 있다. 실사용 반응 보고 판단한다.

---

## 2026-08-14 · v121 — 판결도 안 돌린다: **모델은 규칙보다 예시를 따른다**

### 요청
"이거 판결문도 적용되어 있나? **판결도 되게 애매모호할 때 많던데.**"
(그리고 "「자라면서」 추정형은 일단 남겨" — 유지)

### 감사 결과 — 절반만 적용돼 있었다

**앞면(verdict, 45자)에는 규칙이 이미 강하게 있었다**: 일반론 금지 · 은유 없이 직답 ·
행동 명확성("가을에 다시 던져" 금지) · 지표 정박 · 출력 직전 자가점검 · 회피 금지.

**그런데 뒷면(subline·reasons)은 은유를 허용하고 있었다.** "뜻→은유 순서"만 요구했다.
**앞면만 조이면 모호함은 뒷면으로 숨는다.** 유저가 실제로 오래 읽는 건 뒷면이다.

### 진짜 원인 — **프롬프트가 자기 예시로 자기 규칙을 어기고 있었다**

프롬프트에 이 한 줄이 **모범 예시(O)** 로 박혀 있었다:
> (O)"편재 둘에 암록까지 — 크게 들어오는 재물의 **그릇**이야"

한 줄에 위반이 둘이다.
① **편재·암록**은 v112 에서 만든 [용어 금지] 목록의 말이다.
② **"그릇"** 은 읽고 나서 아무것도 모르는 은유다 — 각인에서 방금 걷어낸 바로 그 단어다.

**모델은 금지 조항보다 (O) 예시를 강하게 따른다.** 금지를 열 줄 써 놔도 예시 한 줄이 이긴다.
"그릇이 어떻니"의 진원지가 여기였다. 예시를 고쳤다:
> (O)"돈이 크게 들어오고 크게 나가는 쪽이야. 벌 땐 몰아서 벌어."
> (X)"편재 둘에 암록까지 — 크게 들어오는 재물의 그릇이야"(용어 노출 + 뜻이 안 서는 은유)

「근거 구체성」의 예시도 같은 병이었다 — (O)"**다섯째 자리**가 지금은 물러서랬어" 를
(O)"지금 손볼 건 **사람 문제**야 — 힘은 있는데 순서가 틀렸어" 로 바꿨다.

### 뒷면에 v120 규칙을 이식
㉠ 뜻이 안 서는 은유를 서술어로 쓰기 ㉡ 개수만 던지기("재성이 셋이야") ㉢ 추상명사로 도망가기
㉣ 판정 유예 어미("~일 수도"·"두고 봐야") — 넷 다 금지.
쓰는 순서: **실제 상황 → 그때 하는 행동 → 그래서 생기는 결과.** 은유는 그 뒤 한 조각까지만.

### 검사 둘을 새로 걸었다

**① 정적 — 프롬프트가 자기 예시로 자기 금칙을 어기는가**(검진 5-g)
SYS 프롬프트에서 `(O)"..."` 형태 예시를 **전부 뽑아**(현재 23개) 금칙 은유가 들어 있는지 본다.
규칙이 아니라 **예시를 검사한다** — 이 사고의 정확한 형태가 그거였으니까.
API 키 없이 배포 전에 돌아간다.

**② 실측 — 평가 하네스의 모호함 축**(eval/run-eval.mjs)
지금까지 자동검사는 **앞면만** 봤다. `--full` 로 받은 subline·reasons 도 같은 잣대로 잰다:
은유서술어 · 판정유예 · 개수만. 호출 순서를 바꿔 뒷면을 받은 뒤 한 번에 채점하게 했다.

⚠ **e2e/verdict.mjs(34건)는 모형 응답으로 배선만 본다** — 모델이 실제로 뭐라고 쓰는지는
평가 하네스만 안다. 그래서 프롬프트를 손대면 `node eval/run-eval.mjs --full --via=...` 를
한 번 돌려야 실제 문장이 확인된다(이번엔 안 돌렸다 — 키가 Vercel 에만 있고 프록시가 막혀 있다).

### 검증
각인 69/69 · 리포트 72/72 · 스모크 25/25 · 판결 34/34 · 스코프 54/54 ·
v29 8/8 · WebGL 7/7 · 여러 하늘 12/12 · 검진 clean(+1)

### 남은 것 (창업자 확인 필요)
프롬프트 수정은 **실모델로 확인하지 않았다.** 평가 하네스를 한 번 돌려야 한다 —
`ANTHROPIC_API_KEY=... node eval/run-eval.mjs --full` 또는 `--via=https://binari-sepia.vercel.app`.
27문항×5인 기준 20분 안팎, 약 $1 미만. 돌리면 모호함 플래그가 CSV 에 그대로 뜬다.

---

## 2026-08-14 · v122 — 그림과 뎁스: **어긋남을 한 절에 가둬 두면 부록이 된다**

### 요청 (다섯)
"리포트 **너무 글의 나열**이야. **그림**도 있으면 좋겠고, **모션**도 있으면 좋겠어.
처음에 사주에서 못 읽는 부분을 말해주는 건 신기한데, **그게 밑에까지는 영향이 안 가는** 거 같네.
**생김새는 어디 근거가 있는 거야?**
사람들이 궁금해할 만한 내용에 대한 뎁스를 말해줬는데 적용이 안 되어 있어 —
**돈을 벌면 언제 얼마나 뭘 해서, 그걸 벌기까지의 여정 그래프**도 있으면 좋고,
**연인을 만나면 언제 어느 방향 뭐하는 사람 생김새 성격 취향 연봉 집안 궁합 자녀 결혼**.
그리고 이 리포트는 나중에 **다른 사람의 생년월일을 넣으면 궁합** 혹은 그 이상."

### ③ 생김새의 근거 — 답
**오행 체상론(五行體相)** 이다. 사람을 목·화·토·금·수 다섯 형(形)으로 나눠 보는 관상 계열 전통
(『신상전편』 등)으로, 실재하는 유파다. **그런데 우리 구현은 축소판이다** —
정통 체상론은 일간·월령·격국을 함께 보는데 우리는 **일간과 최다 오행 둘만** 쓴다.
그리고 **맞았는지 검증한 적이 없다.**

각주가 "일간 오행 목의 상(相)" 한 줄뿐이라 계산값과 같은 급으로 읽혔다. 세 가지를 다 적게 고쳤다:
유파 이름 · 축소판이라는 사실 · 미검증이라는 사실. 그리고 **최다 오행 보정을 실제로 얹었다**
(v121 까지는 각주에만 언급하고 문장에는 안 썼다).
키(cm)를 뺀 것과 같은 이유로 여기도 숫자는 안 쓴다.

### ② 어긋남이 밑까지 안 간 이유 — 맞는 지적
어긋남을 **「사주와 다르게 읽히는 곳」 한 절에 모아 두기만** 했다. 아홉 자리는 여전히 사주 단독이었다.
→ 자리마다 **그 영역을 서양이 어떻게 보는지** 한 줄을 붙였다. 대응이 깔끔하게 떨어진다:
몸1 · 말3 · 마음4 · 자식5 · 연애7 · 공부9 · 일10 · 사람11 · 돈2 하우스.
- 태양이 그 하우스 → "**서양 셈도 여기를 인생의 중심으로 봐. 두 셈이 겹쳐**"
- 달 → "마음이 쉬는 곳", 파트오브포춘 → "복이 도는 자리"
- 아무것도 없음 → "**서양 셈으로는 조용해. 사주가 더 무겁게 보는 영역**"

이제 어긋남이 문서 전체에 퍼진다. 검사가 **겹치는 자리가 사람마다 옮겨 다니는지**까지 본다.

### ④ 돈의 여정 — 그래프 + 구간별 데이터
여든 해를 재물 관점 −3~+4 로 매겨 **곡선**으로 그렸다(면적 채움 + 절정·바닥·지금 표식 + 그려지는 모션).
구간마다 **"뭘 해서 버나"** 를 붙였다 — "월급·고정 계약처럼 끊기지 않는 돈",
"사업·투자·중개처럼 크게 오가는 돈", "벌어도 새는 돈 — 보증·동업·빌려주기".

⚠ **금액은 안 쓴다.** 명리 어디에도 원 단위 표준이 없다 — 키(cm)를 뺀 것과 같은 이유다.
"얼마나"는 남과의 비교가 아니라 **네 여든 해 안에서 언제가 제일 두꺼운가**로 답한다.
그건 계산에서 실제로 나온다. 검사가 `\d+(원|만원|억)` 를 잡는다.

### ④ 짝의 뎁스 — 열 항목 → **열넷**
새로 넷: **어느 쪽에서 오나**(십이지 방위 배당 — 변환 없음) · **뭐하는 사람**(일지 십성의 직업군) ·
**취향**(짝의 자리 별자리 원소) · **둘이 섞이는 결**(일간 vs 일지 오행 생극 — 변환 없음).
연봉은 "고정급인지 성과급인지, 너보다 많이 버는 쪽인지"까지만 — **숫자는 안 찍는다.**
「둘이 섞이는 결」은 기혼자에게도 준다(관계 해석이라 안전하다).

### ① 그림과 모션
그래프가 셋 → **다섯**. 새로: **돈의 여정 곡선**, **방위 나침반**(막힐 때 움직일 쪽 · 짝이 오는 쪽 — 사주에 거의 없는 축이라 그림 값어치가 크다).
모션: 절·그래프가 올라오며 나타나고(`impRise`), 돈 곡선은 **그려진다**(`stroke-dasharray` draw-on).
`prefers-reduced-motion` 을 존중한다 — 검사가 그것까지 본다.

### ⑤ 궁합(상대 생년월일) — 이번엔 안 했다
창업자가 "**나중에**"라고 명시했다. 다만 재료는 이미 다 있다 — `sky.js` 의 `ashtakuta`(36점 8항목)가
계산까지 되어 있고 지금 한 번도 안 쓰인다. 붙일 때 필요한 건 계산이 아니라 **화면과 입력**이다.
「둘이 섞이는 결」을 이번에 넣은 건 그 자리를 미리 열어 둔 것이기도 하다.

### 검증
각인 **97/97**(+28) · 리포트 **79/79**(+7) · 스모크 25/25 · 판결 34/34 · 스코프 54/54 ·
v29 8/8 · WebGL 7/7 · 여러 하늘 12/12 · 검진 clean

새 검사: 아홉 자리 전부에 서양 시선이 붙는가 · 자리마다 같은 말만 하지 않는가 ·
겹치는 자리가 사람마다 옮겨 다니는가 · 돈 곡선이 사람마다 다른가(40명 중 6종+) ·
금액을 지어내지 않는가 · 짝의 아홉 항목이 실재하는가 · 생김새 각주가 유파·축소판·미검증을 밝히는가 ·
그림이 다섯 이상인가 · 모션이 접근성 설정을 존중하는가.

---

## 2026-08-14 · v123 — 너의 이야기: **신화는 모호하지 않다**

### 요청
"혹시 **동화나 영화처럼** 읽히게 할 수 있을까? **신화**나."

### 먼저 — 지난 여섯 판과 충돌하는가? 아니다

지난 여섯 판이 **모호함을 걷어내는 작업**이었다("그릇이 어떻니"·"빙빙 둘러서"·
"신의 컨셉은 말을 모호하게 하란 게 아니고 말투나 비주얼"). 그런데 이번엔 신화다.
되돌리는 것처럼 보이지만 아니다 —

> **신화는 모호하지 않다.** "오디세우스는 십 년을 떠돌았다"는 정확한 문장이다.
> 신화가 주는 건 모호함이 아니라 **배열**이다: 결핍에서 열고, 시련을 지나, 관문에서 갈리고, 돌아온다.

그래서 **바꾼 건 문장이 아니라 순서**다. 검사도 둘을 같이 본다 —
서사 구조가 서는가, **그리고 문장이 여전히 구체적인가**(⑳의 금칙 은유를 서사에도 그대로 적용).

### 바꿀 데이터가 없었다 — 이미 이야기 모양이었다

| 신화의 자리 | 우리가 이미 갖고 있던 값 |
|---|---|
| 결핍 | 가장 빈 자리(BLOCK) |
| 장(章) | 대운 여덟 구간 |
| 관문 | **두 셈이 겹치는 해**(대운 전환 ≈ 다샤 전환) |
| 숨은 마디 | 다샤만 바뀌는 구간 — 겉은 조용한데 속이 뒤집히는 때 |
| 시련 | 배우자 자리를 치는 구간 |
| 보물 | 재물 곡선의 절정 |
| 조력자 | 인성 구간 |
| 이름 | **납음 60갑자** — "검봉금 = 칼끝에 서린 쇠". 이름 자체가 이미 이미지다 |

새 계산은 **0건**이다. 각주에 "다시 배열했을 뿐"이라고 적고, 검사가 장의 사건이
여든 해 지도와 **같은 값인지** 대조한다.

### 화자도 이미 있었다
"-야"체 반말을 유지하면 그대로 **수호신이 들려주는 이야기**가 된다.
문어체("…한 아이가 왔다")로 갔으면 톤이 깨졌을 텐데, 그럴 필요가 없었다.

> **1995년 가을, 네가 왔어.** 그해에 붙은 이름은 **산두화** — **산꼭대기의 불**이라는 뜻이야.
> 다만 셈이 어느 쪽으로 갈리든 **한 가지가 처음부터 비어 있었어 — 돈이 손에 안 남아.**
> **이건 그 빈 하나를 두고 벌어지는 여든 해의 이야기야.**
>
> 제3장 · 25~34세 — **혼자 굴을 판 장**  ◂ 여기
>   [조력자] 누군가 너를 끌어 주는 장이야. 혼자 힘으로 넘은 게 아닌 구간이야
>
> …너는 지금 **제3장**에 서 있어. 남은 장이 **5개**야.
> 처음부터 비어 있던 그 하나는 끝까지 안 채워져. **영웅이 되는 건 결핍을 없애서가 아니라 그걸 안고도 걸어서야.**

### 영화처럼 — 여정 지도
굽이치는 길에 여덟 장을 노드로 찍었다. **지나온 길은 이어진 선, 앞으로는 점선**,
지금 서 있는 자리가 **맥동**한다. 표식은 글자가 아니라 기호로 — ◆관문 ✦보물 ▲시련 ◇조력자 ◉숨은 마디.
선이 순차로 나타난다(단계별 지연). `prefers-reduced-motion` 존중.

### 배치 — 이야기가 앞, 근거가 뒤
겉·속 다음에 바로 이야기를 두고, 아홉 하늘·아홉 자리·짝은 그대로 뒤에 남겼다.
**이야기가 훅이고 나머지가 근거**다. 둘 중 하나를 고르게 하지 않았다 —
읽고 싶은 만큼만 내려가면 되니까.

### 잡은 버그
서사가 **관문 표식을 하나도 못 받고 있었다.** 두 셈의 시간 마디를 아홉 하늘 절 안에서
계산하는데 서사가 그보다 **위에** 있었다. 계산을 `bands` 직후로 끌어올렸다.
→ 교훈: **쓰는 곳보다 먼저 계산한다.** 검사가 "관문 표식은 두 셈이 겹치는 장에만 붙는다"로 잡았다.

### 검증
각인 **112/112**(+15) · 리포트 **84/84**(+5) · 스모크 25/25 · 판결 34/34 ·
스코프 54/54 · v29 8/8 · WebGL 7/7 · 여러 하늘 12/12 · 검진 clean

새 검사 중 둘이 중요하다:
- **「이야기도 모호함으로 도망가지 않는다」** — 서사라고 봐주지 않는다. ⑳의 금칙을 그대로 건다
- **「장의 사건이 여든 해 지도와 같은 값이다」** — 서사가 새 값을 지어내면 즉시 운다

### 남은 것
장 제목(CH_TITLE)은 십성 열 개에 하나씩이라 **같은 십성이면 같은 제목**이 나온다.
30명 중 8종 이상이면 통과하게 해 뒀지만, 사람이 늘면 겹쳐 보일 수 있다.
나이대·표식을 섞어 변주하는 건 다음 판에.

---

## 2026-08-14 · v124 — 직장생활: **직업 선택과 조직 적응은 다른 축이다**

### 요청
"직장생활도 봐주면 어때. **일 스타일, 잘하는 것, 잘 맞는 사람, 어느 위치,
얼마나 오래 다니고, 언제 이직하고** 등등."

### 왜 「일」 한 칸으로는 부족했나
기존 「일 — 어떤 일이 맞나」는 **직업 선택**의 축이다(무슨 일을 하느냐).
직장은 다른 축이다 — **조직 안에서 어떻게 굴러가느냐.**
**맞는 직업을 골라도 조직에서 못 버티는 사람이 있다.** 그래서 절을 따로 뒀고,
검사가 두 절의 문장이 겹치는지 실제로 대조한다(겹치면 둘 중 하나가 죽는다).

### 아홉 항목
| 항목 | 무엇으로 보나 |
|---|---|
| 일 스타일 | 일간 오행의 업무 성정 |
| 잘하는 것 | 가장 두꺼운 십성 무리 |
| **안 되는 것** | 가장 빈 자리 — **못하는 걸 아는 게 더 쓸모 있다.** 피할 수 있으니까 |
| 어느 위치까지 | 관성 + 비겁 → 실무 / 중간 / 리더 / 독립 |
| 잘 맞는 상사 | 나를 누르는 자리(정관·편관·없음) |
| 같이 일하면 좋은 사람 | 오행 상생 — 나를 낳는 쪽이 밀어 주고, 내가 낳는 쪽에 쏟는다(변환 없음) |
| 한 곳에 얼마나 | 관성·비겁·식상 배치 → 안정(5년+) / 보통(3~5년) / 이동(2~3년) |
| 서양이 보는 네 자리 | **중천(MC) 사인** — 사주가 "어떻게 일하나"라면 이건 "어디서 이름이 나나". 축이 달라 안 겹친다 |
| 번아웃 신호 | 가장 빈 자리가 **직장에서 드러나는 형태** → 가장 약한 장기 |

번아웃은 별도 문장을 새로 썼다. 처음엔 `BLOCK.s`를 갖다 썼는데
재성이 빈 사람에게 "지나가는 건 보이는데 남는 게 없어"가 번아웃 신호로 나왔다 — 그건 돈 얘기지 번아웃이 아니다.
직장 맥락 전용으로 다섯 개를 새로 썼다: "점심을 혼자 먹기 시작하면 그게 신호야" 식.
**압력은 빈 자리에서 시작해 얇은 자리(몸)로 간다** — 순서를 알면 앞에서 끊을 수 있다.

### 이직 시기 — 두 층으로
- **십 년 판**(대운): 다음 판이 바뀌는 나이 + 편관·겁재 구간("밀려서 움직이게 되는 때")
- **해마다**(세운): **올해부터 여섯 해**를 순역으로 계산해 막대로 그렸다.
  "옮기기 좋은 해 — 자리가 열려" / "지금 자리에서 값이 오르는 해" /
  "밀려서 움직이게 되는 해 — 미리 준비해 둬" / "움직이면 손해 보는 해 — 버티는 게 남아"

⚠ **좋은 해가 없으면 없다고 쓴다.** 여섯 해 안에 자리가 열리는 해가 없으면
"지금 자리에서 값을 올리는 게 남아"로 닫는다 — 억지로 하나 고르지 않는다(검사가 지킨다).

### 검증
각인 **130/130**(+18) · 리포트 **88/88**(+4) · 스모크 25/25 · 판결 34/34 ·
스코프 54/54 · v29 8/8 · WebGL 7/7 · 여러 하늘 12/12 · 검진 clean

새 검사 중 셋이 이 절의 존재 이유를 지킨다:
- **「일」 자리와 문장이 안 겹친다** — 문장 단위로 실제 대조한다
- **번아웃이 직장 맥락 문장이다** — 돈·말 증상을 갖다 쓰면 운다
- **사람마다 도달 위치·재직 주기가 갈린다**(40명 중 3종·2종 이상)

### 남은 것
- 「같이 일하면 좋은 사람」은 오행으로만 말한다. 상대 생년월일을 받으면
  **실제 그 사람과의 결**을 말할 수 있다 — 궁합 기능이 붙는 자리와 같다.
- 연봉·직급 같은 숫자는 여전히 안 쓴다. 명리에 그 표준이 없다(키·금액과 같은 규칙).

---

## 2026-08-14 · v125 — 궁합: **"87점, 잘 맞아요"는 어디서든 살 수 있다**

### 요청
"궁합 기능도 붙여주고 가격을 어떻게 할지 제안해봐."

### 설계 — 각인과 같은 원칙
궁합은 이 제품에서 **가장 뭉개기 쉬운 자리**다. 시중 대부분이 "87점, 잘 맞아요"로 끝낸다.
그 한 줄은 어디서든 살 수 있고, 그래서 우리가 팔 이유가 없어진다. 네 규칙을 파일에 못 박았다:

1. **분업** — 아홉 축에게 같은 질문을 던져 평균 내지 않는다. 각자 **다른 것**을 본다:
   일간=누가 주고 받나 / 일지=같이 사는 게 맞나(육합·삼합·충·형) / 인도=여덟 항목 /
   해자리=성향 / 달자리=감정 / 자바=두 날의 무게 / 마야=맡은 일 / 납음=소리 / 수비학=배우는 게 같나
2. **갈림을 감추지 않는다** — 이게 알맹이다.
   *"여덟 글자·여덟 항목·해의 자리는 맞는다고 하고, 자리의 글자·달의 자리는 아니라고 해.
   **잘 통하는데 자꾸 부딪히거나, 부딪히는데 못 헤어지는 이유가 여기 있어.**"*
3. **총점을 맨 앞에 두지 않는다** — 숫자를 먼저 보면 나머지를 안 읽는다. 맨 뒤에 두고
   "이 숫자를 먼저 보지 마"라고 적는다.
4. **헤어지라고 말하지 않는다.** 판정은 "무엇을 조심하라"까지다. 관계를 끊는 결정은 우리 몫이 아니다.

인도 여덟 항목은 **합치지 않고 그대로 편다.** 항목마다 무엇을 보는지 적었다 —
"몸과 잠자리의 결", "생각이 통하나", "같이 살 때의 흐름". 나디(8점)는 인도가 가장 무겁게 보는데,
같은 체질이면 0점이다 — **좋고 나쁨이 아니라 "서로의 약점을 못 메운다"**는 뜻이라고 적었다.

### 상대의 이름·연락처는 안 받는다
생년월일만 받고 이 기기에만 남긴다. **남의 개인정보를 우리가 들고 있을 이유가 없다.**
CLAUDE.md §운영 규칙(개인정보 비커밋)의 연장이다. 검진이 이걸 지킨다.
그리고 **연인 전용이 아니라고 안내한다** — 같이 일하는 사람·가족·동업자에게 그대로 쓴다.

### 진입점
로비에 각인 옆. 각인 대비 클릭률로 수요를 잰다(`match_clicked` / `match_opened` / `match_run`).
**각인이 있어야 열린다** — 내 명식이 먼저 있어야 하니까. 그래서 궁합은 구조적으로 **각인의 2차 구매**다.

### 검증
궁합 **29/29**(신규) · 각인 130/130 · 리포트 **99/99**(+11) · 스모크 25/25 · 판결 34/34 ·
스코프 54/54 · v29 8/8 · WebGL 7/7 · 여러 하늘 12/12 · 검진 clean(+1)

검사가 지키는 것: 아홉 축이 서로 다른 질문을 맡는가 · 근거의 급(변환 없음/해석 섞임)을 밝히는가 ·
자·오를 충으로, 자·축을 육합으로 읽는가(전통 배당대로) · 갈릴 때 갈린다고 말하는가 ·
총점 각주가 "앞에 두지 않는다"를 밝히는가 · **헤어지라고 시키지 않는가** ·
60쌍 중 25종 이상 다른 판정이 나오는가.

⚠ 헤어짐 검사는 **명령형만** 잡는다. "부딪히는데 못 헤어지는 이유"는 관계를 설명한 것이지
권고가 아니다 — 서술까지 금지하면 관계를 관계로 말할 수 없어진다. 처음엔 그걸로 헛울음이 났다.

### 만들면서 잡은 것
회귀 검사를 각인 뒤에 뒀더니 "궁합 진입점이 없다"고 계속 울었다. 원인은 로비 버튼이
`!ritual && !res` 조건이라 **판결이 한 번 돌고 나면 사라지는 것**이었다.
검사를 판결 전으로 옮겼고, 왜 그 자리여야 하는지 주석에 남겼다.

### 남은 것
- 「같이 일하면 좋은 사람」(직장생활 절)은 아직 오행으로만 말한다. 궁합 엔진이 생겼으니
  **동료의 생년월일을 넣으면 그 사람과의 결**을 말할 수 있다 — 다음 판에 연결한다.
- 두 사람의 **시기 겹침**(대운·세운)은 아직 안 봤다. "올해 둘 사이가 어떤가"는 재구매 훅이 될 자리다.

---

## 2026-08-14 · v126 — 개인정보·법정고지 A항 다섯

### 요청
전략 세션 작업지시(`claude/goal-validation-feasibility-u2b9ob`의
`app/작업지시_개인정보와법정고지_2026-08-14.md`) **A항부터 처리.**

### 이건 개별 결함이 아니라 추세였다
작업지시가 표로 못 박아 둔 것: 각인 문서는 v113→v124 **여섯 판 연속 넓어졌는데
`ImprintDoc` 안의 고지 줄 수는 0에서 한 번도 안 올라갔다.**
그래서 이번엔 고치는 것만으로 끝내지 않고 **검사 파일을 하나 새로 만들었다**(`e2e/privacy-check.mjs` 38항목).
사람의 기억으로는 여섯 판을 못 버틴다.

### A-1. 처리방침이 안내한 거부 방법이 존재하지 않았다
문서가 두 곳에서 "온보딩의 분석 동의를 해제하는 것만으로 거부할 수 있다"고 적는데,
v122 가 그 체크박스를 화면에서 뺐고 `PROFILE_KEYS` 가 빈 집합이라 **손으로 키를 바꿔도 아무것도 안 막혔다.**

작업지시가 준 두 갈래 중 **①(수단을 실제로 만든다)** 를 택했다 — ②(문서를 동작에 맞춤)는 거부권 자체를 없애는 방향이다.
- `OPTOUT_KEY` 신설 + `track()` **조기 반환**. 속성 제거가 아니라 **전송 자체를 멈춘다**.
- 끄는 순간 대기 큐도 버린다 — 끄기 전에 쌓인 걸 나중에 보내면 거부가 무의미해진다.
- 로비에 「사용 통계 수집을 끌래」. 언제든 되켤 수 있다.
- **처리방침 두 문장을 실제 자리로 고쳤다** — 문서가 없는 절차를 가리키면 안 되지만,
  있는 절차를 **엉뚱한 곳**으로 가리켜도 마찬가지다.

### A-2. 공유 링크에 실명이 실렸다 — 두 경로
`?v=` 는 base64 인코딩만 거친 **평문**이다. 그리고 payload 의 `n` 만 지우면 부족하다 —
SYS 프롬프트가 판결문 안에서 이름을 부르게 하고 있어 **본문 문자열에 실린다.**
- `n` 제거 · 구링크의 `n` 폐기 · 수신 화면 실명 폴백 제거
- `stripName()` 신설 — 호격(지원아/지원님/지원 씨)과 문장 첫머리 호명을 지운다.
- **경계를 실제로 시험했다**(작업지시가 "지원 아끼지"를 예로 들었다). 여덟 경우 누출 0.
- ⚠ **트레이드오프를 알고 골랐다**: 안전한 규칙으로 안 지워지면 남은 이름을 **"너"로 갈아 끼운다.**
  이름이 흔한 명사와 겹치면("하늘아 오늘은 하늘이 흐려") 문장이 어색해진다 — 어색한 문장과 새는 실명 중
  어색한 쪽을 고른 것이고, 코드 주석에 그렇게 적었다.

### A-3. 계측에 판결 원문이 나갔다 — 앞면·뒷면 둘 다
처리방침 §9("분석 도구에는 질문 원문·이름·생년월일 원값을 전송하지 않고")와 정면 충돌이었다.
**뒷면이 더 나빴다** — 콜2 에 이름 금지 지시가 붙는 조건이 "앞면에서 이미 이름을 부른 경우"뿐이라,
**앞면이 이름을 안 부른 경우에만 뒷면이 이름을 부르는** 구조였다.
- `verdict` → `vlen` · `subline`/`funline` → 길이 · `reasons` → 축별 길이
- **처리방침도 같이 고쳤다.** 코드가 안 보내게 됐는데 문서가 "판결 결과 전체를 수집한다"고 적고 있으면
  그것도 코드·문서 충돌이다. 과대 기재도 충돌이다.

⚠ 검사를 처음엔 파일 전체에 걸었다가 `setRecords`(로컬 판결록)를 잡아 헛울음이 났다.
**나가는 것과 남는 것은 다르다** — `track()` 호출 안만 보도록 좁혔다.

### A-4. 각인에 건강 예측이 있는데 고지가 0건이었다
각인은 LLM 을 안 타므로 판결의 S3 가드레일("병세·완치·수명을 점치는 문장 절대 금지")을
**구조적으로 통과하지 않는다.** 그런데 여섯 판 동안 고지가 0이었다.
- 「그때 한 번 크게 앓을 수 있어」 → 「그 구간엔 특히 관리가 필요해. 정기 검진을 그때로 잡아 둬」
- 건강 각주를 **생김새와 같은 수준으로 정직화** — 오행-장부 대응은 전통 배당 그대로라 변환이 없지만,
  그걸 개인의 건강 예측으로 옮기는 데는 해석이 섞이고 **검증한 적이 없다**. 그리고 **의료 판단이 아니다**.
- 각인·궁합에 **문서 하단 고정 블록** 하나씩. 작업지시가 짚은 대로 **절마다 붙이지 않았다** —
  절마다 붙이면 다음 판에서 또 빠진다.

⚠ 클래스를 `ainote imp` 로 붙였다가 컨테이너 `.imp` 셀렉터가 둘을 잡아 회귀가 빨개졌다.
v111 의 `.decl`→`.dcl` 과 **같은 사고**다. `ainote docnote` 로 바꿨고 검사로 못 박았다.

### A-5. 공유 수신 화면·부적 이미지에 AI 표시가 없었다
- 수신 화면은 `position:fixed` 로 뷰포트를 덮어 아래 온보딩 고지가 **물리적으로 안 보인다.**
  링크로 처음 들어온 사람에게는 **거기가 비나리의 첫 화면**이다.
- 부적 이미지는 앱 밖으로 나가 혼자 돌아다닌다 — 그림 안에 안 그리면 표시가 사라진다.
둘 다 넣었다.

### 검증
**개인정보·법정고지 38/38(신규)** · 궁합 29/29 · 각인 130/130 · 리포트 103/103 ·
스모크 25/25 · 판결 34/34 · 스코프 54/54 · v29 8/8 · WebGL 7/7 · 여러 하늘 12/12 ·
검진 clean(A항 통합 검사 1건 추가)

### 남은 것 (작업지시 B·C·D — 이번 범위 밖)
- **B-1** 각인 선택 입력이 리셋에 안 지워져 **다음 사람에게 넘어간다**(키가 `binari_` 라 스윕에 안 걸림)
- **B-2** `clearMemory` 가 키 하나만 지우고 `posthog.reset()` 이 없다
- **B-3** 처리방침에 공유 링크 고지 — **A-2 를 고쳤으니 이제 쓸 수 있다**(§5-1 신설).
  이번에 §9 안전성 조치에 한 줄은 넣었지만 별도 절은 아직이다.
- **C-1~C-4** 결제 붙이기 전 필수(서신 fake-door 표시 · 번호 재발행 약정 · 무제한 유료 호출 · payment=())
- **D-1** 처리방침의 MBTI 4곳 — 이번에 수집 항목표 1곳만 지웠다. 나머지 3곳 남음
- **D-2** CLAUDE.md 의 `App.jsx:1977` 인용 오류

---

## 2026-08-14 · v127 — 저장 키 규칙: **암묵 규칙은 여섯 판을 못 버틴다**

### 요청
작업지시 **A-6**(신규 최우선) — 궁합(v125)이 붙으면서 **제3자의 생년월일**이 앱에 들어왔는데
저장 키가 밑줄이라 리셋에도 안 지워지고 **다음 사람에게 넘어간다.**

### 무엇이 문제였나 — 개별 결함이 아니라 규칙의 구멍
저장 키에 **점(`.`) 접두 = 관리 대상**이라는 **암묵 규칙**이 있었다. `clearMemory` 도,
내보내기/불러오기 스윕도 `binari.` 만 본다. 그런데 새 기능은 계속 **밑줄**로 키를 만들었다:
- `binari_imprint_extra` (v115 각인 선택 입력 — 혼인·자녀·만난 시기)
- `binari_match_last` (v125 궁합 **상대방 생년월일**)

둘 다 스윕 밖이라 **"다른 사람이야? — 처음부터 다시"를 눌러도 남았다.**
궁합 쪽이 특히 무겁다 — **그 사람은 이 앱을 쓴 적도, 동의한 적도 없다.**

작업지시가 정확히 짚었다: *"암묵 규칙이 있는데 새 기능은 계속 밑줄로 만든다 — 다음 기능에서 또 반복된다."*
그래서 개별 수정이 아니라 **규칙을 명시로 올리고 검사를 붙였다.**

### 고친 것
1. **두 키를 `binari.` 접두로 개명** + 구키 1회 마이그레이션(이미 쓰던 사람의 값을 안 잃으면서 규칙 밖 키를 없앤다)
2. **`clearMemory` 를 `binari.` 전량 스윕으로.** 단 **팀 플래그(`INTERNAL_KEY`)는 남긴다** —
   그게 날아가면 계측이 팀 유입으로 오염되고 D7 게이트가 무의미해진다(CLAUDE.md §계측 주의)
3. **`_ph.reset()`** 추가 — 앞사람의 `distinct_id` 를 물려주지 않는다(B-2 앞부분도 같이 해결)
4. **처리방침 §1 에 제3자 항목 신설** — 본인 정보만 적힌 표에 남의 정보가 조용히 추가돼 있었다.
   생년월일·시각·성별만 받고 **이름·연락처는 안 받으며**, 기기에만 저장되고 서버·분석 도구로 안 가며,
   리셋으로 삭제되고, 상대 본인이 열람·삭제를 요청할 수 있다는 것까지 적었다.

### 검사 — 재발을 잡는 게 이 항목의 핵심이다
`health-check` 5-j + `privacy-check` 11항목 + **smoke 브라우저 검사 6항목.**

⚠ 작업지시가 미리 경고한 함정을 그대로 피했다: 규칙을 "`binari_` 로 시작하면 실패"로 넣으면
`binari_bujeok`(다운로드 **파일명**)과 `__binari_t`(localStorage **가용성 프로브**)에서 매번 오탐한다.
**`localStorage.*Item("binari_` 저장 호출 패턴으로 좁혔다.**

그리고 **정적 검사만으로는 부족하다고 봤다.** 제3자 정보라 "코드가 그렇게 생겼다"로는 모자라서
smoke 에 실제 시나리오를 넣었다 — 상대 생년월일을 심어 두고 리셋 버튼을 눌러
**실제로 지워지는지 / 팀 플래그는 남는지**를 브라우저에서 확인한다.

### 잡은 것 하나 더
지난 판에 smoke 에 넣으려던 A-1 검사가 **아예 안 들어가 있었다.** smoke 는 `check()`/`browser` 규약을 쓰는데
내가 `ck()`/`b.close()` 앵커로 붙여서 치환이 조용히 실패했다 — 그런데 통과 개수가 그대로라 눈치를 못 챘다.
**"검사를 추가했다"고 로그에 쓰면서 실제로는 안 늘어난 것**이라 이번에 바로잡았다(25 → 31).
→ 교훈: 검사를 추가하면 **통과 개수가 늘었는지 확인한다.**

### 검증
개인정보·법정고지 **49/49**(+11) · 스모크 **31/31**(+6) · 궁합 29/29 · 각인 130/130 ·
리포트 103/103 · 판결 34/34 · 스코프 54/54 · v29 8/8 · WebGL 7/7 · 여러 하늘 12/12 ·
검진 clean(저장 키 규칙 검사 1건 추가)

### A항 전부 종료 — 남은 것
**B-1 은 A-6 과 같은 뿌리라 함께 해결됐다.** B-2 도 절반(clearMemory 스윕 + posthog reset)이 됐다.
남은 것: **B-2 잔여**(내보내기 스윕 대상 재점검) · **B-3**(처리방침 §5-1 공유 링크 고지 —
A-2 를 고쳤으니 이제 쓸 수 있다) · **C-1~C-4**(결제 전 필수) · **D-1**(처리방침 MBTI 잔여 3곳) ·
**D-2**(CLAUDE.md 의 `App.jsx:1977` 인용 오류).

---

## 2026-08-15 · v128 — 진입 모션과 결제 전 정직화

### 요청
> "그래프, 표가 나타는 모션이 주기가 너무 짧아서 발작일으키는것 처럼 느껴져. 그리고 그래프가 그려지는 모션이
> 있듯이 표도 채워지는 모션이 있어야하겠어 / 나타는 모션은 최초에 한 번만 보여주고, 스크롤해서 벗어났다가
> 다시 오면 다시 그려지게끔하면 좋겠어 / 다 진행해 안했던거"

### 1. 원인은 속도가 아니라 동시성이었다
v127 의 각인 CSS 는 이렇게 생겼었다.

```css
.imp .impdom,.imp .impsky,.imp .impclash,.imp .impband,.imp .impck{animation:impRise .5s … both}
.imp .impsvg{animation:impRise .55s … both}
.drawin .pulse{animation:impPulse 2.6s ease-in-out infinite}
```

선택자에 조건이 없다. **문서가 마운트되는 순간 해당 클래스 전부가 애니메이션을 동시에 시작한다.**
각인은 자리 9 + 하늘 9 + 확인 문항 12 + 여든 해 구간 + 그림 7 = **60여 블록**이다.
0.5초를 60개가 같이 하면 그건 "짧은 모션"이 아니라 **한 번의 경련**이다. 거기에 `.pulse` 가 무한 반복이었다.

그래서 고친 건 duration 이 아니라 **누가 언제 움직이는가**다.

### 2. `useReveal` — 화면에 들어온 것만 움직인다
```
REVEAL_SEL = .impsvg,.impdom,.impsky,.impclash,.impband,.impck,.imptrig,.impch,.improws,.impmrows,.impyrs,.impcore
```
- IntersectionObserver `threshold:[0, 0.12]` — **15%쯤 보이면 켜고, 완전히 사라지면 끈다.**
  경계에 히스테리시스를 둔 건 의도적이다. 켜는 선과 끄는 선이 같으면 스크롤 미세 떨림에 깜빡이는데,
  그게 정확히 창업자가 지적한 증상이다.
- 같은 콜백 배치에 여러 개가 들어오면 **문서 순서로 정렬해 80ms 씩 계단**(최대 6칸)을 `--d` 로 넣는다.
  하나만 들어오면 지연 0 — 스크롤 속도에 따라 자연스럽게 계단이 생겼다 없어졌다 한다.
- 나중에 생기는 블록(근거 펼치기 등)은 MutationObserver 가 주워서 같은 규칙을 준다.

**실측: 한 화면에 동시에 켜지는 블록 최대 11개**(전체 64개 중). v127 은 60여 개가 한꺼번이었다.

### 3. ⚠ 초기 숨김은 CSS 가 아니라 JS 가 붙인다
`.rv{opacity:0}` 를 CSS 에 두고 JS 가 `.rvin` 을 붙이는 게 흔한 구현인데, **그렇게 하면 스크립트가 죽는 순간
9,900원짜리 문서가 백지가 된다.** 그래서 `.rv` 자체를 훅이 붙인다 — 훅이 안 돌면 문서는 그냥 다 보인다.
설계 헌장의 「알 권리 — 리포트 국면」이 여기까지 미친다: **값을 치른 문서에서 "안 보임"은 최악의 실패다.**

### 4. 표도 채워진다
그래프에만 있던 draw-on 을 표에 옮겼다.
- 줄(`.impr`·`.impmrow`·`.impyr`)은 `clip-path:inset(0 100% 0 0)` → `inset(0)` 으로 **왼쪽부터 채워진다**(70ms 계단).
  컨테이너는 안 움직인다 — 칸이 흔들리면서 글자가 차면 읽기가 더 힘들어진다.
- 막대(여든 해·열두 달·여섯 해)는 `transform-box:fill-box` + `scaleY` 로 **축에서 자라 오른다.**
  위 막대는 아래에서, 아래 막대는 위에서 — 원점을 JSX 가 부호 보고 넣는다.
- 궁합 아쉬타쿠타 여덟 칸은 `scaleX` 로 좌→우.
- `.pulse` 의 `infinite` 삭제 → 3.2초 2회 후 멎음. **눈이 머무는 문서에 계속 뛰는 게 있으면 안 된다.**

`prefers-reduced-motion` 은 **모션만** 끈다. `opacity:1!important` 를 전 요소에 밀면 막대의 강약(SVG opacity 속성)까지
뭉개져서, 우리가 0으로 만든 것만 되돌린다.

### 5. "다 진행해 안했던거" — 작업지시 잔여 처리
| 항목 | 처리 |
|---|---|
| **B-2 잔여** | 저장 키 전수 확인 — 10개 전부 `binari.` 접두(+`binari.once.*`). 내보내기 스윕 대상 누락 **0**. 종료 |
| **B-3** | 처리방침 **§5-1 신설**. ⚠ 문구를 **코드를 읽고** 썼다 — payload 실물(`{q, d, v, s, a, t, c, hx}`)을 보고 "질문 원문이 실린다 · base64 라 암호화가 아니다 · 링크가 남는 곳은 다 읽는다 · 회수 못 한다 · 이름은 안 실린다"를 적었다. 검사도 문서와 payload 를 **맞대어** 본다 |
| **C-1** | "열람 후에는 환불되지 않아요" 삭제 → 「지금은 시험 발행이라 값을 받지 않아」. 서신 버튼에도 각인·궁합과 같은 **시험 발행 배지**. 전상법 §17⑥ 고지는 **결제 붙는 날 되살린다**(검진이 양방향 감시) |
| **C-2** | "번호를 대고 다시 받으면 돼" 2곳 삭제. `letterNo` 는 기록에서 계산한 비가역 해시고 계정·주문 DB 가 없어 **우리도 못 되살린다.** 사실대로 적고 실제로 듣는 대책(파일 저장)을 준다 |
| **C-3·C-4** | 결제 미연동이라 지금은 무해. **검진 4-3 을 확장**해 결제 라이브러리가 들어오는 순간 셋(tier 서버검증·재발행 `rec.paid`·`payment=()`)을 한꺼번에 깨우게 했다 |
| **D-1** | 처리방침 MBTI 2곳 삭제 + **계측 속성에서도 제거**. 안 묻는 값을 계속 실어 보내고 있었다(새 유저는 영구 null, 옛 유저 것만 나감). 남은 쓰임은 수호신 얼굴 시드 하나뿐이고 기기 밖으로 안 나간다 |
| **D-2** | 현행 CLAUDE.md 에 `App.jsx:1977` 인용도 "11개 체계" 목록도 **없다** — 전략 세션이 CLAUDE.md 를 다시 쓰면서 사라졌다. 고칠 대상 없음 |
| **D-3** | 세 상품의 클릭을 **같은 자로 맞췄다.** 각인·궁합 버튼에 값 표시(9,900·4,900) + `imprint_offer_shown`·`match_offer_shown` 노출 이벤트(방문당 1회). ROADMAP 의 비교 기준을 고치고 **"v128 이후 데이터로만 판정"**을 못 박았다 |

⚠ **v127까지 쌓인 각인·궁합 클릭은 가격 비교에 쓸 수 없다.** 가격이 안 보이는 버튼을 누른 것이라
서신 클릭(4,900원을 보고 누름)과 같은 종류가 아니다. 데이터를 버리는 게 아니라 **"호기심 클릭"으로 따로 읽는다.**

### 6. 검사
- **검진 5-k 신설** — 진입 애니메이션이 `.rvin` 을 안 거치면 실패, 각인에 `infinite` 가 있으면 실패,
  초기 숨김을 JS 가 안 붙이면 실패. 회귀를 시뮬레이션해 실제로 잡히는지 확인했다(`.imp .impdom{animation:…}` → 검출).
- **리포트 +8** — 브라우저에서 **계산된 스타일**로 본다. CSS 문자열만 보면 셀렉터가 어긋나도 통과한다(v126의 그 사고).
  동시 진입 상한 · infinite 0 · wipe 15줄 · 막대 7개 · **벗어났다 돌아오면 다시 그려지는지**를 실제로 스크롤해서 확인.
- **개인정보 +24** — B-3(문서↔payload 대조 10) · C-1·C-2(6) · D-1·D-3(8).
- **판결 +1** — "환불되지 않아요"의 존재를 지키던 검사를 뒤집었다. 검사가 규칙을 붙들고 있으면 규칙만 고쳐도 안 바뀐다.

### 검증
(병합 후 최종) 리포트 **111/111**(+8) · 개인정보 **108/108**(+24) · 판결 **35/35**(+1) ·
문서계측 16/16 · 계측 24/24 · 스모크 31/31 · 각인 130/130 · 궁합 29/29 · 스코프 54/54 ·
v29 8/8 · WebGL 7/7 · 여러 하늘 12/12 · 검진 clean(모션 검사 1건 추가)

### 7. 병합 — 같은 날 main 에 들어온 두 판과 합쳤다
`v127.1~127.5`(부적 인장·오행 색·광고 진입면)와 **문서 읽기 계측**(`useDocRead`), 그리고
창업자 지시 "질문과 답변은 다 남기자. 다만 개인 식별 불가능하게 해"에 따른 **가명처리(`anon`) 계측**이
먼저 들어와 있었다. 조정 둘:

- **ref 충돌** — 각인·궁합 루트 `<div className="imp fade">` 에 이미 `useDocRead` 의 `readRef` 가 붙어 있었다.
  **한 요소에 ref 는 하나뿐이다.** 두 훅이 각자 ref 를 들면 나중에 붙는 쪽이 앞의 것을 조용히 덮고,
  읽기 계측이나 모션 중 하나가 **소리 없이** 죽는다. `useReveal(ref)` 로 시그니처를 바꿔 같은 ref 를 공유한다.
  검증: `doc-check` 16/16(`imprint_read` read_pct=100·`match_read` read_pct=100) + 모션 검사 8건 동시 통과.
- **§5-1 문구** — "질문 원문은 전송하지 않습니다" 계열 문장이 처리방침에 남으면 안 된다(가명처리본은 실제로 나간다).
  공유 링크 절의 해당 줄을 **링크를 여는 쪽 이벤트**(`shared_verdict_view` = `{dir}` 하나)로 범위를 좁혀 다시 썼다.
  링크를 만든 사람 쪽 기록은 1항 가명처리 설명으로 넘긴다.

### 남은 것
- **C-3·C-4** 본작업 — 결제 배선과 **같은 커밋**에서. 지금 고치면 검증할 대상이 없다
- 서신 예시 5장 재작성(가상 명식으로) · 평가 하네스 실모델 실행 · 「같이 일하면 좋은 사람」에 궁합 엔진 연결
- 커밋 이력의 PII(`46ca39d`/`6b84fe8`) — history rewrite 미결정

---

## 2026-08-15 · v129 — 남은 것 넷

### 1. 시키는 축 ≠ 세는 축 — 열네 판 동안 어긋나 있었다
v114 에 MBTI 를 판결 축에서 뺐다. 그런데 **뺀 건 세는 쪽뿐이었다.**

```
App.jsx:3317  const VOTE_AX = new Set(["사주","달","별자리","수비학", …])   ← MBTI 없음
App.jsx:4120  "votes엔 이번 판결에 참여한 지표를 전부 넣는다(사주·달·별자리·MBTI·수비학·마야 …)"  ← MBTI 있음
```

모델은 시킨 대로 MBTI 표를 실어 보냈고, `tallyVotes` 가 그걸 **조용히 버렸다.**
즉 **모델은 여섯 축으로 판단하고 앱은 다섯 축으로 셌다.** `against/total` 이 모델이 잰 것과 다른 분모였다는 뜻이다.
평가 하네스의 콜2 축 열거에도 MBTI 가 남아 있어, 하네스는 **앱에 없는 프롬프트**로 앱을 채점하고 있었다.

**검진을 양방향으로 고쳤다.** 기존 검사는 "앱에 있는데 하네스에 없는 것"만 봤다 — 반대쪽을 못 잡았다.
- `프롬프트가 시키는 축` vs `VOTE_AX` 대조 (오늘 잡은 그 버그)
- `앱의 축 열거` vs `하네스의 축 열거` 대조
회귀 시뮬레이션으로 실제로 잡히는지 확인했다(MBTI 를 다시 끼우면 → "시키는데 안 세는 축 — MBTI").

**동의 화면의 거짓 두 줄도 같이 고쳤다.**
- "MBTI" — v114 이후 안 묻는데 열네 판 남아 있었다.
- **"네가 적은 질문은 보내지 않아"** — 8/15 가명처리 도입으로 **사실이 아니게 됐다.**
  처리방침은 고쳤는데 동의 화면은 안 고쳤다. **고지의 무게는 문서보다 이 줄이 무겁다** — 여기가 동의를 받는 자리다.
- 거부 수단 위치도 코드를 읽고 적었다. 그 버튼은 `!ritual && !res`, 즉 **로비**에 있다.
  "아래"라고 쓰면 또 없는 자리를 가리키게 된다 — A-1 이 정확히 그 사고였다.

### 2. 평가 픽스처에 실인물이 있었다
`eval/personas.json` 의 P1 이 **창업자 이름 + 사주 네 기둥**이었다.
사주 네 기둥은 60년 안에서 생년월일시와 **일대일**이라, 이름과 나란히 적히면 그 자체로 사람을 특정한다.
CLAUDE.md §운영 규칙이 금지하는 형태 그대로다. (`9de8adc` 가 "실인물 픽스처 없음"이라 적었는데, 이 파일은 안 봤다.)

**손으로 적은 명식 표를 통째로 폐기했다.** 픽스처엔 이제 **가상 생년월일만** 있고,
명식은 `eval/build-personas.mjs` 가 **앱이 export 하는 엔진**으로 그때그때 뽑는다. 이유 둘:
- PII 를 적을 자리 자체가 없어진다.
- 손으로 적은 값은 엔진을 안 따라온다 — v101 대운, v117 위도가 바뀌었을 때 하네스는 **앱과 다른 명식으로 앱을 평가**하고 있었을 것이다. 이제 진실이 한 곳에만 산다.

날짜는 커버리지를 보고 골랐다: **주기운 오행 다섯이 다 나오고**(금2 목2 화1 토2 수1), 남녀·대운 순역·
**시(時) 미상**·**입춘 경계(사주년이 전년으로 넘어감)**·도시 8종이 섞이게.
`eval/personas-check.mjs` 17항목 — 그중 하나가 **기둥 표기가 픽스처에 다시 들어오면 실패**다.
⚠ 그 검사를 처음엔 간지 두 글자로 짰다가 "한여름 **정오**"에 울었다(정+오가 그대로 간지다). 뒤따르는 년/월/일/시까지 봐야 기둥이다.

**그리고 `.mansae-test.tmp.mjs` 가 커밋돼 있었다** — App.jsx 통짜 esbuild 번들 6,229줄.
소스가 두 벌이 되고 비밀키·PII 검사도 두 벌을 봐야 한다. 제거하고 `*.tmp.mjs` 를 무시 목록에.

### 3. 「같이 일하면 좋은 사람」을 궁합으로 이었다
각인의 그 줄은 오행 한 줄이었다 — "금의 기운을 가진 사람이 너를 밀어 줘".
맞는 말이지만 **유저가 아는 건 오행이 아니라 사람**이다. 그래서 궁합에 「같이 일하면 어떤가」 절을 붙였다.

| 행 | 무엇으로 | 근거의 급 |
|---|---|---|
| 둘의 역할 | 일간끼리의 십성 10종 | 배당 그대로 · 직장 말로 옮기는 데 해석 |
| 누가 미나 | 오행 상생 — **각인과 같은 규칙**을 이 사람에게 적용 | 변환 없음 |
| 판은 누가 끄나 | 아쉬타쿠타 바르나(일에서의 결) | 채점 그대로 |
| 말이 통하나 | 아쉬타쿠타 그라하 | 채점 그대로 |
| 얼마나 붙어 있어도 되나 | 일지 육합/삼합/충/형 | 배당 그대로 |

⚠ **축을 안 늘렸다(아홉은 아홉).** ⚠ **총점에도 안 넣었다** — 연애 궁합과 일 궁합을 한 숫자로 뭉개면
"87점"이 무엇에 대한 87점인지 아무도 모르게 된다. ⚠ **"동업하지 마"라고 말하지 않는다** — 관계 판정과 같은 선이다.

⚠ 사고 하나: `Row` 를 ImprintDoc 안쪽에 두고 궁합에서 불러 **궁합 결과 화면이 통째로 죽었다.**
두 문서가 같이 쓰는 조각은 문서 밖에 둔다(`DocRow`). 검사가 잡았다 — `.imp` 가 사라져 타임아웃.

### 4. 태국 요일·하압은 **안 붙이기로 판정**
둘 다 계산은 맞고 검증도 됐는데 붙일 자리가 없다.
- **태국 요일** — 입력이 **요일 하나**인데 자바 웨톤과 아칸 데이네임이 이미 그 요일을 쓴다.
  붙이면 아홉 하늘 중 셋이 같은 입력을 세 번 읽는다. 제1규칙이 "분업이지 투표가 아니다"이고,
  창업자 판정("그냥 사주인데 굳이 쟤네 왜 붙였지")의 답이 정확히 그거였다.
- **하압** — 새로 답할 게 52년 주기(캘린더 라운드)뿐인데 그건 **모두에게 52세**로 상수다.
  v117 에 서양 프로펙션 축을 뺀 것과 같은 이유다(200명 중 148명이 "30세").

**판독기가 있다고 다 붙이지 않는다.** 사유를 `sky.js` 주석에 못 박았다 — 다음 세션이 "안 쓰네?" 하고 붙이지 않게.

### 5. 평가 하네스 실모델 실행 — **이 환경에서는 못 돈다**
로컬에 `ANTHROPIC_API_KEY` 가 없고, 경유 모드는 이 컨테이너의 네트워크 정책이 막는다:
`HTTP 403 — Host not in allowlist: binari-sepia.vercel.app`.
**하네스 자체는 네트워크 직전까지 정상**임을 확인했다(SYS 12,915자 추출 · 새 페르소나 조립 · CSV 출력).
네트워크가 되는 곳에서 이 한 줄이면 된다:
```
cd app && node eval/run-eval.mjs --full --via=https://binari-sepia.vercel.app
```

### 6. 병합 — main 이 같은 자리를 다르게 고쳐 놨다
`v127.5~v128.1`(공유 링크 통계 차단 · 문서 인장 철회 · **MBTI·가치여정 완전 제거**)이 먼저 들어와 있었다.
겹친 자리 넷:
- **VOTE_AX** — main 이 더 나아갔다(가치 축까지 제거). main 을 취하고, 이번 판의 교훈(양방향 대조)을 주석에 얹었다.
- **콜1 지시문** — main 이 이미 MBTI·가치를 뺐다. main 을 취했다.
- **동의 화면** — main 은 "MBTI"만 "관계"로 바꾸고 **"네가 적은 질문은 보내지 않아"는 그대로 뒀다.**
  코드를 확인했다 — `q_anon: anon(q, birth.name)` 이 여전히 나간다. **여전히 거짓이다.** 이번 판정대로 고쳤다.
- 버전 — v128.1 → v129.

**그리고 새 대조 검사가 병합 직후 바로 하나 잡았다.** main 이 앱에서 가치 축을 뺐는데
평가 하네스의 콜2 축 열거엔 `가치`가 남아 있었다 — 앱 8축 vs 하네스 9축. 맞췄다.
같은 종류로 하나 더 나왔다: 하네스 프로필이 **`가치여정(워드소팅 16→6→3→1)`을 계속 싣고 있었다.**
앱은 v128 에 가치여정을 통째로 없앴으니, 하네스는 **앱이 안 보내는 값을 실어 놓고 앱을 채점**하던 것이다.
그 줄을 앱이 실제로 보내는 **「요즘 삶의 국면(직업·관계)」**으로 갈고, 페르소나에도 job·rel 을 넣었다.
→ 검사 2건 추가(페르소나 19). **검사를 붙이자마자 두 건을 잡았다는 게 이 검사가 필요했다는 증거다.**

### 검증
궁합 **39/39**(+10) · 리포트 **114/114**(+3) · 평가 페르소나 **19/19**(신설) · 개인정보 108/108 ·
각인 130/130 · 판결 35/35 · 스모크 35/35 · 문서계측 16/16 · 계측 24/24 · 스코프 54/54 ·
v29 7/7 · WebGL 7/7 · 여러 하늘 12/12 · 만세력 28/28 · 검진 clean(축 대조 2건 추가)

### 남은 것
- **C-3·C-4** — 결제 배선과 같은 커밋에서(검진 4-3 이 그날 셋을 한꺼번에 깨운다)
- **평가 하네스 실모델 실행** — 네트워크 되는 곳에서
- **커밋 이력의 PII**(`46ca39d`/`6b84fe8`) — history rewrite 는 강제푸시라 **사장님 결정 사항**

---

## 2026-08-15 · v130 — 각인·궁합 공유 카드

### 요청
> "각인 궁합도 공유 가능하게 하면좋지 않을까"

### 0. 먼저 읽은 것 — 정책은 이미 서 있었다
디자인 세션이 같은 날 `01_기획전략/비나리_기획전략_바이럴루프판단_v01.md` 를 올려 뒀고,
v129.3 이 그 §2·§4 를 코드로 이행해 뒀다(`shareRisk` · `card_saved`). 그래서 **정책을 다시 세우지 않고 그 위에 얹었다.**
- 창업자 결정: 결과 공개는 허용하되 **선별**한다. 기준은 취향이 아니라 **역산 가능성**.
- §3: 루프의 1차 단위는 **1인 완결형**. 궁합(2인)은 2차 증폭.
- 미결 #1·#2 가 정확히 이번 작업이었다 — 공개 이미지 화이트리스트 확정 · 궁합 공개 범위.

### 1. 링크 안은 기각했다
가장 먼저 떠오르는 건 판결처럼 `?v=` 에 싣는 것이다. **못 한다.**
- 각인 본문은 4,000자가 넘어 URL 에 안 들어간다.
- 그러면 실을 수 있는 건 결국 **입력**(생년월일·시·도시)인데, 그게 CLAUDE.md 가 금지하는 바로 그 세 값이다 —
  "생일+생시+지역 세 값이 함께 적히면 그것만으로 한 사람이 특정된다".
→ **보낼 물건은 문서가 아니라 그림이어야 했다.**

### 2. 무엇을 실을지 — 정책 §2 를 그대로 적용
| 카드 | 싣는 것 | 파생 이름 | 왜 |
|---|---|---|---|
| **각인** | 겉·속 두 줄 + 태어난 해의 이름(납음) + 수호신 + 주소 | **1개**(납음, 60년) | 겉·속은 일간 파생 — 10일 주기라 날짜를 못 좁힌다. 촐킨·웨톤은 **안 싣는다**(둘만으로 LCM 1,820일 = 5년 창에서 유일) |
| **궁합** | 관계 한 줄 + 조심할 것 한 줄 + 수호신 + 주소 | **0개** | 축 이름조차 안 싣는다 — "우리는 충이야"면 한쪽을 아는 사람에게 **상대의 자리 글자**가 좁혀진다 |

**총점도 안 싣는다.** 화면에서 "총점을 앞세우지 않는다"고 해 놓고 카드 한복판에 숫자를 박으면
그 원칙이 앱 밖에서 무너진다. 카드 발치에 그 이유를 적어 뒀다.

### 3. 카드가 나가는 문을 하나로
`saveOrShareCard({build, cardKind, skyKinds, fileBase, title})` 로 배관을 뽑았다.
부적·각인·궁합이 **전부** 이 문을 지나므로 `shareRisk` 판정도 `card_saved` 계측도 빠질 수 없다.
⚠ 위험 판정은 **그리기 전에** 한다 — 그려 놓고 버리면 "위험"인데도 캔버스엔 이미 다 그려진 상태가 된다.

그리고 **카드에 주소를 그려 넣었다.** 지금까지 부적엔 주소가 없었다 —
그림이 퍼져도 받은 사람이 찾아올 길이 없었으니, 루프의 절반이 끊겨 있었던 셈이다.

### 4. ⚠ 검사가 거짓 통과하고 있었다
새 검사를 붙였는데 이상하게 전부 한 번에 통과했다. 파 보니 `sharerisk-check` 의 `grab()` 이
**함수 이름 뒤 첫 `{` 부터** 짝을 맞추고 있었다. 인자가 구조분해면(`function f({ a, b })`)
그 `{` 는 **인자 패턴**이라, 잘라 온 게 본문이 아니라 인자 목록이었다.
→ "본문에 X 가 없다" 류 검사가 **전부 거짓 통과**. 고치자마자 실제 위반 하나가 드러났다.

같은 판에 **문자열 앵커 검사도 성질 검사로 바꿨다.** 기존 검사는
`const risk = shareRisk(args.skyKinds || [])` 라는 문장을 통째로 찾고 있어서,
배선이 멀쩡한데 리팩터링만으로 빨개졌다. 이제 보는 건 성질이다 —
문이 하나인가 · 그 문이 판정을 거치는가 · **판정이 그리기 전인가** · `toDataURL` 이 한 곳뿐인가.

### 5. 고지
처리방침 §5-1 을 「공유 링크와 **이미지 카드**」로 넓히고 카드 둘의 수록 항목을 적었다.
그리고 **왜 개수를 제한하는가**를 적었다 — 파생값은 주기가 다른 나머지값이라 여러 개가 한 장에 모이면
역으로 생년월일이 좁혀진다는 것, 그래서 위험 조합이면 **그림 자체를 만들지 않는다**는 것.
A-4-c 가 잡은 추세(문서는 넓어지고 고지는 0)를 반복하지 않으려는 것이다.

### 검증
공개표면 **33/33**(+10) · 리포트 **121/121**(+7) · 개인정보 **111/111**(+3) · 판결 35/35 ·
궁합 39/39 · 각인 130/130 · 페르소나 19/19 · 스모크 35/35 · 문서계측 16/16 · 계측 24/24 ·
스코프 54/54 · v29 7/7 · WebGL 7/7 · 하늘 12/12 · 만세력 28/28 · 검진 clean

### 남은 것 (바이럴루프판단 v01 기준)
- **판별**: `card_saved{card_kind}` 가 쌓이면 §4 기준으로 읽는다 —
  공유율 15% 미만이면 "물건 문제가 아니었다" → 루프 대신 광고 소재로.
- **유입 귀속**: 카드에 주소는 그렸지만 `?ref=` 는 못 붙인다(사람이 쿼리까지 안 친다).
  카드발 유입은 당분간 `card_saved` 대비 direct 증가로 **간접 추정**만 된다. QR 이나 짧은 주소는 별도 판단.
- C-3·C-4(결제일) · 평가 하네스 실모델 실행(네트워크 되는 곳) · 커밋 이력 PII(사장님 결정)

---

## 2026-08-15 · v131 — 카드를 답에서 미끼로

### 지적
> "궁금함이 느껴지거나 재밌는 포인트가 없어. AI 특유의 설명적인 부분 '총점은 안 실어' 같은,
> 안 알려줘도 되는 어노잉하게 느껴지는 부분으로 정보를 채웠어.
> 호기심이 느껴지게끔해. 내가 궁합을 상대방에게 보내면 왜 보냈을까? 그 사람은 왜 올까?"

정확한 지적이다. v130 카드는 **미니 리포트**였다 — 답을 다 해 버려서 받은 사람이 물어볼 게 안 남았다.

### 1. 진단 — 카드와 문서는 목적이 다르다
| | 문서 | 카드 |
|---|---|---|
| 왜 있나 | **읽히려고** | **열리려고** |
| 그래서 | 설명이 있어야 한다 | 설명이 있으면 안 열린다 |

v130 은 문서 문구를 잘라 카드에 붙였다. 그래서 카드가 문서의 축소판이 됐고,
빈자리에 우리가 **우리 설계 원칙을 해설**해 놓았다 — "총점은 안 실어 — 궁합은 어디가 갈리는가로 읽는 거야".
그건 우리 규칙이지 받는 사람의 궁금증이 아니다. **그 자리가 훅이 들어갔어야 할 자리다.**

### 2. "왜 보냈을까 / 왜 올까"에 답 만들기
- **보내는 이유** — 카드에 **옮겨 적고 싶은 한 줄**이 있어야 한다. 분류명이 아니라 장면이어야 한다.
- **오는 이유** — 카드가 **자기 것으로는 완결되지 않아야** 한다.

그래서 **되묻는 줄**을 넣었다. 궁합 쪽 문구:
> 이건 내 쪽에서 본 거야. 네 쪽에서 보면 다르게 나와.

⚠ **이건 지어낸 훅이 아니라 사실이다.** 일간 십성은 방향이 있어서(내가 상대를 보는 관계 ≠ 상대가 나를 보는 관계)
같은 두 사람이라도 누구 쪽에서 읽느냐로 답이 갈린다. 거짓 훅을 붙이면 열어 본 사람이 배신감을 느낀다 —
**우리가 실제로 갖고 있는 성질을 그대로 말하는 게 제일 센 훅이었다.**

각인 쪽은 "너는 겉과 속이 같은 사람일까?" — 카드가 보낸 사람 얘기로 끝나지 않고 **보는 사람에게 넘어간다.**

### 3. 헤드라인을 분류명에서 장면으로
v130 헤드라인은 `clash.t` 였다 — "하늘끼리 갈린다". **우리 내부 분류명**이라 유저에게 아무 뜻이 없다.
십이지 관계를 일상 장면으로 옮긴 7종을 새로 썼다:

| 관계 | 카드 헤드라인 |
|---|---|
| 육합 | 밥때가 저절로 맞는 둘 |
| 삼합 | 같은 데를 보고 있는 둘 |
| 같음 | 설명 안 해도 통하는 둘 |
| 충 | 조용한 사이가 되긴 어려운 둘 |
| 형 | 크게 싸운 적은 없는데 자꾸 쌓이는 둘 |
| 자형 | 같은 실수를 같이 하는 둘 |
| 무관 | 저절로는 안 굴러가는 둘 |

⚠ **축 이름(충·육합·형)도 십이지 글자도 여전히 안 싣는다** — 한쪽 생일을 아는 사람에게 상대가 좁혀진다.
값이 아니라 **장면**으로 옮기면 재미는 살고 역산은 안 된다. 144쌍 전수로 7종이 다 나오는 것 확인.

### 4. 카드 문구를 엔진에 따로 뒀다
`readMatch().card = { head, line, ask }`. 문서의 `care[0]` 을 잘라 쓰지 않는다 —
문서 쪽은 왜 그런지까지 적어야 해서 길다("같이 사는 동안의 흐름이 낮게 나와. 큰 결정(이사·창업·대출)은…").
카드용은 짧은 명령형 7종을 따로 썼다: "이사·창업·대출은 한 박자 늦출 것. 둘이 따로 재 보고 정할 것."

**폴백도 다시 썼다.** 사장님이 받은 카드에 뜬 게 하필 폴백이었고 제일 밋밋했다 —
"특별히 못 박아 둘 위험은 안 잡혀." 아무 말도 안 하는 것과 같다.
→ "싸워서 무너지는 사이가 아니다. **당연해질 때 무너진다.**" 위험이 없으면 **무너지는 방식**을 말한다.

### 5. 조판 세 건 — 눈으로 보고 잡았다
카드는 그림이라 검사로는 "재미없다"도 "못생겼다"도 못 잡는다. `e2e/card-shot.mjs` 를 만들어 실물 PNG 를 떴다.
- **고아 어절** — 큰 글씨에서 "한번 정하면 안 바꾸는 / **사람**", "저절로는 안 굴러가는 / **둘**" 이 나왔다.
  그리디 랩만 하면 생긴다. ①줄이 넘치면 글자를 줄이고 ②마지막 줄이 한 어절이면 앞 줄에서 하나 내린다.
- **아우라가 그림과 따로 놀았다** — `cardBase` 가 y=620 고정으로 빛을 깔고 그림은 y=450 에 그렸다.
  **빛이 그림 밑에 안 깔려서** 수호신이 납작하고 흐리게 나왔다. 아우라를 `drawGuardianOn` 안으로 옮겨 같이 움직이게.
- **수호신이 손톱만 했다** — 화면 캔버스는 둘레 여백이 넓은데 가운데 정사각으로만 잘라 넣고 있었다.
  **알파 경계 상자**를 찾아 자른다(160×160 으로 축소해 훑으므로 비용은 무시할 수준). 못 읽으면 옛 방식으로 되돌아간다.

### 검증
공개표면 33/33 · 리포트 121/121 · 개인정보 111/111 · 판결 35/35 · 궁합 39/39 · 각인 130/130 ·
스모크 35/35 · 문서계측 16/16 · 계측 24/24 · 스코프 54/54 · v29 7/7 · WebGL 7/7 · 검진 clean
그리고 **실물 카드 두 장을 떠서 눈으로 확인**했다.

### 남은 것
- 카드발 유입 귀속(QR·짧은 주소) — 바이럴루프판단 v01 §5 미결 4
- C-3·C-4(결제일) · 평가 하네스 실모델 실행 · 커밋 이력 PII(사장님 결정)
## 2026-08-14 · v94 — 공유 경로 개인정보 정리 + 부적 계측 + 관계 축 진입

**계기**: 궁합 검증 워크플로가 우리 코드에서 문제를 짚었고, 직접 확인해보니 사실이었다. 창업자 승인 후 5건 일괄 처리.

### 1. 공유 링크에서 실명 제거 (`App.jsx` shareVerdict)
- `payload`에서 `n: (birth.name||"").trim()` **제거**.
- 판단 근거: **질문 원문(`q`)은 공유 문구에 이미 보이므로 유저가 알고 보낸다. 이름은 공유 문구에 안 보이고 URL에만 담겨 나가서 공유자가 모른다.** 보이는 것과 안 보이는 것의 차이로 갈랐다.
- 수신 화면은 손댈 필요 없었다 — `sharedIn.n ? ... : "어떤 이의 수호신이 이렇게 판결했어"` 폴백이 이미 있었다.
- base64는 암호화가 아니다. 링크를 받은 누구나 디코딩한다.

### 2. 부적 저장·공유 계측 (`saveOrShareBujeok`)
- 함수 전체에 `track()` 호출이 **0건**이었다. 잡히던 건 '펼치기' 버튼(`bujeok_opened`)뿐이라 **실제로 밖에 나갔는지를 몰랐다.**
- 3경로 전부 부착: `bujeok_saved{via:"share"|"download"|"newtab", dir}` + 취소는 `bujeok_cancelled`.
- 질문 원문·이름은 싣지 않는다(방향만).

### 3. 부적 포스터에 주소 (`buildBujeokPoster`)
- 기존 하단 텍스트가 `"비 나 리 · B I N A R I"`(브랜드명)뿐 — **받은 사람이 어디로 갈지 몰랐다.**
- `binari-sepia.vercel.app`을 y=1868에 추가. 우리 유일한 밖으로 나가는 자산에 목적지를 붙였다.

### 4. 관계 축 진입 — 문구·프롬프트만 (헌장 무위반)
- `QHINTS`에 3개 추가: "그 사람에게 마음을 더 줘도 될까?" / "이 사람과 계속 같이 일해도 될까?" / "먼저 연락해서 사과할까?"
- **결정 형태(GO/STOP/HOLD)를 유지**하면서 상대를 특정한다. 직장 관계까지 넓혔다(사주도령이 "팀장님은 어떤지"로 확장한 것과 같은 각도).
- `SYS`에 가드 1줄: **상대의 사주·별자리를 지어내지 않는다. 우리가 가진 건 유저 지표뿐이므로 "지금 너의 기운이 그 관계를 어떻게 통과하는가"로만 답한다. 상대의 마음·의도를 단정하지 않는다.**
- 상대 생년월일을 받지 않으므로 개인정보 이슈 0.

### 5. 처리방침 (`app/public/privacy.html`)
- §5 Anthropic 행: "질문 원문 및 사주 프로필" → **"질문 원문, 산출된 프로필, 그리고 입력하신 경우 이름(호칭)"**으로 명시. 이름이 가는데 안 적혀 있었다.
- **§5-1 '판결 공유 링크에 담기는 정보' 신설** — 질문 원문 포함 / 이름 미포함 / 회수 불가 / 서버 미보관 / 공유는 전적으로 선택. 타인 특정 가능 내용 주의 안내 추가.

### 헌장 문언 확장 (`CLAUDE.md`)
- 기존 "**계측** — 질문 원문·실명은 전송하지 않는다"는 **'계측'에 한정돼 공유 경로가 문언 밖으로 빠져나갔다.** 정작 노출은 계측보다 공유가 크다.
- 3경로(계측/판결생성/공유)로 나눠 각각 규칙을 박고, 판단 기준을 한 줄로 남겼다 — **"유저가 무엇이 나가는지 보고 있는가. 안 보이는 채로 나가면 그건 유출이다."**

### 함께 정리한 것
- **방어 3자산에서 '되물음' 제외.** 창업자 실사용("어노잉하기만") + 실측(n=1) + 경쟁사 무료화. **원인 진단: 되물음은 재방문 시 수호신이 먼저 묻는 push라 §모를 권리를 우리가 스스로 어긴 기능이다.** 기능 제거가 아니라 자원 배분 대상에서 뺀 것.
- **새 후보 등재: 11개 체계 동시 계산.** `App.jsx:1977`이 이미 사주·대운·삼재·토정·주역·납음·별자리·달위상·달별자리·수비학·나크샤트라·마야촐킨·MBTI·바이오리듬·가치여정을 LLM에 먹인다. 경쟁 8개사는 전원 만세력 단일 엔진. **우리만 데이터가 11개인데 판결 한 줄 뒤에 숨겨놨다.**

**빌드 통과** (`npm run build`, 291.90 kB / gzip 104.90 kB).
**아직 main에 안 올렸다** — main 푸시 = Vercel 자동배포이고 명시 지시가 필요하다. **①번 수정은 배포 전까지 라이브에 반영되지 않는다.**

### v94 보강 — 앞선 수정이 겉만 고쳤다 (적대적 검증 결과 반영)

변경분을 3축으로 검증시켰더니 **`n` 필드 삭제가 사실상 무효**였다. 확인한 것과 고친 것:

| 발견 | 확인 | 조치 |
|---|---|---|
| **판결문 자체가 이름을 부른다** | `App.jsx:1589` SYS — "호칭이 제공되면 결정적 순간에 이름을 부른다(B:○○아·A:○○님)". payload의 `v: res.verdict`에 그 문장이 그대로 실린다 | 공유 경로에서만 **호칭 형태를 걷어내는 `scrub()`** 추가. `이름+(아|야|님)` **뒤에 공백·문장부호가 올 때만** 지운다 — "지원아끼지"의 '지원아'를 잘못 지우지 않기 위해 |
| **구링크에는 이름이 들어 있다** | 이미 배포돼 나간 `?v=` 링크는 회수 불가인데 수신 화면이 `sharedIn.n`을 렌더 중이었다 | **폴백 분기 자체 제거** + `decodeShare`에서 `delete o.n` — URL→화면 경로 전체가 이름을 안 다룬다 |
| **계측에 판결 원문이 나간다** | `App.jsx:2010` `verdict: r1.verdict` — 그 문장에 이름이 들어갈 수 있으니 **헌장 §계측 실명 금지를 이미 어기고 있었다** | `verdict` → `vlen`(길이)로 교체. **그리고 내가 헌장 확장 때 계측을 "(현행 유지)"로 통과시킨 게 틀렸다** — 확인 안 하고 도장을 찍었다 |
| **처리방침 §5-1 세 문장 중 둘이 거짓** | ①"이름은 링크에 포함되지 않습니다" — 판결문 안에 있다 ②"공유하지 않으면 어떤 정보도 외부로 나가지 않습니다" — **같은 문서 §5가 Anthropic 전송을 적어놨다.** 문서가 자기와 충돌 | 둘 다 재작성. 이름은 "**남을 수 있다**+자동 제거는 완전하지 않다"로, 외부 전송은 "공유 링크를 만들지 않으면 **제3자에게** 전달되지 않는다(Anthropic·분석은 별개, 5·2항 참조)"로 |
| 시행일 미갱신 | `2026년 7월 24일` 그대로인데 §5·§5-1을 바꿨다 | 8월 14일 + 변경 요지 1줄 |
| 버튼 이름 불일치 | 방침은 '공유하기', 실물은 '카톡·라인으로 판결 보내기' | 실물로 맞춤 |
| **`clearMemory`가 6개 키 중 1개만 지웠다** | `store.removeItem(STORE_KEY)` 단독. `belief`(민감정보라고 코드 주석이 자인)·consent·firsttouch·daily가 남아 **"처음부터 다시"가 거짓**이었고, 리로드 시 앞사람 belief가 super property로 재부착됐다 | 5개 키 삭제 + `_ph.reset()`. **`INTERNAL_KEY`만 남긴다** — 지우면 팀 유입이 유저 지표에 섞여 D7 게이트가 무의미해진다 |
| 부적 새 탭 계측이 실패까지 'saved'로 집계 | `if/else` **밖**에서 track — `location.href = dataUrl` 폴백(브라우저가 차단)도 저장됨으로 찍혔다. iOS가 부적 저장의 주 플랫폼이라 도입 목적 자체가 무너진다 | `bujeok_viewer_opened` / `bujeok_failed`로 분리 |
| 공유 버튼에 인라인 고지 없음 | 부적엔 `fine` 안내가 있는데 공유엔 없다. **처리방침은 아무도 안 읽는다** | 버튼 아래 1줄 — "네 물음과 판결이 함께 담겨 나가. 보낸 링크는 되돌릴 수 없어." |

**교훈 하나만 남긴다.** 처리방침 §5-1을 **코드를 확인하지 않고 썼다.** 세 문장 중 둘이 틀렸고 **둘 다 우리에게 유리한 방향**이었다. 이번 세션 내내 남의 문서에 대고 하던 지적을 내 손으로 재현했다.

빌드 통과. **여전히 main 미배포.**

---

# [합류] `app/작업로그.md` 이관 (2026-08-17)

**개발 로그가 두 갈래였다.** 루트 `작업로그.md`(정본)와 별개로 `app/작업로그.md` 가 자라서, 8/12 각인 4단계·8/15 가명처리 전환 같은 무게 있는 기록이 **정본에 없는 쪽**에 쌓였다. 방향점검 §1-7이 지적한 그대로다.

아래 9건이 `app/작업로그.md` 에만 있던 전량이다(전문 이관, 유실 0). **원 문서는 포인터만 남긴 껍데기로 바꿨다.**
원 문서는 최신순이었고 이 문서는 오래된 순이라 **순서를 뒤집어** 넣었다 — 각 항목의 날짜 표기는 그대로다.

⚠ **날짜가 이 문서의 앞 항목들과 겹친다.** 8/6~8/17 구간은 위쪽에도 항목이 있으니, 그 구간을 조사할 때는 **두 곳을 다 봐야 한다.** 시간순으로 재정렬하지 않은 이유는 재정렬이 곧 원문 수정이고, 이관에서 가장 피해야 할 것이 유실이기 때문이다.

**앞으로 개발 로그는 루트 `작업로그.md` 하나다**(CLAUDE.md §기록 규칙).

## [2026-08-06] v109 · 알 권리 — 상세 리포트 완결화

**요청**: "오늘 만들어준 그런 식의 사주 리포트가 지금 상세 리포트 뽑으면 보이나? 지금 반영하면 좋겠는데. 그리고 알권리도 반영."

**확인한 구멍 — 셋 다 '있는데 숨긴' 것이었다**
1. `saju.pillars`(사주 여덟 글자)와 `saju.counts`(오행 개수)는 **온보딩 연출(step 2 · reveal===1, refbox)에만** 렌더됐다. v16 '수호신의 기억'으로 재방문 시 온보딩을 통째로 건너뛰므로 **유저는 자기 사주를 두 번 다시 볼 수 없었다.** 모든 판단의 뿌리인데 리포트에 없었다.
2. `MyeongsikReport` 가 `useState(false)` — 버튼을 눌러야 열렸다.
3. `.msrbody{max-height:170px}` — 열어도 한 번에 여섯 줄만 보였다.

**고친 것**
- `calcSaju`: 진태양시 보정값을 `tstAdj`(분)로 노출. 계산만 되고 화면 어디에도 없던 숫자였다.
- `MyeongsikReport` → 겉껍질(널 가드) + `MyeongsikReportBody`(훅). `open` 기본 `true`.
- 계측: `report_opened`(클릭) → `report_shown`(노출, `useEffect` 1회). 기본 펼침이 되면 클릭 계측은 영영 안 찍힌다.
- **명식 절 신설**: 네 기둥 카드(년·월·일·시, 일기둥 `.me` 강조) + 기둥 뜻 한 줄 + 일간(나) + 오행 막대 5종 + 계산 근거 고지(태양황경·율리우스일·만세력 28건 자동검증) + 진태양시 보정 분.
- 십성 전량 공개: `sipseongDist`가 0개인 십성을 아예 빼고 주므로 `SS10` 기준으로 채워 넣음.
- 온보딩 `<details className="refbox">` → `open`.

**레이아웃 회귀와 그 수정 (이번 작업에서 가장 위험했던 부분)**
`.vcard`는 `display:grid`에 앞뒤 면이 같은 `grid-area:1/1`이라 **카드 높이 = 두 면 중 큰 쪽**이다. 리포트를 키우자 뒷면이 카드를 늘렸고 **앞면 판결문 아래가 텅 비었다**(실측 430 → 593 → 1681px). 앞면은 코어 화면이라 이건 명백한 회귀다.
- 1차 시도(`.vscroll{flex:1;min-height:0}`)는 실패 — grid 행에 상한이 없어 `flex:1`이 안 먹는다.
- 2차 시도(`max-height:min(72vh,600px)`)는 600px로 묶이지만 여전히 앞면(430)보다 커서 공백이 남았다.
- **채택**: `.vface.back{position:absolute;inset:0}` — 뒷면을 카드 높이 산정에서 제외한다. 카드 크기는 앞면이 정하고, 길어진 리포트는 `.vscroll` 안에서 스크롤한다. 430px 복귀 확인.
- 부수: 한 문서로 흐르면서 본문이 고정 헤더 아래로 비쳐 `.vface.back .vtop`을 불투명 처리.
- 명식 네 기둥이 252px 폭에서 넘쳐 '시' 기둥이 잘렸다 → 기둥 뜻을 아래 한 줄로 내림.

**검증**: report-check 12→**20/20** · smoke 25/25 · verdict 34/34 · scope-check 54/54 · webgl 7/7 · v29 8/8 · mansae 28/28 · track 24/24 · origin 20/20 · 검진 심각 0(주의 3건은 기존: 형상 수식 이중화·dev 전용 audit·프록시로 라이브 확인 불가). 앞뒤 면 스크린샷 육안 확인.

**health-check에 추가한 4규칙(사고 #7 재발 방지)**: ①리포트 `useState(true)` ②`.msrbody`에 max-height 재등장 검출 ③`.vface.back` absolute + `.vcard` grid-template-rows 고정 ④리포트에 명식 원판 존재.

## [2026-08-12] v111 · 항목별 4단 — 리포트의 본체를 '삶의 자리'로

**요청(창업자 2026-08-11)**: "리포트는 지금처럼 사주베이스가 아니고 **각 항목별 설명이 들어가는 형태**.
태어날 때 정해진 운명이 이랬어, 그래서 크다가 이런 느낌이 들었을 거야, 넌 지금 어느 단계야, 앞으로 이렇게 될 거야.
항목도 여러가지겠지 — 학업 건강 연애 결혼 자녀 직장 궁합. 그리고 **선을 좀 넘어**. '어떤 류다' 이런 식으로 말하지 말고
어떤 일이 생길 거다 딱 명확히 말해. 아니면 어떤 류의 일이 생기는데 예를 들면 머머머머다, 이렇게 하던가."

**무엇이 문제였나**
리포트가 *사주 항목별*로 짜여 있었다 — 명식 → 타고난 것 → 흐름 → 사람 → 날 → 일.
전부 맞는 정보인데, 유저가 사려는 건 "내 십성 분포가 얼마인가"가 아니라 **"내 건강은 어떤가, 내 돈은 어떤가"**다.
같은 데이터를 **삶의 자리로 재배열**하는 것이 이번 작업이다.

**넣은 것 — `lifeDomains()` (순수 함수)**
- **삶의 자리 아홉**: 몸 · 마음 · 배움 · 일 · 돈 · 연애 · 결혼 · 자녀 · 사람 (연애·자녀는 성별 필요 → 없으면 일곱)
- **자리마다 4단**: 새겨질 때 → 자라면서 → 지금 → 앞으로
- 「지금」·「앞으로」는 **아홉 자리가 같은 규칙**으로 낸다 — 이 자리를 맡은 십성 무리가 지금 대운에 있나,
  없으면 언제 오나(`nowBy`/`nextBy`). 대운이 없으면(성별 미입력) 지어내지 않고 **못 짚는다고 말한다.**
- 「앞으로」에 `SS_EVENT` 사건 예시를 붙였다 — "정재 대운" 대신 **"월급이 오르거나, 계약이 하나 길게 붙거나,
  집·차처럼 네 이름이 올라가는 물건이 생겨"**. 나중에 맞았는지 틀렸는지 셀 수 있는 문장으로 쓴다는 뜻이다.
- `EL_ORGAN` — 오행별 장부와 증상. **병명이 아니라 '어디가 약한가'까지만**(창업자 지시 2026-08-10).
- 「몸」만 십성이 아니라 **오행 규칙**으로 움직인다. 가장 얇은 자리를 가장 센 기운이 치는 배치(火多金熔 꼴)를 따로 짚는다.
- 기존 절들은 「**셈의 근거** — 위 아홉 자리가 어디서 나왔나」 아래로 내려 보존했다. 알 권리는 그대로다.

**실측으로 잡은 결함 셋** (전부 화면을 실제로 찍어 보고 발견)
1. **글이 세로로 찢어졌다.** `.dstep` 이 flex 인데 강조 표시기 `Em` 이 만든 조각(`<span>`/`<b>`)이
   **하나하나 열이 됐다.** 본문을 `.dt` 한 겹으로 싸서 고쳤고, **회귀 검사로 못 박았다**
   (`p.children.length === 2 && children[1].classList.contains("dt")`).
2. **조사 오류** — "식신**로** 결이 바뀌어", "귀**이야**". 표에서 문장을 조립하니 받침 처리가 없으면 바로 티가 난다.
   `JONG()`(종성) → `RO()`/`IYA()` 헬퍼 신설. ㄹ 받침은 '로'를 쓴다.
3. **동률을 순서로 몰래 골랐다** — 십성이 전부 1개인 명식에서 "가장 두꺼운 게 겁재 1개"가 나왔고,
   일 자리에서도 재성 2·비겁 2 동률인데 배열 순서로 재성이 뽑혔다. **실사고 2026-08-02와 같은 병**이다.
   동률이면 **동률이라고 말하고 둘 다 낸다.**

**검사**: report-check **26 → 37항목**(4단 구조·세로 찢김·조사·사건 예시·아홉 제목).
smoke 25/25 · verdict 34/34 · scope-check 54/54 · 검진 79건 이상 없음(주의 4건은 기존).



## [2026-08-12] v112 · 용어 은닉 — 무슨 기법을 쓰는지 화면에 안 내보낸다

**요청(창업자)**: "어떤 분석 기법이 들어갔는지 안 나왔으면 좋겠어. 우리 기밀 유출 같아."

**먼저 정확히 짚은 것**
용어 자체(십성·대운·용신)는 **공개 지식**이라 그것만으로 기밀이 아니다. 실제로 새는 건
**어떤 기법을 어떤 표에 어떻게 매핑했는가**인데, 용어를 그대로 쓰면 그 조합이 한 화면에서 통째로 읽힌다.
이름을 갈면 정보량은 그대로면서 베끼는 비용만 오른다.
그리고 이건 창업자가 전에 낸 지시("너무 어렵게 설명해서 알권리가 헤치는 거 같아", 2026-08-11)와 **같은 방향**이다 —
알 권리는 **이해 도달**이지 용어 노출이 아니다.

**경계선을 먼저 정했다**
> **그 사람의 값은 보여주고(태어난 여덟 글자·간지·개수·나이 구간), 기법의 이름은 안 보여준다.**

값까지 지우면 v109에서 고친 알 권리 위반("카드 뒷면 근거를 안 알려줘서 몰랐다")으로 되돌아간다.

**바꾼 것**
- `EL_KO`(목→나무 …) · `SS_KO`(겁재→겨루는 힘 …) · `GRP_KO` · `SIN_KO`(문창귀인→글의 복 …) · `STR_KO`(신강→제 힘으로 미는 쪽) 신설.
  **코드 안의 키는 그대로 뒀다** — 키까지 갈면 명리 규칙과 대조가 안 돼 유지보수가 불가능해진다. 화면에 나가는 이름만 간다.
- 4단 본문에서 **대운 간지를 뺐다** — "33~42세 임오 대운" → "33~42세". 간지는 근거 부록에만 남는다.
- 연애·자녀 자리는 **어느 자리가 그 인연을 맡는지 말하지 않는다**("남자에게 인연은 재성" → "네 인연을 맡은 자리가"). 매핑 자체가 알맹이다.
- 확신도 꼬리표: 계산값/통설 해석/곁가지 → **확실한 것 / 갈리는 것 / 곁들이는 것**.
- 계산 근거 고지: "태양황경·율리우스일·만세력 대조" → "태양의 실제 위치를 직접 계산 · 대조 검증 28건". **신뢰 장치는 남기고 기법 이름만 뺐다.**
- **서신 프롬프트에 `[용어 금지]` 절 신설** — 프로필에는 용어를 그대로 실어 보낸다(모델이 추론해야 하니까).
  대신 **출력 금지 + 바꿔 쓰는 말 대응표**를 지시서에 박았다. 이게 없으면 9,900원짜리 문서가 용어를 그대로 받아쓴다.
  정직성 ③도 3단(용어→쉬운 말→모습)에서 **2단(쉬운 말→모습)**으로 고쳤다.

**되돌아오는 걸 막는 장치 둘** (이게 이번 작업의 본체다)
- `report-check`: **금칙어 43종을 실제 화면 텍스트에서 검사**한다. 하나라도 있으면 FAIL.
  동시에 "값은 그대로 남는가"(여덟 글자·흐름 구간)도 함께 본다 — 감추다가 근거까지 지우는 걸 막는다.
  `상관`은 "직접 상관없다"로도 쓰여 오탐이 나므로 따로 본다.
- `health-check` 검사 5-c: **서신 지시서에 [용어 금지] 절·대응표·프로필 주의문이 살아 있는지** + 리포트 렌더 구간에 용어 상수가 되돌아왔는지.
  ⚠ 첫 판에 헛울음이 났다 — 렌더 구간 슬라이스에 **코드 주석**이 섞여 "명식에서"를 잡았다. 주석을 걷어내고 보도록 고쳤다.

**검사**: report-check **37 → 43항목** · smoke 25/25 · verdict 34/34 · scope-check 54/54 · 검진 이상 없음(주의 4건은 기존).


## [2026-08-12] v113 · 각인 — 판결 밖의 두 번째 상품 (4단계 완주)

**요청(창업자)**: "1~4번까지 순차적으로 실행. 그리고 결제 안 붙어도 테스트는 해야 하니까 발행도 되게."

**자리를 먼저 정했다 — 서신 옆이 아니다**
| | 수호신의 서신 | 각인 |
|---|---|---|
| 무엇에 딸리나 | **질문 하나** | **사람 자체** |
| 몇 번 사나 | 판결마다 | **평생 한 번** |
| 진입점 | 판결 카드 뒷면 | **수호신 화면(로비)** |

재구매 구조가 반대라 같은 자리에 두면 안 된다. 판결 뒤에 붙이면 판결할 때마다 "또 사라는 건가"가 되고,
안 사는 사람에겐 매번 거절해야 하는 버튼이 된다. 그리고 "나는 너를 오래 지켜봤다"는 판결 끝보다
수호신 앞에서 나와야 세계관이 산다.

**2단계 — 엔진 이식** (`app/src/lib/sky.js`)
05_실험의 판독기 세 모듈을 한 파일로 합쳐 옮겼다. 각인의 「지금·앞으로」를 떠받치는 건 시간축 셋뿐이다 —
사주의 열 해 흐름 · 인도 다샤 · 서양 프로펙션. 나머지 열 종은 전부 정지 사진이다.
천문 함수가 App.jsx 에도 있는데 **합치지 않았다** — 사주 쪽은 만세력 28건 대조를 통과했고 거기를 건드리면
그 검증이 흔들린다. 대신 `e2e/sky-check.mjs` 로 두 벌이 240일치 같은 값을 내는지 못 박았다(차이 0).

**3단계 — 판정 규칙** (`app/src/lib/imprint.js`)
손으로 쓴 카피를 표로 바꿨다. 겉(SURFACE)·속(INNER)·**비어 있는 자리**(BLOCK)를 축으로 삼고,
몸 다섯 항목·짝 열 항목·뒤집히는 조건 셋·여든 해·열두 달을 낸다. 모든 문장이 각주 번호를 함께 낸다.

**검사가 잡은 것 셋** (전부 실물을 열어 보고 나온 것)
1. **여든 해가 통째로 같은 값**이었다. 앱의 대운은 한글 간지("임오"), 흐름 엔진은 한자("壬午") —
   한쪽만 찾으니 `indexOf` 가 −1 이었다. 둘 다 보게 고치고, **못 읽는 구간은 버린다**(조용히 틀린 값을 안 낸다).
2. **"세 셈이 전부 이 나이를 가리킨다"가 갈릴 때도 나왔다.** 목성 시기를 그대로 후보로 쓰니 사람에 따라
   예순여섯이 후보로 들어와 폭이 36년·53년까지 벌어졌다. **혼인 가능 구간(22~45세)에 걸치는 것만** 세고,
   폭 8년을 넘으면 "갈린다"고 적는다. 흩어진 걸 평균 내서 한 숫자로 내놓으면 합의가 아니라 눈속임이다.
3. **"3~12세에 만나는 사람은 오래 못 간다"** — 세 살짜리 인연. 인연 구간은 열여덟 이상만 세고,
   짝을 만나기 전/후로 갈라 뜻을 달리 준다(전=지나갈 인연, 후=흔들리는 결혼). 조사 오류("…일야")도 같이.

**4단계 — 화면** (`ImprintDoc`)
서신과 같은 전체 화면 자리에 걸되 상품은 별개다. **각주는 기본 접힘** — 검증용이라 늘 떠 있으면
기법이 그대로 노출된다. 「근거 보기」를 눌러야 열린다.

**1단계 — 진입점·발행** 결제 전이라 **무료로 열린다**(창업자 지시: 실물이 나와야 값을 매길 수 있다).
버튼에 「시험 발행」 배지를 달고 `imprint_clicked`·`imprint_opened`·`imprint_notes_toggled` 를 계측한다.
서신 4,900원 클릭률과 나란히 비교되므로 **"사람들이 질문 풀이를 원하나, 자기 자신을 원하나"가 숫자로 갈린다.**

**검사**: report-check 43→**57항목** · 여러 하늘 12/12 · 각인 엔진 26/26 ·
smoke 25/25 · verdict 34/34 · scope 54/54 · 검진 81건 이상 없음.


## [2026-08-12] v114 · MBTI 4문항 제거

**요청(창업자)**: "MBTI 묻는 건 이제 그냥 삭제해줘. 판결에 영향도 크지 않을 뿐더러 무슨 말인지 모르겠다는 사용자들이 많아서."

**그냥 지우면 안 되는 자리가 하나 있었다**
MBTI 는 온보딩 문항이면서 동시에 **수호신 비주얼의 축 하나**(밀도·속도·질서·반짝임)를 맡고 있었다.
문항만 지우면 `mbti` 가 null 이 되고, 렌더러의 `mbti?.[0] === "E"` 가 전부 false 로 떨어진다.
→ **새 유저의 수호신이 전부 같은 질감이 된다.** v27~v28 이 공들여 만든 다양성이 통째로 죽는다.

**그래서 묻지 않고 뽑는다** (`texture()`)
설계 헌장 그대로다 — *"수호신은 이미 너를 안다. 자동 파생 가능한 값은 손으로 되묻지 않는다."*
- E/I ← 사주의 뻗는 기운(목·화) vs 거두는 기운(금·수)
- N/S ← 별자리 원소(공기·불이면 N)
- T/F ← 일간 오행(금·토면 T)
- P/J ← 라이프패스 홀짝

**이미 저장된 mbti 는 버리지 않는다.** 시드 문자열에 들어가 있어서, 지우면 **쓰던 유저의 수호신 얼굴이 바뀐다.**
있으면 그대로 쓰고, 없을 때만 파생한다. 묻는 것만 없앤 것이지 받은 걸 버린 게 아니다.

**걷어낸 것** — DIMQ 표·`pickDim`·`dims` 상태·문항 화면·프롬프트의 MBTI 축(`VOTE_AX` 포함)·`reasons` 요구 문구.
회상 확인("응, 기억나") 뒤 곧장 마음의 방으로 간다. 온보딩이 한 화면 짧아졌다.

**검사가 잡은 것 — 이게 제일 컸다**
메모리 저장 조건이 `step === 3 && saju && mbti && core` 였다. mbti 가 null 이 되면서
**localStorage 가 통째로 저장이 안 됐다.** 재회·아침문안·판결록이 전부 죽는 조건이다.
스모크 5건이 한꺼번에 빨개져서 잡혔다. 조건에서 mbti 를 뺐다.
→ **필수 검증 목록에 '없어도 되는 값'이 섞여 있으면, 그 값을 없애는 날 앱이 조용히 죽는다.**

e2e 6개 파일이 MBTI 문항을 클릭하고 있었다. 경로를 고치고, v29-check 의 '문항 순차' 검사는
**'문항이 없다'는 검사로 뒤집었다** — 되돌아오면 우는 쪽으로.

**검사**: smoke 25/25 · report-check 57/57 · verdict 34/34 · scope 54/54 · v29 8/8 · webgl 7/7 ·
여러 하늘 12/12 · 각인 26/26 · 검진 81건 이상 없음.


## [2026-08-12] v115 · 각인 심화 — 9,900원짜리 두께로

**요청(창업자)**: "리온이 리한이 리포트 쓴 거보다 너무 빈약한데? 이거는 9,900원짜리 리포트라 생각하고
내용의 밀도와 깊이, 완성도를 높여봐. 그래프도 필요할 거고. + 추가로 받아야 하는 정보는 없어?"

**빠져 있던 건 4단 구조였다**
v113 각인은 겉·속·몸·짝·여든 해만 있었다. 스크래치패드 리포트의 **뼈대인 4단**
(태어날 때 → 자라면서 → 지금 → 앞으로)이 통째로 없었다. 그게 없으니 "지금 나에게 무슨 뜻인가"가 안 붙는다.

**넣은 것**
- **아홉 자리 × 4단 = 36칸** — 몸·마음·(가장 얇은 자리)·공부·일·돈·사람·연애·자식
- **그래프 셋** — ①겉/속과 막힌 문 도해 ②열두 달 막대 ③**여든 해 굴곡**(지금 구간을 금색으로)
- **확인 문항 열둘** — 근거를 안 대는 대신 확인할 방법을 준다. "일곱 이상 맞으면 참고할 만하다"
- 본문이 **4,700 → 5,957자**로. 회귀에 **4,000자 하한**을 못 박아 다시 빈약해지면 검사가 운다

**추가 입력 — 무엇을 받을지가 진짜 질문이었다**
도시는 이미 받고 있었다(선택). 실제로 값어치를 올리는 건 **지금 상태** 둘이다.

> 이걸 모르면 **마흔 살 기혼자에게 "서른에 짝을 만난다"고 쓰게 된다.** 그 한 줄이 문서 전체를 죽인다.

- **결혼했나 · 아이가 있나** — 두 번 탭. `married: true` 면 연애의 「앞으로」가
  *"이미 만났으니 이 자리는 지나갔어. 앞으로 말하는 건 인연이 아니라 그 사람과의 온도야"* 로 바뀐다.
  `kids: true` 면 자식의 「지금」이 *"이미 왔어. '올까'가 아니라 '어떻게 키우나'의 자리야"* 로 바뀐다.
- **묻는 자리는 각인을 열 때다.** 무료 온보딩을 무겁게 하지 않으면서 유료 문서만 정밀해진다.
  스무 살 미만에겐 안 묻는다. 안 알려줘도 문서는 나온다 — 그 자리가 헐거워질 뿐이다.
- 돈이 없으니 **선택**으로 두되, **왜 필요한지를 그 자리에서 설명**한다("이걸 모르면 지난 일을 앞일처럼 적게 돼").

**만들면서 걸린 것 둘**
1. `put` 이름 충돌 — 십성 세는 헬퍼와 4단 등록 헬퍼가 같은 이름이었다. `putD` 로 분리.
2. CSS 를 JS 주석 자리에 넣어 빌드가 깨졌다. 앵커가 스타일시트가 아니라 컴포넌트 주석이었다.

**검사**: report-check 57→**65항목**(4단 36칸·그래프 3·확인 12·본문 4,000자 하한·선택 입력이 실제로 문장을 바꾸는지) ·
각인 엔진 26/26 · 여러 하늘 12/12 · smoke 25/25 · verdict 34/34 · 검진 이상 없음.


## [2026-08-15] 유료 문서 계측 — '열었나'에서 '읽었나'로

**요청(창업자)**: "지금 메인, 브랜치 다 훑어봐 기능들이 상당히 업데이트되었는데 데이터 트래킹
안되고 있거나 더 새로운 개선 방안 있을지 / 내가 몰라서 말 못한거 까지 잡아내 그리고 바로 실행해"

**셈해 보니 비대칭이 심했다**
서신(4,900원)은 이벤트가 14개인데, **각인(9,900원)은 3개**, 궁합(4,900원)은 3개였다.
더 비싼 물건이 더 안 보였다. 구체적으로 안 보이던 것 셋:

1. **사고** — `readImprint`/`readMatch` 가 null 을 돌려주면 화면엔 오류가 뜨는데 우리한텐 아무것도 안 온다.
   궁합은 더 나쁘다: 입력 검증(`ok`)을 통과하고 `match_run` 까지 찍힌 다음 계산이 죽으면
   **말없이 입력 폼으로 되돌아간다.** 유저 눈엔 버튼이 안 먹는 것이고, 우리 쪽엔 "돌렸는데 결과가 없다"가 안 남는다.
2. **이탈** — 열람 수만 세면 두 줄 보고 닫은 사람과 끝까지 내린 사람이 같은 1건이다.
   9,900원짜리 문서가 값을 하는지에 데이터로 답할 수가 없었다.
3. **재구매** — 궁합의 존재 이유가 "각인은 평생 한 번, 궁합은 사람 수만큼"인데,
   그 논리가 작동하는지 볼 수 있는 유일한 신호(`다른 사람과도 봐볼게` 클릭)를 안 세고 있었다.

**넣은 것**
- `useDocRead(ev, props)` — 문서를 떠날 때 `read_pct`(스크롤 최대 도달률)·`sec`(머문 시간)을
  **열람당 1건만** 보낸다. 스크롤 컨테이너는 문서 자신이 아니라 바깥 `.readwrap` 이라 `closest` 로 올라가 붙인다.
  모바일은 탭을 안 닫고 떠나므로 `visibilitychange`·`pagehide` 에도 같은 flush 를 건다(중복은 `sent` 플래그로 막음).
- `imprint_failed` / `match_failed` — 실패 화면에도 `readRef` 를 붙여서
  "열자마자 깨져서 3초 만에 나갔다"가 남는다.
- `match_again` · `imprint_extra_answered` · `match_notes_toggled`.
- 데일리 리포트: 각인·궁합 줄 + 평균 읽음률, `말_문서실패`·`말_안읽힘` 코멘트.
  안 읽힘은 5건 이상 열렸는데 평균 40% 미만일 때만 뜬다 — 표본이 적을 때 우는 건 소용이 없다.

**별점은 안 붙였다**
서신엔 `letter_rated`(값어치 있었나)가 있는데 각인·궁합엔 없다. 그런데 그건 계측이 아니라
**화면을 새로 세우는 일**이라 여기서 조용히 하지 않았다. 읽기 깊이는 화면을 안 건드리고 얻히는
같은 방향의 신호라 그것부터 넣었다. 별점 UI 는 사장님 판단 대기.

**⚠ 답한 '값'은 보내지 않는다**
각인 문 앞의 선택 문항(혼인 여부·자녀 유무·만난 시기)은 처리방침 1조에 **"기기에만 저장"** 으로
고지돼 있다. 그래서 `imprint_extra_answered` 는 `answered`(답했는가)와 `n`(몇 개 답했는가)만 싣는다.
응답률을 보는 데는 그것으로 충분하고, 값을 실으면 고지 위반이다. 이 경계를 검사로도 못 박았다
(`doc-check`: "답한 '값'은 전송하지 않는다 — 키 자체가 없다").

**sky.js·imprint.js 에는 `track()` 을 넣지 않았다**
계산 라이브러리다. 계측은 화면 경계(App.jsx)에 있는 게 맞고, 순수 함수에 부작용을 넣으면
`sky-check`·`match-check` 같은 노드 단독 검사가 브라우저 전역을 요구하게 된다.

**검사**: doc-check **16/16 신설** · track 24/24 · report-check 103/103 · privacy 38/38 ·
궁합 29/29 · origin 20/20 · 검진 심각 0(주의 5건은 기존).
`e2e/onboard.mjs` 로 온보딩 헬퍼를 분리했고, `report-check` 에 `PW_CHROMIUM` 경로 우회를 붙였다
(번들 playwright 와 설치된 크로미움 버전이 어긋나는 환경에서 안 돌던 것).

---


## [2026-08-15] 질문·답변 본문 복원 — 단, 가명처리를 거쳐서

**요청(창업자)**: "질문과 답변은 다 남기자. 다만 개인 식별 불가능하게 해"

**왜 되돌리는가**
08-14 에 다른 세션이 판결문·근거 본문 전송을 끊고 글자 수만 남겼다. 개인정보 측면에선 안전한 선택이었지만,
그 결과 **어떤 질문에 어떤 답이 나갔고 유저가 그걸 '딱이야'로 봤는지 '빗나감'으로 봤는지를 맞춰 볼 수가 없어졌다.**
판결 품질을 고치려면 그 대조가 있어야 한다. 지금은 "만족도 33%"까지만 알고 왜인지는 영영 모르는 상태였다.

**문을 하나만 둔다 — `anon()`**
자유 서술이 계측으로 나가는 경로를 전부 이 함수 하나로 모았다. 여러 군데서 각자 지우면
한 군데를 빠뜨리는 날이 온다(08-14 의 뒷면 사고가 정확히 그거였다).

지우는 것: 본인 이름(stripName) · 이메일 · 링크 · 전화(`02)555-1234` 같은 괄호 표기 포함) ·
주민번호 · 카드/계좌 · SNS 계정 · 연월일 · 호칭 붙은 제3자 이름.
**지우고 비우지 않고 자리표를 남긴다** — `[메일]`·`[전화]`. 통째로 비우면 나중에
"이 판결이 왜 이상한가"를 읽을 수 없다.

**제일 어려웠던 건 오작동을 막는 쪽이었다**
"○○님"을 지우는 규칙은 만들기 쉬운데, 그러면 **"선생님"이 "○○님"이 되고 "부모님"이 "○○님"이 된다.**
데이터가 통째로 쓰레기가 된다. 그래서 `NOT_NAME` 목록(60여 개)으로 일반명사를 빼고,
검사에 "망가지면 안 되는 말" 여덟 개를 못 박았다(선생님·사장님·부모님·고객님·우리 팀장·어머님·형님·작가님).
그리고 한국어는 이름 뒤에 거의 항상 조사가 붙으므로("민준씨가") 조사까지 허용하도록 고쳤다 —
조사 없이 딱 끊길 때만 잡으면 실제 문장의 대부분을 놓친다.

**정직하게 적은 한계**
규칙으로는 **완전 익명화가 되지 않는다.** "3층 팀장이 어제 회식에서…" 같은 서술은 어떤 정규식으로도 못 잡고,
사람이 읽으면 특정될 수 있다. 그래서 이 산출물은 익명정보가 아니라 **가명정보**(개인정보보호법 §28-2)이고,
처리방침에 **무엇이 안 지워지는지까지 적었다.** 통계 목적 한정·미결합·보관기간이 함께 서야 성립하는 구조다.

**화면 문구도 같이 고쳤다**
로비 하단에 "질문 원문은 통계에 기록하지 않아요"가 있었다. 이 변경으로 **그 문장은 그 순간 거짓이 된다.**
검진이 이걸 심각으로 잡아 줬다(track 에 q 가 실리는지 대조하는 규칙이 있었다).
"질문과 답변은 이름·연락처를 지운 뒤 품질 확인용으로만 기록해요 — 끄고 싶으면 아래에서 끌 수 있어요"로 교체.
**앱이 화면에서 하는 약속과 코드가 하는 일이 어긋나는 건, 아무것도 안 적는 것보다 나쁘다.**

**검사 규칙을 뒤집었다**
privacy-check A-3 은 "본문이 나가면 FAIL"이었다. 지시가 바뀌었으니 검사도 바꿔야 하는데,
**그냥 지우면 안 된다** — 지우면 다음에 누가 원문을 그대로 실어도 아무도 안 운다.
그래서 **금지에서 경유 강제로** 바꿨다: 본문이 나가는 건 되고, `anon()` 을 안 거치는 건 안 된다.
health-check 의 신뢰 라인 규칙도 같은 방향으로 교체(화면이 '지운 뒤 기록'이라 적었는데 지우는 장치가
사라지면 심각으로 운다).

**검사**: privacy-check **76 → 84항목**(anon 누출 11 · 오작동 8 · 자리표 · 상한 · 빈값 · 고지 대조) ·
track 24/24 · doc 16/16 · report-check 103/103 · 궁합 29/29 · origin 20/20 · 검진 심각 0.
실제 질문 12개를 눈으로 대조 — 전남친/팀장님/언니/오빠/강남·판교 같은 평범한 문장은 한 글자도 안 변한다.

---


## [2026-08-16] 새고 있던 넷 봉합 + 곁 탭에 문 하나 (세션지시문 ① A·B)

**배분**: 작업배분 §6-2·§6-4 · 근거: 방향점검 §1-1·§1-9 · 곁탭IA §6

### A-0. tier 비용 누수 — "결제 후 문제"가 아니라 지금 새던 구멍

`api/judge.js` 는 `tier` 를 **요청 본문**에서 읽고, 방어는 Origin 헤더뿐이었다. Origin 은 스크립트가
마음대로 붙인다 — 즉 **결제가 없어도 아무나 `{tier:"paid"}` 로 유료 모델을 쓸 수 있었다.**
예전 주석은 이걸 "결제가 아직 없어서 무해하다"고 적어 뒀는데 그 전제가 틀렸다. 허용목록(TIERS)이
임의 모델 지정은 막지만 **우리가 등록해 둔 비싼 모델은 못 막는다.**

- `PAID_ENABLED = false` — 결제 배선 전까지 서버가 tier 를 안 믿는다. 결제 붙는 날 한 줄로 되돌리고 영수증 검증을 그 옆에 세운다.
- ⚠️ **봉합의 부작용을 하나 같이 막았다.** 로그의 콜 구분이 `tierKey === "paid" ? 3 : …` 이었는데
  tierKey 가 항상 free 가 되면 **서신이 전부 콜2 로 찍힌다** — v105.4 에서 고쳤던 오분류가 되살아난다.
  "무엇을 청했나(tierAsked)"와 "무엇으로 응답했나(tierKey)"를 갈라 `tier_served` 를 같이 남긴다.
- Vercel 환경변수는 이 세션에서 확인 불가(권한 없음). **코드 쪽에서 닫았으므로 env 설정과 무관하게 안전하다.**

### A-1. privacy.html 배너 — 내부 TODO 가 라이브에 떠 있었다

"(2) 변호사 또는 개인정보보호 전문가의 검토를 받고…" 가 **유저 화면 배너**였다. 유저 눈엔
*우리가 아직 안 한 일* 만 보이고, 정작 알아야 할 것(지금 무엇을 받고 무엇을 안 받는가)은 안 보였다.
→ 시험 운영 중이라는 사실 + 지금 방침은 그대로 지킨다 + 문의처 한 줄로 교체.
체크리스트 넷은 **HTML 주석으로 옮겼다** — 없애면 안 되는 목록이지만 배너 자리는 아니다.

### A-2. 궁합 엔진 버그 셋 (역할과초대 §B-0-b)

| | 무엇이 틀렸나 | 어떻게 고쳤나 |
|---|---|---|
| ① | 축⑨ `v` 가 `same ? 1 : 1` — 삼항인데 양쪽이 같아 **늘 긍정표** | `same ? 1 : 0`(마야 축과 같은 눈금). **A/B 실측: 「전부 어렵다」 분기 도달이 40,000쌍 중 0건 → 154건** |
| ② | 납음을 **달력 연도**로 셌다 — 각인은 입춘 보정 사주년 | `a.saju.nayin` 재사용. 두 번 계산하는 대신 한 곳에서 나온 값을 나눠 쓴다 |
| ③ | 축⑥ 이 **합만 보면서** "한쪽이 무겁고 한쪽이 가벼워"라고 단언 | `gap = |neptu차|` 를 실제로 재서, 갈렸을 때만 갈렸다고 말한다 |

①의 연쇄가 제일 컸다 — `plus` 가 항상 ≥1 이라 `split` 이 사실상 `minus` 만 보게 되고,
그래서 「아홉 축이 전부 어렵다」 분기가 **도달 불가능한 죽은 코드**였다. §B 설계의 시금석
("아홉 축이 전부 안 좋은 사람에게 뭐라 할 것인가")이 **코드가 막아서 존재할 수 없었다.**

### A-3. eval 실측이 잡은 진짜 실패 둘

- **direction=undefined 1건(P1-Q19)** — 방향 없는 판결이 그대로 화면에 나갔다. 가드를 넣되
  **HOLD 로 조용히 메우지 않는다**: 앞면 문장은 대개 이미 GO 처럼 쓰여 있어서 표식만 HOLD 로 붙이면
  고쳐진 게 아니라 **앞뒤가 어긋난 카드**가 된다(창업자: "모르겠다 식 hold 면 짜증난다").
  순서는 ①표가 있으면 표가 정한다 ②없으면 한 번 다시 청한다 ③그래도 없으면 실패로 떨군다(다시 청하기 있음).
- **용어·은유 누출 10건(4.0%)** — 샌 말이 **전부 이미 금지 목록에 있던 말**이었다("괘야"·"임오 대운").
  목록을 늘려도 안 고쳐진다는 뜻이다. 그래서 **실제로 새어나간 문장의 (X)→(O) 짝**을 프롬프트에 박았다
  (이 리포의 반복 발견: 모델은 금지 조항보다 예시를 따른다 — 검진 5-g 와 같은 이유).
  요령 한 줄도 같이: **용어를 지우지 말고 그 용어가 뜻하는 일로 바꿔 쓴다.** 지우면 문장이 빈다.
  은유 서술어 금칙을 서신에만 있던 것에서 **verdict·subline·reasons 전부**로 넓혔다.

### A-4. 평가 하네스 — 앱과 다른 프롬프트를 채점하고 있었다

- `:58` TODAY 가 옛 시계 형식(`14시`). 앱은 v132.10~11부터 **시진**(`미시(토)`)을 보낸다.
  시계를 뺀 건 그게 일반 상식("밤이니 자라")을 부르는 재료라서였는데, **하네스는 그 재료를 계속 보내고 있었다.**
- `:131` 동률 기대가 `go >= stop ? "GO"`(경험 편향). **v132.5 에 없어진 규칙**이다 — 지금 앱은 표가
  다수를 못 만들면 모델 방향을 그대로 둔다. 하네스만 옛 규칙에 남아 **동률 3건이 오탐**이었다.
  **오탐은 진짜 실패를 가린다** — 그래서 A-3 보다 이걸 먼저 고쳤다.

### B. 곁 탭 — 막다른 길에 문을 낸다

B-1(명부)·B-3(렌더 연결)은 **v134.2 에서 이미 했다**(`binari.gyeot.v1` + 순위 방어 셋 + `gyeotSeat`).
지시서가 그 머지 전에 쓰여 항목이 남아 있었다. 이번에 남은 건 B-2 하나:

- 곁이 0일 때 **나갈 문이 없었다.** "누가 서게 되면 이 자리에…"만 있고 *어떻게* 서게 하는지가 없다 —
  보이는 건 있고 할 수 있는 건 0인 화면이다. 그게 이 탭의 제일 큰 구멍이었다.
- ⚠️ 곁탭IA §5 「첫 화면을 결제벽으로 만들지 마라」와 부딪히지 않게 **값을 안 쓰고 강조 버튼도 안 쓴다** —
  상품 진열이 아니라 **자리를 채우는 유일한 경로의 안내**다. 검사로도 못 박았다(값·원 표기 0).
- B-4(초대·회신 승격)는 **안 건드렸다** — 분모 100 이후다.

### 검사

- 신설/확장: `match-check` 39→**45**(버그 셋 각각) · `gyeot-check` 21→**28**(B-2 문·계측·'답 대기') · 검진 **5-o** 신설
- 검진 5-o 는 넷 전부에 **오염을 주입해 실제로 붉게 뜨는지 확인**했다. 한 번은 자기 오탐이 났다 —
  깨진 꼴(`same ? 1 : 1`)을 찾게 했더니 **바로 위 주석이 그 문자열을 인용해서** 걸렸다.
  → 규칙은 "없어야 할 것"보다 **"있어야 할 것"** 으로 쓴다(주석에 안 걸린다).
- `gyeot-check` 의 로비 인사말 단언이 **시각 의존**이라 밤에 돌리면 실패했다(선재 결함). 둘 중 하나면 통과로 고침.
- 전량: 검진 심각 0 · smoke 35/35 · verdict 35/35 · doc-check 21/21 · 곁탭 28/28 · 궁합 45/45 ·
  각인 130/130 · 리포트 121/121 · 개인정보 111/111 · 역할 26/26 · 곁명부 30/30 · 곁예산 13/13 ·
  공유서명 16/16 · 공개표면 33/33 · 질감 9/9

### 남긴 것

- **BINARI_MODEL_PAID 의 Vercel 실제 설정값은 확인 못 했다**(세션 권한 없음). 코드에서 닫았으므로
  설정과 무관하게 안전하지만, 창업자 확인이 필요하면 그건 사람 쪽 항목이다.
- A-3 의 두 수정은 **프롬프트 변경**이라 실효는 다음 eval 실행에서만 확인된다. 이 세션엔 API 키가 없다
  (§6-1 데이터 레인 3번과 같은 제약). A-4 를 먼저 고쳐 뒀으므로 **다음 실행은 오탐 4건 없이 시작한다.**

---

## [2026-08-17] 데이터 레인 5건 — 계측 오염 마감·리텐션 자산·대시보드 산식

**요청(창업자)**: 방향점검 §1-6 / 작업배분 §6-1 데이터 레인 1~5번.

### 1. 손 실측 이중 필터 강제 (완료)

`daily-report.py` 가 `is_internal != true` **하나만** 걸고 있었다 — 8/15 전략 문서를 뒤집은
바로 그 오류다. `EXT` 상수 하나에 코호트 436757 제외를 더하고, 내부 쪽 줄은 그 여집합(`INT`)으로
맞췄다. 플래그만 보면 코호트 내부인이 어느 쪽에도 안 세어져 **합이 안 맞는다.**

`person_id NOT IN COHORT` 가 `countIf()` **안에서도** 컴파일되는지 실제로 확인하고 넣었다
(집계 조건에서 코호트가 안 먹으면 리포트가 조용히 빈 값을 낸다).

이중 필터 45일 실측: **판결 69회/13명 · 공유 발신 0 · 카드 저장 0 · 가격노출 26→확인 25(96%)→발행 24.**
문서의 68/12 와 하루치만 다르다 — 정의가 같다는 뜻이다.

### 2. 리텐션 자산 3종 계측 부착 + D7 창 정의 (완료)

`days_since_first` 를 **모든 이벤트의 공통 속성**으로 올렸다. 재방문을 세도 그게 D1인지 D7인지
D30인지 구분할 방법이 없던 게 근본 결손이었다. 이벤트를 새로 만드는 것보다 싸고,
어떤 지표든 D별로 쪼갤 수 있게 된다.

붙인 이벤트 7종: `streak_day`·`streak_broken` / `daily_offered`·`daily_opened`·`daily_received` /
`log_opened`·`log_row_opened`.

설계에서 신경 쓴 셋:
- **`daily_offered` 가 분모다.** 없으면 "안 열었다"가 *안 궁금했다*인지 *안 떴다*인지 영영 못 가른다.
  되물음이 문안 자리를 뺏는 구조라 `blocked_by_askback` 도 같이 싣는다 — 안 밝히면 노출 수가 서로를 오염시킨다.
- **끊김을 따로 남긴다**(`streak_broken`). 이어짐만 세면 "몇 명이 습관이 됐나"는 알아도
  **"며칠에서 끊기나"** 를 못 읽는다. 붙잡을 지점을 정하려면 끊긴 자리가 필요하다.
- **StrictMode 이중 발사 함정** — 스트릭은 `setState` 업데이터 안에서 계산되는데 React 는 개발 빌드에서
  업데이터를 두 번 부른다. 그대로 track 하면 이벤트가 두 배가 된다. 날짜를 열쇠로 한 ref 로 하루 1회를 잠갔다.

**D7 정의는 새로 정하지 않고 실물에서 확인해 적었다** — 게이트 인사이트 `xscpleu9` 가 이미
`RetentionQuery · period=Day · targetEntity=verdict_shown · retention_first_time · Day 7 ≥ 20%` 다.
즉 **"7일 안에 한 번"이 아니라 "7일째 되는 날에 활동"**. 둘은 크게 다르고, 롤링 쪽을 D7 이라 부르면
**게이트를 몰래 낮추는 것**이 된다(CLAUDE.md §관문). 보조로 볼 수는 있으나 반드시 다른 이름으로 부른다.
정본은 `app/HANDOVER-데이터계측-2026-07-25.md` §2-D.

### 4. 루프 지표 재산출 · q_anon 분포 (완료)

`offer_shown` 이라는 이벤트는 **없다** — 실제 이름은 `imprint_offer_shown`/`match_offer_shown` 이고
첫 발생은 2026-08-15T01:06:23Z 다. 그 창으로 좁힌 이중 필터 외부 실측:

| | 노출 | 클릭 | 비율 |
|---|---|---|---|
| 각인 | 7 | 2 | 28.6% |
| 궁합 | 7 | 6 | 85.7% (→ 실행 4) |

⚠ **노출 대상이 3명이다.** 비율을 신호로 읽으면 안 된다. 둘의 분모가 같은 건 두 제안이
같은 자리에 함께 뜨기 때문이고, 그래서 **상대 비교만** 의미가 있다(궁합 훅이 각인 훅보다 세다 —
디자인 레인의 "각인 카드 훅이 구조적으로 약하다" 판정과 방향이 같다).

`q_anon` 은 질문 11건 **전량에 부착**되어 있고, 잔존 식별자 검사(메일·전화·연도·URL·자리표) **전부 0건**.
가명처리가 운영에서 실제로 서 있다. 다만 평균 26자·최대 37자라 **분포를 분석할 표본이 아직 아니다** —
분모 100 이후로 미룬다.

### 5. 게이트 대시보드 산식 갱신 (완료)

`e67ieZvV` 를 `letter_clicked/verdict_shown` → `letter_intent_confirmed/letter_price_shown` 으로 교체.

**다만 지시를 그대로만 따르면 계기판이 거짓말을 한다.** 새 산식은 96%가 나오는데,
같은 방향점검 §1-3 이 "확인 버튼이 지불 관문이 아니라 **무료 콘텐츠 관문**"이라고 이미 판정했다.
그래서 이름을 "지불 의사 신호"에서 **"2단 fake-door = 확인/가격노출"** 로 바꾸고,
설명에 "결제가 붙기 전까지 죽음신호 판정에 쓰지 말 것"을 박았다.

HANDOVER §2 "이벤트 19종" → **실측 54종**(60일 실수신) 표로 교체하고,
"정본은 문서가 아니라 PostHog"이라고 적으며 재산출 질의를 함께 남겼다.
동의 2단계 서술도 사실에 맞췄다(PROFILE_KEYS 는 2026-07-28 이후 빈 집합, MBTI 는 제품에서 제거됨).

### 못 닫은 것 — 창업자 손이 필요하다

**팀 5명 ?i=1 은 여전히 0명이다.** 외부로 잡히는 13명 중 `?i=` 를 거친 사람이 한 명도 없다.
그리고 **`?i=1` 은 소급되지 않는다** — 지금 눌러도 과거 45일은 안 고쳐진다.
과거를 고치는 유일한 길은 코호트 436757 편입이고, 그건 사람을 지목하는 일이라 내가 임의로 하지 않았다.

시금석(`tab_switched`)은 **64회/3명, 그중 1명이 59회**로 커졌다(문서 기록은 62회/2명).
곁 탭이 만들어진 8/15~16 이틀에 집중됐다. 다만 **URL 로는 증명 실패** — 전원 운영 도메인이고
미리보기·`?trackdebug`·`?gyeot` 접속이 0이다. 정황뿐이라 임의 편입은 하지 않았다.

**검사**: retention-check **25항목 신설** · track 24/24 · doc 21/21 · privacy 111/111 ·
report 121/121 · verdict 35/35 · 궁합 39/39 · origin 20/20 · 검진 심각 0.

---

---

## [2026-08-17] 결정 9건 최적안 시행 — 무료 발행 표식·처리방침 문구·이력정리 준비

**요청(창업자)**: "최적안으로 우선 다 진행해" — 결정필요사항 9건 전부 추천안으로 확정하고 실행.

### 1. 결정 8 — 무료 발행 코호트 표식 (완료)

**아홉 중 유일하게 소급이 안 되는 항목이라 먼저 쳤다.** 지금 서신·각인·궁합은 값을 표시하고도
실물을 무료로 준다. 결제가 붙는 날 초기 표본 전원이 "공짜로 받아 본 사람"이 되고,
그 코호트로는 **처음 사는 사람의 지불 의사를 못 잰다.** 지금 표식을 안 남기면 나중에 못 가른다.

- `PRICING_MODE = "free_trial"` 을 `_superProps` 에 깔았다 — **모든 이벤트에 붙는다.**
  이벤트를 새로 만드는 것보다 싸고 어떤 지표든 체제별로 쪼갤 수 있다(§D3 belief 와 같은 패턴).
  결제 붙는 날 `"paid"` 로 한 글자만 바꾼다.
- `markFreeIssue(kind)` — 한 번이라도 무료로 받은 사람에게 `free_issued` 를 **person 속성**으로 박는다.
  `register()` 는 기기 단위라 기기를 바꾸면 끊긴다 → `$set` 으로 person 에.
  첫 수령 시점·종류는 `$set_once` 라 나중 수령이 덮지 않는다.
- **서신만 걸면 안 된다** — 각인·궁합도 값을 표시하고 무료로 준다. 셋 다 걸었다.
- ⚠ TDZ 주의 — `PRICING_MODE` 사용처(`_superProps` 조립)가 정의부보다 위라, 상수를 파일 앞
  `let _superProps` 옆으로 올렸다. 함수 안이라 실제로는 안 터지지만 순서에 기대는 코드를 남기지 않는다.

**검사 `app/e2e/pricing-check.mjs` 12/12 신설** — 소스 성질 8 + 실주행 4.
값이 아니라 **성질**을 본다(상수 이름이 바뀌어도 살아 있게): 고정 속성인가 · person 속성인가 ·
`$set_once` 인가 · 세 상품 전부 걸렸나 · 문이 하나인가 · 사람당 1회인가.
실주행에서는 **아직 아무것도 안 받았으면 표식이 안 나가는 것**까지 본다(오탐 방지).

### 2. 결정 9 — 처리방침 「(초안)」 제거 (완료)

`<h1>개인정보처리방침 (초안)</h1>` → `(초안)` 삭제. **이건 고지 문서지 초고가 아니고,
스스로 초안이라 부르면 고지로서의 지위가 약해진다.** 시험 운영 단계라는 사실은 바로 아래
안내문이 이미 밝히고 있어 정보량 손실이 없다.
*(내부 체크리스트 노출은 8/16 A-1에서 이미 HTML 주석으로 내려가 있었다 — 다시 배너로 올리지 말 것.)*
변호사 검토 자체는 결정 9(b)대로 **판매 개시 직전.**

### 3. 결정 7 — 이력 정리 준비 (실행 대기)

`tools/history-redact.py` 신설. 방식은 **값 치환이 아니라 파일 통째 교체** —
두 검증 스크립트의 모든 과거 판본을 지금의 가상 명식 판본으로 바꾼다.
값을 열거해 치환하면 ①그 스크립트 안에 PII 를 옮겨 적게 되고 ②같은 숫자를 쓰는 다른 파일까지 건드린다.

**AI 가 못 하는 것 둘이 남았다**: ①저장소 비공개 전환(GitHub 설정 화면, 도구 없음 — **이게 먼저다**)
②강제 푸시(다른 세션이 붙어 있는 동안 하면 그 커밋을 날린다 — 사람이 시점을 골라야 한다).
그리고 푸시 뒤에도 **GitHub 지원팀 캐시 삭제 요청**이 남는다 — 고아 커밋은 즉시 안 지워진다.

### 검사

빌드 OK · 개인정보 111/111 · 판결 35/35 · smoke 35/35 · track 24/24 · retention 25/25 ·
**pricing 12/12(신설)** · 검진 심각 0.

## [2026-08-18] 레인별 미완 마감 — 앞면 용어 가드는 코드로 옮겼다 (0-f · 데이터 4·5)

**요청**(창업자): "세션별로 미완된 것들 다 완료시켜."

### 0-f. 프롬프트로 두 번 실패한 자리

v134.4 가 A-3 으로 앞면 용어 누출을 **프롬프트 보강**으로 막으려 했다. 키 보유 세션이 60칸을 재실행해 대조한 결과 **3→4 건으로 늘었고**, P7-Q17 은 **같은 칸에서 같은 단어(「임오 대운」)가 재발**했다. 원인 가설은 뒷면(콜2) 지시문이 근거를 「용어 — 쉬운 풀이」로 쓰라며 예시로 "무오 대운"을 드는 것 — 앞면 금지와 옆자리 권장이 부딪힌다.

→ **세 번째로 조이지 않고 코드로 옮겼다.**

- `FRONT_JARGON` — 하네스 `JARGON` 과 **같은 목록**. 갈리면 한쪽만 잡는다.
- `verdict_jargon` 가드 — 앞면에 용어가 잡히면 **문장만** 다시 받는다.
  ⚠ **판정은 다시 하지 않는다.** 콜1 을 새로 부르면 `direction` 이 바뀔 수 있다 —
  용어 하나 고치자고 판결을 갈아치우는 셈이 된다. 그래서 rewrite 응답에서 `verdict` 만 취하고,
  비었거나 여전히 용어가 있거나 45자를 넘으면 **원문을 그대로 둔다**(용어 섞인 판결은 아쉽지만,
  뜻이 어긋난 판결은 제품 실패다). 실패 사유는 `why` 로 계측에 남긴다.
- 검진 **A-5** — ①가드 생존 ②앱·하네스 목록 일치.
  목록 문자열을 못 박지 않고 **두 파일에서 정규식을 뽑아 맞댄다.** 목록을 늘려도 검사는 살아 있고,
  원래 잡으려던 "둘이 갈렸다"는 그대로 잡는다(CLAUDE.md 가상명식 규칙과 같은 이유).
  ⚠ 같은 파일 882행에 다른 블록의 A-5 가 이미 있다 — 이 블록은 A-0~A-4 를 잇는 연번이다.
- **음성 테스트로 검증**: 목록을 갈라놓으면 "앱·하네스 목록이 갈림", 가드를 지우면 "가드가 없음" —
  둘 다 실제로 걸렸다. 통과만 보고 넘어가면 거짓 통과를 못 거른다(v130 `grab()` 사고와 같은 이유).

**실효는 다음 eval 실행에서만 보인다** — 이번 60칸은 가드 도입 전 산출이다.

### 데이터 레인 4·5

- v128 이후 창 실측(이중 필터): 궁합 노출 14/6명 → 클릭 13**/3명**(회수는 높은데 사람은 절반 — 소수 반복),
  각인 14/6 → 8/4, 서신 가격노출 3 → 확인 2, **card_saved 외부 첫 1건**, 공유 발신 **여전히 0**.
- **q_anon 분포(결정 3 이행)** — 외부 `question_asked` 116건 중 `q_anon` 부착은 13건뿐인데,
  나머지 103건은 **가명처리 도입(8/15) 이전**이라 원래 없다(버그 아님). 붙은 13건은 결측 0.
  분포는 「오늘 뭐먹지」 4 · 일·진로 2 · 연애·결혼 2 · … **n=13 이라 판독 불가로 판정**했다.
  최다가 「오늘 뭐먹지」인 건 CLAUDE.md 가 열어둔 물음과 같은 방향이지만 **13건으로 답하지 않는다.**
  분모 100 후 다시 돌릴 질의를 HANDOVER 에 박아뒀다.
- 대시보드 `e67ieZvV` 는 데이터 세션이 8/17 에 이미 2단 정의로 교체 — 확인만 했다.
  HANDOVER 요약표의 「19종」은 인벤토리 절만 고쳐지고 남아 있어 **54종**으로 정정.

### 안 고친 것

**건강인데HOLD(P7-Q23) — 분산으로 판정.** 같은 칸이 앞 실행에선 GO, 이번엔 HOLD 였다.
n=1 이고 실행 간에 흔들린 것이라 규칙 문제로 볼 근거가 없다. 다음 전량 실행에서 재발하면 그때 본다.


## [2026-08-26] v138 — 방향점검 선결의 코드 몫 이행 (지시: "메인에 반영해서 개선해봐")

방향점검 2026-08-26이 확정한 「분모 100 전 선결 7」 중 코드·문서로 닫히는 넷(①②③④)과 축4 소품 하나를 이행했다. 창업자 결정 몫(⑤혼합 공개·⑥rating 활용)과 사람 몫(⑦실기기 FPS)은 건드리지 않았다.

1. **문안↔되물음 우선순위 교체** — 되물음 제외 결정(8/14)의 진단("모를 권리를 스스로 어긴 push")과 "push가 당김형을 밀어내는 배치"가 어긋나 있었다. 이제 문안이 먼저 서고, '받았어' 후 같은 방문에서 되물음이 이어진다. 계측은 `blocked_by_askback`(문안이 밀림) → `askback_deferred`(되물음이 밀림)로 — **옛 속성명을 재사용하지 않았다**(같은 이름이 반대 뜻이 되면 과거 데이터 합산이 오염된다). `askback_shown`도 실제 노출 순간에만. ⚠ 처음엔 이펙트를 원래 자리에 뒀다가 deps의 `dailyData`가 선언 앞이라 TDZ로 화면이 죽었다 — 검진이 잡아서 이펙트를 선언 뒤로 옮겼다(주석 명기).
2. **/c 카드 귀속(판정 A 6건 전부)** — `CARD_URL` 상수, 부적 포스터·cardFoot 주소 `…/c`, vercel.json redirects(`/c`→`/?ref=card`, 이 리포 첫 사례 — **배포 후 curl 확인 필수**), 처리방침 부적 문단 정정(주소만이 아니라 **수호신 그림·판결문도 열거에 없었다** — 그리는데 안 적힌 상태), HANDOVER 도메인 체크리스트 6번(vercel.app `/c`는 죽이지 말 것). 겸 판정 B 코드 몫 둘: 하네스 길이 임계 50→45, 포스터 두 줄 잘림을 `card_saved.verdict_clipped`로 계측.
3. **subline 규칙 3파전 해소** — SYS 안에서 subline 규칙이 셋으로 갈라져 있었고(제1원칙 금지 / 층위 절 예시가 괘 이름 권장 / 콜2 금지) 실측 subline 용어 33건이 정확히 그 권장 예시 포맷이었다. **정본=제1원칙**(스스로 "다른 모든 규칙보다 앞선다"고 선언한 유일한 규칙 — 새 결정이 아니라 선언된 우선순위의 집행). 층위 절 예시를 맨말로, 콜2의 금지 예문 재조립("편재 둘 — …네 그릇이야")을 교체. 하네스에 subline 용어 플래그 신설 — 단 **측정이지 자동 실패가 아니다**(부분일치 오탐 「야무진」→무진 실재, 주석 명기). 실효는 다음 eval 실행에서.
4. **각인·서신 머리 인장(GuardianSealMini)** — 돈 받는 화면에 수호신이 없던 비대칭(무료 카드엔 神 인장, 유료 문서엔 0)을 궁합 방식(Canvas2D 정지화·컨텍스트 0·프레임 0)의 1행성판으로 해소. 철회 사유(v127.6 렉)는 WebGL 2번째 컨텍스트였으므로 재발 구조가 없다. 시드=일간이라 명식이 같으면 같은 얼굴. 실물 스크린샷으로 확인.
5. **정리** — `introSeen`(쓰기 전용)·`_unusedBar`(v118 잔해, 존재하지 않는 witness 참조) 제거. retention-check를 새 우선순위로 갱신(+옛 속성 부재 검사 1).

검사: 빌드 OK · 검진 "당장 고칠 것 없음" · smoke 35/35 · 판결 경로 35/35 · retention 26/26(신규 2 포함) · 각인 130/130 · track 24/24 · doc 21/21 · privacy 113/113 · scope 55/55 · pricing 12/12 · card-shot 실물 렌더로 `/c` 조판 확인.

---

## [2026-08-27] v142 · 동전이 돌아왔다 — 재미와 마찰을 같은 동작 하나로

**요청(창업자)**: *"C 다시 넣자. 대신 좀 더 재밌게 해봐 확실한 엔터테인먼트 요소가 되도록."*
그리고 사유가 하나 더 붙었다 — *"동전을 되살려야 과도하게 사용하는 걸 방지할 수 있을 거 같아.
무료로 바꾼 이상, 일정 허들은 남겨야겠어."*

### 왜 켜는가 — 이유가 둘이고, 둘 다 코드 주석에 박았다

하나만 알면 다음 세션이 잘못 끈다.
- **재미** — 운세가 줄 수 있는 재미 넷(나에 대한 서술 / 단언 / 우연의 연출 / 관계) 중 '우연의 연출'.
  **재미가 결과 전에 일어나는 유일한 축**이다. 그리고 이것만이 데일리 리텐션 조건 ①을 정직하게 채운다 —
  **명식은 평생 안 바뀌지만 오늘 던진 동전은 매일 다르다.**
- **마찰** — 세 상품이 전부 값을 표시하고 무료로 나간다. **여기서 마찰은 버그가 아니라 기능이다.**
  돈이 아니라 **주의(注意)**로 값을 받는 자리.

### 무엇을 바꿨나

- **`한 번에 던지기` 제거.** 그 버튼이 있으면 재미도 마찰도 동시에 무너진다. `tossAll` 함수째 삭제.
- **쥐었다 놓는 던지기.** `onPointerDown` 은 쥐는 연출만, `onClick` 이 실제 던지기 —
  둘을 나눠야 키보드(Enter/Space)로도 던져진다.
  ⚠ **쥔 시간은 결과에 영향을 주지 않는다.** 체공 시간만 620→최대 1,220ms 로 늘어난다.
  손맛을 위해 결과를 손에 맡기는 척하면 그건 프롬프트에서 금지하는 '지어낸 숫자'와 같다.
  실수로도 안 섞이게 **`oneCoin()` 은 인자를 아예 안 받는다**(검사가 이 성질을 본다).
- **마지막 여섯 번째만 +260ms.** 절정은 길어야 절정이다.
- **변효(6·9) 강조** — 그 효만 밝게 + 한 줄. 네 번에 한 번꼴이라 절정이 될 만큼 드물고 기다릴 만큼 잦다.
  뜻을 먼저 말하고 이름은 안 붙인다(§용어 은닉) — "노양이야"는 아무 말도 안 한 것과 같다.
- **계측 셋 신설** — `ritual_started` · `ritual_tossed`(회차·값·변효·쥔 시간) · `ritual_abandoned`(몇 개 던지고 나갔나).
  **v129.2 에 끈 사유가 "허들같아보여서"라는 검증 불가능한 추측이었다.** 이제 중도 이탈이 보인다.

### 수호신 디자인 변경과 분리해 뒀다 (창업자 참고사항 반영)

디자인 세션이 수호신을 **도트→홀로그램**으로 바꾸는 걸 검토 중이다. 그래서 의식의 시각 요소는
전부 `.hexpanel` 안에서 자족한다 — `canvas`·`texture()`·셰이더를 **한 번도 참조하지 않는다.**
유일한 접점은 기존 `agitateRef`(낙착마다 존재가 일렁인다) 하나이고 그건 없어도 의식이 성립한다.
**수호신이 바뀌어도 이 화면은 안 고쳐도 된다** — `ritual-check` 가 이 성질을 검사로 못 박았다.

### 병합·검사에서 잡은 것 셋

1. **대기 문구 중복** — 의식 패널과 전역 `.brooding` 이 "조각들이 합의하는 중…"을 동시에 그려
   Playwright strict mode 로 `brood-check` ②·④가 깨졌다. 의식 패널 쪽을 없앴다(대기 문구는 한 곳).
2. **클래스 이름 충돌** — 동전 앞면에 `.cf` 를 썼는데 **확신도 꼬리표(`<Cf/>`)가 이미 그 이름을 쓴다.**
   남의 테두리가 「앞」에 붙어 나왔다. **실물 스크린샷을 떠서 잡았다** — 검사로는 안 잡히는 종류다.
   `.cface`/`.cback` 으로 개명하고 사유를 CSS 주석에 남겼다.
3. **검사가 자기 설명에 걸림** — `ritual-check` 가 문자열 `한 번에 던지기` 로 부재를 검사했는데,
   위 주석에 "없앴다"고 적혀 있어서 그 설명 자체가 검사를 깼다. **함수명(`tossAll`)으로 바꿨다.**
4. **주석이 빌드를 죽였다 — 그리고 내가 한동안 못 봤다.** 2번을 고치면서 CSS 주석에 백틱을 썼는데
   **그 CSS 는 템플릿 리터럴 안**이라 백틱이 리터럴을 끊어 빌드가 죽었다.
   ⚠ 더 나쁜 건 **내가 `npm run build 2>&1 | tail -2` 로만 확인해서 못 봤다는 것**이다 —
   실패 스택이 마지막 두 줄을 채워 "✓ built" 가 안 보였고, 그 뒤 검사들이 **낡은 dist 를 통과했다.**
   → **빌드 확인은 `tail` 이 아니라 `grep -E "✓ built|error"` 로 한다.** 산출물 시각·해시도 같이 본다.
   → CSS 블록 안 주석에는 **백틱을 쓰지 않는다**(사유를 그 자리에 못 박았다).
5. **디자인 세션과 버전이 두 번 겹쳤다** — v140·v141 을 그쪽이 먼저 썼다(홀로그램 A/B → 색장 렌더러).
   같은 번호 두 판이 생기면 배지가 거짓말하므로 v142 로 밀었다.
   **그리고 그 재기반이 결합 분리의 실전 시험이 됐다** — 수호신 렌더러가 통째로 바뀐 위에서
   의식이 그대로 돈다(실물 스샷 확인). 안 고쳤다.

### e2e 전면 수리

의식이 꺼져 있던 11판 동안 검사들이 "청한다 → 곧장 판결"을 전제로 쓰였다.
`e2e/ritual.mjs` 헬퍼를 `onboard.mjs` 와 같은 이유로 하나로 빼고(문구가 바뀔 때 한 곳만 고친다),
클릭 지점 13곳에 통과를 삽입했다. **의식이 다시 꺼지면 헬퍼가 조용히 통과한다** — 검사가 스위치에 안 묶인다.

⚠ **`journey.mjs` 는 손대지 않았다.** 확인해 보니 **이 변경 전부터 깨져 있다** — v128.1 에 제거된
MBTI 문항 단계에서 멈춘다(main 에서 재현 확인). 스크린샷 촬영용 스크립트라 판정에 안 들어가고,
되살리려면 온보딩 절부터 다시 써야 해서 별건으로 남긴다.

### 검사

빌드 OK · **ritual 27/27(신설)** · 판결 35/35 · smoke 35/35 · 대기연출 10/10 · 개인정보 113/113 ·
리텐션 26/26 · track 24/24 · pricing 12/12 · 리포트 122/122 · 곁 37/37 · 명부 42/42 · 각인 130/130 ·
궁합 55/55 · 역할 26/26 · 공유 16/16 · 공개표면 33/33 · 검진 심각 0.
⚠ `layout-check` 8/12 는 **main 에서도 같은 4건이 깨진다**(사전 존재, 이번 변경과 무관 — 확인함).

## 2026-08-28 · v144 「빛의 형태」 — 레퍼런스를 JSON 으로 뜯어 그대로 구현

**요청**(창업자): "저렇게 핵폭탄 터진 것처럼 하지 말고 레퍼런스를 json 해서 그대로 구현해봐 / 퍼지지만 윤곽 혹은 형태가 보이고, 빛살 같은 쐐기 형태는 발광하는데 / 빛이 어떤 형태인지도 감정을 드러낼 수 있겠다 — 쐐기로 일렁이면 예민함, 뭉게뭉게면 기분 좋음, 힘든 날이면 지지직거리는 전구처럼 불이 파르르"

**판단 근거**
- v143 은 몸통 감쇠 폭이 0.72(R+0.26 → R−0.46)라 실루엣이 통째로 날아갔다. "핵폭탄"은 정확한 지적이다.
- 레퍼런스의 경계는 **선이 아니라 밝아지는 띠**다. 그래서 `rimWidth`·`rimLift` 를 새로 두었다 — 퍼져도 형태가 남는 유일한 장치.
- "JSON 으로 뜯어라"는 형식 요구가 아니라 **검증 가능성 요구**다. 눈으로 흉내내면 매 판 다른 그림이 나오고 "그대로인가"를 물을 수 없다.

**한 것**
1. `app/src/lib/aura-spec.json` 신설 — base(반경·윤곽무름·림·발광·낟알·반짝임) / forms(ray·puff·flicker) / moods(10종 → 형태 가중치). **셰이더에 상수 없음**, 전부 유니폼으로 주입(`u_wt`·`u_rayP`·`u_puffP`·`u_flkP`·`u_baseP`·`u_bite`).
2. `FIELD_FRAG` 전면 개정 — 형태 3종 + 림 + 발광 2겹.
3. `todayMood()` 가 `w`(형태 가중치)를 실어 준다. **새 축 아님** — 기존 십성 상태값이 형태를 고를 뿐이다(설계 헌장 §판결문 형식 보존).
4. `tools/build-holo-field.mjs` 가 `sliceConst(SRC,"FIELD_FRAG")` 로 앱 셰이더를 뽑아 쓴다 — v143 에서 두 벌로 갈려 보드가 거짓말을 했다.

**기각한 선택지**
- *쐐기를 실루엣 반경에 더하기* — 두 판 연속 톱니바퀴가 됐다. 레퍼런스의 빛살은 몸이 아니라 **몸 밖으로 새는 빛**이다. 실루엣 기여는 0.03 만 남겼다.
- *지지직을 층별 밝기 감쇠로* — 몸통만 줄고 흰 발광 비중이 커져 통째로 창백해졌다. 색 비율은 두고 **불투명도만** 떨게 한다.
- *발광 반경을 그대로 두고 캔버스만 키우기* — 잘림은 없어지지만 GPU 비용이 면적 비례로 는다. 대신 **테두리 비네트**로 알파를 눕혔다.

**실측**
- 지지직: 3초(180프레임)에 33단계, 최소 0.24 / 최대 0.97, 훅 꺼짐 22프레임 — 전구가 나갈 듯 말 듯.
- 앱 배율: 색장은 몸통이 이미 캔버스의 63% 라 입자용 1.85 를 쓰면 화면 밖으로 나간다 → 판결 1.14 · 곁 1.04 로 재측정.
- 검사: CSS백틱 2/2 · 역할문구 26/26 · 곁탭 37/37 · 스모크 35/35 · 렌더러 `field` · 콘솔 오류 0.

**덤으로 잡은 것**: `e2e/layout-check.mjs` 가 **거짓 FAIL** 을 내고 있었다(8/12). 곁 탭에 안내 문구와 버튼이 붙으면서 검사가 맞대던 `.fine` 이 더는 마지막 줄이 아니게 됐는데 검사는 여전히 그 줄을 쟀다 — **화면은 멀쩡한데 검사만 빨간** 종류다. 재는 대상을 패널 전체(`.lobbypanel` ↔ `.gyeotpanel`)로 바꾸니 두 탭 차이가 1~2px 였다(허용 20px, 12/12 PASS). v144 의 변경은 전부 `.stage.holo` 범위라 이 실패와 무관했다.

**남은 것**: 홀로그램 A/B 평가 뒤 기본값 확정 또는 스킨 제거. 형태 가중치(§moods)는 **눈으로 고른 값**이라 팀 평가로 다듬을 여지가 있다 — 근거는 "상태 이름과 형태 인상이 맞는가"뿐이다.

## 2026-08-28 · v145 「구와 미색」 — 여섯 지적을 한 판에

**요청**(창업자): "폰트 좀 다시 디자인해 지금의 하얀색 바탕과 안 맞아 / 그리고 오늘은 또렷함 이게 무슨 말이야? 직관적으로 쓰라는 게 지침이잖아 / 예민함 상태일 때 수호신 본체도 움직이는 게 아니라 빛만 조정되는 거야 / 그리고 움직임은 xyz 축으로 있어야 해 / 지금 디자인이 너무 세포 같이 생겼어 내 레퍼런스 본 거야? / 바탕을 흰색이 아니라 미색의 회색으로 바꾸면 빛을 흰색으로 해도 보일 거 아냐"

**판단 근거**
- **바탕 제안이 근본 해법이었다.** 흰 바탕이라 흰 발광이 안 보였고, 그래서 v144 에서 빛살을 **색으로 칠하는** 우회를 했다(`mix(u_c1,u_c2,0.35)`). 바탕을 반 톤 내리니 우회가 필요 없어졌다 — 흰빛이 그대로 산다. 같은 수가 타이포 문제도 같이 풀었다(흰 바탕에서 뜨던 글자가 앉는다).
- **"세포 같다"는 정확하다.** 각도 노이즈로 반경을 흔든 건 v143 의 "움직임이 없다"에 대한 답이었는데, 답이 과했다. 레퍼런스는 **둥근 발광체**다. 움직여야 하는 건 실루엣이 아니다.
- **"또렷함"은 우리 어휘다.** 십성 이름을 라벨로 쓰면 우리에겐 뜻이 있고 유저에겐 없다. 근거를 적으라는 규칙과 알아듣게 쓰라는 규칙은 충돌하지 않는다 — **근거를 일상어로 적으면 된다.**

**한 것**
1. 바탕 `#d9d5ca` 미색 회색. 타이포를 보랏빛 회색 → **따뜻한 먹색** 계열로 전환(색 21쌍 치환), 수호신 말은 명조 17px, 오늘 상태는 두 줄 위계(문장 20px + 근거 10.5px).
2. 실루엣의 각도 노이즈 **전부 제거**. 몸은 구다.
3. xyz — x·y 드리프트(주기 0.23/0.41, 0.19/0.33 로 나눠떨어지지 않게) + z 앞뒤(크기·밝기·발광 반경) + **자전**(구 표면 법선을 회전시켜 노이즈를 찍는다).
4. 감정 3종을 전부 발광층으로. 몸의 반경 `R` 은 감정과 무관하게 확정된다.
5. `MOOD` 10종 라벨을 서술로, `axis`(「…자리」) 신설. 스펙 `moods` 키를 라벨 → 십성.

**기각한 선택지**
- *평면 도메인 워핑으로 3D 흉내* — 회전해도 평면이라 가장자리 압축이 없어 공으로 안 읽힌다. 구 법선을 돌리는 쪽이 코드도 짧다.
- *십성 이름을 괄호로 병기* — 유저 어휘 옆에 우리 어휘를 붙이면 결국 우리 어휘를 읽힌다. 근거 줄 자체를 일상어로 바꿨다.

**실측**: 렌더러 `field` · 콘솔 오류 0 · CSS백틱 2/2 · 역할 26/26 · 보드 25칸 전부 구로 렌더.

**남은 것**: 형태 가중치(§moods)는 여전히 눈으로 고른 값이다. A/B 에서 물을 것은 "어느 상태가 어느 형태로 보여야 하나" 하나.


## 2026-08-28 · v146 「오라」 — 레퍼런스 그대로

**요청**(창업자): "지금 디자인은 세포같아서 징그러워 / 이거 그대로 구현해" (ENERGY IS 레퍼런스 재첨부)

**판단 근거 — 무엇이 세포로 보이게 하는가**
레퍼런스를 다시 대조해 원인을 셋으로 특정했다. v145 는 셋을 다 갖고 있었다.
1. **몸의 알파 경계**(`smoothstep(R+soft, R-soft*0.7, d)`) → 세포막
2. **밝아지는 림 띠**(v144 에서 "퍼져도 형태가 남게" 넣은 것) → 이중막
3. **자전하는 표면 결**(v145 에서 xyz·자전 지시에 답한 것) → 세포질
셋 다 각각은 다른 지적에 대한 정당한 답이었는데, **합쳐지니 세포가 됐다.** 레퍼런스에는 셋 다 없다 —
경계선이 없고, 띠가 없고, 무늬가 없다. 안쪽이 진하고 밖으로 갈수록 색이 바뀌며 옅어질 뿐이다.

**한 것**
- 셋 전부 제거. 남은 건 반경 `rr` 에 따른 **색 층 넷**(코어 · 몸 · 대비색 헤일로 링 · 옅은 흰빛) + 반짝임.
- 형태: 정원 → 오행별 타원 비율(화 1.10×0.86 세로 / 수 0.84×1.22 가로 …), 굴곡은 2·3차 저주파 둘만.
  응축(곁)하면 비율이 1:1 로 수렴한다 — 판결→곁 전이의 뜻이 그대로 남는다.
- 헤일로 채도: 생 오행의 진한 색 × 밝은 색을 0.42 로 섞는다(`mixHex`). 밝은 색만 쓰면 흐리다.
- 움직임은 x·y 드리프트 + z 앞뒤만. **자전은 버렸다** — 3D 를 표면 무늬로 표현한 게 원인이었다.

**기각한 선택지**
- *림만 빼기* — 셋 중 하나만 빼면 나머지 둘이 그대로 막을 만든다. 실제로 v145 에서 림을 약하게 해도 세포로 보였다.
- *흰빛 헤일로* — 미색 회색 바탕에서 잘 보이지만 **색을 전부 안개로 만든다.** 흰빛은 맨 바깥 0.14 만.

**실측**: 렌더러 `field` · 콘솔 오류 0 · 보드 25칸 전부 코어+링 구조로 렌더.

**남은 것**: 형태 가중치(§moods)는 여전히 눈으로 고른 값. A/B 질문은 "어느 상태가 어느 형태로 보여야 하나".


## 2026-08-28 · v148 「다섯 층」 — 세포로 보이는 원인 3차 분석

**요청**(창업자): "레퍼런스처럼 안되고 자꾸 세포처럼 느껴지는데 왜인지 분석하고 개선해"

**분석 — v146 이 아직 세포였던 이유 넷**
| 원인 | 세포의 특징 | 레퍼런스 | 고친 방법 |
|---|---|---|---|
| 완전한 동심원 | 막·세포질·핵이 동심 | 층마다 중심이 어긋남 | 다섯 층의 중심을 각자 다른 주기로 떠다니게 |
| 색 여정이 짧다 | 안이 진한 동그라미 | 색상이 서너 번 돎 | 층을 다섯으로(반사광·코어·중간 띠·대비색 헤일로·바깥) |
| 가중평균 합성 | 겹칠수록 회색 | 링이 섞이지 않고 얹힘 | `ov()` over 합성 |
| 크기 비율 | 화면을 채우는 덩어리 | 몸통 40% · 전체 75% · 바깥에 여백 | 반경을 재서 맞춤 |

**실측으로 잡은 버그**
- 링을 `smoothstep(hi,lo,r) * smoothstep(a,b,r)` 로 만들었더니 **링 한복판에서 알파가 0.147**.
  손으로 계산해 확인했다. 대비색 헤일로가 "약하게만" 보이던 진짜 이유였다 → `ring(r,c,w)=exp(-((r-c)/w)^2)`.
- 헤일로 알파 0.22 는 바탕색이 그대로 비쳐 **색 없는 안개**가 된다. 배경 L213 위에서 색이 서려면 0.4 이상 필요.

**기각한 선택지**
- *밝은 심으로 뒤집기* — 눈알이 됐다. 레퍼런스 WARMTH 의 중심은 오히려 진한 자홍이다.
- *사각형 비네트* — 헤일로가 캔버스를 채우면 사각 자국이 그대로 보인다. 원형으로.

**남은 것**: 형태 3종(쐐기·뭉게·지지직)의 차이가 v146 보다 약해졌다. 세포를 벗어나는 게 우선이라 감정 표현을 낮춘 결과다 — A/B 에서 "형태가 구분되는가"를 같이 물을 것.


## 2026-08-28 · v149 「불꽃」 — 세포가 아니라 불꽃

**요청**(창업자): "작업 다 완료해봐 / 세포보다는 불꽃의 일렁임 느낌이여"

**판단 근거**
v143~v148 은 "세포 같다"를 **재료**의 문제로 봤다(막·림·표면결·동심원·합성·비율). 여섯을 다 고쳤는데도
남는 게 있었던 건 **방향**이 없어서다. 세포는 사방이 같고, 불꽃은 아래가 뭉치고 위로 흐르며 끝이 흔들린다.
둥근 것을 아무리 흔들어도 세포이고, 아래를 뭉치고 위로 뻗게 하는 순간 불꽃이 된다.

**한 것**
1. **위로 흐르는 왜곡** — `fbm(vec2(w.x*1.9, w.y*0.95 - t*0.85))`. y 에서 시간을 빼면 무늬가 위로 올라간다.
   흔들림 계수를 `up`(위쪽일수록 1)에 물려 **불꽃 끝이 더 흔들리게** 했다.
2. **혀** — `w.y /= (1.0 + lift*0.95)` 로 위쪽 반경을 늘린다. `lift` 에 노이즈를 섞어 폭이 갈라진다.
3. **밑동** — `w.y *= 1.0 + 0.22*(1.0-up)` 로 아래를 뭉친다.
4. **끝이 옅다** — 코어 알파에 `(1.0 - 0.42*up)`.
5. 오행 비율을 세로 우세로. **응축(곁)하면 `sway` 가 0.42 로 떨어져** 일렁임이 잦아들고 둥글어진다.
6. 형태 3종을 v147 에서 낮춰뒀던 만큼 복원(쐐기 0.46 · 뭉게 1.35 · 지지직 depth 0.36).

**기각한 선택지**
- *왜곡만 강하게* — 둥근 얼룩이 심하게 흔들릴 뿐 불꽃이 안 된다. 반경 자체가 위로 늘어나야 한다.
- *뭉게 변조 2.2* — 헤일로가 세잎클로버로 갈렸다. 1.35.

**덤으로 고친 것**: 보드 설명문에 「경계는 선이 아니라 밝아지는 띠(림)다」가 남아 있었다.
**림은 v146 에 제거했으므로 그 문장은 거짓말이었다.** 문서가 코드보다 늦게 따라오는 전형이라 같이 고쳤다.

**실측**: 렌더러 `field` · 콘솔 오류 0 · 보드 25칸 전부 불꽃 형태로 렌더(화 행에서 밑동+혀가 뚜렷).

**남은 것**: 형태 가중치(§moods)는 여전히 눈으로 고른 값. A/B 질문 둘 — ①불꽃으로 읽히는가 ②쐐기·뭉게·지지직이 구분되는가.
---

## 2026-08-28 · v148 — 초대와 회신의 마지막 다리(L-3): 부르면 자리가 서고, 답이 오면 사람이 된다

**요청**: 「해봐」 — 앞 판에서 남겨 둔 L-3(B 랜딩 + 곁 승격)을 끝내라.
지시서 `app/작업지시_초대와회신_2026-08-26.md` §4·§5 · 시안 `app/public/invite-mock.html`(D-1).

### 막혀 있던 것 — 엔진이 양쪽 생년월일을 요구했다

L-3 이 앞 판에 안 끝난 진짜 이유가 이것이다. `readMatch` 는 아홉 축 중 여섯을 **율리우스일**에서
계산했고, 그래서 **두 사람 모두의 `birth.y/m/d`** 를 요구했다. 그런데 서버에는 A의 생년월일이 없다
(있으면 처리방침 §5-2 가 거짓이 된다). 받은 사람 화면에서 **넷밖에 못 세우는** 상태였다.

**한 것 — `matchAxes()` 신설(`src/lib/match.js`)**. 아홉 축이 한 사람에게서 실제로 쓰는 값만 추린
좌표 열넷(일간·일지·납음·해자리·달자리·**나크샤트라·라시**·웨톤 셋·촐킨 둘·라이프패스).
`readMatch` 는 이제 사람이든 좌표든 받는다(`asAxes`). 생년월일은 좌표를 만드는 자리에서 끝난다.

- `sky.js` 에 `sidIdx()`·`ashtakutaIdx()` 를 갈라 냈다 — 아쉬타쿠타는 두 사람에게서 **정수 둘**씩만
  쓰는데 그게 함수 안에 묻혀 있었다. 기존 `ashtakuta(jd,y,jd,y)` 는 그대로 남아 새 함수를 부른다.
- **행동 보존을 실측했다**: 가상 명식 3,000쌍에서 `readMatch(사람) === readMatch(좌표)` **3,000/3,000
  문자 단위 동일**. 이 확인 없이 넘어갔으면 "같은 두 사람인데 화면마다 다른 말"이 됐다.
- ⚠ 좌표는 **되짚을 수 있다**. 숨기지 않고 처리방침 §5-2 「정직한 고지 1」이 그대로 말한다.
  대신 방침의 파생값 목록과 코드가 싣는 칸을 **검사로 맞대 놨다**(아래).

### B 랜딩 — 장면 2 · 탭 4

시안 그대로. 현행 공유 유입은 장면 10·탭 10 에 완주율 40%(합격선 50)이고, 여기는 **장면 2·탭 4**다.
`InviteLanding` 하나에 입력→결과가 다 있고 「네 수호신도 만들어볼래?」는 **결과 안에** 있다
(세 번째 화면을 안 만든다 — 여세를 끊지 않는 게 이 문의 전부다).

- ⑤는 `readMatch({a: B, b: A})` — **B가 a 자리**다. 일간 십성은 방향이 있어 누구 쪽에서 읽느냐로 갈린다.
- ③ 고지는 회색 각주가 아니라 **금색 상자**로 입력 바로 아래. 나가는 것과 **안 나가는 것**을 같이 적고
  안 나가는 쪽을 더 길게 적었다.
- ④ 동의는 켠 채로 시작하고 **결과의 대가로 안 건다.** 꺼도 결과는 전부 보인다 —
  대가로 걸면 그건 동의가 아니라 **통행료**이고, 그렇게 받은 동의는 근거가 못 된다.
- ⑤ 이름 뒤 조사를 계산한다. 안 하면 "연지이 궁금해했어"가 나간다(시안 첫 판에서 실제로 나왔다).
- 만 14세 게이트를 여기에도 뒀다 — **이 사람에게는 이 문이 유일한 문**이다.

**시안과 하나 다르다** — B에게 이름을 **선택으로** 묻는다. 시안의 근거(①줄의 이름은 A의 것)는 그대로
지켰고, 받는 이유가 다르다: 창업자 지시("초대 받은 사람은 이미 이름과 생년월일을 적었으니 온보딩이
반은 끝난 거야"). 받은 이름은 ⑥ 문으로 들어갈 때 **그 사람의 온보딩에 실린다.**
⚠ **그 이름은 A에게 안 간다.** 서버 `label` 칸을 비운 채 보낸다 — A는 이미 자기가 적은 이름을 갖고 있어
얻는 게 없고, **얻는 게 없는 제3자 제공은 하지 않는다.**

### 엿보기 엔드포인트 하나를 더 만들었다 (`GET /api/invite/:id`)

지시서에 없던 것이다. 없으면 **첫 줄을 못 그린다** — 「○○이 너와의 사이를 궁금해했어」의 이름은
POST /answer 만 돌려주는데, 그건 답한 뒤에 오는 값이다. 그리고 `invite_opened` 를 유효 검증 뒤에
찍으라는 §6 규칙도 읽을 길이 없었다.

- 돌려주는 건 **표시용 이름 하나뿐.** 좌표는 안 준다 — 안 그러면 링크를 쥔 누구나 A의 좌표를 긁어 간다.
- **소비하지 않는다.** 여기서 소비하면 화면을 여는 것만으로 초대가 닫힌다.
- **이미 답이 온 초대는 엿보기도 410.** 200 을 주면 두 번째 사람이 생일을 다 넣고 제출에서야 거절당한다 —
  값을 받아 놓고 거절하는 건 제일 나쁜 순서다.

### 곁 승격 — 「부른 곁」이 사람이 된다

지시서 §5 는 *"답이 온 초대의 **상대**를 올린다"* 라고 쓰고, §10 은 *"B의 값을 A에게 보내기 — 안 한다"*
라고 쓴다. 둘을 같이 읽으면 결론이 하나다: **자리는 부를 때 만들어져 있어야 한다.** 답이 온 뒤에는
자리를 만들 재료(오행·일간)가 **영영 오지 않기 때문이다.**

- 「한 사람 더 부를래」가 **이름을 먼저 묻고**, 초대를 만들면서 명부에 `GY_CALLED` 자리를 세운다
  (`key: inv:<id>`). 자리에 실리는 건 A가 적은 이름 하나뿐이고 하늘은 비어 있다 —
  그게 「부른 곁」의 뜻 그대로다: 기척은 있는데 아직 사람이 아니다.
- 앱을 열면 `GET ?ids=` 로 한 번 확인하고, 답이 온 자리를 **0.5초씩 어긋나게** 올린다.
  동시에 밝아지면 그게 곧 "2명"이라는 **숫자**가 된다 — 어긋나면 사건이 둘이다.
- 푸시·알림·배지 0. `at` 을 안 건드린다 — 갱신하면 답이 온 사람이 목록 맨 앞으로 튀어 **명부가 순위표**가 된다.

**승격을 밝기 하나로 말하지 않는다**(§5 D-2). 가산 블렌딩에서 0.45→1.0 은 생각보다 안 띈다.
축 셋을 같이 움직인다 — **밝기**(0.45→1.0) · **자리**(궤도 1.18배 바깥→앞줄) · **꼬리**(길고 흐림→짧고 또렷).
셋이 같이 가야 "밝아졌다"가 아니라 **"가까이 왔다"**로 읽힌다.

- 셰이더에 `u_gts[8]` 을 더하고 **프레임마다 따라가게** 했다(2.2초 ease-out). tier 를 갈아끼우면
  한 프레임에 튀어서 그건 전환이 아니라 교체다(v133 교훈: 끊기면 교체, 이어지면 자세).
- 보간값은 **사람(key)마다** 들고 간다. 슬롯 번호로 들면 곁이 하나 늘어 자리가 밀리는 순간
  **남의 전이를 이어받아 엉뚱한 위성이 밝아진다.**
- `gyeotView` 에서 색에 0.45 를 곱하던 것을 걷어냈다(거기서 곱하면 전이가 없다). 대신 `tier` 만 넘긴다.

### 하늘을 모르는 곁 — 지어내지 않는다

부른 곁에는 오행이 없다. 예전 폴백은 `EL_COLOR.토` 였는데 그건 **"이 사람은 흙이다"** 라고 말하는 것과
같다. 궤도와 점 둘 다 **무채색**으로 두고, 행의 말은 「부르는 중 — 아직 답이 없어」다.

**검사가 하나 더 잡았다**: 써머리의 「n명은 자리를 아직 못 읽었어 — **궁합을 다시 보면 읽혀**」가
부른 곁에도 붙어서 **항상** 떴고, 그 안내가 **틀린 길을 가리켰다**(그 사람은 궁합을 다시 볼 대상이 아니고,
답이 와도 우리는 그 사람의 일간을 안 받는다). 하늘이 있는 자리만 세게 고쳤다.

### 검사

- **`e2e/invite-landing-check.mjs` 신설 39항목**(브라우저). preview 는 정적 서버라 `/api/*` 가 없어서
  **진짜 핸들러를 검사 프로세스에서 돌려** `page.route` 로 물렸다 — 서버를 흉내 내면 흉내가 통과할 뿐이다.
  B 쪽 다섯 절 + **A 쪽 두 절**(부르기→자리→답→승격)을 한 파일에서 끝까지 건넌다.
- `invite-check` +6(엿보기 경계 · 소비 안 함 · 좌표 안 줌 · 410 · 출처). **음성 확인 2건 통과.**
- `gyeot-roster-check` 셋을 새 자리에서 다시 잡게 고쳤다 — **뜻은 하나도 안 무르게 했다.**
  흐리기가 셰이더로 옮겨 갔으므로 소스에서 `mix(0.45,1.0,gt)`·궤도·꼬리 셋을 다 문다.
  **음성 확인 3건 통과**(각 축을 하나씩 깨뜨리면 각각 FAIL).
- `privacy-check` +3 — **방침의 파생값 목록 ↔ 코드가 싣는 칸**을 이름 대 칸으로 맞댄다.
  개수로 맞대면 「웨톤」 하나가 코드에선 셋이라 늘 어긋나서, 표 하나를 다리로 뒀다.
  **음성 확인 2건 통과**(좌표를 몰래 늘리면 FAIL / 방침에서 축을 지우면 FAIL).
- **`e2e/mansae-test.mjs`·`tools/build-invite-mock.mjs` 가 이미 깨져 있었다** — v146 이 `aura-spec.json`
  을 임포트하면서 esbuild 추출이 죽었다(내가 낸 고장이 아니라 지나가다 밟았다). `--bundle --packages=external`
  로 고쳤다.

### 실측

검진 심각 0 · 초대 랜딩 39/39 · 초대 39/39 · 개인정보 127/127 · 곁 명부 46/46 · 곁 탭 37/37 ·
곁 예산 13/13 · 궁합 55/55 · 각인 130/130 · 리포트 122/122 · 판결 35/35 · smoke 35/35 · doc 21/21 ·
만세력 28/28 · 좌표 동일성 3,000/3,000.

### 남은 것

- **KV 프로비저닝은 창업자 몫**(지시서 §8). 붙기 전까지 초대는 **운영에서 안 돈다** —
  메모리 폴백은 인스턴스당·휘발이라 개발·검사용이다. 못 만들면 화면이 **못 만들었다고 말한다.**
- 초대에 답한 사람이 **이미 비나리 유저**면 그 사람의 곁에는 A가 안 선다(A의 좌표는 손에 있으니 기술적으로는
  가능하다). 곁의 뜻이 한쪽에서만 자라는 상태 — 붙일지는 분모 100 이후 판단.

---

## 2026-08-28 · 초대가 라이브에서만 죽어 있었다 — 검사 전부 통과하고 배포에서 404·403

**제보**: 창업자 스크린샷 — 「초대를 못 만들었어 — 지금은 초대를 만들 수 없어」.
Vercel 쪽은 정상이었다(저장소 연결·환경변수 `KV_REST_API_URL`/`KV_REST_API_TOKEN`·재배포 Ready).

**결론부터**: **Vercel 설정 문제가 아니라 내 코드 문제였고, 사고가 둘이었다.**
둘 다 로컬 검사 47항목·브라우저 검사 39항목을 **전부 통과한 채** 배포에서만 죽었다.

### 어떻게 잡았나 — 코드를 읽어서가 아니라 배포된 URL 을 찔러서

프록시가 vercel.app 을 막아 curl 이 안 돼서 Vercel MCP 의 `web_fetch_vercel_url` 로 두 번 찔렀다.

```
GET /api/invite/zzzzzzzz  → 403 {"error":{"message":"허용되지 않은 출처"}}   ← 함수가 답했다
GET /api/invite           → 404 NOT_FOUND (text/plain)                      ← 함수까지 안 갔다
```

이 두 줄이 전부였다. 하나는 함수가 살아 있다는 증거고, 하나는 **맨 경로가 안 잡힌다**는 증거다.
`lambdaRuntimeStats: {"nodejs":3}` 로 함수 개수(judge·share·invite)도 맞는 걸 먼저 확인했다.

### 사고 ① — 「선택적」 캐치올이 선택적이 아니었다

파일이 `api/invite/[[...seg]].js` 다. 대괄호 두 겹은 Next 에서 **조각 0개도 잡는다**는 뜻이라
`/api/invite` 로 만들기를 불렀다. 그런데 이 프로젝트는 Next 가 아니라 **zero-config** 라
조각이 최소 하나 있어야 함수까지 간다. 그래서 만들기만 404 였다.

**그리고 404 는 HTML(text/plain)이라 앱이 JSON 을 못 읽는다.** `d = null` 이 되어
서버 사유 대신 폴백 문구 「지금은 초대를 만들 수 없어」가 떴다 —
**유저 눈에는 "그냥 안 됨"이고, 우리 눈에도 원인이 안 보인다.** 화면이 조용히 거짓말을 한 셈이다.

**고침**: 경로 다섯을 전부 조각 있는 모양으로.
`POST /api/invite/new` · `GET /:id` · `POST /answer` · `GET /check?ids=` · `DELETE /:id`.
예약어 셋(new·answer·check)은 id 길이(base64url 12자)와 안 겹친다 — 검사가 그걸 못 박는다.

### 사고 ② — 브라우저는 같은 출처 GET 에 `Origin` 을 안 붙인다

이건 아직 화면에 안 나타났지만 **다음 단계에서 반드시 터질 것이었다.**
엿보기(`GET /:id`)와 승격 확인(`GET /check`)이 실기에서 전부 403 이 된다.
fetch 규격상 `Origin` 은 GET·HEAD 에 안 붙고, `isAllowedOrigin("")` 은 false 다.
이 리포의 다른 API(`judge`·`share`)가 전부 POST 라 **여태 한 번도 만난 적이 없는 사고**다.

**고침**: 값을 바꾸는 요청(POST·DELETE)만 Origin 을 요구한다.
GET 둘은 상태를 안 바꾸고(엿보기는 소비도 안 한다) CSRF 가 걸리는 자리가 아니다.
**남의 출처가 적혀 온 GET 은 여전히 막는다** — 없는 것과 틀린 것은 다르게 다룬다.

### 왜 검사가 못 잡았나 — 내가 검사에 거짓말을 시켰다

브라우저 검사에서 `page.route` 로 API 를 물리면서 **핸들러에 Origin 을 직접 넣어 줬다.**
같은 파일 머리에 *"서버를 흉내 내면 흉내가 통과할 뿐"* 이라고 써 놓고 내가 그렇게 했다.

**고침**: 중계기가 `route.request().headers()` 를 **그대로** 넘긴다.
그러자 브라우저가 그 GET 에 Origin 을 안 붙인다는 게 검사에 실측으로 찍혔고(`"(없음)"`),
서버를 옛날로 되돌리면 **초대 화면 자체가 안 뜬다**(음성 확인). 이제 이 검사가 사고 ②를 잡는다.

사고 ①은 **로컬에서 원리적으로 못 잡는다** — 검사는 핸들러를 직접 부르고 preview 는
정적 서버라 라우팅이 없다. 그래서 **부르는 모양 자체**를 못 박았다(맨 경로 금지).

### 검사

- `invite-check` 33→**47**. ⑧절 신설 — `origin: null` 을 넘기면 **헤더 자체를 안 붙인다**
  (빈 문자열이 아니라 부재를 재현해야 그 경로가 검사된다).
  **음성 확인 3방향**: 맨 경로로 되돌리면 FAIL / GET 에 Origin 요구하면 FAIL /
  GET 을 아무나 열게 풀면 FAIL 7건.
- `invite-landing-check` 39→**40**. 실제 헤더 통과 + 「브라우저는 이 GET 에 Origin 을 안 붙인다」.
- **검진 5-q 신설** — 배포 직전 관문에 넣었다. 앱이 부르는 초대 경로가 전부 조각을 갖는지,
  서버가 GET 을 Origin 없이 통과시키는지. 음성 확인 통과.

### 남은 것

- **실기 완주 검사는 여전히 사람 몫이다** — 기기 둘로 부르기→답하기→승격을 밟는 것.
  이번 사고가 딱 그 구간(라우팅·헤더)에서 났고, 그건 우리 검사가 못 보는 층이다.
- 다음에 새 API 경로를 만들면 **배포 후 그 URL 을 한 번 찔러 보는 것**을 절차로 넣을 것.
  이번엔 그게 없어서 창업자가 대신 밟았다.

## 2026-08-28 · v151 「손끝과 탄생」 — 실기 제보 여섯 건

**요청**(창업자, 아이폰 실기 스크린샷 4장): "캔버스가 좁아서 상단과 좌측이 잘린거 같아 / 홀로그램에 맞춰서 심볼이 있어야지 처음부터 / 가짜정보가 입력된 칸에 숫자가 너무 찐한색으로 되어있다보니 그냥 이미 데이터가 입려된거 같아. 오인지되니까 회색으로 바꿔 / 점 버전에 비해서 탄생할때의 모션이 너무없다 / 인터랙션 요소도 넣어줘 점 버전 처럼 / 곁은 둥그런 디자인이어야지"

**진단 — 여섯 중 넷은 코드가 아니라 화면에서만 틀렸다**
| 제보 | 실제 원인 |
|---|---|
| 상단·좌측 잘림 | `guardianSize` 가 `vp.w * 1.1`. 입자는 가운데 몰려 티가 안 났고 **색장은 캔버스 전체를 칠한다** |
| 심볼 부재 | 온보딩만 `DustOrb`(입자). 홀로 분기가 없었다 |
| 숫자 오인 | 그 숫자는 **placeholder** 다. 어두운 판용 색이 밝은 판에 그대로 적용됐다 |
| 곁이 안 둥글다 | `agp = mix(agp, vec2(1.0), u_orb*0.7)` — 0.7 만 섞어 불꽃 모양이 남았다 |
| 탄생 모션 없음 | 색장에 `born` 개념 자체가 없었다(입자 엔진에는 있었다) |
| 인터랙션 없음 | 색장이 `reactRef`·터치를 안 받고 있었다 |

**한 것**
1. `guardianSize` 홀로 분기(`vp.w*0.96 · vp.h*0.46 · 460`), 비네트를 사각→원형.
2. 온보딩에 `<Guardian scatter={...}>`. `scatter` 는 진행도(0.34→0.73)라 칸을 채울수록 조각이 모인다.
   중립 팔레트 `HOLO_MOON` 신설 — 기본값 토는 미색 바탕과 겹쳐 안 보인다.
3. `.stage.holo` placeholder·`.line`·`.sub2`·`.ainote`·`.unit` 색 신설.
4. `u_born` — 층마다 흩어짐 배율을 달리해 **모이는 속도가 다르게**. 2.4초 ease-out.
5. `u_touch`/`u_touchAmt` — 장이 손끝 쪽으로 당겨지고 밝아진다. `reactRef` 도 같은 채널.
6. `agp` 를 `u_orb` 로 완전히 섞고, `sway` 도 `mix(1.0, 0.0, u_orb)`.

**실측**
- 탄생: 중앙 불투명도 0.35s **38** → 1.25s **131** → 2.85s **156**.
- 터치: 누르기 전 **156** → 누르는 중 **249**.
- 검사: CSS백틱 2/2 · 역할 26/26 · 스모크 35/35 · 곁탭 37/37 · 레이아웃 12/12 · 재방문 26/26 · WebGL 7/7.

**시행착오 셋**
- 고친 `DustOrb` 가 **다른 화면 것**이었다(같은 코드가 두 곳). 눈으로 안 되니 **DOM 에서 캔버스 크기를 재서** 어느 쪽이 그려지는지 확인했다.
- `.orb` 가 170px 고정이라 240px 색장이 넘쳐 오른쪽 위로 밀려 글자와 겹쳤다.
- **CSS 주석에 백틱을 또 넣어 앱이 죽었다.** `css-backtick-check` 가 그대로 잡았다 — 그러라고 만든 검사다.


## 2026-08-28 · v151.1 — 보드-앱 동기화 검사 신설

**요청**(창업자): "메인에 반영됐어?"

**확인 결과**
- 앱: **반영됨.** `4d9f694` production READY(Vercel), 라이브 번들에 `v151 · 손끝과 탄생`·`u_born`·`u_touchAmt` 모두 존재.
- 보드: **안 됨.** `public/holo-field.html` 이 v149 시점(`027f180`)에 멈춰 있었다.

**원인 — 재발이다**
| 판 | 증상 | 원인 | 조치 |
|---|---|---|---|
| v143 | 보드가 앱과 다른 그림 | 셰이더를 **베껴** 갖고 있었다 | v146 에서 `sliceConst` 로 추출 |
| v150 | 보드가 앱과 다른 그림 | 추출하게 고쳐 놓고 **생성기를 안 돌렸다** | 이번 판 |

원인이 옮겨갔을 뿐 증상은 같다. **사람의 주의로 막는 종류가 아니다** — 검사로 못 박았다.

**한 것**
1. 생성기 재실행 + 새 유니폼(`u_born`=1·`u_touchAmt`=0·`u_touch`=0) 배선.
   ⚠ 이걸 안 했으면 생성기를 돌려도 `bornK`=0.20 이라 **오라가 안 태어난 채** 그려졌다.
2. `e2e/board-sync-check.mjs` 신설(5문항):
   ① 보드에 박힌 셰이더 == `App.jsx` 의 `FIELD_FRAG`
   ② 셰이더가 선언한 uniform 을 보드가 **하나도 빠짐없이** 세팅 (새 축이 생기면 여기서 걸린다)
   ③ 보드가 들고 있는 스펙 == `src/lib/aura-spec.json`
3. **음성 확인** — v149 판 보드를 넣으니 ①②가 FAIL. 되돌리니 5/5 PASS.

**남긴 판단**: `health-check.mjs` 에는 등록하지 않았다. `css-backtick-check` 도 등록돼 있지 않고, 이 리포는 e2e 검사를 문서로 관리한다 — 한 검사만 예외로 넣으면 목록이 두 갈래가 된다.

### 덧 — 배포 뒤 다시 찔러 보니 **세 번째 층**이 있었다

경로 다섯을 조각 있는 모양으로 고치고 올린 뒤 라이브를 다시 찔렀다. 이번엔 함수까지 갔는데
**전부 405 「지원하지 않는 요청이야」** — 어느 분기도 안 잡혔다는 뜻이다.

```
GET /api/invite/check?ids=zzz  → 405   ← 함수는 왔는데 seg 가 [] 이다
```

**`req.query.seg` 가 비어 온다.** 대괄호 파일명은 **라우팅**에만 쓰이고, 조각을 `req.query` 에
실어 주는 건 Next 쪽 동작이었다. 이 프로젝트는 zero-config 라 라우팅만 해 주고 조각은 안 준다.

→ `segsOf(req)` 신설. **`req.url` 에서 직접 읽고** `query.seg` 는 실어 주는 환경을 위한 폴백으로만 둔다.

**셋 다 원인이 하나다 — 검사가 Vercel 이 주는 모양이 아니라 내가 준 모양으로 `req` 를 만들었다.**
맨 경로(404)·Origin(403)·조각 부재(405) 셋이 전부 그 한 가지 착각에서 나왔다.
그래서 검사 대역을 아예 바꿨다: 이제 `url` 과 일반 쿼리만 주고 **`query.seg` 를 일부러 안 준다.**
되돌려 보면 **첫 검사부터** 라이브와 똑같은 405 로 죽는다(음성 확인).
브라우저 검사도 중계기를 하나로 합쳐 브라우저가 실제로 부른 URL·헤더를 그대로 넘긴다 —
세 군데에 복사해 뒀다가 한 곳만 고쳐서 깨뜨린 것도 이번에 잡았다.

**그리고 이건 절차의 실패다.** 새 API 를 처음 붙이면서 **배포된 URL 을 한 번도 안 찔러 봤다.**
검사 200여 개가 통과해도 그 한 번을 대신하지 못한다 — 세 층이 전부 그 한 번에서 나왔다.

## 2026-08-28 · v153 — 온보딩 전 과정에 홀로 스킨 (재확인 지적)

**요청**(창업자): "내가 말한 6가지 중에 반영 안된 것들이 있는데 다시 확인해봐"

**맞다. 셋이 덜 됐다.**
v150 에서 "여섯 건 다 반영"이라고 보고했는데 **판결·곁·이름 세 화면만 눈으로 봤다.**
온보딩은 아홉 화면이다. 화면 수만큼 보지 않고 결론을 냈다.

| 항목 | v150 상태 | 이번 판 |
|---|---|---|
| 잘림 | ✅ (재확인: 캔버스가 뷰포트 안 + 네 변 알파 0) | — |
| 심볼 | ⚠ 앞 두 화면만. **회상 화면(step 2)은 먼지 오브** | 홀로 분기 신설 |
| 예시 숫자 | ✅ (재확인: L74 → L176) | — |
| 탄생 모션 | ✅ (재확인: 38 → 131 → 156) | — |
| 인터랙션 | ✅ (재확인: 156 → 249) | — |
| 곁이 원 | ⚠ **세로/가로 0.93** | 0.98 |

**곁이 원이 아니었던 이유**: 비율(`agp`)·일렁임(`sway`)·혀(`lift`)는 다 `u_orb` 에 물렸는데
**물방울 변형 `w.y *= 1.0 + 0.20*smoothstep(...)` 하나만 안 물려 있었다.** 곁에서도 위가 좁아졌다.

**타이포 8종이 통째로 빠져 있었다**: `.rvbig`·`.rvlunar`·`.mention`·`.formsteps`·`.dimq`·`.msrh`·
`.confirmline`·`.bar b`. 눈으로 찾지 않고 **자동 스캔**으로 찾았다 —
미색 바탕(#d9d5ca, L213) 위에서 글자 밝기 L>170 이면 안 읽힌다는 기준으로 DOM 을 훑었다.

**막는 장치**: `e2e/holo-check.mjs`(20문항). 아홉 화면을 다 지나가며 ①심볼이 `canvas.gcv` 인가
②L>170 글자가 없는가 ③예시가 값보다 옅은가. **음성 확인** — 이번에 고친 셋을 되돌리니 15/20 FAIL.

**남긴 판단**: 이 검사는 실기 제보를 대신하지 못한다. 색 대비와 컴포넌트 종류만 보고,
"예쁜가"는 못 본다. 다만 **"화면 하나를 통째로 안 봤다"는 종류는 이제 사람이 아니라 검사가 잡는다.**

---

## 2026-08-28 · v155 — 초대의 대가를 돌려준다 (「내가 초대했는데 내꺼에도 자동 반영이 되어야지」)

**제보**: 창업자, B 랜딩 스크린샷에 빨간 줄 — *"강석우에게는 '답했다'는 사실만 전해져 —
생일도, 이름도, 결과도 안 가."* 그리고 한 줄: **"내가 초대했는데 내꺼에도 자동 반영이 되어야지."**

**맞다. 그리고 이건 문구 문제가 아니라 기능이 반쪽이었던 것이다.**

### 무엇이 비어 있었나

창업자 게이트는 「첫 입력은 공짜, 그다음부터는 그 사람이 직접 넣어야 한다」다.
그런데 답이 와도 A에게 오는 건 **「답이 왔다」 한 비트뿐**이었다. 오행도 일간도 안 오니
곁이 **색도 역할도 없이 밝아지기만** 하고, A는 그 사람과의 사이를 **영영 못 본다** —
A는 그 사람 생년월일을 모르니 궁합 폼에 넣을 수도 없다.
**게이트를 세워 놓고 통과의 대가가 없었다.** 막다른 길이다.

원인은 내가 따른 지시서 §10 —「B의 값을 A에게 보내기 — 안 한다. 「답이 왔어」 한 비트만」.
사생활만 보면 맞는 선인데, **그 선을 지키면 이 기능이 성립을 안 한다.**
창업자 결정으로 뒤집는다. **금지가 사라진 게 아니라 조건이 붙었다 — 동의.**

### 한 것

- **B가 동의하면** 자기 좌표(`matchAxes` 열넷)와 이름을 함께 보낸다. A의 확인(GET)이 그걸 받는다.
  **동의를 끄면 담지도 않는다** — 담아 두고 "안 보여 준다"로 가르면 언젠가 한 줄 고쳐서 샌다.
- A쪽: 답이 오면 자리가 **채워진다**(`gyeotFill`) — 오행·일간·좌표. 궤도가 제 색을 얻고,
  목록에 역할 이름이 서고, 써머리·판결 탭에도 그 사람이 잡힌다.
  ⚠ **이름은 안 덮는다.** A가 적어 둔 이름이 그 사람을 알아보는 이름이고, B가 적은 이름은
  A가 비워 뒀을 때만 채운다.
- 행에 **「둘 사이」 문** 신설 — 좌표가 찬 곁만. 누르면 궁합 문서가 **생년월일을 안 묻고** 바로 열린다.
  `MatchDoc` 에 `pre` 를 받아 입력 화면을 건너뛴다. **계산은 그대로 A 기기의 `match.js` 가 한다.**

### 동의의 뜻이 바뀌었으므로 문구도 바꿨다

**이게 이 판에서 제일 조심한 부분이다.** 체크박스가 예전엔 「답했다는 걸 알려도 될까」였는데
이제 「○○도 둘 사이를 보게 할까」다. **문구를 안 고치고 값만 보내면 그건 동의가 아니라 기만이다.**

- 고지 상자도 갈랐다 — 켜면 *"{who}에게 가는 건 둘 사이를 계산하는 값이야"* +
  **되짚기 고지**(*"그 값으로 네 생일을 되짚을 수는 있어 — {who}에게만"*),
  끄면 *"지금은 아무것도 안 나가"* + *"대신 {who}는 둘 사이를 못 봐"*.
- 처리방침 §5-2 도 같이 고쳤다: 저장 항목에 B의 파생값(동의 시), 제3자 제공을 양방향으로,
  **"생년월일 원값은 어느 방향으로도 안 간다"**, **"동의 안 하면 답했다는 사실조차 안 간다"**,
  **"되짚을 수 있는 사람은 상대 한 사람뿐"**.

### 검사 — 옛 약속을 지우는 대신 **자리를 옮겨** 다시 물게 했다

`privacy-check` 의 「받은 이의 명식은 어느 경우에도 안 간다」가 FAIL 났다. **정확한 검출이다.**
그 줄을 지우면 다음 판에 더 샌다. 그래서 새 경계를 네 줄로 다시 못 박고,
**방침·화면·코드 셋을 맞대는 검사**를 새로 넣었다:

- ① B의 값은 동의했을 때만 ② 생년월일 원값은 어느 방향으로도 ③ 동의 안 하면 한 비트도
  ④ 되짚기 가능한 건 상대 한 사람뿐
- **화면 대조 3건** — 체크 문구가 「둘 사이를 함께 본다」로 말하는가 / 되짚기를 숨기지 않는가 /
  끄면 실제로 안 싣는가. **음성 확인의 첫 번째가 딱 기만 케이스다**:
  *화면 문구만 옛날로 두고 값은 계속 보내면* → FAIL.

검사 +18(초대 49→54 · 랜딩 40→48 · 개인정보 127→133). 음성 확인 3방향.
**그리고 검진 5-p 가 내 새 문구를 잡았다** — `{who}는` 을 박아 놨다(두 번째다).

### 실측

검진 심각 0 · 초대 54/54 · 랜딩 48/48 · 개인정보 133/133 · 곁명부 46/46 · 곁탭 37/37 ·
smoke 35/35 · 판결 35/35 · 리포트 122/122 · 궁합 55/55 · 각인 130/130 · 의식 27/27 ·
결제표식 12/12 · 보드동기화 5/5 · 문서 21/21.
브라우저에서 완주 확인 — 부르기 → 답(동의) → **자리에 하늘이 차고**(el 금 · dg 7) →
역할 이름이 서고 → 「둘 사이」로 문서가 **생년월일을 안 묻고** 열린다.

### 남은 것

- **동의를 끄면 A는 아무것도 못 받는다**(답했다는 사실조차). 초대 한 장이 조용히 소모된다 —
  의도한 설계지만, 실사용에서 이게 답답하면 「거절당했다」를 알려 줄지는 창업자 판단.
- B가 이미 비나리 유저인 경우 **A는 B의 곁에 안 선다**(A의 좌표는 B 손에 있으니 기술적으로는 가능).
  곁이 한쪽에서만 자라는 상태 — 분모 100 이후 판단.

## 2026-08-28 · v155 — 둥근 곁과 손끝 모임

**요청**(창업자): "곁 탭 안둥그런데" / "인터랙션도 이전 버전 처럼 클릭한 곳에 모이는 게 아니자너"

**① 먼저 내 지표가 틀렸다**
v153 에서 "곁이 둥글다(세로/가로 0.98)"고 보고했는데, **삼각형도 바운딩박스 비는 1 에 가깝다.**
형태를 안 재는 숫자로 통과시킨 것이다. 무게중심에서 **각도별 반경 72방향**을 재고
**3주기 성분**(삼각형의 지문)만 뽑는 지표로 바꿨다. 전체 편차는 층 구조(코어/헤일로) 때문에
늘 0.4~0.7 이라 쓸모가 없다 — 주파수를 골라야 형태가 보인다.

**② 원인 셋이 전부 「각도에 3이 곱해진 항」이었다**
| 항 | 무엇 | 조치 |
|---|---|---|
| `sin(ang*3.0)` | 실루엣 각도 굴곡 | `(1-u_orb)` |
| `c0~c4` | 층 중심 어긋남 | `mix(1, 0.18, u_orb)` |
| **`lobe`** | **뭉게 변조 — `lobes`=3 이라 `sin(ang*3)`, 헤일로 반경에 곱해진다** | `(1 - u_orb*0.92)` |

앞의 둘을 껐는데도 각져 보였던 게 셋째 때문이다. **곁에서는 형태 변조만 접는다** —
감정(쐐기·뭉게·지지직)은 밝기와 색으로 계속 실린다. 실측 삼각 성분 **0.077 → 0.019**.

**③ 손끝이 「모임」이 아니었다**
누른 자리 부근만 국소로 당겼다(`exp(-td²)` 감쇠). 그건 "모인다"가 아니라 "눌린다"로 보인다.
입자 버전의 모임은 주석에 적혀 있듯 **형태 반지름을 줄이는 것**이다. 색장도 같게 했다 —
중심이 손끝으로 옮겨가고(`p -= tp*tk*0.82`) 반경이 준다(`mix(1.0, 0.58, tk)`).
실측: 무게중심 (0.524,0.548) → (0.409,0.675), **면적 32.2% → 16.1%**. 떼면 2.0초에 풀린다.

**④ 백틱 검사가 절반만 보고 있었다**
`css-backtick-check` 는 CSS 상수만 봤는데 **같은 사고가 셰이더에서 났다**(FIELD_FRAG 도
템플릿 리터럴이다). 대상을 CSS·FIELD_FRAG·FIELD_VERT 로 넓혔다.
⚠ sim 엔진 셰이더는 **일부러 문자열을 결합**하므로(`` ` + SHAPE_UNI + ` ``) 제외했다 —
넣었더니 늘 FAIL 이 났다. 규칙은 "백틱 금지"가 아니라 **"한 덩어리 템플릿 안의 백틱 금지"** 다.

**막는 장치**: `holo-check` 에 ⑩ 「곁이 둥글다 — 삼각(3주기) 성분 < 0.06」 추가(21문항).
음성 확인 — 뭉게 접기를 되돌리니 0.077 로 FAIL.

**남긴 교훈**: **틀린 지표로 통과시키는 게 아예 안 재는 것보다 나쁘다.**
"0.98 이니 둥글다"고 보고했고 실기에서는 삼각형이었다. 지표가 무엇을 재는지 먼저 의심할 것.

---

## 2026-08-28 · v157 — 이름은 받은 사람이 쓴다 (+ 약속만 하고 안 하던 취소를 실제로 한다)

**창업자**: *"누구를 부를지 이름을 쓰는 게 왜 필요해? 받은 사람이 쓰면 되지."*

맞다. **v155 에서 답이 올 때 그 사람 이름이 함께 오게 됐는데**(동의한 경우) 그 앞 단계를 안 걷어냈다.
같은 값을 두 번 받고 있었고, 부르는 데 화면이 하나 더 껴 있었다.

- 「한 사람 더 부를래」가 **한 번 눌러 바로** 초대를 만든다. 자리는 **이름 없이** 선다.
- 답이 오면 그때 받은 사람이 쓴 이름이 붙는다. ⚠ **A가 직접 적어 둔 이름이 있으면 안 덮는다** —
  그게 A가 그 사람을 알아보는 이름이다(목록의 이름 칸은 여전히 편집 가능).
- B 화면의 이름 칸 힌트도 바꿨다 — 동의가 켜져 있으면 *"{who}의 곁에 이 이름으로 서게 돼"*,
  꺼져 있으면 *"이 이름은 이 기기에만 남아"*. 고지 상자에도 **이름이 간다**는 걸 명시했다.

### 지나가다 잡은 것 — 취소가 화면에서만 되고 있었다

`✕ → 지울래` 가 **명부에서만 지우고 서버는 안 건드렸다.** 그런데 처리방침 §5-2 는
*"보낸 이는 초대를 취소할 수 있고, 취소하면 즉시 삭제됩니다"* 라고 약속하고 있다 —
**약속과 코드가 어긋난 자리**이고, 이 리포가 v127.7 에 겪은 것과 같은 종류다(방향만 반대).
이제 `DELETE /api/invite/:id` 를 부르고 내 초대 목록(`INVITE_KEY`)에서도 뺀다.
⚠ 서버 호출이 실패해도 **화면에서는 지운다** — 유저가 지웠는데 안 지워지는 게 더 나쁘고,
서버 쪽은 30일 뒤 어차피 사라진다.

### 검사

- **검진이 먼저 잡았다** — 「e2e 가 화면에 없는 버튼을 클릭」(`"부를게"`). 테스트를 돌리기도 전에
  걸렸고, 안 고쳤으면 타임아웃으로만 알려 줬을 것이다.
- ⚠ **내가 아무것도 안 재는 검사를 하나 썼다가 지웠다.** 「A가 적어 둔 이름은 안 덮인다」를 브라우저
  검사에 넣었는데, 그 화면에선 A가 이름을 안 적으므로 **그 경로가 아예 안 밟힌다** — 늘 통과하는
  빈 항목이었다. 재는 자리로 옮겼다: `gyeot-roster-check` 가 `gyeotFill()` 을 직접 부른다(④-c 7항목).
- 곁 명부 46→**53**. 초대 랜딩 48/48 · 초대 54/54 · 개인정보 133/133 유지.

### 남은 것 (창업자 판단 필요) — 역할 이름이 아직 십성 하나다

같은 판에 창업자 질문: *"「네 뒤를 봐 주는 자리」는 사주 베이스야 아니면 총합 결론이야?"*
**사주 베이스이고, 그중에서도 일간 십성 딱 하나다.** `roleOf(myDG, theirDG)` → `sipseong()` 이
전부이고 입력은 **두 사람의 일간 두 글자**뿐이다 — 일지도, 생시도, 나머지 여덟 축도 안 들어간다.

그런데 **2026-08-15 창업자 판정 4가 「십성만으로는 안 된다」였고**, 그걸 설계로 옮긴 정본이
`01_기획전략/비나리_기획전략_아홉축역할설계_v01.md` 다(§③ 「극성 층 + 희소성 순위」 · POL/SUR 상수표).
**그 설계가 코드에 하나도 안 들어와 있다** — `POL`·`SUR` 도, 축 선택 로직도 없다.
문서는 결정됐는데 구현이 안 따라간 상태다. 붙일지는 창업자 결정.

---

## 2026-08-28 · v159 — 분까지 받는다 · 선택지를 없앤다 · 카드 안에서 두 날개가 만난다

창업자 제보 여섯 건을 한 판에 처리했다. **셋은 코드가 아니라 사실이 틀린 것이었다.**

### ① 분 — 창업자 *"시간뿐 아니라 분도 받아야지. 이건 신뢰도의 문제야."*

맞다. 그리고 **얼마나 맞는지 재 봤다**(가상 명식 400쌍):

| 바꾼 것 | 궁합 결과가 달라진 비율 |
|---|---|
| 분만 0 ↔ 59 | **30/400 (7.5%)** |
| 시만 03시 ↔ 15시 | **280/400 (70%)** |

달의 자리·나크샤트라가 하루 안에서 움직여서 그렇다. **온보딩은 이미 분을 받고 있었으므로
궁합 폼만 안 받고 있었던 것** — 같은 앱이 같은 사람을 두 정밀도로 보고 있었다.

### ② 「이 결과는 시를 안 써」가 거짓이었다 — 아무도 안 짚은 자리

위 표의 두 번째 줄이 이걸 잡았다. 초대 화면에 **「태어난 시는 몰라도 돼 — 이 결과는 시를 안 써」**가
떠 있었는데, 시를 안 받으면 **정오로 넣고 계산한다.** 70%에서 답이 달라지는 값을 두고
"안 쓴다"고 말하고 있었다. 시안의 근거("이 계산은 일간뿐이라 시와 무관하다")가 사실과 달랐다.

→ 문구를 사실로 바꾸고, **시·분을 노크형으로** 열었다(온보딩의 한자 이름과 같은 방식).
탭 수는 그대로 4다 — 아는 사람만 열어 적는다.

### ③ 이름 칸 — 창업자 *"받은 사람이 쓰면 되지"* (v157 에서 처리, 이번 판에 배포)

### ④ 동의 체크 제거 — 창업자 *"이 칸 없애. 무조건 공유되도록. 선택지 없애."*

**체크가 사라졌다고 동의까지 사라지면 안 된다.** 받은 사람은 이 앱을 쓴 적 없는 제3자이고,
그 값이 부른 사람에게 가는 건 제3자 제공이다. 그래서 **고지를 키우고 행위로 동의를 받는다** —
무엇이 가는지 버튼 바로 위에 적고, 버튼을 누르는 것이 동의다.
**이 앱이 온보딩에서 이미 쓰는 방식과 같다**(「'하늘을 열기'를 누르면 …동의한 것으로 볼게」).

처리방침 §5-2 제3자 제공 절도 그렇게 고쳤다. **검사는 지우지 않고 자리를 옮겼다** —
「별도 동의를 받는다」 → 「무엇으로 동의를 받는지 밝힌다」. 음성 확인 2건:
**고지 문구만 지우고 값은 계속 보내면 FAIL**(그게 기만 케이스다).

### ⑤ 「중간에 짤렸어」 — `.stage` 가 `overflow:hidden` 이었다

초대 결과는 아홉 칸 + 문까지 있어 화면보다 길다. 무대가 잘라 내고 있었다.
이 화면만 스스로 스크롤하게 했다(`.invstage`) — 다른 화면은 안 건드린다.

### ⑥ 카드 뒤집기 + 두 날개 — 창업자 *"귀엽게, 카드 안에서 모션해"*

곁 행에 **「둘 사이 보기」** 손잡이. 누르면 카드가 뒤집히며 **촐킨 두 날개가 만난다.**

- **헌장 「신규 캐릭터 제작 금지」와의 경계를 코드에 적어 뒀다** — 이건 페르소나가 아니라
  **데이터 표식**이다. 촐킨 20날개는 이미 궁합 축 ⑦이 쓰는 값이고, 여기서는 그걸
  **글자 대신 그림으로** 보일 뿐이다. 수호신은 안 건드린다. 마스코트로 읽지 마라.
- **20종을 손으로 안 그린다** — 한 벌의 몸에 **특징 하나씩**만 갈아 끼운다(귀·꼬리·부리·뿔).
  그래야 캐릭터 세트가 안 되고, 새 날개가 생겨도 표 한 줄이면 된다.
- 움직임은 셋뿐 — 다가감 → 부딪힘 → 물러남. **몸이 한 번 눌렸다 뜨는 것(squash)** 이
  귀여움의 거의 전부다. 같은 날개면 겹쳐 서고(드묾) 장면이 달라진다.
- **눈으로 안 믿고 쟀다**: 두 짐승이 각각 **45px·46px** 움직인다. 움직임 줄이기에서는 자리만 잡고 멈춘다.

**재열람이 되려면 좌표가 있어야 했다.** 그 전엔 오행·일간만 남겨서 **다시 보려면 생년월일을
또 쳐야 했다.** 궁합을 볼 때 `matchAxes()` 를 함께 저장한다(파생값 · 기기에만).

### 잡힌 것들

- **CSS 템플릿 리터럴 안에 백틱을 또 썼다** — 이 판에서만 셰이더와 CSS 두 번. 문자열이 거기서 끊긴다.
- **행이 `display:flex` 한 줄이라 뒷면이 같은 줄에 끼었다** — 이름 칸이 한 글자씩 접히고 그림이
  오른쪽으로 넘쳤다. **스샷으로 잡았다, 검사로는 안 잡힌다.**
- `flip` 이라는 상태 이름이 **이미 있었다**(판결 카드 앞뒤). 빌드가 잡아 줬는데, 안 잡혔으면
  판결 카드가 곁 탭에서 뒤집혔을 것이다.
- 곁 명부 검사가 **필드가 하나 는 것**을 잡았다. 목록을 넓히는 대신 **좌표 안에 원값이 없는지**까지 물게 했다.

### 실측

검진 심각 0 · 초대 랜딩 50/50 · 초대 54/54 · 개인정보 136/136 · 곁명부 55/55 · 곁탭 37/37 ·
smoke 35/35 · 판결 35/35 · 리포트 122/122 · 궁합 55/55 · 각인 130/130 · 의식 27/27 ·
결제표식 12/12 · 보드동기화 5/5 · 문서 21/21.

### 답만 하고 코드는 안 고친 것 — 「네 뒤를 봐 주는 자리」의 출처

창업자 질문: *"저건 사주 베이스야 아니면 총합 결론이야?"*
**사주 베이스이고, 일간 십성 딱 하나다.** `roleOf(myDG, theirDG)` → `sipseong()` 이 전부이고
입력은 **두 사람의 일간 두 글자**뿐 — 일지도 생시도, 나머지 여덟 축도 안 들어간다.

그런데 **2026-08-15 창업자 판정 4가 「십성만으로는 안 된다」였고**, 그걸 설계로 옮긴 정본이
`01_기획전략/비나리_기획전략_아홉축역할설계_v01.md`(§③ 「극성 층 + 희소성 순위」 · POL/SUR 상수표)다.
**그 설계가 코드에 하나도 안 들어와 있다.** 문서는 결정됐는데 구현이 안 따라간 상태 — 창업자 결정 대기.

### 어린아이는 궁합을 못 보나 (창업자 질문)

**볼 수 있다.** 궁합 폼은 나이를 안 막는다 — 부모가 아이 생년월일을 넣는 건 그대로 된다.
막히는 건 **그 아이가 직접 앱을 쓰는 경우**(온보딩·초대 화면)이고, 그건 개보법 제22조의2다
(만 14세 미만은 법정대리인 동의 없이 개인정보 처리 불가). 우리가 고를 수 있는 선이 아니다.

## 2026-08-28 · v157 — 보이는 움직임

**요청**(창업자): "둥굴어지긴 하는데 모이는 속도가 느리고, 변화되는게 잘 안느껴져. / 근데 XYZ축으로 움직이고 있는 건 맞아?"

**xyz 는 움직이고 있었다. 다만 안 보였다.**
말로 "네"라고 답하지 않고 쟀다 — 12초 관측(0.5초 간격 24회):

| | 이전 | 지금 |
|---|---|---|
| x 이동 폭 | 20px | **61px** |
| y 이동 폭 | 21px | **37px** |
| 면적(z) 변화 | 18% | **75%** |
| 주기 | ~23초 | ~8~10초 |

주기가 23초면 **보고 있는 동안 안 변한다.** 구현 여부와 지각 여부는 다른 문제이고,
지표가 없어서 그 차이를 놓쳤다. "움직이는 게 맞아?"라는 질문이 나온 것 자체가 증거다.

⚠ 폭의 상한: r4 최외곽 0.81 + 드리프트 0.25 = 1.06, 캔버스 반폭 1.175.
더 키우면 헤일로가 캔버스를 넘는다. 지금 값이 상한 근처라 잘림을 재확인했다(네 변 알파 0).

**손끝 모임을 즉각으로**
시상수 0.42 → **0.14초**, 축소 폭 `mix(1,0.58)` → `mix(1,0.44)`, 중심 이동 0.82 → 0.95, 밝기 +42% → +62%.
실측: **120ms 에 36.6% → 23.1%**, 270ms 에 15.9%, **500ms 에 13.6%** 로 도달. 떼면 1.5초에 복귀(여운).

**막는 장치**: `holo-check` 에 ⑪ 두 문항 추가(23문항) —
「x·y 로 떠다닌다(9초 안에 합 45px 초과)」·「z 로 커졌다 작아진다(면적 변화 25% 초과)」.
음성 확인 — 옛 진폭으로 되돌리니 x 14px + y 16px 로 FAIL.

**남긴 교훈**: 있는 기능을 **물어보게 만들면 없는 것과 같다.** 지각되는 크기까지가 구현이다.


## 2026-08-28 · v158 「위습」 — 따라옴에서 다가옴으로

**요청**(창업자): "곁 탭으로 이동했을 때 모이는게 아직도 느려 / 인터랙션은 모이는 모양이 이상한데…? 그냥 통채로 딸려오는 느낌이야. / 나는 개인적으로 이 홀로그램 디자인은 위습 같은 모양이라고 생각해. 인터랙션을 단순히 누르는대로 따라오는게 아니고 같이 뭔가 상호작용할 수 있는 느낌이면 어때? 마치 키우기 게임 같이 / 사람얼굴을 만들라거나 무슨 요정의 전신을 그려달라는게 아니라는 건 명심해"

**「통째로 딸려온다」가 구현 방식을 정확히 짚은 말이었다**
코드가 `p -= tp * tk * 0.95` 로 **장 전체를 평행이동**시키고 있었다. 그건 아무리 값을 만져도
"끌려온다"이지 "다가온다"가 되지 않는다. 위습의 위치를 **JS 물리로 푼다**:

- 스프링(K=34, D=4.6) — **감쇠를 일부러 낮춰** 살짝 지나쳤다 돌아온다. 임계감쇠로 두면 다시 기계다.
- 손끝 0.45 안으로 들어오면 **접선 방향 힘** — 둘레를 맴돈다.
- 실측: 500ms 에 (0.313,0.682) → 1000ms 에 (0.347,0.630). **되돌아오는 오버슈트가 보인다.**

**꼬리 — 위습의 절반**
55ms 마다 위치를 기록해 여섯 점을 뒤로 갈수록 작고 옅게 그린다. 색보다 **밝기**다(흰색 70% 혼합).

**들뜸(ex) — 「키우기 게임」의 자리**
톡 칠 때마다 +0.5(최대 1.6), 2.6초에 걸쳐 식는다. 들뜨면 밝아지고(+28%) 반짝임이 늘고(+90%)
꼬리가 길어지고 맴도는 속도가 빨라진다.
⚠ **얼굴도 팔다리도 그리지 않는다.** 반응하는 건 빛의 세기와 움직임뿐 — 설계 헌장 §페르소나 톤을 지킨다.

**곁 전이는 시간만의 문제가 아니었다**
보간을 0.45 → 0.26초로 당겨도 **줄어드는 양이 19%뿐**이라 변화가 안 보였다.
응축 폭을 키웠다(R 0.42 → 0.34, 면적 −46%). 실측 0.15초에 −12%, 0.5초에 −33%.

**시행착오 셋**
1. 치환이 잘려 셰이더 코드 줄에 **주석 꼬리가 남아** 컴파일 실패(`'?' : syntax error`).
   폴백이 조용히 입자로 떨어져 면적이 2.2%로 붕괴했다 — `console.error` 를 남겨 둔 덕에 찾았다.
2. 꼬리 감쇠 5.5 → **점보다 작아 안 보임** → 2.2 → **본체보다 큰 덩어리** → 3.8 로 안착.
3. `board-sync-check` 가 배열 uniform `u_trail[3]` 의 첨자를 못 떼 늘 FAIL — 검사도 같이 고쳤다.


## 2026-08-28 · v159 「푸딩」 — 곁 전환의 탄성, 손끝의 즉각성

**요청**(창업자): "인터랙션 굼뜨고 이상해 / 곁 탭 말이야. 탭 변환하면 가운데로 모였다가(오버슈팅? 느낌) 다시 퍼지는 느낌. 말랑하고 귀여운 푸딩같은 느낌. 그렇게 모션 가자"

**「말랑하고 귀여운」은 감성 표현이 아니라 스프링 감쇠비 지정이었다**
지수 감쇠(`1 - exp(-dt/τ)`)는 목표에 **도착만** 한다. τ 를 아무리 줄여도 말랑함이 안 나온다 —
빨라질 뿐이다. 스프링으로 바꿔야 지나쳤다 되돌아온다.

```
orbV += ((orbT - orb) * 190 - orbV * 10.0) * dt;
orb   = clamp(orb + orbV * dt, -0.18, 1.28);   // ⚠ 0~1 로 자르면 오버슈트가 죽는다
```

**눌림(squash)** — 푸딩이 말랑해 보이는 두 번째 축. `orbV` 를 세로/가로 비에 **부호 반대로**
실어 넓이가 거의 보존되게 했다(젤리가 찌그러져도 양은 그대로다).

실측 — 곁 탭을 누른 뒤 몸통 가로·세로:
| 시각 | 세로/가로 | 몸통 |
|---|---|---|
| 90ms | **1.60** (세로로 눌림) | 12.5% |
| 180ms | 1.07 | **11.5% (최대 응축)** |
| 270ms | **0.90** (옆으로 퍼짐) | 14.5% |
| 450ms | 1.15 | 13.2% |
| 630ms | 1.01 (원 안착) | 12.9% |

**두 번 출렁이고 안착한다.** 이게 "모였다가 다시 퍼지는" 그 모션이다.

**손끝이 굼뜬 건 세기 문제였다**
K 34 → 170(놓을 때 26 → 30). 말랑함은 **감쇠를 낮춰서** 얻는 것이지 세기를 낮춰서가 아니다.
모임 보간도 0.14 → 0.06초. 실측 **120ms 에 면적 37.1% → 25.8%**.

**맴돌기가 접근을 막고 있었다**
반경 0.45 · 세기 5.4 라 위습이 손끝에 안 붙고 주위만 돌았다(실측 목표의 70%까지).
0.28 안쪽에서만 3.0 으로 낮췄다. **맴돌기는 붙은 뒤의 애교지 접근을 막는 힘이 아니다.**

⚠ 코어 무게중심이 목표에 정확히 안 가는 건 정상이다 — **드리프트(xyz 떠다님)가 위에 얹히기** 때문이고,
손끝에 와서도 살랑거리는 게 오히려 자연스럽다.


## 2026-08-28 · v160 — 손끝에 모일 때 형태를 버린다

**요청**(창업자): "그냥 인터랙션 때 클릭하는 순간 원본 형태 유지가 아니고 그냥 새로운 형태로 하나의 점으로 모이면 좋겠어. 그대로 형태 유지하면서 오니까 이상해서"

**옮기는 것과 모이는 것은 다르다**
지금까지는 오행 비율(`agp`)·불꽃 혀(`sway`·`lift`)·각도 굴곡(`sh`)·물방울·층 어긋남(`orbC`)·
감정 변조(`puffK`·`aRay`)를 **다 지닌 채 위치만** 옮겼다. 그러니 "덩어리가 끌려온다"가 맞다.
**모임은 위치 이동이 아니라 형태의 포기다.**

- 접을 곳이 일곱 군데라 `fold = max(clamp(u_orb,0,1), tk)` **하나로 묶었다.**
  따로 두면 또 하나를 빠뜨린다 — 곁이 둥근 삼각형이던 게 정확히 그 사고였다.
- 반경 `mix(1.0, 0.20, tk)`, **층 간격 `mix(1.0, 0.62, tk)`**.
  반경만 줄이면 작은 오라일 뿐이고, 층이 겹쳐야 점이 된다.
- **꼬리를 속도에 물렸다**(`u_tailK = min(1, |v|*0.55)`). 안 물리면 위습이 멈춘 순간
  여섯 점이 같은 자리에 겹쳐 **커다란 원**이 되고 그 원이 점을 통째로 가린다(실기 확인).
  꼬리는 지나간 자국이므로 **지나갈 때만** 있어야 한다.
- 반짝임도 같이 모은다(위치 0.26배·크기 0.55배).

**시행착오 — 부분 실패가 전체 무반영으로 보였다**
치환 아홉 개 중 마지막 하나(r0~r4 블록)가 실제 코드와 안 맞아 예외가 났는데,
스크립트가 **마지막에 한 번 파일을 쓰는 구조**라 앞의 여덟 개도 전부 날아갔다.
빌드는 성공하고 화면은 그대로여서 "적용됐는데 효과가 없다"로 보였다.
창업자가 "재실행"이라고 해서 알았다. → **여러 치환을 한 번에 할 땐 실패 지점을 먼저 확인한다.**

---

## 2026-08-28 · v162 — 받은 사람도 같은 문서를 본다 · 시·분은 기본 · 내 링크는 내가 안 태운다

### ① 「저 9개만 보는 게 무슨 재미가 있겠어」 — 맞다, 요약은 문서가 아니었다

창업자: *"궁합 결과를 초대한 사람이랑 응한 사람이랑 같은 결과를 볼 수 있게 해줘."*

아홉 칸은 **머리글의 근거**지 문서가 아니다. 「그 사이」가 셋쯤 뜨면 아무 말도 안 한 화면이 된다.
그리고 더 나쁜 건 **부른 사람은 곁 탭에서 궁합 전문을 보는데 받은 사람만 요약을 봤다**는 것 —
같은 두 사람을 두고 둘이 다른 걸 보고 있었고, 그건 이 앱이 계속 피해 온 것이다.

→ 받은 사람 화면이 **같은 컴포넌트**(`MatchDoc`)를 쓴다. 실측 **200자 → 3,268자**.
관계 그림·아홉 축 전문·「하늘끼리 갈린다」·「같이 일하면」까지 전부 온다.
⚠ 화면용으로 다시 쓰지 않았다 — 두 벌이 되면 갈린다. ⑥ 문은 문서 **아래에** 그대로 붙어서
여전히 세 번째 화면이 없다.

**스샷이 하나 잡았다**: 좌표로 연 문서라 폼이 비어 있어 부제가 **「년 월 일생과 맞대 봤어」**로
나갔다. 좌표로 연 문서는 상대 생년월일을 **모르는 게 정상**이므로, 날짜 대신 누구와 맞댔는지를
말하고 *"생년월일은 그 사람 기기에만 있어"* 를 덧붙인다. 모르는 걸 아는 척하지 않는다.

### ② 시·분을 접지 않는다 — 창업자 *"기본값으로. 접어두지 마."*

앞 판에 노크형으로 뒀던 이유는 탭 수(4)를 지키려던 것인데, **안 열면 정오로 계산되고
그건 70%에서 답이 달라진다**(같은 판 실측). **안 보이는 정밀도를 아껴서 얻는 탭 하나보다
틀린 답이 더 비싸다.** 이제 기본으로 보이고, 비워 둬도 볼 수 있다 — 필수가 아니라 기본이다.

### ③ 내가 보낸 링크를 내가 눌러서 태우고 있었다

창업자 질문: *"이건 내가 한 번 클릭해서 작성하고 있는 링크면 안 열리는 거지?"* — **그렇다.**
그런데 그게 문제다. **링크가 잘 갔는지 눌러 보는 건 자연스러운 행동인데, 그러면 응답 1회를
자기가 써 버려서 정작 받을 사람이 못 연다.** 창업자가 실제로 그렇게 태웠다.

서버는 누가 보냈는지 모르지만 **보낸 기기는 자기 초대 id 를 갖고 있다**(`INVITE_KEY`).
그걸로 화면에서 가른다 — 내 초대면 「이건 네가 보낸 초대야」 + 링크 다시 보내기.
**서버는 안 건드린다** — 재공유 방어(응답 1회)는 그대로다.

### 실측

검진 심각 0 · 초대 랜딩 51/51 · 초대 54/54 · 개인정보 136/136 · 곁명부 55/55 · 곁탭 37/37 ·
smoke 35/35 · 판결 35/35 · 리포트 122/122 · 궁합 55/55 · 각인 130/130 · 의식 27/27 ·
결제표식 12/12 · 보드동기화 5/5 · 문서 21/21.

### 남은 것

- **궁합 문서는 명목상 4,900원 상품이다.** 받은 사람 전원에게 전문을 주는 건 그 값을 바꾸는
  결정이다. 지금은 전 상품이 무료 발행(`PRICING_MODE="free_trial"`)이라 당장은 같지만,
  **결제를 붙이는 날 이 화면을 어떻게 할지는 창업자 결정**이다(그냥 두면 초대가 유료 문서의
  무료 우회로가 된다).

## 2026-08-28 · v162 — 폰에서 손끝을 스크롤에 뺏기지 않는다

**요청**(창업자): "폰에서 왜 잡아서 드래그하려라면 스크롤이 되지?"

**내 판단이 틀렸다**
색장 캔버스에 `touch-action` 을 안 걸고 주석에 이렇게 적어 뒀었다 —
「`passive:false` 로 두면 스크롤을 막는다 — 오라는 배경이므로 스크롤을 뺏으면 안 된다」.
**오라는 배경이 아니라 누르는 대상이다.** 그 배려가 정작 눌러야 할 것을 못 누르게 만들었다.
입자 엔진은 처음부터 `touchAction:"none"` 을 걸고 있었고 **색장에만 빠져 있었다.**

- `touchAction:"none"` + `cursor:"pointer"`.
- `setPointerCapture` — 캔버스 밖으로 끌어도 계속 따라온다(없으면 가장자리에서 뚝 끊긴다).
- `pointercancel` 처리 추가.

**검사 지표를 잘못 잡을 뻔했다**
처음엔 `window.scrollY` 로 재려 했는데 **로비는 스크롤이 없어서 늘 0** 이다 — 고치기 전에도
통과해 버린다. 대신 **위습이 손끝을 끝까지 따라왔는가**로 잰다:
제스처를 뺏기면 `pointermove` 가 중간에 끊겨 위습이 목표에 못 간다.
실측 **0.489(뺏김) vs 0.298(정상)**, 목표 0.22.

**막는 장치**: `holo-check` ⑫ 「폰 드래그를 스크롤에 안 뺏긴다」(24문항).
`hasTouch:true` 컨텍스트를 새로 열고 CDP 로 실제 터치 이벤트를 보낸다 —
마우스로는 이 버그가 재현되지 않는다. 음성 확인: `touch-action` 제거 시 0.489 로 FAIL.


## 2026-08-28 · v163 — 캔버스를 검은 판 수준으로 넓힌다

**요청**(창업자): "까만색 버전 대비 캔버스가 작은 거 아냐?"

**재 보니 두 가지가 갈렸다**
| | 검은 판(입자) | 홀로(색장) |
|---|---|---|
| 캔버스 CSS | 800×800 | **370×370** |
| 화면에서 수호신 크기 | 163×259 | **308×373** |
| 오라/캔버스 비율 | 21~32% | **70%** |

**수호신 자체는 홀로가 더 크다.** 작은 건 캔버스였고, 그래서 오라가 캔버스의 70%를 차지해
**떠다니고 손끝을 따라갈 여백이 없었다** — xyz 드리프트도 손끝 추적도 가장자리에 부딪혔다.

- `guardianSize` 홀로: `vp.w*0.96 · vp.h*0.46 · 460` → `vp.w*1.50 · vp.h*0.70 · 660`(578px)
- `R`: 0.52 → 0.32 (곁 0.34 → 0.208). 캔버스가 커진 만큼 줄여 오라 크기를 지키려 했다.
- 온보딩·회상 화면 캔버스도 같은 비율로.

결과: 오라/캔버스 **70% → 53%**. 오라 자체는 261 → 308 로 조금 커졌다.

⚠ 캔버스가 뷰포트보다 넓어 좌우로 92px 씩 나가지만 **검은 판도 800 이라 같은 방식**이고,
가장자리 알파는 네 변 모두 0 이다(잘림 없음).

**남긴 교훈**: "캔버스가 작다"와 "수호신이 작다"는 다른 말이다.
재 보지 않았으면 **오라를 키워서 여백을 더 없앨 뻔했다** — 지적과 정반대로 갔을 것이다.

---

## 2026-08-28 · v165 — 궁합을 무료로 풀고 판결 탭에서 뺀다

**창업자**: *"궁합 공짜로 풀자 / 판결 탭에서 궁합 없애."*
판단 근거·기각한 선택지·남은 위험은 `전략로그.md` 2026-08-28 에 적었다. 여기는 **코드가 무엇을 했는가**만.

### 한 것

- **`MATCH_PRICE` 상수를 지웠다.** 표시만 지우면 다음 세션이 "값 숨긴 유료 상품"으로 읽는다.
- **로비에서 궁합 버튼 제거.** 입구는 곁 탭 하나 — 사람을 들이는 유일한 문이고 결과가 명부로 이어진다.
  아래 안내문이 「둘 다 지금은 값을 안 받아」였던 것도 이제 각인 하나라 같이 고쳤다
  (**문구를 안 고치면 화면이 없는 버튼을 가리킨다**).
- **곁 빈 화면 CTA 가 탭을 안 옮긴다.** 예전엔 `setTab("judge")` 로 보내고 문서를 열었는데,
  궁합이 판결 탭에서 빠진 뒤로는 **없는 자리로 보내는 셈**이다. 문서는 화면을 덮으므로 애초에
  탭을 바꿀 이유가 없었다. 문구도 *"둘 사이를 보면 그 사람이 곁에 서"* 로.
- **`markFreeIssue("match")` 제거** — 사유는 전략로그. 코드 주석에도 박아 뒀다.
- 계측: `match_offer_shown` 폐기 · `match_run` 에 `free: true`.

### 검사 — 지우지 않고 **뒤집었다**

무료화·이동으로 네 검사가 깨졌는데, 넷 다 **정확한 검출**이라 지우는 대신 방향을 뒤집었다.

- `privacy-check` 「궁합 버튼이 값을 보여준다」 → **「값을 안 보여준다」** + `MATCH_PRICE` 잔존 금지.
  무료로 푼 상품이 값 표시를 달고 있으면 그게 거짓말이다.
- `privacy-check` 「세 상품 모두 노출 이벤트」 → **두 상품** + 「무료가 된 상품의 분모는 걷었다」.
- `pricing-check` 무료발행 표식 세 상품 → **둘** + 「무료 상품엔 표식을 안 건다」.
- `report-check` 「궁합 진입점이 로비에 있다」 → **「로비에 없다」 + 「곁 탭에 있다」.**
  둘 다 안 물면 다음 판에 조용히 되살아난다.

### 잡은 것 — 길이 세 군데 베껴져 있었다

`report-check`·`gyeot-check`·`doc-check` 셋이 **각자 로비 버튼을 눌러** 궁합을 열고 있었고,
문이 옮겨지자 셋이 동시에 **타임아웃으로만** 알려 왔다(무엇이 문제인지는 안 알려 준다).
→ `e2e/open-match.mjs` 신설. **궁합 여는 길은 한 곳에만 적는다.**

### 실측

검진 심각 0 · 리포트 123/123 · 개인정보 137/137 · 결제표식 12/12 · 곁탭 37/37 · 초대 랜딩 51/51 ·
초대 54/54 · 곁명부 55/55 · smoke 35/35 · 판결 35/35 · 궁합 55/55 · 각인 130/130 · 의식 27/27 ·
곁예산 13/13 · 보드동기화 5/5 · 문서 21/21.

## 2026-08-28 · v165 — 예비동작, 그리고 얼굴 시안 보드

**요청**(창업자): "곁 탭으로 탭간 이동할 때 작아지면서 뽀잉이 아니고 **커졌다가 작아지면서 뽀잉**으로 해야 아 변했구나!라는게 확실히 느껴질거 같아" / "하울의 움직이는 성의 캘시퍼처럼 눈과 입도 추가해볼까? **우선 적용하기 전에** 나에게 어떤 디자인 적용할건지 먼저 보드에 붙여서 보여줘"

### 예비동작 — 세 번 만에 됐다
| 시도 | 방법 | 왜 안 됐나 |
|---|---|---|
| ① | 전환 순간 속도에 반대 임펄스 | 같은 프레임에 스프링 항이 곧바로 상쇄 |
| ② | 위치를 반대편 끝으로 던짐 | K=190 이라 **120ms 만에 지나가** 안 보임 |
| ③ | **0.17초 동안 목표 자체를 반대편에** | ✅ |

**스프링 하나로는 시작점만 바뀔 뿐 머무는 시간이 없다.** 예비동작은 시간을 줘야 보인다.

실측 `orb` 추이: **0 → −0.43(커짐) → 1.30(작아짐) → 0.95 → 1.00 안착.**

**막고 있던 것 둘**
- `R` 에 `clamp(u_orb, 0, 1)` — **커지는 쪽 오버슈트가 죽었다.**
  v159 에 "0~1 로 자르면 오버슈트가 죽는다"고 적어 놓고 정작 반경 식에 넣었다.
- `squash` 0.105 가 과해 초반에 세로로 길쭉해지며(가로 99 세로 231) **오히려 면적이 줄어**
  "커졌다"가 눌림에 먹혔다 → 0.042.

**측정 방법을 바꿨다**: 화면을 읽어 재면 프레임당 130ms 라 300ms 전환을 못 본다.
`window.__BINARI_ORB` 로 응축값을 노출해 잰다 — `window.__BINARI_R` 과 같은 관례다.

### 얼굴 시안 보드 (제안 · 앱에는 안 넣음)
`app/public/face-mock.html` — 생성기 `tools/build-face-mock.mjs`.
- **눈 8종**: 점 · 웃는 눈 · 반쯤 감김 · 동그란 눈 · 세로 눈 · 감은 눈 · 세 눈 · 별 눈
- **입 6종**: 점 · 미소 · 지그재그(캘시퍼) · 오 · 일자 · 물결
- **오행별 추천 조합** — 근거는 오행의 성질(화=크게 뜨고 장난스럽게, 금=재는 눈에 단정한 입 …)
- **오늘 상태는 입만 바꾼다** — 눈은 사주(타고난 것), 입은 오늘(변하는 것)

⚠ 얼굴 윤곽·머리카락·몸·팔다리는 그리지 않는다. 캘시퍼도 불꽃에 **눈과 입만** 있다 —
그 선을 넘으면 설계 헌장이 막은 "정령 시각요소 부활"이 된다.
⚠ 눈·입 카탈로그 칸은 오라를 안 켠다 — 칸마다 WebGL 컨텍스트를 열면 **브라우저 상한(≈16)** 에
걸려 앞쪽이 통째로 빈다(v141 에서 같은 걸 겪었다).


## 2026-08-28 · 얼굴 시안 v02 — 캘시퍼를 잘못 그리고 있었다

**요청**(창업자, 원본 GIF 첨부): "가루시파가 어떻게 생겼는지는 알아?"

**핵심을 빠뜨렸다 — 흰자**
원본 눈은 **큰 흰 타원 + 작은 검은 동공**이다. 나는 검은 점·선만 그렸고, 그건 캘시퍼가 아니라
이모티콘이다. **흰자가 있어야 시선이 생기고 표정이 산다** — 두 요소짜리 그림에서 이게 전부다.

입도 틀렸다. 지그재그가 아니라 **얇고 구불거리는 선**이고, 눈보다 훨씬 작으며
한쪽이 내려가 비대칭이다.

**다시 짠 목록**
- 눈 8: 캘시퍼 눈 · 더 크게 · 작은 동공 · **곁눈** · 반쯤 감김 · 웃는 눈 · 세로 동공 · 세 눈
- 입 6: **구불선** · 일자 · 미소 · 삐죽 · 오 · 물결

**곁눈이 인터랙션과 붙는 자리다** — 동공만 옮기면 되므로, 나중에 **위습이 손끝을 쳐다보게**
만들 수 있다. 얼굴을 더 그리지 않고도 "반응한다"가 하나 더 는다.

⚠ 시행착오: `ball()` 이 y 를 클로저로만 써서 **세 번째 눈을 위에 못 놓아** 세 눈이
가로로 뭉갠 타원이 됐다 → y 를 인자로 받게.

⚠ 여전히 **제안이고 앱에는 안 넣었다.**

**남긴 교훈**: 레퍼런스를 "안다"고 여기고 그린 게 틀렸다. 원본을 보고서야 흰자가 핵심인 걸 알았다.

---

## [2026-08-28] v165.1 — 동전 카피 축약 · 「타고난 그릇」 무료 표면에서 내림

**요청(창업자)**: ①*"코인 던질 때 버튼에 '쥐었던' 이런식으로 나오는데 카피가 어색해. 그냥 줄여. 간결하고 직관적이게."* ②*"타고난 그릇은 우리 리포트 9,900원짜리의 일부로 보여질 정도로 너무 상세한데, 일단은 보여주지 않는 걸로 하자."*

### 1. 동전 카피 (완료)

| | 전 | 후 |
|---|---|---|
| 기본 | 쥐었다 놓아 던진다 (0/6) | **던진다 (0/6)** |
| 쥐는 중 | 손 안에서 구르는 중… 놓으면 던져져 | **놓으면 던져져** |
| 공중 | 동전이 공중에… | **공중에…** |
| 밑줄 | 오래 쥐고 있어도 돼 — 손에 머무는 시간만 길어지고, 나오는 건 하늘이 정해. | **오래 쥐어도 결과는 안 바뀌어.** |

밑줄은 줄이되 **문장의 목적은 지켰다** — 쥔 시간이 결과에 영향을 준다는 오해를 막는 줄이라 빼면 안 된다.
`ritual.mjs`·`ritual-check.mjs` 의 버튼 이름 정규식도 같이 갱신했다(`/^던진다 \(/`).

### 2. 「타고난 그릇」 무료 표면에서 내림 (완료)

`MSR_FREE` 스위치 신설. **컴포넌트는 안 지웠다** — `COIN_RITUAL` 과 같은 이유로, 지웠다가 되살리면 같은 물건이 안 나온다. 각인에 편입하든 여기로 되돌리든 원본이 있어야 한다.

⚠ **§알 권리(헌장 2026-08-06)와 충돌하지 않는다 — 국면이 다르다.** 그 조항은 *"값을 치른 문서를 여는 유저"* 에게 정보를 숨기지 말라는 것이고, 여기는 **값을 안 치른 판결 카드 뒷면**이다. 문제가 은닉이 아니라 **반대로 과잉 무상 제공**이다 — 각인(9,900원)이 팔려는 두께를 판결 카드가 공짜로 먼저 준다.
⚠ **단 v109 가 이 블록을 §알 권리 근거로 '기본 펼침'으로 만들었다.** 그 결정을 뒤집는 것이므로, 되돌릴 때 v109 의 근거(실유저 지적 2건)를 같이 읽으라고 주석에 못 박았다.

**`?msr=1` 디버그 문을 냈다.** `report-check` 122항이 그 문으로 들어가므로 **숨긴 동안에도 회귀가 계속 잡힌다.** 안 그러면 122항이 통째로 잠든다. 실측 확인: `?i=0` → 0개 / `?i=0&msr=1` → 1개.

### 3. brood-check 간헐 실패 — 원인이 내 것이었다

「② 가라앉는다」가 세 번 중 두 번 실패했다. main 에서도 재현돼 처음엔 사전 존재로 봤는데, **v142(동전 부활)가 원인이었다** — 마지막 낙착의 일렁임(`agitateRef`, 1.4초)이 측정 창에 겹친다. 의식이 꺼져 있던 동안엔 이 경로에 일렁임 자체가 없었다.
측정 대기를 2.2초 → **3.4초**로 늘리고 사유를 검사에 적었다. **3/3 안정.**

### 검사

빌드 OK · ritual 27/27 · **리포트 122/122(디버그 문으로)** · 판결 35/35 · smoke 35/35 · **대기연출 10/10(수리)** ·
개인정보 133/133 · webgl 7/7 · v29 7/7 · 리텐션 26/26 · track 24/24 · pricing 12/12 · 곁 37/37 ·
각인 130/130 · 궁합 55/55 · 공개표면 33/33 · 검진 심각 0.

---

## [2026-08-29] v166 — 문장 감사 1~3단계: 배포 · 처리방침 · 어긋난 자리 다섯

**요청(창업자)**: 문장 감사 보고의 「손대는 순서」대로 *"순차진행"*.

### 1단계 — 배포 (완료)

감사가 잡아낸 것 중 가장 먼저 봐야 할 게 **지시 2건이 라이브에 없다는 사실**이었다.
`merge-base --is-ancestor` 로 확인했고(NOT MERGED), main 에 올렸다 — 동전 카피 축약과 `MSR_FREE`.
**원인은 실수가 아니라 흐름이다** — "main에 올릴까요?"를 여쭙고 답을 못 받은 채 다음 요청이 들어왔다.
→ 교훈: **지시를 반영한 커밋이 브랜치에만 있으면 그건 반영이 안 된 것이다.**

### 2단계 — 처리방침: 보낸 이 이름이 서버에 간다 (완료)

`inviteNew()` 가 `{ axes, name: birth.name }` 를 올리고 서버가 30일 보관한 뒤 B 에게 돌려준다
(B 화면의 「○○가 너와의 사이를 궁금해했어」가 그 값이다). 그런데 두 곳이 그 사실을 안 적고 있었다.

- **처리방침 §5-2 「저장하는 것」** — **받은 이의 이름만** 적혀 있었다. 보낸 이 것을 추가했다(용도·12자 상한·없으면 안 보냄까지).
- **곁 명부 화면** — 「이름도 생년월일도 이 기기에만 있어 — 서버로도, 통계로도 안 나가」가
  **그 이름을 보내는 버튼 바로 위**에 서 있었다. 곁에 적어 둔 상대들 이름은 정말로 기기에만 있으므로
  **둘을 갈라 적었다**(곁 이름은 로컬 / 초대를 보내면 내 이름은 서버를 거친다 · 30일 · 그 사람에게만).

⚠ **기존 `privacy-check` 셋은 이 구멍을 구조적으로 못 봤다** — 파생값 **축 이름**만 대조하고
「무엇이 저장되는가」의 목록 자체는 안 물었다. 검사 2항을 신설해 그 자리를 물게 했다.

### 3단계 — 화면과 코드가 어긋난 자리 다섯 (완료)

| 무엇 | 어긋남 |
|---|---|
| 「끄고 싶으면 **아래에서**」 | 그 화면(step 0)에 스위치가 없다. 실물은 온보딩 5칸 + 형성 3.2초 + **더블탭** 뒤에야 뜬다. **A-1 이라 이름까지 붙여 놓고 동의문만 고친 자리**였다 → 버튼 이름 그대로 적었다 |
| 「**두드려봐**」 | 깨우는 건 **더블탭**이다(`now - wakeTapRef < 350`). 옆 탭은 「두 번 두드려봐」로 이미 정확한데 **전 유저가 매 방문 통과하는 이 문에서만** 틀렸다 |
| `akHigh = ratio >= 0.8` | 가나 **5점 = 기질이 다름**인데 5/6=0.833 이 통과 → 화면이 「기질이 맞나가 **잘 맞아**」. 高 판정의 57.8%. 그라하도 같은 구멍(4/5) → **만점(`sc === max`)으로** |
| 「주도권이 자연스럽게 **갈려**」 | `VARNA[rA] >= VARNA[rB]` 라 **동급도 1점**. 전수 144쌍 중 36(40.0%)에게 거짓이었다 → 엔진은 안 건드리고 **1점일 때 100% 참인 서술**로 |
| `["천을귀인", "암록"]` | push 는 `"암록(暗祿)"` 라 **영영 안 걸렸다**. 같은 화면 아래쪽은 `SIN_KO` 매핑으로 「숨은 복」이 뜨는데 위에서만 빠져 있었다 |

⚠ **궁합 둘은 무는 검사가 0건이었다**(`grep '바르나|가나|akHigh' e2e/*.mjs` → 0).
`match-check` ⑪ 4항 신설 — **소스 성질 + 산출물 전수(120쌍)** 둘 다 본다. 문턱 숫자를 바꿔도 거짓이 되살아나면 걸린다.
⚠ 검사를 처음 쓸 때 한 번 헛디뎠다 — 「…가 잘 맞아. **대신** …이 낮아」 한 문장에 高·低 목록이 둘 다 들어 있어서
통째로 훑으면 낮은 항목까지 잡힌다(112/120 오탐). **「잘 맞아」 앞부분만 잘라야 한다** — 사유를 검사에 적었다.

### 검사

빌드 OK · 개인정보 **138/138(+2)** · 궁합 **59/59(+4)** · 판결 35 · smoke 35 · ritual 27 · 리포트 122 ·
각인 130 · 곁 37 · 명부 55 · 역할 26 · 공유 16 · 공개표면 33 · origin 20 · doc 21 · 리텐션 26 ·
track 24 · pricing 12 · 검진 심각 0.
⚠ `brood-check` 는 여전히 간헐(9/10 ↔ 10/10). **수호신 캔버스 밝기·무게중심 측정**이고 그 렌더러가
디자인 세션에서 최근 교체됐다. 이번 변경은 캔버스를 한 줄도 안 건드린다 — 렌더러 레인 항목으로 남긴다.

---

## [2026-08-29] v167 — 문장 감사 4단계: 서신 프롬프트 (R1·R2)

감사가 **"가장 나쁜 곳"**으로 지목한 표면이고, 원인이 문자열이 아니라 프롬프트라 화면을 아무리 고쳐도 안 바뀌는 자리다.

### R1 — 5장 제목에서 부제가 사라진다

목차(`LETTER_SECTIONS`)는 「무엇을 걸고 — **이 판결이 틀릴 조건까지**」를 걸고 파는데,
본문 **10건 전부** 제목이 「무엇을 걸고」로만 나왔다. 지시문이 제목을 그렇게 주고 있었기 때문이다.
**경쟁 8사에 없는 유일한 물건(반증 조건)이 제목에서 잘려**, 산 사람이 그게 거기 있는 줄 모른다.
→ 지시문 제목에 부제를 붙이고 「이 줄 그대로 쓴다(부제까지)」를 명시.

⚠ **부작용을 같은 커밋에서 막았다.** `health-check.mjs` 가 서신 4장 구역을
`cut(app, '4) "누구와', '5) "무엇을 걸고"')` 로 자른다 — **닫는 따옴표가 앵커에 있었다.**
제목을 고치면 그 문자열이 사라져 `indexOf` 가 −1 → **구역이 파일 끝까지 부풀어** 무관한 줄에서 오탐이 터진다.
앵커에서 닫는 따옴표를 빼 **제목 앞부분만** 물게 했다. 적용 후 구역 547자로 정상 경계 확인.

### R2 — 「명식」이 그대로 나간다

「쓰지 않는 말」 줄이 **금지만 하고 대체어를 안 줬다.** 바로 위 줄은 「비견=나란히 서는 힘」처럼
**짝**을 주는데 이 줄만 목록이었다. 실측: 본문 10건 중 **「명식」 5건**이 유저 화면에 나갔다.
그리고 서신에는 앞면의 `FRONT_JARGON` 같은 **사후 가드가 0**이라 프롬프트가 유일한 방어선이다.
→ 금지 줄은 **남기고**(다른 줄이 그걸 가리킨다) 그 아래에 짝 13개를 덧붙였다.
SYS 스스로 같은 진단을 적어 놨다 — *"이 목록은 외워서 지켜지지 않는다 … 고쳐 쓴 짝을 따라라."*

**검사 신설 — 금지어에는 짝이 있어야 한다.** 금지 목록과 짝 목록을 맞대어,
짝 없는 금지어가 생기면 검진이 문다. 실제로 신설하자마자 **둘을 잡았다**(유파·진태양시) —
「유파·통설=보는 방식」처럼 둘을 묶어 적으면 앞 낱말은 짝이 없는 것이 된다. 각각 주도록 고쳤다.
⚠ 검사 구역을 처음엔 「쓰지 않는 말:」~「**단,」로 잡았는데 **내가 새로 넣은 짝 줄까지 삼켜서**
금지어가 뭉개져 잡혔다. 「바꿔 쓰는 말(이어서)」에서 끊게 고쳤다.

### 아직 안 한 것 (감사 §③ 잔여)

R3(아홉 하늘인데 한 하늘만 쓴다) · R4(장끼리 같은 재료를 두 번 판다) · R5(마야 (O) 예시가 은유로 끝난다) ·
R6(세운 — 「리포트 배경 전용」과 「우선 인용」이 한 호출에 같이 들어간다 · **창업자 결정 사항**) ·
R7(곁 사람에 대해 뭘 말해도 되는지가 안 적혀 있다).
**R3·R4 는 같이 손대야 한다** — R4(중복)의 뿌리가 R3(재료가 한 우물)이다.

### 검사

빌드 OK · 검진 **금지어 짝 13/13** · 개인정보 138 · 궁합 59 · 판결 35 · smoke 35 · ritual 27 ·
리포트 122 · 각인 130 · 검진 심각 0.

---

## 2026-08-28 · v166 — 오늘의 상태: 체계를 섞고, 문구를 「그래서 오늘 뭘」로

**창업자**: *"오늘의 상태는 왜 사주 베이스로 했어?"* → *"체계를 섞어. 그리고 표현을 이해 가능하게 해.
혼자 파고들게 돼 = 뭘…? 이런 식의 표현은 뭘 말하고자 하는지 이해가 안 돼."*

### 왜 사주였나 (질문에 대한 답 — 코드에 없던 것)

「오늘의 상태」는 **(a) 날마다 바뀌고 (b) 사람마다 달라야** 성립한다. 11개 체계를 그 두 조건에 걸면
평생 고정(명식·별자리·나크샤트라·웨톤·납음·수비학)이 (a)에서 떨어지고,
날마다 바뀌는 것들(달 위상·달 별자리·오늘의 촐킨)은 **모두에게 같아서** (b)에서 떨어진다.
남는 건 **「오늘의 값 × 내 값의 관계」**뿐이고, 그 관계표를 이미 갖고 있는 게 십성이었다.

**단 유일한 선택은 아니었다** — 인도 타라도 되고, 그 계산은 궁합 축 ③(`ashtakutaIdx`)에 이미 있었다.
코드에 그 비교가 안 적혀 있었다. 이제 적었다.

### 실측이 문제를 하나 더 드러냈다

십성은 **일간 10개**만 보므로(일지를 안 본다) 반복 주기가 **정확히 10일**이었다 —
한 달에 세 번 같은 문장. 그리고 더 큰 것: CLAUDE.md 가 방어자산으로 적은 「11개 체계」가
**유저가 매일 보는 한 줄에서는 하나**였다. 그 자리에서는 우리도 만세력 단일 엔진이다.

### 섞었다 — 합치지 않고 분업시켰다

| 체계 | 답하는 질문 | 가짓수 |
|---|---|---|
| 사주 일진 십성 | 오늘 **무엇이 잘 되나** | 10 |
| 인도 타라 | 오늘 **움직여도 되나** | 9 |

`match.js` 파일 머리 원칙 그대로 — **평균이 아니라 분업.** 실측 주기:
십성 10일 / 타라는 달이 27수를 1.0119일에 하나씩 지나 딱 안 떨어짐 →
**섞으면 90가지, 400일 안에 정확한 반복 없음**.

⚠ **축을 늘린 게 아니다** — 타라는 궁합이 이미 쓰는 값이고 계산도 있는 함수를 부르는 게 전부다.
**판결에는 여전히 안 들어간다**(설계 헌장 §판결문 형식 보존). 달 위상도 그대로 밝기에만 ±6%.

### 문구 — 같은 교정을 두 번째로 한다

*"혼자 파고들게 돼 = 뭘…?"* 맞다. **속마음에 이름을 붙인 것**이지 오늘 뭘 하라는 말이 아니다.
이 리포는 같은 교정을 이미 했다 — 곁 역할표(`match.js` ROLE, 2026-08-15 규칙 3
「좋은 말이 아니라 쓸모」). 그때 이름을 **구조 서술**에서 **쓸모**로 바꿨다. 여기도 같은 규칙:
**모든 줄이 「그래서 오늘 뭘」에 답한다.**

- 혼자 파고들게 돼 → 혼자 파고드는 게 잘 되는 날 — **사람 만나는 일정은 내일로**
- 계산이 잘 서 → 숫자가 잘 맞아 — **정산·계약 볼 게 있으면 오늘**
- 말이 잘 나와 → 말이 술술 나와 — **오래 남을 말이니 뱉기 전에 한 번 삼켜**

### 첫 판에서 둘을 더 잡았다 (실물 화면으로)

- **근거 줄에 「달은 삼파트자리」가 나갔다.** 본문만 용어를 막고 근거 줄을 안 막았다.
  산스크리트에 「자리」를 붙여도 뜻이 하나도 안 전해진다 → **「들어오는 자리」**처럼 우리말로.
  어휘 계열은 곁 역할표·`axis` 와 같다. 원어는 주석에만 남겼다.
- **「오늘 하늘 기운 을」이 목적격 조사처럼 읽혔다** — 십간 열 중 '을' 하나가 그렇다.
  한자를 붙여 갈랐다(`을(乙)`). `GANH` 를 App.jsx 에 들였다.

### 검사 — `e2e/mood-check.mjs` 17항목 신설

**문장을 심사할 수는 없다.** 대신 **되살아나면 안 되는 모양**을 문다.
⚠ 처음엔 행동 동사 목록으로 쟀는데 목록이 계속 모자랐다(볼·미루·내일로가 다 빠졌다) —
넓히면 아무거나 통과한다. 그래서 **검사 쪽을 고쳤다**: 잡을 건 어휘가 아니라 **구조**다.
지적받은 줄들은 전부 **한 마디**로 끝나고 고친 줄들은 전부 **두 마디**(상태 + 그래서 뭘)다.
**음성 확인을 검사 안에 심었다** — 옛 문구 넷을 넣으면 4/4 다 걸린다.

⚠ 그리고 이 파일에서도 **자기 주석에 걸렸다(여섯 번째)** — 주석이 `sidIdx` 를 인용해서
「새 경로를 안 쓴다」 검사가 FAIL 났다. 주석을 걷고 보게 고쳤다.

### 알아 둘 것

**「오늘의 상태」는 깨우기 전 로비에서만 뜬다**(`?skin=holo` · `phase>=1 && !res && !awake`).
판결을 물으러 깨우면 사라진다. 검사하다 한참 헤맸는데 버그가 아니라 설계다 — 검사 주석에 박아 뒀다.

### 실측

검진 심각 0 · 오늘의 상태 17/17 · smoke 35/35 · 판결 35/35 · 곁탭 37/37 · 리포트 123/123 ·
개인정보 139/139 · 초대 54/54 · 랜딩 51/51 · 문서 21/21 · 보드동기화 5/5.

---

## 2026-08-28 · v170 — 한 번에 던지기 되살림 · 긴 문서에 길을 놓는다

### ① 한 번에 던지기 — **v142 결정을 뒤집는다**

창업자: *"일괄 던지기 버튼도 추가해. 없애기 직전으로 롤백한다 생각하면 돼, 이 기능만."*

⚠ **없앤 것도 창업자 지시였다**(v142): *"무료로 바꾼 이상 일정 허들은 남겨야겠어"* +
「그 버튼이 있으면 재미도 마찰도 동시에 무너진다」. **없앤 이유를 지우지 않는다** —
코드 주석과 검사에 그대로 남겨 뒀다. 다음 세션이 "왜 있지" 하고 또 걷어내지 않게.

되살리되 **여섯 번을 한 번으로 압축할 뿐 규칙은 그대로**다 — 같은 `oneCoin()`, 같은 `finalize()`.
한 번에 던졌다고 다른 괘가 나오면 그건 다른 물건이다.
- **위계는 한 수 아래**(ghost). 손으로 던지는 게 기본이고 이건 급한 사람의 우회로다.
  같은 무게로 두면 아무도 여섯 번 안 던진다.
- **계측은 던진 횟수만큼 남긴다**(`ritual_tossed` × n, `bulk:true`). 한 건으로 뭉치면
  「몇 번째에서 이탈하나」를 못 읽는다 — v142 가 그 계측을 만든 이유가 정확히 그것이다.

### ② 긴 문서에 목차 — 유저 제보 둘

*"내용이 너무 길고 … 보려던 내용을 다시 위로 올리니 어디였는지 찾기 어렵다"* +
*"섹션별 타이틀이 잘 눈에 안 들어온다."*

맞다. 각인은 섹션이 **열둘**이고 세로로만 이어져 있다. 그리고 §알 권리(2026-08-06)가
*"값을 치른 문서는 줄 수 있는 걸 다 주고 완결시킨다"* 로 정한 이상 **문서는 앞으로도 길다** —
짧게 만드는 게 답이 아니라 **길을 놓는 게** 답이다.

- **`DocNav` 신설** — 각인·궁합이 **같은 컴포넌트**를 쓴다(두 벌이 되면 갈린다).
- ⚠ **왼쪽 사이드바로 안 만들었다.** 제보는 "좌측이나 어디"였는데 이 앱은 폭 430px 기준이라
  세로 목차를 옆에 세우면 본문이 반으로 준다 → **위에 붙는 가로 띠**.
- ⚠ **목차를 손으로 안 적는다.** 섹션 셋이 조건부다(짝·직장·삶의 자리) — 목록을 따로 적으면
  **없는 자리로 보내는 목차**가 된다. 그래서 **그려진 DOM 에서 읽는다**(`.imph[data-tag]`).
  실측: 이 페르소나는 9칩(12섹션 중 셋이 안 그려짐). 검사가 「칩 수 = 제목 수」를 문다.
- **제목을 제목처럼** — 10.5px 자간 .16em 이라 **라벨로** 읽혔다(본문보다 작았다).
  16px·600 으로 올리고 부제를 오른쪽 띄우기에서 **아랫줄**로 내렸다.

### 잡은 것 넷 — 셋은 내 실수

1. **CSS 문자열 안에 백틱을 또 썼다(이 세션 세 번째).** `e2e/css-backtick-check.mjs` 가
   이미 있었는데 **검진이 그걸 안 불렀다** — 그래서 세 번을 반복했다.
   **검진 5-r 로 편입**했다(음성 확인 통과). 빌드도 잡긴 하지만 메시지가
   「Expected ";" but found …」라 원인이 안 보인다.
2. **활성 칩이 안 따라왔다** — 관찰 띠를 화면의 5%로 잡아서, 눌러서 건너뛰면 제목이
   그 틈을 **지나쳐 버렸다**(「삶의 자리」로 갔는데 칩은 「너」). 띠를 넓히고
   「화면 위쪽에 있는 마지막 제목」을 현재로 치게 + 스크롤이 멎은 뒤 한 번 더 본다.
3. **칩 자동 스크롤이 문서를 흔들었다** — `scrollIntoView` 는 세로도 건드린다.
   가로 위치만 직접 계산하게 바꿨다.
4. **마지막 칩이 닫기(✕) 밑에 깔렸다** — 실물 스샷으로 잡았다. 오른쪽을 74px 비웠다.

### 검사에서 내가 두 번 틀렸다

- **`ritual-check` 의 `ck()` 는 `ck(조건, 설명)` 순서인데 설명을 앞에 썼다.**
  문자열이 조건 자리에 들어가면 늘 참이라 **항상 통과하는 빈 검사 여섯**을 넣을 뻔했다.
  출력에 ⑧ 이 한 줄도 안 보이는 걸로 알아챘다.
- 그리고 그 블록을 **`catch` 안에** 넣어서, 앞이 실패할 때만 도는 검사였다. try 안으로 옮겼다.
- ⚠ `ritual-check` 의 「tossAll 이 없다」 두 항목은 **지우지 않고 뒤집었다** —
  「있되 같은 규칙(같은 동전·같은 마무리)으로 도는가」로. 지우기만 하면 다음 판에
  아무 규칙 없이 도는 버튼이 된다.

### 실측

검진 심각 0 · 문서 목차 11/11(신설) · 동전 의식 35/35 · 오늘의 상태 17/17 · smoke 35/35 ·
판결 35/35 · 곁탭 37/37 · 리포트 123/123 · 개인정보 139/139 · 각인 130/130 · 궁합 59/59 ·
초대 54/54 · 랜딩 51/51 · 곁명부 55/55 · 곁예산 13/13 · 결제표식 12/12 · 보드동기화 5/5 · 문서 21/21.
## 2026-08-28 · 얼굴 시안 v03 — 조사 대신 비교자

**요청**(창업자): "인스타그램에서 인스타툰 중심으로 사람들에게 인기 많은 채널들의 캐릭터 생김새가 어떤지를 보고. 눈과 입 디자인, 크기, 간격, 위치를 검토해볼래?"

**못 했다. 먼저 그걸 적는다.**
- 인스타그램: 로그인 필요 + 망 정책 차단
- 검색으로 나온 글(고구마팜·나무위키·클립스튜디오): **전부 `EGRESS_BLOCKED`**
- `WebFetch`: 페이지를 마크다운으로 바꾸므로 **그림 자체를 못 본다**

그림을 못 본 채 "인기 인스타툰은 눈이 크고 간격이 넓다" 같은 문장을 쓰면 그건
**학습 데이터에서 끌어온 추정**이지 조사가 아니다. CLAUDE.md §응답 규칙 —
"근거가 없으면 근거 없음/추정이라고 표시한다" — 를 지키려면 아예 안 쓰는 게 맞다.

**대신 대조할 자를 만들었다**
`face-mock.html` 에 「비교자」 섹션 신설. 한 번에 **한 축만** 바꾼다 —
두 축을 같이 바꾸면 뭐가 원인인지 못 짚는다.
- 눈 크기(흰자 지름) 11 · 13 · **15(지금)** · 18 · 21%
- 두 눈 간격 20 · 24 · **27(지금)** · 31 · 35%
- 눈 높이 38 · 42 · **46(지금)** · 50 · 54%
- 입 크기와 눈–입 거리 4단

**지금 값을 수치로 못 박았다** (전부 칸 한 변 대비 %라 어떤 캡처와도 바로 비교된다)
| | |
|---|---|
| 눈 흰자 | 가로 15.1% · 세로 16.6% |
| 동공 | 흰자의 29% |
| 두 눈 중심 간격 | 27.0% (흰자 지름의 1.79배) |
| 눈 높이 | 위에서 45.5% |
| 입 폭 | 8.9% (흰자의 0.59배) |
| 눈→입 | 12.0% (흰자의 0.79배) |

**남긴 판단**: 조사를 못 하면 추정으로 메우지 말고 **비교 가능한 도구를 넘긴다.**
캡처를 옆에 놓고 어느 칸에 가까운지 짚어 주면 그 값으로 맞추면 된다 —
캘시퍼도 원본을 보고서야 흰자가 핵심인 걸 알았다.


## 2026-08-29 · 얼굴 시안 v04 — 실물을 보고 방향이 틀렸던 걸 알았다

**요청**(창업자, 인스타툰 캡처 5건 첨부): 인기 채널 캐릭터의 눈·입 디자인·크기·간격·위치 검토

**관찰 다섯** (yurang 5.8만 · wadadabear 8.8만 · young_forest · free.hada · milletoon)
| | 실물 | 우리 |
|---|---|---|
| 흰자 | **다섯 중 넷이 없다**(까만 점) | 흰자가 기본 |
| 눈 크기 | 얼굴 폭의 5~10% | **15% — 두 배** |
| 눈 간격 | 얼굴 폭의 40~50% | **27% — 훨씬 좁다** |
| 눈썹 | **다섯 중 셋이 쓴다** | **아예 없다** |
| 눈 높이 | 얼굴의 55~65%(아래쪽) | 45.5% — 높다 |

**캘시퍼와 인스타툰은 다른 계열이다.** 캘시퍼는 흰자가 핵심이고, 인스타툰 주류는 까만 점 + 눈썹이다.
나는 레퍼런스 하나(캘시퍼)를 보고 그 구조를 기본값으로 깔았다.

**눈썹이 빠진 게 연쇄를 만들었다.** 표정의 절반이 눈썹에 있는데 그게 없으니
눈·입만으로 감정을 짜내야 했고, 그 결과 **눈이 커졌다.** 눈썹을 넣으면 눈을 줄일 수 있다.

**한 것**: 점눈(흰자 없음) · 눈썹 4종(일자·치켜·내림·호) · 볼터치를 그리기 함수에 신설하고,
보드에 **두 계열을 나란히** 놓았다 — 지금 / +눈썹 / 인스타툰형(점눈·넓게·아래로) / +눈썹 / +볼터치.
「같은 눈, 눈썹만 바꾼 다섯 칸」도 따로 뒀다.

⚠ 수치는 **육안 추정**이다 — 픽셀로 재지 않았다.
⚠ 여전히 **제안이고 앱에는 안 넣었다.**

**남긴 교훈**: 두 번 연속 같은 실수였다. 캘시퍼도 원본을 보고서야 흰자를 알았고,
인스타툰도 캡처를 보고서야 **흰자가 오히려 소수파**인 걸 알았다.
**레퍼런스 하나로 계열 전체를 추정하면 안 된다.**


## 2026-08-29 · 얼굴 시안 v05 — 표본 9건, 내 결론 둘이 뒤집혔다

**요청**(창업자): 인스타툰 검색 상위 결과 4건 추가 (ppyorong_96 3.8만 · kkunoping.health ·
jobeam_studio 5,283 · deyi_min **22.9만**)

**정정 둘**
| 5건일 때 내가 쓴 것 | 9건 집계 |
|---|---|
| 눈썹이 표정의 절반 — **3/5** | **3/9. 다수파가 아니다** |
| 눈 높이 55~65%(아래쪽) | **45~60% 로 흩어진다. 규칙이 아니다** |

비율을 세는 순간 확신이 붙는다. 5건에서 "다섯 중 셋"이라고 쓴 게 그랬다.

**살아남은 셋**
- 점눈 **7/9** (흰자는 kkunoping 481 · free.hada 163 — 둘 다 하위)
- 볼터치 **5/9** — 눈썹(3/9)보다 흔하다
- **눈이 아주 작다** — 상위 둘(22.9만·3.8만)이 얼굴 폭의 3~4%

**상위 넷**(22.9만·8.8만·5.8만·3.8만): 전부 점눈 · 넷 중 셋 볼터치 · 넷 중 셋 눈썹 없음.
⚠ **상관이지 인과가 아니다.** 표본 9건이고, 눈을 줄인다고 좋아요가 늘지 않는다. 보드에도 그렇게 적었다.

**한 것**: 눈 크기 비교자에 **실물급 4%** 단 추가(기존 최소가 11%였다), 「상위 넷을 따라」 프리셋 신설
(점눈 3% · 간격 47% · 볼터치 · 눈썹 없음).

⚠ 여전히 **제안이고 앱에는 안 넣었다.**


## 2026-08-29 · 얼굴 v06 — 눈만으로 표정, 그리고 앱 적용

**요청**(창업자): "눈썹 없이 눈 만으로 표정을 드러낼수 있을거 같아. 그것도 하나 만들어볼래?
이후에 상위 4개를 앱에 얹어서 내가 보고 평가할게"

**`src/lib/face.js` 신설 — 앱과 보드가 같은 파일**
셰이더에서 두 벌로 갈려 두 번 사고가 났다(v143 베끼기 · v150 생성기 미실행).
얼굴은 처음부터 하나로 둔다. 보드 생성기가 이 파일을 읽어 인라인한다.

**눈만으로 표정 10종**
평온(점) · 기쁨(위로 굽은 호) · 시무룩(바깥이 처짐) · 놀람(커지고 위로) · 졸림(눈꺼풀) ·
굳음(위가 눌린 반달) · 곁눈(점이 한쪽) · 들뜸(점 안 반짝임) · 감음(선) · 울먹(물기 한 방울).
눈썹이 하던 일을 **눈 모양 자체**가 한다 — 9건 집계에서 눈썹은 3/9 로 소수파였다.
오늘 상태 10종(십성)에 하나씩 붙여 **눈도 오늘, 입도 오늘**이 된다(전엔 입만 오늘이었다).

**앱에 얹었다** — `?skin=holo&face=a|b|c|d`
| | |
|---|---|
| A | 아주 작은 점 (22.9만형) · 볼터치 · 미소 |
| B | 작은 점 (8.8만형) · 볼터치 · 물결 |
| C | 굳은 눈 (5.8만형) · 볼터치 없음 · 일자 |
| D | 반짝임 (3.8만형) · 볼터치 · 구불선 |

**주소에 안 붙이면 얼굴 없는 지금 화면 그대로**라 A/B 가 안전하다.

**얼굴이 오라를 따라다닌다**: 위습은 스프링으로 **뒤따라오므로** 손끝 좌표를 그대로 쓰면 어긋난다.
셰이더의 `u_wisp`·드리프트를 역산해 얹고, 응축·모임에 따라 같이 작아진다(점으로 모이면 사라진다).

**시행착오 셋**
1. 인스타툰 간격(얼굴 폭의 40~50%)을 그대로 옮겼더니 **눈이 코어 밖 헤일로에 떠 있었다.**
   인스타툰의 "얼굴 폭"은 캐릭터 몸통이고 우리 오라는 **코어가 작고 헤일로가 크다.**
2. `cy=0.50`(캔버스 중앙)에 두니 **눈이 코어보다 0.07 아래**였다 —
   오라의 밝은 중심은 정중앙이 아니다(반사광 층 c0 이 위로 치우쳐 있다).
3. 보드에 **같은 이름의 옛 함수**(캘시퍼 흰자 계열)가 있어 `face.js` 를 덮었고,
   시그니처가 달라 **아무것도 안 그려졌다** → `fx` 접두어로 분리.

**남긴 판단**: 눈썹을 뺀 자리를 눈이 메운다. 그러면 **선 두 개로 감정 열 가지**가 나온다 —
요소를 늘리지 않고 어휘만 늘린 셈이다.

---

## [2026-08-29] v168.1 — 문장 감사 5단계: 남은 것 전부

창업자 *"다 진행해"*. 감사 §②의 잔여와 §③의 R3·R4·R5·R7 을 한 판에 넣었다.
**R6 과 부적 괘 표기 둘은 창업자 결정이라 손대지 않았다**(아래 §남긴 것).

### 서신 프롬프트 — 이름값·중복·예시

- **R3 아홉 하늘인데 한 하늘만 쓴다.** 본문 10건 전수에서 별자리·달자리·나크샤트라·촐킨·라이프패스 **0회**.
  `makeSystem()` 이 프로필에 다 실어 보내는데 어느 장도 안 불렀다 →
  2장에 **「여덟 글자 밖의 하늘을 최소 하나 끌어온다」**를 넣고 *기법 이름 대신 값이 말하는 바*로 쓰게 했다.
- **R4 장끼리 겹친다(9/10).** 두 조각이 **같은 프로필·같은 근거를 받고 서로의 출력을 못 본다.**
  → 「겹치지 않기」 절 신설: 1·2장은 **어떤 사람인가**, 3·4·5장은 **언제·누구와·무엇을 걸고**.
  ⚠ **R4 의 뿌리가 R3 이다** — 다섯 장이 같은 우물 하나만 파면 겹치는 게 정상이 된다. 그래서 같이 손댔다.
- **R5 마야 (O) 예시가 은유로 끝난다.** 이 축은 「매 판결 반드시 포함」이라 강제인데
  예시 둘이 *"힘이 실린 날"*·*"발을 붙잡아"* 로 **눈에 보이는 일로 안 끝났다.**
  같은 파일이 스스로 적어 놨다 — ***"모델은 규칙보다 예시를 따른다."*** → 예시 둘을 갈아 끼우고
  **「신화적 어감은 앞 단까지, 뒷 단은 반드시 눈에 보이는 일로」**를 명문화.
  (이 줄은 7/26 도입분이라 **은유 헌장보다 앞선다** — 사유 있는 예외가 아니라 헌장 세울 때 같이 안 고친 잔존물이었다.)
- **R7 곁에 대해 뭘 말해도 되는지가 없었다.** 「언제 꺼낼지」만 있고 「무엇을 말해도 되는지」가 없는데,
  SYS 는 반대로 「회피 금지·조건부 단언」을 요구한다. **상대는 이 앱을 쓴 적도 동의한 적도 없고
  우리가 아는 건 자리 이름 하나뿐**이다 → (X)(O) 짝으로 **유저가 할 행동으로만** 쓰게 못 박았다.

### 화면·엔진 열두 건

일진 병기 · 각인 `block.t`→`.s`(「사람이 얇아」) · **각인 서양 셈 반복**(아홉 중 여섯 이상이 토씨까지 같은 문장 →
`HOUSE_KO[h]` 로 자리마다 다르게, **새 계산 0**) · 「소리는 물야」 조사(같은 파일에 `jong()` 이 이미 있었다) ·
**나디 라벨이 정반대였다**(만점 = 체질이 **다름**인데 「겹치나」 + 막대 캡션은 「길수록 맞는다」) ·
서신 미리보기 첫 문장 「일간」 · **「지표들은 갈라졌지만」 고정 문자열**(만장일치 49.6%) ·
「명식 깊이 보기」 · 「발급됐나」 · 「네 속은 다르다」 사전체 · **에러 원문 노출 2곳**(한글 가드 복사) ·
`EL_TRAIT` 관형절 · 「가야 할 길은」 띄어쓰기 · **「아홉 축 중 아홉 중」 겹말**(22.1%) ·
**앞면 괘에 병기 신설**(`hexPlain` — `TRI_EL` 을 그대로 옮긴 것이라 어긋날 수 없다).

### 검사가 두 번 걸었고, 둘 다 내 변경이 맞았다

- `verdict.mjs` 가 「가야할길은」을 **통째로 단언**하고 있었다 → 띄어쓰기만 바뀌었으므로 단언도 같이.
- `report-check` 가 「네 속은 **다르다**」를 문자열로 물었다 → 검사의 의도는 *겉과 속을 가르는가*이지
  특정 어미가 아니므로 **성질을 물게** 고쳤다(`다르다|달라`).

⚠ **`letterPreview` 시그니처는 안 바꿨다** — `health-check.mjs` 가 `letterPreview(saju, hesit)` 를 문다.
표 상태를 알려면 인자가 필요한데, 대신 **표와 무관하게 참인 문장**으로 바꿔 우회했다.

### 검사

빌드 OK · 판결 35 · 리포트 122 · 개인정보 138 · 궁합 59 · 각인 130 · smoke 35 · ritual 27 ·
곁 37 · 명부 55 · 초대랜딩 51 · 역할 26 · 공유 16 · 공개표면 33 · origin 20 · doc 21 ·
리텐션 26 · track 24 · pricing 12 · 대기연출 10 · 검진 심각 0.

## 2026-08-29 · 얼굴 v07 — 카툰 원근, 두리번거림, E

**요청**(창업자): "이 디자인 기준으로 E 만들어봐 · 눈 크기 11% 눈간격 35% /
눈 높이와 좌우 위치는 시선과 얼굴의 방향을 나타낼수 있을거 같아서 이거는 살아있는 생명체처럼
왔다갔다하면 좋겠어 / 카툰렌더링 보면 2d인데 각도에 따라 원근감도 생기고 그렇잖아 그렇게
표현해볼까? 지금은 그냥 이미지가 냅다 붙은거 같아서 단조로워"

**「냅다 붙은」이 정확한 진단이었다**
눈·입을 **평면에 고정 좌표로** 찍고 있었다. 그러니 오라가 떠다니고 응축해도 얼굴은 스티커다.

**고치는 법은 카툰이 늘 쓰던 것** — 얼굴 요소를 **구 표면에 놓고 각도로 돌린 뒤 정사영**한다.

| yaw | 화면 간격 | 왼눈 크기 | 오른눈 크기 |
|---|---|---|---|
| −0.29 | 0.428 | 0.82 | 1.00 |
| 0 (정면) | **0.447** | 0.96 | 0.96 |
| +0.29 | 0.428 | 1.00 | 0.82 |

셋이 **동시에** 일어난다 — 먼 쪽 눈이 작아지고 · 간격이 좁아지고 · 가로로 눌린다.
**하나만 하면 미끄러진 것으로 보이고, 셋이 같이 가야 「돌았다」로 읽힌다.**

**두리번거림**: 각도가 스스로 왔다갔다(주기를 나눠떨어지지 않게 — 왕복이 아니라 두리번).
**손끝을 누르면 그쪽으로 돌아본다.**

**E 프리셋**: 창업자 지정 눈 11%·간격 35%, 흰자 눈. **동공이 시선을 따라간다** —
점눈은 눈 전체가 움직여야 하지만 흰자는 동공만 굴리면 된다. 시선 표현이 훨씬 싸다.

**시행착오**: 투영값을 그대로 쓰니 **얼굴 전체가 회전 방향으로 밀려나** 오라 가장자리에 붙었다.
구가 도니 앞면도 옮겨가기 때문이다. **카툰의 머리는 제자리에서 돌기만 한다** —
정중앙(u=v=0)의 이동량을 빼서 중심을 고정하고, 회전은 요소 배치에만 남겼다.

**남긴 교훈**: "단조롭다"는 취향 지적이 아니라 **좌표계 지적**이었다.
평면에 찍으면 무엇을 얹어도 스티커다.


## 2026-08-29 · 얼굴 v08 — 눈 위에 선을 얹고 있었다

**요청**(창업자): "A~d 다 틀렸어 진짜 눈썹이 눈에 붙어있는게 아냐. 눈 모양으로 그냥 표정이 읽히는거야"

**정확한 지적이었다.**
시무룩·졸림·굳음을 만들 때 눈 위에 **선을 하나씩 그었다.**
눈에서 떨어진 선은 눈꺼풀이 아니라 **눈썹으로 읽힌다.**
"눈썹 없이 눈만으로"라고 써 놓고 정작 눈썹을 그리고 있었다.

**인스타툰이 하는 건 실루엣을 깎는 것이다**
| 표정 | 방법 |
|---|---|
| 굳음 | 위를 평평하게 자른다 — 째려봄은 **절단면**이다 |
| 졸림 | 아래쪽만 남긴다 — 위를 크게 잘라낸다 |
| 시무룩 | 바깥으로 기울이고 바깥을 **내린다** |
| 화남 | 안쪽으로 기울이고 바깥을 **올린다** |

클리핑으로 타원을 잘라 쓴다. **선을 얹지 않고 도형 하나로 끝낸다.**

**기울기만으로는 시무룩과 화남이 안 갈렸다** — 눈 하나의 **높이까지 어긋나야** 구분된다.
`cut()` 에 세로 오프셋(`dy`)을 더해 바깥을 내리거나 올린다.

**보드의 프리셋 칸이 전부 무표정 점눈이었다** — A~D 가 다 똑같아 보인 직접 원인이다.
프리셋이 정하는 건 **크기·간격·입·볼터치**이고 **눈 모양은 오늘 상태**가 정한다.
칸마다 다른 눈을 넣어 무엇이 다른지 보이게 했다.

**남긴 교훈**: "눈썹 없이"라고 이름 붙여도 **선을 얹으면 그게 눈썹이다.**
이름이 아니라 모양이 읽힌다.

## 2026-08-29 — 얼굴 프리셋 E 제거 · 비교자를 점눈으로 재작성

**요청** — "흰자 있으니까 이상하다 e 제외하자 / 눈 크기, 간격 정도별로 베리해서 보여줘 이것처럼"

**판단 근거**
- 흰자는 **밝은 미색 오라 위에서 배경과 붙는다.** 인스타툰 9건 중 흰자를 쓴 둘은 **흰 배경에 검은 윤곽선**이 있는 그림이었다 — 우리 판은 윤곽선이 없어 그 조건이 아니다.
- `ball` 눈 모양 자체는 남겼다(보드 계열 비교용). 앱 프리셋에서만 뺐고 `App.jsx` 의 face 파라미터도 `[abcd]` 로 좁혔다.

**같이 고친 것 — 비교자가 앱과 다른 그림이었다**
- 기존 비교자는 옛 `tuned()`(흰자) 경로로 그려져 있었다. **창업자가 방금 "이상하다"고 한 바로 그 그림으로 값을 고르고 있었던 셈**이다. 전부 `face.js` 의 `drawFace` 로 옮겼다 — 이제 보드와 앱이 같은 함수를 쓴다.
- 축을 넷으로: 눈 크기(3~11%) · 간격(12~35%) · 높이(42~58%) · 입 크기와 눈–입 거리.
- **크기 × 간격 매트릭스**를 더했다. 한 축씩 보면 각각 괜찮은데 조합에서 무너지는 자리가 있다(큰 눈+좁은 간격=몰림, 작은 눈+넓은 간격=흩어짐) — 축 하나씩만 보면 그게 안 보인다.

**사고와 대응 — 백틱 3회차**
- 생성기 주석에 백틱을 써서 `build-face-mock.mjs` 가 파싱 단계에서 죽었다. 8/27 CSS, 8/28 셰이더에 이어 **세 번째 같은 사고**.
- 그리고 그 수습 중 비교자 섹션을 통째로 지우면서 **`눈만으로 표정`·`카툰 원근`·`앱 프리셋` 세 섹션의 HTML 까지 같이 날렸다**(채우는 코드만 남아 `ReferenceError`). 복원했다.
- `css-backtick-check` 에 **`node --check` 로 생성기 문법을 직접 묻는 검사**를 더했다. 상수 이름을 외우는 방식은 새 상수가 생기면 또 샌다 — 파싱 여부를 묻는 쪽이 앞으로 생길 것까지 잡는다. 백틱을 일부러 넣어 FAIL 이 나는 것까지 확인했다.

**검사** — 백틱 3/3 · 보드동기화 5/5 · 홀로 24/24 · smoke 35/35 · 개인정보 139/139
---

## [2026-08-29] v169 — 세운 문언 정정 · 각주 예외 확정

창업자 물음: *"1-a,b 에 대해서 내가 지시한 이유가 뭐야?"* → **이력을 파서 답했고, 그 과정에서 내 질문 자체가 틀렸다는 걸 알았다.**

### 두 지시의 출처는 같은 커밋이다 — `c9d0cf0` (v101, 2026-07-30)

실제 철학관 리딩을 분해해 **"밀도의 원천은 문장력이 아니라 지표 개수"**라는 결론을 낸 판이다.
그 판이 십성·신살·충/원진·택일·세운·직업을 한꺼번에 넣었고, **두 지시가 거기서 같이 태어났다.**

- **A 「판결의 시계로 쓰지 말 것」** — 커밋이 금지선을 명시한다: *"verdict·votes·tallyVotes·콜1 스키마·축 목록 불변(**축 신설 없음**)."*
  세운은 **리포트 밀도**를 위해 들어온 값이고, 그게 판결의 타이밍 축으로 승격되는 걸 막는 태그다.
- **B 「그 실제 값을 우선 인용한다」** — 같은 커밋의 배선 목록에 *"콜2 사주 축 인용 지시"* 로 있다.
  프로필에 새 값을 잔뜩 넣었는데 **모델이 그걸 안 쓰고 일반론을 쓰면** 넣은 의미가 없다. 그걸 막는 배선이다.

### 그래서 이건 결정 사항이 아니었다

**두 줄은 서로 다른 콜을 겨눈다.** A는 `system` 에 실려 **콜1(판결·votes)** 에 걸리고,
B는 **콜2(`explainMsg`)** 의 유저 메시지다. 그리고 콜2는 *"이 판결을 절대 뒤집지 말고, 이 결론의 근거만"* 이라
**방향을 못 바꾼다.** 근거에 세운을 인용해도 판결의 시계가 되지 않는다.

**틀린 건 낱말 하나 — 「전용」.** 콜2의 근거는 리포트가 아닌데 「리포트 배경 **전용**」이라 적혀 있어
그 문장이 스스로 거짓이 됐다. 문언만 고쳤다.
⚠ **미확인**: 세운이 실제 근거에 얼마나 인용되는지는 안 쟀다(감사도 안 쟀다).

### 각주 예외 (창업자 결정)

감사 §⑦-7 이 각주의 무병기 용어를 지적했는데, `imprint-check` ④는 **정반대로 각주에 기법 이름을 요구**한다.
결함이 아니라 규칙 충돌이었고 창업자가 *"우선 각주는 예외로 하자"* 로 정했다.
**각주는 유저용 설명이 아니라 검산용 출처 표시**이므로, 기법 이름이 빠지면 값의 출처를 못 되짚는다.
사유를 검사 주석과 감사 문서에 못 박았다 — 다음 세션이 「고치지」 않게.

### 검사

빌드 OK · 판결 35 · smoke 35 · 리포트 123 · 각인 130 · 개인정보 139 · 궁합 59 · ritual 27 · 곁 37 · 검진 심각 0.

## 2026-08-29 — 수호신 깜빡임의 원인 · 얼굴 값 적용과 움직임

**요청** — "눈크기 3%, 간격 26% / 지금 수호신 깜빡이는 느낌이 드는데 검토부탁 / 얼굴은 계속 움직임이 있어야함 3d렌더링 필요"

### 1. 깜빡임 — 착시가 아니라 코드에 있었다

`FIELD_FRAG` 의 플리커 항이 **`floor(t * 11.0)` 계단**이었다. 값을 재현해 보니:

- 정상 구간 `fl` 0.64~1.00, 그런데 dropout 10% 구간에서 **0.27 까지 떨어진다**
- 그게 초당 **1.1회**, 지속 90ms
- 단계 사이가 이어지지 않아 **한 프레임 만에 0.73 점프**
- 그리고 이 값이 `gl_FragColor.a` 에 통째로 곱해진다 → **국소가 아니라 물체 전체가 명멸**

스펙의 의도는 "지지직 — 전구가 파르르"였는데, 구현이 **전구 깜빡임 그 자체**가 됐다. 불꽃은 깜빡이지 않고 흔들린다.

셋을 고쳤다 — ①단계 사이 보간(smoothstep) ②급락 바닥 0.34 → 0.72 ③`up` 가중으로 **밑동은 안 흔들린다**(물체 전체가 같이 어두워지면 그건 흔들림이 아니라 깜빡임으로 읽힌다). 실측 프레임 간 최대 점프 **0.73 → 0.090**. 감정 신호(mood별 flicker 가중 0~0.66)는 그대로 살아 있다.

### 2. 얼굴 값 — 보드에서 고른 그대로

`eyeSz 0.015 · gap 0.130`(보드 「점 지름」의 절반). **넷이 같은 값**이 됐다 — 프리셋이 가르는 건 이제 눈 모양·입·홍조뿐이다.

### 3. "계속 움직임이 있어야 함 · 3d렌더링"

원근 자체는 있었다. 안 보인 이유는 **진폭**이다 — yaw 0.20rad 에 눈이 3% 로 작아지니 화면에서 몇 px 못 움직인다. 넷을 더했다.

- 두리번 진폭 2배(yaw 0.40+0.17, pitch 0.22+0.10)
- **고개 기울임(roll)** 신설 — yaw·pitch 만이면 좌우·상하 왕복이라 기계로 보인다
- **눈 깜빡임** — 1.4~4초 간격, 150ms, 22% 확률로 두 번 연달아. 눈 종류를 갈아끼우지 않고 **세로로 눌러서** 감으므로 어떤 눈 모양에도 붙는다
- 홍조를 같은 구면에 투영 — 얼굴이 도는데 홍조만 제자리에 남으면 붙여 놓은 표가 난다

실측(383px 캔버스, 120프레임): 두 눈 사이 가로거리가 **평균 95px 에서 24px(25%) 변동** — 원근이 실제로 먹고 있다. 고개 기울임으로 두 눈 높이차 20px 변동. 깜빡일 때 잉크 픽셀이 0.23배까지 줄어든다.

**검사** — 백틱 3/3 · 보드동기화 5/5 · 홀로 24/24 · smoke 35/35 · 곁 37/37 · 개인정보 139/139

## 2026-08-29 — 얼굴을 코어에 붙이고, 인터랙션을 안/밖으로 갈랐다

**요청** — "얼굴이 코어쪽에 붙어야하지 않을까? / 얼굴 안쪽에서 터치하면 누가 꾹 누른듯이 얼굴이 한쪽으로 밀리며 귀엽게 괴로워 하고 얼굴 바깥에서 누르면 몸이 약간 튕겨져 나가는 느낌으로 밀려나가"

### 1. 얼굴이 코어에서 떠 있었다 — 맞는 지적이었다

실측(383px 캔버스, 눈 중점 vs 알파² 가중 블롭 무게중심): **y 26px 아래, x 28px 옆.**

원인은 둘이었다. ①얼굴이 드리프트와 위습만 따라가고 `uv.y += u_sink*0.06` 을 안 봤다 ②코어가 캔버스 정중앙이 아닌 걸 `cy: 0.433` 이라는 **상수로 때우고 있었다** — 코어는 R·응축에 따라 움직이므로 상수로는 못 따라간다.

이제 `App.jsx` 가 셰이더와 **같은 식으로** 코어 좌표를 매 프레임 푼다(`core`). 프리셋 `cy` 는 0.500 으로 되돌렸다 — 그건 이제 "캔버스 안에서의 높이"가 아니라 **코어 안에서의 눈 높이**다. 잔차 **y 4.3px · x 6.0px**.

⚠ **중간에 한 번 틀렸다.** 코어를 반사광 층 `c0` 의 어긋남으로 잡았더니 얼굴이 더 내려갔다. `c0` 는 다섯 층 중 하나일 뿐이고, 눈이 앉아야 할 자리는 **밝기 전체의 무게중심**이다. 그 중심은 불꽃이 위로 뻗는 만큼 정중앙보다 위에 있고, 응축하면 덜 올라간다 — 그래서 들어올림을 R 과 fold 에 비례시켰다.

### 2. 인터랙션 — 어디를 눌러도 모이던 것을 둘로 갈랐다

얼굴이 생기기 전엔 「손끝으로 모임」 하나였다. 얼굴이 생긴 뒤로는 그게 안 맞는다 — 얼굴은 **누르는 대상**이고 몸통 밖은 **밀어내는** 자리다.

- **안쪽**(중심에서 0.20 이내): 손끝 **반대쪽으로** 밀린다(말랑한 걸 누르면 그렇게 된다). 고개도 밀리는 쪽으로 돌고, 눈은 질끈 감기고(`closed`), 입은 흔들린다(`wave`). 오라는 `u_squash` 로 눌린다.
- **바깥쪽**: 스프링 **목표를 옮기지 않고 속도만 준다.** 목표를 옮기면 밀려난 자리에 머무르지만, 속도만 주면 튕겼다 제자리로 돌아온다.

실측 — 왼쪽 눈가를 누르면 얼굴이 오른쪽으로 32.5px 이동, 잉크량 0.76배(눈 감김). 오른쪽 바깥을 누르면 몸통이 왼쪽으로 21.6px 밀렸다 돌아온다.

### 3. 검사가 옛 동작을 못 박고 있었다

홀로 ⑫ 「폰 드래그를 스크롤에 안 뺏긴다」가 **위습이 손끝을 끝까지 따라왔는가**로 재고 있었다. 인터랙션이 바뀌자 곧바로 FAIL — 하지만 이건 회귀가 아니다. **검사가 고쳐진 버그가 아니라 그때의 동작을 못 박고 있었던 것**이다. 원인을 직접 묻도록 다시 썼다: ⑫-a 캔버스 `touch-action: none`(이게 그 버그의 정체다) · ⑫-b 드래그가 장을 실제로 움직인다(방향은 설계에 맡기고 크기만).

**검사** — 백틱 3/3 · 보드동기화 5/5 · 홀로 25/25 · smoke 35/35 · 곁 37/37 · 개인정보 139/139

### 덧 — 병합 뒤 목차 검사가 하나를 더 잡았다 (v171)

다른 세션 판과 합치자 「마지막 칩이 닫기(✕) 밑에 안 깔린다」가 FAIL 났다. **첫 판에 통과한 게
우연이었다** — 검사가 **마지막 칩만** 보고 있었는데, 칩은 스크롤하며 지나가므로 **어느 칩이든**
✕ 밑에 들어갈 수 있다. 스크롤 위치가 조금 달라지자 가운데 칩이 걸렸다.

고치는 데 두 번 헛짚었다:
- `padding-right` — 가로 스크롤 컨테이너에서 **끝쪽 패딩은 스크롤 영역에 안 잡힌다.**
- 끝에 빈 자리(`::after`) — 자리는 생기지만 **칩이 그 앞을 지나가는 건 그대로**다.

**둘 다 「끝」만 막는데 문제는 「지나감」이었다.** 그래서 **띠와 스크롤 영역을 갈랐다** —
`.docnav`(배경, 화면 끝까지) + `.docstrip`(실제 스크롤, ✕ 앞에서 멎음). 이제 칩이 지나갈 수 있는
**폭 자체**가 ✕ 앞에서 끝난다.

⚠ **그러고도 검사가 계속 FAIL 이었는데, 이번엔 검사가 틀렸다.** `getBoundingClientRect()` 는
**잘리기 전 상자**를 준다 — 칩이 `overflow` 로 잘려 안 보여도 좌표는 그대로다.
실물 스샷으로 「잘려서 안 보인다」를 확인하고, 검사를 **보이는 부분(스크롤 영역과의 교집합)**만
재도록 고쳤다. 음성 확인 통과(띠를 화면 끝까지 늘리면 FAIL).

**세 번째로 배운 같은 것**: 화면이 맞는지 검사가 맞는지 헷갈리면 **스샷을 먼저 본다.**

## 2026-08-30 — 축을 하나로 · 밀리는 인터랙션 · 아기 볼

**요청** — "얼굴이랑 몸이랑 구심축이 다른 느낌이야 따로 놀아 / 몸 바깥에서 터치하는게 잘 안먹혀 파란색 전체 블럭이 잡히네..? / 터치한채로 드래그하면 드래그하는대로 밀리는 느낌이 나야지 / 아래호 모양은 그다지 안고통스러워 보여 / 얼굴 터치하면 아기 얼굴 누른거처럼"

### 1. 축이 둘이었다

얼굴의 두리번거림이 **자기만의 사인파**였다. 몸은 드리프트로 떠다니고 고개는 고개대로 돌았으니, 위치가 같아도 축은 둘이다. 사인파의 위상을 아무리 맞춰도 이건 안 맞는다.

**고개가 몸이 가는 쪽을 보게** 했다 — `yaw = look.x*5.2`, `pitch = look.y*4.0`, `roll = -look.x*1.6`(도는 쪽으로 기욺). `look` 은 코어 속도를 0.22초 저역통과한 값이다(날것은 고개가 떤다). 사인파는 0.075/0.045/0.022 만 남겼다 — 완전히 빼면 몸이 멈출 때 얼굴도 굳는다.

### 2. 파란 블록 — touch-action 은 선택을 막지 않는다

`touchAction:"none"` 은 **스크롤** 채널이고 **선택**은 다른 채널이다. 누르는 대상에 선택·길게눌러 메뉴·탭 하이라이트가 붙을 이유가 없으므로 `userSelect`·`WebkitTouchCallout`·`WebkitTapHighlightColor` 까지 끄고 `preventDefault()` 를 걸었다.

### 3. "잘 안 먹힌다" 의 정체는 **한 방이라서**였다

바깥 터치가 임펄스 한 번이었다. 0.5초면 스프링이 되돌려 놓으니 **누른 줄도 모른다.** 누르고 있는 동안 매 프레임 밀어내도록 바꿨다(가까울수록 세게 — 먼 데서도 같은 힘이면 순간이동이다). 실측: 바깥에서 잡고 왼→오로 끌면 몸통이 **99.7px 따라 밀린다**(예전 21.6px 튕김).

### 4. 아픈 눈 — ⌣ 는 흐뭇하게 감은 눈이다

`><` 로 바꿨다(`wince`). 그리고 **두 번 틀렸다**:
- 3% 눈 크기 그대로 그렸더니 6px 안에 획이 겹쳐 뭉갰다 → 아픈 눈만 1.9배(홍조까지 커지지 않게 `eyeScale` 를 따로 뺐다)
- 그래도 **X 자국**이 나왔다 → **버그였다.** `drawFace` 는 눈을 하나씩 따로 투영해 `gap 0` 으로 `drawEyes` 를 부르는데, `drawEyes` 는 그 자리에 **좌우 눈을 겹쳐** 그렸다. 점처럼 좌우대칭인 눈은 티가 안 났지만 시무룩·화남·질끈처럼 **비대칭인 눈은 제 거울상과 겹쳐** 있었다. `only` 인자로 한쪽만 그리게 고쳤다.

### 5. 아기 볼

밀리기만 하면 「미끄러진」 것이지 「눌린」 게 아니다. 누른 축으로 30% 눌리고 직각으로 20% 부푼다 — 부피가 보존되는 것처럼 보여야 살로 읽힌다. 누를 때 고개 돌아가는 양도 0.55 → 0.26 으로 줄였다(30° 넘게 돌아 표정이 안 읽혔다).

### 6. 홀로 ⑩ — 회귀인 줄 알았는데 지표가 노이즈를 재고 있었다

⑩(곁이 둥글다)이 FAIL 이 나서 오늘 작업을 의심했는데, **오늘 작업 이전 커밋에서도 0.163 이 나왔다.** 원인은 지표였다 — 광선마다 알파가 **처음** 90 아래로 떨어지는 점을 반경으로 삼았는데, 셰이더가 픽셀 단위 그레인을 뿌리므로 가장자리에서 **난수 한 픽셀이 광선을 일찍 끊는다.** 통과·실패가 운에 좌우됐다.

고친 것 — ①3픽셀 평균으로 문턱 판정 ②마지막 교차를 반경으로 ③5프레임 평균. 그리고 **지표를 먼저 검증한다**(⑩-0): 합성 원과 둥근 삼각형을 넣어 원 0.001 / 삼각 0.278 이 나오는지 본다. 이게 없으면 "낮게 나왔다"가 둥글다는 뜻인지 지표가 죽었다는 뜻인지 모른다.

그렇게 재니 곁의 실제 값이 **0.096** — 노이즈가 아니라 진짜 잔량이었다. 범인은 뭉게 변조의 접힘 계수 `1.0 - fold*0.92` 에 남은 **8%**. 헌장은 "곁에서는 형태 변조를 접는다"인데 코드가 92%만 지키고 있었다. 1.0 으로 끝까지 접으니 **0.001**.

**검사** — 백틱 3/3 · 보드동기화 5/5 · 홀로 26/26 · smoke 35/35 · 곁 37/37 · 개인정보 139/139

---

## 2026-08-30 · v172 — 그 자리를 수호신이 말하는 자리로 (홀로 전용)

**창업자**: *"「다시 왔네 강석우, 기다렸어」 이 부분을 수호신이 말하는 공간으로 두자.
처음엔 웰커밍 멘트를 날리고, 인터랙션하는 거에 따라 대사가 바뀌는 걸로."*

### 세 가지를 먼저 정하고 시작했다

- **모델을 안 부른다.** 같은 날 창업자가 원가를 물었고, 지금 API 를 태우는 건 판결·서신 둘뿐이다.
  이 줄 하나로 판결마다 돈이 붙으면 안 된다 → **표에서 고른다.**
- **전부 「반응」이다. 먼저 말을 걸지 않는다.** 설계 헌장 §모를 권리 — 판결 국면에서 시스템이
  먼저 펼치는 push 는 금지다. 그래서 **혼잣말 타이머를 안 만들었다.** 가만히 두면 환영 한 줄에서
  안 움직인다(검사가 그걸 문다). 환영 한 줄만 예외이고 그건 원래 있던 줄이다.
- **새 캐릭터가 아니다.** 말하는 건 이미 있는 수호신이고, **목소리가 그 사람 오행에서 나온다** —
  같은 손짓에도 화(火)와 수(水)가 다르게 답한다. 앱이 이미 *"나는 '…'의 기운을 두른 존재야"* 라고
  말하고 있으므로, 목소리가 거기서 안 나오면 그건 딴 사람이다. 헌장 §페르소나를 안 건드린다.

### 무엇에 답하나 (오행 5 × 상황 5 = 25줄)

| | |
|---|---|
| 열자마자 | 「다시 왔네, ○○. 기다렸어.」 (기존 줄 그대로) |
| 한 번 두드림 | 火 *"앗, 뜨거워. …아니 내가 뜨거운 거지."* / 水 *"…파문이 생겼어."* |
| 두 번째 | 金 *"또? 벼려지려면 두드려야 하긴 하지."* |
| 세 번 이상 | 木 *"알았어 알았어, 자랄게. 뭐가 그렇게 급해?"* |
| 곁을 보고 돌아옴 | 土 *"곁을 보고 왔구나. 사람은 땅처럼 쌓이는 거야."* |
| 깨움(더블탭) | 金 *"말해. 잘라 줄 건 잘라 줄게."* — **한 마디만 하고 물러난다** |

### 실물로 잡은 것 셋 — 셋 다 「써 놓고 화면에 못 나오는 대사」

1. **곁 탭 대사가 영영 안 보였다.** 곁 탭에는 이 자리가 없다. → 곁에 대한 반응은
   **돌아왔을 때** 판결 탭에서 한다. **안 보이는 말은 안 만든다.**
2. **깨움 대사도 안 보였다.** 깨우면 인사 블록이 통째로 사라진다. → 깨어난 패널로 자리를 이었다.
3. **그런데 거기가 v55 가 「수호신이 물러난 순수 질문입력 구간」으로 정해 둔 자리였다**
   (`asking` 은 「타이핑 중」이 아니라 **깨어난 상태 그 자체**다 — 그걸 몰라서 `!asking` 을 걸었고
   대사가 또 안 나왔다). 그 결정을 깨지 않는다: **물러나면서 한 마디만 하고 2.6초 뒤 스스로 사라진다.**
   그 뒤엔 질문 칸만 남는다 — 유저가 말할 차례다.

⚠ `touchRef.taps` 는 방문 내내 쌓이는 계측값이라 그대로 쓰면 **두 번째 방문부터 늘 「그만 만져」**다.
말용 카운터를 따로 뒀다.

### 검사 — `e2e/gtalk-check.mjs` 20항목 신설

- 표: 오행 다섯 × 상황 전부 있는가 · **상황마다 오행 다섯이 서로 다른가**(같으면 목소리가
  그 사람 것이 아니라 앱 것이다) · 모델을 안 부르는가
- 화면: 열자마자 환영 · **가만히 두면 안 바뀌는가**(push 금지) · 손짓마다 바뀌는가 ·
  깨우면 한 마디 하고 물러나는가 · **홀로가 아니면 한 줄도 안 바뀌는가**
- **양방향**: 표에 있는데 안 쓰는 대사 없음 / 코드가 부르는 열쇠가 표에 다 있음

⚠ **검사를 두 번 고쳤다.**
① `say("…")` 리터럴만 훑어서 삼항으로 넘기는 touch1·2·N 을 못 봤다(실제로는 잘 돌고 있었다).
② **열쇠 목록을 손으로 적어 둬서**, 음성 확인으로 안 쓰이는 대사를 넣었는데 **안 걸렸다.**
표에서 읽게 고치니 걸린다. 음성 확인 2방향 통과.

### 실측

검진 심각 0 · 수호신 말하기 21/21(신설) · 문서 목차 11/11 · 동전 35/35 · 오늘의 상태 17/17 ·
smoke 35/35 · 판결 35/35 · 곁탭 37/37 · 리포트 123/123 · 개인정보 139/139 · 각인 130/130 ·
궁합 59/59 · 초대 54/54 · 랜딩 51/51 · 곁명부 55/55 · 곁예산 13/13 · 결제표식 12/12 ·
보드동기화 5/5 · 백틱 3/3 · 문서 21/21.
## 2026-08-30 — 공유 카드에 수호신이 안 실리던 것

**요청** — "고쳐서 메인에 올려"(홀로 색장 캔버스에 `data-renderer` 가 없어 카드의 수호신 초상이 빈다는 앞선 보고에 대해)

### 무엇이 문제였나

공유 카드 셋(부적·각인·궁합)은 **살아 있는 수호신 캔버스를 한 장 떠서** 포스터에 얹는다(`buildBujeokPoster` 안의 `ctx.drawImage(guardian, …)`). 그 캔버스를 찾는 게 `grabGuardianFrame()` 이고 검색식이 `canvas[data-renderer]` 다.

**색장 렌더러(`GuardianField`)에만 그 표식이 없었다.** 다른 렌더러 셋(2d·gl·sim)에는 다 붙어 있다 — 렌더러를 새로 만들면서 이 약속을 안 지킨 것이다. 결과: 홀로에서 카드를 만들면 **수호신 자리가 통째로 빈다.** 화면에서는 멀쩡히 보이니 눈으로는 안 잡힌다.

그리고 표식만 붙여도 절반이다 — **얼굴은 별도 2D 캔버스**다. 화면에서 둘이 겹쳐 보이는 건 CSS 가 겹쳐 준 것이지 한 장이 아니다. `grabGuardianFrame()` 이 같은 크기의 얼굴 층을 찾으면 오프스크린에서 합쳐 **한 장으로** 돌려주게 했다.

### 검사 — 두 번 고쳤다

⑬ 을 새로 붙였는데 **처음 판은 가짜였다.** 검사가 `canvas[data-renderer]` 검색과 합성을 **직접 베껴서** 재고 있었다. 앱에서 합성을 빼도 그대로 통과했다 — **검사의 복사본을 검사하고 있었던 것**이다. 앱의 `grabGuardianFrame` 을 `window.__BINARI_GRAB` 으로 내놓고 검사가 **그 함수를 직접 부르도록** 바꿨다(선례: `__BINARI_ORB`·`__BINARI_R`). 표식을 떼면 ⑬-a 가, 합성을 빼면 ⑬-b 가 실제로 문다 — 둘 다 확인했다.

⑬-b 는 또 **한 번도 실행되지 않는 공허한 검사**이기도 했다. 얼굴은 `?face=` 로만 뜨는데 걸음이 그걸 안 켰다. 온보딩을 마친 페이지 그대로 얼굴을 켜고 다시 들어가게 했다.

그 김에 ⑫ 도 하나 고쳤다. 손끝 x 를 가운데로 두니 시작점이 **얼굴 반경 경계(0.20)에 정확히 걸려** 안/밖 판정이 갈렸고, 「안」으로 떨어지면 몸통이 안 움직여 검사가 0.000 이 됐다. 이 검사가 묻는 건 「제스처를 브라우저에 뺏기지 않는가」지 안/밖 판정이 아니므로 **명백한 바깥**을 잡게 했다.

**검사** — 백틱 3/3 · 보드동기화 5/5 · 홀로 28/28 · smoke 35/35 · 곁 37/37 · 개인정보 139/139

## 2026-08-30 — 홀로 가독성 정리 (본선이 아니어도)

**요청** — "아직은 본선아냐. 하지만 본선 아니더라도 최적화는 되어야지"

### 먼저 — 감사의 숫자를 그대로 쓰지 않고 다시 쟀다

가독성 감사는 다른 세션이 `00a2c82` 기준으로 썼고 그 뒤로 리포가 여러 판 움직였다. 그래서 **지금 코드에서 직접 재는 도구**(`e2e/contrast-check.mjs`)를 먼저 만들었다. 결과: 칩 2.04 · 고른 칩 1.22 · 처리방침 링크 1.64 — **감사의 값이 소수점까지 재현됐다.** 감사가 맞았다.

### 무엇을 고쳤나

**① 어두운 문서로 샌 것.** 홀로 규칙이 `.stage.holo X` 후손 선택자라 어두운 문서 안까지 내려간다. `.imptitle` 토큰 하나를 빼서 각인·궁합 문서 제목 셋이 살아났고, `.gathering`·`.msrh`·`.docnav`/`.docchip` 은 오적용률이 100% 라 줄을 지웠다. `.ainote` 는 **걷으면 안 되는 자리**였다 — 어두운 문서에서는 홀로가 오히려 개선한 값이라, 밝은 판만 내리고 문서용을 따로 세웠다.

**② 밝은 판 글자 40여 줄.** 우선순위는 감사가 잡은 대로 — 온보딩 칩(고른 칩 1.22가 안 고른 칩 2.04보다 **안 보여 화면이 거꾸로 말하고 있었다**) → 검증 오류 → 처리방침 링크 → 로비 노크 → 나머지.

근본 원인은 **「흐림」의 번역이 뒤집힌 것**이다. 어두운 판의 흐림은 「더 어둡게」인데 홀로가 그 자리를 **중간 회색**으로 옮겼다. 밝은 판에서 중간 회색은 흐린 게 아니라 안 보이는 것이고, 그래서 두 곳은 오버라이드를 건 쪽이 안 건 쪽보다 나빴다.

**실측 결과 — 홀로가 만든 대비 실패 18건 → 0건**(442요소 측정).

### 검사 — 세 번 고쳤다

1. **로비만 걸으면 못 잡는다.** 가장 큰 실패(문서 제목 1.16)가 문서 안에 있었다. 각인 문서까지 걷게 하니 측정이 41 → 442 요소가 됐다.
2. **걸음이 조용히 실패하면 그 화면을 안 재고도 통과한다.** 실제로 한 걸음이 타임아웃으로 빠졌는데 검사는 PASS 였다. 실패를 모아 두었다가 통째로 떨어뜨린다.
3. **홀로만 재면 「원래부터 낮은 것」까지 홀로 탓으로 잡힌다.** 기존 화면에 13건이 있었고 홀로에 남은 2건이 **그 안에 들어 있었다.** 그래서 **두 판을 다 걷고 diff** 한다 — 이 검사가 물어야 하는 건 「대비가 낮은 곳」이 아니라 **「홀로가 나쁘게 만든 곳」** 이다.

되돌려서 무는 것까지 확인했다(칩 규칙 한 줄을 빼니 12건 검출, exit 1).

### 사고 — 백틱 네 번째

CSS 템플릿 안 주석에 백틱을 또 썼다. 화면이 통째로 안 떴다. `css-backtick-check` 가 바로 잡았다 — **검사를 만들어 둔 게 이번에 값을 했다.**

### 남긴 것 — 본선 화면 문제 10건

각인 문서의 아홉 하늘 이름표가 **3.61** 이다(9.5px `<i>`, `rgba(201,143,61,.667)`). 이건 홀로와 무관하게 **원래 그렇다** — 고치려면 본선 화면을 바꾸는 일이라 손대지 않았다. 검사가 「원래부터 낮은 곳」으로 계속 보고한다. **창업자 판단이 필요한 별건.**

**검사** — 백틱 3/3 · 보드동기화 5/5 · 홀로 28/28 · 대비 0건(신설) · smoke 35/35 · 곁 53/53 · 문서 22/22 · 개인정보 139/139

## 2026-08-30 — 얼굴과 몸을 한 물체로 · 뽀잉 키우기 · 얼굴 20% 축소 · 살이 몰리는 눌림

**요청** — "얼굴이랑 몸이랑 어우러지는 게 아니고 동떨어진 느낌. 특히 얼굴을 돌렸을 때. 살아있는 생명체라면 어떻게 생겼을지 생각해봐 / 곁 탭 확대 모션을 좀 더 크게 / 판결·곁 얼굴 영역 20% 축소 / 누르면 살이 가운데에 몰리는 느낌이 없어서 타격감이 덜하다"

### 1. 얼굴과 몸이 따로 논 **구조적** 원인

시선(yaw·pitch·roll)을 **얼굴 블록 안에서** 계산하고 있었다. 그 블록은 셰이더가 이미 그려진 **뒤**에 돈다 — 즉 **몸은 그 값을 볼 수가 없었다.** 얼굴은 구 위에서 돌고 몸은 시간 사인파로만 흔들리니 두 물체로 읽힌 게 당연하다.

시선을 **한 곳에서 풀어** 둘 다에게 준다(`gaze` → `u_look`). 살아 있는 것이 고개를 돌리면 몸이 따라가는 세 가지를 건다 — ①무게가 그쪽으로 쏠린다 ②기울인 쪽으로 몸이 기운다 ③**도는 쪽이 밝아지고 반대쪽이 그늘로 들어간다.** ⚠ 얼굴·팔다리를 그린 게 아니다. 움직이는 건 덩어리와 빛뿐이다(헌장).

실측 고개↔몸 상관계수 **0.91**.

**세 번 틀렸고 셋 다 실측이 잡았다.**
- **속도 추정이 폭발했다.** `(Δcore/dt)` 는 프레임 간격이 한 번만 튀어도 치솟는다 — 고개가 **138°(2.41rad)** 까지 돌았다. 원인은 물리가 아니라 나눗셈이다. 순간값부터 잘라 넣었다.
- **몸만 옮기고 얼굴을 안 옮겼다.** 셰이더가 덩어리를 고개 쪽으로 보냈는데 얼굴은 제자리라 **아까보다 더 따로 놀았다.** 같은 양을 얼굴에도 더했다(단 `look` 계산 **뒤에** — 앞에 두면 자기 이동이 속도로 되먹임된다).
- **음영이 과했다(0.46).** 밝기 무게중심을 기하 이동보다 더 끌고 가서 얼굴이 밝은 앞면이 아니라 **그늘 쪽에** 놓였다. 그리고 `e/R` 을 clamp 로 자른 게 **알파에 직선 경계**를 만들어 곁 구슬이 안 둥글어졌다 — clamp 는 형태를 만든다. 정규화로 바꿔 자를 필요를 없앴다.

### 2. 곁 뽀잉 — 목표만 고치면 아무 일도 안 일어난다

예비동작 목표가 -0.35 였는데 **그 아래 clamp 가 -0.45** 였다. 목표를 아무리 낮춰도 거기서 잘린다. 둘 다 키웠다(목표 -0.85 / clamp -0.72). 반경 1.16 → **1.25배**, 면적 1.57배. 캔버스 가장자리 알파 0 — 안 잘린다.

### 3. 얼굴 20% 축소

`RAD` 와 요소 치수를 **함께** 0.8 배 했다. 하나만 줄이면 눈이 벌어진 각도가 바뀌어 원근이 달라진다.

### 4. 살이 가운데로 몰린다

축으로 눌리기만 하면 「미끄러진」 것이다. **이목구비가 서로 가까워져야** 「눌린」 것이다 — 눈 간격과 눈–입 거리를 같이 줄였다. 화면에 `>ᴗ<` 로 나온다.

### 5. 검사 오검출 2건 — 둘 다 검사가 틀렸다

- **⑩(곁이 둥글다)이 0.14 로 계속 떨어졌다.** 그런데 같은 오브를 따로 재면 **0.002** 였다. 두 측정을 맞대어 원인을 찾았다 — 검사가 **깨우기 전 상태**를 재고 있었다. 그때는 장이 흐려 광선마다 임계 교차가 딴 데서 난다. 창업자가 「곁 탭 안 둥그런데」라고 한 건 깨운 뒤 화면이다. 깨우고 재게 고쳤다 → 0.002.
  ⚠ 이 과정에서 **셰이더를 두 번 헛되이 고쳤다**(접는 계수 조정). 지표를 안 믿고 코드를 고치는 게 아니라 **지표부터 검증했어야** 했다.
- **⑫-b(드래그가 장을 움직인다)가 0.000.** 밀어내기는 손끝 반대 방향이라 **주로 가로**인데 세로만 쟀다. 가로도 재게 했다 → 0.229.

### 6. 사고 — 백틱 다섯 번째

또 CSS/셰이더 주석에 백틱을 썼다. `css-backtick-check` 가 잡았다.

**검사** — 백틱 3/3 · 보드동기화 5/5 · 홀로 28/28 · 대비 0건 · smoke 35/35 · 곁 53/53 · 개인정보 139/139

## 2026-08-30 — 탭 전이: 확대는 빠르게, 축소는 느리게

**요청** — "탭 이동할 때 확대는 빠르게, 축소는 비교적 느리게 그리고 확대 좀 더 크게"

### 1. 비대칭

스프링 하나로는 부는 것과 꺼지는 것이 같은 속도다 — 그건 「뽀잉」이 아니라 그냥 튕김이다. 숨은 들이쉬는 게 빠르고 내쉬는 게 길다. 방향별로 갈랐다(부풀 때 K 220·D 9.0 / 돌아올 때 K 95·D 13.5).

⚠ **감쇠까지 올려야 한다** — K 만 낮추면 느려지는 게 아니라 **더 오래 출렁인다.**

실측 **확대 ~80ms / 축소 ~600ms**(7.5배). 확대 폭은 반경 1.37배·**면적 1.87배**(직전 1.57). 캔버스 가장자리 알파 0 — 안 잘린다.

⚠ 그리고 **또 clamp 였다.** 목표를 -1.25 로 낮췄는데 그 아래 clamp 가 -0.72 라 거기서 잘린다. 지난번에 똑같은 실수를 적어 놓고 또 했다 — 목표와 clamp 는 **항상 같이** 움직여야 한다.

### 2. 확대를 키우니 삼각 불꽃이 돌아왔다

필름으로 확인했다. 부푸는 동안 오라가 **삼각 텐트 모양**이 됐다.

원인: 형태 접힘이 `clamp(u_orb, 0.0, 1.0)` 이었다. 예비동작에서 orb 가 **음수로 크게 내려가면 clamp 가 0** 이 되어 형태가 통째로 펴진다 — 불꽃 혀·뭉게 변조(lobes=3)가 전부 되살아난다. 확대를 키울수록 더 크고 더 오래 보인다.

**크기와 형태를 갈랐다.** 크기는 스프링(`u_orb`)이 흔들어도 되지만 **형태는 목표를 따라야 한다** — 접힘을 별도 값(`u_fold`)으로 받는다. 오버슈트 없는 지수 감쇠(75ms)라 부푸는 동안 이미 둥글다. JS 의 코어 계산도 같은 값을 쓴다.

### 3. 검사가 이 종류를 원리적으로 못 잡고 있었다

⑩ 은 **다 도착한 뒤**만 쟀다. 전이 중 모양은 아무도 안 봤다 — 그래서 삼각형이 200ms 동안 떠 있어도 통과한다. ⑩-b(전이 중 4프레임)를 더했다. 되돌려서 무는 것까지 확인했다(0.005 → **0.174**).

⑫-b 도 또 0.000 이 나왔는데 이번엔 **깨우기 전이라 알파 170 을 넘는 픽셀이 없어 무게중심이 null** 이었다 — 안 움직인 게 아니라 못 잰 것이다. ⑩ 이 같은 이유로 틀렸던 것과 한 종류다. 깨우고 재게 했다(0.343).

### 4. 백틱 여섯 번째

또 셰이더 주석에 썼다. 검사가 잡았다.

**검사** — 백틱 3/3 · 보드동기화 5/5 · 홀로 **29/29** · 대비 0건 · smoke 35/35 · 곁 53/53 · 개인정보 139/139

---

## 2026-08-30 · v178 — 한 링크에 여럿이 답한다 (단톡방)

**창업자**: *"링크가 단톡방에 가면 여러 사람이 참여할 텐데 지금은 한 명만 가능한 것 같아."*
판단 근거·기각안·남은 위험은 `전략로그.md` 2026-08-30. 여기는 **코드가 무엇을 했는가**만.

### 서버

- `answered`(한 칸) → `answers`(목록, 상한 24). ⚠ **옛 판이 저장한 한 칸도 읽는다**
  (`answersOf`) — 이미 살아 있는 초대가 30일 남아 있어서, 안 받아 주면 그 사람들 답이 조용히 사라진다.
- 엿보기: 답이 와도 **계속 열린다.** 닫히는 건 **상한에 닿았을 때뿐**이고, 그때는
  **엿보기부터 닫는다** — 200 을 주면 마지막 사람이 생일을 다 넣고 제출에서야 거절당한다.
- 조회: 동의한 사람만, **자리 번호(i)와 함께** 준다.

### A 기기 — 초대 하나가 자리 하나가 아니다

- 답한 사람 수만큼 자리가 선다. 첫 사람은 부를 때 세워 둔 자리에, 둘째부터는 새 자리에.
- ⚠ **자리 열쇠를 답 번호로 정한다**(`inv:<id>` · `inv:<id>#2` …).
  「빈 자리를 찾아 채우기」로 하면 **앱을 열 때마다 순서가 달라져 사람이 섞인다.**
  번호로 정하면 `gyeotAdd` 가 같은 열쇠를 알아봐서 **다시 열어도 안 늘어난다**(검사가 실측).

### 고지 — 두 곳을 같이 고쳤다

처리방침 §5-2 의 두 문장이 **거짓이 됐다**: 「한 번만 응답되고 만료」·「되짚기가 가능한 사람은
상대방 한 사람뿐」. 둘 다 고쳤고, **곁 화면에도 적었다** — 처리방침은 열어 보는 사람만 읽는다.

### 검사 — 지우지 않고 자리를 옮겼다

「응답 1회」를 무는 항목이 **네 개**였다(초대 ③④⑦ · 랜딩 ④). 넷 다 **정확한 검출**이라
지우는 대신 뒤집었다 — 「여럿이 답하는가 · 앞사람 답이 안 덮이는가 · 상한이 있는가 ·
동의를 끈 사람은 목록에 아예 없는가」. `privacy-check` 도 새 경계 다섯으로 옮겼고,
**화면이 그 말을 하는지**까지 문다(음성 확인: 고지만 지우고 동작을 두면 FAIL).

### ⚠ 내 새 문구가 헌장 규칙에 걸렸고, 규칙을 안 깎았다

*"한 링크에 최대 24명"* 이라고 적었다가 「개수 표기 금지」(곁탭IA §5)에 걸렸다.
그 규칙이 지키는 건 **명부가 카운터가 되지 않는 것**이고 링크 상한은 성격이 다르긴 하지만,
**예외를 파는 대신 문구에서 숫자를 뺐다** — 전할 내용은 숫자 없이 다 전해지고,
정확한 상한은 처리방침이 적는다. **규칙을 깎아 내 문장을 통과시키지 않는다.**

⚠ 반대로 **검사가 틀린 것도 하나 있었다** — 「답한」이라는 **낱말**까지 막고 있어서
*"답한 사람은 네 명식 값을 보게 돼"* 같은 설명 문장이 걸렸다. 막을 건 낱말이 아니라 **수**다.
음성 확인으로 확인했다(화면에 「3명이 답했어」를 넣으면 걸린다).

### 실측

검진 심각 0 · 초대 59/59 · 랜딩 60/60 · 개인정보 144/144 · 말하기 21/21 · 목차 11/11 ·
곁탭 57/57 · 곁명부 55/55 · 동전 35/35 · 오늘의 상태 17/17 · smoke 35/35 · 판결 35/35 ·
리포트 124/124 · 문서 22/22 · 보드동기화 5/5 · 백틱 3/3 · 결제표식 12/12.
브라우저 완주: 한 링크로 셋이 답하고 **각자 제 하늘·제 이름으로** 서고, 다시 열어도 안 늘어난다.
## 2026-08-30 — 「몸과 얼굴의 도형이 다른가?」 — 그렇다, 두 군데가 달랐다

**요청** — "몸과 얼굴의 도형이 다른가? 돌리면 따로 노는 느낌이지? 곁 탭이 그나마 낫지만 얼굴만 돌고 몸은 안 도는 느낌은 동일. 판결탭은 심하고"

**둘 다 맞았고 원인이 둘이다. 그리고 「판결이 더 심하다」까지 숫자로 설명된다.**

### 원인 1 — 얼굴이 앉은 구가 몸과 무관한 상수였다

실측(캔버스 590px):

| | 몸의 코어 반경 | 얼굴이 앉은 구 | 두 눈 반폭 | 눈/코어 |
|---|---|---|---|---|
| 판결 | 69px | **142px** (S*0.24 상수) | 64px | **0.93** |
| 곁 | 44px | **142px** | 45px | 1.02 |

구가 몸보다 2~3배 크니 고개를 돌리면 눈이 **몸 위가 아니라 몸을 가로질러** 미끄러진다. 그리고 눈이 코어 반경의 93% — **몸 가장자리에 얹혀 있었다.** 판결이 더 심한 이유도 여기 있다: 판결은 몸이 크고 불꽃이라 그 어긋남이 더 드러난다.

**몸의 코어를 그대로 구로 쓴다**(`radFrac` = 셰이더의 R/2.35). 눈·입 치수도 캔버스가 아니라 **구 기준**으로 넘긴다 — 캔버스 기준으로 두면 구가 줄 때 눈만 그대로라 또 가장자리로 밀린다. 눈은 구의 **0.56 자리**(사람 얼굴이 대략 그렇다). 탭이 바뀌어 몸이 줄면 얼굴도 같이 준다.

### 원인 2 — 몸을 평행이동시키고 있었다

앞 판에서 「몸이 고개를 따라간다」를 **덩어리째 옮기는 것**으로 구현했다. 그런데 **평행이동은 「돌았다」가 아니라 「미끄러졌다」로 읽힌다.** 도는 물체의 지문은 **겉(실루엣)은 그대로인데 속이 움직이는 것**이다. 겉까지 같이 옮기니 회전 단서가 통째로 지워졌다.

이제 겉은 안 옮기고 **안쪽 두 층(c0·c1 = 머리의 앞면)만** 고개 쪽으로 보낸다.

실측(고개 yaw 와 무게중심의 상관):

| | 고개↔**속** | 고개↔**겉** |
|---|---|---|
| 판결 | **0.76** (속 165px 이동) | 0.13 |
| 곁 | 0.24 | 0.17 |

속은 따라가고 겉은 안 따라간다 — 그게 회전이다.
⚠ **곁은 약하다(0.24). 일부러 접었다** — 응축된 구슬에서 앞면을 크게 밀면 구슬이 안 둥글어진다(실측 3주기 0.061, 반경 42~66px). 곁의 수호신은 「돌아보는 머리」가 아니라 「응축된 구슬」이다.

### 세 번 조정했고 전부 검사가 잡았다

- 앞면 이동량 0.62 → 전이 중 실루엣이 찌그러졌다(⑩-b 0.288)
- 스프링으로 **부푼 R** 을 쓰고 있었다 → 확대 순간 앞면이 1.37배로 튕겨 나갔다. 회전은 크기와 무관하므로 **오버슈트 없는 목표 크기**로 바꿨다
- 그래도 0.34 로 낮춰야 3회 연속 통과했다

### ⑩-b 를 최대값에서 **연속 초과**로

크기가 빠르게 변하는 동안 임계 등고선이 한 프레임씩 튀어 같은 코드에서 0.031 과 0.154 가 갈렸다. 진짜 삼각형은 **여러 프레임 연속으로** 높다(원래 버그는 0.063·0.174·0.089 가 이어졌다). 연속 두 프레임이 넘을 때만 잡는다.

**검사** — 백틱 3/3 · 보드동기화 5/5 · 홀로 29/29 · 대비 0건 · smoke 35/35 · 곁 57/57 · 개인정보 139/139

## 2026-08-30 — 몸에 입체감 · 곁 얼굴 원복 · 심지가 눌림을 따라간다

**요청** — "곁 얼굴 크기 원복 / 몸의 심지(코어, 빨간 부분)가 얼굴의 중심으로 느껴지는데 인터랙션할 때 이 부분은 변동 없이 가운데를 유지하니 더 몸과 얼굴이 따로 가는 느낌 / 몸이 애초에 3d 느낌, 입체적인 느낌이 없어서 그런 듯 / 얼굴을 누르면 약간 측면으로 틀잖아, 그러면 몸도 틀어져야지"

### 1. 몸이 평평했다 — 이게 가장 큰 원인이었다

**맞는 지적이고, 앞의 실패들도 이걸로 설명된다.** 지금까지 몸은 **평평한 발광**이었다. 평면 위에서 얼굴만 돌면 당연히 붙여 놓은 표가 난다 — 회전 단서를 아무리 더해도 얹힐 입체가 없었다.

반구로 보고 램버트 음영을 넣었다. 빛은 왼위앞에서 오고 **고개가 도는 쪽으로 같이 돈다** — 그래서 고개를 돌리면 **명암 경계가 움직인다.** 이게 「몸이 돌았다」의 가장 센 단서다.

**두 번 틀렸다.**
- **코어(R)를 반구 크기로 잡았더니** 헤일로 전체가 포화돼 음영이 **테두리 한 줄**로만 나왔다. 눈에 보이는 덩어리 전체(R×2.05)를 반구로 잡아야 한다.
- **그늘 쪽 덮임(알파)을 올렸다.** 색만으로는 알파가 낮아 밝은 바탕이 비쳐 거의 안 보였기 때문인데 — 그러자 **밝기 무게중심이 그쪽으로 밀려 얼굴이 또 몸에서 벗어났다.** 이 세션에서 같은 함정에 두 번 빠졌다. **알파는 안 건드린다.** 색만 쓰되 그늘을 깊게(0.26) 잡는다.

### 2. 곁 얼굴 크기 원복

곁의 몸은 응축돼 R 이 작으니 코어를 그대로 따르면 얼굴이 너무 작아진다(두 눈 반폭 45 → **20px**). 판결에서는 몸의 코어를 그대로 쓰고, 곁로 갈수록 예전 값(0.24)으로 섞는다 → **54px**.

### 3. 심지가 눌림을 따라간다

얼굴은 밀리는데 심지가 제자리면 **얼굴이 몸에서 떨어져 나간 것**으로 보인다. 얼굴이 가는 만큼 안쪽 층도 같은 방향으로 보낸다. 실측 — 왼쪽을 누르면 얼굴 172.7px · 심지 89.7px, 같은 부호.

### 4. 그리고 시선 추정에 되먹임이 있었다

⑩-b(전이 중 둥근가)가 다시 물었다. 원인: `look`(고개 방향)을 **core 의 속도**로 재는데, core 에는 탭 전환으로 몸이 줄어드는 몫(lift)이 섞여 있다. 곁으로 갈 때 그 변화가 속도로 잡혀 **고개가 홱 돌고 → 앞면이 던져져 → 전이 중 실루엣이 찌그러졌다.** 크기 변화는 고개 방향이 아니다 — 드리프트와 위습만으로 속도를 잰다. 3회 연속 통과 확인.

**검사** — 백틱 3/3 · 보드동기화 5/5 · 홀로 29/29 · 대비 0건 · smoke 35/35 · 곁 57/57 · 개인정보 139/139

## 2026-08-30 — 실기 영상에서 문제 도출

**요청** — 화면 녹화 2건(6.2초·10.9초, iPhone) "문제점 도출해봐 / 곁탭 10% 얼굴 줄이는거 포함"

영상에서 프레임을 뽑아(ffmpeg) 격자로 늘어놓고 봤다. **로컬 테스트에서는 하나도 안 보이던 것들이다** — 영상 속 수호신은 초록 계열의 **가늘고 긴 불꽃형**이고, 내 고정 명식은 넓은 삼각형이라 체형이 다르다.

### ① 몸에서 떨어져 나온 거대한 회색 얼룩 — **내가 어제 넣은 것**

v178 의 반구 음영이 **헤일로까지 어둡게 칠했다.** 헤일로는 옅게 퍼진 빛이라 거기에 그림자를 얹으면 그림자가 아니라 **딴 물체**로 보인다. 영상에서 몸보다 큰 회색 덩어리가 옆에 떠 있다.

→ 음영을 **살이 있는 곳**에만 준다(`dens = 1 - smoothstep(0.34, 0.88, q)`). 고친 뒤 스틸에서 얼룩이 사라졌고 입체감은 남았다.

### ② 눈이 몸 밖으로 나간다

영상 6.2초 프레임에서 **오른눈이 배경 위에 떠 있다.** 몸은 오행마다 가로폭이 다른데(셰이더 `agp.x` — 화 1.26 · 목 1.18 이 가장 좁다) 눈 간격을 **한 값**으로 두고 있었다. 넓은 몸 기준으로 맞춘 값이 좁은 몸에서는 밖으로 나간다.

→ 셰이더와 **같은 표**를 JS 에서도 읽어 얼굴 가로를 오행만큼 좁힌다(`gapR = 0.42 / AGX`).

### ③ 드래그 중 얼굴이 몸에서 떨어진다

영상 후반부에서 얼굴 덩어리와 몸이 완전히 갈라진다. 눌림 이동량 0.052(캔버스의 5.2%, 590px 기준 31px)가 좁은 몸에서는 과하다. 0.032 로 줄였다.

### ④ 곁 얼굴 10% 축소 (요청)

`radFrac` 곁 값 0.24 → 0.216.

### ⚠ 정직하게 — ②③은 **재현 못 했다**

명식 넷을 바꿔 가며 「얼굴 잉크가 몸 밖에 있는 비율」을 쟀는데 **전부 0%** 였고, 눈 간격을 0.95(구의 가장자리)까지 벌려도 0% 였다 — **지표가 안 문다.** 로컬 체형이 영상만큼 가늘지 않아서다. 그러므로 ②③은 **완화이지 검증된 수정이 아니다.** 다음 실기 영상에서 다시 확인해야 한다.

**아직 안 고친 것 — 창업자 확인 필요**
- **얼굴이 몸의 아래쪽 끝에 붙는다.** 불꽃형에서 특히 심하다. 얼굴 높이를 상수(`lift`)로 잡고 있어 체형이 바뀌면 어긋난다. 몸의 밝은 덩어리를 실제로 따라가게 하려면 구조를 바꿔야 한다.
- **주황 심지가 입 자리에 겹쳐 「빨간 코/입」처럼 읽힌다.**

**검사** — 백틱 3/3 · 보드동기화 5/5 · 홀로 29/29 · 대비 0건 · smoke 35/35 · 곁 57/57 · 개인정보 139/139

## 2026-08-30 — 눈 크기·간격·홍조 베리에이션 보드

**요청** — "눈 크기, 간격 베리에이션 보드 만들어봐 / 홍조 있는 거 없는거 있는것도 버전 베리에이션 해봐"

### 앱과 **같은 경로**로 그린다

이 보드는 전에 한 번 틀린 적이 있다 — 비교자가 **보드 전용 경로**로 그려져 앱과 다른 그림이었고, 그 그림으로 값을 골랐다. 이번엔 앱이 실제로 넘기는 값을 그대로 쓴다: `radFrac`·`gapR`·`eyeSz`·`mSz`·`mouthR` — 전부 **구 기준**이다(캔버스 기준이 아니다).

**배경에 몸을 같이 그린다.** 코어를 점선 원으로 표시해서 **눈이 살 안에 있는지** 한눈에 보이게 했다 — 실기 영상에서 나온 「눈이 몸 밖으로」가 바로 이걸 못 보고 있어서 생긴 문제다.

**칸 안에서 얼굴이 너무 작았다**(첫 판). 몸과 얼굴을 **같이** 2배 확대한다 — 비율은 그대로고 보이기만 커진다.

### 무엇이 실렸나

- 눈 크기 6단(0.055~0.165) · 두 눈 간격 6단(0.24~0.68) — 지금 값에 테두리
- 크기 × 간격 4×4 매트릭스 — 한 축씩 보면 안 보이는 조합의 붕괴를 본다
- **홍조 있음/없음 2행** × 눈·입 조합 6종(dot·stern·shine·droop·wince)

### 같이 치운 것

- **옛 비교자 절을 지웠다.** 앱과 다른 경로라 숫자 체계가 달라 **같은 화면에 두면 헷갈린다**(3% vs 0.088 이 같은 것을 가리키는데 단위가 다르다).
- **「지금 값」 표가 폐기된 흰자 설계의 숫자**였다(흰자 지름·동공 비율…). 현행으로 갈아끼우고, 값을 바꿀 때 App.jsx 와 보드를 **같이** 고치라고 못박았다.

**검사** — 백틱 3/3 · 보드동기화 5/5 · 홀로 29/29 · smoke 35/35

## 2026-08-30 — 눈 0.155 / 간격 0.64 · 홍조는 감정일 때만

**요청** — "0.155, 0.64 / 기본은 홍조 제거. 홍조는 감정 표현 시에 노출"

### 값 적용

- `eyeSz` 0.088 → **0.155**
- `gapR` — 보드에서 고른 건 **최종 0.64**다. 코드 상수는 오행 폭으로 나누기 **전** 값이라 기본 오행(금 1.08) 기준으로 되돌려 **0.691** 로 넣었다. 그래야 금에서 정확히 0.64 가 되고 좁은 오행은 그만큼 좁아진다(화 0.55 · 목 0.59).

### 홍조 — 상태를 한 군데서만 정한다

프리셋의 `blush` 플래그를 **더 안 쓴다.** 기본 눈은 `dot`(평온)이고, 그 밖의 눈 모양은 전부 오늘 상태가 고른 것이거나 눌림(`wince`)이라 감정이 실려 있다. 그래서 **「점눈이 아니면 감정」** 으로 가른다. 두 군데서 정하면 어긋난다.

### ⚠ 값을 넣자 곁이 깨졌다 — 그리고 원인은 **보드**였다

적용하고 재니 **눈/코어 비가 곁 1.55**(눈이 몸 밖으로 55% 나감), 판결 0.94. 실기 스크린샷에서 곁의 눈·홍조가 배경 위에 떠 있었다.

원인: 「곁 얼굴 원복」을 **구를 상수 0.216 으로 부풀려서** 했다. 그런데 곁의 몸은 응축돼 실측 코어가 **40px** 인데 그 상수는 **127px 짜리 구**를 만든다. 간격 0.64 를 그 구에 곱하면 눈이 몸을 한참 벗어난다.

→ 원복은 살리되 **몸에 붙인 채로** 키운다: 코어를 따라가되 곁에서 +35%. 곁 RAD ≈ 70px, 눈 ±45px, 구슬 반경 ≈ 72px → 안쪽. 실측 비 **0.57**.

### 보드가 나를 속였다

점선 원을 **구 반지름**에 그려 놓고 「몸의 코어」라고 적어 뒀다. 실측하면 코어는 구의 **0.72배**다. 그 차이를 모른 채 0.64 를 고르면 눈이 코어 경계에 딱 걸린다 — 실제로 판결에서 비 0.89 로 경계선이다.

→ 보드가 **실측 비율로** 두 원을 그린다: 안쪽 점선 = 코어(0.72배), 바깥 점선 = 눈에 보이는 끝(1.30배). 이제 보드에서 0.64 를 보면 눈이 안쪽 원에 걸쳐 있는 게 그대로 보인다.

**검사** — 백틱 3/3 · 보드동기화 5/5 · 홀로 29/29 · 대비 0건 · smoke 35/35 · 곁 57/57 · 개인정보 139/139

## 2026-08-31 — 세 번째 실기 영상 (9.2초)

### 보이는 문제 다섯

프레임을 뽑아 확인했다. 영상의 수호신은 **가늘고 긴 초록 불꽃**이다.

1. **찡그린 눈이 몸보다 넓다.** 획이 불꽃 양옆 배경까지 뻗어 **수염처럼** 보인다. 가장 눈에 띄는 문제.
2. **얼굴이 불꽃의 맨 아래에 붙는다.** 본문 글자와 겹칠 만큼 내려가 있다.
3. **드래그 중 몸이 형태를 잃고 얼굴과 갈라진다.** 초록과 분홍 덩어리가 따로 놀고 얼굴은 왼위 구석에 있다.
4. **주황 코어가 얼굴 자리에 겹쳐 「빨간 코」로 읽힌다.**
5. **극단적으로 돌면 두 눈 크기가 크게 달라진다**(한쪽은 점, 한쪽은 큰 타원).

### 고친 것 둘

**① 얼굴이 몸보다 두 배 내려간다 — 계산이 틀렸다.**
셰이더의 `uv.y += u_sink*0.06` 을 JS 가 **그대로 베꼈다.** 그런데 셰이더는 장 전체를 0.06 내리지만 **캔버스 밖으로 나간 부분이 잘려 눈에 보이는 무게중심은 절반만** 움직인다. 실측(SINK=1): 몸 16px · 얼굴 35px. 얼굴은 「식」이 아니라 **「보이는 몸」** 을 따라가야 한다. 계수 0.06 → **0.028**(실측으로 맞춤).
⚠ 이 값은 mood 마다 다르므로 **어떤 유저에게만 나타난다** — 그래서 내 고정 명식에서는 안 보였다.

**② 도달 잠금 — 값을 줄이는 대신 넘지 못하게 막는다.**
눈 0.155 에 아픔 배율 1.9 가 곱해지고 간격 0.64 가 더해지면 가로 도달이 구를 **넘는다.** 값을 하나씩 줄이면 다음에 또 어떤 조합이 넘는다. `drawFace` 안에서 **도달 자체를 잠갔다** — 눈 바깥 끝이 구의 0.90 을 못 넘는다. 실측: 잠금 없으면 눌림 시 얼굴 가로 110px·몸 밖 잉크 1%, 잠그면 102px·0%.

### 아직 안 고친 셋 — 구조를 건드려야 한다

- **얼굴이 불꽃 아래에 붙는다(②와 별개).** `lift` 가 상수라 체형을 안 따라간다. 불꽃은 위로 뻗는데 얼굴 높이는 고정이다. 셰이더의 형태 식을 JS 에서 다시 풀어 **밝은 덩어리의 높이**를 구해야 한다.
- **드래그 중 몸이 형태를 잃는다.** 밀어내기가 안쪽 층만 밀어서 겉과 속이 찢어진다.
- **주황 코어 = 빨간 코.** 심지를 얼굴 뒤로 낮출지, 얼굴을 심지 위로 올릴지는 디자인 판단.

**검사** — 백틱 3/3 · 보드동기화 5/5 · 홀로 29/29 · 대비 0건 · smoke 35/35 · 곁 57/57 · 개인정보 139/139

## 2026-08-31 — 눈 간격 0.56

**요청** — "0.56 ㄱㄱ"

코드 상수는 오행 폭으로 나누기 **전** 값이라 기본 오행(금 1.08) 기준으로 되돌려 **0.605** 로 넣었다. 금에서 정확히 0.56 이 되고 좁은 오행은 더 좁아진다(화 0.48 · 목 0.51).

**실측** — 눈/코어 비 판결 **0.70** · 곁 **0.68**(0.64 일 때는 0.89 로 경계선이었다). 평상·눌림 모두 **몸 밖 잉크 0%**, 얼굴 가로폭 91 → 눌림 97px.

보드의 「지금」 테두리와 「지금 값」 표도 같이 옮겼다 — 한쪽만 고치면 다음 세션이 옛 값을 보고 고른다.

⚠ **여전히 남은 것**: 크게 돌 때 두 눈 크기 차이가 크다(한쪽은 점, 한쪽은 큰 타원). 눈이 커진 만큼 그 비대칭도 같이 커졌다 — 원근이지만 결함으로 읽힌다. 창업자 판단 필요.

**검사** — 백틱 3/3 · 보드동기화 5/5 · 홀로 29/29 · 대비 0건 · smoke 35/35 · 곁 57/57 · 개인정보 139/139

---

## 2026-08-30 · v179 — 곁에게 묻는다 (탭은 안 나눴다)

**창업자**: *"곁 탭에 질문 채팅칸을 넣으면 어때? 판결탭처럼. 팀원들이 엄청 궁금해해서.
질문 칸 위에 물어보고 싶은 사람을 선택할 수 있는 토글 … 전체를 누르면 다 선택.
아니면 너무 복잡스러우면 탭을 나눠야 할까?"*

### 탭은 안 나눈다 — 근거 넷

1. **곁탭IA 가 「판결/곁 두 탭」으로 가른 문서**다. 뒤집을 근거가 아직 없다(분모 21명).
2. **곁 탭은 이미 「사람이 서는 자리」**다. 사람을 묻는 자리가 거기 아니면 어디인가.
3. **토글은 명부가 이미 화면에 있는 곳에 있어야** 한다. 다른 탭에 두면 명부를 두 번 그린다.
4. 하단 탭이 셋이 되면 각 탭의 뜻이 흐려진다.

**「복잡하다」는 다른 데서 덜었다** — 질문 칸은 **명부를 두 번 두드려 연 사람에게만** 뜬다.
닫힌 곁 탭은 지금처럼 조용하다. 탭을 늘리는 대신 **깊이로 감췄다.**

### 판결 포맷을 안 씌운다

GO/STOP/HOLD 는 「할까 말까」의 답이지 **「누구와」의 답이 아니다.** 여기서 필요한 건 판정이 아니라
**비교**다. 헌장 §판결문 형식 보존도 축·카드를 안 늘리라고 한다.
그래서 답이 **사람마다 한 줄 + 마지막 한 줄**이고, 순위를 매기지 말라고 프롬프트에 못 박았다.

### 원가 — 새 시스템 프롬프트를 안 만들었다

같은 날 창업자가 원가를 물었다. **판결과 같은 SYS 를 쓴다** — 캐시 프리픽스가 같아 read(0.1배)로
걸린다. 새로 만들면 write 가 한 벌 더 생겨 원가가 뛴다. 콜2·콜3 이 쓰는 그 방식이다.
출력 상한도 700 으로 묶었다(관계 답은 길 이유가 없다).
그리고 **재료는 기기가 만든다** — 자리 이름은 `roleOf` 가 이미 답했고, 모델은 말로 옮기기만 한다.

### 이름은 모델에 안 간다

`gyeotPromptLine` 과 **같은 규칙**이다 — 자리표(`곁1`·`곁2`)로 보내고 앱이 이름으로 되돌린다.
상대는 이 앱을 쓴 적도 동의한 적도 없는 제3자이고, 우리가 아는 건 자리 이름 하나뿐이다.

### 실물 스샷이 버그를 하나 잡았다 — **사람이 뒤바뀌었다**

프롬프트는 `곁1..N` 을 **고른 사람들** 위에서 세는데, 화면은 `gyeotSorted`(**전체 명부**)로
되돌리고 있었다. 팀장님·엄마만 골랐는데 **답이 민수 이름으로 나갔다.**
→ 번호를 매긴 그 목록을 답과 함께 들고 다니게 고쳤다(`gqRes.who`).
**검사가 아니라 스샷이 잡았다** — 검사는 그때 아직 없었다. 지금은 음성 확인으로 못 박았다.

### 토글

- 「전체」 + 사람 칩. **한 줄 · 가로 스와이프 · 다중 선택.**
- ⚠ 칩 순서는 **명부와 같은 최근순**이다(§C-3 방어 ①). 다른 순서면 그 줄이 순위가 된다.
- ⚠ **고른 수를 안 센다**(곁탭IA §5). 「2명 선택」이 붙는 순간 그 줄이 카운터가 된다.
- 전체에서 하나를 끄면 **나머지만** 고른 것이 된다(전체를 끄고 다시 고르게 하면 손이 두 번 간다).
- 다 끄면 **전체로 되돌린다** — 빈 물음을 막는다.

### 잡은 것 — 둘 다 내 실수

1. **블록을 「곁이 하나도 없을 때만 뜨는 가지」에 넣었다.** 명부가 열려도 안 보였다 —
   붙일 자리를 문자열로 찾다가 같은 문자열의 **다른 가지**에 걸렸다.
2. **검사가 제 증거를 먹었다.** 프롬프트를 `pop()` 으로 읽어서 「몇 번 불렀나」가 0 이 됐다.

### 실측

검진 심각 0 · 곁에게 묻기 21/21(신설) · 곁탭 57/57 · 말하기 21/21 · 목차 11/11 ·
smoke 35/35 · 판결 35/35 · 랜딩 60/60 · 개인정보 144/144 · 초대 59/59 · 리포트 124/124 ·
곁명부 55/55 · 문서 22/22 · 보드동기화 5/5 · 백틱 3/3.
## 2026-08-31 — 홀로에 곁 위성 · 느린 기기에서 스프링이 터지던 것

**요청** — "사람 추가 됐는데, 위성이 왜 안생기지?" → 원인 확인 후 "(가)로 하되 이질적이지 않게"

### 원인 — 버그가 아니라 **없는 기능**이었다

위성은 **입자 렌더러**에만 있다. 점 수천 개 중 일부를 궤도로 떼어내는 방식이라 **뗄 입자가 없는 색장**으로는 옮겨오지 못했고, 그대로 빠진 채 왔다. `GuardianField` 는 곁 명부를 **받지도 않고** 있었다.
⚠ 그런데 문구는 그대로였다 — 「네가 부른 사람들이 지금 같이 돌고 있어」. **화면이 거짓말을 하고 있었다.**

### 만든 것 — 2D 층에, 재질을 맞춰서

얼굴과 같은 오버레이에 그린다. 자리·방향·층은 **입자판과 같은 규칙**(seat 각도 · 극이면 반대로 · 앞줄이 안쪽).
「이질적이지 않게」를 위해 — 가장자리 없는 빛덩이 + 지나온 자리에 남는 꼬리, 궤도 뒤쪽은 흐려져 **몸 뒤로 지나간다**.

**두 번 안 보였고 둘 다 같은 착각이었다.**
1. 흰쪽으로 많이 섞고 알파를 낮췄다 → 오라에 통째로 묻혔다(그려지고는 있었다 — 오버레이 층만 따로 떠서 확인).
2. 색을 살렸는데도 안 보였다 → **오행색이 어두운 판용 밝은 변종**이었다(목이면 `#a8f0c0` 파스텔). 입자판은 검은 판에 가산 합성이라 그게 빛나지만, **밝은 판에서 「보인다」는 밝은 게 아니라 짙은 것**이다(파란 심지가 잘 보이는 이유가 그것이다). 색을 깊은 쪽으로 뒤집었다.

### 같이 나온 것 — **느린 기기에서 스프링이 발산한다**

컨테이너 재시작 뒤 프레임이 **103ms(10fps)** 로 늘어졌고, 그러자 오라가 눈에 띄게 찌그러졌다(검사 ⑩ 0.002 → 0.126). **내 위성 작업 탓이 아니었다** — 되돌려서 확인했다(HEAD 에서도 동일).

원인 둘을 고쳤다. 둘 다 **느린 폰에서 그대로 일어난다.**
- **프레임 통째로 적분하면 터진다.** K=220 에 dt=0.05 면 이득이 11 이라 발산한다. 1/120초 걸음으로 **나눠 적분**한다 — 몇 fps 든 결과가 같다.
- **속도를 잘린 dt 로 나누고 있었다.** dt 는 0.05 로 자르는데 실제 프레임이 0.10 이면 떠다닌 거리는 0.10 어치라 **속도가 두 배로 뻥튀기**된다. 실제 경과시간으로 나눈다.
- 그리고 `fwd`(고개 따라 안쪽 층이 도는 항)를 **곁에서 더 접었다**(0.45 → 0.82). 곁 구슬은 작아서 앞면을 조금만 밀어도 임계 등고선이 무너진다. 항을 하나씩 꺼 보고 범인을 특정했다.

### 검사 둘도 고쳤다

- **①이 오버레이 캔버스를 「입자가 남았다」로 오검출**했다. 위성 때문에 이 층이 항상 뜨게 되자 아홉 화면이 전부 FAIL. 이 검사가 묻는 건 「심볼이 색장인가」지 「캔버스가 몇 개인가」가 아니다.
- **⑩이 느린 환경에서 못 믿을 값을 낸다.** 같은 커밋이 60fps 0.001 / 10fps 0.09. 프레임을 같이 재서 **느리면 수치를 보여 주되 판정은 보류**한다. ⚠ 숨기는 게 아니다 — 화면에 값을 그대로 적어 다음 세션이 본다.

**검사** — 백틱 3/3 · 보드동기화 5/5 · 홀로 28/28(⑩ 보류) · smoke 35/35 · 곁 57/57 · 개인정보 139/139

## 2026-08-31 — 얼굴 홀로가 기본 · 위성 색 · 몰림 완화

**요청** — "곰팡이가 떠다니는 거 같아 색상이 이상해 안이뻐 / 얼굴 있는 버전 내부 평이 좋아 다른 버전은 폐기하고 나중에 언제든지 되살릴 수 있도록 저장해놔 / 눌렀을 때 가운데로 몰리는게 너무 심해 / 눈 사이 간격, 눈크기 등 이거 사주마다, 사람마다 다르게 보여?"

### 1. 기본을 바꿨다 — **지우지 않고**

`SKIN` 기본이 홀로, `FACE` 기본이 A 가 됐다. **입자 렌더러와 그 셰이더는 한 줄도 안 지웠다** — 지시의 핵심이 「되살릴 수 있게」이고, 그건 홀로가 WebGL 에 실패했을 때의 폴백이기도 하다.
되살리는 법: `?skin=dark`(까만 판) · `?face=off`(얼굴만 끔) · `?face=b|c|d`(다른 프리셋).
⚠ 기본을 바꾸면 **다른 검사가 전부 홀로를 보게 된다.** 전수로 돌려 확인했다 — smoke 35/35 · 곁 57/57 · 문서 22/22 · 개인정보 144/144 전부 통과. 대비 검사만 「옛 화면」 경로를 `?skin=dark` 로 고쳤다.

### 2. 위성이 곰팡이로 보인 것 — **내 판단이 틀렸다**

어제 「밝은 판에서 보이려면 짙어야 한다」고 색을 어둡게 뒤집었다. 보이기는 했는데 **밝은 판 위의 어두운 점은 빛이 아니라 얼룩으로 읽힌다.** 창업자 말이 맞다.

빛으로 읽히려면 **가운데가 희어야 한다** — 반딧불이가 그렇다. 두 겹으로 다시 그렸다: 바깥은 오행색 후광(채도만 올리고 명도는 안 낮춘다), 안은 흰 심. 색은 정체성만 맡고 「빛이다」는 흰 심이 말한다. 꼬리도 다섯 → 셋으로 줄였다(길게 끌면 그것도 얼룩이다).
그리고 궤도가 **오라 바깥 맨바탕**이라 외톨이 점으로 보였다 — 빛의 가장자리를 타도록 안으로 당겼다(2.05 → 1.58).
⚠ **지금은 반대로 옅다.** 어두우면 얼룩 / 밝으면 오라의 반짝임과 안 구분된다 사이에서 아직 답을 못 찾았다. 이 컨테이너는 10fps 라 실기 판단이 필요하다.

### 3. 몰림 완화

0.42/0.45 → **0.17/0.18**. 눌리는 순간 이목구비가 한 점으로 모여 얼굴이 뭉개졌다. 「눌렸다」는 남기고 「뭉개졌다」만 없앤다.

### 4. 물음 — 사주마다 다른가

**거의 안 다르다.** 지금 개인차는 **눈 간격 하나뿐**이고 그것도 **오행 다섯 종**이다(몸이 가로로 좁은 만큼 눈도 좁아지게 나눈 것 — 원래 개성이 아니라 **몸 밖으로 안 나가게 하는 보정**이었다). 눈 크기·입 크기·눈높이·홍조는 전원 같다. 눈 **모양**만 오늘 상태(십성)로 갈린다 — 그건 사람이 아니라 **날**마다 바뀐다.
→ 사람마다 다르게 하려면 축을 새로 정해야 한다(예: 일간으로 눈 크기, 오행 균형으로 간격). **창업자 결정 사항**으로 남긴다.

**검사** — 백틱 3/3 · 보드동기화 5/5 · 홀로 28/28 · smoke 35/35 · 곁 57/57 · 문서 22/22 · 대비 0건 · 개인정보 144/144

### ⚠ 덧 — 중단된 실험이 코드가 됐다

「어느 항이 오라를 찌그러뜨리나」를 찾으려고 `fwd`·`shade`·`vol` 을 하나씩 끄는 루프를 돌렸는데, **2분 타임아웃에 걸려 두 번째 반복 중간에 끊겼다.** 그 반복의 복구(`cp` 되돌리기)가 실행되지 않아 **`shade` 가 `1.0` 으로 죽은 채** 남았고, 그 위에 다음 작업이 쌓여 그대로 커밋됐다.

→ **끄는 실험은 복구까지 한 호흡에 묶는다.** 끊기면 그게 코드가 된다.
→ 발견 경로도 남긴다: 백틱 검사를 고치다 `grep shade` 를 했고 `float shade = 1.0;` 이 눈에 띄었다. **우연이었다** — 검사 중 어느 것도 이걸 못 잡는다. 셰이더 상수의 «있어야 할 항이 사라졌는지»를 보는 검사는 아직 없다.

### 백틱 검사가 좁아져 있었다

다른 세션이 이 파일을 덮어쓰며 **CSS 블록만 보는 판**이 됐다. 그런데 이번 세션 백틱 사고 여섯 건 중 **넷이 CSS 밖**이었다(셰이더 셋 · 보드 생성기 하나). 셋 다 보도록 복구했다 — ①CSS ②셰이더 상수 ③생성기가 **문법적으로 파싱되는가**.
복구하자마자 **바로 물었다** — 위 `shade` 복구 주석에 내가 또 백틱을 썼다.

## 2026-08-31 — 「눈이 크고 몰려서 징그럽다」 (팀 실기)

**제보** — 정민 대리 폰. 내 화면에서는 안 그랬다 — **오행이 다르면 다른 얼굴이 나오기 때문**이다.

### 원인 셋. 첫째가 내가 만든 것이다.

**① 간격만 나누고 눈 크기는 그대로 뒀다.**
몸이 가로로 좁은 오행에서 눈이 밖으로 나가길래 **간격을 오행 폭으로 나눴는데**, 눈 크기는 안 건드렸다. 그러면 좁은 몸에서는 **눈은 그대로인데 사이만 붙는다** — 뭉친다.
실측 간격/눈 비: 화 3.10 · 목 3.31 · 금 3.61 · 토 3.98 · 수 4.15 — **34% 차이**. 화·목인 사람만 몰려 보였다.
→ 비율은 사람마다 같아야 한다. 좁은 몸에서는 **얼굴 전체가 같이 작아진다**(눈·간격·입을 한 계수로). 비가 **전원 3.61** 로 고정됐다.

**② 두 눈이 딴판이었다.** 원근 배율 `0.55 + 0.45·z` 와 가로 눌림 바닥 `0.48` 이 겹쳐, 크게 돌면 한쪽은 점 한쪽은 덩어리가 됐다. 눈이 0.088 → 0.155 로 커진 뒤 특히 심해졌다 — 원근이 아니라 **결함**으로 읽힌다. `0.84 + 0.16·z` · 바닥 `0.72` 로 좁히고 고개 진폭도 줄였다(yaw 0.40 → 0.30).

**③ 호 모양 눈이 시커먼 애벌레가 됐다.** 획 두께가 **눈 크기에 비례**한다. 눈이 76% 커지자 획도 그만큼 굵어졌다. 두께는 크기를 덜 따라가게 했다(0.78 → 0.42).

⚠ **교훈** — 「보드에서 값 하나(눈 크기)를 고른다」가 실제로는 **획 두께·원근 비대칭·오행별 비율**을 동시에 움직였다. 크기를 바꿀 때 **같이 움직이는 것들의 목록**이 필요하다. 보드가 그걸 못 보여줬다 — 보드는 늘 금(기본 오행) 하나만 그린다.

**검사** — 백틱 4/4 · 보드동기화 5/5 · 홀로 28/28 · smoke 35/35 · 곁 57/57 · 개인정보 144/144

### 덧 — 병합 뒤 「기존 화면 불변」 검사가 울었다 (전제가 낡았다)

다른 세션이 그사이 **홀로를 기본으로** 바꿨다(`?skin=dark` 가 옛 화면). 그래서 인자 없이 열면
이제 홀로가 뜨고, *"홀로가 아니면 말이 안 바뀐다"* 를 무는 검사가 **정확하게** 울었다 —
**틀린 건 코드가 아니라 검사의 전제**였다. 지키려는 뜻(옛 화면은 한 줄도 안 바뀐다)은 그대로 두고
가는 길만 `?skin=dark` 로 고쳤다.

⚠ **그리고 이건 창업자에게 알릴 일이다** — 수호신이 말하는 자리가 이제 **기본 화면**이다.
`?skin=holo` 를 붙인 사람만 보는 게 아니라 **전 유저**가 본다.
## 2026-08-31 — 대사가 안 뜨고, 보내기 버튼이 숨던 것

**제보** — "수호신의 대사가 안떠. 그리고 판결 청하는 버튼이 아래에 숨어 있어서 질문하기 불편해"

### ① 대사 — **2.6초 뒤 스스로 지우고 있었다**

깨우면 한 마디 하고 `setTimeout(…, 2600)` 으로 지웠다. 2.6초는 **화면을 보기 전에 끝난다** — 써 놓고 아무도 못 읽는 대사였다.

v55 의 「수호신이 물러난 순수 질문입력 구간」은 **안 깼다.** 물러나는 신호를 **시계에서 유저의 차례로** 옮겼을 뿐이다 — 질문 칸에 손이 닿으면 그때 사라진다. 원래 뜻에 오히려 더 맞는다: 물러나는 건 「시간이 지나서」가 아니라 **「네가 말할 차례라서」**다.
실측 — 깨운 직후·4초·10초까지 남아 있고, 질문 칸을 누르면 사라진다.

### ② 보내기 버튼이 탭바 뒤로 숨었다

홀로 수호신이 `vh 0.70` 이라 **화면의 57%** 를 먹는다. 거기에 재방문 화면은 아침 문안 카드가 더 붙는다. 실측(393×760): 질문 칸 y633~737, **「판결을 청한다」 y751** — 화면(760) 밖이고 탭바(704) 뒤다.

수호신이 크면 좋지만 **질문을 못 하면 앱이 아니다.** `vh 0.52`, 짧은 화면(700 미만)은 `0.44`, 가로도 `1.50 → 1.34`(폭 좁은 기기에서는 세로가 아니라 가로가 먼저 걸린다).
실측 버튼 끝 — SE 570(탭 611) · 15 706(796) · Pixel 734(859) · 데스크탑 679(744). 전부 여유 있게 위.

### ③ 검사가 이걸 못 잡고 있었다 — **두 군데가 틀렸다**

- **깨우기 전 화면만 쟀다.** 첫 방문 화면에는 아침 문안·질문 칸이 없어 세로가 짧다. **실제로 쓰는 화면을 안 재고 있었다.** 재방문으로 만들고 깨운 뒤에 재게 했다.
- **버튼을 안 봤다.** 「본문이 탭에 가리는가」만 봤다. 본문은 멀쩡한데 버튼만 밖으로 나가면 12/12 로 통과한다 — 실제로 그랬다. 버튼을 직접 문다.
  ⚠ 단 패널 검사는 **깨우기 전**에 둔 채로 남겼다 — 깨우면 페이지가 스크롤되므로 「패널 끝이 탭 위」라는 기준 자체가 성립하지 않는다.
  옛 값(vh 0.70)으로 되돌려 무는 것까지 확인했다(SE 버튼 끝 743 vs 탭 611).

### ④ 홀로 ⑩-b 에도 느린 환경 가드

⑩ 은 가드가 있는데 ⑩-b 는 없어서, 프레임 62ms 에서 ⑩ 0.047(통과)인데 ⑩-b 0.154(실패)가 나왔다. **정착한 모양조차 못 재는 환경이면 움직이는 중은 더 못 잰다.** 같은 잣대를 쓰게 했다 — 값은 그대로 화면에 적는다.

**검사** — 백틱 4/4 · 보드동기화 5/5 · 홀로 27/27 · **레이아웃 16/16(신설 4)** · smoke 35/35 · 곁 57/57 · 개인정보 144/144

## 2026-08-31 · 홀로가 기본이 된 뒤 — 첫 방문자가 빈 화면을 보고 있었다

**증상.** WebGL 이 없거나 색장 셰이더가 실패하는 기기에서 **첫 방문자 화면이 통째로 빈다.**
실측: `root` 0자 · 버튼 0개 · `Cannot read properties of undefined (reading 'main')`.
배경이 `#0a0812` 단색이라 **고장이 아니라 로딩 중으로 보인다.** 재방문자는 산다(명식이 있다).
`?skin=dark` 는 두 조건 모두에서 멀쩡했다 — **보관해 둔 옛 판이 유일한 생존자였다.**

**원인.** 홀로가 기본이 되면서 온보딩 0·1단계가 수호신을 **명식 없이** 세운다(검은 판 자리는
`DustOrb` 라 이 경로가 없었다). 색장(`GuardianField`)은 그걸 견디게 짜여 있는데
(`const el = (saju && saju.main) || null`) **폴백 입자 렌더러 셋은 아니었다** — 진실이 두 곳에 있었다.
앱에 에러 경계가 0개라 React 가 트리를 통째로 언마운트한다.

**고친 것 셋.**
1. `NO_SAJU` 채움값을 세우고 폴백 렌더러 셋의 `saju` 기본값으로 물렸다.
   ⚠ 오행 이름을 한글 리터럴로 쓰지 않고 `Object.keys(EL_COLOR)[0]` 로 끌어온다 — NFD/NFC 가 어긋나면
   같은 자리에서 조용히 다시 죽는다.
2. `canHolo` — 못 그릴 기기에는 **애초에 홀로를 주지 않는다.** WebGL + 프래그먼트 highp 를 본다
   (색장 셰이더만 `precision highp float` 이고 대체 경로가 0건이다). 걸러 내면 CSS·JS 분기·얼굴 기본값이
   **한 점에서 동시에** 옛 판으로 넘어간다.
3. 색장에만 없던 `webglcontextlost` 처리를 형제 셋과 맞췄다 — 런타임에 GPU 를 잃으면 폴백이 안 걸렸다.

**검사.** `webgl-check` 에 ⓪ 신설 — **값이 아니라 성질을 묻는다**(「첫 방문자가 누를 것을 보는가」).
고치기 전 상태로 되돌려 **실제로 FAIL 이 나는 것까지 확인**하고 되돌렸다.

⚠ **검사는 이미 있었는데 검진이 안 불렀다.** `webgl-check` 은 여태 파일 내용을 읽는 데만 쓰였고
실행 목록에 없었다. `health-check.mjs` 5-p 주석이 그 함정을 **이미 이름 붙여 놨는데** 바로 그 아래에서
또 겪었다. 실행 목록에 넣었다.
⚠ 그리고 이 검사만 브라우저를 못 찾아 죽고 있었다 — `executablePath: undefined` 를 **키로 넘기면**
기본 해석이 안 된다. 후보를 차례로 시도하고 **전부 실패하면 조용히 통과하지 말고 그 사실을 말하고 죽는다.**

**남긴 것(별건).** `brood-check` 8/10 — 색장에 **벼름·낙착 일렁임·정독 저프레임이 아예 구현돼 있지 않다**
(3510~4106 구간에 `brood`/`agitate`/`restRef` 0건). prop 을 안 넘긴 게 아니라 기능이 없다.
셰이더 작업이라 색장을 쥐고 있는 세션 몫으로 넘긴다.

---

## 2026-09-04 · 마지막 그물 — 화면이 죽어도 유저에게 누를 것이 남는다

**8/31 사고는 원인만 고쳤고 구조는 그대로였다.** 그날 셋(`NO_SAJU`·`canHolo`·컨텍스트 상실)을
고쳤지만 **에러 경계는 여전히 0개**다. 즉 **다음에 어디서 렌더가 던지든 같은 빈 화면이 다시 나온다.**
사고 수습이 「이 원인」을 막았지 「이 결과」를 막지는 않았다.

**그리고 그 빈 화면은 계측에 안 보인다 — 이게 더 무겁다.**
`posthog.init` 이 App 안의 `useEffect` 에서 돌고 `capture_pageview` 도 꺼져 있다. 마운트가 실패하면
이벤트가 **0건**이다. 그래서 데이터에서 **「아무도 안 왔다」와 「스무 명이 왔는데 전부 죽었다」가
같은 그림**이다. 광고로 분모 100 을 채우는 동안 이 눈멂은 그대로 **예산을 태운다** —
실패가 신규 유입에만 걸리고, 그게 안 보인다.

**8/31 그 4시간에 실제 피해가 있었는지 실측했다(이중 필터).** 사고 창(KST 8/31 23:23 ~ 9/1 03:28)에
외부 방문자는 **1명**이고 그 사람의 궤적은 멀쩡했다(`guardian_wake` → `judge_requested` → `verdict_shown`).
⚠ **단 이건 「아무도 안 죽었다」의 증거가 못 된다** — 죽은 사람은 애초에 이벤트를 안 남긴다.
정확히 말할 수 있는 건 **「살아남은 사람 중 피해는 0」** 까지다. 이 한계가 바로 그물을 만든 이유다.

**만든 것.**
1. `BootNet`(App.jsx) — App **바깥**에 세우는 에러 경계. 안에 두면 App 이 던질 때 같이 죽는다.
   - 화면: 빈 화면 대신 「하늘이 잠깐 닫혔어 / 다시 열어볼래」. 기억은 안 지운다.
   - 계측: `app_crashed` — 죽었다는 **유일한 신호**. 어디서 죽었는지(`where`)도 싣는다.
   - ⚠ **거부권을 스스로 다시 읽는다.** 마운트가 죽으면 App 의 이펙트가 안 돌아서 `_optout` 이
     기본값(false)에 머문다. 안 읽고 보내면 **거부한 사람에게서도 나간다.** 검사 ⑤가 이걸 문다.
   - ⚠ 오류 문구는 **140자 상한**. 유저가 친 글이 예외 메시지에 섞여 나올 여지를 줄인다.
2. `?boom` — 그물 검사용 주소 스위치(`?trackdebug` 와 같은 계열). 그물은 **평소에 안 보이는 장치**라
   일부러 죽여 보지 않으면 살아 있는지 알 수 없다. 유저가 열어도 손해 없다 — 그물이 받는다.
3. `e2e/crash-net-check.mjs` 11칸 — **성질을 묻는다**(「죽어도 누를 것이 남는가」).
   `health-check` 5-p3 으로 **실행 목록에 넣었다.**

**고치기 전 상태로 되돌려 FAIL 나는 것까지 확인했다** — 그물을 떼면 `root` **0자**, 즉 사고 당일과
**같은 서명**이 나오고 11칸 중 8칸이 FAIL 한다.

**남는 구멍 하나(고의).** 번들 자체를 못 받거나 모듈 최상위에서 던지면 그물도 못 뜬다. 그건
`index.html` 에 React 이전 비콘을 심어야 잡히는데, **동의·거부를 읽기 전에 발사되는 계측**이라
이 리포의 정보 반출 헌장과 정면으로 부딪힌다. 값을 안 재고 넣지 않는다 — **미결로 남긴다.**

